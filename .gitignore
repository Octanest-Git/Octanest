node_modules/
target/
dist/
.turbo/
.env
.env.local
.env.dev-auth
*.log
.DS_Store
data/
var/
coverage/
test-results/
playwright-report/
.playwright-mcp/
.vitest/

# Competing JS package managers at repo root — Bun is the monorepo PM (issue #37)
/yarn.lock
/package-lock.json
/pnpm-lock.yaml
.yarn/
.pnp.*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
npm-debug.log*

# Bun.WebView / Chrome scratch if a path escapes var/
**/.bun-webview/
**/chrome-user-data/
.env.sqlite
dev-dist/

# Agent / local scratch — keep tmp/README.md tracked; ignore everything else under tmp/
tmp/*
!tmp/README.md

# Local agent / UAT artifacts (do not ship)
.cursor/gsd-core/
diff-commit-*.png
uat-*.png
login-snap.md

# Python bytecode (agent skill scripts, local tooling)
__pycache__/
*.py[cod]
