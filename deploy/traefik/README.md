# Traefik static extras (optional). Primary config is Compose labels + CLI flags.
# Place future middlewares / TLS here when promoting beyond local HTTP.

# Example (unused by default compose — kept for operators extending deploy/):
# http:
#   middlewares:
#     compress:
#       compress: {}
