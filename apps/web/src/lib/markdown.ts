/**
 * GFM Markdown → sanitized HTML (D-18).
 * RED stub: returns raw input so sanitize tests fail intentionally.
 */
export async function renderGfm(markdown: string): Promise<string> {
  return markdown;
}
