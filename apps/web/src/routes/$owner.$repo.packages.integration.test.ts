/**
 * Phase 20 Wave 0 — repo-linked packages view (D-PKG-11).
 * Greened when $owner.$repo.packages route ships for packages with repository_id.
 */
import { describe, expect, it } from "vitest";

describe("/$owner/$repo/packages Wave 0 stub", () => {
  it.fails("lists packages linked to the repository", () => {
    expect(false).toBe(true);
  });

  it.fails("hides packages without repository_id for this repo", () => {
    expect(false).toBe(true);
  });
});
