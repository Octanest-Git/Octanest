/**
 * Phase 19 Wave 0 — repo Actions list (ACT-03 / D-ACT-12).
 * Greened when /$owner/$repo/actions route ships in 19-09.
 */
import { describe, expect, it } from "vitest";

describe("/$owner/$repo/actions Wave 0 stub", () => {
  it.fails("lists workflow runs for the repository", () => {
    expect(false).toBe(true);
  });

  it.fails("hides Actions runs for private repos without Read (D-ACT-18)", () => {
    expect(false).toBe(true);
  });

  it.fails("RepoChrome exposes Actions tab + active actions", () => {
    expect(false).toBe(true);
  });
});
