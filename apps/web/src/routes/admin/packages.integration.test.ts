/**
 * Phase 20 Wave 0 — Admin packages quota/usage (D-PKG-09).
 * Greened when /admin/packages ships with format breakdown.
 */
import { describe, expect, it } from "vitest";

describe("/admin/packages Wave 0 stub", () => {
  it.fails("shows package storage usage broken down by format", () => {
    expect(false).toBe(true);
  });

  it.fails("exposes max blob size and per-owner quota settings", () => {
    expect(false).toBe(true);
  });
});
