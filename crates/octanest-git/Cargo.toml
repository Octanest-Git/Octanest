[package]
name = "octanest-git"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Git forge backend (CLI primary; gitoxide path deferred)"

[dependencies]
# Wave 0 scaffold — CliGitBackend + async trait land in later Phase 07 plans.

[dev-dependencies]
