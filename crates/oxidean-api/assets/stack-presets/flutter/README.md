# Flutter

Counter app (`flutter create` lib + test shape). Platform folders (`android/`, `ios/`, …)
are omitted to stay under the embed size budget — generate them locally:

```bash
flutter create . --project-name app
```

## Getting started

```bash
flutter pub get
flutter run
flutter test
```
