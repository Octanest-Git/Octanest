# Nix flake

Flake with `devShells.default` and a hello package (`nix flake init` style).

## Getting started

```bash
# enter the dev shell
nix develop
# or via direnv
direnv allow

# run the default package
nix run
```
