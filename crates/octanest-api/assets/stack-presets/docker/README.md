# Docker

Multi-stage Node Dockerfile + Compose service.

## Getting started

```bash
docker compose up --build
# or
docker build -t app .
docker run --rm -p 3000:3000 app
```

Open http://localhost:3000
