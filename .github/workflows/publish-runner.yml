# Publish the official octanest-runner image to GHCR.
#
# Triggers:
#   - push to main touching the runner crate or its Dockerfile (tag: main)
#   - tags v*.*.* (tag: semver)
#   - manual dispatch (tag: latest + sha)
#
# PRs that touch the runner get a compile-only build check instead — see
# docker-ci-images in ci.yml.

name: Publish runner image

on:
  push:
    branches: [main]
    paths:
      - crates/octanest-runner/**
      - docker/octanest-runner/**
      - Cargo.lock
      - .github/workflows/publish-runner.yml
    tags: ["v*.*.*"]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publish:
    name: ghcr.io/${{ github.repository_owner }}/octanest-runner
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: free disk
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc \
            /usr/local/share/boost /usr/share/swift || true
      - uses: docker/setup-buildx-action@v3
      - name: login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/octanest-runner
          tags: |
            type=ref,event=branch
            type=ref,event=tag
            type=sha,prefix=
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
      - name: build + push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/octanest-runner/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
