---
phase: 07-git-repos-browse
plan: "21"
type: execute
wave: 2
depends_on:
  - "19"
files_modified:
  - crates/oxidean-api/src/repo/mod.rs
  - crates/oxidean-api/tests/repo_create.rs
  - apps/web/src/lib/repo-browse.ts
  - apps/web/src/lib/repo-browse.unit.test.ts
  - apps/web/src/routes/$owner.$repo.tree.$.tsrx
  - apps/web/src/routes/$owner.$repo.blob.$.tsrx
  - apps/web/src/routes/$owner.$repo.blame.$.tsrx
requirements:
  - GIT-01
  - GIT-05
autonomous: true
gap_closure: true
gap_ids:
  - WR-01
  - WR-03
  - D-17
user_setup: []
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "When git init_bare or seed_commit fails after insert_repository, the DB row is soft-deleted (or otherwise not name-blocking) so the owner can recreate the same name (WR-01 / GIT-01)"
    - "parseRefAndPath with known refs resolves the longest matching hierarchical ref prefix before the path remainder (WR-03 / D-17 / GIT-05)"
  artifacts:
    - path: crates/oxidean-api/src/repo/mod.rs
      provides: "Compensating soft-delete on create git failure"
    - path: apps/web/src/lib/repo-browse.ts
      provides: "Longest-prefix ref parse for slashy branches"
    - path: apps/web/src/lib/repo-browse.unit.test.ts
      provides: "Unit coverage for hierarchical ref parse"
  key_links:
    - from: crates/oxidean-api/src/repo/mod.rs
      to: crates/oxidean-db/src/repositories.rs
      via: "soft_delete_repository after git failure"
    - from: apps/web/src/routes/$owner.$repo.tree.$.tsrx
      to: apps/web/src/lib/repo-browse.ts
      via: "parseRefAndPath(splat, knownRefs)"
---

<objective>
Close advisory WR-01 and WR-03: compensate failed create so names are not stuck, and parse hierarchical branch refs for tree/blob/blame URLs (D-17).

Purpose: Same gap pass as CR blockers; non-blocking but practical UX/correctness fixes.
Output: Create compensating soft-delete + recreate test; longest-prefix parseRefAndPath + unit tests + route wiring.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-VERIFICATION.md
@.planning/phases/07-git-repos-browse/07-REVIEW.md
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@crates/oxidean-api/src/repo/mod.rs
@crates/oxidean-api/tests/repo_create.rs
@apps/web/src/lib/repo-browse.ts
@apps/web/src/routes/$owner.$repo.tree.$.tsrx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated create → DB + disk | Partial failure must not leave exploitable orphan name locks beyond soft-delete semantics |
| Browser splat → ref/path split | Client-side parse only; server still validates treeish |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-GC21-01 | Denial of service | `repo.create` partial failure | medium | mitigate | Soft-delete inserted row (and remove partial bare dir when present) on init_bare/seed_commit failure so name can be reused (WR-01) |
| T-07-GC21-02 | Tampering | `parseRefAndPath` | low | accept | Client parse only; wrong split yields empty/failed browse, not privilege escalation; server validates refs |
| T-07-SC | Tampering | packages | low | accept | No new packages; vitest for unit file only |
</threat_model>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (07-21-T1): Tracer — create git failure soft-deletes row so name is reusable</name>
  <files>crates/oxidean-api/src/repo/mod.rs, crates/oxidean-api/tests/repo_create.rs</files>
  <read_first>crates/oxidean-api/src/repo/mod.rs, crates/oxidean-api/tests/repo_create.rs, crates/oxidean-db/src/lib.rs, .planning/phases/07-git-repos-browse/07-REVIEW.md</read_first>
  <behavior>
    - RED first: add a create-failure test (name e.g. `repo_create_git_failure_soft_deletes_row_allows_recreate`) that forces init_bare or seed_commit to fail after insert (preferred: pre-create a blocking non-directory file or unwritable parent at the destination bare path under the test repos_dir), asserts create returns `repo.git_init_failed` or `repo.git_seed_failed`, then asserts a subsequent `repo.create` with the same name succeeds (or find_repository_by_owner_name sees no live row) per WR-01 / GIT-01
    - GREEN: on init_bare or seed_commit Err after insert_repository, call `soft_delete_repository` for the new id, best-effort remove the partial bare directory under repos_dir, then return the existing git_*_failed AppError (still log the original git error)
  </behavior>
  <action>
Per WR-01: TDD compensating soft-delete on create git failure. Use existing `Database::soft_delete_repository`. Do not hard-delete historically soft-deleted repos outside this compensate path. Keep duplicate-name and happy-path create tests green. Avoid changing ACL or visibility rules. Reversibility: reversible — compensate path only.
  </action>
  <reversibility rating="reversible">Soft-delete compensate on failure; no schema change.</reversibility>
  <verify>
    <automated>cargo test -p oxidean-api --test repo_create repo_create_git_failure_soft_deletes_row_allows_recreate -- --exact</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"; or the named test is missing from the harness output</fails_when>
  </verify>
  <acceptance_criteria>
    - Named test exists in `crates/oxidean-api/tests/repo_create.rs`
    - `rg -n 'soft_delete_repository' crates/oxidean-api/src/repo/mod.rs` appears on the create failure paths after insert
    - cargo exact test exits 0
  </acceptance_criteria>
  <done>Failed create no longer permanently occupies the repository name</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (07-21-T2): Longest-prefix parseRefAndPath for hierarchical refs (WR-03 / D-17)</name>
  <files>apps/web/src/lib/repo-browse.ts, apps/web/src/lib/repo-browse.unit.test.ts, apps/web/src/routes/$owner.$repo.tree.$.tsrx, apps/web/src/routes/$owner.$repo.blob.$.tsrx, apps/web/src/routes/$owner.$repo.blame.$.tsrx</files>
  <read_first>apps/web/src/lib/repo-browse.ts, apps/web/src/routes/$owner.$repo.tree.$.tsrx, apps/web/src/routes/$owner.$repo.blob.$.tsrx, apps/web/src/lib/markdown.test.ts</read_first>
  <behavior>
    - Given known refs including `feature/foo`, splat `feature/foo/src/a.ts` yields ref `feature/foo` and path `src/a.ts`
    - Without known refs (or no match), behavior remains first-segment fallback for backward compatibility
    - Empty splat still yields empty ref and path
  </behavior>
  <action>
Per WR-03 / D-17 / GIT-05: extend `parseRefAndPath` to accept an optional list of known ref names (short branch/tag forms via existing `shortRefName` where callers have `RepoRefEntry[]`). Resolve longest matching prefix of splat segments against that list, then treat the remainder as path. Add `apps/web/src/lib/repo-browse.unit.test.ts` covering hierarchical and fallback cases. Wire tree/blob/blame loaders or page bodies that already fetch `repo.refs` to pass short ref names into `parseRefAndPath` when refs are available (head metadata may keep first-segment-only if refs are not loaded yet — document that in SUMMARY). Do not change server validators here.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/lib/repo-browse.unit.test.ts && rg -n 'parseRefAndPath\([^)]*knownRefs' 'apps/web/src/routes/$owner.$repo.tree.$.tsrx' 'apps/web/src/routes/$owner.$repo.blob.$.tsrx' 'apps/web/src/routes/$owner.$repo.blame.$.tsrx'</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAIL"; or the test file is not found; or any of the three route files lacks a parseRefAndPath call that passes knownRefs</fails_when>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/repo-browse.unit.test.ts` exists and asserts longest-prefix hierarchical split
    - `rg -n 'parseRefAndPath' apps/web/src/lib/repo-browse.ts` shows optional known-refs parameter
    - `rg -n 'parseRefAndPath\([^)]*knownRefs' apps/web/src/routes/$owner.$repo.tree.$.tsrx apps/web/src/routes/$owner.$repo.blob.$.tsrx apps/web/src/routes/$owner.$repo.blame.$.tsrx` matches in all three route files (WR-03 / D-17 key_link)
    - vitest command exits 0
  </acceptance_criteria>
  <done>Hierarchical branch URLs parse correctly when known refs are supplied (D-17)</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| Create compensate soft-delete | 07-21 | WR-01 / GIT-01 |
| `parseRefAndPath` longest prefix | 07-21 | WR-03 / D-17 / GIT-05 |
| `repo-browse.unit.test.ts` | 07-21 | New unit file |

<verification>
cargo create compensate test green; vitest repo-browse unit green.
</verification>

<success_criteria>
WR-01 and WR-03 closed; owners can recreate after git failure; slashy refs parse with known refs list.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-21-SUMMARY.md` when done
</output>
