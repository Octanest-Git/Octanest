---
phase: 07-git-repos-browse
plan: "10"
type: execute
wave: 14
depends_on:
  - "12"
  - "09"
files_modified:
  - crates/oxidean-api/src/auth/admin.rs
  - crates/oxidean-db/src/lib.rs
  - crates/oxidean-api/src/jobs
  - crates/oxidean-api/src/main.rs
  - crates/oxidean-api/src/rpc.rs
  - apps/web/src/routes/admin/auth.tsrx
  - apps/web/src/components/ui/radio-group.tsx
  - docs/CONFIGURATION.md
  - packages/api-client
requirements:
  - GIT-08
  - GIT-09
autonomous: true
user_setup: []
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Factory reset modal offers Database only vs Database and repositories radios; default Database only; RESET phrase required (D-34)"
    - "Periodic orphan reconcile removes disk dirs without DB rows (and/or DB soft-deleted past retention) on configured frequency (D-36)"
    - "Scheduled git gc plus sys-admin manual GC trigger exist (D-37)"
    - "CONFIGURATION documents OXIDEAN_REPOS_DIR, git ≥2.5, Compose UID/GID ownership (D-31, D-33, D-38)"
    - "UI E10 factory reset empty/loading/error/partial states honored"
  artifacts:
    - path: apps/web/src/routes/admin/auth.tsrx
      provides: "Reset scope modal"
    - path: crates/oxidean-api/src/jobs
      provides: "orphan reconcile + gc scheduler"
  key_links:
    - from: crates/oxidean-api/src/auth/admin.rs
      to: crates/oxidean-db/src/lib.rs
      via: "factory_reset scope"
    - from: crates/oxidean-api/src/jobs
      to: crates/oxidean-git/src/cli.rs
      via: "gc()"
  assumptions:
    - "ASSUME (A3 RESEARCH): in-process interval jobs acceptable for Phase 7"
    - "ASSUME (A4): soft-delete retention default 14 days"
  prohibitions:
    - statement: "Must not wipe repository files when Database only scope is selected"
      flagged: true
---

<objective>
Ops lifecycle: factory-reset scope radios, orphan reconcile, scheduled/manual gc, and operator docs (D-34–D-38).

Purpose: Disk/DB consistency for volume-backed repos.
Output: Admin modal; background jobs; CONFIGURATION updates.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-UI-SPEC.md
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@.planning/phases/07-git-repos-browse/07-09-SUMMARY.md
@crates/oxidean-api/src/auth/admin.rs
@apps/web/src/routes/admin/auth.tsrx
@docs/CONFIGURATION.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sys-admin → factory reset | Destructive; confirm phrase + scope |
| Background job → filesystem | Must not delete outside repos_dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-25 | Elevation of privilege | factory_reset | high | mitigate | Existing admin gate + RESET phrase; scope enum validated server-side (D-34) |
| T-07-26 | Tampering | orphan purge paths | high | mitigate | Canonicalize under OXIDEAN_REPOS_DIR; refuse escapes |
| T-07-27 | Denial of service | gc storm | low | accept | Manual + modest schedule; document frequency knob |
| T-07-SC | Tampering | packages | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (07-10-T1): Factory reset scope API + admin modal</name>
  <files>crates/oxidean-api/src/auth/admin.rs, crates/oxidean-db/src/lib.rs, crates/oxidean-api/src/rpc.rs, apps/web/src/routes/admin/auth.tsrx, apps/web/src/components/ui/radio-group.tsx, packages/api-client</files>
  <read_first>crates/oxidean-api/src/auth/admin.rs, crates/oxidean-db/src/lib.rs, apps/web/src/routes/admin/auth.tsrx, .planning/phases/07-git-repos-browse/07-UI-SPEC.md</read_first>
  <action>
Extend `admin.instance.factory_reset` input with scope enum: `database_only` (default) vs `database_and_repositories` (D-34). DB-only keeps files; both deletes under repos_dir after DB wipe. Keep RESET confirm phrase. Update danger-zone Dialog: title Reset this instance; radios with UI-SPEC copy; Resetting… → `/setup`. rpc-gen. Add/adjust admin tests.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(factory_reset) | test(admin)' && bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"; or build fails on admin auth modal</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'database_only|database_and_repositories|Reset this instance' crates/oxidean-api/src/auth/admin.rs apps/web/src/routes/admin/auth.tsrx`
  </acceptance_criteria>
  <done>Scoped factory reset works end-to-end</done>
</task>

<task type="auto">
  <name>Task 2 (07-10-T2): Orphan reconcile + scheduled/manual gc</name>
  <files>crates/oxidean-api/src/jobs, crates/oxidean-api/src/main.rs, crates/oxidean-api/src/rpc.rs, crates/oxidean-git/src/cli.rs, packages/api-client</files>
  <read_first>crates/oxidean-git/src/backend.rs, crates/oxidean-api/src/main.rs, .planning/phases/07-git-repos-browse/07-RESEARCH.md</read_first>
  <action>
Add in-process jobs (D-36, D-37): (1) orphan reconcile — remove bare dirs under repos_dir with no matching non-deleted DB row and/or purge soft-deleted past retention (default 14d); frequency from instance setting with sane default; (2) scheduled `git gc` via GitBackend::gc; (3) sys-admin RPC to trigger manual GC for a repo or all. Path safety under repos_dir. Start timers from main after boot.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(orphan) | test(repo_gc) | test(gc)' && cargo nextest run -p oxidean-git -E 'test(gc)'</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"; or no job module/files exist when filters find zero tests — ensure at least one automated test file covers reconcile or gc</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'orphan|reconcile|gc' crates/oxidean-api/src/jobs crates/oxidean-api/src/main.rs`
    - `rg -n 'fn gc' crates/oxidean-git/src/backend.rs`
  </acceptance_criteria>
  <done>Reconcile + gc automation present</done>
</task>

<task type="auto">
  <name>Task 3 (07-10-T3): CONFIGURATION operator docs for repos + git</name>
  <files>docs/CONFIGURATION.md</files>
  <read_first>docs/CONFIGURATION.md, .env.example, .planning/phases/07-git-repos-browse/07-CONTEXT.md</read_first>
  <action>
Document `OXIDEAN_REPOS_DIR` (default var/repos), Compose bind `./var/repos:/var/repos`, git ≥2.5 boot requirement, API image installs git, repos owned by API process user / Compose UID/GID notes (D-31, D-33, D-38), cleanup frequency + retention knobs for orphan/gc. Do not rewrite unrelated sections.
  </action>
  <verify>
    <automated>rg -n 'OXIDEAN_REPOS_DIR|git.*2\.5|var/repos|UID|GID|orphan|gc' docs/CONFIGURATION.md</automated>
    <fails_when>OXIDEAN_REPOS_DIR or git 2.5 floor missing from CONFIGURATION.md</fails_when>
  </verify>
  <acceptance_criteria>
    - CONFIGURATION mentions repos dir, git version floor, and volume ownership
  </acceptance_criteria>
  <done>Operators have repos/git runbooks in CONFIGURATION</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| Factory reset scope | 07-10 | D-34 |
| Orphan + gc jobs | 07-10 | D-36/D-37 |
| CONFIGURATION repos/git | 07-10 | D-31/33/38 |

<verification>
admin/factory_reset + job tests + CONFIGURATION rg green.
</verification>

<success_criteria>
Ops controls for reset/reconcile/gc documented and implemented.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-10-SUMMARY.md` when done.
</output>
