---
phase: 07-git-repos-browse
plan: "18"
type: execute
wave: 12
depends_on:
  - "07"
files_modified:
  - apps/web/src/routes/$owner.$repo.branches.tsrx
  - apps/web/src/routes/$owner.$repo.tags.tsrx
  - apps/web/src/components/repo
  - apps/web/src/components/ui/dialog.tsx
  - apps/web/src/components/ui/alert-dialog.tsx
requirements:
  - GIT-06
  - GIT-05
autonomous: true
user_setup: []
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Owner can create/rename/delete branches from UI; non-owner read-only (D-27, GIT-06)"
    - "Default branch rename/delete controls disabled with soft-protect copy (D-28)"
    - "Tags list + archive download actions; no create-tag UI (UI-SPEC lock)"
    - "UI E6 empty/loading/error/populated/partial states honored"
  artifacts:
    - path: apps/web/src/routes/$owner.$repo.branches.tsrx
      provides: "Branch management UI"
    - path: apps/web/src/routes/$owner.$repo.tags.tsrx
      provides: "Tags list UI"
  key_links:
    - from: apps/web/src/routes/$owner.$repo.branches.tsrx
      to: crates/oxidean-api/src/rpc.rs
      via: "repo.branch_*"
  assumptions: []
  prohibitions:
    - statement: "Must not allow non-owners to mutate branches from UI"
      flagged: true
    - statement: "Must not ship create-tag primary CTA in Phase 7"
      flagged: true
---

<objective>
Branches and Tags Octane UI + confirm dialogs (peeled from 07-07 for file budget).

Purpose: Present GIT-06 soft-protect and tags list half of GIT-05 without bloating the API plan.
Output: Branches/Tags pages with shadcn Dialog/AlertDialog.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-UI-SPEC.md
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@.planning/phases/07-git-repos-browse/07-07-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → branch mutate RPC | UI hides actions; server still enforces owner |
| Dialog confirm → destructive delete | Typing branch name reduces accidents |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-19 | Elevation of privilege | branch UI | medium | mitigate | Hide mutate for non-owners; API still owner-checks (D-27) |
| T-07-SC | Tampering | shadcn dialog | medium | mitigate | Official shadcn Base UI only — no third-party registries |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (07-18-T1): Branches / Tags UI + delete confirm dialog</name>
  <files>apps/web/src/routes/$owner.$repo.branches.tsrx, apps/web/src/routes/$owner.$repo.tags.tsrx, apps/web/src/components/repo, apps/web/src/components/ui/dialog.tsx, apps/web/src/components/ui/alert-dialog.tsx</files>
  <read_first>.planning/phases/07-git-repos-browse/07-UI-SPEC.md, apps/web/components.json, .planning/phases/07-git-repos-browse/07-07-SUMMARY.md</read_first>
  <precondition>07-07 branch_* RPC and soft-protect tests are green</precondition>
  <action>
Add Branches page: list name/tip/updated; owner New branch, rename, delete; default soft-protect disabled controls with title "Default branch can’t be renamed or deleted here."; AlertDialog confirm typing branch name; destructive Delete branch. Tags page: list + archive download actions only — no create tag UI. Add official shadcn Dialog/AlertDialog if missing. Link-row nav. Titles per UI-SPEC. Honor UI E6 empty/loading/error/populated/partial states.
  </action>
  <verify>
    <automated>bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'New branch|Default branch can.t be renamed|Delete branch' apps/web/src/routes/`
    - `test -f apps/web/src/routes/\$owner.\$repo.branches.tsrx`
    - Tags route has no create-tag primary CTA
  </acceptance_criteria>
  <done>GIT-06 UI complete; tags list shipped</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| Branches/Tags pages | 07-18 | D-27/D-28 |

<verification>
web build green.
</verification>

<success_criteria>
Owner branch CRUD UI with soft-protect; tags list only.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-18-SUMMARY.md` when done
</output>
