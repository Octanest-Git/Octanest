---
phase: 07-git-repos-browse
plan: "07"
type: execute
wave: 11
depends_on:
  - "05"
  - "06"
files_modified:
  - crates/oxidean-git/src/backend.rs
  - crates/oxidean-git/src/cli.rs
  - crates/oxidean-api/src/repo/mod.rs
  - crates/oxidean-api/src/rpc.rs
  - crates/oxidean-api/tests/repo_branch_soft_protect.rs
  - packages/api-client
requirements:
  - GIT-06
  - GIT-05
autonomous: true
user_setup: []
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Owner can create/rename/delete branches via API; non-owner mutate blocked (D-27, GIT-06)"
    - "Default branch rename/delete rejected with soft-protect error (D-28)"
    - "list_refs exposes branches and tags for UI consumers"
  artifacts:
    - path: crates/oxidean-api/tests/repo_branch_soft_protect.rs
      provides: "Soft-protect + owner ACL tests"
  key_links:
    - from: crates/oxidean-api/src/rpc.rs
      to: crates/oxidean-git/src/cli.rs
      via: "repo.branch_*"
  assumptions:
    - "ASSUME (GIT-06 idempotency/concurrency): duplicate create and concurrent mutate return stable errors without corrupting refs"
  prohibitions:
    - statement: "Must not allow non-owners to mutate branches"
      flagged: true
    - statement: "Must not allow delete or rename of the repository default branch from Phase 7 API"
      flagged: true
---

<objective>
Branch CRUD API (owner-only, soft-protect default) for GIT-06; Branches/Tags UI lives in 07-18.

Purpose: Keep API file budget under warning threshold; UI expansion follows.
Output: Green soft-protect tests; branch_* + list_refs RPC.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@.planning/phases/07-git-repos-browse/07-RESEARCH.md
@.planning/phases/07-git-repos-browse/07-05-SUMMARY.md
@.planning/phases/07-git-repos-browse/07-06-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner session → branch mutate | Non-owners must not mutate |
| Branch name → git argv | Injection via ref names |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-19 | Elevation of privilege | branch mutate | high | mitigate | Server checks caller is owner (D-27) |
| T-07-20 | Tampering | default branch | medium | mitigate | API rejects rename/delete of default branch even if UI bypassed (D-28) |
| T-07-17 | Tampering | branch names | high | mitigate | validate ref names; argv arrays only |
| T-07-SC | Tampering | packages | low | accept | No new packages; rpc-gen only |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (07-07-T1): branch create/rename/delete + soft-protect API</name>
  <files>crates/oxidean-git/src/backend.rs, crates/oxidean-git/src/cli.rs, crates/oxidean-api/src/repo/mod.rs, crates/oxidean-api/src/rpc.rs, crates/oxidean-api/tests/repo_branch_soft_protect.rs, packages/api-client</files>
  <read_first>crates/oxidean-git/src/backend.rs, crates/oxidean-api/src/repo/acl.rs, .planning/phases/07-git-repos-browse/07-RESEARCH.md</read_first>
  <behavior>
    - owner create branch from start ref succeeds
    - non-owner mutate → not_found or auth error without leaking
    - rename/delete default branch → stable soft-protect error
  </behavior>
  <action>
Implement branch_create, branch_rename, branch_delete on CliGitBackend. RPC requires owner (D-27). Block rename/delete when name equals repo default_branch (D-28). List branches/tags via list_refs. Green repo_branch_soft_protect. rpc-gen. Do not add Branches/Tags Octane routes or dialogs here (07-18).
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(repo_branch)'</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'branch_create|branch_rename|branch_delete|default_branch' crates/oxidean-api/src/repo/ crates/oxidean-git/src/`
    - nextest repo_branch filter exits 0
  </acceptance_criteria>
  <done>Branch mutate API enforces owner + soft-protect</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| branch_* GitBackend + RPC | 07-07 | GIT-06 |
| Branches/Tags pages | 07-18 | D-27/D-28 UI |

<verification>
repo_branch nextest green.
</verification>

<success_criteria>
Owner branch CRUD API with soft-protect; UI deferred to 07-18.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-07-SUMMARY.md` when done
</output>
