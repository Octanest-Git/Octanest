---
phase: 07-git-repos-browse
plan: "19"
type: tdd
wave: 1
depends_on:
  - "07"
files_modified:
  - crates/oxidean-git/src/cli.rs
  - crates/oxidean-api/src/repo/mod.rs
  - crates/oxidean-api/tests/repo_branch_soft_protect.rs
requirements:
  - GIT-06
autonomous: true
gap_closure: true
gap_ids:
  - CR-02
  - D-28
user_setup: []
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Owner calling repo.branchCreate with branch equal to -D fails with an invalid-ref style error and the repository default branch still exists (CR-02 / D-28 / GIT-06)"
    - "branch_create, branch_rename, and branch_delete pass an end-of-options marker before user-controlled name/start/from/to arguments"
    - "validate_treeish rejects refs whose trimmed form begins with a hyphen"
  artifacts:
    - path: crates/oxidean-git/src/cli.rs
      provides: "Leading-hyphen reject in validate_treeish; -- before branch argv operands"
    - path: crates/oxidean-api/tests/repo_branch_soft_protect.rs
      provides: "Injection regression for branchCreate(-D)"
  key_links:
    - from: crates/oxidean-api/src/repo/mod.rs
      to: crates/oxidean-git/src/cli.rs
      via: "repo.branchCreate → branch_create"
  prohibitions:
    - statement: "Must not allow delete or rename of the repository default branch from Phase 7 API"
      flagged: true
---

<objective>
Close CR-02: stop option-like branch names from becoming git CLI switches so soft-protect (D-28) cannot be bypassed via repo.branchCreate.

Purpose: Restore the GIT-06 / D-28 invariant that default-branch delete only goes through soft-protected branchDelete.
Output: Hardened validate_treeish + branch argv; green injection regression in repo_branch_soft_protect.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-VERIFICATION.md
@.planning/phases/07-git-repos-browse/07-REVIEW.md
@.planning/phases/07-git-repos-browse/07-07-PLAN.md
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@crates/oxidean-git/src/cli.rs
@crates/oxidean-api/src/repo/mod.rs
@crates/oxidean-api/tests/repo_branch_soft_protect.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner session → branch mutate RPC | Authenticated owner input becomes git argv |
| Branch/start string → `git branch` argv | Option-like tokens must not be interpreted as switches |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-GC19-01 | Tampering | `branch_create` / `validate_treeish` | high | mitigate | Reject trimmed refs that begin with `-`; pass end-of-options before name and start (CR-02) |
| T-07-GC19-02 | Elevation of privilege | `repo.branchCreate` | high | mitigate | Reject reserved option tokens (-d/-D/-m/-M/-f and equivalents) in API validators before CLI (D-28) |
| T-07-GC19-03 | Tampering | `branch_rename` / `branch_delete` | high | mitigate | Same end-of-options before from/to/name so `-m`/`-D` operands cannot slide into option position |
| T-07-SC | Tampering | packages | low | accept | No new crates; test-only changes |
</threat_model>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (07-19-T1): Tracer — branchCreate(-D) rejected, default branch intact</name>
  <files>crates/oxidean-api/tests/repo_branch_soft_protect.rs, crates/oxidean-git/src/cli.rs, crates/oxidean-api/src/repo/mod.rs</files>
  <read_first>crates/oxidean-api/tests/repo_branch_soft_protect.rs, crates/oxidean-git/src/cli.rs, crates/oxidean-api/src/repo/mod.rs, .planning/phases/07-git-repos-browse/07-REVIEW.md</read_first>
  <behavior>
    - RED first: add `repo_branch_create_rejects_option_like_name_leaves_default_intact` that seeds an owner public repo, calls `repo.branchCreate` with branch `-D` and start equal to the default branch (or omit start so default is used), asserts `ok` is false and error code is an invalid-ref / invalid-arg style code (not success), then asserts `repo.branchDelete` of the default still returns `repo.default_branch_protected` (proves default still exists and soft-protect path still applies) per D-28 / CR-02
    - GREEN: extend `validate_treeish` so trimmed empty/`..`/NUL/metachar checks also reject when the trimmed value begins with `-`; in `branch_create`, after validation, invoke `git branch` with an end-of-options marker before name and start; in `branch_create` RPC, reject reserved option tokens (`-d`, `-D`, `-m`, `-M`, `-f`, and case variants already covered by leading `-`) with `repo.invalid_ref` before calling git per D-28
  </behavior>
  <action>
Per CR-02 / GIT-06 / D-28: write the failing integration test first (TDD), then harden `validate_treeish` and `branch_create` argv plus API reserved-token checks so the tracer path fails closed. Do not change soft-protect rename/delete happy-path assertions. Do not modify archive argv here (07-20). Reversibility: reversible — validation and argv order only.
  </action>
  <reversibility rating="reversible">Validation and argv delimiter; no schema or data migration.</reversibility>
  <verify>
    <automated>cargo test -p oxidean-api --test repo_branch_soft_protect repo_branch_create_rejects_option_like_name_leaves_default_intact -- --exact</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"; or the named test is missing from the harness output</fails_when>
  </verify>
  <acceptance_criteria>
    - Named test exists in `crates/oxidean-api/tests/repo_branch_soft_protect.rs`
    - `rg -n "starts_with\\('-'\\)" crates/oxidean-git/src/cli.rs` matches inside `validate_treeish`
    - `rg -n 'branch_create' -A15 crates/oxidean-git/src/cli.rs` shows an end-of-options `--` string before name/start operands
    - cargo exact test exits 0
  </acceptance_criteria>
  <done>branchCreate(-D) cannot force-delete the default branch; soft-protect still observes the default ref</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (07-19-T2): Expand — rename/delete argv end-of-options + soft-protect suite green</name>
  <files>crates/oxidean-git/src/cli.rs, crates/oxidean-api/tests/repo_branch_soft_protect.rs</files>
  <read_first>crates/oxidean-git/src/cli.rs, crates/oxidean-api/tests/repo_branch_soft_protect.rs</read_first>
  <behavior>
    - `branch_rename` runs `git branch -m` then end-of-options then from and to
    - `branch_delete` runs `git branch -D` then end-of-options then name
    - Existing soft-protect and non-default CRUD tests still pass
  </behavior>
  <action>
Per CR-02: after Task 1, insert the same end-of-options marker before user-controlled operands in `branch_rename` and `branch_delete` so operands cannot be parsed as switches. Keep intentional `-m` / `-D` as known flags before the marker. Re-run the full soft-protect test binary. Do not touch `archive` or HTTP validators.
  </action>
  <verify>
    <automated>cargo test -p oxidean-api --test repo_branch_soft_protect</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'branch_rename|branch_delete' -A12 crates/oxidean-git/src/cli.rs` shows `--` before from/to and before delete name
    - Full `repo_branch_soft_protect` binary exits 0 including prior soft-protect and CRUD cases
  </acceptance_criteria>
  <done>All branch mutate argv paths use end-of-options; soft-protect suite green with injection case</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `validate_treeish` leading-`-` reject | 07-19 | Shared foundation for CR-01 too |
| `branch_*` end-of-options argv | 07-19 | CR-02 |
| `repo_branch_create_rejects_option_like_name_leaves_default_intact` | 07-19 | GIT-06 / D-28 regression |
| Archive `--` + HTTP validators | 07-20 | CR-01 |
| Advisory WR-01–WR-03 | 07-21 | Non-blocking |

<verification>
cargo test -p oxidean-api --test repo_branch_soft_protect exits 0; injection case proves default intact.
</verification>

<success_criteria>
CR-02 closed: option-like branchCreate cannot delete the soft-protected default branch.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-19-SUMMARY.md` when done
</output>
