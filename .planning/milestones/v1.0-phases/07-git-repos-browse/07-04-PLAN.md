---
phase: 07-git-repos-browse
plan: "04"
type: execute
wave: 6
depends_on:
  - "13"
  - "03"
  - "16"
files_modified:
  - apps/web/src/components/signed-in-home.tsrx
  - apps/web/src/components/signed-in-home.integration.test.ts
  - apps/web/src/routes/index.tsrx
  - apps/web/src/routes/settings/profile.tsrx
  - crates/oxidean-api/src/repo/mod.rs
  - crates/oxidean-api/src/auth/profile.rs
  - crates/oxidean-api/src/auth/admin.rs
  - crates/oxidean-api/src/rpc.rs
  - packages/api-client
requirements:
  - GIT-01
autonomous: true
user_setup: []
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Signed-in / is a dashboard with repo list recently updated first, visibility Badge, empty hero, activity placeholder only (D-13)"
    - "Document title Repositories · Oxidean for signed-in home (UI-SPEC)"
    - "Account settings can save default branch name for new repos (D-09)"
    - "Sys-admin can configure instance default visibility (D-08)"
    - "Home list empty/loading/error/populated states match UI Considerations E1"
  artifacts:
    - path: apps/web/src/components/signed-in-home.tsrx
      provides: "Dashboard home IA"
    - path: crates/oxidean-api/src/repo/mod.rs
      provides: "repo.list_mine or equivalent"
  key_links:
    - from: apps/web/src/components/signed-in-home.tsrx
      to: crates/oxidean-api/src/rpc.rs
      via: "repo list Query"
  assumptions:
    - "ASSUME (UI E1): activity panel is placeholder copy only — no event feed"
  prohibitions:
    - statement: "Must not ship a populated fake activity feed on home"
      flagged: true
---

<objective>
Replace signed-in home stub with forge dashboard IA and wire account/instance defaults for branch name and visibility (D-08, D-09, D-13).

Purpose: GIT-01 discoverability + create defaults after tracer create works.
Output: Repo list home; default branch + default visibility settings RPCs/UI.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-UI-SPEC.md
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@.planning/phases/07-git-repos-browse/07-13-SUMMARY.md
@.planning/phases/07-git-repos-browse/07-03-SUMMARY.md
@.planning/phases/07-git-repos-browse/07-02-SUMMARY.md
@.planning/phases/07-git-repos-browse/07-03-SUMMARY.md
@apps/web/src/components/signed-in-home.tsrx
@apps/web/src/routes/settings/profile.tsrx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session → repo list | Only owner’s non-deleted repos |
| Sys-admin → default_visibility | Admin-only mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-11 | Information disclosure | repo.list | medium | mitigate | Return only caller-owned repos; never others’ private metadata |
| T-07-12 | Elevation of privilege | instance default_visibility | high | mitigate | Reuse admin gate patterns from existing admin.auth settings |
| T-07-SC | Tampering | npm installs | high | mitigate | No new packages in this plan |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (07-04-T1): repo.list_mine + SignedInHome dashboard</name>
  <reversibility rating="costly">Signed-in home IA contract (D-13).</reversibility>
  <files>crates/oxidean-api/src/repo/mod.rs, crates/oxidean-api/src/rpc.rs, apps/web/src/components/signed-in-home.tsrx, apps/web/src/components/signed-in-home.integration.test.ts, apps/web/src/routes/index.tsrx, packages/api-client</files>
  <read_first>apps/web/src/components/signed-in-home.tsrx, .planning/phases/07-git-repos-browse/07-UI-SPEC.md, crates/oxidean-api/src/rpc.rs</read_first>
  <behavior>
    - list returns caller repos sorted by updated_at desc
    - empty list → empty hero copy Create your first repository
    - unverified New repository disabled with verify hint
  </behavior>
  <action>
Add `repo.list_mine` (name may vary but stable) returning owner/name, visibility, updated_at, description. Soft-deleted excluded. Implement SignedInHome per UI-SPEC: title Your repositories, list rows with Badge, empty hero, activity placeholder "Activity will show up here.", skeletons, keep incomplete-profile banner. Title `Repositories · Oxidean`. `make rpc-gen`. Green signed-in-home.integration tests (D-13, UI E1).
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/components/signed-in-home.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Your repositories|Activity will show up here|Create your first repository' apps/web/src/components/signed-in-home.tsrx`
    - `rg -n 'list_mine|repo\.list' crates/oxidean-api/src/rpc.rs crates/oxidean-api/src/repo/`
  </acceptance_criteria>
  <done>Dashboard home lists repos with empty/activity states</done>
</task>

<task type="auto">
  <name>Task 2 (07-04-T2): Account default branch + instance default visibility</name>
  <files>apps/web/src/routes/settings/profile.tsrx, crates/oxidean-api/src/auth/profile.rs, crates/oxidean-api/src/auth/admin.rs, crates/oxidean-api/src/rpc.rs, crates/oxidean-db/src/lib.rs, packages/api-client</files>
  <read_first>apps/web/src/routes/settings/profile.tsrx, apps/web/src/routes/admin/auth.tsrx, crates/oxidean-api/src/auth/admin.rs, .planning/phases/07-git-repos-browse/07-UI-SPEC.md</read_first>
  <action>
Persist user `default_branch` (default `main`) via profile/settings RPC + UI section **Default branch name** / **Save default branch** (D-09). Persist instance `default_visibility` for sys-admin (admin settings surface — extend admin.auth or adjacent) (D-08). `repo.create` already consumes these — verify wiring. rpc-gen. Omit org-level settings (deferred).
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(repo_create) | test(profile) | test(admin_auth)' && bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"; or build fails on profile settings</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'default_branch|Default branch name|Save default branch' apps/web/src/routes/settings/profile.tsrx`
    - `rg -n 'default_visibility' crates/oxidean-api/src/ crates/oxidean-db/src/`
  </acceptance_criteria>
  <done>D-08/D-09 settings round-trip</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| SignedInHome dashboard | 07-04 | D-13 |
| `repo.list_mine` | 07-04 | GIT-01 |
| default_branch / default_visibility settings | 07-04 | D-08/D-09 |

<verification>
Home integration + build green; create still respects defaults.
</verification>

<success_criteria>
Signed-in home matches UI-SPEC dashboard; defaults configurable.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-04-SUMMARY.md` when done
</output>
