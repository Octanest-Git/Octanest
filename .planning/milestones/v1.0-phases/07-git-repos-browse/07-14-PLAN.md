---
phase: 07-git-repos-browse
plan: "14"
type: execute
wave: 8
depends_on:
  - "03"
files_modified:
  - apps/web/package.json
  - apps/web/src/lib/markdown.ts
  - apps/web/src/lib/markdown.test.ts
  - apps/web/src/lib/highlight.ts
  - apps/web/src/lib/highlight.test.ts
  - apps/web/src/lib/grammars
requirements:
  - GIT-05
autonomous: true
user_setup: []
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "renderGfm sanitizes GFM to HTML with rehype-sanitize last (D-18)"
    - "Shiki highlights GitHub-class languages plus in-repo TextMate grammars for .tsrx and .ripple (D-19) — no alias-only gap"
  artifacts:
    - path: apps/web/src/lib/markdown.ts
      provides: "remark-gfm + rehype-sanitize pipeline"
    - path: apps/web/src/lib/highlight.ts
      provides: "Shiki highlighter + tsrx/ripple grammars"
    - path: apps/web/src/lib/grammars
      provides: "In-repo TextMate grammars for tsrx and ripple"
  key_links:
    - from: apps/web/src/lib/highlight.ts
      to: apps/web/src/lib/grammars
      via: "load custom grammars into createHighlighter"
  assumptions: []
  prohibitions:
    - statement: "Must not ship .tsrx/.ripple highlighting via TypeScript/JavaScript alias only"
      flagged: true
    - statement: "Must not render unsanitized README HTML"
      flagged: true
---

<objective>
Add sanitized GFM Markdown rendering and Shiki highlighting with in-repo `.tsrx`/`.ripple` grammars (D-18, D-19).

Purpose: Isolate npm + grammar work from ACL APIs and Octane routes.
Output: markdown/highlight libs + tests; package.json pins from RESEARCH legitimacy table.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-RESEARCH.md
@.planning/phases/07-git-repos-browse/07-PATTERNS.md
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@apps/web/package.json
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo blob/README → browser HTML | XSS via Markdown/HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-14 | Tampering / XSS | markdown.ts | high | mitigate | rehype-sanitize after remark-rehype; no raw HTML passthrough |
| T-07-SC | Tampering | shiki/unified npm | high | mitigate | Install only RESEARCH OK packages at pinned versions; human verify if ASSUMED |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (07-14-T1): Markdown sanitize + Shiki highlight libs with tsrx/ripple grammars</name>
  <files>apps/web/package.json, apps/web/src/lib/markdown.ts, apps/web/src/lib/markdown.test.ts, apps/web/src/lib/highlight.ts, apps/web/src/lib/highlight.test.ts, apps/web/src/lib/grammars</files>
  <read_first>.planning/phases/07-git-repos-browse/07-RESEARCH.md, .planning/phases/07-git-repos-browse/07-PATTERNS.md, apps/web/package.json</read_first>
  <behavior>
    - renderGfm strips script tags / unsafe HTML
    - highlight maps .tsrx and .ripple via in-repo grammars (not TS/JS alias alone)
    - createHighlighter loads cool-biased github-light/dark themes
  </behavior>
  <action>
Closest analog: none in PATTERNS.md for markdown/highlight libs — follow RESEARCH.md Standard Stack / recommendations (Shiki + remark/rehype sanitize; in-repo TextMate grammars for tsrx/ripple). Add RESEARCH-pinned packages: `shiki`, `unified`, `remark-parse`, `remark-gfm`, `remark-rehype`, `rehype-sanitize`, `rehype-stringify` (all OK legitimacy). Implement `renderGfm` with sanitize last (D-18). Implement Shiki singleton with cool-biased github-light/dark themes; load custom TextMate grammars for `tsrx` and `ripple` under `src/lib/grammars/` (D-19 — full coverage; no TypeScript-alias gap escape). Unit tests in markdown.test.ts / highlight.test.ts assert sanitize + grammar language ids register.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/lib/markdown.test.ts src/lib/highlight.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or package.json missing shiki/rehype-sanitize; or grammars directory lacks tsrx/ripple; or build fails</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'rehype-sanitize|remark-gfm' apps/web/src/lib/markdown.ts apps/web/package.json`
    - `rg -n 'tsrx|ripple|createHighlighter' apps/web/src/lib/highlight.ts apps/web/src/lib/grammars`
    - `test -d apps/web/src/lib/grammars`
    - `rg -n 'tsrx|ripple' apps/web/src/lib/grammars`
  </acceptance_criteria>
  <done>Safe Markdown + D-19 highlight ready for Code UI</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `markdown.ts` / `highlight.ts` + grammars | 07-14 | D-18/D-19 |

<verification>
vitest markdown/highlight + build green.
</verification>

<success_criteria>
D-19 grammars for tsrx/ripple present in-repo; sanitize pipeline ready.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-14-SUMMARY.md` when done
</output>
