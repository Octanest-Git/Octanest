---
phase: 07-git-repos-browse
plan: "02"
type: execute
wave: 2
depends_on:
  - "00"
  - "01"
files_modified:
  - crates/oxidean-core/src/auth_types.rs
  - crates/oxidean-core/src/repo_types.rs
  - crates/oxidean-core/src/lib.rs
  - crates/oxidean-db/migrations/postgres/0007_repositories.sql
  - crates/oxidean-db/migrations/mysql/0007_repositories.sql
  - crates/oxidean-db/migrations/sqlite/0007_repositories.sql
  - crates/oxidean-db/src/repositories.rs
  - crates/oxidean-db/src/lib.rs
  - crates/oxidean-db/tests/dialect_repositories.rs
requirements:
  - GIT-01
  - GIT-08
autonomous: false
user_setup: []
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Tri-dialect 0007_repositories exists with soft-delete + uniqueness among non-deleted (GIT-01, GIT-08)"
    - "validate_repo_name allows hyphen underscore period; RESERVED_USERNAMES includes new and UI-SPEC flat routes (D-06, D-14)"
    - "User default_branch and instance default_visibility columns exist for later create defaults (D-08, D-09)"
  artifacts:
    - path: crates/oxidean-db/migrations/postgres/0007_repositories.sql
      provides: "repositories table migration"
    - path: crates/oxidean-db/src/repositories.rs
      provides: "insert/get repository dialect helpers"
    - path: crates/oxidean-core/src/repo_types.rs
      provides: "repo DTOs + validate_repo_name"
  key_links:
    - from: crates/oxidean-db/src/repositories.rs
      to: crates/oxidean-db/migrations/postgres/0007_repositories.sql
      via: "dialect match helpers for 0007 columns"
  assumptions:
    - "ASSUME (GIT-01 empty): description null/empty OK at schema layer; create RPC deferred to 07-12"
    - "RESOLVED (07-01): human selected owner_repo_path and fail_boot_git"
  prohibitions:
    - statement: "Must not wire repo.create RPC or GitBackend in this plan"
      flagged: true
---

<objective>
Confirm one-way D-14/D-33 proceed gate, then land repositories schema + validators so the create tracer (07-12) can insert rows safely.

Purpose: Keep schema/file budget separate from GitBackend and `/new` UI.
Output: Green migration_parity + dialect_repositories + validate_repo_name; proceed_locked confirmed.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@.planning/phases/07-git-repos-browse/07-RESEARCH.md
@.planning/phases/07-git-repos-browse/07-PATTERNS.md
@.planning/phases/07-git-repos-browse/07-00-SUMMARY.md
@.planning/phases/07-git-repos-browse/07-01-SUMMARY.md
@crates/oxidean-db/migrations/postgres/0006_bootstrap_flags.sql
@crates/oxidean-core/src/auth_types.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → DB | Schema defines uniqueness / soft-delete contracts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05a | Tampering | repositories uniqueness | medium | mitigate | Unique among non-deleted (owner_id, lower(name)); soft-delete column |
| T-07-SC | Tampering | packages | high | mitigate | No new crates.io installs in this plan |
</threat_model>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 0 (07-02-T0): Confirm proceed with D-14/D-33 choices from 07-01</name>
  <decision>
Proceed to implement the one-way doors already selected in 07-01: public URLs `/{owner}/{repo}` (D-14) and fail-boot when system git is missing or older than 2.5 (D-33).
  </decision>
  <context>
Structure requires a same-plan checkpoint before schema work that enables the one-way tracer in 07-12. 07-01 recorded the option IDs; this gate confirms the executor may walk through those doors now.
  </context>
  <options>
    <option id="proceed_locked">
      <title>Proceed with owner_repo_path + fail_boot_git (recommended)</title>
      <pros>Honors locked CONTEXT + 07-01 decisions</pros>
      <cons>None if 07-01 already confirmed</cons>
    </option>
    <option id="stop">
      <title>Stop — revisit 07-01</title>
      <pros>Safe if decisions were not actually confirmed</pros>
      <cons>Blocks tracer</cons>
    </option>
  </options>
  <resume-signal>Reply with proceed_locked (recommended) or stop</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1 (07-02-T1): Migration 0007 + core validators + DB repositories API</name>
  <reversibility rating="costly">Bare layout + repositories table uniqueness become storage contracts (D-06, D-30).</reversibility>
  <precondition>07-01 human selected owner_repo_path and fail_boot_git; 07-02-T0 proceed_locked</precondition>
  <files>crates/oxidean-db/migrations/postgres/0007_repositories.sql, crates/oxidean-db/migrations/mysql/0007_repositories.sql, crates/oxidean-db/migrations/sqlite/0007_repositories.sql, crates/oxidean-db/src/repositories.rs, crates/oxidean-db/src/lib.rs, crates/oxidean-core/src/repo_types.rs, crates/oxidean-core/src/auth_types.rs, crates/oxidean-core/src/lib.rs, crates/oxidean-db/tests/dialect_repositories.rs</files>
  <read_first>crates/oxidean-db/migrations/postgres/0006_bootstrap_flags.sql, crates/oxidean-db/src/users.rs, crates/oxidean-core/src/auth_types.rs, .planning/phases/07-git-repos-browse/07-PATTERNS.md, docs/database.md</read_first>
  <behavior>
    - migration_parity finds 0007_repositories on all three dialects
    - validate_repo_name accepts my_app and rejects reserved/empty
    - insert_repository + get by owner+name for non-deleted rows
  </behavior>
  <action>
Add tri-dialect `0007_repositories` (owner_id FK, name, visibility, description, default_branch, soft-delete timestamp, updated_at, unique among non-deleted) (D-06, D-30, GIT-01, GIT-08). Add columns for user `default_branch` and instance `default_visibility` (D-08, D-09) via same migration or paired ALTERs matching 0006 style. Implement `repositories.rs` dialect match helpers + Database facade methods. Add `validate_repo_name` (D-06) separate from username; extend `RESERVED_USERNAMES` with `new` and other flat routes from UI-SPEC (D-14). Export DTOs in `repo_types.rs`. Turn dialect_repositories stub toward green for schema presence. Do not wire RPC or git yet (07-12).
  </action>
  <verify>
    <automated>cargo test -p oxidean-core --lib && cargo test -p oxidean-db --lib migration_parity && cargo test -p oxidean-db --test dialect_repositories</automated>
    <fails_when>exit code != 0; or migration_parity missing 0007; or validate_repo_name absent from core</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f crates/oxidean-db/migrations/postgres/0007_repositories.sql`
    - `rg -n 'validate_repo_name' crates/oxidean-core/src/`
    - `rg -n '"new"' crates/oxidean-core/src/auth_types.rs`
    - `rg -n 'insert_repository|RepositoryRow' crates/oxidean-db/src/repositories.rs`
  </acceptance_criteria>
  <done>DB + validators ready for tracer create in 07-12</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `0007_repositories` + `repositories.rs` | 07-02 | GIT-01/08 |
| `validate_repo_name` + reserved `new` | 07-02 | D-06/D-14 |

<verification>
migration_parity + dialect_repositories + core lib green; proceed_locked recorded.
</verification>

<success_criteria>
Schema and validators exist; create RPC still deferred to 07-12.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-02-SUMMARY.md` when done
</output>
