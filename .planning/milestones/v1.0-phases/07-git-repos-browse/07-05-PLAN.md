---
phase: 07-git-repos-browse
plan: "05"
type: execute
wave: 7
depends_on:
  - "12"
  - "04"
files_modified:
  - crates/oxidean-git/src/backend.rs
  - crates/oxidean-git/src/cli.rs
  - crates/oxidean-api/src/repo/mod.rs
  - crates/oxidean-api/src/repo/acl.rs
  - crates/oxidean-api/src/routes/repo_raw.rs
  - crates/oxidean-api/src/app.rs
  - crates/oxidean-api/src/rpc.rs
  - crates/oxidean-api/tests/repo_private_404.rs
  - packages/api-client
requirements:
  - GIT-05
  - GIT-08
autonomous: true
user_setup: []
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Private/non-access returns identical 404 / repo.not_found; anonymous can read public (D-23, D-24, D-25)"
    - "ls_tree / cat_blob / raw HTTP serve ACL-safe tree and blob bytes (GIT-05, GIT-08)"
    - "Empty repo read returns structured empty without 500"
  artifacts:
    - path: crates/oxidean-api/src/repo/acl.rs
      provides: "Owner-only private + unified not_found"
    - path: crates/oxidean-api/src/routes/repo_raw.rs
      provides: "GET raw blob under /api"
  key_links:
    - from: crates/oxidean-api/src/repo/mod.rs
      to: crates/oxidean-git/src/cli.rs
      via: "ls_tree / cat_blob"
  assumptions:
    - "ASSUME (GIT-05 concurrency): read-only browse; parallel OK"
  prohibitions:
    - statement: "Must not show private repository existence via distinct forbidden messaging"
      flagged: true
---

<objective>
Ship ACL-safe tree/blob/raw APIs so Code UI (07-15) can browse without leaking private repos (GIT-05 core backend).

Purpose: Separate API/ACL file budget from Markdown/Shiki and Octane routes.
Output: Green repo_private_404 + git read APIs.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-RESEARCH.md
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@.planning/phases/07-git-repos-browse/07-12-SUMMARY.md
@crates/oxidean-api/src/routes/avatar.rs
@crates/oxidean-api/src/auth/gate.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous/auth → repo read | ACL stub private=owner-only |
| HTTP raw path params → filesystem/git | Path traversal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-13 | Information disclosure | repo ACL | high | mitigate | Unified not_found for missing and unauthorized private (D-25) |
| T-07-15 | Tampering | raw route | high | mitigate | Validate ref/path; resolve under repos_dir; avatar-style canonicalize |
| T-07-16 | Denial of service | huge blobs | medium | mitigate | Soft size limit headers/errors for UI (D-20) |
| T-07-SC | Tampering | packages | high | mitigate | No new npm; extend existing crates only |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (07-05-T1): GitBackend read APIs + ACL + raw HTTP</name>
  <reversibility rating="costly">Private ACL stub until Phase 10 (D-23).</reversibility>
  <files>crates/oxidean-git/src/backend.rs, crates/oxidean-git/src/cli.rs, crates/oxidean-api/src/repo/acl.rs, crates/oxidean-api/src/repo/mod.rs, crates/oxidean-api/src/routes/repo_raw.rs, crates/oxidean-api/src/app.rs, crates/oxidean-api/src/rpc.rs, crates/oxidean-api/tests/repo_private_404.rs, packages/api-client</files>
  <read_first>crates/oxidean-git/src/backend.rs, crates/oxidean-api/src/routes/avatar.rs, crates/oxidean-api/src/auth/gate.rs, .planning/phases/07-git-repos-browse/07-RESEARCH.md</read_first>
  <behavior>
    - private repo as non-owner → repo.not_found same as missing
    - public anonymous read succeeds
    - ls_tree returns dirs/files/gitlink modes
  </behavior>
  <action>
Extend `GitBackend` with `ls_tree`, `cat_blob`, `list_refs` (as needed). Implement ACL helper: resolve owner+name; if missing OR private and caller≠owner → identical not_found (D-23–D-25). Add RPC procedures for tree/blob/readme metadata. Add `GET` raw blob route under `/api` (Traefik already routes `/api`) with cookie session ACL — not RPC JSON. Validate refs/paths; reject `..`. Green `repo_private_404`. Empty repo read returns structured empty (no 500). Do not add Markdown/Shiki or Octane browse routes here (07-14 / 07-15).
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(repo_private) | test(repo_create)' && cargo nextest run -p oxidean-git --lib</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'not_found|private' crates/oxidean-api/src/repo/acl.rs`
    - `rg -n 'ls_tree|cat_blob' crates/oxidean-git/src/`
    - `rg -n 'repo_raw|raw' crates/oxidean-api/src/routes/ crates/oxidean-api/src/app.rs`
  </acceptance_criteria>
  <done>ACL-safe tree/blob/raw APIs green</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `ls_tree` / `cat_blob` + ACL | 07-05 | GIT-05/D-25 |
| `repo_raw` HTTP | 07-05 | GIT-05 |

<verification>
private_404 + git lib green.
</verification>

<success_criteria>
GIT-05 browse APIs + ACL complete; UI deferred to 07-15.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-05-SUMMARY.md` when done
</output>
