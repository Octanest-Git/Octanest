---
phase: 07-git-repos-browse
plan: "03"
type: execute
wave: 5
depends_on:
  - "12"
  - "13"
files_modified:
  - crates/oxidean-api/src/repo/mod.rs
  - crates/oxidean-api/src/repo/templates.rs
  - crates/oxidean-api/assets/gitignore
  - crates/oxidean-api/assets/stack-presets
  - apps/web/src/routes/new.tsrx
  - apps/web/package.json
  - apps/web/src/lib/spdx-licenses.ts
  - docs/guides/stack-presets.md
  - crates/oxidean-api/tests/repo_create.rs
requirements:
  - GIT-01
autonomous: true
user_setup: []
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "/new offers grouped stack + License (full SPDX + None) + .gitignore pickers; create can seed initial commit when any template content selected (D-02, D-03, D-04)"
    - "Duplicate name shows inline field error copy from UI-SPEC (D-12)"
    - "Default visibility comes from instance setting else public (D-08)"
    - "Community presets documented as in-repo packs + guide — no marketplace UI (D-03)"
    - statement: "/new description Textarea wraps without horizontal overflow"
      verification: backstop
  artifacts:
    - path: crates/oxidean-api/src/repo/templates.rs
      provides: "Initial commit seeding for stack/license/gitignore"
    - path: apps/web/src/routes/new.tsrx
      provides: "Full create form pickers"
    - path: docs/guides/stack-presets.md
      provides: "Community pack contribution guide"
  key_links:
    - from: apps/web/src/routes/new.tsrx
      to: crates/oxidean-api/src/repo/mod.rs
      via: "repo.create template fields"
  assumptions:
    - "ASSUME (RESEARCH Q2): if all template pickers are None → leave bare empty + Quick setup; if any selected → single initial commit on default branch"
  prohibitions:
    - statement: "Must not fetch license or gitignore catalogs from the network at request time"
      flagged: true
---

<objective>
Expand create flow to templates, SPDX/gitignore catalogs, duplicate inline errors, and instance default visibility — completing GIT-01 product surface beyond the tracer.

Purpose: Day-one forge create parity without marketplace UI.
Output: Rich `/new`; initial commits when templates chosen; green expanded repo_create tests.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@.planning/phases/07-git-repos-browse/07-UI-SPEC.md
@.planning/phases/07-git-repos-browse/07-RESEARCH.md
@.planning/phases/07-git-repos-browse/07-12-SUMMARY.md
@.planning/phases/07-git-repos-browse/07-13-SUMMARY.md
@.planning/phases/07-git-repos-browse/07-02-SUMMARY.md
@apps/web/src/routes/new.tsrx
@crates/oxidean-api/src/repo/mod.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client template IDs → server assets | Only serve known pack IDs; no path traversal into assets |
| SPDX / gitignore file content → git commit | Untrusted only as selected catalog entries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09 | Tampering | template asset paths | high | mitigate | Resolve pack IDs via allowlist map under assets/; reject `..` |
| T-07-10 | Information disclosure | duplicate create | low | accept | Duplicate error is expected UX for owner namespace |
| T-07-SC | Tampering | npm spdx-license-list | high | mitigate | RESEARCH legitimacy OK — install pinned version from RESEARCH table only |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (07-03-T1): Vendored catalogs + initial-commit seeder</name>
  <files>crates/oxidean-api/src/repo/templates.rs, crates/oxidean-api/src/repo/mod.rs, crates/oxidean-api/assets/gitignore, crates/oxidean-api/assets/stack-presets, crates/oxidean-api/tests/repo_create.rs</files>
  <read_first>crates/oxidean-api/src/repo/mod.rs, .planning/phases/07-git-repos-browse/07-RESEARCH.md, crates/oxidean-git/src/cli.rs</read_first>
  <action>
Per D-02/D-03/D-04 and RESEARCH discretion: vendor a wide day-one stack-presets pack tree and a curated github/gitignore-style catalog under `crates/oxidean-api/assets/` (static files — no live gitignore.io). Extend `GitBackend`/`CliGitBackend` only as needed to write an initial commit on the default branch when template content is present (temp worktree or plumbing — RESEARCH Q2). Extend `repo.create` input with stack_id, license_id (or none), gitignore_id (or none). When all none → empty bare remains. Turn create tests green for template seed + duplicate stable code. Document pack layout for community PRs.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(repo_create)'</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -d crates/oxidean-api/assets/stack-presets || test -d crates/oxidean-api/assets/gitignore`
    - `rg -n 'templates|gitignore|license|stack' crates/oxidean-api/src/repo/`
    - nextest repo_create exits 0
  </acceptance_criteria>
  <done>Server can seed initial commit from vendored catalogs</done>
</task>

<task type="auto">
  <name>Task 2 (07-03-T2): /new pickers + SPDX client list + duplicate inline error</name>
  <files>apps/web/src/routes/new.tsrx, apps/web/package.json, apps/web/src/lib/spdx-licenses.ts, apps/web/src/routes/new.integration.test.ts</files>
  <read_first>apps/web/src/routes/new.tsrx, .planning/phases/07-git-repos-browse/07-UI-SPEC.md, .planning/phases/07-git-repos-browse/07-RESEARCH.md</read_first>
  <action>
Install `spdx-license-list` at RESEARCH-pinned version (legitimacy OK). Build license Select from full SPDX IDs + None; gitignore Select from catalog + None; stack Select grouped. Visibility radios default from instance setting else Public (D-08) — fetch setting via existing or new thin RPC if needed. Map duplicate error to inline field copy exactly: "A repository with this name already exists. Choose a different name." (D-12). Creating… pending state. Octane `.tsrx` only.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/new.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'spdx-license-list' apps/web/package.json`
    - `rg -n 'A repository with this name already exists|Stack template|License|\.gitignore' apps/web/src/routes/new.tsrx`
  </acceptance_criteria>
  <done>/new matches UI-SPEC create contract for templates and errors</done>
</task>

<task type="auto">
  <name>Task 3 (07-03-T3): Stack presets contribution guide</name>
  <files>docs/guides/stack-presets.md</files>
  <read_first>docs/ARCHITECTURE.md, crates/oxidean-api/assets/stack-presets, .planning/phases/07-git-repos-browse/07-CONTEXT.md</read_first>
  <action>
Write `docs/guides/stack-presets.md` explaining in-repo pack layout, how to PR a community preset, and that a public marketplace UI is later (D-03). Link from ARCHITECTURE or CONFIGURATION briefly if a natural anchor exists — prefer one short cross-link without large doc rewrites.
  </action>
  <verify>
    <automated>test -f docs/guides/stack-presets.md && rg -n 'pack|PR|marketplace' docs/guides/stack-presets.md</automated>
    <fails_when>file missing; or guide lacks contribution/PR guidance</fails_when>
  </verify>
  <acceptance_criteria>
    - Guide exists and states marketplace UI is out of scope for now
    - Guide points at the assets pack path used in-repo
  </acceptance_criteria>
  <done>Community preset path documented</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| Template seeder + assets catalogs | 07-03 | D-02–D-04 |
| SPDX picker via spdx-license-list | 07-03 | D-04 |
| `docs/guides/stack-presets.md` | 07-03 | D-03 |

<verification>
repo_create + /new integration green; guide present.
</verification>

<success_criteria>
GIT-01 create UX includes templates, visibility default, duplicate inline error.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-03-SUMMARY.md` when done
</output>
