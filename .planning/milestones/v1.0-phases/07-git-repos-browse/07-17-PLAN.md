---
phase: 07-git-repos-browse
plan: "17"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - crates/oxidean-git/src/version.rs
  - crates/oxidean-api/src/main.rs
  - crates/oxidean-api/Dockerfile
  - docker-compose.yml
  - .env.example
requirements:
  - GIT-09
  - GIT-08
autonomous: true
user_setup: []
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "API boot exits 1 when git missing or version older than 2.5.0 (D-33, GIT-09)"
    - "Dockerfile installs git; Compose binds ./var/repos to /var/repos (D-30, D-38)"
    - "OXIDEAN_REPOS_DIR documented in .env.example (D-31)"
  artifacts:
    - path: crates/oxidean-git/src/version.rs
      provides: "assert_git_version floor 2.5.0"
    - path: crates/oxidean-api/Dockerfile
      provides: "git package in API image"
  key_links:
    - from: crates/oxidean-api/src/main.rs
      to: crates/oxidean-git/src/version.rs
      via: "assert_git_version → exit(1)"
  assumptions:
    - "PRECONDITION: 07-01 fail_boot_git + 07-02-T0 proceed_locked"
  prohibitions:
    - statement: "Must not soft-continue API boot when git is missing or older than 2.5.0"
      flagged: true
---

<objective>
Thin ops/boot plan: implement git version fail-boot (D-33) and Compose/Dockerfile repos volume wiring (D-30/D-38) peeled from the create tracer for file budget.

Purpose: Keep 07-12 tracer ≤9 files while still shipping operator contracts.
Output: Green git_version_gate; Dockerfile git; Compose `./var/repos`; `.env.example` OXIDEAN_REPOS_DIR.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@.planning/phases/07-git-repos-browse/07-RESEARCH.md
@.planning/phases/07-git-repos-browse/07-02-SUMMARY.md
@crates/oxidean-api/Dockerfile
@docker-compose.yml
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host/Compose image → API process | Missing git must not serve forge routes |
| Compose volume → repos_dir | Persist bare repos across restarts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08 | Denial of service | missing git | high | mitigate | Boot assert_git_version((2,5,0)) exit 1 (D-33) |
| T-07-SC | Tampering | apt git in Dockerfile | medium | accept | Distro git package; no new crates.io deps |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (07-17-T1): Fail-boot git gate + Dockerfile/Compose repos volume</name>
  <precondition>07-01 human selected fail_boot_git; 07-02-T0 proceed_locked; oxidean-git version stub from 07-00 exists. Upstream locked doors (07-01-T2 + 07-02-T0) are sufficient — no duplicate in-plan checkpoint.</precondition>
  <files>crates/oxidean-git/src/version.rs, crates/oxidean-api/src/main.rs, crates/oxidean-api/Dockerfile, docker-compose.yml, .env.example</files>
  <read_first>crates/oxidean-api/src/main.rs, crates/oxidean-api/Dockerfile, docker-compose.yml, .env.example, crates/oxidean-git/src/version.rs, .planning/phases/07-git-repos-browse/07-RESEARCH.md</read_first>
  <action>
Implement assert_git_version((2,5,0)) and call it during API boot; missing/old git → eprintln + exit(1) (D-33, GIT-09). Install git in crates/oxidean-api/Dockerfile. Bind Compose volume ./var/repos:/var/repos; document OXIDEAN_REPOS_DIR in .env.example (D-30, D-31, D-38). Green git_version_gate unit tests. Do not implement repo.create or GitBackend init_bare here (07-12).
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-git -E 'test(git_version_gate)' && rg -n 'git' crates/oxidean-api/Dockerfile && rg -n 'var/repos|OXIDEAN_REPOS_DIR' docker-compose.yml .env.example && rg -n 'assert_git_version|exit\(1\)' crates/oxidean-api/src/main.rs crates/oxidean-git/src/version.rs</automated>
    <fails_when>exit code != 0; or Dockerfile lacks git install; or Compose/.env.example omit repos volume/path docs</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'assert_git_version|exit\(1\)' crates/oxidean-api/src/main.rs crates/oxidean-git/src/version.rs`
    - `rg -n 'git' crates/oxidean-api/Dockerfile`
    - `rg -n 'var/repos|OXIDEAN_REPOS_DIR' docker-compose.yml .env.example`
  </acceptance_criteria>
  <done>Boot gate + Compose/Dockerfile repos ops shipped</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `assert_git_version` + main boot | 07-17 | D-33 |
| Dockerfile git + Compose volume | 07-17 | D-30/D-38 |

<verification>
git_version_gate + Dockerfile/Compose rg green.
</verification>

<success_criteria>
Operator boot and volume contracts for Phase 7 git storage are in place.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-17-SUMMARY.md` when done
</output>
