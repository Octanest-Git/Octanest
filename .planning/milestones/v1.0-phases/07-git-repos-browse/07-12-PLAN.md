---
phase: 07-git-repos-browse
plan: "12"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - crates/oxidean-git/src/lib.rs
  - crates/oxidean-git/src/backend.rs
  - crates/oxidean-git/src/cli.rs
  - crates/oxidean-api/src/git/mod.rs
  - crates/oxidean-api/src/repo/mod.rs
  - crates/oxidean-api/src/rpc.rs
  - crates/oxidean-api/src/app.rs
  - crates/oxidean-api/tests/repo_create.rs
  - crates/oxidean-api/tests/repo_fs_layout.rs
requirements:
  - GIT-01
  - GIT-08
  - GIT-09
  - GIT-10
autonomous: true
user_setup: []
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Verified user can create an empty public repository via repo.create (D-01, D-11, GIT-01)"
    - "Bare repo exists at {OXIDEAN_REPOS_DIR}/{owner}/{name}.git with HEAD symbolic-ref to default branch (D-09, D-30, D-31, GIT-08)"
    - "AppState holds Arc<dyn GitBackend> implemented only by CliGitBackend; module docs mention future GixGitBackend (D-32, GIT-10)"
    - "Unverified repo.create returns auth.email_unverified (D-11)"
  artifacts:
    - path: crates/oxidean-git/src/backend.rs
      provides: "GitBackend trait + GitError"
    - path: crates/oxidean-git/src/cli.rs
      provides: "CliGitBackend init_bare via argv"
    - path: crates/oxidean-api/src/repo/mod.rs
      provides: "repo.create RPC"
  key_links:
    - from: crates/oxidean-api/src/repo/mod.rs
      to: crates/oxidean-git/src/cli.rs
      via: "init_bare after DB insert"
  assumptions:
    - "ASSUME (GIT-01 empty): tracer creates with name only; description null/empty OK; templates deferred to 07-03"
    - "PRECONDITION: 07-02 schema + validate_repo_name + proceed_locked complete"
    - "Locked doors D-14/D-33/D-30 already gated by 07-01 checkpoints + 07-02-T0 proceed_locked — no in-plan checkpoint required on this tracer"
  prohibitions:
    - statement: "Must not shell git through sh -c or string-concatenated shell lines"
      flagged: true
    - statement: "Must not implement GixGitBackend body in this phase"
      flagged: true
---

<objective>
Tracer: one production-quality path — verified user creates an empty public repo (DB + bare git on volume path) proving GitBackend CLI end-to-end (UI in 07-13; boot/Dockerfile ops in 07-17).

Purpose: Catch architecture dead-ends before browse/template expansion.
Output: Green repo_create + repo_fs_layout for the happy path.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@.planning/phases/07-git-repos-browse/07-RESEARCH.md
@.planning/phases/07-git-repos-browse/07-PATTERNS.md
@.planning/phases/07-git-repos-browse/07-02-SUMMARY.md
@crates/oxidean-api/src/routes/avatar.rs
@crates/oxidean-api/src/email/mod.rs
@crates/oxidean-api/src/auth/gate.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → repo.create | Untrusted name/visibility; require_verified |
| API → filesystem repos_dir | Path must stay under canonical root |
| API → git argv | User-influenced name/ref must not reach a shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Elevation of privilege | repo.create | high | mitigate | require_verified before create (D-11); bootstrap lock still blocks while needs_setup |
| T-07-06 | Tampering | CliGitBackend | high | mitigate | tokio::process::Command argv arrays only; validate_repo_name before path join |
| T-07-07 | Tampering | repos_dir paths | high | mitigate | Canonicalize under OXIDEAN_REPOS_DIR; mirror avatar basename guards |
| T-07-SC | Tampering | crates | medium | accept | Workspace member only; no new crates.io deps beyond oxidean-git |
</threat_model>

<tasks>

<task type="tracer">
  <name>Task 1 (07-12-T1): End-to-end empty public create via GitBackend CLI</name>
  <precondition>07-01 human selected owner_repo_path and fail_boot_git; 07-02-T0 proceed_locked; 07-02 migrations and validate_repo_name exist. Upstream locked doors (07-01-T1/T2 + 07-02-T0 proceed_locked) are sufficient — no duplicate in-plan checkpoint on this tracer.</precondition>
  <files>crates/oxidean-git/src/backend.rs, crates/oxidean-git/src/cli.rs, crates/oxidean-git/src/lib.rs, crates/oxidean-api/src/git/mod.rs, crates/oxidean-api/src/repo/mod.rs, crates/oxidean-api/src/rpc.rs, crates/oxidean-api/src/app.rs, crates/oxidean-api/tests/repo_create.rs, crates/oxidean-api/tests/repo_fs_layout.rs</files>
  <read_first>crates/oxidean-api/src/email/mod.rs, crates/oxidean-api/src/routes/avatar.rs, crates/oxidean-api/src/app.rs, .planning/phases/07-git-repos-browse/07-RESEARCH.md</read_first>
  <action>
Wire ONE happy path through every backend create layer (D-07 optional description field on create; D-30–D-32, D-05, D-08 default public if unset, D-09 main, GIT-01/08/09/10):

1. Implement GitBackend + CliGitBackend with at least init_bare using git init --bare then git symbolic-ref HEAD refs/heads/{branch} (no reliance on --initial-branch). Argv only. Document future GixGitBackend in module rustdoc (D-32).
2. Wire OXIDEAN_REPOS_DIR default var/repos on AppState like uploads (D-31). Boot fail-gate + Dockerfile/Compose volume live in 07-17 — do not re-implement them here.
3. repo.create: require_verified; owner = current user; validate name; optional description; insert row; init_bare under {repos_dir}/{owner_username}/{name}.git; stable duplicate error code for inline UI later; visibility public/private; return DTO with owner/name.
4. Register RPC; hold Arc dyn GitBackend on AppState as CliGitBackend only. Green repo_create verified-create + repo_fs_layout for this path. Skip templates, browse tree, archives, branch CRUD, and Octane routes (07-03 / 07-13 / later).
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(repo_create) | test(repo_fs)'</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"; or create succeeds without a bare .git directory under repos_dir</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'trait GitBackend' crates/oxidean-git/src/backend.rs`
    - `rg -n 'CliGitBackend|init_bare|symbolic-ref' crates/oxidean-git/src/cli.rs`
    - `rg -n 'repo\.create' crates/oxidean-api/src/rpc.rs`
    - `rg -n 'OXIDEAN_REPOS_DIR|repos_dir' crates/oxidean-api/src/app.rs crates/oxidean-api/src/repo/mod.rs`
  </acceptance_criteria>
  <done>Empty public create works end-to-end under tests with CLI backend</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `GitBackend` / `CliGitBackend` | 07-12 | GIT-09/10, D-32 |
| `repo.create` RPC | 07-12 | GIT-01 |
| Fail-boot + Dockerfile/Compose | 07-17 | D-30/31/33/38 |

<verification>
nextest create+fs green.
</verification>

<success_criteria>
Tracer happy path: verified create → bare repo on volume path (API tests).
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-12-SUMMARY.md` when done
</output>
