---
phase: 07-git-repos-browse
plan: "20"
type: tdd
wave: 2
depends_on:
  - "19"
files_modified:
  - crates/oxidean-git/src/cli.rs
  - crates/oxidean-api/src/routes/repo_raw.rs
  - crates/oxidean-api/tests/repo_archive.rs
requirements:
  - GIT-07
  - GIT-05
autonomous: true
gap_closure: true
gap_ids:
  - CR-01
  - WR-02
user_setup: []
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "GET archive with a treeish that begins with --output= is rejected before git runs and creates no new file under a monitored temp path (CR-01 / GIT-07)"
    - "CliGitBackend::archive places an end-of-options marker immediately before the treeish operand"
    - "validate_archive_treeish and validate_ref reject trimmed values that begin with a hyphen; validate_ref allows slashy branch names consistent with archive (WR-02)"
  artifacts:
    - path: crates/oxidean-git/src/cli.rs
      provides: "archive argv uses -- before treeish"
    - path: crates/oxidean-api/src/routes/repo_raw.rs
      provides: "HTTP treeish/ref validators reject option-like refs; raw allows /"
    - path: crates/oxidean-api/tests/repo_archive.rs
      provides: "Archive --output injection regression"
  key_links:
    - from: crates/oxidean-api/src/routes/repo_raw.rs
      to: crates/oxidean-git/src/cli.rs
      via: "serve_archive → git.archive"
  prohibitions:
    - statement: "Must not allow user treeish to be interpreted as git CLI options"
      flagged: true
---

<objective>
Close CR-01: reject option-like archive/raw treeish and force end-of-options before the revision so public archive URLs cannot create/truncate files as the API process user. Also align raw `validate_ref` slash rules with archive (WR-02).

Purpose: Safe GIT-07 downloads and consistent hierarchical refs on raw (GIT-05 browse parity).
Output: Hardened archive argv + HTTP validators; green `--output` injection regression; raw `/` allowed.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-VERIFICATION.md
@.planning/phases/07-git-repos-browse/07-REVIEW.md
@.planning/phases/07-git-repos-browse/07-08-PLAN.md
@.planning/phases/07-git-repos-browse/07-19-PLAN.md
@crates/oxidean-git/src/cli.rs
@crates/oxidean-api/src/routes/repo_raw.rs
@crates/oxidean-api/tests/repo_archive.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous/public browser → archive GET | Untrusted treeish in URL path becomes git argv |
| Treeish → `git archive` argv | Option-like values must never become `--output` / other switches |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-GC20-01 | Tampering | `CliGitBackend::archive` | critical | mitigate | End-of-options before treeish; rely on 07-19 `validate_treeish` leading-`-` reject (CR-01) |
| T-07-GC20-02 | Tampering | `validate_archive_treeish` / `validate_ref` | critical | mitigate | Reject trimmed values beginning with `-` at HTTP boundary before spawn |
| T-07-GC20-03 | Information disclosure | raw vs archive ref rules | medium | mitigate | Allow `/` in raw `validate_ref` like archive (WR-02) while keeping `..`/NUL/metachar/leading-`-` rejects |
| T-07-SC | Tampering | packages | low | accept | No new packages |
</threat_model>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (07-20-T1): Tracer — archive --output= treeish rejected, no file write</name>
  <files>crates/oxidean-api/tests/repo_archive.rs, crates/oxidean-git/src/cli.rs, crates/oxidean-api/src/routes/repo_raw.rs</files>
  <read_first>crates/oxidean-api/tests/repo_archive.rs, crates/oxidean-git/src/cli.rs, crates/oxidean-api/src/routes/repo_raw.rs, .planning/phases/07-git-repos-browse/07-REVIEW.md</read_first>
  <behavior>
    - RED first: add `repo_archive_rejects_option_like_treeish_no_output_file` that seeds a public repo, picks a unique non-existent path under the test tempdir as a would-be `--output=` target, GETs an archive URL whose treeish path segment is that option-like string ending in `.zip` (URL-encode as needed), asserts response is 4xx with `repo.invalid_ref` (or equivalent invalid treeish code), and asserts the monitored path still does not exist as a file afterward per CR-01 / GIT-07
    - GREEN: in `archive`, after existing format/prefix args, insert end-of-options then treeish; mirror leading-`-` reject in `validate_archive_treeish` (and keep metachar/`..`/NUL checks). Rely on 07-19 `validate_treeish` for the CLI layer.
  </behavior>
  <action>
Per CR-01 / GIT-07: TDD the injection regression first, then fix `archive` argv ordering and `validate_archive_treeish`. Do not remove format/prefix flags; place end-of-options only before the revision operand. Do not change clone-box UI. Reversibility: reversible — argv and validation only.
  </action>
  <reversibility rating="reversible">HTTP validation and git argv delimiter; no migration.</reversibility>
  <verify>
    <automated>cargo test -p oxidean-api --test repo_archive repo_archive_rejects_option_like_treeish_no_output_file -- --exact</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"; or the named test is missing from the harness output</fails_when>
  </verify>
  <acceptance_criteria>
    - Named test exists in `crates/oxidean-api/tests/repo_archive.rs`
    - `rg -n 'archive' -A25 crates/oxidean-git/src/cli.rs` shows `"--"` immediately before the treeish operand in the args array
    - `rg -n "starts_with\\('-'\\)" crates/oxidean-api/src/routes/repo_raw.rs` matches inside `validate_archive_treeish`
    - cargo exact test exits 0
  </acceptance_criteria>
  <done>Option-like archive treeish cannot create/truncate files via git --output</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (07-20-T2): Expand — validate_ref leading-hyphen + slash parity (WR-02) + archive suite green</name>
  <files>crates/oxidean-api/src/routes/repo_raw.rs, crates/oxidean-api/tests/repo_archive.rs</files>
  <read_first>crates/oxidean-api/src/routes/repo_raw.rs, crates/oxidean-api/tests/repo_archive.rs</read_first>
  <behavior>
    - `validate_ref` rejects trimmed values beginning with `-` and still rejects `..` / NUL / empty
    - `validate_ref` allows `/` so hierarchical branch names work on raw the same way archive already allows (WR-02 / GIT-05)
    - Existing nonempty zip/tar.gz and ACL archive tests still pass
  </behavior>
  <action>
Per WR-02 and CR-01 defense-in-depth: update `validate_ref` to drop the blanket `/` ban while adding the leading-`-` reject and retaining `..`/NUL/empty checks (align with `validate_archive_treeish` metachar policy where applicable). Optionally add a small archive or raw assertion that a slashy public ref is accepted when present; do not break happy-path `repo_archive_zip_and_tar_gz_nonempty_for_seeded_ref`. Do not rescan or change other `run_git*` sites in `cli.rs` here — archive end-of-options is Task 1; leading-hyphen reject for treeish is 07-19 `validate_treeish`. Do not invent new packages.
  </action>
  <verify>
    <automated>cargo test -p oxidean-api --test repo_archive</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'fn validate_ref' -A20 crates/oxidean-api/src/routes/repo_raw.rs` shows leading-`-` reject and does not reject solely because the ref contains `/`
    - Full `repo_archive` test binary exits 0 including injection and nonempty archive cases
  </acceptance_criteria>
  <done>CR-01 closed; WR-02 raw/archive slash parity; archive suite green</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `git archive` with end-of-options before treeish | 07-20 | CR-01 |
| HTTP `validate_archive_treeish` / `validate_ref` hardening | 07-20 | CR-01 + WR-02 |
| `repo_archive_rejects_option_like_treeish_no_output_file` | 07-20 | GIT-07 regression |

<verification>
cargo test -p oxidean-api --test repo_archive exits 0; injection case proves no output file side effect.
</verification>

<success_criteria>
CR-01 closed: option-like archive treeish cannot write files; WR-02 raw refs allow `/` and reject leading `-`.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-20-SUMMARY.md` when done
</output>
