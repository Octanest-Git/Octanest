---
phase: 07-git-repos-browse
plan: "15"
type: execute
wave: 9
depends_on:
  - "05"
  - "14"
files_modified:
  - apps/web/src/routes/$owner.$repo.index.tsrx
  - apps/web/src/routes/$owner.$repo.tree.$.tsrx
  - apps/web/src/routes/$owner.$repo.blob.$.tsrx
  - apps/web/src/routes/$owner.$repo.integration.test.ts
  - apps/web/src/components/repo
  - packages/api-client
requirements:
  - GIT-05
autonomous: true
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Code home shows file tree (dirs first) + sanitized GFM README when present; empty stays Quick setup (D-15, D-18)"
    - "Routes /tree/{ref}/… /blob/{ref}/… work; refs prefer branch/tag names (D-17)"
    - "Blob highlighting covers GitHub-class languages plus .tsrx and .ripple; line permalinks #L/#L-L; large-file soft limit; images inline else cannot preview (D-19, D-20)"
    - "Submodules appear as gitlink entries and support recursive browse when resolvable (D-21)"
    - "Private → Not found page same as missing (D-25)"
    - "UI E3/E4 empty/loading/error/populated/partial states honored"
    - statement: "Long paths/descriptions ellipsis or wrap — held-out visual"
      verification: backstop
  artifacts:
    - path: apps/web/src/routes/$owner.$repo.tree.$.tsrx
      provides: "Tree browse route"
    - path: apps/web/src/routes/$owner.$repo.blob.$.tsrx
      provides: "Blob browse + highlight"
    - path: apps/web/src/routes/$owner.$repo.integration.test.ts
      provides: "empty / tree / private-404 integration coverage"
  key_links:
    - from: apps/web/src/routes/$owner.$repo.index.tsrx
      to: apps/web/src/lib/markdown.ts
      via: "README render"
    - from: apps/web/src/routes/$owner.$repo.blob.$.tsrx
      to: apps/web/src/lib/highlight.ts
      via: "syntax highlight"
  assumptions: []
  prohibitions:
    - statement: "Must not omit Issues/PRs by showing stub nav that invents those products"
      flagged: true
---

<objective>
Ship Code / tree / blob Octane routes using 07-05 APIs and 07-14 render libs (GIT-05 UI).

Purpose: Complete readable forge surface after create.
Output: Green owner/repo integration tests for empty, tree, and private 404; build green.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-UI-SPEC.md
@.planning/phases/07-git-repos-browse/07-05-SUMMARY.md
@.planning/phases/07-git-repos-browse/07-14-SUMMARY.md
@apps/web/src/routes/$owner.$repo.index.tsrx
@apps/web/src/lib/markdown.ts
@apps/web/src/lib/highlight.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → browse routes | Must not leak private existence |
| README/blob HTML | Relies on 07-14 sanitize |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-13b | Information disclosure | Code UI | high | mitigate | Private → Not found page identical to missing (D-25) |
| T-07-SC | Tampering | packages | high | mitigate | No new packages; consume 07-14 libs |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (07-15-T1): Code / tree / blob Octane routes + integration coverage</name>
  <files>apps/web/src/routes/$owner.$repo.index.tsrx, apps/web/src/routes/$owner.$repo.tree.$.tsrx, apps/web/src/routes/$owner.$repo.blob.$.tsrx, apps/web/src/routes/$owner.$repo.integration.test.ts, apps/web/src/components/repo, packages/api-client</files>
  <read_first>.planning/phases/07-git-repos-browse/07-UI-SPEC.md, apps/web/src/routes/$owner.$repo.index.tsrx, apps/web/src/lib/markdown.ts, apps/web/src/lib/highlight.ts, apps/web/src/routes/new.integration.test.ts</read_first>
  <behavior>
    - empty repo Code home shows Quick setup (not tree)
    - tree route lists dirs first when commits exist
    - private non-owner (or missing) shows Not found — same copy
  </behavior>
  <action>
Implement Code home + tree/blob per UI-SPEC (D-15–D-21): link-row nav Code active; ref Select; tree dirs first; README via renderGfm below; breadcrumbs; line numbers + `#L`/`#L-L` highlight wash; large-file notice + Download file / View raw file; binary image inline else cannot-preview copy; submodule icon + recursive navigation when resolvable; titles per Document titles table; skeletons. Private → Not found page same as missing (D-25). Omit Issues/PRs nav. Clone box can be stub until 07-08. Prefer official Badge/Breadcrumb if adding shadcn wrappers. Add `apps/web/src/routes/$owner.$repo.integration.test.ts` covering empty Quick setup, populated tree, and private/missing Not found so must_haves are fail-able without human-only checks.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/\$owner.\$repo.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or TypeScript/Octane errors on owner/repo routes; or integration file missing empty/tree/404 cases</fails_when>
    <human-check>Open a seeded repo blob for .ts / .tsrx; confirm highlight + README sanitize (script attempt stripped).</human-check>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Quick setup|Directories|README|Page not found' apps/web/src/routes/ apps/web/src/components/repo/`
    - `test -f apps/web/src/routes/\$owner.\$repo.integration.test.ts`
    - `rg -n 'Quick setup|Not found|tree' apps/web/src/routes/\$owner.\$repo.integration.test.ts`
    - Routes exist for tree and blob under owner/repo
  </acceptance_criteria>
  <done>Users can browse files on public repos and get 404 on private non-owner; integration covers empty/tree/404</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| Code/tree/blob routes | 07-15 | D-15–D-21 |
| owner/repo integration tests | 07-15 | empty/tree/404 |

<verification>
vitest owner/repo integration + build green.
</verification>

<success_criteria>
GIT-05 file browse UI complete with ACL-visible 404 and safe rendering.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-15-SUMMARY.md` when done
</output>
