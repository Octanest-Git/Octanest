---
phase: 20-packages-registry
plan: "10"
type: execute
wave: 6
depends_on:
  - "01"
  - "08"
files_modified:
  - "apps/web/src/routes/$owner.packages.tsrx"
  - "apps/web/src/routes/$owner.$repo.packages.tsrx"
  - apps/web/src/components/packages/delete-version-dialog.tsrx
  - apps/web/src/lib/session-queries.ts
  - "apps/web/src/routes/$owner.packages.integration.test.ts"
  - "apps/web/src/routes/$owner.$repo.packages.integration.test.ts"
requirements:
  - PKG-05
autonomous: true
user_setup: []
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Owner packages page lists packages/versions the viewer may read (D-PKG-11)"
    - "Repo packages page lists packages linked to that repository (D-PKG-11)"
    - "Delete requires typing name@version before calling packages.deleteVersion (D-PKG-12)"
  artifacts:
    - path: "apps/web/src/routes/$owner.packages.tsrx"
      provides: "Owner packages UI"
    - path: apps/web/src/components/packages/delete-version-dialog.tsrx
      provides: "Type-to-confirm delete"
  key_links:
    - from: apps/web packages routes
      to: "@oxidean/api-client packages.list/deleteVersion"
      via: "TanStack Query session helpers"
  assumptions:
    - "ASSUME: Follow Phase 15 type-to-confirm danger UX patterns for the dialog"
  prohibitions:
    - statement: "Must not mix JSX return parentheses with Rivet template blocks in the same .tsrx component"
      flagged: true
    - statement: "Must not use Zustand for server package lists"
      flagged: true
---

<objective>
Ship Octane owner and repo-linked packages pages with type-to-confirm version delete (PKG-05, D-PKG-11, D-PKG-12).

Purpose: Users can manage packages in the forge UI.
Output: Routes + dialog; Wave 0 Vitest stubs green.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-packages-registry/20-CONTEXT.md
@.agents/skills/octane/SKILL.md
@apps/web/src/routes/$owner.tsrx
@apps/web/src/routes/$owner.$repo.tsrx
@apps/web/src/lib/session-queries.ts
@apps/web/src/routes/$owner.packages.integration.test.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → session RPC | Only session; registry PAT never in UI storage beyond user paste elsewhere |
| Delete dialog → RPC | Confirm string required client + server |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-07 | Elevation of privilege | delete dialog | high | mitigate | Type-to-confirm + server Admin check (D-PKG-12) |
| T-20-SC | Tampering | installs | low | accept | No new npm deps |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (20-10-T1): Owner + repo packages pages</name>
  <files>apps/web/src/routes/$owner.packages.tsrx, apps/web/src/routes/$owner.$repo.packages.tsrx, apps/web/src/lib/session-queries.ts, apps/web/src/routes/$owner.packages.integration.test.ts, apps/web/src/routes/$owner.$repo.packages.integration.test.ts</files>
  <read_first>.agents/skills/octane/SKILL.md, apps/web/src/lib/session-queries.ts, apps/web/src/routes/$owner.tsrx</read_first>
  <behavior>
    - Owner page shows package format, name, versions for that owner
    - Repo page shows only packages with repository_id link
    - Empty states when none
  </behavior>
  <action>
Author .tsrx routes with Rivet syntax only (no JSX return mix). Use TanStack Query via session-queries helpers calling generated packages.list. Wire nav from owner/repo chrome per existing patterns (D-PKG-11). Green list Vitest stubs from 20-01.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/\$owner.packages.integration.test.ts src/routes/\$owner.\$repo.packages.integration.test.ts && bun --cwd apps/web run build</automated>
  </verify>
  <done>Owner and repo packages pages render from live RPC types</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (20-10-T2): Type-to-confirm delete dialog</name>
  <files>apps/web/src/components/packages/delete-version-dialog.tsrx, apps/web/src/routes/$owner.packages.tsrx, apps/web/src/routes/$owner.packages.integration.test.ts</files>
  <read_first>apps/web/src/routes/$owner.settings.members.tsrx</read_first>
  <behavior>
    - Delete disabled until input equals name@version
    - Cancel leaves version; Confirm calls deleteVersion and refreshes list
    - Non-Admin does not see delete affordance
  </behavior>
  <action>
Implement delete dialog per D-PKG-12 matching Phase 15 danger confirm UX. Only show for Admin managers. Green delete Vitest cases.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/\$owner.packages.integration.test.ts</automated>
  </verify>
  <done>Type-to-confirm delete UX green — PKG-05 UI complete for owner/repo</done>
</task>

</tasks>

<verification>
Packages pages + delete dialog Vitest green; web build OK.
</verification>

<success_criteria>
Users can list and delete permitted package versions in the UI with type-to-confirm.
</success_criteria>

<output>
Create `.planning/phases/20-packages-registry/20-10-SUMMARY.md` when done
</output>
