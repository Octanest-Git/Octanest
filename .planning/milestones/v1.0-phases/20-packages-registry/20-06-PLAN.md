---
phase: 20-packages-registry
plan: "06"
type: execute
wave: 4
depends_on:
  - "04"
files_modified:
  - crates/octanest-api/src/packages/npm.rs
  - crates/octanest-api/tests/npm_registry.rs
requirements:
  - PKG-02
  - PKG-04
autonomous: true
user_setup: []
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "User can publish and install npm packages via registry root /npm/{owner}/ (PKG-02, D-PKG-01, D-PKG-03)"
    - "Packument dist.tarball URLs use OCTANEST_PUBLIC_ORIGIN (pitfall 4)"
    - "Existing name@version cannot be overwritten (D-PKG-10); delete then republish allowed"
  artifacts:
    - path: crates/octanest-api/src/packages/npm.rs
      provides: "packument GET, publish PUT, tarball GET"
  key_links:
    - from: packument dist.tarball
      to: OCTANEST_PUBLIC_ORIGIN
      via: "Absolute /npm/{owner}/…/-/….tgz URLs"
    - from: crates/octanest-api/src/packages/npm.rs
      to: crates/octanest-api/src/packages/acl.rs
      via: "Hybrid ACL∩PAT on publish/install"
  assumptions:
    - "ASSUME: Per-owner registry URL /npm/{owner}/ is the client registry config root"
  prohibitions:
    - statement: "Must not build tarball URLs from request Host alone"
      flagged: true
    - statement: "Must not overwrite an existing npm version while present"
      flagged: true
---

<objective>
Implement npm registry publish and install against /npm/{owner}/ (packument, publish with attachments, tarball) for PKG-02 core (D-PKG-14 publish/install portion).

Purpose: Parallel wave with OCI (20-05) — npm.rs only file ownership.
Output: npm publish/install green; dist-tags/deprecate/search deferred to 20-07.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-packages-registry/20-RESEARCH.md
@crates/octanest-api/src/packages/npm.rs
@crates/octanest-api/src/packages/store.rs
@crates/octanest-api/src/packages/acl.rs
@crates/octanest-api/tests/npm_registry.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm/pnpm/yarn → /npm | PAT Bearer or Basic; Cookie ignored |
| Packument → tarball URL | PUBLIC_ORIGIN prevents Host injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-06 | Spoofing | tarball URLs | high | mitigate | Absolute URLs from OCTANEST_PUBLIC_ORIGIN only |
| T-20-04 | Tampering | publish | high | mitigate | 409 / EPUBLISHCONFLICT on version overwrite (D-PKG-10) |
| T-20-02 | Elevation of privilege | publish | high | mitigate | Write+ and package:write required |
| T-20-SC | Tampering | installs | low | accept | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (20-06-T1): npm packument GET + tarball GET</name>
  <files>crates/octanest-api/src/packages/npm.rs, crates/octanest-api/tests/npm_registry.rs</files>
  <read_first>.planning/phases/20-packages-registry/20-RESEARCH.md Pattern 2, crates/octanest-api/src/packages/npm.rs</read_first>
  <behavior>
    - GET /npm/{owner}/{package} returns packument with versions and dist.tarball under PUBLIC_ORIGIN
    - GET tarball path returns bytes for public anon or authorized private
    - Scoped package path encoding /@scope%2fname supported
  </behavior>
  <action>
Replace npm 404 stub with packument and tarball handlers under /npm/{owner}/ per D-PKG-01/03. Enforce D-PKG-05 visibility. Do not edit oci.rs or generic.rs. Green install-metadata npm_registry cases.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(npm_registry)'</automated>
  </verify>
  <done>Packument and tarball GET work with PUBLIC_ORIGIN tarball URLs</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (20-06-T2): npm publish PUT with attachments</name>
  <files>crates/octanest-api/src/packages/npm.rs, crates/octanest-api/tests/npm_registry.rs</files>
  <read_first>.planning/phases/20-packages-registry/20-RESEARCH.md npm API subset, crates/octanest-api/src/packages/store.rs</read_first>
  <behavior>
    - PUT /npm/{owner}/{package} publishes version + tarball attachment
    - Duplicate version while present → conflict
    - Requires Write+ ACL and package:write PAT
  </behavior>
  <action>
Implement classic CouchDB-shaped publish PUT with attachments, storing tarball in CA store and version metadata immutable per D-PKG-10. After delete (later RPC/UI) republish allowed. Green publish npm_registry cases (leave dist-tags/deprecate/search failing/ignored for 20-07).
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(npm_registry)'</automated>
  </verify>
  <done>npm publish/install core green for PKG-02</done>
</task>

</tasks>

<verification>
npm_registry publish + packument + tarball cases green; extras still for 20-07.
</verification>

<success_criteria>
Users can publish and pull npm packages scoped to owner via /npm/{owner}/.
</success_criteria>

<output>
Create `.planning/phases/20-packages-registry/20-06-SUMMARY.md` when done
</output>
