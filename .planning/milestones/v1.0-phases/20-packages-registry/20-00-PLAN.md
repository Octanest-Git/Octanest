---
phase: 20-packages-registry
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/oxidean-api/tests/oci_registry.rs
  - crates/oxidean-api/tests/npm_registry.rs
  - crates/oxidean-api/tests/generic_registry.rs
  - crates/oxidean-api/tests/package_acl.rs
  - crates/oxidean-api/tests/package_rpc.rs
  - crates/oxidean-db/tests/dialect_packages.rs
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
  - PKG-04
  - PKG-05
autonomous: true
user_setup: []
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Wave 0 discoverable stubs exist for oci_registry, npm_registry, generic_registry, package_acl, package_rpc, and dialect_packages (PKG-01..05)"
  artifacts:
    - path: crates/oxidean-api/tests/oci_registry.rs
      provides: "PKG-01 OCI Distribution Spec failing stubs"
    - path: crates/oxidean-api/tests/npm_registry.rs
      provides: "PKG-02 npm registry failing stubs"
    - path: crates/oxidean-api/tests/generic_registry.rs
      provides: "PKG-03 generic/raw failing stubs"
    - path: crates/oxidean-api/tests/package_acl.rs
      provides: "PKG-04 hybrid ACL∩PAT failing stubs"
    - path: crates/oxidean-api/tests/package_rpc.rs
      provides: "PKG-05 list/delete RPC failing stubs"
    - path: crates/oxidean-db/tests/dialect_packages.rs
      provides: "packages migration parity stub"
  key_links:
    - from: .planning/phases/20-packages-registry/20-VALIDATION.md
      to: crates/oxidean-api/tests/oci_registry.rs
      via: "Wave 0 Requirements checklist"
  assumptions:
    - "ASSUME: Wave 0 stubs may #[ignore] or assert!(false) until later plans turn green"
    - "ASSUME: Migration id is 00xx_packages resolved at execute against latest dialect migration (after 0010_orgs_acl)"
  prohibitions:
    - statement: "Must not implement production registry handlers, package ACL, or migrations in this Wave 0 plan"
      flagged: true
    - statement: "Must not add new crates.io or npm packages"
      flagged: true
---

<objective>
Scaffold Nyquist/Wave 0 failing Rust stubs for Phase 20 OCI, npm, generic, package ACL, package RPC, and dialect packages before implementation plans turn them green.

Purpose: Every later `<automated>` verify names a real test path (20-VALIDATION.md Wave 0). Web stubs live in 20-01.
Output: Discoverable RED/ignored nextest filters matching protocol and package_* names.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-packages-registry/20-CONTEXT.md
@.planning/phases/20-packages-registry/20-RESEARCH.md
@.planning/phases/20-packages-registry/20-VALIDATION.md
@crates/oxidean-api/tests/git_smart_http.rs
@crates/oxidean-api/tests/pat_rpc.rs
@crates/oxidean-db/tests/dialect_pats.rs
@crates/oxidean-db/tests/dialect_orgs.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → temp DB / PACKAGES_DIR | Parallel nextest must isolate blob store + DB |
| Stub assertions → production auth | Cookie ignore, anonymous public pull, Admin delete encoded before impl |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Spoofing | oci/npm stubs | high | mitigate | Stubs expect PAT Basic/Bearer only; session Cookie ignored on registry paths (D-PKG-04, Phase 8 D-12 lesson) |
| T-20-02 | Elevation of privilege | package_acl stubs | high | mitigate | Stubs expect classic `repo` alone does not grant package access; require package:read/write (D-PKG-04 fail-closed) |
| T-20-03 | Tampering | generic/npm stubs | high | mitigate | Stubs expect 409 on version overwrite (D-PKG-10) |
| T-20-SC | Tampering | cargo/npm installs | high | mitigate | Wave 0 adds test files only; no new packages |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (20-00-T1): Protocol + ACL + RPC Wave 0 stubs</name>
  <files>crates/oxidean-api/tests/oci_registry.rs, crates/oxidean-api/tests/npm_registry.rs, crates/oxidean-api/tests/generic_registry.rs, crates/oxidean-api/tests/package_acl.rs, crates/oxidean-api/tests/package_rpc.rs</files>
  <read_first>.planning/phases/20-packages-registry/20-VALIDATION.md, .planning/phases/20-packages-registry/20-RESEARCH.md, crates/oxidean-api/tests/git_smart_http.rs, crates/oxidean-api/tests/pat_rpc.rs</read_first>
  <action>
Add Wave 0 integration stubs (Phase 08/10 style: compile, discoverable by nextest, intentionally fail or #[ignore] until later plans). Per D-PKG-01, D-PKG-13, D-PKG-15: oci_registry.rs names tests matching filter test(oci_registry) covering GET /v2/ discovery, public anonymous blob/manifest pull, auth push of manifest+blob, tags/list, digest immutable conflict, tag retarget allowed, manifest delete Admin, Cookie header ignored. Per D-PKG-14: npm_registry.rs covering packument GET, publish PUT with attachment, tarball GET via OXIDEAN_PUBLIC_ORIGIN URL, dist-tags, deprecate, search /-/v1/search, version overwrite conflict. Per D-PKG-16: generic_registry.rs covering PUT/GET/DELETE /generic/{owner}/{name}/{version}/{file}, 409 overwrite, version delete removes files. Per D-PKG-04..06: package_acl.rs covering anonymous public pull OK; private pull needs Read+ and package:read; publish needs Write+ and package:write; delete needs Admin and package:write; classic repo scope alone denied. Per D-PKG-05, D-PKG-12: package_rpc.rs covering packages.list by owner/repo link; packages.deleteVersion requires Admin session and confirm string name@version. Do not implement handlers here.
  </action>
  <verify>
    <automated>test -f crates/oxidean-api/tests/oci_registry.rs && test -f crates/oxidean-api/tests/npm_registry.rs && test -f crates/oxidean-api/tests/generic_registry.rs && test -f crates/oxidean-api/tests/package_acl.rs && test -f crates/oxidean-api/tests/package_rpc.rs && cargo nextest list -p oxidean-api -E 'test(oci_registry) | test(npm_registry) | test(generic_registry) | test(package_acl) | test(package_rpc)' | rg -q 'oci_registry|npm_registry|generic_registry|package_acl|package_rpc'</automated>
  </verify>
  <done>Rust Wave 0 protocol/ACL/RPC stubs exist and are listed by nextest</done>
</task>

<task type="auto">
  <name>Task 2 (20-00-T2): dialect_packages Wave 0 stub</name>
  <files>crates/oxidean-db/tests/dialect_packages.rs</files>
  <read_first>crates/oxidean-db/tests/dialect_pats.rs, crates/oxidean-db/tests/dialect_orgs.rs, .planning/phases/20-packages-registry/20-RESEARCH.md</read_first>
  <action>
Add dialect_packages.rs expecting tri-dialect packages migration (placeholder name 00xx_packages) with tables for packages, package_versions, package_blobs, package_blob_refs, and package_quota_overrides (or equivalent names from RESEARCH Pattern 5). Mirror dialect_pats/dialect_orgs parity style. Do not write migrations here — stub asserts expected object names only. Resolve exact migration number later in 20-02.
  </action>
  <verify>
    <automated>test -f crates/oxidean-db/tests/dialect_packages.rs && cargo nextest list -p oxidean-db -E 'test(dialect_packages)' | rg -q 'dialect_packages'</automated>
  </verify>
  <done>dialect_packages stub is discoverable by nextest</done>
</task>

</tasks>

<verification>
Wave 0 nextest list filters find oci_/npm_/generic_/package_*/dialect_packages stubs per 20-VALIDATION.md.
</verification>

<success_criteria>
All Wave 0 Rust stub files exist and are listed by nextest; no production registry behavior shipped in this plan.
</success_criteria>

<output>
Create `.planning/phases/20-packages-registry/20-00-SUMMARY.md` when done
</output>
