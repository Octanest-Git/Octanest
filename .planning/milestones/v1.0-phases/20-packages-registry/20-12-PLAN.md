---
phase: 20-packages-registry
plan: "12"
type: execute
wave: 8
depends_on:
  - "05"
  - "06"
  - "07"
  - "08"
  - "09"
  - "10"
  - "11"
files_modified:
  - docs/CONFIGURATION.md
  - docs/API.md
  - docs/ARCHITECTURE.md
  - scripts/smoke-packages.sh
  - Makefile
  - .planning/phases/20-packages-registry/20-VALIDATION.md
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
  - PKG-04
  - PKG-05
autonomous: true
user_setup: []
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Docs describe /v2 /npm /generic, PACKAGES_DIR, PAT package scopes, and PUBLIC_ORIGIN tarball/clone-style URLs"
    - "make smoke-packages proves edge routing (skip-ok without Docker) and phase automated gates are green"
  artifacts:
    - path: docs/CONFIGURATION.md
      provides: "Registry env + reverse-proxy rules"
    - path: scripts/smoke-packages.sh
      provides: "Greened smoke for registry prefixes"
  key_links:
    - from: docs/API.md
      to: /v2 /npm /generic + packages.* RPC
      via: "Operator/developer contract"
  assumptions:
    - "ASSUME: Phase 15 transfer/rename will update package owner metadata — document integration hook; do not implement transfer here"
  prohibitions:
    - statement: "Must not put production secrets in docs examples"
      flagged: true
---

<objective>
Document the packages registry, green smoke-packages against Traefik routing, refresh VALIDATION task map, and run the phase gate (PKG-01..05).

Purpose: Operators and clients can configure docker/npm/generic against Octanest; CI-ready smoke.
Output: Docs updates, smoke green (or skip-ok), VALIDATION checklist progressed, gate commands pass.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-packages-registry/20-CONTEXT.md
@.planning/phases/20-packages-registry/20-RESEARCH.md
@.planning/phases/20-packages-registry/20-VALIDATION.md
@docs/CONFIGURATION.md
@docs/API.md
@scripts/smoke-packages.sh
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → operators | No real secrets; PAT examples use redacted prefixes |
| Smoke → public origin | Health of routing only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Spoofing | smoke/docs | medium | mitigate | Document Traefik PathPrefix priority so /v2 never hits SPA |
| T-20-SC | Tampering | installs | low | accept | No package installs in this plan |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (20-12-T1): Operator and API docs for packages registry</name>
  <files>docs/CONFIGURATION.md, docs/API.md, docs/ARCHITECTURE.md</files>
  <read_first>docs/CONFIGURATION.md, docs/API.md, docs/ARCHITECTURE.md, .planning/phases/20-packages-registry/20-RESEARCH.md</read_first>
  <action>
Document D-PKG-01 path prefixes, OCTANEST_PACKAGES_DIR, quota env defaults, Traefik/Vite routing, docker login (PAT-as-password), npm registry URL /npm/{owner}/, generic curl paths, hybrid auth matrix (package:read/write; repo does not imply packages), immutability (D-PKG-10), and note that owner transfer/rename (Phase 15) must update package owner paths when that phase lands. No secrets in examples.
  </action>
  <verify>
    <automated>rg -n 'OCTANEST_PACKAGES_DIR|/v2|/npm/|/generic/|package:read|package:write' docs/CONFIGURATION.md docs/API.md docs/ARCHITECTURE.md</automated>
  </verify>
  <done>Docs cover registry endpoints, env, and auth matrix</done>
</task>

<task type="auto">
  <name>Task 2 (20-12-T2): Smoke green + VALIDATION refresh + phase gate</name>
  <files>scripts/smoke-packages.sh, Makefile, .planning/phases/20-packages-registry/20-VALIDATION.md</files>
  <read_first>scripts/smoke-packages.sh, .planning/phases/20-packages-registry/20-VALIDATION.md</read_first>
  <action>
Update smoke-packages.sh to assert /v2/ reaches API (not SPA HTML), and /npm|/generic PathPrefix routing when Compose is up; keep skip-ok without Docker. Mark Wave 0 checklist items complete in 20-VALIDATION.md where greened; refresh per-task status rows to reflect execution. Run phase gate: cargo nextest for package filters, make rpc-sync-check, dialect_packages, web packages Vitest, make smoke-packages. Do not set nyquist_compliant true (owned by validate-phase).
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(oci_registry) | test(npm_registry) | test(generic_registry) | test(package_acl) | test(package_rpc)' && cargo test -p octanest-db --test dialect_packages && make rpc-sync-check && bun --cwd apps/web exec vitest run src/routes/\$owner.packages.integration.test.ts src/routes/\$owner.\$repo.packages.integration.test.ts src/routes/admin/packages.integration.test.ts src/routes/settings/tokens.packages.integration.test.ts && make smoke-packages</automated>
  </verify>
  <done>Phase gate green; smoke and VALIDATION updated for PKG-01..05</done>
</task>

</tasks>

<verification>
Docs + smoke + automated package filters prove Phase 20 success criteria.
</verification>

<success_criteria>
All PKG-01..05 behaviors have greened automated coverage or documented skip-ok smoke; docs publish client/operator contracts.
</success_criteria>

<output>
Create `.planning/phases/20-packages-registry/20-12-SUMMARY.md` when done
</output>
