---
phase: 20-packages-registry
plan: "04"
type: execute
wave: 3
depends_on:
  - "03"
files_modified:
  - crates/oxidean-api/src/packages/generic.rs
  - crates/oxidean-api/src/packages/oci.rs
  - crates/oxidean-api/src/packages/npm.rs
  - crates/oxidean-api/src/packages/mod.rs
  - crates/oxidean-api/src/app.rs
  - crates/oxidean-api/tests/generic_registry.rs
requirements:
  - PKG-03
  - PKG-04
autonomous: true
user_setup: []
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "User can PUT and GET a generic file at /generic/{owner}/{name}/{version}/{filename} with PAT auth (PKG-03, D-PKG-16)"
    - "Overwrite of an existing generic version returns 409; delete removes the version (D-PKG-10)"
    - "/v2, /npm, and /generic are mounted on the Axum router (D-PKG-01); GET /v2/ answers registry discovery"
  artifacts:
    - path: crates/oxidean-api/src/packages/generic.rs
      provides: "Fully featured generic handlers for tracer path"
    - path: crates/oxidean-api/src/app.rs
      provides: "Mounts for /v2 /npm /generic"
  key_links:
    - from: crates/oxidean-api/src/packages/generic.rs
      to: crates/oxidean-api/src/packages/store.rs
      via: "CA blob commit on PUT"
    - from: crates/oxidean-api/src/packages/generic.rs
      to: crates/oxidean-api/src/packages/acl.rs
      via: "Write+ publish; Admin delete; anon public pull"
  assumptions:
    - "ASSUME: After delete, republish of same generic version is allowed (D-PKG-10 reading)"
    - "ASSUME: OCI/npm modules in this plan only stub discovery/404 until 20-05/20-06 — architecture mounts are real"
  prohibitions:
    - statement: "Must not overwrite an existing generic version in place"
      flagged: true
    - statement: "Must not use session cookies for /generic auth"
      flagged: true
---

<objective>
Tracer bullet: wire one generic publish/pull/delete path through ACL, store, DB, and Axum mounts for /generic, while mounting /v2 and /npm prefixes so edge routing is end-to-end proven (PKG-03, D-PKG-01, D-PKG-16).

Purpose: Prove architecture on one format before expanding OCI and npm.
Output: Working generic registry + router mounts; generic_registry tests green for happy path and immutability conflict.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-packages-registry/20-RESEARCH.md
@.planning/phases/20-packages-registry/20-CONTEXT.md
@crates/oxidean-api/src/packages/store.rs
@crates/oxidean-api/src/packages/acl.rs
@crates/oxidean-api/src/app.rs
@crates/oxidean-api/tests/generic_registry.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curl/CI → /generic | PAT Basic; public GET may be anon |
| Version id → blob refs | Immutable while present |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-04 | Tampering | generic PUT | high | mitigate | Reject overwrite with 409 (D-PKG-10); Admin-only DELETE |
| T-20-02 | Elevation of privilege | generic auth | high | mitigate | Enforce Write+/package:write on PUT; ignore Cookie |
| T-20-SC | Tampering | installs | low | accept | No new packages |
</threat_model>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (20-04-T1): End-to-end generic PUT/GET/DELETE — one path</name>
  <files>crates/oxidean-api/src/packages/generic.rs, crates/oxidean-api/src/packages/mod.rs, crates/oxidean-api/src/app.rs, crates/oxidean-api/tests/generic_registry.rs</files>
  <read_first>.planning/phases/20-packages-registry/20-RESEARCH.md Pattern 3, crates/oxidean-api/src/packages/acl.rs, crates/oxidean-api/src/packages/store.rs, crates/oxidean-api/tests/generic_registry.rs</read_first>
  <behavior>
    - Authenticated PUT stores file bytes and creates package+version metadata under owner/name
    - GET returns bytes for public anon or authorized private
    - Second PUT same version → 409
    - DELETE version (Admin + package:write) removes files; republish allowed after delete
  </behavior>
  <action>
Implement generic.rs handlers for PUT/GET/DELETE /generic/{owner}/{name}/{version}/{filename} per D-PKG-16 and D-PKG-03. Wire through packages/acl.rs and store.rs with real error handling on this single path. Mount packages router in app.rs for /generic. Also mount /v2 and /npm prefixes (oci.rs may only implement GET /v2/ returning 200 empty API version; npm.rs may return 404 Not Found for all paths) so Traefik paths hit Axum not SPA — fill without architectural change in 20-05/20-06. Green generic_registry.rs happy path + immutability + auth cases from Wave 0.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(generic_registry)'</automated>
  </verify>
  <done>Generic publish/pull/delete works end-to-end; mounts exist for all three prefixes</done>
</task>

<task type="auto">
  <name>Task 2 (20-04-T2): Generic list + owner package metadata for tracer</name>
  <files>crates/oxidean-api/src/packages/generic.rs, crates/oxidean-db/src/packages.rs, crates/oxidean-api/tests/generic_registry.rs</files>
  <read_first>crates/oxidean-api/src/packages/generic.rs, crates/oxidean-db/src/packages.rs</read_first>
  <action>
Ensure listing files for a generic package version works (GET without filename or dedicated list path per RESEARCH discretion) and package row records format=generic, owner, optional repository_id when provided on first publish. Extend generic_registry tests for list. Keep OCI/npm stubs untouched beyond mount health.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(generic_registry)'</automated>
  </verify>
  <done>Generic list and metadata recorded; tracer slice complete for PKG-03</done>
</task>

</tasks>

<verification>
generic_registry green; curl-level generic path works through API; /v2/ discovery responds from Axum.
</verification>

<success_criteria>
PKG-03 tracer path ships production-quality generic registry; prefixes mounted for OCI/npm expansion.
</success_criteria>

<output>
Create `.planning/phases/20-packages-registry/20-04-SUMMARY.md` when done
</output>
