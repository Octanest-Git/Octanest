---
phase: 20-packages-registry
plan: "07"
type: execute
wave: 5
depends_on:
  - "06"
files_modified:
  - crates/oxidean-api/src/packages/npm.rs
  - crates/oxidean-api/tests/npm_registry.rs
requirements:
  - PKG-02
autonomous: true
user_setup: []
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "npm dist-tags can be GET/PUT/DELETE under /npm/{owner}/-/package/{package}/dist-tags (D-PKG-14)"
    - "Package deprecate fields update via publish PUT without replacing tarball immutably (D-PKG-14)"
    - "/-/v1/search returns matching packages for the owner registry (D-PKG-14)"
  artifacts:
    - path: crates/oxidean-api/src/packages/npm.rs
      provides: "dist-tags, deprecate, search handlers"
  key_links:
    - from: dist-tags mutations
      to: packages/acl.rs
      via: "Write+ and package:write (same as publish)"
  assumptions: []
  prohibitions:
    - statement: "Must not overwrite immutable version tarball bytes when updating deprecate metadata"
      flagged: true
---

<objective>
Complete D-PKG-14 npm surface: dist-tags, deprecate, and search on top of publish/install from 20-06.

Purpose: Fully featured npm registry — not publish-only.
Output: Remaining npm_registry Wave 0 cases green.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-packages-registry/20-RESEARCH.md
@crates/oxidean-api/src/packages/npm.rs
@crates/oxidean-api/tests/npm_registry.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dist-tag write → package | Same Write+/package:write as publish |
| search → package list | Respect visibility; no private leak to anon |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-03 | Information disclosure | search | medium | mitigate | Anon search returns public packages only for that owner |
| T-20-02 | Elevation of privilege | dist-tags | high | mitigate | Mutations require Write+ ∩ package:write |
| T-20-SC | Tampering | installs | low | accept | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (20-07-T1): dist-tags + deprecate</name>
  <files>crates/oxidean-api/src/packages/npm.rs, crates/oxidean-api/tests/npm_registry.rs</files>
  <read_first>.planning/phases/20-packages-registry/20-RESEARCH.md npm API subset, crates/oxidean-api/src/packages/npm.rs</read_first>
  <behavior>
    - GET/PUT/DELETE dist-tags paths under /-/package/{package}/dist-tags[/{tag}]
    - Deprecate updates version metadata without replacing tarball
  </behavior>
  <action>
Implement dist-tags and deprecate per D-PKG-14. Keep version tarball immutability (D-PKG-10). Green corresponding npm_registry tests.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(npm_registry)'</automated>
  </verify>
  <done>dist-tags and deprecate green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (20-07-T2): npm /-/v1/search</name>
  <files>crates/oxidean-api/src/packages/npm.rs, crates/oxidean-api/tests/npm_registry.rs</files>
  <read_first>crates/oxidean-api/src/packages/npm.rs</read_first>
  <behavior>
    - GET /npm/{owner}/-/v1/search?text=&size=&from= returns objects matching text
    - Private packages omitted for anonymous callers
  </behavior>
  <action>
Implement search endpoint per D-PKG-14 and npm Registry API shape. Green search npm_registry cases so PKG-02 / D-PKG-14 is complete.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(npm_registry)'</automated>
  </verify>
  <done>npm_registry fully green including search</done>
</task>

</tasks>

<verification>
All npm_registry tests green — D-PKG-14 complete.
</verification>

<success_criteria>
npm registry supports publish/install plus dist-tags, deprecate, and search.
</success_criteria>

<output>
Create `.planning/phases/20-packages-registry/20-07-SUMMARY.md` when done
</output>
