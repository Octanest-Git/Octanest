---
phase: 20-packages-registry
plan: "03"
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - crates/oxidean-api/src/packages/mod.rs
  - crates/oxidean-api/src/packages/store.rs
  - crates/oxidean-api/src/packages/acl.rs
  - crates/oxidean-api/src/packages/auth.rs
  - crates/oxidean-core/src/pat_types.rs
  - crates/oxidean-api/src/pat/mod.rs
  - crates/oxidean-db/src/packages.rs
  - crates/oxidean-db/src/lib.rs
  - crates/oxidean-api/tests/package_acl.rs
  - packages/api-client/src/index.ts
requirements:
  - PKG-04
autonomous: true
user_setup: []
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Content-addressed blobs live under OXIDEAN_PACKAGES_DIR with digest sharding and DB refcounts (D-PKG-07, D-PKG-08)"
    - "Package access = owner Capability ACL intersected with PAT package:read/write; classic repo alone denied (D-PKG-04..06)"
    - "Registry auth ignores session cookies; anonymous pull allowed only for public packages (D-PKG-05)"
  artifacts:
    - path: crates/oxidean-api/src/packages/store.rs
      provides: "CA blob put/get/refcount helpers"
    - path: crates/oxidean-api/src/packages/acl.rs
      provides: "package Capability + visibility checks"
    - path: crates/oxidean-core/src/pat_types.rs
      provides: "ClassicPatScope PackageRead/PackageWrite + FG PackagesPerm"
  key_links:
    - from: crates/oxidean-api/src/packages/acl.rs
      to: crates/oxidean-api/src/repo/acl.rs
      via: "Reuse Capability ladder Read/Write/Admin for owner"
    - from: crates/oxidean-api/src/packages/auth.rs
      to: crates/oxidean-api/src/routes/git_smart_http.rs
      via: "PAT Basic patterns; Cookie ignored"
  assumptions:
    - "ASSUME: Linked package inherits repo visibility/ACL; unlinked uses package.visibility default private"
    - "ASSUME: Delete uses package:write + Admin Capability (no separate package:delete scope)"
  prohibitions:
    - statement: "Must not grant package access from classic repo scope alone"
      flagged: true
    - statement: "Must not authenticate registry paths with session cookies"
      flagged: true
    - statement: "Must not implement full OCI/npm/generic protocol handlers here — shared subsystem only"
      flagged: true
---

<objective>
Build the shared packages subsystem: content-addressed blob store, package ACL helpers, and hybrid PAT package:read/write scopes that protocol adapters will call (PKG-04, D-PKG-04…09 storage/auth pieces).

Purpose: One store + one auth matrix for three formats — avoid three products.
Output: packages/{store,acl,auth}.rs, db packages module, extended pat_types + rpc-gen, package_acl tests greening core matrix rows.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-packages-registry/20-RESEARCH.md
@.planning/phases/08-git-https-pats/08-CONTEXT.md
@.planning/phases/10-orgs-permissions/10-CONTEXT.md
@crates/oxidean-api/src/repo/acl.rs
@crates/oxidean-api/src/routes/git_smart_http.rs
@crates/oxidean-core/src/pat_types.rs
@crates/oxidean-api/src/pat/mod.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry client → PAT auth | Basic/Bearer only; Cookie discarded |
| PAT scopes → package Capability | Intersection fail-closed |
| Upload stream → blob path | Digest regex; reject path traversal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-02 | Elevation of privilege | pat_types + acl | high | mitigate | Require package:read/write; repo scope does not imply packages (D-PKG-04) |
| T-20-03 | Information disclosure | package acl | high | mitigate | Private deny anonymous; registry 401 (not web soft-404) for clients |
| T-20-10 | Tampering | store paths | high | mitigate | Strict sha256 digest validation; shard only hex; never trust client paths |
| T-20-SC | Tampering | installs | low | accept | Reuse sha2/uuid/tempfile already in tree |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (20-03-T1): Content-addressed package blob store</name>
  <files>crates/oxidean-api/src/packages/mod.rs, crates/oxidean-api/src/packages/store.rs, crates/oxidean-db/src/packages.rs, crates/oxidean-db/src/lib.rs</files>
  <read_first>.planning/phases/20-packages-registry/20-RESEARCH.md, crates/oxidean-db/src/lib.rs</read_first>
  <behavior>
    - put_blob stages via tempfile then commits to {PACKAGES_DIR}/{algo}/{aa}/{bb}/{digest}
    - get_blob returns bytes for known digest; missing → not found
    - refcount increments only on committed version/manifest refs; abort upload does not leak live refs
  </behavior>
  <action>
Implement packages module and store.rs per D-PKG-07/08 using sha2 for digests, tempfile staging, uuid only if needed for upload sessions later. DB helpers in oxidean-db for blob rows and refs. Do not share LFS directory. Quota check hooks may be stubs returning Ok until 20-09, but max-size constant/env read may exist.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(package_store) | test(packages::store)' && cargo test -p oxidean-db --lib -- packages</automated>
  </verify>
  <done>Blob store put/get/refcount helpers compile and unit-covered</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (20-03-T2): Package ACL + registry auth helpers</name>
  <files>crates/oxidean-api/src/packages/acl.rs, crates/oxidean-api/src/packages/auth.rs, crates/oxidean-api/tests/package_acl.rs</files>
  <read_first>crates/oxidean-api/src/repo/acl.rs, crates/oxidean-api/src/routes/git_smart_http.rs, .planning/phases/20-packages-registry/20-RESEARCH.md</read_first>
  <behavior>
    - Public package: anonymous pull allowed; publish always auth
    - Private: Read+ and package:read (or write) for pull; Write+ and package:write for publish; Admin and package:write for delete
    - Cookie-only request treated as anonymous
  </behavior>
  <action>
Implement acl.rs mirroring Capability ladder from repo/acl.rs for package owner (user or org) per D-PKG-04..06. Linked packages inherit repo visibility/ACL; unlinked use package.visibility. auth.rs parses PAT Basic (and prepares Bearer opaque token minting surface for 20-05) and explicitly ignores Cookie on registry paths. Green package_acl.rs matrix rows that do not require full protocol handlers (unit/integration against helpers + minimal harness).
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(package_acl)'</automated>
  </verify>
  <done>package_acl core matrix green for helper-level cases</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (20-03-T3): Extend PAT classic + FG package scopes</name>
  <files>crates/oxidean-core/src/pat_types.rs, crates/oxidean-api/src/pat/mod.rs, packages/api-client/src/index.ts</files>
  <read_first>crates/oxidean-core/src/pat_types.rs, crates/oxidean-api/src/pat/mod.rs</read_first>
  <behavior>
    - ClassicPatScope serializes package:read and package:write
    - Fine-grained PackagesPerm { Read, Write } available on create/list DTOs
    - createClassic/createFineGrained accept and persist new scopes
  </behavior>
  <action>
Per D-PKG-04 extend ClassicPatScope with PackageRead/PackageWrite (serde rename package:read / package:write) and add PackagesPerm for FG tokens. Wire persistence + validation in pat module. Fail closed: existing tokens with only repo keep git access only. Run make rpc-gen so api-client updates; do not hand-edit generated client as source of truth.
  </action>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && cargo nextest run -p oxidean-api -E 'test(pat_)'</automated>
  </verify>
  <done>PAT types include package scopes; rpc-sync-check clean; existing pat_ tests still pass</done>
</task>

</tasks>

<verification>
Shared store + ACL + PAT package scopes ready for protocol tracer.
</verification>

<success_criteria>
PKG-04 hybrid auth foundations and CA store exist; package_acl tests cover matrix; rpc-gen synced.
</success_criteria>

<output>
Create `.planning/phases/20-packages-registry/20-03-SUMMARY.md` when done
</output>
