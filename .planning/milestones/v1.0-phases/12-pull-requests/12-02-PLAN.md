---
phase: 12-pull-requests
plan: "02"
type: execute
wave: 1
depends_on:
  - "00"
files_modified:
  - crates/oxidean-db/migrations/sqlite/0016_pull_requests.sql
  - crates/oxidean-db/migrations/postgres/0016_pull_requests.sql
  - crates/oxidean-db/migrations/mysql/0016_pull_requests.sql
  - crates/oxidean-db/src/pulls.rs
  - crates/oxidean-db/src/lib.rs
  - crates/oxidean-core/src/pull_types.rs
  - crates/oxidean-core/src/lib.rs
  - crates/oxidean-core/src/issue_types.rs
  - crates/oxidean-git/src/backend.rs
  - crates/oxidean-git/src/cli.rs
  - crates/oxidean-db/tests/dialect_pulls.rs
requirements:
  - PR-01
  - PR-05
  - PR-07
autonomous: true
estimate:
  tokens: 48000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "Tri-dialect 0016_pull_requests lands; dialect_pulls green"
    - "GitBackend exposes merge_commit/squash_merge/rebase_merge with unit tests"
    - "pull_types DTOs + IssueLinkKind::Pr compile"
  assumptions:
    - "ASSUME: migration 0016 after 0015_packages"
    - "ASSUME: shared numbering via allocate_next_issue_number"
    - "ASSUME: forked_from_repo_id nullable for D-PR-01 (SOC-04 UI later)"
  prohibitions:
    - statement: "Must not ship pull.* RPC handlers or UI in this plan"
      flagged: true
---

<objective>
Land schema, core DTOs, DB helpers, and GitBackend merge operations for Phase 12 (foundation before tracer RPC).
</objective>

<context>
@.planning/phases/12-pull-requests/12-RESEARCH.md
@crates/oxidean-db/migrations/sqlite/0011_issues.sql
@crates/oxidean-git/src/backend.rs
@crates/oxidean-git/src/cli.rs
@crates/oxidean-core/src/issue_types.rs
</context>

<threat_model>
| Threat ID | Category | Severity | Disposition | Mitigation |
|-----------|----------|----------|-------------|------------|
| T-12-04 | Tampering merge | high | mitigate | Merge only via GitBackend; conflict → error |
| T-12-05 | Spoofing fork parent | medium | mitigate | forked_from_repo_id FK; validate head is self or fork of base |
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1 (12-02-T1): Tri-dialect 0016_pull_requests + dialect_pulls green</name>
  <files>crates/oxidean-db/migrations/sqlite/0016_pull_requests.sql, crates/oxidean-db/migrations/postgres/0016_pull_requests.sql, crates/oxidean-db/migrations/mysql/0016_pull_requests.sql, crates/oxidean-db/tests/dialect_pulls.rs</files>
  <read_first>.planning/phases/12-pull-requests/12-RESEARCH.md, crates/oxidean-db/migrations/sqlite/0011_issues.sql, crates/oxidean-db/tests/dialect_issues.rs</read_first>
  <action>
Create 0016_pull_requests for sqlite/postgres/mysql per RESEARCH schema: repositories columns allow_merge_commit/allow_squash_merge/allow_rebase_merge (default true) + forked_from_repo_id; pull_requests; pull_comments (general+line anchors+outdated+resolved); pull_reviews; pull_review_requests; pull_labels; pull_assignees; expand issue_links kind to include 'pr' (keep pr_stub). Green dialect_pulls assertions. CASCADE from repositories.
  </action>
  <verify>
    <automated>test -f crates/oxidean-db/migrations/sqlite/0016_pull_requests.sql && cargo nextest run -p oxidean-db -E 'test(dialect_pulls)'</automated>
  </verify>
  <done>0016 tri-dialect present; dialect_pulls green</done>
</task>

<task type="auto">
  <name>Task 2 (12-02-T2): pull_types + DB helpers + IssueLinkKind::Pr</name>
  <files>crates/oxidean-core/src/pull_types.rs, crates/oxidean-core/src/lib.rs, crates/oxidean-core/src/issue_types.rs, crates/oxidean-db/src/pulls.rs, crates/oxidean-db/src/lib.rs</files>
  <read_first>crates/oxidean-core/src/issue_types.rs, crates/oxidean-db/src/issues.rs, crates/oxidean-db/src/lib.rs</read_first>
  <action>
Add pull_types (PullPublic, states, review states, merge method enum, requests/responses) with specta derives matching issue_types style. Add IssueLinkKind::Pr ("pr"). Implement oxidean-db pulls module: insert/get/list/update state, comments, reviews, settings getters/setters, allocate number via existing issue_counters. Export from lib.rs. Compile-only; no RPC.
  </action>
  <verify>
    <automated>cargo check -p oxidean-core -p oxidean-db</automated>
  </verify>
  <done>Types + DB helpers compile</done>
</task>

<task type="auto">
  <name>Task 3 (12-02-T3): GitBackend merge/squash/rebase</name>
  <files>crates/oxidean-git/src/backend.rs, crates/oxidean-git/src/cli.rs</files>
  <read_first>crates/oxidean-git/src/backend.rs, crates/oxidean-git/src/cli.rs</read_first>
  <action>
Extend GitBackend with merge_commit, squash_merge, rebase_merge (bare repo + temp worktree pattern like seed_commit). Return resulting SHA or GitError on conflict. Add optional fetch_ref_from for cross-repo objects. Unit tests merge_* in cli.rs (or tests module) covering happy path + conflict. Do not wire API handlers yet.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-git -E 'test(merge_)'</automated>
  </verify>
  <done>GitBackend merge methods green</done>
</task>
</tasks>

<verification>
Schema + types + git merge foundation ready for tracer.
</verification>

<success_criteria>
dialect_pulls green; cargo check core/db; git merge_* tests green.
</success_criteria>

<output>
Create `.planning/phases/12-pull-requests/12-02-SUMMARY.md` when done
</output>
