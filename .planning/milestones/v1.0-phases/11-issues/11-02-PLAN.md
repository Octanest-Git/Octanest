---
phase: 11-issues
plan: "02"
type: execute
wave: 1
depends_on:
  - "00"
  - "01"
files_modified:
  - crates/oxidean-db/migrations/postgres/0011_issues.sql
  - crates/oxidean-db/migrations/mysql/0011_issues.sql
  - crates/oxidean-db/migrations/sqlite/0011_issues.sql
  - crates/oxidean-db/src/issues.rs
  - crates/oxidean-db/src/issue_labels.rs
  - crates/oxidean-db/src/lib.rs
  - crates/oxidean-db/tests/dialect_issues.rs
  - crates/oxidean-core/src/issue_types.rs
  - crates/oxidean-core/src/lib.rs
requirements:
  - ISS-01
  - ISS-02
  - ISS-03
  - ISS-04
autonomous: false
user_setup: []
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Tri-dialect 0011_issues lands issues + issue_counters + comments/revisions/labels/assignees/reactions/links with CASCADE from repositories (ISS-01..04, D-ISS-01)"
    - "issue_counters allocates monotonic #N without reclaim on delete; UNIQUE(repo_id, number)"
    - "issue_types DTOs compile; dialect_issues asserts 0011 shape"
  artifacts:
    - path: crates/oxidean-db/migrations/postgres/0011_issues.sql
      provides: "Issues domain schema"
    - path: crates/oxidean-db/src/issues.rs
      provides: "insert issue + counter helpers"
    - path: crates/oxidean-core/src/issue_types.rs
      provides: "Issue DTOs for later issue.* RPC"
  key_links:
    - from: crates/oxidean-db/src/issues.rs
      to: crates/oxidean-db/migrations/postgres/0011_issues.sql
      via: "counter upsert in same txn as insert"
  assumptions:
    - "ASSUME (discretion): label merge model is inherit org labels + repo hide + repo local-only; unique name per scope (D-ISS-05)"
    - "ASSUME (discretion): reaction content enum is GitHub eight (+1,-1,laugh,confused,heart,hooray,rocket,eyes) (D-ISS-11)"
    - "ASSUME (discretion): issue_links kind includes pr_stub with opaque id + target_number + optional title (D-ISS-13)"
    - "ASSUME: Migration number is 0011_issues after 0010_orgs_acl"
  prohibitions:
    - statement: "Must not place dialect SQL in oxidean-api"
      flagged: true
    - statement: "Must not implement issue.* RPC or Issues UI in this plan — deferred to 11-03+"
      flagged: true
    - statement: "Must not reclaim issue numbers after hard-delete"
      flagged: true
---

<objective>
Confirm the one-way per-repo `#N` schema door (D-ISS-01), then land tri-dialect 0011_issues plus DB helpers and issue_types (no RPC/UI yet).

Purpose: Unlock numbering + collaboration tables before the create/list/get tracer in plan 03.
Output: Green dialect_issues migration assertions; insert issue + counter helpers ready for RPC.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-issues/11-CONTEXT.md
@.planning/phases/11-issues/11-RESEARCH.md
@.planning/phases/11-issues/11-VALIDATION.md
@crates/oxidean-db/migrations/postgres/0010_orgs_acl.sql
@crates/oxidean-db/src/repositories.rs
@crates/oxidean-core/src/repo_types.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → repositories | Issue FKs must CASCADE so factory reset remains safe |
| Public #N URLs | Numbering door is one-way once issues ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Tampering | issue_counters | high | mitigate | Monotonic max_number in txn; UNIQUE(repo_id, number); never decrement on delete (D-ISS-01, D-ISS-02) |
| T-11-05 | Tampering / residual data | FK cascades | high | mitigate | ON DELETE CASCADE from repositories (and org for org labels) so reset wipes issue rows |
| T-11-SC | Tampering | packages | high | mitigate | No new crates.io/npm packages |
</threat_model>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 0 (11-02-T0): Confirm D-ISS-01 numbering + 0011 schema door</name>
  <decision>
Confirm proceed with RESEARCH discretion for Phase 11 schema:

1. Migration id **0011_issues** (tri-dialect) after **0010_orgs_acl**.
2. Per-repo sequential `#N` via **issue_counters(repo_id PK, max_number)** upserted in the same transaction as issue insert; hard-delete does not reclaim numbers (D-ISS-01, D-ISS-02).
3. Tables in same migration: issues, issue_counters, issue_comments, issue_revisions, comment_revisions, labels (org + repo scope + hide overrides), issue_labels, issue_assignees, issue_reactions, comment_reactions, issue_links — even if some RPCs land later.
4. Discretion locks: GitHub eight reaction contents; label inherit+hide+local-only; pr_stub link rows; RPC namespace `issue.*` + `label.*` (handlers in later plans).

Locked product decision D-ISS-01 (per-repo `#N` URLs) is not reopened — only the physical counter-table shape above.
  </decision>
  <context>
One-way door: public URLs `/{owner}/{repo}/issues/{n}` and DB sequences are permanent once issues exist. RESEARCH prefers Gitea-style counters over MAX(number)+1.
  </context>
  <options>
    <option id="proceed_0011">
      <title>Proceed with 0011_issues + issue_counters (recommended)</title>
      <pros>Matches RESEARCH; avoids number reuse; single migration for issue domain</pros>
      <cons>Undo requires data migration after any issues exist</cons>
    </option>
    <option id="revise">
      <title>Stop — revise numbering/schema before tracer</title>
      <pros>Allows alternate design if preferred</pros>
      <cons>Blocks tracer until replan</cons>
    </option>
  </options>
  <resume-signal>Reply with proceed_0011 (recommended) or revise</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1 (11-02-T1): 0011_issues + DB helpers + issue_types</name>
  <files>crates/oxidean-db/migrations/postgres/0011_issues.sql, crates/oxidean-db/migrations/mysql/0011_issues.sql, crates/oxidean-db/migrations/sqlite/0011_issues.sql, crates/oxidean-db/src/issues.rs, crates/oxidean-db/src/issue_labels.rs, crates/oxidean-db/src/lib.rs, crates/oxidean-core/src/issue_types.rs, crates/oxidean-core/src/lib.rs, crates/oxidean-db/tests/dialect_issues.rs</files>
  <read_first>.planning/phases/11-issues/11-RESEARCH.md, crates/oxidean-db/src/repositories.rs, crates/oxidean-db/migrations/postgres/0010_orgs_acl.sql, crates/oxidean-core/src/repo_types.rs</read_first>
  <behavior>
    - dialect_issues asserts tri-dialect 0011_issues tables/columns for issues, issue_counters, comments, revisions, labels, assignees, reactions, links
    - allocate_next_number + insert issue helpers succeed on sqlite harness; deleted numbers are not reused
  </behavior>
  <action>
After T0 proceed_0011: land tri-dialect 0011_issues per D-ISS-01 (per-repo #N counters) covering lifecycle/open-closed columns (D-ISS-02), revision tables for issue+comment history (D-ISS-04, D-ISS-12), dual-scope labels (D-ISS-05), M:N assignees (D-ISS-06), reaction rows for eight contents (D-ISS-11), and issue_links including pr_stub (D-ISS-13). Implement Database helpers for counter allocate + insert issue (+ minimal label/assignee insert helpers as needed for later plans). issue_types DTOs/enums (no RPC handlers). Green dialect_issues. Do not ship issue.create, rpc-gen, or Issues UI here (plan 03).
  </action>
  <verify>
    <automated>cargo test -p oxidean-db --lib migration_parity && cargo test -p oxidean-db --test dialect_issues</automated>
  </verify>
  <done>0011 migrates on all dialects; issue + counter helpers and issue_types compile</done>
</task>

</tasks>

<verification>
Schema door confirmed; dialect_issues green; RPC/UI deferred to 11-03.
</verification>

<success_criteria>
ISS-01..04 schema foundation ready; monotonic #N counters; no issue.* RPC yet.
</success_criteria>

<output>
Create `.planning/phases/11-issues/11-02-SUMMARY.md` when done
</output>
