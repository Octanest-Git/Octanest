---
phase: 06-self-host-admin-bootstrap
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - crates/oxidean-api/src/auth/bootstrap.rs
  - crates/oxidean-api/src/auth/local.rs
  - crates/oxidean-api/src/rpc.rs
  - crates/oxidean-api/src/routes/auth_callbacks.rs
  - crates/oxidean-api/tests/auth_bootstrap.rs
requirements:
  - AUTH-07
autonomous: true
user_setup: []
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Empty users + ADMIN ENV absent/incomplete → needs_setup true and auth.bootstrap_setup creates auto-verified sys-admin + session (AUTH-07, D-09–D-12)"
    - "Wizard persists allow_signup from BootstrapSetupRequest (D-08); default off when client omits/false"
    - "While needs_setup, RPC allowlist is only auth.bootstrap_status, auth.bootstrap_setup, and system.health — others fail (D-11)"
    - "Second bootstrap_setup returns auth.setup_unavailable — idempotency (AUTH-07)"
    - "Parallel/raced setup re-checks count_users before create so only one admin wins — concurrency (AUTH-07)"
    - "SSO starts reject_if_setup_required while needs_setup (D-09/D-10)"
    - "No cloud/self-host conditionals in bootstrap module (D-01–D-04)"
  artifacts:
    - path: crates/oxidean-api/src/auth/bootstrap.rs
      provides: "needs_setup / bootstrap_status / bootstrap_setup + allow_signup"
    - path: crates/oxidean-api/src/rpc.rs
      provides: "needs_setup RPC allowlist"
    - path: crates/oxidean-api/tests/auth_bootstrap.rs
      provides: "AUTH-07 + allowlist coverage"
  key_links:
    - from: crates/oxidean-api/src/rpc.rs
      to: crates/oxidean-api/src/auth/bootstrap.rs
      via: "early needs_setup allowlist before dispatch arms"
    - from: crates/oxidean-api/src/routes/auth_callbacks.rs
      to: crates/oxidean-api/src/auth/bootstrap.rs
      via: "reject_if_setup_required"
  assumptions:
    - "ASSUME (AUTH-07 concurrency): transactional uniqueness via re-check count_users is sufficient (existing pattern); no DB advisory lock required this phase"
  prohibitions:
    - statement: "Must not allow auth.signup, auth.login, or other non-allowlisted RPCs to succeed while needs_setup"
      flagged: true
    - statement: "Must not add deployment-mode forks around wizard availability"
      flagged: true
---

<objective>
Stabilize empty-instance wizard (AUTH-07): `bootstrap_setup` with `allow_signup`, strict RPC allowlist while `needs_setup`, and SSO reject — extending the 06-02 tracer without greenfield auth.

Purpose: Lock the interactive path when ENV seed is absent/incomplete.
Output: Green auth_bootstrap tests for wizard, allowlist, and SSO gate.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-self-host-admin-bootstrap/06-02-SUMMARY.md
@.planning/phases/06-self-host-admin-bootstrap/06-CONTEXT.md
@.planning/phases/06-self-host-admin-bootstrap/06-RESEARCH.md
@.planning/phases/06-self-host-admin-bootstrap/06-PATTERNS.md
@crates/oxidean-api/src/auth/seed.rs
@crates/oxidean-api/src/auth/local.rs
@crates/oxidean-api/src/rpc.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unauthenticated client → bootstrap_setup | Creates first sys-admin |
| Unauthenticated client → other RPCs | Must not bypass empty-instance lock |
| SSO start → IdP redirect | Must not proceed during needs_setup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Elevation of privilege | RPC during needs_setup | high | mitigate | Central allowlist in rpc::dispatch (D-11); reject with clear AppError |
| T-06-07 | Elevation of privilege | Race two wizards | high | mitigate | Re-check count_users before create; second gets setup_unavailable |
| T-06-08 | Spoofing | SSO during setup | high | mitigate | reject_if_setup_required on WorkOS/OIDC starts |
| T-06-SC | Tampering | cargo/npm installs | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (06-03-T1): bootstrap_setup + allow_signup + reserved bypass</name>
  <reversibility rating="costly">Wizard persists allow_signup as instance policy (D-08).</reversibility>
  <files>crates/oxidean-api/src/auth/bootstrap.rs, crates/oxidean-api/src/auth/local.rs, crates/oxidean-api/tests/auth_bootstrap.rs</files>
  <read_first>crates/oxidean-api/src/auth/seed.rs, crates/oxidean-api/src/auth/local.rs, crates/oxidean-api/src/auth/bootstrap.rs, .planning/phases/06-self-host-admin-bootstrap/06-RESEARCH.md, .planning/phases/06-self-host-admin-bootstrap/06-PATTERNS.md</read_first>
  <behavior>
    - needs_setup true only when count_users==0 and ADMIN ENV not fully configured
    - bootstrap_setup creates sys-admin, auto-verifies, issues session, must_change_credentials false (wizard-chosen — D-17)
    - allow_signup from request persisted on instance_auth_settings
    - reserved username bypass for bootstrap names (preserve admin-style reserved bypass pattern)
    - second setup → auth.setup_unavailable
  </behavior>
  <action>
Complete AUTH-07 wizard path on top of 06-02 bootstrap module (D-01, D-08, D-09, D-12, D-13, D-17):

1. Ensure `admin_env_configured` / `needs_setup` match D-13 (both ENV non-empty ⇒ not needs_setup; either unset ⇒ wizard when empty).
2. `bootstrap_setup`: validate input; reserved bypass per RESEARCH; create sys-admin; persist allow_signup; auto-verify; issue_session; return UserPublic; re-check emptiness for races.
3. Keep signup’s existing needs_setup hard block if present; wizard path is the only create while empty without ENV.
4. Green Wave 0 auth_bootstrap wizard + allow_signup + idempotency tests.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(bootstrap)'</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'bootstrap_setup|needs_setup|allow_signup' crates/oxidean-api/src/auth/bootstrap.rs`
    - `rg -n 'setup_unavailable' crates/oxidean-api/src/auth/bootstrap.rs`
    - nextest `test(bootstrap)` exits 0
  </acceptance_criteria>
  <done>Wizard creates first admin with allow_signup persistence and idempotent reject</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (06-03-T2): Strict RPC allowlist + SSO setup reject</name>
  <reversibility rating="costly">Empty-instance RPC surface becomes a security contract (D-11).</reversibility>
  <files>crates/oxidean-api/src/rpc.rs, crates/oxidean-api/src/routes/auth_callbacks.rs, crates/oxidean-api/tests/auth_bootstrap.rs</files>
  <read_first>crates/oxidean-api/src/rpc.rs, crates/oxidean-api/src/routes/auth_callbacks.rs, crates/oxidean-api/src/auth/gate.rs, .planning/phases/06-self-host-admin-bootstrap/06-RESEARCH.md</read_first>
  <behavior>
    - While needs_setup: auth.bootstrap_status / auth.bootstrap_setup / system.health succeed
    - While needs_setup: auth.login / auth.me / auth.signup fail with setup-required style error
    - SSO start handlers redirect/reject when needs_setup (D-09/D-10 carve-out: /status + health remain public outside this RPC path)
  </behavior>
  <action>
Per D-10/D-11 and RESEARCH Pitfall 2: early in `dispatch`, if needs_setup, allowlist only bootstrap_* + system.health; reject others with stable AppError code (e.g. auth.setup_required). Keep `confirm_admin_credentials` off the allowlist (ENV path has users). Ensure SSO `reject_if_setup_required` on WorkOS/OIDC starts. Add/green allowlist tests from Wave 0. Grep bootstrap sources for deployment-mode strings — none allowed (D-04).
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(bootstrap) | test(seeded_admin) | test(signup)'</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"; or rg finds OXIDEAN_DEPLOYMENT_MODE / self.host-only forks in bootstrap.rs</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'needs_setup|bootstrap_status|bootstrap_setup|system.health' crates/oxidean-api/src/rpc.rs`
    - `rg -n 'reject_if_setup_required|needs_setup' crates/oxidean-api/src/routes/auth_callbacks.rs`
    - nextest filter exits 0
  </acceptance_criteria>
  <done>Strict setup lock enforced at RPC and SSO seams</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `auth.bootstrap_*` with allow_signup | 06-03 | AUTH-07 |
| RPC needs_setup allowlist | 06-03 | D-11 |
| SSO setup reject | 06-03 | D-09 |

<verification>
auth_bootstrap nextest green including allowlist and second-setup.
</verification>

<success_criteria>
AUTH-07 success criterion 2 satisfied at API layer (wizard when ENV absent).
</success_criteria>

<output>
Create `.planning/phases/06-self-host-admin-bootstrap/06-03-SUMMARY.md` when done
</output>
