---
phase: 06-self-host-admin-bootstrap
plan: "07"
type: execute
wave: 8
depends_on:
  - "02"
  - "04"
  - "06"
  - "08"
files_modified:
  - docs/CONFIGURATION.md
  - docs/ARCHITECTURE.md
  - docs/API.md
  - .env.example
  - .planning/REQUIREMENTS.md
  - packages/api-client/src/index.ts
  - .planning/phases/06-self-host-admin-bootstrap/06-COVERAGE.md
  - .planning/todos/pending/2026-09-11-fix-signed-in-home-flicker-on-load.md
requirements:
  - AUTH-06
  - AUTH-07
autonomous: true
user_setup: []
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "REQUIREMENTS AUTH-06/07 reframed as empty-instance (not On self-host) per D-02"
    - "CONFIGURATION + .env.example document OCTANEST_ALLOW_SIGNUP default false and ADMIN seed vars; note cloud deploys should set true when open signup desired"
    - "ARCHITECTURE/API docs describe ENV seed, wizard, forced credentials, RPC allowlist, allow_signup"
    - "rpc-gen regenerates client including confirm_admin_credentials, allow_signup, must_change_credentials"
    - "Folded flicker todo marked done or removed after SSR home lands"
    - "COVERAGE.md declares no external API integration this phase"
  artifacts:
    - path: docs/CONFIGURATION.md
      provides: "OCTANEST_ALLOW_SIGNUP + ADMIN docs"
    - path: .planning/REQUIREMENTS.md
      provides: "AUTH-06/07 empty-instance wording"
    - path: .planning/phases/06-self-host-admin-bootstrap/06-COVERAGE.md
      provides: "no external API declaration"
    - path: packages/api-client/src/index.ts
      provides: "generated client sync"
  key_links:
    - from: crates/octanest-api/src/bin/rpc_gen.rs
      to: packages/api-client/src/index.ts
      via: "cargo run -p octanest-api --bin rpc-gen"
  assumptions:
    - "RESOLVED (Open Q2): documenting OCTANEST_ALLOW_SIGNUP=true for cloud manifests is sufficient; no Compose file change required unless already listing ADMIN env"
  prohibitions:
    - statement: "Must not reintroduce self-host-only wording that implies a second product path"
      flagged: true
---

<objective>
Document empty-instance bootstrap, regenerate RPC client, reframe REQUIREMENTS (D-02), and close the folded flicker todo.

Purpose: Operators and future phases see one-product bootstrap contracts; generated client matches DTOs.
Output: Docs + REQUIREMENTS + COVERAGE.md + rpc-gen + todo resolution.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-self-host-admin-bootstrap/06-CONTEXT.md
@.planning/phases/06-self-host-admin-bootstrap/06-RESEARCH.md
@.planning/REQUIREMENTS.md
@docs/CONFIGURATION.md
@.env.example
@docs/ARCHITECTURE.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator docs → ENV secrets | Docs must not encourage committing real passwords |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-15 | Information disclosure | .env.example / docs | medium | mitigate | Comment placeholders only; never real credentials; note fail-closed boot |
| T-06-SC | Tampering | cargo/npm installs | high | mitigate | No new packages; rpc-gen only |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (06-07-T1): Docs + REQUIREMENTS reframe + COVERAGE</name>
  <files>docs/CONFIGURATION.md, docs/ARCHITECTURE.md, docs/API.md, .env.example, .planning/REQUIREMENTS.md, .planning/phases/06-self-host-admin-bootstrap/06-COVERAGE.md</files>
  <read_first>docs/CONFIGURATION.md, .env.example, .planning/REQUIREMENTS.md, .planning/phases/06-self-host-admin-bootstrap/06-CONTEXT.md, docs/ARCHITECTURE.md</read_first>
  <action>
Per D-02/D-07/D-08/D-14/D-15:

1. Update REQUIREMENTS AUTH-06/07 text to empty-instance (not On self-host); note AUTH-05 interaction with allow_signup in v1 notes if appropriate without reopening Phase 5 checkbox churn beyond clarity.
2. Document OCTANEST_ADMIN_*, OCTANEST_ALLOW_SIGNUP (true/1, default false), fail-closed seed, system-administrator + forced credentials, wizard path in CONFIGURATION/ARCHITECTURE/API.
3. `.env.example`: commented OCTANEST_ALLOW_SIGNUP=false beside ADMIN vars.
4. Write `06-COVERAGE.md` one-liner: No external API integration this phase (bootstrap uses existing Octanest auth RPC/ENV only).
5. No deployment-mode documentation forks (D-03/D-04).
  </action>
  <verify>
    <automated>rg -n 'OCTANEST_ALLOW_SIGNUP|empty.instance|system-administrator' docs/CONFIGURATION.md .env.example .planning/REQUIREMENTS.md && test -f .planning/phases/06-self-host-admin-bootstrap/06-COVERAGE.md && rg -n 'On self-host' .planning/REQUIREMENTS.md | rg -v 'AUTH-0[67]' | head -5; rg -n '^\- \[ \] \*\*AUTH-0[67]' .planning/REQUIREMENTS.md; rg -n 'empty instance|empty-instance|Empty instance' .planning/REQUIREMENTS.md</automated>
    <fails_when>OCTANEST_ALLOW_SIGNUP missing from CONFIGURATION or .env.example; or AUTH-06/07 still lead with On self-host; or COVERAGE.md missing</fails_when>
  </verify>
  <acceptance_criteria>
    - AUTH-06/07 wording uses empty-instance language
    - CONFIGURATION documents ALLOW_SIGNUP default false
    - `06-COVERAGE.md` exists with no-external-API declaration
  </acceptance_criteria>
  <done>Operator docs and requirements match locked empty-instance product rule</done>
</task>

<task type="auto">
  <name>Task 2 (06-07-T2): rpc-gen + flicker todo close + phase smoke</name>
  <files>packages/api-client/src/index.ts, .planning/todos/pending/2026-09-11-fix-signed-in-home-flicker-on-load.md</files>
  <read_first>crates/octanest-api/src/bin/rpc_gen.rs, packages/api-client/src/index.ts, .planning/todos/pending/2026-09-11-fix-signed-in-home-flicker-on-load.md</read_first>
  <action>
1. Run `cargo run -p octanest-api --bin rpc-gen` and ensure generated client includes bootstrap/confirm/allow_signup/must_change fields used by web.
2. Mark folded flicker todo completed/moved (SSR home delivered in 06-05; chrome omit in 06-08) — delete pending file or relocate per project todo convention.
3. Smoke: `cargo nextest run -p octanest-api -E 'test(bootstrap) | test(seeded_admin) | test(signup)'` and `bun --cwd apps/web run build`.
  </action>
  <verify>
    <automated>cargo run -p octanest-api --bin rpc-gen && bun --cwd apps/web run build && cargo nextest run -p octanest-api -E 'test(bootstrap) | test(seeded_admin) | test(signup)'</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"; or generated client missing confirm_admin / allow_signup / must_change</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'allow_signup|must_change_credentials|confirmAdminCredentials|confirm_admin_credentials|bootstrapSetup' packages/api-client/src/index.ts`
    - web build exits 0
    - nextest filter exits 0
  </acceptance_criteria>
  <done>Client regenerated; phase smoke green; flicker todo closed</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| Docs + REQUIREMENTS reframe | 06-07 | D-02 |
| `06-COVERAGE.md` | 06-07 | no external API |
| Regenerated api-client | 06-07 | |
| Flicker todo closed | 06-07 | folded from CONTEXT |

<verification>
Docs contain ALLOW_SIGNUP + empty-instance; rpc-gen + build + nextest filter green.
</verification>

<success_criteria>
Phase documentation and generated client match implemented bootstrap behavior.
</success_criteria>

<output>
Create `.planning/phases/06-self-host-admin-bootstrap/06-07-SUMMARY.md` when done
</output>
