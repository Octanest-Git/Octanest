---
phase: 06-self-host-admin-bootstrap
plan: "05"
type: execute
wave: 5
depends_on:
  - "03"
  - "04"
files_modified:
  - apps/web/src/lib/ssr-auth.ts
  - apps/web/src/lib/api-client.ts
  - packages/api-client/src/index.ts
  - apps/web/src/routes/__root.tsx
  - apps/web/src/lib/bootstrap.ts
  - apps/web/src/routes/index.tsx
  - apps/web/src/routes/index.integration.test.ts
  - apps/web/src/lib/ssr-auth.gate.test.ts
requirements:
  - AUTH-06
  - AUTH-07
autonomous: true
user_setup: []
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "`/` SSR priority after shared gate: session → SignedInHome; else marketing — first HTML matches final tree (D-18, D-20)"
    - "Shared root SSR beforeLoad: while needs_setup, all app UI → /setup except /status (readiness) and /setup itself (D-09, D-10)"
    - "Shared root SSR beforeLoad: while must_change_credentials, all app UI → /setup/credentials except that route + /status (UI-SPEC global gate)"
    - "SSR uses Cookie-forward createServerFn to Octanest RPC — not TanStack Start useSession (D-11 phase-4 session ownership)"
    - "Presence hint octanest_signed_in remains progressive enhancement only (D-21 discretion: keep unless it fights SSR)"
    - "UI Consideration: SSR/home gate unclassified row covered — no marketing flash for signed-in"
  artifacts:
    - path: apps/web/src/lib/ssr-auth.ts
      provides: "fetchBootstrapStatus / fetchSessionMe / fetchProviderConfig + resolveAppAccessRedirect"
    - path: apps/web/src/routes/__root.tsx
      provides: "shared SSR beforeLoad app-access gate (D-09/D-10 + must_change)"
    - path: apps/web/src/routes/index.tsx
      provides: "SSR home tree selection after access gate"
    - path: apps/web/src/routes/index.integration.test.ts
      provides: "D-18/D-20 first-HTML gate automated check"
    - path: apps/web/src/lib/ssr-auth.gate.test.ts
      provides: "path-matrix coverage for verify/reset-password/settings/admin/login carve-outs"
  key_links:
    - from: apps/web/src/lib/ssr-auth.ts
      to: packages/api-client/src/index.ts
      via: "createClient fetch override with Cookie header"
    - from: apps/web/src/routes/__root.tsx
      to: apps/web/src/lib/ssr-auth.ts
      via: "beforeLoad → resolveAppAccessRedirect(needs_setup, must_change, pathname)"
    - from: apps/web/src/routes/index.tsx
      to: apps/web/src/lib/ssr-auth.ts
      via: "loader session vs marketing after root gate"
  assumptions:
    - "ASSUME (RESEARCH A1): CreateClientOptions.fetch override is enough for Cookie forward"
    - "ASSUME: tracked .tsx paths are canonical; executor reconciles in-flight .tsrx rename when landing edits"
    - "ASSUME: /setup/credentials route module lands in 06-06; root may redirect there from wave 5 (missing module = temporary notFound until 06-06)"
  prohibitions:
    - statement: "Must not use client-only redirectIfNeedsSetup as the security/first-paint boundary"
      flagged: true
    - statement: "Must not enumerate per-route needs_setup beforeLoads as the D-09/D-10 mechanism — shared root/layout gate is required"
      flagged: true
---

<objective>
Replace client-only bootstrap gating with Cookie-forward SSR helpers plus a **shared root beforeLoad** that enforces empty-instance and must_change locks for all app UI, then SSR-select `/` marketing vs SignedInHome.

Purpose: Deliver D-09/D-10 and UI-SPEC must_change global gate before paint; eliminate signed-in `/` flicker (folded todo).
Output: `ssr-auth` helpers; `__root` shared access gate; index SSR tree; demoted client bootstrap helper.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-self-host-admin-bootstrap/06-04-SUMMARY.md
@.planning/phases/06-self-host-admin-bootstrap/06-CONTEXT.md
@.planning/phases/06-self-host-admin-bootstrap/06-UI-SPEC.md
@.planning/phases/06-self-host-admin-bootstrap/06-RESEARCH.md
@.planning/phases/06-self-host-admin-bootstrap/06-PATTERNS.md
@apps/web/src/routes/__root.tsrx
@apps/web/src/routes/index.tsrx
@apps/web/src/lib/api-client.ts
@packages/api-client/src/index.ts
@apps/web/src/lib/return-to.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser Cookie → SSR server fn → API | Session forwarded; must not log cookie values |
| SSR gate → route tree | UX redirect only; API allowlist remains authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-11 | Information disclosure | SSR Cookie forward | high | mitigate | Forward Cookie header only to configured API origin; never log cookies; credentials include |
| T-06-12 | Spoofing | Open redirect after gates | medium | mitigate | Reuse safeReturnTo for post-login paths (D-22) — no raw returnTo |
| T-06-15 | Elevation of privilege | App UI while needs_setup / must_change | high | mitigate | Shared root beforeLoad redirects all non-carve-out routes before paint (D-09/D-10 + UI-SPEC) |
| T-06-SC | Tampering | cargo/npm installs | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (06-05-T1): ssr-auth Cookie-forward server fns</name>
  <files>apps/web/src/lib/ssr-auth.ts, apps/web/src/lib/api-client.ts, packages/api-client/src/index.ts</files>
  <read_first>apps/web/src/lib/api-client.ts, packages/api-client/src/index.ts, .planning/phases/06-self-host-admin-bootstrap/06-RESEARCH.md, .planning/phases/06-self-host-admin-bootstrap/06-PATTERNS.md</read_first>
  <action>
Per RESEARCH Pattern 2 (D-18/D-20): create intentionally new `apps/web/src/lib/ssr-auth.ts` using `createServerFn` from `@octanejs/tanstack-start` and `getRequestHeader("cookie")` from `@octanejs/tanstack-start/server`. Build createClient with fetch override that injects the incoming Cookie. Export helpers: fetchBootstrapStatus, fetchSessionMe (auth.me), fetchProviderConfig (allow_signup). Also export a pure `resolveAppAccessRedirect({ pathname, needsSetup, mustChangeCredentials })` used by root beforeLoad (Task 2) — no I/O inside the pure helper. Extend CreateClientOptions only if fetch override proves insufficient (A1). Do not adopt Start useSession.
  </action>
  <verify>
    <automated>bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or TypeScript errors mentioning ssr-auth / createServerFn unresolved</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/lib/ssr-auth.ts`
    - `rg -n 'createServerFn|getRequestHeader|bootstrapStatus|provider_config|providerConfig|resolveAppAccessRedirect' apps/web/src/lib/ssr-auth.ts`
  </acceptance_criteria>
  <done>SSR auth helpers compile and forward Cookie to API; pure access redirect helper exported</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (06-05-T2): Shared root SSR app-access gate (D-09/D-10 + must_change)</name>
  <files>apps/web/src/routes/__root.tsx, apps/web/src/lib/ssr-auth.ts, apps/web/src/lib/ssr-auth.gate.test.ts, apps/web/src/lib/bootstrap.ts</files>
  <read_first>apps/web/src/routes/__root.tsrx, apps/web/src/lib/bootstrap.ts, .planning/phases/06-self-host-admin-bootstrap/06-CONTEXT.md, .planning/phases/06-self-host-admin-bootstrap/06-UI-SPEC.md</read_first>
  <behavior>
    - needs_setup true + pathname /login|/verify|/reset-password|/settings/profile|/admin/auth|/dashboard|/signup|/ → redirect /setup
    - needs_setup true + pathname /status or /setup → no redirect (D-10 carve-outs)
    - must_change_credentials true + pathname app UI (same set as above) → redirect /setup/credentials
    - must_change true + pathname /setup/credentials or /status → no redirect
    - priority: needs_setup wins over must_change when both could apply
  </behavior>
  <action>
1. Prefer tracked `__root.tsx` (reconcile `__root.tsrx` if rename in flight). Add root `beforeLoad` that Cookie-forwards via fetchBootstrapStatus + fetchSessionMe, reads location pathname, and calls `resolveAppAccessRedirect` (D-09, D-10, UI-SPEC must_change matrix). Carve-outs only: `/status` (readiness), `/setup` (wizard), `/setup/credentials` (forced-change). Health is API-only — not a web route. Do not per-route-enumerate gates on login/verify/reset-password/settings/admin — root covers them.
2. Green `ssr-auth.gate.test.ts` against the pure helper path matrix in behavior (covers routes checker flagged as unplanned).
3. Only after root beforeLoad is wired and tests pass: demote/delete client `redirectIfNeedsSetup` as security/first-paint boundary — PE-only or remove call sites that treated it as the lock (D-09). Do not demote before the shared SSR gate lands.
4. `/setup/credentials` full UI is 06-06; root may redirect there from this plan.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/lib/ssr-auth.gate.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAIL"; or gate test passes while /login|/verify|/admin/auth remain ungated when needs_setup</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'beforeLoad|resolveAppAccessRedirect|needs_setup|must_change' apps/web/src/routes/__root.tsx apps/web/src/routes/__root.tsrx 2>/dev/null | head`
    - `rg -n 'resolveAppAccessRedirect|/setup/credentials|/status' apps/web/src/lib/ssr-auth.ts`
    - `bun --cwd apps/web exec vitest run src/lib/ssr-auth.gate.test.ts` exits 0
    - Client bootstrap helper is no longer the first-paint lock (demoted or removed after root gate)
  </acceptance_criteria>
  <done>All app UI except status/setup carve-outs SSR-redirects for needs_setup and must_change before paint</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (06-05-T3): `/` SSR session vs marketing tree</name>
  <files>apps/web/src/routes/index.tsx, apps/web/src/routes/index.integration.test.ts</files>
  <read_first>apps/web/src/routes/index.tsrx, apps/web/src/components/signed-in-home.tsx, apps/web/src/lib/return-to.ts, apps/web/src/routes/index.integration.test.ts, .planning/phases/06-self-host-admin-bootstrap/06-UI-SPEC.md</read_first>
  <behavior>
    - After root gate clears: valid session → SignedInHome in first HTML
    - anonymous → marketing without SignedInHome flash
    - needs_setup / must_change handled by root — index must not reintroduce client-only setup lock as the boundary
  </behavior>
  <action>
Implement index beforeLoad/loader for session vs marketing per UI-SPEC / D-18–D-20 after the shared root gate. Prefer tracked `index.tsx` (reconcile `.tsrx` if rename in flight). Keep presence hint unless it causes hydration races (D-21). Honor safeReturnTo patterns already on login (D-22) — do not weaken. Turn Wave 0 `index.integration.test.ts` green — wrong tree selection must fail the automated check. Dashboard notFound and signup allow_signup notFound are 06-09 (not this task).
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/index.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAIL"; or index.integration.test.ts passes while loader selects wrong tree for session / anonymous fixtures</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'SignedInHome|fetchSessionMe' apps/web/src/routes/index.tsx apps/web/src/routes/index.tsrx 2>/dev/null | head`
    - `bun --cwd apps/web exec vitest run src/routes/index.integration.test.ts` exits 0
  </acceptance_criteria>
  <done>`/` first HTML matches session vs marketing decision under the shared access gate</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `apps/web/src/lib/ssr-auth.ts` | 06-05 | Cookie-forward + resolveAppAccessRedirect |
| Shared root SSR access gate | 06-05 | D-09/D-10 + must_change |
| SSR `/` session vs marketing | 06-05 | D-18–D-20 |
| `/dashboard` notFound + signup closed gate | 06-09 | D-19 / D-06 |

<verification>
web build green; ssr-auth.gate + index.integration green; __root uses shared access gate; index uses ssr-auth after gate.
</verification>

<success_criteria>
Empty-instance and must_change redirects happen before paint on all app UI except carve-outs; `/` first paint matches final signed-in vs marketing UI.
</success_criteria>

<output>
Create `.planning/phases/06-self-host-admin-bootstrap/06-05-SUMMARY.md` when done
</output>
