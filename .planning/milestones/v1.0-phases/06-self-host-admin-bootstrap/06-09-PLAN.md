---
phase: 06-self-host-admin-bootstrap
plan: "09"
type: execute
wave: 6
depends_on:
  - "05"
files_modified:
  - apps/web/src/routes/dashboard.tsx
  - apps/web/src/routes/signup.tsx
  - apps/web/src/routes/dashboard.integration.test.ts
requirements:
  - AUTH-06
  - AUTH-07
autonomous: true
user_setup: []
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Direct /dashboard → notFound 404 — not a soft redirect (D-19)"
    - "/signup beforeLoad: allow_signup false → notFound; needs_setup already handled by 06-05 root gate (D-06, D-09)"
  artifacts:
    - path: apps/web/src/routes/dashboard.tsx
      provides: "notFound()"
    - path: apps/web/src/routes/dashboard.integration.test.ts
      provides: "D-19 notFound automated check"
    - path: apps/web/src/routes/signup.tsx
      provides: "SSR allow_signup closed → notFound"
  key_links:
    - from: apps/web/src/routes/signup.tsx
      to: apps/web/src/lib/ssr-auth.ts
      via: "beforeLoad fetchProviderConfig.allow_signup"
    - from: apps/web/src/routes/dashboard.tsx
      to: "@octanejs/tanstack-router"
      via: "notFound()"
  assumptions:
    - "ASSUME: shared root gate from 06-05 already redirects needs_setup/must_change for these routes"
    - "ASSUME: reconcile .tsrx rename when editing routes"
  prohibitions:
    - statement: "Must not soft-redirect /dashboard to / or login"
      flagged: true
    - statement: "Must not show AuthShell soft page for closed /signup — 404 only"
      flagged: true
---

<objective>
Hard-404 `/dashboard` and SSR-404 closed `/signup` on top of the shared root access gate from 06-05.

Purpose: Finish D-19 and D-06 route-level outcomes without bloating the SSR foundation plan.
Output: dashboard notFound; signup allow_signup gate; green dashboard.integration.test.ts.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-self-host-admin-bootstrap/06-05-SUMMARY.md
@.planning/phases/06-self-host-admin-bootstrap/06-UI-SPEC.md
@.planning/phases/06-self-host-admin-bootstrap/06-CONTEXT.md
@apps/web/src/routes/dashboard.tsrx
@apps/web/src/routes/signup.tsrx
@apps/web/src/lib/ssr-auth.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSR signup gate → route tree | UX 404 only; API allow_signup remains authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-16 | Information disclosure | /dashboard soft page | medium | mitigate | notFound() — no soft redirect content (D-19) |
| T-06-SC | Tampering | cargo/npm installs | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (06-09-T1): `/dashboard` notFound</name>
  <files>apps/web/src/routes/dashboard.tsx, apps/web/src/routes/dashboard.integration.test.ts</files>
  <read_first>apps/web/src/routes/dashboard.tsrx, apps/web/src/routes/dashboard.integration.test.ts, .planning/phases/06-self-host-admin-bootstrap/06-UI-SPEC.md</read_first>
  <behavior>
    - /dashboard → notFound()
    - no soft redirect to / or login
  </behavior>
  <action>
Per D-19: replace dashboard soft-redirect with `notFound()` from `@octanejs/tanstack-router`. Prefer tracked `dashboard.tsx` (reconcile `.tsrx` if rename in flight). Green Wave 0 `dashboard.integration.test.ts` as far as unit-testable without full Start server (mock route options / beforeLoad result). Shared root gate already covers needs_setup/must_change for this path (06-05).
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/dashboard.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'notFound' apps/web/src/routes/dashboard.tsx apps/web/src/routes/dashboard.tsrx 2>/dev/null`
    - `bun --cwd apps/web exec vitest run src/routes/dashboard.integration.test.ts` exits 0
  </acceptance_criteria>
  <done>Dashboard is a hard 404</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (06-09-T2): `/signup` closed-signup SSR notFound</name>
  <files>apps/web/src/routes/signup.tsx</files>
  <read_first>apps/web/src/routes/signup.tsrx, apps/web/src/lib/ssr-auth.ts, .planning/phases/06-self-host-admin-bootstrap/06-UI-SPEC.md</read_first>
  <behavior>
    - allow_signup false → notFound
    - allow_signup true → existing signup UI (needs_setup already redirected by root)
  </behavior>
  <action>
Per D-06 + UI-SPEC: add signup `beforeLoad` that uses ssr-auth fetchProviderConfig; when allow_signup is false call `notFound()`. Do not soft-render AuthShell for closed signup. Chrome/landing omit of Sign up links remains 06-08. Prefer tracked `signup.tsx` (reconcile `.tsrx`).
  </action>
  <verify>
    <automated>bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or TypeScript errors on signup beforeLoad / notFound</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'beforeLoad|notFound|allow_signup|fetchProviderConfig' apps/web/src/routes/signup.tsx apps/web/src/routes/signup.tsrx 2>/dev/null | head`
  </acceptance_criteria>
  <done>Closed signup is SSR 404 on the route module</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `/dashboard` notFound | 06-09 | D-19 |
| `/signup` allow_signup notFound | 06-09 | D-06 SSR |

<verification>
dashboard.integration green; web build green; signup beforeLoad closed-signup notFound present.
</verification>

<success_criteria>
`/dashboard` is not a public soft page; closed `/signup` 404s at SSR.
</success_criteria>

<output>
Create `.planning/phases/06-self-host-admin-bootstrap/06-09-SUMMARY.md` when done
</output>
