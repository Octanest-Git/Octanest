---
phase: 10-orgs-permissions
plan: "07"
type: execute
wave: 7
depends_on:
  - "03"
  - "04"
  - "06"
files_modified:
  - crates/octanest-api/src/repo/collaborators.rs
  - crates/octanest-api/src/repo/mod.rs
  - crates/octanest-api/src/rpc.rs
  - crates/octanest-db/src/repo_collaborators.rs
  - crates/octanest-db/src/lib.rs
  - crates/octanest-core/src/repo_types.rs
  - crates/octanest-api/tests/repo_collaborators_acl.rs
  - crates/octanest-api/tests/repo_private_404.rs
  - packages/api-client/src/index.ts
requirements:
  - ORG-03
  - ORG-04
autonomous: true
user_setup: []
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Repo admin can add/update/remove collaborators with read|write|admin on personal and org-owned repos (ORG-03, D-ORG-02c, D-ORG-04)"
    - "Collaborator grant raises effective_capability; unauthorized private still soft not_found (ORG-04, D-ORG-05)"
    - "Visibility updates require admin capability (not mere personal owner_id equality)"
  artifacts:
    - path: crates/octanest-api/src/repo/collaborators.rs
      provides: "repo.collaborators.* RPC"
  key_links:
    - from: crates/octanest-api/src/repo/collaborators.rs
      to: crates/octanest-api/src/repo/acl.rs
      via: "admin gate + capability after grant"
  assumptions:
    - "ASSUME: Out-of-org users receive Collaborator grants only unless also org Members (D-ORG-04)"
  prohibitions:
    - statement: "Must not treat Collaborator as an organization membership role"
      flagged: true
---

<objective>
Ship per-repository collaborator CRUD on personal and org-owned repos and prove ACL raises access (ORG-03, ORG-04, D-ORG-02c, D-ORG-04).

Purpose: Outside (and inside) grants without org membership.
Output: Green repo_collaborators_acl matrix; visibility admin-gated via capability.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-orgs-permissions/10-CONTEXT.md
@.planning/phases/10-orgs-permissions/10-RESEARCH.md
@crates/octanest-api/src/repo/acl.rs
@crates/octanest-api/src/repo/mod.rs
@crates/octanest-db/migrations/postgres/0008_pats.sql
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → collaborators.* | Privilege grant attempts |
| Collaborator admin → further grants | Bounded by admin capability |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02 | Elevation of privilege | collaborators.add | high | mitigate | require meets(Admin); permission enum read|write|admin only; soft not_found if cannot admin |
| T-10-01 | Information disclosure | list | medium | mitigate | Non-admin list denied via not_found same as missing |
| T-10-SC | Tampering | packages | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (10-07-T1): repo.collaborators.* CRUD</name>
  <files>crates/octanest-api/src/repo/collaborators.rs, crates/octanest-api/src/repo/mod.rs, crates/octanest-api/src/rpc.rs, crates/octanest-db/src/repo_collaborators.rs, crates/octanest-core/src/repo_types.rs, packages/api-client/src/index.ts, crates/octanest-api/tests/repo_collaborators_acl.rs</files>
  <read_first>crates/octanest-api/src/repo/mod.rs, crates/octanest-api/src/repo/acl.rs, .planning/phases/10-orgs-permissions/10-RESEARCH.md</read_first>
  <behavior>
    - Admin can add collaborator by username with read|write|admin on personal repo and org repo
    - update/remove work; duplicate unique (repo_id,user_id)
    - Non-admin → repo.not_found
  </behavior>
  <action>
Implement repo.collaborators.list/add/update/remove gated by meets(Admin) per D-ORG-02c and D-ORG-04 (both personal and org-owned). make rpc-gen. Green collaborator CRUD tests in repo_collaborators_acl.rs.
  </action>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && cargo nextest run -p octanest-api -E 'test(collab)'</automated>
    <fails_when>rpc-gen/rpc-sync-check exits non-zero or client drifts</fails_when>
  </verify>
  <done>Collaborator CRUD works on personal and org repos for admins</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (10-07-T2): ACL raise + visibility admin gate</name>
  <files>crates/octanest-api/src/repo/mod.rs, crates/octanest-api/tests/repo_collaborators_acl.rs, crates/octanest-api/tests/repo_private_404.rs</files>
  <read_first>crates/octanest-api/src/repo/mod.rs, crates/octanest-api/src/repo/acl.rs</read_first>
  <behavior>
    - Collaborator read can repo.get private; write not required for get
    - Stranger still not_found
    - updateVisibility requires Admin capability (org Admin or collab admin or personal owner)
  </behavior>
  <action>
Replace remaining session.user_id == owner_id checks on visibility (and soft-delete if present) with meets(Admin) via resolve helper, keeping soft not_found. Prove collaborator-granted private read in tests (ORG-03 visibility already exists — ensure admin gate).
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(collab) | test(repo_private) | test(repo_settings)'</automated>
    <fails_when>cargo/nextest exits non-zero or expected tests fail</fails_when>
  </verify>
  <done>Collaborator raises private read; visibility admin-gated via ACL</done>
</task>

</tasks>

<verification>
ORG-03 collaborators + ORG-04 private grant/deny green for web RPC.
</verification>

<success_criteria>
Personal+org collaborator ladder live; visibility uses admin capability.
</success_criteria>

<output>
Create `.planning/phases/10-orgs-permissions/10-07-SUMMARY.md` when done
</output>
