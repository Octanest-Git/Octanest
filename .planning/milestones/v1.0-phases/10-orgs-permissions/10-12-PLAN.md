---
phase: 10-orgs-permissions
plan: "12"
type: execute
wave: 10
depends_on:
  - "08"
  - "10"
  - "11"
files_modified:
  - crates/oxidean-db/src/lib.rs
  - crates/oxidean-db/tests/factory_reset_orgs.rs
  - crates/oxidean-api/tests/factory_reset_scope.rs
  - docs/ARCHITECTURE.md
  - docs/API.md
  - docs/CONFIGURATION.md
  - .planning/phases/10-orgs-permissions/10-VALIDATION.md
requirements:
  - ORG-01
  - ORG-02
  - ORG-03
  - ORG-04
autonomous: true
user_setup: []
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "factory_reset_instance wipes organizations, members, invites, collaborators, and org-owned repos"
    - "Docs describe polymorphic owners, ACL capabilities, invite closed-signup, PAT∩ACL; ARCHITECTURE owner-only stub language removed"
    - "Phase gate: org_/collab/git_smart/repo_private + rpc-sync + web build green; VALIDATION maps updated"
  artifacts:
    - path: docs/API.md
      provides: "org.* / collaborators / user.lookup docs"
    - path: .planning/phases/10-orgs-permissions/10-VALIDATION.md
      provides: "nyquist_compliant refresh"
  key_links:
    - from: crates/oxidean-db/src/lib.rs
      to: factory_reset_instance
      via: "DELETE order for org tables"
  assumptions:
    - "ASSUME: No new env vars for orgs; invite mail uses existing email settings"
  prohibitions:
    - statement: "Must not leave ARCHITECTURE claiming private ACL is owner-only"
      flagged: true
---

<objective>
Close Phase 10 with factory-reset coverage, documentation, rpc-sync confirmation, VALIDATION refresh, and full automated gate (ORG-01..04).

Purpose: Ship-ready orgs & permissions with durable ops/docs.
Output: Green phase gate; nyquist map marks Wave 0 gaps closed.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-orgs-permissions/10-RESEARCH.md
@.planning/phases/10-orgs-permissions/10-VALIDATION.md
@crates/oxidean-db/src/lib.rs
@docs/ARCHITECTURE.md
@docs/API.md
@crates/oxidean-api/tests/factory_reset_scope.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| factory_reset → all tenant data | Must not leave org rows orphaned |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-15 | Tampering / residual data | factory_reset | high | mitigate | Wipe orgs/members/invites/collaborators/org repos in reset path; extend factory_reset tests |
| T-10-SC | Tampering | packages | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (10-12-T1): Factory reset org tables + tests</name>
  <files>crates/oxidean-db/src/lib.rs, crates/oxidean-db/tests/factory_reset_orgs.rs, crates/oxidean-api/tests/factory_reset_scope.rs</files>
  <read_first>crates/oxidean-db/src/lib.rs, crates/oxidean-api/tests/factory_reset_scope.rs, .planning/phases/10-orgs-permissions/10-RESEARCH.md</read_first>
  <behavior>
    - After factory_reset, organizations/members/invites/collaborators counts are zero
    - User-owned and org-owned repos removed as today for user cascade plus org wipe
  </behavior>
  <action>
Extend factory_reset_instance to delete org ACL tables (order-safe FKs) and org-owned repositories/disk as required. Green factory reset coverage for org rows.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(factory_reset)' && cargo test -p oxidean-db --test factory_reset_orgs</automated>
    <fails_when>cargo/nextest exits non-zero or expected tests fail</fails_when>
  </verify>
  <done>Factory reset clears org ACL state</done>
</task>

<task type="auto">
  <name>Task 2 (10-12-T2): Docs + VALIDATION + phase gate</name>
  <files>docs/ARCHITECTURE.md, docs/API.md, docs/CONFIGURATION.md, .planning/phases/10-orgs-permissions/10-VALIDATION.md</files>
  <read_first>docs/ARCHITECTURE.md, docs/API.md, .planning/phases/10-orgs-permissions/10-VALIDATION.md</read_first>
  <action>
Update ARCHITECTURE/API (and CONFIGURATION if invite/email notes needed) for shared slug owners, Capability ACL, members/invites, collaborators, PAT∩ACL, closed-signup invite accept. Refresh 10-VALIDATION.md Wave 0 checkboxes and nyquist_compliant. Run phase gate: org/collab/private/git_smart/pat filters, migration_parity, rpc-sync-check, web build.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(org_) | test(collab) | test(repo_private) | test(git_smart) | test(pat_) | test(coalesce)' && cargo test -p oxidean-db --lib migration_parity && make rpc-sync-check && bun --cwd apps/web run build && rg -n 'Capability|organization|collaborator|member_base' docs/ARCHITECTURE.md docs/API.md</automated>
    <fails_when>web build exits non-zero</fails_when>
  </verify>
  <done>Docs accurate; VALIDATION updated; full Phase 10 automated gate green</done>
</task>

</tasks>

<verification>
All ORG-01..04 success criteria backed by green automated gate + docs.
</verification>

<success_criteria>
Phase 10 ship gate green; private data truly private across web+git+PAT; factory reset safe.
</success_criteria>

<output>
Create `.planning/phases/10-orgs-permissions/10-12-SUMMARY.md` when done
</output>
