---
phase: 10-orgs-permissions
plan: "02"
type: execute
wave: 1
depends_on:
  - "00"
  - "01"
files_modified:
  - crates/oxidean-db/migrations/postgres/0010_orgs_acl.sql
  - crates/oxidean-db/migrations/mysql/0010_orgs_acl.sql
  - crates/oxidean-db/migrations/sqlite/0010_orgs_acl.sql
  - crates/oxidean-db/src/organizations.rs
  - crates/oxidean-db/src/org_members.rs
  - crates/oxidean-db/src/lib.rs
  - crates/oxidean-db/tests/dialect_orgs.rs
  - crates/oxidean-core/src/org_types.rs
  - crates/oxidean-core/src/lib.rs
requirements:
  - ORG-01
autonomous: false
user_setup: []
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Tri-dialect 0010_orgs_acl lands organizations + members (+ invite/collaborator/owner_type columns for later fill) without dialect SQL in API (ORG-01, D-ORG-01)"
    - "Database helpers can insert an organization and an Owner membership row"
    - "org_types DTOs compile and dialect_orgs asserts 0010 shape"
  artifacts:
    - path: crates/oxidean-db/migrations/postgres/0010_orgs_acl.sql
      provides: "Org ACL schema (0010 vs Phase 09 0009_ssh_keys)"
    - path: crates/oxidean-db/src/organizations.rs
      provides: "insert organization + Owner membership helpers"
    - path: crates/oxidean-core/src/org_types.rs
      provides: "Org DTOs for later org.create"
  key_links:
    - from: crates/oxidean-db/src/organizations.rs
      to: crates/oxidean-db/migrations/postgres/0010_orgs_acl.sql
      via: "insert uses organizations + organization_members tables"
  assumptions:
    - "ASSUME (discretion): polymorphic owner uses owner_type + owner_id on repositories (not a separate owners table)"
    - "ASSUME: Migration number is 0010_orgs_acl; if only SSH 0009 exists first, orgs still 0010; never steal 0009 from Phase 09"
    - "ASSUME: Invite/collaborator tables may be created empty in 0010 for architectural completeness; RPCs filled in later plans"
    - "ASSUME: org.create RPC + /orgs/new UI land in plan 13 (scope split from this schema door)"
  prohibitions:
    - statement: "Must not implement teams"
      flagged: true
    - statement: "Must not place dialect SQL in oxidean-api"
      flagged: true
    - statement: "Must not implement org.create RPC or /orgs/new in this plan — deferred to 10-13"
      flagged: true
---

<objective>
Confirm the one-way shared-slug / polymorphic-owner schema door, then land tri-dialect 0010_orgs_acl plus DB helpers and org_types (no RPC/UI yet).

Purpose: Unlock the architectural door before the org.create tracer in plan 13 (scope split keeps this plan under the file budget).
Output: Green dialect_orgs migration assertions; insert org + Owner membership helpers ready for RPC.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-orgs-permissions/10-CONTEXT.md
@.planning/phases/10-orgs-permissions/10-RESEARCH.md
@.planning/phases/10-orgs-permissions/10-PATTERNS.md
@crates/oxidean-core/src/auth_types.rs
@crates/oxidean-db/migrations/postgres/0007_repositories.sql
@crates/oxidean-db/migrations/postgres/0008_pats.sql
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shared slug namespace | User username ↔ org slug collision = takeover risk (enforced at RPC in 10-13) |
| Migration → repositories | owner_type/owner_id backfill must preserve existing user-owned repos |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Tampering | org slug | high | mitigate | Schema UNIQUE on organizations.slug; RPC dual-check vs users in 10-13 (D-ORG-01) |
| T-10-SC | Tampering | packages | high | mitigate | No new crates.io/npm packages |
</threat_model>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 0 (10-02-T0): Confirm D-ORG-01 schema door (0010 + owner_type)</name>
  <decision>
Confirm proceed with RESEARCH discretion for Phase 10 schema:

1. Migration id **0010_orgs_acl** (tri-dialect) — Phase 09 SSH claims **0009_ssh_keys**; do not reuse 0009 for orgs.
2. Polymorphic repos: **owner_type** (`user`|`org`) + **owner_id** (TEXT, no FK to users); backfill existing rows owner_type=user; drop user-only FK carefully per dialect.
3. Tables in same migration: organizations (slug UNIQUE, member_base_permission default none), organization_members, organization_invites, repository_collaborators — even if invite/collaborator RPCs land later.

Locked product decision D-ORG-01 (shared slug namespace) is not reopened — only the physical schema shape above.
  </decision>
  <context>
One-way door: URL + disk path + ACL identity key off shared slug and owner_type. Phase 09 plans already use 0009_ssh_keys — orgs must not collide.
  </context>
  <options>
    <option id="proceed_0010">
      <title>Proceed with 0010_orgs_acl + owner_type/owner_id (recommended)</title>
      <pros>Matches RESEARCH discretion; avoids Phase 09 migration collision; single migration for org ACL tables</pros>
      <cons>Undo requires data migration after any org-owned repos exist</cons>
    </option>
    <option id="revise">
      <title>Stop — revise schema numbering/shape before tracer</title>
      <pros>Allows alternate owners-table design if preferred</pros>
      <cons>Blocks tracer until replan</cons>
    </option>
  </options>
  <resume-signal>Reply with proceed_0010 (recommended) or revise</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1 (10-02-T1): 0010_orgs_acl + DB helpers + org_types</name>
  <files>crates/oxidean-db/migrations/postgres/0010_orgs_acl.sql, crates/oxidean-db/migrations/mysql/0010_orgs_acl.sql, crates/oxidean-db/migrations/sqlite/0010_orgs_acl.sql, crates/oxidean-db/src/organizations.rs, crates/oxidean-db/src/org_members.rs, crates/oxidean-db/src/lib.rs, crates/oxidean-core/src/org_types.rs, crates/oxidean-core/src/lib.rs, crates/oxidean-db/tests/dialect_orgs.rs</files>
  <read_first>.planning/phases/10-orgs-permissions/10-RESEARCH.md, crates/oxidean-db/src/repositories.rs, crates/oxidean-core/src/auth_types.rs, crates/oxidean-db/migrations/postgres/0007_repositories.sql</read_first>
  <behavior>
    - dialect_orgs asserts tri-dialect 0010_orgs_acl tables/columns for organizations, organization_members, invites, collaborators, repositories.owner_type
    - insert organization + Owner membership helpers succeed on sqlite test harness
  </behavior>
  <action>
After T0 proceed_0010: land tri-dialect 0010_orgs_acl per D-ORG-01 (shared slug) and locked roles D-ORG-02a (Owner/Admin/Member columns). Implement Database helpers for insert organization + insert Owner membership; org_types DTOs (no RPC handlers). Green dialect_orgs migration assertions. Do not ship org.create, rpc-gen, or /orgs/new here (plan 13). Do not implement teams (D-ORG-07).
  </action>
  <verify>
    <automated>cargo test -p oxidean-db --lib migration_parity && cargo test -p oxidean-db --test dialect_orgs</automated>
    <fails_when>migration_parity or dialect_orgs exits non-zero; or 0010_orgs_acl missing on any dialect</fails_when>
  </verify>
  <done>0010 migrates on all dialects; org + Owner membership helpers and org_types compile</done>
</task>

</tasks>

<verification>
Schema door confirmed; dialect_orgs green; RPC/UI deferred to 10-13.
</verification>

<success_criteria>
ORG-01 schema foundation ready; no teams; no org.create yet.
</success_criteria>

<output>
Create `.planning/phases/10-orgs-permissions/10-02-SUMMARY.md` when done
</output>
