---
phase: 10-orgs-permissions
plan: "09"
type: execute
wave: 8
depends_on:
  - "07"
files_modified:
  - crates/oxidean-api/src/user/lookup.rs
  - crates/oxidean-api/src/rpc.rs
  - crates/oxidean-db/src/users.rs
  - crates/oxidean-db/src/lib.rs
  - crates/oxidean-core/src/auth_types.rs
  - crates/oxidean-api/tests/org_create_members.rs
  - packages/api-client/src/index.ts
requirements:
  - ORG-01
autonomous: true
user_setup: []
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "user.lookup returns ≤10 public profiles for prefix ≥2; never emails (D-ORG-03)"
    - "Rate-limited per session; empty/short prefix returns empty list"
  artifacts:
    - path: crates/oxidean-api/src/user/lookup.rs
      provides: "user.lookup RPC"
  key_links:
    - from: apps/web members UI
      to: crates/oxidean-api/src/user/lookup.rs
      via: "autocomplete (wired in 10-10)"
  assumptions:
    - "ASSUME: Prefix match case-insensitive on username; require_verified for lookup"
  prohibitions:
    - statement: "Must not search or return email addresses in lookup results"
      flagged: true
---

<objective>
Ship live username lookup for member/collaborator autocomplete with anti-enumeration limits (ORG-01, D-ORG-03).

Purpose: Enable UI add-by-username without leaking emails.
Output: Green lookup tests; rpc-gen client method.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-orgs-permissions/10-CONTEXT.md
@.planning/phases/10-orgs-permissions/10-RESEARCH.md
@.planning/phases/10-orgs-permissions/10-PATTERNS.md
@crates/oxidean-db/src/users.rs
@crates/oxidean-api/src/auth/profile.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Autocomplete oracle | Username enumeration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03 | Information disclosure | user.lookup | medium | mitigate | prefix≥2, limit≤10, rate-limit, username+display+avatar only; reject email-shaped queries |
| T-10-SC | Tampering | packages | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (10-09-T1): DB prefix search + user.lookup RPC</name>
  <files>crates/oxidean-db/src/users.rs, crates/oxidean-db/src/lib.rs, crates/oxidean-api/src/user/lookup.rs, crates/oxidean-api/src/rpc.rs, crates/oxidean-core/src/auth_types.rs, packages/api-client/src/index.ts</files>
  <read_first>crates/oxidean-db/src/users.rs, .planning/phases/10-orgs-permissions/10-RESEARCH.md</read_first>
  <behavior>
    - prefix len &lt; 2 → empty results
    - returns at most 10 {username, display_name, avatar_url?}
    - email-shaped input → empty (no @ search)
  </behavior>
  <action>
Add dialect-safe username prefix list helper and user.lookup RPC (require_verified + simple per-session rate limit). make rpc-gen. Do not return emails. Wire module under existing user/ or auth/ layout consistently with codebase.
  </action>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && cargo nextest run -p oxidean-api -E 'test(lookup) | test(org_)'</automated>
    <fails_when>rpc-gen/rpc-sync-check exits non-zero or client drifts</fails_when>
  </verify>
  <done>user.lookup RPC green with limits and no email leakage</done>
</task>

<task type="auto">
  <name>Task 2 (10-09-T2): Lookup unit coverage in org_create_members or dedicated test</name>
  <files>crates/oxidean-api/tests/org_create_members.rs</files>
  <read_first>crates/oxidean-api/tests/org_create_members.rs</read_first>
  <action>
Add/turn-green Wave 0 lookup shape assertions (prefix/limit/no-email) colocated with org tests or a small user_lookup.rs test file if cleaner — keep filter discoverable as test(lookup) or test(org_).
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(lookup) | test(org_lookup)'</automated>
    <fails_when>cargo/nextest exits non-zero or expected tests fail</fails_when>
  </verify>
  <done>Lookup anti-enumeration tests green</done>
</task>

</tasks>

<verification>
D-ORG-03 live username lookup API ready for UI.
</verification>

<success_criteria>
Autocomplete-safe user.lookup shipped.
</success_criteria>

<output>
Create `.planning/phases/10-orgs-permissions/10-09-SUMMARY.md` when done
</output>
