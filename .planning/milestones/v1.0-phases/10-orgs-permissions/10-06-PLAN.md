---
phase: 10-orgs-permissions
plan: "06"
type: execute
wave: 6
depends_on:
  - "05"
files_modified:
  - crates/oxidean-db/src/org_invites.rs
  - crates/oxidean-db/src/lib.rs
  - crates/oxidean-api/src/org/invites.rs
  - crates/oxidean-api/src/org/mod.rs
  - crates/oxidean-api/src/rpc.rs
  - crates/oxidean-core/src/org_types.rs
  - crates/oxidean-api/tests/org_invites.rs
  - apps/web/src/routes/invites.$token.tsrx
  - packages/api-client/src/index.ts
requirements:
  - ORG-01
autonomous: true
user_setup: []
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Admin+ can create/list/revoke email invites; accept joins org with invited role (ORG-01, D-ORG-03)"
    - "Invite accept may create account when allow_signup is false (discretion A2)"
    - "Invite tokens stored hashed; plaintext shown only at create email link"
  artifacts:
    - path: crates/oxidean-api/src/org/invites.rs
      provides: "org.invites.* RPC"
    - path: apps/web/src/routes/invites.$token.tsrx
      provides: "Accept invite page"
  key_links:
    - from: crates/oxidean-api/src/org/invites.rs
      to: crates/oxidean-api/src/email
      via: "EmailSender OutboundEmail magic link"
  assumptions:
    - "ASSUME: 32-byte CSPRNG token, SHA-256 hex at rest, TTL 7 days, single-use accept"
    - "ASSUME: Anti-enumeration soft-ok on invite create for unknown policy where appropriate; rate-limit like verify_reset"
  prohibitions:
    - statement: "Must not route invite accept through public signup that respects allow_signup=false rejection"
      flagged: true
    - statement: "Must not store plaintext invite token in DB"
      flagged: true
---

<objective>
Ship email invite create/list/revoke/accept with closed-signup-safe account provisioning (ORG-01, D-ORG-03).

Purpose: Onboard people not yet on the instance without opening public signup.
Output: Green org_invites tests + /invites/$token accept route.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-orgs-permissions/10-CONTEXT.md
@.planning/phases/10-orgs-permissions/10-RESEARCH.md
@.planning/phases/10-orgs-permissions/10-PATTERNS.md
@.planning/phases/10-orgs-permissions/10-UI-SPEC.md
@crates/oxidean-api/src/auth/verify_reset.rs
@crates/oxidean-api/src/auth/local.rs
@crates/oxidean-db/migrations/postgres/0003_email_tokens.sql
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Email → accept token | Token theft → membership |
| Closed signup → invite create-user | Bypass must be invite-gated only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-11 | Spoofing | invite token | high | mitigate | High-entropy token, SHA-256 at rest, expiry, single-use, HTTPS link via public_origin |
| T-10-12 | Elevation of privilege | closed-signup accept | high | mitigate | Account create only with valid unused invite; rate-limit invite create; require_verified for create |
| T-10-03 | Information disclosure | invite create | medium | mitigate | Soft success / generic errors where email enumeration matters |
| T-10-SC | Tampering | packages | high | mitigate | No new packages; reuse EmailSender |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (10-06-T1): org.invites create/list/revoke/accept RPC</name>
  <files>crates/oxidean-db/src/org_invites.rs, crates/oxidean-db/src/lib.rs, crates/oxidean-api/src/org/invites.rs, crates/oxidean-api/src/org/mod.rs, crates/oxidean-api/src/rpc.rs, crates/oxidean-core/src/org_types.rs, packages/api-client/src/index.ts, crates/oxidean-api/tests/org_invites.rs</files>
  <read_first>crates/oxidean-api/src/auth/verify_reset.rs, crates/oxidean-api/src/auth/local.rs, .planning/phases/10-orgs-permissions/10-RESEARCH.md</read_first>
  <behavior>
    - Admin+ creates invite → email sent via EmailSender; DB stores token_hash
    - accept with valid token adds membership; if no user, creates local user despite allow_signup false
    - expired/used token → stable error
    - revoke removes pending invite
  </behavior>
  <action>
Implement organization_invites DB API and org.invites.create/list/revoke/accept per D-ORG-03. Mirror verify_reset token hashing and rate limits. Accept path: if session email matches or password signup fields provided for invite email, create/link user and insert membership with invited role; bypass allow_signup closed gate only for this path (A2). make rpc-gen. Green org_invites.rs including closed-signup case. Use LogSink in tests.
  </action>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && cargo nextest run -p oxidean-api -E 'test(org_invite)'</automated>
    <fails_when>rpc-gen/rpc-sync-check exits non-zero or client drifts</fails_when>
  </verify>
  <done>Email invite lifecycle works including closed signup accept</done>
</task>

<task type="auto">
  <name>Task 2 (10-06-T2): /invites/$token accept page</name>
  <files>apps/web/src/routes/invites.$token.tsrx</files>
  <read_first>.planning/phases/10-orgs-permissions/10-UI-SPEC.md, apps/web/src/routes/signup.tsrx, apps/web/src/routes/verify.tsrx, .agents/skills/octane/SKILL.md</read_first>
  <action>
Add Octane route /invites/$token that calls org.invites.accept (logged-in match or collect password/username for new account). Copy explains invite-gated join when signup is closed. Rivet-only; no JSX return mix. Minimal happy-path coverage via existing org_invites API tests; optional thin vitest later.
  </action>
  <verify>
    <automated>bun --cwd apps/web run build && test -f apps/web/src/routes/invites.\$token.tsrx</automated>
    <fails_when>web build exits non-zero</fails_when>
  </verify>
  <done>Accept page builds and is routed</done>
</task>

</tasks>

<verification>
ORG-01 email invite path green with closed-signup accept.
</verification>

<success_criteria>
Invites create/list/revoke/accept complete; tokens hashed; D-ORG-03 both paths present with username path from 05.
</success_criteria>

<output>
Create `.planning/phases/10-orgs-permissions/10-06-SUMMARY.md` when done
</output>
