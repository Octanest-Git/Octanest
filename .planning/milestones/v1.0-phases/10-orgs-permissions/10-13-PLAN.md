---
phase: 10-orgs-permissions
plan: "13"
type: tracer
wave: 2
depends_on:
  - "02"
files_modified:
  - crates/oxidean-api/src/org/mod.rs
  - crates/oxidean-api/src/rpc.rs
  - crates/oxidean-api/tests/org_create_members.rs
  - packages/api-client/src/index.ts
  - apps/web/src/routes/orgs.new.tsrx
  - apps/web/src/routes/orgs.new.integration.test.ts
requirements:
  - ORG-01
autonomous: true
user_setup: []
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Verified user can create an organization with a shared-namespace slug and become Owner (ORG-01, D-ORG-01, D-ORG-02a)"
    - "/orgs/new creates via org.create and navigates to /{slug}"
    - "make rpc-gen emits org.create; rpc-sync-check passes"
  artifacts:
    - path: crates/oxidean-api/src/org/mod.rs
      provides: "org.create RPC"
    - path: apps/web/src/routes/orgs.new.tsrx
      provides: "/orgs/new tracer UI"
    - path: packages/api-client/src/index.ts
      provides: "generated org.create client"
  key_links:
    - from: apps/web/src/routes/orgs.new.tsrx
      to: crates/oxidean-api/src/org/mod.rs
      via: "org.create after make rpc-gen"
    - from: crates/oxidean-api/src/org/mod.rs
      to: crates/oxidean-db/src/organizations.rs
      via: "Database facade insert + Owner membership row"
  assumptions:
    - "ASSUME: Plan 02 already landed 0010 + DB helpers + org_types"
    - "ASSUME: Full org overview UI may wait for plan 10; create must still navigate to /{slug}"
  prohibitions:
    - statement: "Must not implement teams"
      flagged: true
    - statement: "Must not hand-edit api-client as lasting source of truth — run make rpc-gen"
      flagged: true
    - statement: "Must not place dialect SQL in oxidean-api"
      flagged: true
---

<objective>
Ship the Phase 10 tracer: org.create RPC, generated client, and /orgs/new on top of the 0010 schema door from plan 02.

Purpose: Prove create-organization end-to-end before expanding members, invites, ACL consumers, and collaborators (tracer-first, D-ORG-01, ORG-01).
Output: Green org.create path + /orgs/new.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-orgs-permissions/10-CONTEXT.md
@.planning/phases/10-orgs-permissions/10-RESEARCH.md
@.planning/phases/10-orgs-permissions/10-UI-SPEC.md
@.planning/phases/10-orgs-permissions/10-02-SUMMARY.md
@apps/web/src/routes/new.tsrx
@.agents/skills/octane/SKILL.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → org.create | Untrusted slug/display_name; require_verified |
| Shared slug namespace | User username ↔ org slug collision = takeover risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Tampering | org slug | high | mitigate | validate_username + is_reserved_username + transactional uniqueness vs users.username and organizations.slug (D-ORG-01) |
| T-10-05 | Elevation of privilege | org.create | medium | mitigate | require_verified; creator sole Owner membership row |
| T-10-SC | Tampering | packages | high | mitigate | No new crates.io/npm packages |
</threat_model>

<tasks>

<task type="tracer">
  <name>End-to-end org.create — RPC + /orgs/new</name>
  <reversibility rating="one-way">Shared slug create path depends on 0010 polymorphic owner door (D-ORG-01); RPC naming is a published contract once clients generate.</reversibility>
  <files>crates/oxidean-api/src/org/mod.rs, crates/oxidean-api/src/rpc.rs, packages/api-client/src/index.ts, apps/web/src/routes/orgs.new.tsrx, crates/oxidean-api/tests/org_create_members.rs, apps/web/src/routes/orgs.new.integration.test.ts</files>
  <read_first>.planning/phases/10-orgs-permissions/10-RESEARCH.md, crates/oxidean-api/src/repo/mod.rs, crates/oxidean-db/src/organizations.rs, apps/web/src/routes/new.tsrx, .agents/skills/octane/SKILL.md</read_first>
  <action>
Implement org.create RPC (require_verified, validate_username, reserved check, dual uniqueness vs users+orgs, creator Owner via plan-02 DB helpers). Wire rpc.rs + make rpc-gen. Ship minimal /orgs/new Octane form per 10-UI-SPEC (slug + optional display name → org.create → navigate /{slug}). Turn green the org.create happy-path tests in org_create_members.rs and orgs.new.integration.test.ts. Stub org.get overview route only if needed for navigation target — full overview UI may be plan 10. Do not ship members/invites/collaborator RPCs, ACL rewrite, or Smart HTTP changes. Do not implement teams (D-ORG-07).
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(org_create)' && make rpc-gen && make rpc-sync-check && bun --cwd apps/web exec vitest run src/routes/orgs.new.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>org_create nextest fails; rpc-sync-check fails; vitest or web build exits non-zero</fails_when>
  </verify>
  <done>One verified user can create an org via RPC and /orgs/new; creator is Owner</done>
</task>

</tasks>

<verification>
Tracer happy path: org.create + UI green; members/ACL expansion deferred.
</verification>

<success_criteria>
ORG-01 create-organization slice works end-to-end through /orgs/new; no teams.
</success_criteria>

<output>
Create `.planning/phases/10-orgs-permissions/10-13-SUMMARY.md` when done
</output>
