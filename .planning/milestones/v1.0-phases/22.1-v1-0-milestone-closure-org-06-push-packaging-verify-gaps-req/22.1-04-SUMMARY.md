---
phase: 22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req
plan: "04"
subsystem: git-protection
tags: [org-06, branch-protection, boot-sweep, update-hook, d-pkg-04]

requires:
  - phase: 22.1-01
    provides: packaged helper + Compose/Smart HTTP path
  - phase: 22.1-02
    provides: fail-closed update hook script (D-PKG-02); install_protection_hooks overwrite semantics
  - phase: 13-branch-protection
    provides: install_hooks / hooks_installed / reconcile_hooks

provides:
  - sweep_protection_hooks(repos_dir) bare walk with install overwrite (D-PKG-04)
  - API boot invokes sweep after AppState::new before background jobs
  - nextest coverage for missing-hooks install, outdated overwrite, non-bare skip

affects:
  - v1.0 milestone audit re-run (ORG-06 upgrade path for pre-existing forks)
  - 22.1 remaining plans (verify/windows hygiene)

actuals:
  tokens: 3732
  tasks: 2
  commits: 3

plan_head_before: 0ce8e3c9da5098f962e09cd6dda1c66f937e4a82

tech-stack:
  added: []
  patterns:
    - "Serial owner/*.git walk with HEAD+objects bare detection"
    - "install_hooks overwrite on boot; never reconcile-only for D-PKG-04"

key-files:
  created:
    - .planning/phases/22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req/.tdd/22.1-04-t1-red-evidence.json
  modified:
    - crates/oxidean-api/src/protection/mod.rs
    - crates/oxidean-api/src/main.rs
    - crates/oxidean-api/tests/branch_protect_push.rs

key-decisions:
  - "Sweep uses install_hooks (overwrite) so D-PKG-02 script upgrades land on existing bares"
  - "Per-repo install failures increment SweepHooksStats.errors and log; listen continues"
  - "Progress tracing every SWEEP_PROGRESS_EVERY=25 scanned bares (A4 serial walk)"

patterns-established:
  - "looks_like_bare_repo(HEAD file + objects dir) before install"
  - "Boot: AppState::new → sweep_protection_hooks → spawn_background_jobs"

requirements-completed: [ORG-06]

coverage:
  - id: D1
    description: Bare under repos_dir without hooks becomes hooks_installed after sweep
    requirement: ORG-06
    verification:
      - kind: unit
        ref: "cargo nextest run -p oxidean-api -E 'test(sweep_protection_hooks_installs_missing_hooks)'"
        status: pass
    human_judgment: false
  - id: D2
    description: Outdated update hook script is overwritten (install, not reconcile-skip)
    requirement: ORG-06
    verification:
      - kind: unit
        ref: "cargo nextest run -p oxidean-api -E 'test(sweep_protection_hooks_overwrites_outdated_hook)'"
        status: pass
    human_judgment: false
  - id: D3
    description: Non-bare entries skipped without panic; main boot calls sweep_protection_hooks
    requirement: ORG-06
    verification:
      - kind: unit
        ref: "cargo nextest run -p oxidean-api -E 'test(sweep)'; rg sweep_protection_hooks crates/oxidean-api/src/main.rs"
        status: pass
    human_judgment: false

duration: 5min
completed: 2026-09-19
status: complete
---

# Phase 22.1 Plan 04: Boot Protection Hook Sweep Summary

**API boot walks `OXIDEAN_REPOS_DIR` and overwrites protection `hooks/update` on every bare repo so packaged fail-closed scripts reach pre-phase forks (D-PKG-04).**

## Performance

- **Duration:** 5 min
- **Started:** 2026-09-19T16:22:03Z
- **Completed:** 2026-09-19T16:27:10Z
- **Tasks:** 2/2
- **Files modified:** 3 (+ RED evidence)

## Accomplishments

- `sweep_protection_hooks` walks `owner/*.git`, detects bare via HEAD+objects, calls `install_hooks` overwrite (never reconcile-only)
- Serial progress logs every 25 scanned repos; per-repo errors counted without aborting the sweep
- `main` awaits the sweep after `AppState::new` and before `spawn_background_jobs`; aggregate errors warn but listen continues

## Task Commits

1. **Task 1 RED:** `e3e3f90` — test(22.1-04): RED boot sweep protection hooks coverage
2. **Task 1 GREEN:** `018e383` — feat(22.1-04): implement sweep_protection_hooks overwrite walker
3. **Task 2:** `abc1096` — feat(22.1-04): invoke protection hook sweep on API boot

## Files Created/Modified

- `crates/oxidean-api/src/protection/mod.rs` — `SweepHooksStats`, `looks_like_bare_repo`, `sweep_protection_hooks`
- `crates/oxidean-api/src/main.rs` — boot-time sweep on `state.repos_dir`
- `crates/oxidean-api/tests/branch_protect_push.rs` — install / overwrite / non-bare skip nextest
- `.tdd/22.1-04-t1-red-evidence.json` — RED gate evidence (`RED_EVIDENCE_OK`)

## Decisions Made

- Prefer install overwrite over reconcile so stale fail-open scripts are replaced on upgrade
- Keep SSH spawn and migrate ordering unchanged; sweep sits after AppState construction only

## Deviations from Plan

None — plan executed as written (TDD RED→GREEN for Task 1; boot wire-up for Task 2).

### Auto-fixed Issues

**1. [Rule 1 - Bug] while-let + match `break` type mismatch**
- **Found during:** Task 1 GREEN compile
- **Issue:** `while let Some(_) = match { Ok(e) => e, Err(_) => break }` made the match return `()` on error arms
- **Fix:** Restructured to explicit `loop` + `Ok(Some)/Ok(None)/Err` matching (same pattern as serial walk intent)
- **Files modified:** `crates/oxidean-api/src/protection/mod.rs`
- **Commit:** `018e383`

## TDD Gate Compliance

| Gate | Commit | Evidence |
|------|--------|----------|
| RED | `e3e3f90` | `.tdd/22.1-04-t1-red-evidence.json` — `RED_EVIDENCE_OK` (`sweep_protection_hooks_installs_missing_hooks`) |
| GREEN | `018e383` | nextest `test(sweep) \| test(branch_protect)`: 14 passed |

## Threat Flags

None — boot filesystem walk stays under `OXIDEAN_REPOS_DIR` and only rewrites the known protection hook script (T-22.1-03 / T-22.1-01 mitigated as planned).

## Known Stubs

None.

## Self-Check: PASSED
