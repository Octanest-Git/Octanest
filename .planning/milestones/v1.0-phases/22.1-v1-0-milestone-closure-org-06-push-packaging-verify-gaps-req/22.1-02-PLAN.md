---
phase: 22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req
plan: "02"
type: execute
wave: 2
depends_on:
  - "22.1-01"
files_modified:
  - crates/octanest-git/src/cli.rs
  - crates/octanest-api/src/repo/mod.rs
  - crates/octanest-api/tests/repo_fork.rs
  - crates/octanest-api/tests/branch_protect_push.rs
requirements:
  - ORG-06
autonomous: true
user_setup: []
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Update hook script fail-closed when helper missing and OCTANEST_ENV is production or cloud; fail-open for development/dev/compose (D-PKG-02)"
    - "clone_bare installs hooks/update; successful repo.fork leaves hooks_installed true (D-FORK-01, D-FORK-02, D-FORK-03)"
    - "If hook install fails after clone_bare, fork RPC compensates storage and returns repo.fork_failed (D-FORK-04)"
    - statement: "ORG-06 unclassified — fork path packaging reviewed against hooks_installed evidence"
      verification: backstop
  artifacts:
    - path: crates/octanest-git/src/cli.rs
      provides: "production-aware update hook script + install_protection_hooks after clone_bare"
    - path: crates/octanest-api/tests/repo_fork.rs
      provides: "hooks_installed assert + fork_failed compensate coverage"
  key_links:
    - from: crates/octanest-git/src/cli.rs
      to: crates/octanest-api/src/repo/mod.rs
      via: "clone_bare GitError → compensate_failed_create + repo.fork_failed"
  assumptions:
    - "ASSUME: Reuse repo.fork_failed for hook-install failure (RESEARCH A2)"
  prohibitions:
    - statement: "Must not leave clone_bare without install_protection_hooks"
      flagged: true
    - statement: "Must not keep production/cloud fail-open when helper is missing"
      flagged: true
---

<objective>
Harden update-hook fail-open semantics for production/cloud (D-PKG-02) and install protection hooks on every `clone_bare` / fork path (D-FORK-01…04).

Purpose: Close audit WARNING that forks lack hooks and production images cannot silently skip the helper.
Output: Updated hook script + clone_bare install + fork tests green.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req/22.1-CONTEXT.md
@.planning/phases/22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req/22.1-RESEARCH.md
@.planning/phases/22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req/22.1-PATTERNS.md
@crates/octanest-git/src/cli.rs
@crates/octanest-api/src/repo/mod.rs
@crates/octanest-api/tests/repo_fork.rs
@crates/octanest-api/tests/branch_protect_push.rs
@crates/octanest-api/src/webhook/deliver.rs
</context>

## Artifacts this phase produces

- Production/cloud fail-closed update hook script (D-PKG-02)
- `clone_bare` hook install + fork compensate path (D-FORK-*)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Update hook → helper binary | Missing helper must not allow protected updates in production/cloud |
| Fork RPC → disk bare | Hook install failure must not leave orphan unprotected bares |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22.1-01 | Elevation of privilege | Fail-open missing helper | high | mitigate | D-PKG-02 production\|cloud exit 1 when helper missing |
| T-22.1-03 | Tampering | Fork without hooks | high | mitigate | install_protection_hooks in clone_bare; compensate on failure (D-FORK-02, D-FORK-04) |
| T-22.1-SC | Tampering | packages | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Production/cloud fail-closed update hook script</name>
  <files>crates/octanest-git/src/cli.rs, crates/octanest-api/tests/branch_protect_push.rs</files>
  <read_first>crates/octanest-git/src/cli.rs, crates/octanest-api/src/webhook/deliver.rs, crates/octanest-api/tests/branch_protect_push.rs</read_first>
  <behavior>
    - Missing/non-executable helper + OCTANEST_ENV=production|cloud → update hook exits non-zero with stderr message
    - Missing helper + OCTANEST_ENV=compose|development|dev (or default development) → exit 0
    - When helper is executable, script execs helper with ref args
  </behavior>
  <action>
Per D-PKG-02: Change the embedded hooks/update script in install_protection_hooks to gate missing helper on OCTANEST_ENV matching production or cloud (mirror webhook deliver.rs production|cloud signal). Keep fail-open for compose/development/dev so local git without helper still works. Add nextest coverage for script behavior (install then invoke with env). Note: reconcile_protection_hooks alone will not upgrade old scripts — boot sweep in 22.1-04 overwrites via install.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(branch_protect) | test(fail_closed) | test(protection_hook_script)' ; rg -n "production\|cloud|OCTANEST_ENV" crates/octanest-git/src/cli.rs | head -20</automated>
    <fails_when>nextest non-zero; or cli.rs update-hook script lacks production/cloud fail-closed case</fails_when>
  </verify>
  <acceptance_criteria>
    - production/cloud missing helper denies updates
    - compose/development missing helper still fail-open
    - Automated tests cover both branches
  </acceptance_criteria>
  <done>D-PKG-02 fail-closed semantics installed and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: clone_bare installs hooks; fork fails closed on install error</name>
  <files>crates/octanest-git/src/cli.rs, crates/octanest-api/src/repo/mod.rs, crates/octanest-api/tests/repo_fork.rs</files>
  <read_first>crates/octanest-git/src/cli.rs, crates/octanest-api/src/repo/mod.rs, crates/octanest-api/tests/repo_fork.rs, crates/octanest-api/tests/branch_protect_push.rs</read_first>
  <behavior>
    - After successful clone_bare, hooks_installed(dest) is true
    - After successful repo.fork, dest bare has hooks/update
    - Hook-install failure after clone causes repo.fork_failed and compensate_failed_create (no leftover live fork row/bare)
  </behavior>
  <action>
Per D-FORK-01…04: After successful `git clone --bare` in clone_bare, call install_protection_hooks on dest (same pattern as init_bare). Propagate GitError so existing fork RPC compensate_failed_create + repo.fork_failed path covers hook-install failure (reuse code unless a more specific code is clearly justified). Extend repo_fork.rs with hooks_installed assert on happy path and a forced install-failure case expecting fork_failed + compensation.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(repo_fork) | test(fork)' ; rg -n "install_protection_hooks" crates/octanest-git/src/cli.rs | head -20</automated>
    <fails_when>nextest non-zero; or clone_bare path has no install_protection_hooks call</fails_when>
  </verify>
  <acceptance_criteria>
    - clone_bare installs hooks/update
    - fork happy path asserts hooks_installed
    - hook install failure fails the fork with compensation
  </acceptance_criteria>
  <done>Forks always get protection hooks or fail with compensation.</done>
</task>

</tasks>

<verification>
Fail-closed script nextest green; clone_bare installs hooks; repo_fork hooks_installed + fork_failed compensate green.
</verification>

<success_criteria>
D-PKG-02 and D-FORK-01…04 satisfied with automated proof.
</success_criteria>

<output>
Create `.planning/phases/22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req/22.1-02-SUMMARY.md` when done
</output>
