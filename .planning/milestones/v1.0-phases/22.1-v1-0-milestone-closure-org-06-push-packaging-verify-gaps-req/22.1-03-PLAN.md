---
phase: 22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req
plan: "03"
type: execute
wave: 2
depends_on:
  - "22.1-01"
files_modified:
  - crates/oxidean-api/src/ssh/pack.rs
  - crates/oxidean-api/src/ssh/server.rs
  - scripts/compose-smoke-protection.sh
  - scripts/smoke-git-ssh.sh
  - Makefile
requirements:
  - ORG-06
autonomous: true
user_setup:
  - service: docker
    why: "SSH protected-push smoke needs Compose TCP 2222 on Docker-capable host"
    env_vars: []
    dashboard_config: []
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "SSH receive-pack child receives OXIDEAN_PROTECTION_HELPER (env or default_helper_path), OXIDEAN_DATABASE_URL, OXIDEAN_REPOS_DIR, OXIDEAN_ACTOR_CAPABILITY, and OXIDEAN_ENV (D-PKG-01)"
    - "AuthzDecision::Allow carries actor capability so Admin bypass works when enforce_admins=false over SSH (D-PKG-01 / Phase 13 D-16)"
    - "When Compose publishes SSH TCP and SMOKE_SKIP_LS_REMOTE is unset, protected-branch SSH push is denied (D-PKG-03)"
    - statement: "ORG-06 unclassified — SSH packaging reviewed via env pass-through + optional smoke"
      verification: backstop
  artifacts:
    - path: crates/oxidean-api/src/ssh/pack.rs
      provides: "receive-pack protection env + capability on Allow"
    - path: crates/oxidean-api/src/ssh/server.rs
      provides: "capability threaded into run_pack_command"
  key_links:
    - from: crates/oxidean-api/src/ssh/server.rs
      to: crates/oxidean-api/src/ssh/pack.rs
      via: "Allow.capability → OXIDEAN_ACTOR_CAPABILITY on receive-pack"
    - from: scripts/compose-smoke-protection.sh
      to: docker-compose.yml
      via: "SSH TCP 2222 denial when reachable"
  assumptions:
    - "ASSUME: HTTPS denial remains mandatory; SSH denial is required when TCP reachable and not skip-flagged (RESEARCH open Q3)"
  prohibitions:
    - statement: "Must not omit OXIDEAN_ACTOR_CAPABILITY on SSH receive-pack"
      flagged: true
    - statement: "Must not change upload-pack ACL behavior while wiring receive-pack env"
      flagged: true
---

<objective>
Complete D-PKG-01 SSH half: pass protection helper, DB, repos, actor capability, and OXIDEAN_ENV into git receive-pack; prove SSH protected-push denial when Compose exposes SSH.

Purpose: Close the audit gap where SSH inherit-only treated Admins as Read and never saw the helper.
Output: SSH pack env wiring + optional Compose SSH denial in protection smoke.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req/22.1-RESEARCH.md
@.planning/phases/22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req/22.1-PATTERNS.md
@crates/oxidean-api/src/ssh/pack.rs
@crates/oxidean-api/src/ssh/server.rs
@crates/oxidean-api/src/git/http_backend.rs
@crates/oxidean-api/src/protection/mod.rs
@scripts/smoke-git-ssh.sh
@scripts/compose-smoke-protection.sh
</context>

## Artifacts this phase produces

- SSH receive-pack protection env + capability (D-PKG-01)
- Compose SSH protected-push denial when TCP up (D-PKG-03)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSH session → git receive-pack | Child must see helper + capability; otherwise Admin bypass / protection fail-open |
| Host → Compose :2222 | Optional smoke path when TCP published |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22.1-02 | Elevation of privilege | SSH env omission | high | mitigate | Set helper/DB/repos/capability/ENV on receive-pack only |
| T-22.1-SC | Tampering | packages | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SSH Allow capability + receive-pack protection env</name>
  <files>crates/oxidean-api/src/ssh/pack.rs, crates/oxidean-api/src/ssh/server.rs</files>
  <read_first>crates/oxidean-api/src/ssh/pack.rs, crates/oxidean-api/src/ssh/server.rs, crates/oxidean-api/src/git/http_backend.rs, crates/oxidean-api/src/protection/mod.rs</read_first>
  <behavior>
    - AuthzDecision::Allow includes actor capability label used for OXIDEAN_ACTOR_CAPABILITY
    - receive-pack Command sets OXIDEAN_PROTECTION_HELPER, OXIDEAN_DATABASE_URL, OXIDEAN_REPOS_DIR, OXIDEAN_ACTOR_CAPABILITY, OXIDEAN_ENV
    - upload-pack path does not require protection env injection
  </behavior>
  <action>
Per D-PKG-01: Extend AuthzDecision::Allow with the already-computed capability (or pass parallel arg). Update server.rs destructure and run_pack_command signature so receive-pack sets helper (env or default_helper_path), database URL, repos dir, actor capability, and OXIDEAN_ENV — mirror http_backend ProtectionCgiEnv re-injection. Do not alter upload-pack authorization semantics. Add unit/integration coverage where feasible (existing git_ssh / branch_protect patterns).
  </action>
  <verify>
    <automated>rg -n "OXIDEAN_PROTECTION_HELPER|OXIDEAN_ACTOR_CAPABILITY|OXIDEAN_ENV" crates/oxidean-api/src/ssh/pack.rs | head -30; cargo nextest run -p oxidean-api -E 'test(git_ssh) | test(ssh) | test(branch_protect)'</automated>
    <fails_when>pack.rs lacks protection env sets; nextest exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - receive-pack spawn sets all five protection-related env vars
    - Allow carries capability into OXIDEAN_ACTOR_CAPABILITY
    - Related nextest still green
  </acceptance_criteria>
  <done>SSH receive-pack can run update hooks with correct helper and capability.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Compose SSH protected-push denial when TCP reachable</name>
  <files>scripts/compose-smoke-protection.sh, scripts/smoke-git-ssh.sh, Makefile</files>
  <read_first>scripts/compose-smoke-protection.sh, scripts/smoke-git-ssh.sh, docker-compose.yml, Makefile</read_first>
  <action>
Per D-PKG-03: Extend the protection smoke so when SSH TCP 2222 is reachable and SMOKE_SKIP_LS_REMOTE is not set to 1, attempt SSH push to the same protected ref and expect non-zero. When skip flag is set or TCP unreachable, document skip without failing the HTTPS-mandatory path. Reuse smoke-git-ssh auth patterns; do not invent a new CI job type.
  </action>
  <verify>
    <automated>rg -n "2222|SMOKE_SKIP_LS_REMOTE|protected|receive-pack" scripts/compose-smoke-protection.sh scripts/smoke-git-ssh.sh Makefile | head -40</automated>
    <fails_when>protection smoke has no SSH denial branch or skip documentation</fails_when>
  </verify>
  <acceptance_criteria>
    - SSH denial attempted when TCP up and not skip-flagged
    - HTTPS denial remains mandatory regardless of SSH skip
  </acceptance_criteria>
  <done>D-PKG-03 SSH half covered in Compose protection smoke.</done>
</task>

</tasks>

<verification>
SSH pack env present; capability threaded; protection smoke documents/runs SSH denial when TCP up.
</verification>

<success_criteria>
ORG-06 SSH ship path wires helper+capability and can prove denial in Compose when SSH is exposed.
</success_criteria>

<output>
Create `.planning/phases/22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req/22.1-03-SUMMARY.md` when done
</output>
