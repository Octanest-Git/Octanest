---
phase: 22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/oxidean-api/Dockerfile
  - docker-compose.yml
  - crates/oxidean-api/src/routes/git_smart_http.rs
  - crates/oxidean-api/src/git/http_backend.rs
  - scripts/compose-smoke.sh
  - scripts/compose-smoke-protection.sh
  - Makefile
  - docs/TESTING.md
  - crates/oxidean-api/tests/branch_protect_push.rs
requirements:
  - ORG-06
autonomous: true
user_setup:
  - service: docker
    why: "D-PKG-03 Compose protected-push smoke needs Docker engine on the executor host"
    env_vars: []
    dashboard_config: []
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "API image contains executable /usr/local/bin/oxidean-protection-hook and Compose sets OXIDEAN_PROTECTION_HELPER to that path (D-PKG-01, ORG-06)"
    - "After Compose bring-up, a direct HTTPS push to a protected branch requiring reviews is denied (D-PKG-03, ORG-06)"
    - "Smart HTTP receive-pack resolves OXIDEAN_PROTECTION_HELPER from env or default_helper_path() and re-injects helper + OXIDEAN_ENV after CGI env_clear (D-PKG-01)"
    - statement: "ORG-06 unclassified edge — review packaging path manually against audit evidence (helper in image, Compose env, HTTPS denial)"
      verification: backstop
    - statement: "If Compose smoke is interrupted mid-run, teardown leaves no orphaned published protected-push assertions (retry from clean compose down)"
      verification: backstop
  artifacts:
    - path: crates/oxidean-api/Dockerfile
      provides: "release build + COPY of oxidean-protection-hook beside oxidean-api"
    - path: docker-compose.yml
      provides: "OXIDEAN_PROTECTION_HELPER=/usr/local/bin/oxidean-protection-hook on api"
    - path: scripts/compose-smoke-protection.sh
      provides: "helper executable assert + HTTPS protected-push denial"
    - path: crates/oxidean-api/src/routes/git_smart_http.rs
      provides: "env OR default_helper_path() for ProtectionCgiEnv"
  key_links:
    - from: docker-compose.yml
      to: crates/oxidean-api/Dockerfile
      via: "OXIDEAN_PROTECTION_HELPER path matches COPY destination"
    - from: crates/oxidean-api/src/routes/git_smart_http.rs
      to: crates/oxidean-api/src/git/http_backend.rs
      via: "ProtectionCgiEnv helper_path + OXIDEAN_ENV after env_clear"
    - from: scripts/compose-smoke-protection.sh
      to: Makefile
      via: "make smoke or sibling target invokes protection assert"
  assumptions:
    - "ASSUME: EDGE ORG-06 unclassified — treat audit packaging checklist as the acceptance review surface"
    - "ASSUME: Compose OXIDEAN_ENV=compose stays fail-open for missing helper; smoke must test -x helper (RESEARCH A1)"
  prohibitions:
    - statement: "Must not ship a second API Dockerfile for Compose vs cloud"
      flagged: true
    - statement: "Must not leave OXIDEAN_PROTECTION_HELPER unset in Compose while claiming ORG-06 push closed"
      flagged: true
    - statement: "Must not rely on ambient process env for Smart HTTP CGI children after env_clear"
      flagged: true
---

<objective>
Prove ORG-06 direct-push packaging end-to-end: helper binary in the API image, Compose env, Smart HTTP helper resolution, and HTTPS protected-branch push denied in Compose smoke (D-PKG-01, D-PKG-03).

Purpose: Close the v1.0 audit packaging blocker on the thinnest ship path before SSH/fail-closed/boot/fork expansions (D-VER-04).
Output: Multi-bin Dockerfile + Compose env + Smart HTTP fallback + protection smoke green on Docker-capable host.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req/22.1-CONTEXT.md
@.planning/phases/22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req/22.1-RESEARCH.md
@.planning/phases/22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req/22.1-PATTERNS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@crates/oxidean-api/Dockerfile
@docker-compose.yml
@crates/oxidean-api/src/routes/git_smart_http.rs
@crates/oxidean-api/src/git/http_backend.rs
@crates/oxidean-api/src/protection/mod.rs
@scripts/compose-smoke.sh
@scripts/smoke-git-https.sh
@Makefile
</context>

## Artifacts this phase produces

- Shipped `oxidean-protection-hook` in API image + Compose `OXIDEAN_PROTECTION_HELPER`
- Compose protection smoke proving HTTPS protected-push denial
- Smart HTTP `default_helper_path()` fallback + CGI `OXIDEAN_ENV` forward
- (Later plans) SSH env, fail-closed hooks, boot sweep, fork hooks, VERIFICATION/Nyquist/WINDOWS/PLAT hygiene

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host git client → Traefik/API Smart HTTP | Untrusted push payloads; protection must deny when rules require reviews |
| API process → CGI git-http-backend | env_clear drops ambient env; helper/DB/repos/capability/ENV must be re-injected |
| Image filesystem | Helper path must be image-owned, not user-writable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22.1-01 | Elevation of privilege | Missing helper in production path | high | mitigate | Package helper + Compose env; smoke asserts executable + HTTPS denial (D-PKG-01, D-PKG-03) |
| T-22.1-02 | Tampering | Smart HTTP env_clear drops helper | high | mitigate | Pass ProtectionCgiEnv helper_path from env or default_helper_path(); forward OXIDEAN_ENV |
| T-22.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages this plan — in-tree binary only |
</threat_model>

<tasks>

<task type="tracer" tdd="false">
  <name>End-to-end ORG-06 packaging — Compose HTTPS protected push denied</name>
  <reversibility rating="costly">D-PKG-01: image layout + Compose env + HTTPS receive path become the ship contract; undo needs coordinated Dockerfile/Compose/Smart HTTP changes.</reversibility>
  <files>crates/oxidean-api/Dockerfile, docker-compose.yml, crates/oxidean-api/src/routes/git_smart_http.rs, crates/oxidean-api/src/git/http_backend.rs, scripts/compose-smoke-protection.sh, scripts/compose-smoke.sh, Makefile</files>
  <read_first>crates/oxidean-api/Dockerfile, docker-compose.yml, crates/oxidean-api/src/routes/git_smart_http.rs, crates/oxidean-api/src/git/http_backend.rs, crates/oxidean-api/src/protection/mod.rs, scripts/compose-smoke.sh, scripts/smoke-git-https.sh, Makefile</read_first>
  <action>
Per D-PKG-01 and D-PKG-03: Extend the existing API Dockerfile builder to `cargo build --release -p oxidean-api --bin oxidean-api --bin oxidean-protection-hook` and COPY both binaries to `/usr/local/bin/`. Under `api.environment` in docker-compose.yml set `OXIDEAN_PROTECTION_HELPER: /usr/local/bin/oxidean-protection-hook` beside `OXIDEAN_REPOS_DIR`. In git_smart_http receive-pack path, resolve helper as non-empty `OXIDEAN_PROTECTION_HELPER` OR `crate::protection::default_helper_path()` mapped to string; keep the existing receive+DB URL gate. Extend ProtectionCgiEnv (or equivalent) so http_backend re-injects `OXIDEAN_ENV` after `env_clear` along with helper/DB/repos/capability. Add scripts/compose-smoke-protection.sh (or tightly scoped extension of compose-smoke.sh) that after stack health: `docker compose exec -T api test -x /usr/local/bin/oxidean-protection-hook`, creates a repo + branch-protection rule requiring reviews (reuse smoke-git-https PAT/auth patterns), attempts HTTPS push to the protected ref, and expects non-zero. Wire a Make target (invoke from `make smoke` or a documented sibling used by smoke/CI) without inventing a parallel CI job type. Commit-local feedback is static wiring checks plus Task 2 nextest; run Compose protection smoke on Docker-capable hosts or at wave end (D-PKG-03) — do not block the tracer commit on a full Compose bring-up. Do not change SSH pack env, fail-closed script, boot sweep, or clone_bare in this tracer.
  </action>
  <verify>
    <automated>rg -n "oxidean-protection-hook|OXIDEAN_PROTECTION_HELPER" crates/oxidean-api/Dockerfile docker-compose.yml crates/oxidean-api/src/routes/git_smart_http.rs | head -40; test -f scripts/compose-smoke-protection.sh || rg -n "protection-hook|protected" scripts/compose-smoke.sh Makefile | head -30; rg -n "smoke-protection|compose-smoke-protection" Makefile | head -10</automated>
    <fails_when>rg finds no helper COPY or Compose OXIDEAN_PROTECTION_HELPER; protection smoke script/Make target missing</fails_when>
  </verify>
  <acceptance_criteria>
    - Dockerfile builds and copies oxidean-protection-hook to /usr/local/bin/oxidean-protection-hook
    - docker-compose.yml sets OXIDEAN_PROTECTION_HELPER to that path
    - Smart HTTP uses env or default_helper_path and CGI receives OXIDEAN_ENV
    - Protection smoke script/Make target exists; on Docker-capable host or wave end, smoke denies HTTPS push to a reviews-required protected branch (D-PKG-03)
  </acceptance_criteria>
  <done>Shipped-image helper + Compose env + Smart HTTP wiring land with a runnable protection smoke entrypoint; D-PKG-03 Compose denial proven on Docker-capable host or wave end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit coverage for Smart HTTP helper fallback when env unset</name>
  <files>crates/oxidean-api/tests/branch_protect_push.rs, crates/oxidean-api/src/routes/git_smart_http.rs, crates/oxidean-api/src/git/http_backend.rs</files>
  <read_first>crates/oxidean-api/tests/branch_protect_push.rs, crates/oxidean-api/src/protection/mod.rs, crates/oxidean-api/src/routes/git_smart_http.rs</read_first>
  <behavior>
    - When OXIDEAN_PROTECTION_HELPER is unset/empty and sibling default_helper_path exists, receive-pack protection env uses that path
    - When env is set non-empty, env wins over sibling path
  </behavior>
  <action>
Per D-PKG-01: Add nextest coverage near branch_protect / smart-http tests proving helper resolution prefers env when set and falls back to default_helper_path when unset. Prefer testing the resolution helper or CGI env builder without requiring full Docker. Do not weaken existing branch_protect_push assertions.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(branch_protect) | test(default_helper) | test(protection_helper)'</automated>
    <fails_when>nextest exits non-zero, or filter matches zero tests after the new cases should exist</fails_when>
  </verify>
  <acceptance_criteria>
    - Automated test proves env-unset path uses default_helper_path
    - Existing branch_protect nextest filter still green
  </acceptance_criteria>
  <done>Helper fallback behavior is covered by nextest without Docker.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document Make/smoke entry for protection assert</name>
  <files>Makefile, docs/TESTING.md</files>
  <read_first>Makefile, docs/TESTING.md, scripts/compose-smoke.sh</read_first>
  <action>
Per D-PKG-03 discretion: Ensure operators/CI can discover the protection smoke via Make (dedicated target and/or `make smoke` chain) and a TESTING.md row/note pointing at the script. Keep Phase 22 dialect smoke contracts intact.
  </action>
  <verify>
    <automated>rg -n "smoke-protection|compose-smoke-protection|protection-hook" Makefile docs/TESTING.md | head -20</automated>
    <fails_when>no Makefile or TESTING.md mention of the protection smoke entrypoint</fails_when>
  </verify>
  <acceptance_criteria>
    - Make documents how to run the protected-push smoke
    - docs/TESTING.md mentions the protection assert
  </acceptance_criteria>
  <done>Protection smoke is discoverable via Make and TESTING docs.</done>
</task>

</tasks>

<verification>
Dockerfile lists both bins; Compose sets helper; Smart HTTP fallback + OXIDEAN_ENV CGI forward present; protection smoke Make/script entrypoint exists; helper-fallback nextest green; Docker-capable host or wave-end `make smoke-protection` (or smoke chain) denies HTTPS protected push (D-PKG-03).
</verification>

<success_criteria>
ORG-06 push packaging tracer is green: helper in image, Compose wired, HTTPS denial in Compose smoke.
</success_criteria>

<output>
Create `.planning/phases/22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req/22.1-01-SUMMARY.md` when done
</output>
