---
phase: 09-git-ssh
plan: "04"
type: execute
wave: 3
depends_on:
  - "03"
files_modified:
  - crates/octanest-api/src/ssh/pack.rs
  - crates/octanest-api/src/ssh/auth.rs
  - crates/octanest-api/src/ssh/server.rs
  - crates/octanest-api/src/ssh/rate_limit.rs
  - crates/octanest-api/src/ssh_keys/mod.rs
  - crates/octanest-api/src/app.rs
  - crates/octanest-api/tests/git_ssh.rs
  - crates/octanest-api/tests/ssh_key_rpc.rs
requirements:
  - GIT-03
  - GIT-04
autonomous: true
user_setup: []
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Private non-owner fetch denied with clear git stderr error; push requires owner + verified email (D-SSH-04, GIT-03)"
    - "Only git-upload-pack and git-receive-pack exec succeed; other exec rejected (D-SSH-01)"
    - "Failed pubkey auth rate-limited per IP and fingerprint; successful auth clears fingerprint bucket; last_used updated (D-SSH-07)"
    - "Max 25 keys and duplicate fingerprint enforced on add (D-SSH-05)"
  artifacts:
    - path: crates/octanest-api/src/ssh/pack.rs
      provides: "ACL + receive-pack + git stderr denials"
    - path: crates/octanest-api/src/ssh/rate_limit.rs
      provides: "IP + fingerprint failed-auth limiter"
  key_links:
    - from: crates/octanest-api/src/ssh/pack.rs
      to: crates/octanest-api/src/repo/acl.rs
      via: "can_read_as_owner / is_private_visibility (D-SSH-04)"
  assumptions:
    - "ASSUME: rate limits mirror PAT 20/IP/15m and 10/fingerprint/15m (discretion)"
    - "ASSUME: count failures on auth_publickey reject only, not offered"
  prohibitions:
    - statement: "Must not fork ACL logic outside repo/acl helpers"
      flagged: true
    - statement: "Must not return HTTP status codes on SSH deny paths"
      flagged: true
---

<objective>
Expand the SSH tracer to full Smart HTTP ACL parity, pack allowlist, failed-auth rate limits, last-used tracking, and remaining key registration edge cases.

Purpose: Close GIT-03/04 security matrix after architecture is proven.
Output: Remaining git_ssh + ssh_key_rpc Wave 0 cases green.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-git-ssh/09-RESEARCH.md
@.planning/phases/09-git-ssh/09-03-SUMMARY.md
@crates/octanest-api/src/routes/git_smart_http.rs
@crates/octanest-api/src/repo/acl.rs
@crates/octanest-api/src/pat/rate_limit.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authed key → private repo | Owner-only until Phase 10; deny via git stderr |
| Unverified user → receive-pack | Must fail with verify-email git error |
| Internet → pubkey spray | Rate-limit IP + fingerprint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-08 | Information disclosure | private deny | medium | mitigate | Prefer forge-shaped not-found/permission git stderr; no HTTP codes (D-SSH-04) |
| T-09-09 | Denial of service | failed auth | high | mitigate | Sliding-window limiter IP+fingerprint (D-SSH-07) |
| T-09-10 | Elevation of privilege | unverified push | high | mitigate | Block receive-pack when email_verified_at null |
| T-09-SC | Tampering | packages | low | accept | No new packages; reuse FailedAuthLimiter pattern |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (09-04-T1): ACL parity + receive-pack + pack allowlist</name>
  <files>crates/octanest-api/src/ssh/pack.rs, crates/octanest-api/src/ssh/server.rs, crates/octanest-api/tests/git_ssh.rs</files>
  <read_first>crates/octanest-api/src/routes/git_smart_http.rs, crates/octanest-api/src/repo/acl.rs, crates/octanest-api/src/ssh/pack.rs, .planning/phases/09-git-ssh/09-RESEARCH.md</read_first>
  <behavior>
    - public upload-pack OK with any registered key (authenticated)
    - private non-owner → clear git stderr deny
    - receive-pack owner + verified → OK; unverified → email verification git error
    - non allowlisted exec → channel failure / reject
  </behavior>
  <action>
Extend pack handler per D-SSH-04: allow git-upload-pack and git-receive-pack only; parse path; resolve repo; call is_private_visibility / can_read_as_owner (same as Smart HTTP). Public fetch OK when authenticated; private requires owner; push requires owner and email_verified_at set — write clear git stderr messages then close channel (not HTTP 401/404). Touch last_used on successful authz. Green remaining ACL/pack allowlist tests in git_ssh.rs. Leave rate-limit cases for Task 2 if cleaner.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(git_ssh)'</automated>
    <fails_when>private non-owner allowed; or unverified receive-pack allowed; or shell exec succeeds</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'can_read_as_owner|is_private_visibility|git-receive-pack' crates/octanest-api/src/ssh/pack.rs`
    - `rg -n 'email_verified|Permission denied|not found|ERROR:' crates/octanest-api/src/ssh/pack.rs`
  </acceptance_criteria>
  <done>SSH ACL + receive-pack parity with Smart HTTP decisions</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (09-04-T2): Failed-auth rate limit + key edge cases</name>
  <files>crates/octanest-api/src/ssh/rate_limit.rs, crates/octanest-api/src/ssh/auth.rs, crates/octanest-api/src/app.rs, crates/octanest-api/src/ssh_keys/mod.rs, crates/octanest-api/tests/git_ssh.rs, crates/octanest-api/tests/ssh_key_rpc.rs</files>
  <read_first>crates/octanest-api/src/pat/rate_limit.rs, crates/octanest-api/src/ssh/auth.rs, crates/octanest-api/src/ssh_keys/mod.rs</read_first>
  <behavior>
    - after >20 failed auths per IP / 15m or >10 per fingerprint / 15m further auth rejected
    - success clears fingerprint bucket
    - 26th key and duplicate fingerprint rejected on add
  </behavior>
  <action>
Add or extend FailedAuthLimiter-style helper for SSH (reuse pat rate_limit module if fingerprint bucket fits cleanly, else ssh/rate_limit.rs). Wire on AppState. Record failures only on auth_publickey Reject (not offered). Clear fingerprint bucket on Accept. Enforce max 25 and duplicate fingerprint errors in sshKey.add if not already green. Green remaining Wave 0 git_ssh rate-limit and ssh_key_rpc edge tests (D-SSH-05, D-SSH-07).
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(ssh_key) | test(git_ssh)'</automated>
    <fails_when>exit != 0; or rate-limit tests still Wave-0-assert-false</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'fingerprint|check_ip|record|clear' crates/octanest-api/src/ssh/rate_limit.rs crates/octanest-api/src/ssh/auth.rs crates/octanest-api/src/pat/rate_limit.rs`
    - `rg -n '25|duplicate|fingerprint' crates/octanest-api/src/ssh_keys/mod.rs`
  </acceptance_criteria>
  <done>Rate limits + registration edge cases green</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| ACL + receive-pack | 09-04 | GIT-03 D-SSH-04 |
| Rate limit + max keys | 09-04 | D-SSH-05/07 |

<verification>
All git_ssh and ssh_key nextest filters green.
</verification>

<success_criteria>
SSH authz matches Smart HTTP owner/verify rules; spray limited; key caps enforced.
</success_criteria>

<output>
Create `.planning/phases/09-git-ssh/09-04-SUMMARY.md` when done
</output>
