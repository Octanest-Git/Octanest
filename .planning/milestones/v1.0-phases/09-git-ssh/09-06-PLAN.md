---
phase: 09-git-ssh
plan: "06"
type: execute
wave: 4
depends_on:
  - "03"
  - "04"
files_modified:
  - packages/api-client/src/index.ts
  - packages/api-client/src/types.ts
  - docs/API.md
requirements:
  - GIT-04
autonomous: true
user_setup: []
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "make rpc-gen emits sshKey.add/list/revoke client + query options; rpc-sync-check passes"
    - "docs/API.md documents SSH key RPCs and notes keys map to full account identity (no PAT scopes)"
  artifacts:
    - path: packages/api-client/src/index.ts
      provides: "Generated sshKey client"
    - path: docs/API.md
      provides: "SSH key + git-over-SSH API notes"
  key_links:
    - from: crates/oxidean-api/src/rpc.rs
      to: packages/api-client/src/index.ts
      via: "make rpc-gen"
  assumptions: []
  prohibitions:
    - statement: "Must not hand-edit generated client as source of truth"
      flagged: true
---

<objective>
Regenerate `@oxidean/api-client` for sshKey.* procedures and document SSH key RPC + git-over-SSH auth in API.md.

Purpose: Unblock web UI plans; keep client drift gate green.
Output: rpc-gen artifacts + API.md section.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-git-ssh/09-03-SUMMARY.md
@.planning/phases/09-git-ssh/09-04-SUMMARY.md
@docs/API.md
@crates/oxidean-api/src/rpc.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated client → browser | Session cookie credentials; no SSH private keys in RPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-13 | Information disclosure | API.md | low | mitigate | Document public keys only; no private key handling |
| T-09-SC | Tampering | generated client | medium | mitigate | Only via make rpc-gen + rpc-sync-check |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (09-06-T1): make rpc-gen + API.md SSH docs</name>
  <files>packages/api-client/src/index.ts, packages/api-client/src/types.ts, docs/API.md</files>
  <read_first>docs/API.md, crates/oxidean-api/src/rpc.rs, crates/oxidean-api/src/bin/rpc_gen.rs</read_first>
  <action>
Run make rpc-gen so packages/api-client exposes sshKey.add, sshKey.list, sshKey.revoke (and query helpers if generated). Run make rpc-sync-check. Update docs/API.md: session RPC shapes for add/list/revoke; require_verified on add; max 25; fingerprint uniqueness; note SSH transport uses registered keys as full account identity (no PAT scopes) and login user git (D-SSH-03, D-SSH-04, D-SSH-05). Do not hand-edit client sources except via rpc-gen output commit.
  </action>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && rg -n 'sshKey\.add|sshKey\.list|sshKey\.revoke' packages/api-client/src/ && rg -n 'sshKey|SSH key|git@' docs/API.md</automated>
    <fails_when>rpc-sync-check fails; or client missing sshKey procedures</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'sshKey' packages/api-client/src/`
    - `rg -n 'sshKey\.add|fingerprint|verified' docs/API.md`
  </acceptance_criteria>
  <done>Generated client + API docs cover SSH keys</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| api-client sshKey.* | 09-06 | UI prerequisite |
| API.md SSH section | 09-06 | Docs |

<verification>
rpc-sync-check green; API.md mentions sshKey procedures.
</verification>

<success_criteria>
Web can import generated sshKey client; docs match RPC.
</success_criteria>

<output>
Create `.planning/phases/09-git-ssh/09-06-SUMMARY.md` when done
</output>
