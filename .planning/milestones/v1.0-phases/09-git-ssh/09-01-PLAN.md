---
phase: 09-git-ssh
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/src/routes/settings/ssh-keys.integration.test.ts
  - apps/web/src/components/repo/clone-box.ssh.integration.test.ts
requirements:
  - GIT-03
  - GIT-04
autonomous: true
user_setup: []
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Wave 0 web stubs exist for /settings/ssh-keys list/add/revoke and CloneBox live SSH URL + add-key CTA (D-SSH-06, GIT-03, GIT-04)"
  artifacts:
    - path: apps/web/src/routes/settings/ssh-keys.integration.test.ts
      provides: "GIT-04 settings SSH keys UI failing stubs"
    - path: apps/web/src/components/repo/clone-box.ssh.integration.test.ts
      provides: "GIT-03 CloneBox SSH failing stubs"
  key_links:
    - from: apps/web/src/routes/settings/ssh-keys.integration.test.ts
      to: apps/web/src/routes/settings/ssh-keys.tsrx
      via: "Wave 0 stub greened by 09-07"
  assumptions: []
  prohibitions:
    - statement: "Must not implement production /settings/ssh-keys or live CloneBox SSH panel in this plan"
      flagged: true
---

<objective>
Wave 0 web integration stubs for `/settings/ssh-keys` and CloneBox SSH (parallel with 09-00 Rust stubs).

Purpose: Keep Rust Wave 0 and web Wave 0 parallel with thin file budgets.
Output: Discoverable Vitest stubs for D-SSH-06 UI paths per 09-UI-SPEC.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-git-ssh/09-UI-SPEC.md
@.planning/phases/09-git-ssh/09-CONTEXT.md
@.agents/skills/octane/SKILL.md
@apps/web/src/routes/settings/tokens.integration.test.ts
@apps/web/src/components/repo/clone-box.pat.integration.test.ts
@apps/web/src/components/repo/clone-box.tsrx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vitest harness → route stubs | Stubs encode verify-wall and revoke confirm before production routes exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03 | Elevation of privilege | ssh-keys stubs | medium | mitigate | Stub asserts unverified Add disabled + verify copy (D-SSH-05) |
| T-09-SC | Tampering | packages | low | accept | No new packages; vitest stubs only |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (09-01-T1): Web Wave 0 stubs for ssh-keys settings and CloneBox SSH</name>
  <files>apps/web/src/routes/settings/ssh-keys.integration.test.ts, apps/web/src/components/repo/clone-box.ssh.integration.test.ts</files>
  <read_first>apps/web/src/routes/settings/tokens.integration.test.ts, apps/web/src/components/repo/clone-box.pat.integration.test.ts, apps/web/src/components/repo/clone-box.tsrx, .planning/phases/09-git-ssh/09-UI-SPEC.md, .agents/skills/octane/SKILL.md</read_first>
  <action>
Add Vitest integration stubs (Phase 08 pattern; @vite-ignore dynamic import if route absent). ssh-keys.integration.test.ts must assert discoverable expectations for: page title SSH keys; SettingsNav sibling; empty hero No SSH keys; Add SSH key CTA; unverified list visible with Add disabled + Verify your email to add an SSH key.; revoke AlertDialog Revoke SSH key? / Keep key / Revoke key; fingerprint shown on list rows (GIT-04, D-SSH-05, D-SSH-06, UI-SPEC). clone-box.ssh.integration.test.ts must assert scp-style git@host:owner/repo.git primary string (not ssh:// as primary), Port hint when port is 2222, compact add-key CTA toward /settings/ssh-keys, and login user git (D-SSH-02, D-SSH-06, GIT-03). Tests may RED until production .tsrx exists. Do not implement production routes here (09-07/09-08).
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/settings/ssh-keys.integration.test.ts src/components/repo/clone-box.ssh.integration.test.ts</automated>
    <fails_when>vitest cannot find either stub file; or exit 0 with all green while production routes still absent (Wave 0 expects RED until later plans)</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/routes/settings/ssh-keys.integration.test.ts`
    - `test -f apps/web/src/components/repo/clone-box.ssh.integration.test.ts`
    - `rg -n 'SSH keys|Add SSH key|Keep key|Verify your email' apps/web/src/routes/settings/ssh-keys.integration.test.ts`
    - `rg -n 'git@|/settings/ssh-keys|Port|ssh://' apps/web/src/components/repo/clone-box.ssh.integration.test.ts`
  </acceptance_criteria>
  <done>Web Wave 0 stubs discoverable by vitest</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| ssh-keys Vitest stubs | 09-01 | Greened by 09-07 |
| CloneBox SSH Vitest stubs | 09-01 | Greened by 09-08 |

<verification>
Wave 0 web stubs exist for settings + CloneBox SSH.
</verification>

<success_criteria>
UI requirement paths have failing automated stub targets before greening.
</success_criteria>

<output>
Create `.planning/phases/09-git-ssh/09-01-SUMMARY.md` when done
</output>
