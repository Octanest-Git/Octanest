---
phase: 09-git-ssh
plan: "05"
type: execute
wave: 4
depends_on:
  - "03"
  - "04"
files_modified:
  - docker-compose.yml
  - .env.example
  - docs/dev-auth.env.example
  - scripts/smoke-git-ssh.sh
  - Makefile
requirements:
  - GIT-03
autonomous: true
user_setup: []
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Compose publishes TCP 2222 for SSH on the api service without Traefik HTTP routing (D-SSH-07)"
    - "Host keys persist via volume-backed OXIDEAN_SSH_HOST_KEY_DIR so restarts do not rotate TOFU keys"
    - "make smoke-git-ssh can ls-remote (and optionally push) over scp-style git@host:owner/repo.git on port 2222 (D-SSH-02, GIT-03)"
  artifacts:
    - path: docker-compose.yml
      provides: "TCP publish + SSH env for api"
    - path: scripts/smoke-git-ssh.sh
      provides: "Compose SSH smoke"
  key_links:
    - from: docker-compose.yml
      to: crates/oxidean-api/src/ssh/mod.rs
      via: "OXIDEAN_SSH_ENABLED + PORT + HOST_KEY_DIR"
  assumptions:
    - "ASSUME: Docker available for make up smoke; script skips exit 0 if docker missing (mirror HTTPS)"
    - "ASSUME: make dev may omit SSH listener unless OXIDEAN_SSH_ENABLED set (D-SSH-07)"
  prohibitions:
    - statement: "Must not add Traefik TCP/HTTP router for SSH"
      flagged: true
---

<objective>
Wire Compose TCP publish, SSH env, persisted host keys, and green the smoke-git-ssh script beside HTTPS smoke.

Purpose: Ops path for forge-shaped remotes on port 2222 without Traefik.
Output: Updated compose/env examples + working smoke target.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-git-ssh/09-RESEARCH.md
@.planning/phases/09-git-ssh/09-03-SUMMARY.md
@docker-compose.yml
@scripts/smoke-git-https.sh
@Makefile
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → host:2222 → api russh | Raw TCP; not Traefik |
| Volume → host key files | Persistence prevents TOFU churn / swap surprises |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-11 | Spoofing | host keys | high | mitigate | Persist under volume; generate-once if missing (RESEARCH pitfall 5) |
| T-09-12 | Elevation of privilege | Traefik bypass | medium | mitigate | Direct ports publish only (D-SSH-07) |
| T-09-SC | Tampering | packages | low | accept | Compose/Make/docs only |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (09-05-T1): Compose TCP 2222 + SSH env + host key volume</name>
  <files>docker-compose.yml, .env.example, docs/dev-auth.env.example</files>
  <read_first>docker-compose.yml, .env.example, .planning/phases/09-git-ssh/09-RESEARCH.md</read_first>
  <action>
On api service: set OXIDEAN_SSH_ENABLED=true, OXIDEAN_SSH_PORT=2222, OXIDEAN_SSH_HOST from public origin hostname fallback (document), OXIDEAN_SSH_HOST_KEY_DIR=/var/ssh; publish ports 2222:2222 (host TCP — not Traefik labels). Add volume for host keys sharing with api. Document new env vars in .env.example and docs/dev-auth.env.example with non-secret placeholders. Do not route SSH via Traefik HTTP (D-SSH-02, D-SSH-07).
  </action>
  <verify>
    <automated>rg -n '2222|OXIDEAN_SSH_ENABLED|OXIDEAN_SSH_HOST_KEY_DIR|OXIDEAN_SSH_PORT' docker-compose.yml .env.example</automated>
    <fails_when>2222 missing from compose ports; or Traefik router labels added for SSH</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n '2222:2222|OXIDEAN_SSH_ENABLED' docker-compose.yml`
    - `rg -n 'OXIDEAN_SSH_' .env.example`
  </acceptance_criteria>
  <done>Compose publishes SSH TCP with persisted host key dir</done>
</task>

<task type="auto">
  <name>Task 2 (09-05-T2): Green smoke-git-ssh</name>
  <files>scripts/smoke-git-ssh.sh, Makefile</files>
  <read_first>scripts/smoke-git-https.sh, scripts/smoke-git-ssh.sh, Makefile</read_first>
  <action>
Implement smoke-git-ssh.sh: skip exit 0 if docker missing; wait for health; generate ephemeral ed25519 key; register via RPC or documented test helper path used by HTTPS smoke patterns; GIT_SSH_COMMAND with -p 2222 and StrictHostKeyChecking accept-new for CI; git ls-remote git@{host}:{owner}/{repo}.git; optional push when env provided. Keep Make target. Do not assert Traefik HTML routing.
  </action>
  <verify>
    <automated>rg -n 'ls-remote|2222|GIT_SSH_COMMAND|git@' scripts/smoke-git-ssh.sh && rg -n 'smoke-git-ssh' Makefile</automated>
    <fails_when>script still Wave-0-only exit 1 without docker-skip path; or missing ls-remote</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'ls-remote|2222' scripts/smoke-git-ssh.sh`
    - `rg -n 'smoke-git-ssh' Makefile`
  </acceptance_criteria>
  <done>Smoke script ready for Compose phase gate</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| Compose TCP + env | 09-05 | D-SSH-07 |
| smoke-git-ssh | 09-05 | GIT-03 |

<verification>
Compose env + smoke script assertable; full smoke run at phase gate.
</verification>

<success_criteria>
Operators can expose SSH on 2222 and smoke ls-remote over scp-style remotes.
</success_criteria>

<output>
Create `.planning/phases/09-git-ssh/09-05-SUMMARY.md` when done
</output>
