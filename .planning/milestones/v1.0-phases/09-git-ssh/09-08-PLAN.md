---
phase: 09-git-ssh
plan: "08"
type: execute
wave: 6
depends_on:
  - "07"
  - "01"
files_modified:
  - apps/web/src/lib/public-origin.ts
  - apps/web/src/lib/public-origin.unit.test.ts
  - apps/web/src/components/repo/ssh-how-to.tsrx
  - apps/web/src/components/repo/clone-box.tsrx
  - apps/web/src/components/repo/clone-box.integration.test.ts
  - apps/web/src/components/repo/clone-box.ssh.integration.test.ts
requirements:
  - GIT-03
  - GIT-04
autonomous: true
user_setup: []
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CloneBox shows copyable scp-style git@{host}:{owner}/{repo}.git and Port hint when advertised port ≠ 22 (D-SSH-02)"
    - "Compact add-key CTA links to /settings/ssh-keys; Phase 7/8 SSH placeholder removed (D-SSH-06)"
  artifacts:
    - path: apps/web/src/lib/public-origin.ts
      provides: "sshCloneUrl helper"
    - path: apps/web/src/components/repo/ssh-how-to.tsrx
      provides: "PatHowTo-style SSH panel"
    - path: apps/web/src/components/repo/clone-box.tsrx
      provides: "Live SSH clone UI"
  key_links:
    - from: apps/web/src/components/repo/clone-box.tsrx
      to: apps/web/src/lib/public-origin.ts
      via: "sshCloneUrl(host, port, owner, repo)"
  assumptions:
    - "ASSUME: OXIDEAN_SSH_HOST / OXIDEAN_SSH_PORT available to web via existing public env pattern or loader — document in SUMMARY if vite env prefix required"
  prohibitions:
    - statement: "Must not make ssh:// the primary CloneBox copy string"
      flagged: true
---

<objective>
Replace CloneBox SSH placeholder with live scp-style URL, Port hint, and compact add-key how-to CTA.

Purpose: Close D-SSH-02/D-SSH-06 UI for GIT-03 discoverability.
Output: Green clone-box.ssh (+ updated placeholder) tests and unit tests for sshCloneUrl.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-git-ssh/09-UI-SPEC.md
@.planning/phases/09-git-ssh/09-07-SUMMARY.md
@apps/web/src/components/repo/clone-box.tsrx
@apps/web/src/components/repo/pat-how-to.tsrx
@apps/web/src/lib/public-origin.ts
@.agents/skills/octane/SKILL.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → displayed host/port | Config-derived advertise values; not secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-15 | Spoofing | wrong port in UI | medium | mitigate | Advertise OXIDEAN_SSH_PORT; show Port hint when ≠ 22 (D-SSH-02) |
| T-09-SC | Tampering | packages | low | accept | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (09-08-T1): sshCloneUrl helper</name>
  <files>apps/web/src/lib/public-origin.ts, apps/web/src/lib/public-origin.unit.test.ts</files>
  <read_first>apps/web/src/lib/public-origin.ts, apps/web/src/lib/public-origin.unit.test.ts, .planning/phases/09-git-ssh/09-UI-SPEC.md</read_first>
  <behavior>
    - sshCloneUrl(host, port, owner, repo) → git@{host}:{owner}/{repo}.git regardless of port
    - helper or sibling documents when port !== 22 for UI hint (port not embedded in scp primary string)
  </behavior>
  <action>
Add sshCloneUrl per D-SSH-02 always returning scp-style git@host:owner/repo.git (strip trailing slashes on host). Unit test port 22 and 2222 both yield scp form without ssh://. Export any small needsPortHint(port) helper if useful.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/lib/public-origin.unit.test.ts</automated>
    <fails_when>unit tests fail; or ssh:// appears as primary return</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'sshCloneUrl|git@' apps/web/src/lib/public-origin.ts`
    - `rg -n 'sshCloneUrl' apps/web/src/lib/public-origin.unit.test.ts`
  </acceptance_criteria>
  <done>sshCloneUrl unit-tested</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (09-08-T2): CloneBox SSH panel + how-to CTA</name>
  <files>apps/web/src/components/repo/ssh-how-to.tsrx, apps/web/src/components/repo/clone-box.tsrx, apps/web/src/components/repo/clone-box.integration.test.ts, apps/web/src/components/repo/clone-box.ssh.integration.test.ts</files>
  <read_first>apps/web/src/components/repo/clone-box.tsrx, apps/web/src/components/repo/pat-how-to.tsrx, .planning/phases/09-git-ssh/09-UI-SPEC.md, .agents/skills/octane/SKILL.md</read_first>
  <behavior>
    - SSH panel shows copyable scp URL; Port hint when port is 2222
    - Add an SSH key CTA → /settings/ssh-keys; documents login user git
    - placeholder text SSH cloning arrives in a later phase removed
  </behavior>
  <action>
Add SshHowTo compact component (PatHowTo analog). Wire CloneBox SSH section to sshCloneUrl + how-to; remove later-phase placeholder. Update clone-box.integration.test.ts expectations and green clone-box.ssh.integration.test.ts (D-SSH-02, D-SSH-06).
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/components/repo/clone-box.ssh.integration.test.ts src/components/repo/clone-box.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>placeholder still asserted as present; or scp URL missing; or build fails</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'sshCloneUrl|SshHowTo|/settings/ssh-keys' apps/web/src/components/repo/clone-box.tsrx apps/web/src/components/repo/ssh-how-to.tsrx`
    - `rg -n 'SSH cloning arrives in a later phase' apps/web/src/components/repo/clone-box.tsrx` exits 1 (no matches)
  </acceptance_criteria>
  <done>CloneBox live SSH URL + CTA shipped</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| sshCloneUrl | 09-08 | D-SSH-02 |
| CloneBox SSH | 09-08 | D-SSH-06 |

<verification>
CloneBox SSH vitest + build green; placeholder gone.
</verification>

<success_criteria>
Users see forge-shaped SSH remotes and a path to register keys.
</success_criteria>

<output>
Create `.planning/phases/09-git-ssh/09-08-SUMMARY.md` when done
</output>
