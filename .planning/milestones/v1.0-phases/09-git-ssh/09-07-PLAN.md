---
phase: 09-git-ssh
plan: "07"
type: execute
wave: 5
depends_on:
  - "06"
  - "01"
files_modified:
  - apps/web/src/routes/settings/ssh-keys.tsrx
  - apps/web/src/components/settings/ssh-key-list.tsrx
  - apps/web/src/components/settings/ssh-key-add-form.tsrx
  - apps/web/src/components/settings/ssh-key-revoke-dialog.tsrx
  - apps/web/src/components/settings/settings-nav.tsrx
  - apps/web/src/components/chrome.tsrx
  - apps/web/src/routes/settings/profile.tsrx
  - apps/web/src/routes/settings/tokens.tsrx
  - apps/web/src/routes/settings/ssh-keys.integration.test.ts
requirements:
  - GIT-04
autonomous: true
user_setup: []
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "User can open /settings/ssh-keys, add a titled key when verified, list fingerprints, and revoke with confirm dialog (D-SSH-05, D-SSH-06, GIT-04)"
    - "Unverified users see list but Add is disabled with verify copy (D-SSH-05)"
    - "At 25 keys, Add is disabled with Maximum of 25 SSH keys (UI-SPEC Cap / D-SSH-05)"
    - "SettingsNav and Account menu include SSH keys sibling to tokens"
  artifacts:
    - path: apps/web/src/routes/settings/ssh-keys.tsrx
      provides: "SSH keys settings route"
    - path: apps/web/src/components/settings/ssh-key-revoke-dialog.tsrx
      provides: "Revoke confirm (D-SSH-05)"
  key_links:
    - from: apps/web/src/routes/settings/ssh-keys.tsrx
      to: packages/api-client/src/index.ts
      via: "sshKey.list / add / revoke Query + mutations"
  assumptions: []
  prohibitions:
    - statement: "Must not mix React return ( JSX with Rivet @{ in the same .tsrx module"
      flagged: true
    - statement: "Must not show a one-time secret reveal on add"
      flagged: true
---

<objective>
Ship `/settings/ssh-keys` Octane UI mirroring PAT settings: list, add form, revoke confirm, nav + chrome links.

Purpose: Deliver GIT-04 user-facing key management per D-SSH-06 / 09-UI-SPEC.
Output: Green ssh-keys.integration.test.ts + build.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-git-ssh/09-UI-SPEC.md
@.planning/phases/09-git-ssh/09-06-SUMMARY.md
@.agents/skills/octane/SKILL.md
@apps/web/src/routes/settings/tokens.tsrx
@apps/web/src/components/settings/pat-list.tsrx
@apps/web/src/components/settings/pat-revoke-dialog.tsrx
@apps/web/src/components/settings/settings-nav.tsrx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → sshKey.* | Session cookie; unverified cannot add |
| Revoke dialog → destroy | Confirm required before delete |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03 | Elevation of privilege | Add CTA | medium | mitigate | Disable when !email_verified (D-SSH-05) |
| T-09-14 | Tampering | revoke | medium | mitigate | AlertDialog confirm Keep/Revoke (D-SSH-05) |
| T-09-SC | Tampering | packages | low | accept | No new UI kits |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (09-07-T1): /settings/ssh-keys list + nav + Add gate</name>
  <files>apps/web/src/routes/settings/ssh-keys.tsrx, apps/web/src/components/settings/ssh-key-list.tsrx, apps/web/src/components/settings/ssh-key-add-form.tsrx, apps/web/src/components/settings/settings-nav.tsrx, apps/web/src/components/chrome.tsrx, apps/web/src/routes/settings/profile.tsrx, apps/web/src/routes/settings/tokens.tsrx, apps/web/src/routes/settings/ssh-keys.integration.test.ts</files>
  <read_first>apps/web/src/routes/settings/tokens.tsrx, apps/web/src/components/settings/settings-nav.tsrx, apps/web/src/components/chrome.tsrx, .planning/phases/09-git-ssh/09-UI-SPEC.md, .agents/skills/octane/SKILL.md</read_first>
  <behavior>
    - authenticated verified user sees SSH keys heading, empty hero or list, Add SSH key form
    - unverified: Add disabled + Verify your email to add an SSH key.
    - when list length >= 25: Add disabled + Maximum of 25 SSH keys (UI-SPEC Cap)
    - SettingsNav active ssh-keys; Account menu link present
  </behavior>
  <action>
Implement route /settings/ssh-keys per UI-SPEC and D-SSH-06: session loader like tokens; SettingsNav active union adds ssh-keys; update profile/tokens nav callers; chrome Account menu sibling SSH keys. List via TanStack Query sshKey.list; add form title + public key textarea calling sshKey.add; disable Add when !email_verified OR keys.length >= 25 (show Maximum of 25 SSH keys when at cap). Octane .tsrx Rivet only. Green list/nav/verify-wall/cap portions of ssh-keys.integration.test.ts.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/settings/ssh-keys.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>vitest fails; or build fails; or SettingsNav lacks ssh-keys; or 25-key cap UI missing</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'ssh-keys|SSH keys' apps/web/src/components/settings/settings-nav.tsrx apps/web/src/routes/settings/ssh-keys.tsrx`
    - `rg -n 'email_verified|Add SSH key|sshKey\.add|Maximum of 25' apps/web/src/routes/settings/ssh-keys.tsrx apps/web/src/components/settings/ssh-key-add-form.tsrx`
  </acceptance_criteria>
  <done>Settings SSH keys page + nav wired</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (09-07-T2): Revoke AlertDialog</name>
  <files>apps/web/src/components/settings/ssh-key-revoke-dialog.tsrx, apps/web/src/components/settings/ssh-key-list.tsrx, apps/web/src/routes/settings/ssh-keys.integration.test.ts</files>
  <read_first>apps/web/src/components/settings/pat-revoke-dialog.tsrx, .planning/phases/09-git-ssh/09-UI-SPEC.md</read_first>
  <behavior>
    - Revoke opens AlertDialog Revoke SSH key? with Keep key / Revoke key
    - confirm calls sshKey.revoke and refreshes list
  </behavior>
  <action>
Mirror PatRevokeDialog for SSH keys per D-SSH-05: confirm only (no type-to-confirm); wire from list row. Green revoke expectations in integration test.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/settings/ssh-keys.integration.test.ts</automated>
    <fails_when>revoke dialog copy missing; or revoke does not call API</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Revoke SSH key|Keep key|Revoke key' apps/web/src/components/settings/ssh-key-revoke-dialog.tsrx`
  </acceptance_criteria>
  <done>Revoke confirm path green</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `/settings/ssh-keys` | 09-07 | GIT-04 UI |
| SettingsNav + chrome | 09-07 | D-SSH-06 |

<verification>
Vitest ssh-keys + web build green.
</verification>

<success_criteria>
Users manage SSH keys in settings with verify gate and revoke confirm.
</success_criteria>

<output>
Create `.planning/phases/09-git-ssh/09-07-SUMMARY.md` when done
</output>
