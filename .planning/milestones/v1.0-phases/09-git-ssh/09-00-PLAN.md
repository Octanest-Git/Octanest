---
phase: 09-git-ssh
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/octanest-api/tests/git_ssh.rs
  - crates/octanest-api/tests/ssh_key_rpc.rs
  - crates/octanest-db/tests/dialect_ssh_keys.rs
  - scripts/smoke-git-ssh.sh
  - Makefile
requirements:
  - GIT-03
  - GIT-04
autonomous: true
user_setup: []
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Wave 0 failing stubs exist for sshKey.add/list/revoke, require_verified add, unique fingerprint, and git-over-SSH auth/ACL/pack allowlist (GIT-03, GIT-04, D-SSH-03..05)"
    - "dialect_ssh_keys stub expects tri-dialect 0009_ssh_keys with ssh_public_keys + UNIQUE fingerprint"
    - "smoke-git-ssh.sh stub + Makefile target discoverable (D-SSH-07)"
  artifacts:
    - path: crates/octanest-api/tests/ssh_key_rpc.rs
      provides: "GIT-04 RPC failing stubs"
    - path: crates/octanest-api/tests/git_ssh.rs
      provides: "GIT-03 SSH transport failing stubs"
    - path: crates/octanest-db/tests/dialect_ssh_keys.rs
      provides: "0009_ssh_keys migration parity stub"
    - path: scripts/smoke-git-ssh.sh
      provides: "Compose SSH smoke stub"
  key_links:
    - from: .planning/phases/09-git-ssh/09-VALIDATION.md
      to: crates/octanest-api/tests/git_ssh.rs
      via: "Wave 0 Requirements checklist"
  assumptions:
    - "ASSUME (A1 RESEARCH): all SSH requires a registered key; anonymous public SSH clone out of scope"
    - "ASSUME (GIT-04 ordering): sshKey.list returns keys ordered by created_at DESC"
    - "ASSUME (GIT-04 revoke): hard-delete; unknown/other-user id → sshKey.not_found"
  prohibitions:
    - statement: "Must not implement production russh listener, migrations, or RPC handlers in this Wave 0 plan"
      flagged: true
    - statement: "Must not route SSH through Traefik HTTP"
      flagged: true
---

<objective>
Scaffold Nyquist/Wave 0 failing Rust + smoke stubs for Phase 9 SSH keys and git-over-SSH before implementation plans turn them green.

Purpose: Every later `<automated>` verify names a real test path (09-VALIDATION.md Wave 0). Web stubs live in 09-01.
Output: Discoverable RED (or intentionally failing) `ssh_key_*` / `git_ssh*` / dialect stubs + smoke script stub matching nextest/Make filters.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-git-ssh/09-CONTEXT.md
@.planning/phases/09-git-ssh/09-RESEARCH.md
@.planning/phases/09-git-ssh/09-VALIDATION.md
@.planning/phases/09-git-ssh/09-PATTERNS.md
@crates/octanest-api/tests/pat_rpc.rs
@crates/octanest-api/tests/git_smart_http.rs
@crates/octanest-db/tests/dialect_pats.rs
@scripts/smoke-git-https.sh
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → temp DB / repos_dir / ephemeral SSH port | Parallel nextest must isolate state |
| Stub assertions → production auth | Force user git, pack allowlist, ACL git-errors encoded before impl |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Elevation of privilege | git_ssh stubs | high | mitigate | Stub asserts force user git; reject shell/pty; private non-owner denied (D-SSH-03, D-SSH-04) |
| T-09-02 | Spoofing | ssh_key_rpc stubs | medium | mitigate | Stub asserts require_verified on add; fingerprint unique (D-SSH-05) |
| T-09-SC | Tampering | cargo installs | high | mitigate | Wave 0 adds test/script files only; no new crates.io packages |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (09-00-T1): SSH key RPC + git_ssh + dialect Wave 0 stubs</name>
  <files>crates/octanest-api/tests/ssh_key_rpc.rs, crates/octanest-api/tests/git_ssh.rs, crates/octanest-db/tests/dialect_ssh_keys.rs</files>
  <read_first>.planning/phases/09-git-ssh/09-VALIDATION.md, .planning/phases/09-git-ssh/09-RESEARCH.md, crates/octanest-api/tests/pat_rpc.rs, crates/octanest-api/tests/git_smart_http.rs, crates/octanest-db/tests/dialect_pats.rs</read_first>
  <action>
Add Wave 0 integration stubs (Phase 08 style: compile, discoverable by nextest, intentionally fail via assert!(false, "Wave 0") or #[ignore] until later plans). ssh_key_rpc.rs must name tests matching filter test(ssh_key) covering: verified sshKey.add with title + ed25519 OpenSSH line returns list item with SHA256 fingerprint and no secret reveal; list after add; revoke removes; unverified add → auth.email_unverified; empty title → sshKey.title_required; duplicate fingerprint → domain error; 26th key → max 25 (GIT-04, D-SSH-05). git_ssh.rs must name tests matching test(git_ssh) covering: SSH username other than git rejected; registered key + user git accepts; public upload-pack happy-path stub; private non-owner clear git stderr deny; push unverified email denied; non pack exec (shell) rejected; failed pubkey over limit rate-limited (GIT-03, D-SSH-03, D-SSH-04, D-SSH-07). dialect_ssh_keys.rs expects tri-dialect 0009_ssh_keys with ssh_public_keys columns id/user_id/title/public_key/fingerprint UNIQUE/key_type/last_used_at/created_at (mirror dialect_pats). Do not implement russh, migrations, or RPC handlers here.
  </action>
  <verify>
    <automated>test -f crates/octanest-api/tests/ssh_key_rpc.rs && test -f crates/octanest-api/tests/git_ssh.rs && test -f crates/octanest-db/tests/dialect_ssh_keys.rs && cargo nextest list -p octanest-api -E 'test(ssh_key) | test(git_ssh)' | rg -q 'ssh_key|git_ssh' && cargo nextest list -p octanest-db -E 'test(dialect_ssh_keys)' | rg -q 'dialect_ssh_keys'</automated>
    <fails_when>any listed stub path missing; or nextest list finds zero ssh_key/git_ssh/dialect_ssh_keys tests</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f crates/octanest-api/tests/ssh_key_rpc.rs && test -f crates/octanest-api/tests/git_ssh.rs && test -f crates/octanest-db/tests/dialect_ssh_keys.rs`
    - `rg -n 'sshKey\.add|email_unverified|title_required|fingerprint' crates/octanest-api/tests/ssh_key_rpc.rs`
    - `rg -n 'git-upload-pack|git-receive-pack|username|email' crates/octanest-api/tests/git_ssh.rs`
    - `rg -n '0009_ssh_keys|ssh_public_keys|fingerprint' crates/octanest-db/tests/dialect_ssh_keys.rs`
  </acceptance_criteria>
  <done>Rust Wave 0 stubs exist; filters discover RED/ignored tests</done>
</task>

<task type="auto">
  <name>Task 2 (09-00-T2): smoke-git-ssh stub + Makefile target</name>
  <files>scripts/smoke-git-ssh.sh, Makefile</files>
  <read_first>scripts/smoke-git-https.sh, Makefile, .planning/phases/09-git-ssh/09-RESEARCH.md</read_first>
  <action>
Add scripts/smoke-git-ssh.sh stub mirroring smoke-git-https.sh shebang/set -euo pipefail and docker-missing graceful skip (exit 0). Stub body may exit 1 with a Wave 0 message when Docker is present until 09-05 greens ls-remote/push over TCP 2222 with scp-style git@host:owner/repo.git (D-SSH-02, D-SSH-07). Wire Makefile smoke-git-ssh target beside smoke-git-https; update .PHONY and help echo. Do not publish Compose ports here (09-05).
  </action>
  <verify>
    <automated>test -f scripts/smoke-git-ssh.sh && rg -n 'smoke-git-ssh' Makefile && test -x scripts/smoke-git-ssh.sh || chmod +x scripts/smoke-git-ssh.sh</automated>
    <fails_when>script missing; or Makefile lacks smoke-git-ssh target</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/smoke-git-ssh.sh`
    - `rg -n 'smoke-git-ssh' Makefile`
  </acceptance_criteria>
  <done>Smoke stub + Make target discoverable for later greening</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `ssh_key_rpc.rs` / `git_ssh.rs` stubs | 09-00 | Nyquist GIT-04 / GIT-03 |
| `dialect_ssh_keys.rs` stub | 09-00 | Schema gate precursor |
| `smoke-git-ssh.sh` stub | 09-00 | Greened by 09-05 |
| Web Vitest stubs | 09-01 | Parallel Wave 0 |

<verification>
Wave 0 Rust/smoke stubs exist; VALIDATION checklist items addressable by later plans.
</verification>

<success_criteria>
Phase 9 requirement IDs have failing automated stub targets before green implementation (web stubs in 09-01).
</success_criteria>

<output>
Create `.planning/phases/09-git-ssh/09-00-SUMMARY.md` when done
</output>
