---
phase: 09-git-ssh
plan: "09"
type: execute
wave: 7
depends_on:
  - "04"
  - "05"
  - "06"
  - "07"
  - "08"
files_modified:
  - docs/CONFIGURATION.md
  - docs/ARCHITECTURE.md
  - .planning/phases/09-git-ssh/09-VALIDATION.md
requirements:
  - GIT-03
  - GIT-04
autonomous: true
user_setup: []
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CONFIGURATION/ARCHITECTURE document russh in-api SSH, TCP 2222, OXIDEAN_SSH_* env, host keys, and smoke-git-ssh (D-SSH-01, D-SSH-07)"
    - "09-VALIDATION.md Wave 0 gaps checked off with File Exists? updated; phase gate commands runnable"
  artifacts:
    - path: docs/CONFIGURATION.md
      provides: "SSH env + Compose ops"
    - path: docs/ARCHITECTURE.md
      provides: "SSH transport architecture note"
    - path: .planning/phases/09-git-ssh/09-VALIDATION.md
      provides: "Nyquist map refresh"
  key_links:
    - from: docs/CONFIGURATION.md
      to: docker-compose.yml
      via: "OXIDEAN_SSH_* documented"
  assumptions:
    - "ASSUME: nyquist_compliant remains false until /gsd-validate-phase (do not flip here)"
  prohibitions:
    - statement: "Must not claim Traefik terminates SSH"
      flagged: true
---

<objective>
Document SSH ops/architecture and refresh VALIDATION.md requirement→test map after implementation; run phase-gate automated checks.

Purpose: Close Dimension 8 sampling continuity and operator docs for Phase 9.
Output: Updated docs + VALIDATION checklist; green targeted test gate.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-git-ssh/09-RESEARCH.md
@.planning/phases/09-git-ssh/09-VALIDATION.md
@docs/CONFIGURATION.md
@docs/ARCHITECTURE.md
@scripts/smoke-git-ssh.sh
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → operators | Must not instruct Traefik HTTP for SSH or leak secrets in examples |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-16 | Information disclosure | docs examples | medium | mitigate | Non-secret placeholders only; public keys ok |
| T-09-SC | Tampering | packages | low | accept | Docs/VALIDATION only |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (09-09-T1): CONFIGURATION + ARCHITECTURE SSH notes</name>
  <files>docs/CONFIGURATION.md, docs/ARCHITECTURE.md</files>
  <read_first>docs/CONFIGURATION.md, docs/ARCHITECTURE.md, .planning/phases/09-git-ssh/09-RESEARCH.md</read_first>
  <action>
Document OXIDEAN_SSH_ENABLED, OXIDEAN_SSH_HOST, OXIDEAN_SSH_PORT (listen+advertise), OXIDEAN_SSH_HOST_KEY_DIR; Compose TCP 2222; make dev may omit listener; ~/.ssh/config Port when ≠ 22; scp-style remotes; force user git; keys = account identity. ARCHITECTURE: russh in-process module, pack spawn, shared repos_dir, not Traefik (D-SSH-01, D-SSH-02, D-SSH-03, D-SSH-07).
  </action>
  <verify>
    <automated>rg -n 'OXIDEAN_SSH_|russh|2222|smoke-git-ssh|git@' docs/CONFIGURATION.md docs/ARCHITECTURE.md</automated>
    <fails_when>SSH env or russh absent from both docs</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'OXIDEAN_SSH_ENABLED|OXIDEAN_SSH_PORT' docs/CONFIGURATION.md`
    - `rg -n 'russh|upload-pack|SSH' docs/ARCHITECTURE.md`
  </acceptance_criteria>
  <done>Operator + architecture docs cover Git SSH</done>
</task>

<task type="auto">
  <name>Task 2 (09-09-T2): Phase gate verify + VALIDATION map refresh</name>
  <files>.planning/phases/09-git-ssh/09-VALIDATION.md</files>
  <read_first>.planning/phases/09-git-ssh/09-VALIDATION.md, .planning/phases/08-git-https-pats/08-VALIDATION.md</read_first>
  <action>
Update 09-VALIDATION.md: mark Wave 0 gaps complete; set File Exists? to yes for created tests/smoke; refresh commands to match landed filters; set wave_0_complete: true; leave nyquist_compliant false for validate-phase. Run phase gate: cargo nextest ssh filters, dialect_ssh_keys, make rpc-sync-check, web vitest ssh-keys + clone-box.ssh, bun build. Note make smoke-git-ssh for human/Compose gate when Docker available.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(ssh_key) | test(git_ssh)' && cargo test -p oxidean-db --test dialect_ssh_keys && make rpc-sync-check && bun --cwd apps/web exec vitest run src/routes/settings/ssh-keys.integration.test.ts src/components/repo/clone-box.ssh.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>any gate command fails</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'wave_0_complete: true' .planning/phases/09-git-ssh/09-VALIDATION.md`
    - `rg -n 'File Exists\?.*✅|yes|✓' .planning/phases/09-git-ssh/09-VALIDATION.md` or equivalent checked Wave 0 list
  </acceptance_criteria>
  <done>VALIDATION refreshed; automated phase gate green</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| CONFIGURATION/ARCHITECTURE SSH | 09-09 | Ops |
| VALIDATION refresh | 09-09 | Nyquist Dimension 8 |

<verification>
Docs + targeted suite green; smoke noted for Compose.
</verification>

<success_criteria>
Phase 9 docs and validation map match shipped SSH behavior; automated gates pass.
</success_criteria>

<output>
Create `.planning/phases/09-git-ssh/09-09-SUMMARY.md` when done
</output>
