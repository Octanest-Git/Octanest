---
phase: 13-branch-protection
plan: "05"
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - crates/oxidean-api/src/protection
  - crates/oxidean-git/src
  - crates/oxidean-api/src/repo/mod.rs
  - crates/oxidean-api/tests/branch_protect_push.rs
  - crates/oxidean-api/tests/repo_branch_soft_protect.rs
requirements:
  - ORG-06
autonomous: true
user_setup: []
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Force-push and delete of protected refs denied when allow_* false (D-15, ORG-06)"
    - "lock_branch rejects all pushes/merges for non-bypass actors (D-18)"
    - "Existing bare repos without hooks are reconciled before enforcement relies on them (D-19)"
  artifacts:
    - path: crates/oxidean-git/src
      provides: "Hook install + reconcile"
    - path: crates/oxidean-api/tests/branch_protect_push.rs
      provides: "Force-push/delete/lock coverage"
  key_links:
    - from: crates/oxidean-git/src
      to: crates/oxidean-api/src/protection
      via: "Hook helper invokes evaluate"
  assumptions:
    - "ASSUME: Admin bypass when enforce_admins=false is covered; enforce_admins=true covered in 13-06"
  prohibitions:
    - statement: "User/team push allowlists (restrictions) are deferred (D-21)"
      flagged: true
---

<objective>
Complete direct-push enforcement: force-push/delete/lock_branch flags, branch RPC delete/rename honor rules, and hook reconcile for existing bare repos (ORG-06, D-15..20).

Purpose: Protection is real on git push paths, not only the tracer require-PR case.
Output: Green expanded branch_protect_push suite; soft-protect still green.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-branch-protection/13-CONTEXT.md
@.planning/phases/13-branch-protection/13-02-SUMMARY.md
@.planning/phases/13-branch-protection/13-03-SUMMARY.md
@crates/oxidean-git/src/cli.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git push → hooks | Ref updates must be authorized by evaluate |
| reconcile job → hooks dir | Writes executables under bare repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-10 | Tampering | hook scripts | high | mitigate | Install known-good hook content only; verify path under repos_dir |
| T-13-11 | Bypass | missing hooks | high | mitigate | Reconcile on startup/push/admin path (D-19) |
| T-13-12 | Elevation of privilege | force-push | high | mitigate | Default allow_force_pushes false; Admin bypass only if enforce_admins false |
| T-13-SC | Tampering | crates | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (13-05-T1): Force-push, delete, lock_branch push intents</name>
  <files>crates/oxidean-api/src/protection, crates/oxidean-api/tests/branch_protect_push.rs, crates/oxidean-api/src/repo/mod.rs</files>
  <read_first>crates/oxidean-api/src/protection, crates/oxidean-api/tests/branch_protect_push.rs</read_first>
  <behavior>
    - Non-fast-forward update denied when allow_force_pushes false
    - Branch delete denied when allow_deletions false; repo.branchDelete returns protection error
    - lock_branch denies Push intent for Write non-bypass actor
  </behavior>
  <action>
Expand evaluate Push/ForcePush/Delete intents per D-15, D-18, D-20. Wire repo.branchDelete/rename to consult protection in addition to soft-protect (D-04). Green branch_protect_push force/delete/lock cases. Do not implement Teams restrictions (D-21).
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(branch_protect_push) | test(repo_branch_soft_protect)'</automated>
  </verify>
  <done>Force-push, delete, lock, and soft-protect coexistence covered and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (13-05-T2): Hook install on create + reconcile existing</name>
  <files>crates/oxidean-git/src, crates/oxidean-api/tests/branch_protect_push.rs</files>
  <read_first>crates/oxidean-git/src/cli.rs, .planning/phases/13-branch-protection/13-RESEARCH.md</read_first>
  <behavior>
    - New bare repos contain protection hooks after init_bare
    - Bare repo missing hooks gains them via reconcile before next push check succeeds/fails correctly
  </behavior>
  <action>
Harden D-19: ensure init_bare always installs hooks; add reconcile helper for existing repos (lazy-on-push or explicit API/admin path — discretion). Cover with a test that starts without hooks, reconciles, then denies protected push. Keep HTTPS and SSH sharing the same hook files.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(branch_protect_push)'</automated>
  </verify>
  <done>Hook install + reconcile proven in tests.</done>
</task>

</tasks>

<verification>
ORG-06 direct-push enforcement complete for force/delete/lock + hooks.
</verification>

<success_criteria>
Protected refs cannot be force-pushed or deleted contrary to rules; hooks exist on all bare repos after reconcile.
</success_criteria>

<output>
Create `.planning/phases/13-branch-protection/13-05-SUMMARY.md` when done
</output>
