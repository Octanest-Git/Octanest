---
phase: 13-branch-protection
plan: "07"
type: execute
wave: 6
depends_on:
  - "03"
  - "04"
files_modified:
  - apps/web/src/routes/$owner.$repo.settings.tsrx
  - apps/web/src/components/repo
  - apps/web/src/routes/$owner.$repo.settings.branches.integration.test.ts
  - apps/web/src/lib
requirements:
  - ORG-05
autonomous: true
user_setup: []
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Admin sees Branch protection section in repo Settings and can CRUD rules (ORG-05, D-25)"
    - "Non-Admin cannot load protection management UI (soft not-found / gated like other Admin panels)"
  artifacts:
    - path: apps/web/src/routes/$owner.$repo.settings.tsrx
      provides: "Branches / protection entry"
    - path: apps/web/src/components/repo
      provides: "Branch protection panel component(s)"
  key_links:
    - from: apps/web/src/components/repo
      to: packages/api-client
      via: "repo.branchProtection.* session RPC"
  assumptions:
    - "ASSUME: Sub-route vs inline panel is Claude discretion"
  prohibitions:
    - statement: "React JSX authoring is forbidden; use Octane .tsrx Rivet templates"
      flagged: true
---

<objective>
Ship Octane Admin Settings UI for classic branch protection rule CRUD (ORG-05, D-25).

Purpose: Repo admins can configure reviews and status-check requirements without raw RPC.
Output: Green settings.branches Vitest; make web-lint / web-format-check clean for touched files.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-branch-protection/13-CONTEXT.md
@.agents/skills/octane/SKILL.md
@apps/web/src/routes/$owner.$repo.settings.tsrx
@apps/web/src/components/repo/lfs-settings-panel.tsrx
@apps/web/src/components/repo/collaborators-panel.tsrx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser session → protection CRUD | Must be Admin; server re-checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-16 | Elevation of privilege | Settings UI | high | mitigate | Gate on can_admin; server Admin ACL remains authoritative |
| T-13-17 | Tampering | form fields | medium | mitigate | Validate counts 1..6; contexts list; server rejects invalid |
| T-13-SC | Tampering | npm | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (13-07-T1): Branch protection settings panel</name>
  <files>apps/web/src/components/repo, apps/web/src/routes/$owner.$repo.settings.tsrx, apps/web/src/lib</files>
  <read_first>.agents/skills/octane/SKILL.md, apps/web/src/components/repo/lfs-settings-panel.tsrx, apps/web/src/routes/$owner.$repo.settings.tsrx</read_first>
  <behavior>
    - Admin can list rules, create rule with pattern + required reviews and/or status contexts, edit flags, delete
    - Uses TanStack Query/session helpers; Octane Rivet templates only
  </behavior>
  <action>
Implement Branch protection Admin UI per D-25 using Octane `.tsrx` (no React JSX returns mixed with Rivet). Follow LFS/collaborators panel patterns and can_admin gate. Wire repo.branchProtection.* from generated client. Cover fields for D-05..18 that Settings should expose (reviews count, dismiss stale, conversation resolution, last-push approval, status contexts + strict, force push, deletions, enforce admins, linear history, lock). Do not remount RepoChrome.
  </action>
  <verify>
    <automated>make web-lint && make web-format-check</automated>
  </verify>
  <done>Settings panel compiles; web-lint and web-format-check pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (13-07-T2): Settings branches Vitest green</name>
  <files>apps/web/src/routes/$owner.$repo.settings.branches.integration.test.ts</files>
  <read_first>apps/web/src/routes/$owner.$repo.settings.branches.integration.test.ts, apps/web/src/routes/$owner.$repo.settings.collaborators.integration.test.ts</read_first>
  <behavior>
    - Test asserts Admin protection section discoverability and at least one CRUD interaction path (mocked RPC acceptable)
  </behavior>
  <action>
Turn Wave 0 settings.branches Vitest stub green against the new panel (ORG-05). Prefer mocking api-client like other settings integration tests.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/\$owner.\$repo.settings.branches.integration.test.ts</automated>
  </verify>
  <done>settings.branches integration test green.</done>
</task>

</tasks>

<verification>
ORG-05 configurable via Settings UI.
</verification>

<success_criteria>
Admins can manage protection rules in the web UI with lint/format clean.
</success_criteria>

<output>
Create `.planning/phases/13-branch-protection/13-07-SUMMARY.md` when done
</output>
