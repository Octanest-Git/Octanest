---
phase: 13-branch-protection
plan: "04"
type: execute
wave: 3
depends_on:
  - "03"
files_modified:
  - crates/oxidean-db/migrations/sqlite
  - crates/oxidean-db/migrations/postgres
  - crates/oxidean-db/migrations/mysql
  - crates/oxidean-db/src
  - crates/oxidean-api/src/protection
  - crates/oxidean-api/src/repo
  - crates/oxidean-api/src/bin/rpc_gen.rs
  - crates/oxidean-api/tests/commit_status_rpc.rs
  - crates/oxidean-api/tests/branch_protect_merge.rs
  - packages/api-client
requirements:
  - ORG-05
  - PR-08
autonomous: true
user_setup: []
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Write+ can upsert commit statuses by sha+context; Read+ can list (D-11, D-12, D-13)"
    - "Merge blocks when a required context is missing or not success (ORG-05/PR-08, D-10)"
    - "strict=true blocks merge when head is not up to date with base (D-10)"
  artifacts:
    - path: crates/oxidean-db/migrations
      provides: "commit_statuses table"
    - path: crates/oxidean-api/tests/commit_status_rpc.rs
      provides: "Status RPC coverage"
  key_links:
    - from: crates/oxidean-api/src/protection
      to: commit_statuses rows
      via: "required_status_checks evaluation"
  assumptions:
    - "ASSUME: Empty contexts list does not block; only named contexts enforce (Claude discretion)"
  prohibitions:
    - statement: "Actions check-run UI and workflow runners are out of scope (Phase 19)"
      flagged: true
---

<objective>
Ship classic commit-status store + RPC and wire required_status_checks (+ strict) into the shared merge evaluator (D-10..13).

Purpose: ORG-05 status checks work before Phase 19 Actions; external CI can report contexts.
Output: Green commit_status_rpc; merge blocked/unblocked on required contexts and strict up-to-date.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-branch-protection/13-CONTEXT.md
@.planning/phases/13-branch-protection/13-02-SUMMARY.md
@.planning/phases/13-branch-protection/13-RESEARCH.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI / PAT → commitStatus.create | Untrusted state/context strings |
| Merge evaluator → statuses | Trust DB latest-wins only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-07 | Spoofing | commitStatus.create | high | mitigate | Write+ Capability + PAT contents/repo write; no anonymous status write |
| T-13-08 | Tampering | context/state | medium | mitigate | Enumerate allowed states; bound context length; upsert by (repo,sha,context) |
| T-13-09 | Elevation of privilege | status forge | medium | mitigate | Statuses do not grant merge rights; only satisfy named required contexts |
| T-13-SC | Tampering | crates | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (13-04-T1): commit_statuses table + repo.commitStatus RPC</name>
  <files>crates/oxidean-db/migrations/sqlite, crates/oxidean-db/migrations/postgres, crates/oxidean-db/migrations/mysql, crates/oxidean-db/src, crates/oxidean-api/src/repo, crates/oxidean-api/src/bin/rpc_gen.rs, packages/api-client, crates/oxidean-api/tests/commit_status_rpc.rs</files>
  <read_first>.planning/phases/13-branch-protection/13-CONTEXT.md, crates/oxidean-api/src/repo/acl.rs</read_first>
  <behavior>
    - create upserts pending/success/failure/error for (sha, context); list returns latest per context for sha
    - Write+ required to create; Read+ to list; private ACL soft not_found preserved
  </behavior>
  <action>
Add commit_statuses migration (D-11, D-12) and repo.commitStatus.create/list (D-13, D-26). make rpc-gen. Green commit_status_rpc. Leave Actions check-run mapping for Phase 19; evaluator may accept neutral/skipped aliases if stored later.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(commit_status)' && make rpc-sync-check</automated>
  </verify>
  <done>Status create/list green; client in sync.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (13-04-T2): Required contexts + strict in merge evaluate</name>
  <precondition>Phase 12 pull.merge available for merge-gate integration tests.</precondition>
  <files>crates/oxidean-api/src/protection, crates/oxidean-api/tests/branch_protect_merge.rs</files>
  <read_first>crates/oxidean-api/src/protection, .planning/phases/13-branch-protection/13-RESEARCH.md</read_first>
  <behavior>
    - Rule requiring context ci/test blocks merge until success status on head sha
    - strict=true blocks when head behind base tip; after update, checks still required
  </behavior>
  <action>
Extend protection::evaluate Merge intent for D-10: union of required contexts across matching rules; empty contexts means no check requirement; missing/pending/failure/error unsatisfied; success (and neutral/skipped aliases) pass. Implement strict up-to-date via git ancestry against base tip. Surface reasons in merge error payload (D-22, D-24). Green branch_protect_merge status/strict cases.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(branch_protect_merge)'</automated>
  </verify>
  <done>Required checks and strict up-to-date enforced on merge path.</done>
</task>

</tasks>

<verification>
Status API + required checks satisfy ORG-05 example and PR-08 for checks.
</verification>

<success_criteria>
CI can report statuses; merges respect required contexts and strict.
</success_criteria>

<output>
Create `.planning/phases/13-branch-protection/13-04-SUMMARY.md` when done
</output>
