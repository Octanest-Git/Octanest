---
phase: 13-branch-protection
plan: "02"
type: tracer
wave: 1
depends_on:
  - "00"
files_modified:
  - crates/oxidean-db/migrations/sqlite
  - crates/oxidean-db/migrations/postgres
  - crates/oxidean-db/migrations/mysql
  - crates/oxidean-db/src
  - crates/oxidean-api/src/protection
  - crates/oxidean-api/src/repo
  - crates/oxidean-git/src
  - crates/oxidean-api/tests/branch_protect_merge.rs
  - crates/oxidean-api/tests/branch_protect_push.rs
  - crates/oxidean-db/tests/dialect_branch_protection.rs
requirements:
  - ORG-05
  - ORG-06
  - PR-08
autonomous: true
user_setup: []
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Admin can create one exact-branch rule requiring 1 approving review (ORG-05, D-03, D-05)"
    - "Direct push to that protected branch is rejected for a Write non-Admin actor (ORG-06, D-14, D-19)"
    - "PR merge into that base is blocked without the required approval and succeeds after a valid Approve (PR-08, D-22)"
  artifacts:
    - path: crates/oxidean-api/src/protection
      provides: "Shared evaluate() for push + merge (D-19, D-22)"
    - path: crates/oxidean-db/migrations
      provides: "branch_protection_rules (+ minimal columns for tracer)"
  key_links:
    - from: crates/oxidean-api/src/protection
      to: crates/oxidean-api/tests/branch_protect_merge.rs
      via: "merge RPC calls evaluate before Phase 12 merge body"
    - from: crates/oxidean-git/src
      to: bare repo hooks/
      via: "init_bare installs update/pre-receive hook"
  assumptions:
    - "PRECONDITION: Phase 12 pull merge + review RPCs exist on the integration branch before merge-gate tests can go green"
    - "ASSUME: Tracer may use exact branch name match only; wildcard union expands in 13-03"
    - "ASSUME: Status checks, dismiss-stale, conversation resolution, enforce_admins expand in later plans"
  prohibitions:
    - statement: "Settings UI and commit-status RPC are out of scope for the tracer"
      flagged: true
    - statement: "No new crates.io packages"
      flagged: true
---

<objective>
Tracer vertical slice: one production-quality path — Admin creates a rule on exact branch `main` requiring 1 approval; Write user cannot push directly; PR merge is blocked until an eligible Approve lands, then merge succeeds.

Purpose: Prove shared evaluator + hooks + merge gate architecture before expanding patterns/statuses/UI (D-05, D-14, D-19, D-22).
Output: Green branch_protect_push + branch_protect_merge happy/deny paths; dialect migration green for tracer tables.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-branch-protection/13-CONTEXT.md
@.planning/phases/13-branch-protection/13-RESEARCH.md
@.planning/phases/12-pull-requests/12-CONTEXT.md
@crates/oxidean-api/src/routes/git_smart_http.rs
@crates/oxidean-api/src/repo/mod.rs
@crates/oxidean-git/src/cli.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git client → bare hooks | Untrusted ref updates |
| RPC client → protection CRUD | Admin-only mutations |
| Merge RPC → evaluator | Write+ merge attempt may be blocked |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-02 | Elevation of privilege | rule CRUD | high | mitigate | Admin Capability gate (D-03) |
| T-13-03 | Tampering | push bypass | high | mitigate | Hooks call same evaluate as merge; Admin bypass only when enforce_admins false (default) |
| T-13-04 | Spoofing | hook env | medium | mitigate | Derive repo identity from GIT_DIR under OXIDEAN_REPOS_DIR; reject path escape |
| T-13-SC | Tampering | crates | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (13-02-T1): End-to-end protect main — push deny + merge gate</name>
  <precondition>Phase 12 pull.merge and review Approve RPCs are available on the branch under test (or tests remain ignored until rebase onto Phase 12).</precondition>
  <reversibility rating="costly">Protection tables + bare-repo hooks (D-01, D-19).</reversibility>
  <files>crates/oxidean-db/migrations/sqlite, crates/oxidean-db/migrations/postgres, crates/oxidean-db/migrations/mysql, crates/oxidean-db/src, crates/oxidean-api/src/protection, crates/oxidean-api/src/repo, crates/oxidean-git/src, crates/oxidean-api/tests/branch_protect_merge.rs, crates/oxidean-api/tests/branch_protect_push.rs, crates/oxidean-db/tests/dialect_branch_protection.rs</files>
  <read_first>crates/oxidean-api/src/repo/mod.rs, crates/oxidean-git/src/cli.rs, crates/oxidean-api/src/routes/git_smart_http.rs, .planning/phases/13-branch-protection/13-RESEARCH.md</read_first>
  <behavior>
    - Admin creates rule pattern main with required_pull_request_reviews count 1 via repo.branchProtection.create (or tracer-equivalent)
    - Write non-Admin git push to refs/heads/main is rejected by hook with protection message
    - Open PR into main with zero Approves: pull.merge fails with structured protection reasons (PR-08)
    - After eligible Write+ Approve (not author): pull.merge succeeds for an enabled merge method
    - dialect_branch_protection migration parity passes
  </behavior>
  <action>
Implement the tracer per D-01, D-03, D-05, D-14, D-19, D-22, D-26: add next-free tri-dialect migration for branch_protection_rules with columns needed for required reviews on an exact pattern (expand columns later without breaking). Add protection::evaluate shared module. Wire minimal repo.branchProtection.create/list (Admin). Install update/pre-receive hooks from init_bare so HTTPS and SSH receive-pack both enforce. Gate Phase 12 pull.merge through evaluate against base branch. Turn green branch_protect_push and branch_protect_merge tracer cases; green dialect_branch_protection for tracer tables. Defer wildcards, statuses, dismiss-stale, conversation resolution, enforce_admins toggles, and UI to later plans. Do not add crates.io packages.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(branch_protect_push) | test(branch_protect_merge)' && cargo test -p oxidean-db --test dialect_branch_protection</automated>
  </verify>
  <done>Tracer push-deny and merge-block/unblock paths green (or explicitly ignored only when Phase 12 precondition unmet, with clear skip). Dialect parity green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (13-02-T2): Soft-protect still applies beside rules</name>
  <files>crates/oxidean-api/tests/repo_branch_soft_protect.rs, crates/oxidean-api/src/repo/mod.rs</files>
  <read_first>crates/oxidean-api/tests/repo_branch_soft_protect.rs, .planning/phases/13-branch-protection/13-CONTEXT.md</read_first>
  <behavior>
    - Default branch rename/delete still returns repo.default_branch_protected even when a protection rule also exists (D-04)
  </behavior>
  <action>
Confirm soft-protect floor remains after hook/rule wiring per D-04. Extend or re-run repo_branch_soft_protect coverage so default-branch rename/delete still fails with repo.default_branch_protected when a matching protection rule is present. Do not remove soft-protect in favor of rules-only delete handling.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(repo_branch_soft_protect)'</automated>
  </verify>
  <done>Existing soft-protect tests still green with protection tables/hooks present.</done>
</task>

</tasks>

<verification>
Tracer proves ORG-05 configure + ORG-06 push deny + PR-08 merge block on one path.
</verification>

<success_criteria>
Architecture skeleton for shared evaluate + hooks + merge gate is committed and tested.
</success_criteria>

<output>
Create `.planning/phases/13-branch-protection/13-02-SUMMARY.md` when done
</output>
