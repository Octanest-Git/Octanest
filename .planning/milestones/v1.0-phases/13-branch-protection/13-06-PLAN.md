---
phase: 13-branch-protection
plan: "06"
type: execute
wave: 5
depends_on:
  - "04"
  - "05"
files_modified:
  - crates/oxidean-api/src/protection
  - crates/oxidean-api/tests/branch_protect_merge.rs
  - crates/oxidean-api/tests/branch_protect_push.rs
requirements:
  - ORG-06
  - PR-08
autonomous: true
user_setup: []
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "dismiss_stale_reviews, require_conversation_resolution, require_last_push_approval enforced on merge (D-06..08, PR-08)"
    - "enforce_admins true blocks Admin bypass on push and merge (D-16)"
    - "required_linear_history blocks merge-commit method when enabled (D-17)"
  artifacts:
    - path: crates/oxidean-api/src/protection
      provides: "Full merge/push flag evaluation"
  key_links:
    - from: crates/oxidean-api/src/protection
      to: Phase 12 review + thread APIs
      via: "Approval count, dismiss, unresolved threads"
  assumptions:
    - "PRECONDITION: Phase 12 review dismiss, conversation resolve, and merge-method settings exist"
  prohibitions:
    - statement: "CODEOWNERS require_code_owner_reviews remains out of scope (D-09)"
      flagged: true
---

<objective>
Complete merge/push evaluation flags: stale dismiss, conversation resolution, last-push approval, enforce_admins, linear history (D-06..08, D-16..17).

Purpose: GitHub-parity review and admin-bypass behavior beyond the tracer’s single approval count.
Output: Green expanded branch_protect_merge (+ push admin-bypass) cases.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-branch-protection/13-CONTEXT.md
@.planning/phases/12-pull-requests/12-CONTEXT.md
@.planning/phases/13-branch-protection/13-04-SUMMARY.md
@.planning/phases/13-branch-protection/13-05-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin actor → bypass | Only when enforce_admins false |
| Review state → merge | Latest-per-user Approves only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-13 | Elevation of privilege | Admin bypass | high | mitigate | Honor enforce_admins (D-16); document default false |
| T-13-14 | Tampering | stale approvals | medium | mitigate | dismiss_stale_reviews clears Approves on head push when enabled |
| T-13-15 | Bypass | self-approve | high | mitigate | Keep D-PR-06 author cannot Approve |
| T-13-SC | Tampering | crates | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (13-06-T1): Review extras on merge path</name>
  <precondition>Phase 12 review, dismiss, and conversation-resolve APIs are available.</precondition>
  <files>crates/oxidean-api/src/protection, crates/oxidean-api/tests/branch_protect_merge.rs</files>
  <read_first>.planning/phases/12-pull-requests/12-CONTEXT.md, crates/oxidean-api/src/protection</read_first>
  <behavior>
    - dismiss_stale_reviews: after head push, prior Approve no longer satisfies until re-approve
    - require_conversation_resolution: unresolved threads block merge
    - require_last_push_approval: Approve from head pusher alone does not satisfy
  </behavior>
  <action>
Wire D-06, D-07, D-08 into Merge evaluate using Phase 12 review/thread primitives (D-PR-07, D-PR-14). Prefer persisting dismiss-on-push when dismiss_stale_reviews is true (Claude discretion). Ignore CODEOWNERS (D-09). Green branch_protect_merge cases for these flags.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(branch_protect_merge)'</automated>
  </verify>
  <done>Stale dismiss, conversation resolution, and last-push approval enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (13-06-T2): enforce_admins + linear history</name>
  <files>crates/oxidean-api/src/protection, crates/oxidean-api/tests/branch_protect_merge.rs, crates/oxidean-api/tests/branch_protect_push.rs</files>
  <read_first>crates/oxidean-api/src/protection, .planning/phases/13-branch-protection/13-CONTEXT.md</read_first>
  <behavior>
    - enforce_admins false: Admin can push/merge despite unmet reviews
    - enforce_admins true: Admin cannot bypass
    - required_linear_history true: merge-commit strategy rejected; squash/rebase still allowed if otherwise satisfied
  </behavior>
  <action>
Implement D-16 Admin bypass gating and D-17 linear-history constraint on merge method selection. Cover push and merge in tests. Draft PR merge refusal if Phase 12 draft exists (D-23).
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(branch_protect_merge) | test(branch_protect_push)'</automated>
  </verify>
  <done>Admin bypass and linear history behaviors green.</done>
</task>

</tasks>

<verification>
PR-08 and ORG-06 review/admin flags complete at API layer.
</verification>

<success_criteria>
Full GitHub-parity evaluation flags for reviews/admins/linear history are tested.
</success_criteria>

<output>
Create `.planning/phases/13-branch-protection/13-06-SUMMARY.md` when done
</output>
