---
phase: 05-cloud-verify-reset
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - crates/oxidean-api/src/auth/gate.rs
  - crates/oxidean-api/src/auth/verify_reset.rs
  - crates/oxidean-api/src/auth/local.rs
  - crates/oxidean-api/src/auth/mod.rs
  - crates/oxidean-api/src/rpc.rs
  - crates/oxidean-api/src/app.rs
  - crates/oxidean-api/tests/auth_verify_gate.rs
  - crates/oxidean-api/tests/auth_verify_reset.rs
requirements:
  - AUTH-04
autonomous: true
user_setup: []
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Unverified signed-in user calling auth.dev.privileged_ping receives auth.email_unverified (HTTP 403)"
    - "After consuming a valid 8-digit verify OTP while signed in as the target user, email_verified is true on auth.me and privileged_ping succeeds"
    - "auth.me maps UserPublic.email_verified from users.email_verified_at (D-13)"
    - "auth.dev.privileged_ping is registered only for OXIDEAN_ENV in {development,dev,test,compose} or cfg(test)"
  artifacts:
    - path: crates/oxidean-api/src/auth/gate.rs
      provides: "require_verified helper"
    - path: crates/oxidean-api/src/auth/verify_reset.rs
      provides: "issue/consume verify OTP for tracer"
    - path: crates/oxidean-api/tests/auth_verify_gate.rs
      provides: "gate + privileged_ping integration coverage"
  key_links:
    - from: crates/oxidean-api/src/auth/gate.rs
      to: crates/oxidean-db/src/users.rs
      via: "email_verified_at.is_none → auth.email_unverified"
    - from: crates/oxidean-api/src/rpc.rs
      to: crates/oxidean-api/src/auth/verify_reset.rs
      via: "auth.verify OTP path"
    - from: crates/oxidean-api/src/app.rs
      to: auth.email_unverified
      via: "rpc_status → FORBIDDEN"
---

<objective>
Prove AUTH-04 end-to-end with one production-quality tracer on top of 05-01 persistence: OTP issue/consume → `auth.me.email_verified` → `require_verified` / `auth.dev.privileged_ping`.

Purpose: Catch wiring failures early (D-01, D-02, D-06, D-07, D-09, D-10, D-13, D-14, D-20, D-21) before expanding resend/reset/UI.
Output: Gate + verify OTP RPC path and green gate/verify integration tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cloud-verify-reset/05-01-SUMMARY.md
@.planning/phases/05-cloud-verify-reset/05-CONTEXT.md
@.planning/phases/05-cloud-verify-reset/05-RESEARCH.md
@.planning/phases/05-cloud-verify-reset/05-PATTERNS.md
@crates/oxidean-api/src/auth/admin.rs
@crates/oxidean-api/src/auth/session.rs
@crates/oxidean-api/src/auth/local.rs
@crates/oxidean-api/tests/auth_session.rs

<interfaces>
API surface this plan:
```
require_verified(ctx) -> UserRow | Err auth.unauthenticated | Err auth.email_unverified
auth.verify { token?: string, code?: string }  // OTP path required in tracer; magic expanded in 05-03
auth.dev.privileged_ping () -> { ok: true } when require_verified passes
  // OXIDEAN_ENV ∈ {development,dev,test,compose} or cfg(test); never production (Open Q2 RESOLVED)
auth.me includes email_verified via user_to_public
HTTP: auth.email_unverified → 403
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/RPC client → privileged RPC | Untrusted callers may attempt privileged actions while unverified |
| Test/dev privileged RPC → production | Env-gated procedure must not exist in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Elevation of privilege | `require_verified` / privileged RPCs | high | mitigate | Deny when `email_verified_at` is null with stable code `auth.email_unverified` + short message; map HTTP 403 in `rpc_status` (D-07, D-09, D-10) |
| T-05-02 | Spoofing | `auth.verify` consume | high | mitigate | Require signed-in session whose `user_id` matches token row (D-21); reject wrong-user / anonymous consume |
| T-05-04 | Elevation of privilege | `auth.dev.privileged_ping` | high | mitigate | Register only for `OXIDEAN_ENV` in `{development,dev,test,compose}` or `cfg(test)`; otherwise `rpc.unknown_procedure` |
| T-05-SC | Tampering | cargo/npm installs | high | mitigate | No new packages in this plan |
</threat_model>

<tasks>

<task type="tracer">
  <name>Task 1 (05-02-T1): E2E verify OTP → email_verified → privileged_ping</name>
  <reversibility rating="costly">Publishes auth.email_unverified and wires UserPublic.email_verified as client/e2e contracts (D-07, D-13); renaming later touches generated clients and tests.</reversibility>
  <files>crates/oxidean-api/src/auth/gate.rs, crates/oxidean-api/src/auth/verify_reset.rs, crates/oxidean-api/src/auth/local.rs, crates/oxidean-api/src/auth/mod.rs, crates/oxidean-api/src/rpc.rs, crates/oxidean-api/src/app.rs, crates/oxidean-api/tests/auth_verify_gate.rs, crates/oxidean-api/tests/auth_verify_reset.rs</files>
  <read_first>crates/oxidean-api/src/auth/admin.rs, crates/oxidean-api/src/auth/session.rs, crates/oxidean-api/src/auth/local.rs, crates/oxidean-api/src/app.rs, crates/oxidean-api/tests/auth_session.rs, crates/oxidean-db/src/email_tokens.rs, .planning/phases/05-cloud-verify-reset/05-01-SUMMARY.md, .planning/phases/05-cloud-verify-reset/05-RESEARCH.md</read_first>
  <precondition>05-01 delivered 0003 migrations, email_tokens CRUD, set/clear email_verified_at, and UserPublic.email_verified field</precondition>
  <action>
Wire ONE happy path through API layers on top of 05-01 persistence (per D-01, D-02, D-06, D-07, D-09, D-10, D-13, D-14, D-18, D-20, D-21):

1. Implement `gate::require_verified` mirroring `require_admin` — unauthenticated → `auth.unauthenticated`; unverified → `auth.email_unverified` / "verify your email to continue" (D-07).
2. Implement `verify_reset` for purpose `verify`: CSPRNG 32-byte hex magic + 8-digit OTP; SHA-256 hex at rest via 05-01 DB helpers; 30-minute TTL; `issue_verify` for tests; `consume` accepting `code` (OTP) when session user matches token user (D-21). Do not implement full resend/rate-limit/reset yet (05-03/05-04).
3. Map `user_to_public` `email_verified: row.email_verified_at.is_some()` (D-13).
4. RPC: `auth.verify` (signed-in consume), `auth.dev.privileged_ping` (calls `require_verified`, returns `{ok:true}`) env-gated to `{development,dev,test,compose}` else unknown_procedure (Open Q2 RESOLVED); ensure `auth.me` returns updated UserPublic.
5. `rpc_status`: `auth.email_unverified` → FORBIDDEN (403).
6. Integration tests: `auth_verify_gate.rs` — unverified ping fails with code + 403; after OTP verify, ping ok. `auth_verify_reset.rs` — issue OTP, consume sets `email_verified_at`, `auth.me.email_verified == true`. Reuse `auth_session.rs` harness. Login must still succeed while unverified (D-06).
  </action>
  <verify>
    <automated>cargo test -p oxidean-api --test auth_verify_gate --test auth_verify_reset</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"; or either test binary is missing</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -q 'auth.email_unverified' crates/oxidean-api/src/auth/gate.rs`
    - `grep -q 'privileged_ping' crates/oxidean-api/src/rpc.rs`
    - `grep -q 'email_unverified' crates/oxidean-api/src/app.rs`
    - `grep -q 'email_verified' crates/oxidean-api/src/auth/local.rs`
    - `cargo test -p oxidean-api --test auth_verify_gate --test auth_verify_reset` exits 0
  </acceptance_criteria>
  <done>Unverified privileged_ping denied; OTP verify then me.email_verified and privileged_ping succeed end-to-end</done>
</task>

<task type="auto">
  <name>Task 2 (05-02-T2): Confirm env-gated ping unknown outside allowlist</name>
  <files>crates/oxidean-api/tests/auth_verify_gate.rs, crates/oxidean-api/src/rpc.rs</files>
  <read_first>crates/oxidean-api/tests/auth_verify_gate.rs, crates/oxidean-api/src/rpc.rs</read_first>
  <action>
Extend `auth_verify_gate` so that when AppState env is `production` (or another non-allowlisted value), `auth.dev.privileged_ping` returns `rpc.unknown_procedure` (404). Keep allowlist `{development,dev,test,compose}` per RESEARCH Open Q2 RESOLVED.
  </action>
  <verify>
    <automated>cargo test -p oxidean-api --test auth_verify_gate</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"</fails_when>
  </verify>
  <acceptance_criteria>
    - gate tests cover production/unknown_procedure for privileged_ping
    - `cargo test -p oxidean-api --test auth_verify_gate` exits 0
  </acceptance_criteria>
  <done>privileged_ping absent outside allowlisted envs</done>
</task>

</tasks>

## Artifacts this phase produces

Tracer owns `require_verified`, `auth.verify` OTP path, `auth.dev.privileged_ping`, and `auth.me.email_verified` wiring.

<verification>
`cargo test -p oxidean-api --test auth_verify_gate --test auth_verify_reset` green.
</verification>

<success_criteria>
Tracer path works: unverified denial, OTP verify, email_verified on me, privileged_ping ok.
</success_criteria>

<output>
Create `.planning/phases/05-cloud-verify-reset/05-02-SUMMARY.md` when done
</output>
