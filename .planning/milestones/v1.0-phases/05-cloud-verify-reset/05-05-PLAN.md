---
phase: 05-cloud-verify-reset
plan: "05"
type: execute
wave: 5
depends_on:
  - "04"
files_modified:
  - crates/octanest-api/src/auth/external.rs
  - crates/octanest-api/src/auth/workos.rs
  - crates/octanest-api/src/auth/oidc.rs
  - crates/octanest-db/src/users.rs
  - crates/octanest-db/src/lib.rs
  - crates/octanest-api/src/auth/verify_reset.rs
  - crates/octanest-api/tests/auth_verify_gate.rs
requirements:
  - AUTH-04
autonomous: true
user_setup: []
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "WorkOS/OIDC success with IdP email_verified asserted marks users.email_verified_at (D-03, D-15)"
    - "When IdP does not assert verified email, user stays unverified and reuses the same local verify flows"
    - "clear_email_verification(user_id) helper exists for future email-change (D-05) even if no email-edit RPC yet"
  artifacts:
    - path: crates/octanest-api/src/auth/external.rs
      provides: "ExternalIdentity.email_verified + apply on link"
    - path: crates/octanest-db/src/users.rs
      provides: "clear_email_verified_at facade used by helper"
  key_links:
    - from: crates/octanest-api/src/auth/workos.rs
      to: crates/octanest-api/src/auth/external.rs
      via: "User.email_verified → ExternalIdentity"
    - from: crates/octanest-api/src/auth/oidc.rs
      to: crates/octanest-api/src/auth/external.rs
      via: "claims.email_verified == Some(true)"
---

<objective>
Apply IdP-trust verification for WorkOS/OIDC and ship the D-05 clear-verification helper for future email changes — without implementing email-edit UI/RPC.

Purpose: AUTH-04 SSO edge of D-03/D-15; prepare D-05.
Output: ExternalIdentity carries email_verified; link/create sets verified_at when trusted; clear helper + tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cloud-verify-reset/05-04-SUMMARY.md
@.planning/phases/05-cloud-verify-reset/05-RESEARCH.md
@.planning/phases/05-cloud-verify-reset/05-PATTERNS.md
@crates/octanest-api/src/auth/external.rs
@crates/octanest-api/src/auth/workos.rs
@crates/octanest-api/src/auth/oidc.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IdP claims → Octanest verified flag | Only trustworthy after successful SSO callback |
| clear_email_verification helper | Must not be exposed as public unauthenticated RPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-13 | Spoofing | IdP email_verified claim | high | mitigate | Set verified only after successful WorkOS/OIDC finish when provider asserts verified; false/absent leaves null and forces local verify (D-03, D-15) |
| T-05-14 | Elevation of privilege | clear_email_verification | medium | mitigate | Internal/db helper only this phase; no public RPC to clear others' verification; document for future email-update path (D-05) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (05-05-T1): IdP-trust mark verified on SSO success</name>
  <files>crates/octanest-api/src/auth/external.rs, crates/octanest-api/src/auth/workos.rs, crates/octanest-api/src/auth/oidc.rs, crates/octanest-api/tests/auth_verify_gate.rs</files>
  <read_first>crates/octanest-api/src/auth/external.rs, crates/octanest-api/src/auth/workos.rs, crates/octanest-api/src/auth/oidc.rs, .planning/phases/05-cloud-verify-reset/05-RESEARCH.md</read_first>
  <behavior>
    - ExternalIdentity includes email_verified: bool
    - WorkOS maps user.email_verified; OIDC maps claims.email_verified() == Some(true)
    - link_or_create_user sets email_verified_at when identity.email_verified; otherwise leaves null
  </behavior>
  <action>
Per D-03 and D-15: extend `ExternalIdentity` with `email_verified`. Map WorkOS `User.email_verified` and OIDC `email_verified` claim (Some(true) only). In `link_or_create_user`, after create/link, if verified call `set_email_verified_at`; if false/absent leave unverified so the same verify email flows apply. Update stubs/fixtures as needed (RESEARCH A3). Add unit/integration assert: IdP verified → me.email_verified true / privileged_ping ok without local OTP.
  </action>
  <verify>
    <automated>cargo test -p octanest-api --lib auth::external auth::workos auth::oidc && cargo test -p octanest-api --test auth_verify_gate</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -q 'email_verified' crates/octanest-api/src/auth/external.rs`
    - workos/oidc map verified claim into ExternalIdentity
    - tests prove IdP-trust sets verified_at
  </acceptance_criteria>
  <done>SSO IdP-trust marks verified when asserted; otherwise local verify remains</done>
</task>

<task type="auto">
  <name>Task 2 (05-05-T2): clear_email_verification helper for D-05</name>
  <files>crates/octanest-db/src/users.rs, crates/octanest-db/src/lib.rs, crates/octanest-api/src/auth/verify_reset.rs</files>
  <read_first>crates/octanest-db/src/users.rs, crates/octanest-api/src/auth/verify_reset.rs</read_first>
  <action>
Ensure `clear_email_verified_at` is available on Database. Add API helper `pub(crate) fn clear_email_verification` in `verify_reset.rs` (or adjacent auth module re-exported for D-05) that clears verified_at; rustdoc: call when email change lands (D-05) and issue a new verify email. No email-edit RPC in this phase. REQUIRED unit test `clear_email_verification_clears_verified_flag` (or equally named) proving clear then `require_verified` fails — do not ship without this test.
  </action>
  <verify>
    <automated>grep -q 'fn clear_email_verification' crates/octanest-api/src/auth/verify_reset.rs && cargo test -p octanest-api --lib clear_email_verification</automated>
    <fails_when>grep exit != 0 (helper missing); or cargo test exit != 0; or stdout/stderr contains "FAILED"; or "0 passed" / "no tests to run" / "filtered out" with zero matching tests</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -q 'fn clear_email_verification' crates/octanest-api/src/auth/verify_reset.rs`
    - `grep -q 'clear_email_verified' crates/octanest-db/src/users.rs`
    - `cargo test -p octanest-api --lib clear_email_verification` exits 0 with ≥1 test passed
  </acceptance_criteria>
  <done>D-05 clear-verification helper exists for future email-change</done>
</task>

</tasks>

## Artifacts this phase produces

IdP-trust verified marking + `clear_email_verification` helper (D-03, D-05, D-15).

<verification>
API auth lib tests + auth_verify_gate green for IdP-trust path.
</verification>

<success_criteria>
SSO verified claim marks users verified; clear helper ready for email change.
</success_criteria>

<output>
Create `.planning/phases/05-cloud-verify-reset/05-05-SUMMARY.md` when done
</output>
