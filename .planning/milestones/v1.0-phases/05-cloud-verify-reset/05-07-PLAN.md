---
phase: 05-cloud-verify-reset
plan: "07"
type: execute
wave: 7
depends_on:
  - "06"
files_modified:
  - apps/web/src/routes/reset-password.tsx
  - apps/web/src/routes/login.tsx
  - apps/web/src/routes/dashboard.tsx
  - apps/web/src/styles.css
requirements:
  - AUTH-12
  - AUTH-04
  - AUTH-05
autonomous: true
user_setup: []
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Default /reset-password request shows unfilled email form; unknown email uses anti-enumeration success panel (never not-found)"
    - "Send reset email shows disabled + Working…; AuthShell stays visible"
    - "AuthErrorBanner on reset request for network/validation; anti-enumeration never uses not-found error"
    - "Reset redeem empty OTP slots + empty password fields until user input"
    - "Update password / token redeem: disabled + Working…"
    - "AuthErrorBanner on reset redeem for mismatch, too short, invalid/expired, SSO-only"
    - "Token-only, code-only, or both on redeem; password+confirm required before Update password"
    - "Helpers and errors wrap under fields; password helper stays Label size"
    - "Privileged New repository CTA hint wraps under disabled button; not truncated mid-word"
    - "Login shows Forgot password? → /reset-password in local mode only"
    - "Signup remains open with no invite UI (AUTH-05)"
    - "Unverified dashboard New repository is visible disabled with verify hint; verified uses later-phase hint (D-12)"
    - statement: "Anti-enumeration success body wraps in max-w-md"
      verification: backstop
  artifacts:
    - path: apps/web/src/routes/reset-password.tsx
      provides: "request + redeem reset UI"
    - path: apps/web/src/routes/login.tsx
      provides: "Forgot password entry"
    - path: apps/web/src/routes/dashboard.tsx
      provides: "disabled New repository CTA pattern"
  key_links:
    - from: apps/web/src/routes/reset-password.tsx
      to: packages/api-client/src/index.ts
      via: "auth.requestPasswordReset / auth.resetPassword"
    - from: apps/web/src/routes/login.tsx
      to: apps/web/src/routes/reset-password.tsx
      via: "Forgot password?"
    - from: apps/web/src/routes/dashboard.tsx
      to: packages/api-client/src/index.ts
      via: "auth.me.email_verified drives CTA hint"
---

<objective>
Finish Phase 5 UX: `/reset-password` (request + redeem), login forgot entry, and dashboard disabled privileged CTA pattern — matching 05-UI-SPEC exactly.

Purpose: AUTH-12 user flows + AUTH-04 CTA/banner complement + AUTH-05 no-invite confirmation in UI.
Output: Reset pages, forgot link, New repository disabled pattern.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cloud-verify-reset/05-UI-SPEC.md
@.planning/phases/05-cloud-verify-reset/05-06-SUMMARY.md
@apps/web/src/routes/verify.tsx
@apps/web/src/routes/login.tsx
@apps/web/src/routes/dashboard.tsx
@apps/web/src/components/ui/input-otp.tsx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous reset forms → API | Enumeration / XSS via error rendering |
| Magic token query on reset page | Referrer leakage |
| Disabled CTA → future repo.create | Must not fake create |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-17 | Information disclosure | reset request UI | high | mitigate | Always show identical anti-enumeration success panel copy from UI-SPEC (D-28); never render not-found |
| T-05-18 | Information disclosure | reset magic URL | medium | mitigate | Referrer-Policy no-referrer on `/reset-password`; never echo token into fields |
| T-05-19 | Elevation of privilege | New repository CTA | medium | mitigate | Button stays disabled for everyone in Phase 5; hints differ verified vs unverified (D-12); no fake create call |
| T-05-20 | Injection XSS | AuthErrorBanner | medium | mitigate | Render server/client messages as text nodes only; use UI-SPEC copy |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (05-07-T1): /reset-password + login forgot link</name>
  <files>apps/web/src/routes/reset-password.tsx, apps/web/src/routes/login.tsx, apps/web/src/styles.css</files>
  <read_first>apps/web/src/routes/verify.tsx, apps/web/src/routes/login.tsx, apps/web/src/routes/signup.tsx, .planning/phases/05-cloud-verify-reset/05-UI-SPEC.md</read_first>
  <action>
Per D-24…D-28 and UI-SPEC:
1. `/reset-password` (`Reset password · Octanest`) with AuthShell + `oct-auth-enter`.
2. Request state (local mode): email field, Send reset email, exact anti-enumeration success heading/body; workos/oidc mode: IdP-managed body + Continue CTA (no email form).
3. Redeem state (token query and/or code entry): title Choose a new password; InputOtp 8 slots with onComplete auto-submit path; new password + confirm (min 8, mismatch client check); Update password; token auto-attempt when present; SSO-only error copy; success redirects signed-in to dashboard/returnTo.
4. Referrer-Policy no-referrer. Exact Copywriting Contract strings.
5. On `/login` local mode only: Label-size Forgot password? after password field before Remember me → `/reset-password`. Omit for WorkOS/OIDC panels.
6. Confirm signup page still has no invite fields (AUTH-05 / D-08).
  </action>
  <verify>
    <automated>bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or stderr contains "error TS"</fails_when>
  </verify>
  <human-check>
    Local mode: forgot link → request reset → identical success for unknown email; redeem OTP/password path; SSO mode shows IdP message without email form.
  </human-check>
  <acceptance_criteria>
    - `test -f apps/web/src/routes/reset-password.tsx`
    - `grep -q 'Forgot password' apps/web/src/routes/login.tsx`
    - `bun --cwd apps/web run build` exits 0
  </acceptance_criteria>
  <done>Reset request/redeem UI and login forgot entry match UI-SPEC</done>
</task>

<task type="auto">
  <name>Task 2 (05-07-T2): Dashboard disabled New repository CTA</name>
  <files>apps/web/src/routes/dashboard.tsx</files>
  <read_first>apps/web/src/routes/dashboard.tsx, .planning/phases/05-cloud-verify-reset/05-UI-SPEC.md</read_first>
  <action>
Per D-12: add primary Button New repository to dashboard link row. Always disabled in Phase 5 (`disabled`, `aria-disabled="true"`). If `email_verified === false`: title/hint Verify your email to create a repository. If verified: hint Repository creation arrives in a later phase. Use reduced-opacity primary disabled styling (not warm secondary). Hint wraps under button. Do not call any create RPC. Do not implement real repo.create (Phase 7 deferred).
  </action>
  <verify>
    <automated>bun --cwd apps/web run build && cargo test -p octanest-api --test auth_verify_gate --test auth_verify_reset && cargo test -p octanest-api --test auth_signup</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"; or web build emits error TS</fails_when>
  </verify>
  <human-check>
    Unverified: CTA disabled + verify hint; verified: CTA disabled + later-phase hint; banner + /verify + /reset-password light/dark; no invite on signup.
  </human-check>
  <acceptance_criteria>
    - `grep -q 'New repository' apps/web/src/routes/dashboard.tsx`
    - `grep -q 'email_verified\|Verify your email' apps/web/src/routes/dashboard.tsx`
    - phase regression: auth_verify_gate, auth_verify_reset, auth_signup exit 0
    - `bun --cwd apps/web run build` exits 0
  </acceptance_criteria>
  <done>Privileged CTA pattern shipped; phase regression suite green</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `0003_email_tokens` + DB helpers + UserPublic.email_verified field | 05-01 |
| `require_verified` + verify OTP tracer + privileged_ping | 05-02 |
| Verify issue/resend + admin seed + AUTH-05 | 05-03 |
| Password reset RPCs | 05-04 |
| IdP-trust + clear_email_verification | 05-05 |
| api-client + `/verify` + VerifyBanner | 05-06 |
| `/reset-password` + forgot + disabled New repository CTA | 05-07 |

Phase complete when AUTH-04, AUTH-05, AUTH-12 success criteria are true and UI-SPEC copy/states match.

<verification>
Web build + auth_verify_* + auth_signup green; end-of-phase human UAT harvests human-check blocks.
</verification>

<success_criteria>
Users can reset passwords via email UX; privileged CTA pattern and open signup hold; roadmap criteria 1–3 met.
</success_criteria>

<output>
Create `.planning/phases/05-cloud-verify-reset/05-07-SUMMARY.md` when done
</output>
