---
phase: 11.1-quality-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/repo/repo-chrome.tsrx
  - apps/web/src/lib/repo-chrome-active.ts
  - apps/web/src/lib/repo-chrome-active.unit.test.ts
  - apps/web/src/routes/$owner.$repo.tsrx
  - apps/web/src/routes/$owner.$repo.packages.tsrx
  - apps/web/src/routes/$owner.$repo.packages.integration.test.ts
  - apps/web/src/routes/$owner.$repo.integration.test.ts
requirements:
  - ISSUE-3
autonomous: true
user_setup: []
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Repo chrome mounts once in /$owner/$repo layout when repo status is ok (D-QH-01)"
    - "Packages is a first-class chrome tab with active packages highlighting (D-QH-01)"
    - "Repo packages page renders under the shared shell without its own max-w-6xl chrome duplicate (D-QH-01)"
  artifacts:
    - path: apps/web/src/components/repo/repo-chrome.tsrx
      provides: "active union includes packages; Packages nav link"
    - path: apps/web/src/routes/$owner.$repo.tsrx
      provides: "RepoChrome above Outlet from useRepoStore"
    - path: apps/web/src/lib/repo-chrome-active.ts
      provides: "pathname → active tab helper"
    - path: apps/web/src/routes/$owner.$repo.packages.tsrx
      provides: "packages content under layout shell"
  key_links:
    - from: apps/web/src/routes/$owner.$repo.tsrx
      to: apps/web/src/components/repo/repo-chrome.tsrx
      via: "layout mounts chrome; active from repo-chrome-active"
    - from: apps/web/src/components/repo/repo-chrome.tsrx
      to: apps/web/src/routes/$owner.$repo.packages.tsrx
      via: "Packages href /$owner/$repo/packages"
  assumptions:
    - "ASSUME (discretion): active tab derived from pathname segments (issues|releases|packages|settings|commits|branches|tags → matching; else code)"
    - "ASSUME: leaf routes may still mount RepoChrome until plan 11.1-03; tracer proves layout+packages path only"
  prohibitions:
    - statement: "Must not invent Phase 12 PR domain or treat pr_stub as real PRs"
      flagged: true
    - statement: "Must not hand-edit packages/api-client as source of truth"
      flagged: true
    - statement: "Must not split mega settings into sub-routes (D-QH-06 P2 only)"
      flagged: true
---

<objective>
Tracer for D-QH-01: mount `RepoChrome` in `$owner.$repo` layout, add Packages tab + `active: "packages"`, and render repo packages through the shared shell.

Purpose: Stop packages discovery IA hole before Phase 12 reuses chrome; prove single-mount path for one leaf.
Output: Layout chrome + Packages nav + packages page under shell; green unit/integration asserts for this path.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.1-quality-hardening/11.1-CONTEXT.md
@.planning/phases/11.1-quality-hardening/11.1-RESEARCH.md
@.agents/skills/octane/SKILL.md
@apps/web/src/components/repo/repo-chrome.tsrx
@apps/web/src/routes/$owner.$repo.tsrx
@apps/web/src/routes/$owner.$repo.packages.tsrx
@apps/web/src/lib/repo-store.ts
@apps/web/src/components/repo/repo-store-provider.tsrx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → repo layout | Public/private repo data already gated by loader ACL |
| Chrome nav → routes | Untrusted path segments only select highlight; no auth change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.1-01 | Information disclosure | RepoChrome Settings link | medium | mitigate | Keep Settings visibility on `repo.can_admin` / `showSettings` (unchanged) |
| T-11.1-02 | Spoofing | active tab from pathname | low | accept | Highlight only; no capability elevation from path |
| T-11.1-SC | Tampering | npm/cargo installs | high | mitigate | No new packages in this plan |
</threat_model>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (11.1-01-T1): Layout chrome + Packages tab end-to-end</name>
  <files>apps/web/src/components/repo/repo-chrome.tsrx, apps/web/src/lib/repo-chrome-active.ts, apps/web/src/lib/repo-chrome-active.unit.test.ts, apps/web/src/routes/$owner.$repo.tsrx, apps/web/src/routes/$owner.$repo.packages.tsrx</files>
  <read_first>.agents/skills/octane/SKILL.md, apps/web/src/components/repo/repo-chrome.tsrx, apps/web/src/routes/$owner.$repo.tsrx, apps/web/src/routes/$owner.$repo.packages.tsrx, apps/web/src/lib/repo-store.ts</read_first>
  <behavior>
    - repoChromeActiveFromPath maps /o/r/packages → packages; /o/r/issues → issues; /o/r → code
    - RepoChrome active union includes packages; nav has Packages link to /$owner/$repo/packages
    - When layout status is ok, RepoChrome renders above Outlet using useRepoStore repo
    - Repo packages page is content-only under layout (no duplicate title chrome / outer max-w-6xl shell that fights layout)
  </behavior>
  <action>
Per D-QH-01: add `apps/web/src/lib/repo-chrome-active.ts` (+ unit tests) mapping pathname → chrome active including packages. Extend `RepoChrome` active union with packages and a Packages tab between Releases and Settings. In `$owner.$repo.tsrx`, when status is ok, render `RepoChrome` from `useRepoStore` repo + derived active above `Outlet` (Octane `.tsrx` only — follow `.agents/skills/octane/SKILL.md`). Rework `$owner.$repo.packages.tsrx` to drop standalone page chrome/shell so it inherits layout shell. Leave other leaves remounting chrome until plan 03. Do not invent Phase 12 PR UI.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/lib/repo-chrome-active.unit.test.ts && bun --cwd apps/web run build</automated>
  </verify>
  <done>Layout mounts chrome with Packages tab; packages route shares shell; unit tests for active helper pass; web build succeeds</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (11.1-01-T2): Packages + layout integration asserts</name>
  <files>apps/web/src/routes/$owner.$repo.packages.integration.test.ts, apps/web/src/routes/$owner.$repo.integration.test.ts</files>
  <read_first>apps/web/src/routes/$owner.$repo.packages.integration.test.ts, apps/web/src/routes/$owner.$repo.integration.test.ts, apps/web/src/routes/$owner.$repo.issues-releases.layout.integration.test.ts</read_first>
  <behavior>
    - packages integration asserts Packages tab / active packages wiring (source or render)
    - layout integration asserts RepoChrome present in layout source for ok status path
    - Existing repository_id list scoping asserts remain green
  </behavior>
  <action>
Per D-QH-01: extend packages and layout integration tests so CI proves chrome Packages tab and layout mount (prefer `?raw` / export contracts consistent with existing Wave 0 style, plus any render asserts the harness already supports). Keep D-PKG-11 repository_id scoping checks. Avoid reintroducing a Playwright component project.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/\$owner.\$repo.packages.integration.test.ts src/routes/\$owner.\$repo.integration.test.ts</automated>
  </verify>
  <done>Packages and layout integration tests green for chrome+packages discovery path</done>
</task>

<task type="auto">
  <name>Task 3 (11.1-01-T3): Web build + chrome packages smoke grep</name>
  <files>apps/web/src/components/repo/repo-chrome.tsrx, apps/web/src/routes/$owner.$repo.tsrx</files>
  <read_first>apps/web/src/components/repo/repo-chrome.tsrx, apps/web/src/routes/$owner.$repo.tsrx</read_first>
  <action>
Confirm Packages tab and layout `RepoChrome` import remain wired after test edits. Run a full web build. Discretion: if Settings still appears after Packages, preserve can_admin gating unchanged.
  </action>
  <verify>
    <automated>bun --cwd apps/web run build && rg -n 'packages' apps/web/src/components/repo/repo-chrome.tsrx | rg -v '^#' | rg -q 'packages' && rg -n 'RepoChrome' apps/web/src/routes/\$owner.\$repo.tsrx | rg -v '^#' | rg -q 'RepoChrome'</automated>
  </verify>
  <done>Build green; chrome sources reference packages and layout references RepoChrome</done>
</task>

</tasks>

<verification>
Tracer path: layout chrome → Packages tab → packages route under shell; unit + integration + build green.
</verification>

<success_criteria>
D-QH-01 packages discovery works from layout chrome; leaf dedupe deferred to 11.1-03.
</success_criteria>

<output>
Create `.planning/phases/11.1-quality-hardening/11.1-01-SUMMARY.md` when done
</output>
