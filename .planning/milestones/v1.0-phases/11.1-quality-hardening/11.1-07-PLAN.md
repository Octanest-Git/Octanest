---
phase: 11.1-quality-hardening
plan: "07"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/routes/admin/lfs.tsrx
  - apps/web/src/routes/admin/lfs.integration.test.ts
  - apps/web/e2e/stack-browser/commands.ts
  - apps/web/e2e/stack-browser/admin-lfs.stack.browser.test.tsx
  - apps/web/vitest.config.ts
gap_closure: true
gap_ids:
  - G-11.1-15
requirements:
  - ISSUE-3
autonomous: true
user_setup: []
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Admin LFS quotas page (/admin/lfs) mounts without ReferenceError / Octane template failure for a sys-admin session (G-11.1-15)"
    - "happy-dom integration test mounts AdminLfsPage via renderWithQueryClient and asserts visible page chrome (not raw-source-only) (G-11.1-15)"
    - "Stack-browser e2e opens /admin/lfs as admin and sees the quotas page (D-QH-03, G-11.1-15)"
  artifacts:
    - path: apps/web/src/routes/admin/lfs.tsrx
      provides: "useState for error/pending; nested @if/@else only"
    - path: apps/web/src/routes/admin/lfs.integration.test.ts
      provides: "happy-dom render mount of AdminLfsPage"
    - path: apps/web/e2e/stack-browser/admin-lfs.stack.browser.test.tsx
      provides: "browser smoke for /admin/lfs"
  key_links:
    - from: apps/web/src/routes/admin/lfs.tsrx
      to: apps/web/src/routes/admin/lfs.integration.test.ts
      via: "exported AdminLfsPage + renderWithQueryClient"
    - from: apps/web/e2e/stack-browser/commands.ts
      to: apps/web/e2e/stack-browser/admin-lfs.stack.browser.test.tsx
      via: "Playwright browser command registered in vitest.config.ts"
  assumptions:
    - "ASSUME: working-tree partial fix (useState + nested @if) may already exist — executor verifies green and completes any remaining gaps"
    - "ASSUME: reuse ensureForgeAdminSession / injectSessionCookie patterns from existing forge stack-browser commands"
  prohibitions:
    - statement: "Must not invent Phase 12 PR objects or PR UI"
      flagged: true
    - statement: "Must not revive removed e2e/component Playwright project (D-QH-03)"
      flagged: true
---

<objective>
Close G-11.1-15 for Admin LFS quotas: finish the Octane/ReferenceError fix on `/admin/lfs`, prove it with a happy-dom **render mount** (not raw-source-only), and add a stack-browser smoke for the critical admin path (D-QH-03).

Purpose: UAT confirmed the quotas page still broke at render; Phase 11.1 forge matrix never hit admin LFS, so CI could not catch it.
Output: Green AdminLfsPage + integration render test + `admin-lfs` stack-browser suite.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.1-quality-hardening/11.1-UAT.md
@.planning/phases/11.1-quality-hardening/11.1-CONTEXT.md
@.planning/phases/11.1-quality-hardening/11.1-04-SUMMARY.md
@.agents/skills/octane/SKILL.md
@apps/web/src/routes/admin/lfs.tsrx
@apps/web/src/routes/admin/lfs.integration.test.ts
@apps/web/src/test/render-with-query.ts
@apps/web/e2e/stack-browser/commands.ts
@apps/web/e2e/stack-browser/forge-repo.stack.browser.test.tsx
@apps/web/vitest.config.ts
</context>

<precondition>Phase 11.1 plans 01–06 SUMMARYs exist; stack-browser command patterns from 11.1-04 are available.</precondition>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser e2e → admin LFS RPC | Sys-admin session cookie crosses into quota settings UI |
| happy-dom test → mocked apiClient | Untrusted UI state must not assume real credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.1-70 | Elevation of privilege | /admin/lfs stack-browser | medium | mitigate | Use ENV admin only; assert forbidden path remains for non-admin in existing/happy-dom coverage where present |
| T-11.1-71 | Information disclosure | LFS quota values in e2e | low | accept | Local stub stack; no production secrets |
| T-11.1-SC | Tampering | packages | high | mitigate | No new package installs |
</threat_model>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (11.1-07-T1): Admin LFS render path — fix + happy-dom mount</name>
  <files>apps/web/src/routes/admin/lfs.tsrx, apps/web/src/routes/admin/lfs.integration.test.ts</files>
  <read_first>.agents/skills/octane/SKILL.md, apps/web/src/routes/admin/lfs.tsrx, apps/web/src/routes/admin/lfs.integration.test.ts, apps/web/src/routes/admin/auth.integration.test.ts, apps/web/src/test/render-with-query.ts</read_first>
  <behavior>
    - AdminLfsPage declares local state for error and pending (useState) before setError/setPending use
    - Template uses nested @if/@else only (Octane skill — no unsupported else-if directive)
    - Integration test calls renderWithQueryClient(AdminLfsPage) and finds Git LFS quotas / admin-lfs-page markers without throw
    - Raw-source-only assertions alone are insufficient; at least one test must mount the component
  </behavior>
  <action>
Per G-11.1-15 and D-QH-03: ensure `AdminLfsPage` in `apps/web/src/routes/admin/lfs.tsrx` declares `useState` for `error`/`pending` (and any other setters used at render/save). Keep Rivet `@{` authoring; nest `@if`/`@else` for multi-branch UI per `.agents/skills/octane/SKILL.md`. Working tree may already contain this fix — verify and complete any remaining gaps rather than reverting. Upgrade `lfs.integration.test.ts` so it **mounts** `AdminLfsPage` via `renderWithQueryClient` with mocked `apiClient.admin.lfs.*` + `auth.me` (mirror `admin/auth.integration.test.ts`). Keep export/source sanity checks if useful, but the suite must fail if the page throws on mount. Do not invent Phase 12 PR UI.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/admin/lfs.integration.test.ts && rg -n 'renderWithQueryClient\(AdminLfsPage' apps/web/src/routes/admin/lfs.integration.test.ts | rg -v '^#' | rg -q . && ! rg -n '@else if' apps/web/src/routes/admin/lfs.tsrx</automated>
  </verify>
  <done>Admin LFS page mounts under happy-dom without ReferenceError; integration suite includes a real render mount (G-11.1-15)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (11.1-07-T2): Stack-browser smoke for /admin/lfs</name>
  <files>apps/web/e2e/stack-browser/commands.ts, apps/web/e2e/stack-browser/admin-lfs.stack.browser.test.tsx, apps/web/vitest.config.ts</files>
  <read_first>apps/web/e2e/stack-browser/commands.ts, apps/web/e2e/stack-browser/forge-repo.stack.browser.test.tsx, apps/web/vitest.config.ts, apps/web/src/routes/admin/lfs.tsrx</read_first>
  <behavior>
    - Signed-in sys-admin (ENV/stack admin after credential confirm) opens /admin/lfs
    - Page shows Git LFS quotas heading or data-testid admin-lfs-page without Octane error overlay
  </behavior>
  <action>
Per G-11.1-15 and D-QH-03: add a Playwright browser command (e.g. `expectAdminLfsQuotasFlow`) that injects the admin session cookie (reuse `ensureForgeAdminSession` / `injectSessionCookie`), navigates to `/admin/lfs`, and waits for `data-testid="admin-lfs-page"` or heading "Git LFS quotas". Add `admin-lfs.stack.browser.test.tsx` calling the command. Register the command in `apps/web/vitest.config.ts` `browser.commands` beside existing forge commands. Do not revive `e2e/component`. Prefer thin smoke (open + assert render) over full quota edit CRUD unless already trivial.
  </action>
  <verify>
    <automated>test -f apps/web/e2e/stack-browser/admin-lfs.stack.browser.test.tsx && rg -n 'expectAdminLfsQuotasFlow|admin-lfs' apps/web/vitest.config.ts | rg -v '^#' | rg -q . && make test-e2e-stack</automated>
  </verify>
  <done>Stack-browser covers /admin/lfs; suite present and green under make test-e2e-stack (G-11.1-15)</done>
</task>

</tasks>

<verification>
bun --cwd apps/web exec vitest run src/routes/admin/lfs.integration.test.ts
make test-e2e-stack (includes admin-lfs stack-browser)
</verification>

<success_criteria>
- Admin LFS quotas page renders without ReferenceError for sys-admin
- happy-dom mount test exists and passes
- /admin/lfs has stack-browser coverage
</success_criteria>

<output>
Create `.planning/phases/11.1-quality-hardening/11.1-07-SUMMARY.md` when done
</output>
