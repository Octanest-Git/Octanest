---
phase: 11.1-quality-hardening
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/vitest.config.ts
  - packages/api-client/package.json
  - docs/TESTING.md
  - scripts/coverage-weighted.sh
  - Makefile
  - .github/workflows/ci.yml
requirements:
  - ISSUE-3
autonomous: false
user_setup: []
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "docs/TESTING.md documents unit/integration/e2e weights 25/40/35 and how to collect coverage (D-QH-02)"
    - "CI runs a weighted coverage gate that fails below the documented initial floor (~0.70) (D-QH-02)"
    - "Web unit+integration and Rust lib/integration coverage artifacts can be produced via Make/scripts"
  artifacts:
    - path: docs/TESTING.md
      provides: "Coverage layers, weights, commands, gate floor"
    - path: scripts/coverage-weighted.sh
      provides: "Weighted aggregator exit non-zero under floor"
    - path: .github/workflows/ci.yml
      provides: "coverage / weighted gate job or steps"
  key_links:
    - from: apps/web/vitest.config.ts
      to: scripts/coverage-weighted.sh
      via: "Vitest coverage JSON/LCOV consumed by aggregator"
    - from: scripts/coverage-weighted.sh
      to: .github/workflows/ci.yml
      via: "CI invokes script after collecting layer scores"
  assumptions:
    - "ASSUME (discretion): initial weighted floor 0.70 with ratchet note in TESTING.md"
    - "ASSUME: E2E layer score may start as checklist binary (stack-browser required flows + selected smokes) scaled into 0..1 until % coverage exists"
    - "ASSUME: @vitest/coverage-v8 is [ASSUMED] until human legitimacy checkpoint passes"
  prohibitions:
    - statement: "Must not revive removed e2e/component Playwright project (D-QH-03)"
      flagged: true
    - statement: "Must not hand-edit generated api-client sources as source of truth"
      flagged: true
---

<objective>
Install coverage tooling, document D-QH-02 weights (25/40/35) in `docs/TESTING.md`, and wire a CI weighted gate with initial floor ~0.70.

Purpose: Make forge-core quality measurable under CI before Phase 12.
Output: Vitest coverage config, Rust llvm-cov (or equivalent) collection, aggregator script, CI step, TESTING.md coverage section.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.1-quality-hardening/11.1-CONTEXT.md
@.planning/phases/11.1-quality-hardening/11.1-RESEARCH.md
@docs/TESTING.md
@apps/web/vitest.config.ts
@apps/web/package.json
@.github/workflows/ci.yml
@Makefile
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → package registry | New coverage reporter dependency install |
| Coverage artifacts → gate | Untrusted local numbers must not be forgeable without failing collect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.1-10 | Tampering | @vitest/coverage-v8 install | high | mitigate | Package legitimacy human checkpoint before bun add; prefer npm registry official package |
| T-11.1-11 | Denial of service | CI coverage job | medium | mitigate | Bound runtime; reuse rust-cache; fail-fast on gate only after collect |
| T-11.1-12 | Information disclosure | coverage reports | low | accept | No secrets in LCOV; standard CI logs |
| T-11.1-SC | Tampering | npm/cargo installs | high | mitigate | Legitimacy checkpoint for [ASSUMED] packages; cargo-llvm-cov via taiki-e/install-action like nextest |

</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1 (11.1-02-T1): Legitimacy check @vitest/coverage-v8</name>
  <files>apps/web/package.json</files>
  <action>
RESEARCH.md has no Package Legitimacy Audit table; treat `@vitest/coverage-v8` as [ASSUMED]. Pause before any bun add. Present the how-to-verify steps and wait for approval. Do not install until approved. cargo-llvm-cov will use taiki-e/install-action in CI (same trust model as nextest) — note that in the approval reply.
  </action>
  <what-built>
    Planned coverage stack needing one new npm provider: `@vitest/coverage-v8` for Vitest ^5 already in apps/web. No install yet. Rust coverage planned via cargo-llvm-cov on CI (taiki-e/install-action), not an npm package.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@vitest/coverage-v8
    2. Confirm publisher/maintainers are Vitest/official, package name exact (no typo-squat), and version range compatible with Vitest ^5 in apps/web/package.json
    3. Confirm you accept cargo-llvm-cov via taiki-e/install-action in CI (parallel to nextest)
    4. Reply approved (or reject / suggest alternate coverage approach)
  </how-to-verify>
  <verify>
    <automated>test -f apps/web/package.json && echo "checkpoint: awaiting human legitimacy approval for @vitest/coverage-v8"</automated>
  </verify>
  <resume-signal>Type "approved" or reject with alternate coverage approach</resume-signal>
  <done>Human approved @vitest/coverage-v8 legitimacy (and noted cargo-llvm-cov CI install approach)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (11.1-02-T2): Coverage collect + weighted aggregator + TESTING.md</name>
  <files>apps/web/package.json, apps/web/vitest.config.ts, packages/api-client/package.json, docs/TESTING.md, scripts/coverage-weighted.sh, Makefile</files>
  <read_first>docs/TESTING.md, apps/web/vitest.config.ts, apps/web/package.json, Makefile</read_first>
  <behavior>
    - make coverage-web (or documented bun script) emits Vitest coverage for unit+integration
    - Aggregator reads layer scores with weights unit=0.25 integration=0.40 e2e=0.35 (D-QH-02)
    - Weighted score below documented floor (~0.70) exits non-zero
    - docs/TESTING.md replaces empty coverage section with commands + weights + ratchet note
  </behavior>
  <action>
After legitimacy approval: add `@vitest/coverage-v8` to apps/web (and api-client only if needed for its Vitest run). Configure Vitest coverage reporters (json-summary and/or lcov) for unit+integration projects — keep e2e-stack-browser out of default `bun run test`. Add `scripts/coverage-weighted.sh` implementing D-QH-02 weights 25/40/35 and initial floor ~0.70 (document exact constant). E2E layer may use checklist score (fraction of required forge flows + smoke presence) until % coverage exists — document the formula. Add Make targets (e.g. `coverage-web`, `coverage-rust`, `coverage-weighted`) that collect inputs then run the aggregator. Rewrite `docs/TESTING.md` Coverage requirements + CI sections accordingly. Prefer cargo-llvm-cov / llvm-cov for Rust lib+tests via documented Make target (install in CI later), not tarpaulin.
  </action>
  <verify>
    <automated>test -f scripts/coverage-weighted.sh && rg -n '0\.25|25%' docs/TESTING.md scripts/coverage-weighted.sh | rg -v '^#' | head -1 | rg -q . && rg -n '0\.40|40%' docs/TESTING.md scripts/coverage-weighted.sh | rg -v '^#' | head -1 | rg -q . && rg -n '0\.35|35%' docs/TESTING.md scripts/coverage-weighted.sh | rg -v '^#' | head -1 | rg -q . && chmod +x scripts/coverage-weighted.sh && make help | rg -q coverage</automated>
  </verify>
  <done>Weights documented; aggregator + Make targets exist; Vitest coverage provider installed after approval</done>
</task>

<task type="auto">
  <name>Task 3 (11.1-02-T3): CI weighted coverage gate</name>
  <files>.github/workflows/ci.yml, docs/TESTING.md</files>
  <read_first>.github/workflows/ci.yml, docs/TESTING.md, scripts/coverage-weighted.sh</read_first>
  <action>
Per D-QH-02: wire CI to collect web (+ rust if feasible in time budget) coverage and run `scripts/coverage-weighted.sh` so PRs fail under the documented floor. Prefer a dedicated job or steps on `web-octane`/`api-rust` that upload artifacts on failure. Install cargo-llvm-cov via taiki-e/install-action when rust coverage is collected. Do not revive removed component e2e for coverage. Update TESTING.md CI table to list the new gate.
  </action>
  <verify>
    <automated>rg -n 'coverage-weighted|coverage_weighted|weighted' .github/workflows/ci.yml docs/TESTING.md | rg -v '^#' | rg -q .</automated>
  </verify>
  <done>CI references weighted coverage gate; TESTING.md CI table updated</done>
</task>

</tasks>

<verification>
Local Make coverage targets + docs weights + CI gate wiring present; human legitimacy approved before install.
</verification>

<success_criteria>
D-QH-02 satisfied: documented 25/40/35 weights and enforceable weighted floor in CI.
</success_criteria>

<output>
Create `.planning/phases/11.1-quality-hardening/11.1-02-SUMMARY.md` when done
</output>
