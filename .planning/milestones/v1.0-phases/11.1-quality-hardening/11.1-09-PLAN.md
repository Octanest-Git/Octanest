---
phase: 11.1-quality-hardening
plan: "09"
type: execute
wave: 3
depends_on:
  - "07"
  - "08"
files_modified:
  - apps/web/e2e/route-coverage.manifest.json
  - scripts/check-route-coverage.sh
  - apps/web/src/routes/**/*.integration.test.ts
  - Makefile
  - .github/workflows/ci.yml
  - docs/TESTING.md
gap_closure: true
gap_ids:
  - G-11.1-15
requirements:
  - ISSUE-3
autonomous: true
user_setup: []
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every user-facing apps/web/src/routes/**/*.tsrx has at least one of: happy-dom render mount, stack-browser flow evidence, or documented skip with rationale (G-11.1-15)"
    - "CI / Make gate fails when a new .tsrx route is added without coverage or an approved skip entry (G-11.1-15)"
    - "docs/TESTING.md documents the route-coverage policy and how to register coverage or skips"
  artifacts:
    - path: apps/web/e2e/route-coverage.manifest.json
      provides: "Per-route coverage kind + evidence path or skip rationale"
    - path: scripts/check-route-coverage.sh
      provides: "Discovery gate vs manifest — non-zero on drift"
  key_links:
    - from: scripts/check-route-coverage.sh
      to: apps/web/e2e/route-coverage.manifest.json
      via: "Enumerate .tsrx; require manifest entry"
    - from: .github/workflows/ci.yml
      to: scripts/check-route-coverage.sh
      via: "CI step or Make target invoked on PR"
    - from: apps/web/e2e/route-coverage.manifest.json
      to: apps/web/e2e/stack-browser/
      via: "stack-browser evidence paths for forge + admin flows"
  assumptions:
    - "ASSUME (discretion): __root and pure Outlet layout shells may use skip entries that cite child-route evidence"
    - "ASSUME: attribute existing forge stack-browser suites to the leaf routes they already exercise before writing new tests"
  prohibitions:
    - statement: "Must not invent Phase 12 PR routes or PR UI"
      flagged: true
    - statement: "Must not claim coverage from raw-source-only tests (must be render mount, stack-browser, or skip)"
      flagged: true
---

<objective>
Close the remainder of G-11.1-15: inventory every user-facing `.tsrx` route, ensure each has happy-dom render mount, stack-browser evidence, or a documented skip with rationale, and add a CI/Make gate that fails when new routes lack coverage.

Purpose: UAT root cause — forge e2e missed admin LFS because there was no whole-route coverage contract.
Output: route-coverage manifest + check script + filled gaps + CI/Make wiring + TESTING.md policy.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.1-quality-hardening/11.1-07-PLAN.md
@.planning/phases/11.1-quality-hardening/11.1-08-PLAN.md
@.planning/phases/11.1-quality-hardening/11.1-UAT.md
@.planning/phases/11.1-quality-hardening/11.1-CONTEXT.md
@docs/TESTING.md
@scripts/coverage-e2e-checklist.sh
@apps/web/src/test/render-with-query.ts
@.agents/skills/octane/SKILL.md
</context>

<precondition>Plans 11.1-07 and 11.1-08 SUMMARYs exist (admin LFS/packages/auth render + stack-browser).</precondition>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate → repo filesystem | Script trusts checked-in manifest; malicious skip text is reviewable |
| happy-dom route smokes → mocked APIs | New mounts must mock network like existing admin tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.1-74 | Tampering | route-coverage.manifest.json skip abuse | medium | mitigate | Require non-empty rationale string; CI fails on missing evidence path for non-skip kinds; document policy in TESTING.md |
| T-11.1-75 | Denial of service | mass happy-dom mounts in CI | low | accept | Thin first-paint mounts; reuse renderWithQueryClient; integration project already bounded |
| T-11.1-SC | Tampering | packages | high | mitigate | No new package installs |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (11.1-09-T1): Route coverage manifest + check script</name>
  <files>apps/web/e2e/route-coverage.manifest.json, scripts/check-route-coverage.sh, Makefile</files>
  <read_first>scripts/coverage-e2e-checklist.sh, docs/TESTING.md, apps/web/src/routes</read_first>
  <action>
Per G-11.1-15: create `apps/web/e2e/route-coverage.manifest.json` listing every `apps/web/src/routes/**/*.tsrx` path with `kind` one of `happy-dom` | `stack-browser` | `skip`, plus `evidence` (test file path or suite id) or `rationale` (required when kind=skip). Create `scripts/check-route-coverage.sh` that: (1) discovers all `.tsrx` under `apps/web/src/routes`, (2) requires a manifest entry for each, (3) for `happy-dom` verifies evidence file exists and contains `renderWithQueryClient` (or equivalent mount helper), (4) for `stack-browser` verifies evidence file exists under `e2e/stack-browser/`, (5) for `skip` requires non-empty rationale, (6) exits non-zero on any drift (orphan routes, missing evidence, empty rationale). Add `make check-route-coverage` (or equivalent) invoking the script. Seed the manifest by attributing existing forge/admin stack-browser suites and known happy-dom integration tests to their routes; leave remaining gaps for Task 2. Do not invent Phase 12 routes.
  </action>
  <verify>
    <automated>test -f apps/web/e2e/route-coverage.manifest.json && test -f scripts/check-route-coverage.sh && rg -n 'check-route-coverage' Makefile | rg -v '^#' | rg -q . && bash -n scripts/check-route-coverage.sh</automated>
  </verify>
  <done>Manifest + check script exist; Make target declared; script syntax-valid (G-11.1-15)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (11.1-09-T2): Fill uncovered user-facing routes</name>
  <files>apps/web/e2e/route-coverage.manifest.json, apps/web/src/routes</files>
  <read_first>apps/web/e2e/route-coverage.manifest.json, apps/web/src/routes/admin/lfs.integration.test.ts, apps/web/src/test/render-with-query.ts, apps/web/e2e/stack-browser, .agents/skills/octane/SKILL.md</read_first>
  <behavior>
    - make check-route-coverage exits 0
    - Every user-facing .tsrx has happy-dom, stack-browser, or skip+rationale
    - Skips limited to non-user-facing shells (__root, pure Outlet layouts) with rationale citing child coverage — not used to dodge real pages
  </behavior>
  <action>
Per G-11.1-15: run the check script, then for each uncovered user-facing route either (a) attribute an existing stack-browser flow in the manifest when that suite already navigates the path, (b) add a thin happy-dom integration test that mounts the exported page via `renderWithQueryClient` with mocked APIs (follow admin LFS/auth patterns; Octane skill if editing `.tsrx`), or (c) for true non-user-facing shells only, add `kind: skip` with explicit rationale naming the child evidence. Prefer stack-browser attribution for critical admin/forge paths already covered in plans 07–08 / 11.1-04. Do not invent Phase 12 PR pages. Repeat until `make check-route-coverage` is green.
  </action>
  <verify>
    <automated>make check-route-coverage</automated>
  </verify>
  <done>All user-facing .tsrx routes satisfy the coverage contract; check script exits 0 (G-11.1-15)</done>
</task>

<task type="auto">
  <name>Task 3 (11.1-09-T3): Wire CI gate + document policy</name>
  <files>.github/workflows/ci.yml, docs/TESTING.md, Makefile</files>
  <read_first>.github/workflows/ci.yml, docs/TESTING.md, scripts/check-route-coverage.sh</read_first>
  <action>
Per G-11.1-15 and D-QH-02: wire `make check-route-coverage` into CI (coverage-weighted job or a dedicated fast step that does not require Docker — filesystem gate only). Document in `docs/TESTING.md`: policy (every user-facing `.tsrx` needs happy-dom mount, stack-browser evidence, or skip+rationale), how to update `route-coverage.manifest.json`, and that raw-source-only tests do not count. Confirm gate fails if a new `.tsrx` is added without a manifest entry (executor may temporarily add a throwaway route under `tmp/` is forbidden — instead document the failure mode with a dry-run comment in SUMMARY or by asserting script detects an intentional missing entry in a contract self-test if cheap).
  </action>
  <verify>
    <automated>make check-route-coverage && rg -n 'check-route-coverage|route-coverage' .github/workflows/ci.yml Makefile docs/TESTING.md | rg -v '^#' | rg -q .</automated>
  </verify>
  <done>CI/Make enforce route coverage; TESTING.md documents the policy (G-11.1-15)</done>
</task>

</tasks>

<verification>
make check-route-coverage
rg -n 'check-route-coverage|route-coverage' .github/workflows/ci.yml docs/TESTING.md
</verification>

<success_criteria>
- Full .tsrx route coverage contract exists and is green
- New routes without coverage fail CI
- Policy documented in TESTING.md
</success_criteria>

<output>
Create `.planning/phases/11.1-quality-hardening/11.1-09-SUMMARY.md` when done
</output>
