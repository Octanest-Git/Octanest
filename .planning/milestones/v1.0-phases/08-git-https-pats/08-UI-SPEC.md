---
phase: 8
slug: git-https-pats
status: approved
shadcn_initialized: true
preset: base-nova
created: 2026-09-13
reviewed_at: "2026-09-13T19:02:00Z"
---

# Phase 8 — UI Design Contract

> Visual and interaction contract for Git HTTPS & PATs. Generated by gsd-ui-researcher.
> **EXTENDS** Phase 7 (`07-UI-SPEC.md`) and Phases 3–6 — do **not** reinvent mark, fonts, semantic tokens, spacing scale, typography roles, or global chrome.
> Locked upstream: `08-CONTEXT.md` D-01…D-26; `08-RESEARCH.md` scope catalogs / prefixes / username aliases; requirements GIT-02, GIT-11 (HTTPS git only for PAT auth per D-01).
> Authoring: Octane `.tsrx` (`.agents/skills/octane/SKILL.md`).

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (already initialized — **do not** re-run `shadcn init`) |
| Preset / style | **base-nova** (`apps/web/components.json`) — unchanged |
| Component library | Base UI (shadcn Base UI path via `@octanejs/base-ui`) |
| Icon library | lucide (`@octanejs/lucide`) |
| CSS variables | enabled; **live tokens in `apps/web/src/styles.css`** are source of truth |
| Font display | **Sora Variable** (`--font-display`) — locked Phase 3 |
| Font body / UI | **Source Sans 3 Variable** (`--font-sans`) — locked Phase 3 |
| Variant utility | **CVA** on reusable components when variants apply |

**Standing chrome rule (Phase 3 D-09):** Prefer reusable ShadCN + Base UI components over plain HTML controls.

**Authoring:** Octane `.tsrx` with Rivet directives; TanStack Query for `pat.list` / session; local `useState` for create forms and reveal. Do not mix React-style `return (` with Rivet in the same module.

**Do not reopen:** SSH key UI (Phase 9); PAT as RPC Bearer (D-01); OAuth apps; org-scoped tokens (Phase 10); third-party shadcn registries; new design tokens/fonts.

**Sources:** `08-CONTEXT.md`; `08-RESEARCH.md`; `07-UI-SPEC.md`; `components.json`; scouted `clone-box.tsrx`, `quick-setup.tsrx`, `settings/profile.tsrx`, `chrome.tsrx`, `new.tsrx` verify wall, `styles.css`.

---

## Component Inventory

Enumerated by `ls apps/web/src/components/ui/*.{tsx,tsrx}` + resolved `@octanejs/base-ui` / `@base-ui/react` package.json — **15** local ui wrappers — `@octanejs/base-ui@0.1.52` (primitives `@base-ui/react@1.8.0`) — 2026-09-13.

(`npx shadcn info` is unreliable for this Vite/Octane hand-authored tree; provenance uses the local wrappers directory + resolved package versions.)

Non-exhaustive known-good list (not a closed allowlist) — executor may use any design-system export; checking outside this table is expected.

| Component | Import path | Notes |
|-----------|-------------|-------|
| Button | `@/components/ui/button` | Primary create CTAs; ghost copy; destructive revoke; ≥44px |
| Input | `@/components/ui/input` | Note/name; expiry date; one-time token (read-only) |
| Label | `@/components/ui/label` | Field labels + helpers |
| Textarea | `@/components/ui/textarea` | Unused this phase unless note needs multi-line — prefer single-line Input for note |
| Select | `@/components/ui/select` | Expiry presets optional; FG contents level if not radios |
| Checkbox | `@/components/ui/checkbox` | Classic `repo` scope (required); FG repo multi-select rows |
| Switch | `@/components/ui/switch` | Unused this phase |
| Radio Group | `@/components/ui/radio-group` | Expiry Yes/No; FG repo access `selected` / `all`; FG contents `read` / `write` |
| Dropdown Menu | `@/components/ui/dropdown-menu` | **Generate new token** split; Account menu; Clone box (extend) |
| Skeleton | `@/components/ui/skeleton` | Token list skeleton |
| Badge | `@/components/ui/badge` | Kind Classic / Fine-grained; Expired / Never expires |
| Dialog / AlertDialog | `@/components/ui/dialog`, `@/components/ui/alert-dialog` | Revoke confirm (D-17) — mirror branch delete |
| Toaster | `@/components/ui/toaster` | Optional “Copied” for token reveal — prefer inline Copied like clone box |
| InputOtp | `@/components/ui/input-otp` | Unused this phase |
| AuthShell | `@/components/auth-shell` | Unverified create wall (D-24/D-25) — same pattern as `/new` |
| SiteHeader / SiteFooter | `@/components/chrome` | Add **Personal access tokens** under Account → Profile sibling or tokens nav |
| CloneBox | `@/components/repo/clone-box` | Extend with PAT how-to panel (D-13) |
| QuickSetup | `@/components/repo/quick-setup` | Extend empty-repo guide with PAT how-to (D-13) |

**Phase 8 add actions:** No new shadcn primitives required if AlertDialog / Radio Group / Checkbox / Badge / Dropdown already present. Optional Separator only if useful for settings sections — official registry only. No third-party registries.

---

## Screens in scope

| Route / surface | Purpose | Focal point (eye first) |
|-----------------|---------|-------------------------|
| `/settings/tokens` | List PATs; entry to create flows; revoke | Heading + **Generate new token** + list |
| `/settings/tokens/new` | Classic PAT create form | Note field + scope + Generate token CTA |
| `/settings/tokens/new/fine-grained` | Fine-grained PAT create form | Note + repo access + contents + Generate token CTA |
| Create success (same route post-submit) | One-time plaintext reveal (D-15) | Token value + Copy + dismiss warning |
| Unverified create wall | Block create until email verified (D-24/D-25) | AuthShell + Verify email CTA |
| Settings secondary nav | Profile ↔ Personal access tokens (D-14) | Link-row under settings chrome |
| Account menu | Link to tokens (or settings) | Existing Profile + **Personal access tokens** |
| CloneBox how-to | HTTPS credential contract + create CTA (D-13) | How-to panel inside/beside clone dropdown |
| Quick setup how-to | Empty-repo first push + PAT password (D-13) | Section under HTTPS URL |

**Out of scope UI:** SSH keys/clone (Phase 9); OAuth apps; org tokens; RPC Bearer token docs as product UI; credential-helper installers beyond copy/examples.

---

## Visual hierarchy & layout contracts

### Settings chrome (D-14)

| Rule | Contract |
|------|----------|
| Routes | `/settings/profile` (existing) + `/settings/tokens` (+ create children) |
| Layout | Content column **max-w-2xl** centered; page padding **3xl** vertical / **md** horizontal — match profile |
| Secondary nav | Link-row above page title: **Profile** \| **Personal access tokens** — Label 600 active / 400 inactive; primary bottom border or underline for active (same pattern as repo Code nav) |
| Account menu | Add item **Personal access tokens** → `/settings/tokens` directly under **Profile** |
| Auth | Unauthenticated → redirect `/login?returnTo=/settings/tokens` (profile pattern) |
| Title | Heading **Personal access tokens** |
| Support | One Body muted sentence: Tokens authenticate git over HTTPS. Account passwords are never accepted. |

### Token list `/settings/tokens` (D-09, D-05 entry)

| Element | Contract |
|---------|----------|
| Primary CTAs | Dropdown **Generate new token** with two items: **Classic token**, **Fine-grained token** — OR two adjacent Buttons: primary **Generate classic token**, secondary outline **Generate fine-grained token**. **Lock: dropdown split** (GitHub-familiar; one primary control). |
| Empty | Empty hero: Heading **No personal access tokens** + Body + open Generate dropdown |
| Rows | Card/row on `--card`: **note/name** (Heading-weight Label 600), kind Badge (**Classic** / **Fine-grained**), scope summary (Label muted), expires (date or **No expiration**), **Last used** relative time + IP (or **Never**), created relative, **Revoke token** destructive ghost/outline |
| Prefix hint | Optional Label muted showing truncated prefix only (`ona_pat_…` / `ona_fg_…`) — never full secret |
| Loading | List skeletons (3 rows) — do not flash empty |
| Error | Inline Body destructive + retry |
| Long note | Ellipsis + `title` full note |
| Expired | Badge **Expired**; **Revoke token** still available |

### Classic create `/settings/tokens/new` (D-05, D-07, D-16)

| Element | Contract |
|---------|----------|
| Column | **max-w-2xl**; back link **← Personal access tokens** |
| Title | **New classic token** |
| Note | Required Input label **Note**; helper “What’s this token for? e.g. laptop, CI”; empty submit → inline **Note is required.** |
| Scopes | Required Checkbox **repo** labeled **Full control of private repositories** (research classic catalog — only `repo`); must stay checked (or disable Generate until checked) |
| Expiration | Radio Group: **No expiration** (default) \| **Expires on** + date Input (`type="date"`, min = tomorrow). Optional: Select presets 7/30/60/90 days that set the date field — if presets used, keep radios as source of truth. |
| Primary CTA | **Generate token** (≥44px) |
| Pending | Disabled + **Generating…** |
| Unverified | Do not show form: AuthShell wall (see verify wall) |
| Success | Replace form with one-time reveal (below) — do not navigate away until user dismisses |

### Fine-grained create `/settings/tokens/new/fine-grained` (D-05, D-06, D-07, D-16)

| Element | Contract |
|---------|----------|
| Title | **New fine-grained token** |
| Note | Same required Note as classic |
| Repository access | Radio: **All repositories** (current and future you own) \| **Only select repositories** |
| Selected repos | When selected: checklist of owner’s repos (name + visibility Badge); at least one required or show inline error **Select at least one repository.** Loading: skeleton list. Empty owned-repos: Body “You don’t have any repositories yet.” + link **New repository** |
| Permissions | Radio or Select for **Contents**: **Read-only** \| **Read and write** (maps to `contents` read/write). Helper: Read = clone/fetch; Read and write = push. Metadata read always implied — no separate control (research). |
| Expiration | Same as classic |
| Primary CTA | **Generate token** |
| Success | Same one-time reveal pattern |

### One-time reveal (D-15)

| Element | Contract |
|---------|----------|
| Placement | Same create route after successful mint — full-width alert panel on `--card` with destructive/warning border |
| Warning | Heading **Make sure to copy your personal access token now** + Body **You won’t be able to see it again.** |
| Token | Read-only Input/monospace block showing full `ona_pat_…` or `ona_fg_…` string; select-on-focus |
| Copy | Button **Copy token** (aria-label same); success inline **Copied** (2s) like CloneBox |
| Copy fail | **Couldn’t copy. Select the token and copy manually.** |
| Continue | Primary **Back to tokens** → `/settings/tokens` (list without secret) |
| Never | Do not show plaintext on list, reload, or soft-nav back into create |

### Revoke confirm (D-17)

| Element | Contract |
|---------|----------|
| Trigger | Row **Revoke token** |
| Dialog | AlertDialog (branch-delete pattern) |
| Title | **Revoke token?** |
| Body | Revokes **{note}**. Git clients using this token will stop working. This can’t be undone. |
| Confirm | Destructive **Revoke token** — no type-to-confirm (note is not a secret; lighter than repo delete) |
| Dismiss | **Keep token** (closes dialog; does not revoke) |
| Pending | **Revoking…**; **Keep token** disabled while pending; then close + refresh list |
| Server failure | Inline Body destructive in dialog: **Couldn’t revoke token. Check your connection and try again.** — keep dialog open; confirm stays enabled |

### Verify wall (D-24, D-25)

| Element | Contract |
|---------|----------|
| Surfaces | Classic + fine-grained create routes when `email_verified` false |
| Pattern | AuthShell — mirror `/new` |
| Title | **Verify your email** |
| Body | **Verify your email before creating a personal access token.** |
| CTA | **Verify email** → `/verify` |
| List | Unverified users **may** view empty/list of existing tokens if any; Create CTAs disabled with hint **Verify your email to create a token.** OR hide Generate and show banner — **lock: list visible; Generate disabled + hint** (matches home New repository pattern) |

### CloneBox + Quick setup how-to (D-13)

| Element | Contract |
|---------|----------|
| CloneBox | Below HTTPS URL row (inside dropdown), add expandable or always-visible **Authenticate with a personal access token** panel |
| QuickSetup | After HTTPS URL block, add same how-to section (shared component preferred: `PatHowTo` / `HttpsPatGuide`) |
| Content order | (1) Username: your Oxidean username, or `git`, `token`, or `oauth2`. (2) Password: a personal access token — **not** your account password. (3) Primary link/button **Create a personal access token** → `/settings/tokens`. (4) Example: `git clone https://…` then when prompted, username + PAT. |
| SSH | Keep existing muted SSH placeholder — unchanged |
| Visual | Label headings + Body muted; code examples monospace Label 13–14px in `bg-muted/50` bordered blocks matching QuickSetup |
| Signed-out | How-to still visible on public empty repos; CTA may go to login with `returnTo=/settings/tokens` |

---

## Document titles

| Route | `<title>` |
|-------|-----------|
| `/settings/tokens` | `Personal access tokens · Oxidean` |
| `/settings/tokens/new` | `New classic token · Oxidean` |
| `/settings/tokens/new/fine-grained` | `New fine-grained token · Oxidean` |
| Profile (unchanged) | `Profile · Oxidean` |

Pattern: `Page · Oxidean` (Phase 3 D-21).

---

## Spacing Scale

**Inherited from Phase 3–7** (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps; badge↔title |
| sm | 8px | Compact row padding; how-to stack gaps |
| md | 16px | Default stack; page horizontal padding |
| lg | 24px | Panel inner padding; form field gaps |
| xl | 32px | Title → content |
| 2xl | 48px | Empty hero breaks |
| 3xl | 64px | Page vertical padding |

**Exceptions:**
- Touch targets (Buttons, Selects, menu items, Revoke token): **≥ 44px** height / effective target
- Monospace token/URL blocks: **13px** type OK (inherits Phase 7 code surface exception)
- Settings forms: **max-w-2xl**

---

## Typography

**Inherited — exactly 4 sizes, 2 weights:**

| Role | Size | Weight | Line Height | Font |
|------|------|--------|-------------|------|
| Label | 14px | 400 | 1.4 | Source Sans 3 |
| Body | 16px | 400 | 1.5 | Source Sans 3 |
| Heading | 24px | 600 | 1.2 | Sora |
| Display | 40px | 600 | 1.15 | Sora |

**Weights used:** **400** and **600** only.

**Phase 8 application map:**
- Page titles, empty heroes, reveal warning title → **Heading**
- Support, how-to prose, errors → **Body**
- Nav links, badges, meta (last used, IP), helpers, CTA labels → **Label** (CTA labels weight **600**)
- Token plaintext, clone URLs, example commands → monospace at Label **14px** or **13px** weight **400** (not a fifth typography role)

Do **not** use Display on settings forms.

---

## Color

**Inherit Phase 4–7 / live `styles.css`:**

| Role | Light (current) | Dark (current) | Usage |
|------|-----------------|----------------|-------|
| Dominant (60%) | `--background` `#DFE8F0` | `#07080A` | Page canvas |
| Secondary (30%) | `--card` `#FFFFFF` | `#12151B` | Token rows, reveal panel, how-to blocks, settings panels |
| Primary cool (≤10% with warm) | `#0B6488` | `#4EB8D9` | Generate CTAs, focus rings, active settings nav, Back to tokens |
| Secondary warm | `#C43D0F` | `#FF7A3D` | Header Sign up only — **never** for token CTAs |
| Destructive | `#B42318` | `#FF6B6B` | Revoke token, form errors, reveal urgency border OK via destructive/10 wash |

### Accent reserved for (Phase 8)

**Primary (cool) reserved for:**
1. **Generate token**, **Generate new token** trigger, **Back to tokens** after reveal, **Create a personal access token** how-to CTA
2. Active settings secondary-nav indicator (Profile / Tokens)
3. Focus rings on inputs, dialogs, checkboxes
4. Existing Phase 3–7 reservations (auth CTAs, repo CTAs, Switch on, etc.)

**Secondary (warm) reserved for:** header **Sign up** only when `allow_signup`.

**Destructive reserved for:** **Revoke token**, validation/server errors, reveal “won’t see again” emphasis (text or border — not solid warm accent).

**Not accent:** kind Badges (muted/outline), SSH placeholder, last-used IP text, expired Badge (muted or destructive outline only).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Product name | **Oxidean** |
| Settings nav Profile | Profile |
| Settings nav Tokens | Personal access tokens |
| Account menu Tokens | Personal access tokens |
| List title | Personal access tokens |
| List support | Tokens authenticate git over HTTPS. Your account password is never accepted for git. |
| Primary CTA (list) | **Generate new token** |
| Dropdown Classic | Classic token |
| Dropdown Fine-grained | Fine-grained token |
| Empty heading | No personal access tokens |
| Empty body | Create a token to clone, fetch, and push over HTTPS. |
| Unverified list hint | Verify your email to create a token. |
| Classic title | New classic token |
| Fine-grained title | New fine-grained token |
| Note label | Note |
| Note helper | What’s this token for? For example, laptop or CI. |
| Note required error | Note is required. |
| Classic scope label | Full control of private repositories |
| Classic scope helper | Allows HTTPS clone, fetch, and push for repositories you can access. |
| Expiration label | Expiration |
| Expiration none | No expiration |
| Expiration on | Expires on |
| FG access all | All repositories |
| FG access all helper | Includes all current and future repositories you own. |
| FG access selected | Only select repositories |
| FG select error | Select at least one repository. |
| FG no repos body | You don’t have any repositories yet. |
| FG no repos CTA | **New repository** |
| FG contents label | Contents permission |
| FG contents read | Read-only |
| FG contents write | Read and write |
| FG contents helper | Read-only allows clone and fetch. Read and write also allows push. |
| Generate CTA | **Generate token** |
| Generate pending | Generating… |
| Reveal title | Make sure to copy your personal access token now |
| Reveal body | You won’t be able to see it again. Store it somewhere safe. |
| Copy token | **Copy token** |
| Copy success | Copied |
| Copy fail | Couldn’t copy. Select the token and copy manually. |
| Reveal done | **Back to tokens** |
| Kind Classic badge | Classic |
| Kind FG badge | Fine-grained |
| Expires never | No expiration |
| Expired badge | Expired |
| Last used never | Never |
| Last used row | Last used {relative} from {ip} |
| Revoke row | **Revoke token** |
| Revoke title | Revoke token? |
| Revoke body | Revokes “{note}”. Git clients using this token will stop working. This can’t be undone. |
| Revoke confirm CTA | **Revoke token** |
| Revoke pending | Revoking… |
| Revoke dismiss | **Keep token** |
| Revoke server error | Couldn’t revoke token. Check your connection and try again. |
| Unverified wall title | Verify your email |
| Unverified wall body | Verify your email before creating a personal access token. |
| Unverified wall CTA | **Verify email** |
| How-to heading | Authenticate with a personal access token |
| How-to username | Username: your Oxidean username, or `git`, `token`, or `oauth2`. |
| How-to password | Password: a personal access token — not your account password. |
| How-to CTA | **Create a personal access token** |
| How-to example lead | When Git asks for a password, paste your token. |
| Network error | Can’t reach Oxidean. Check your connection and try again. |
| List load error | Couldn’t load tokens. Refresh and try again. |

---

## UI Considerations

> Shape-rooted UI state coverage for Phase 8. Empty/error **copy** lives in Copywriting — this section covers state shape only.
> Kinds confirmed 2026-09-13: E1 list-collection · E2 form · E3 form+list · E4 interactive-control · E5 interactive-control · E6 nav · E7 static-content · E8 form/gate.
> Probe resolved via existing contract + forge-parity pass (user: match GitHub / forge alternatives as closely as possible). **0 unresolved.**

Applicable: **45** — **38** explicit · **4** backstop · **3** dismissed · **0** unresolved

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | E1 Token list | ✅ covered | Zero tokens → empty hero **No personal access tokens** + **Generate new token** (GitHub Settings → Developer settings empty pattern) |
| loading | E1 Token list | ✅ covered | Three row skeletons; no marketing flash |
| error | E1 Token list | ✅ covered | Inline **Couldn’t load tokens. Refresh and try again.** |
| populated | E1 Token list | ✅ covered | Note, kind Badge (Classic / Fine-grained), scopes, expiry, last used + IP, **Revoke token** |
| partial | E1 Token list | ✅ covered | Never last-used → **Never**; missing IP omits “from {ip}” |
| overflow | E1 Token list | ✅ covered | Long notes ellipsis + `title`; page scrolls |
| zero-one-many | E1 Token list | ✅ covered | Zero = empty hero; one/many = identical row layout (no singular layout fork) |
| long-text | E1 Token list note | 🧪 backstop | `{ statement: "Long PAT notes ellipsis with full title tooltip on list rows", verification: backstop }` |
| empty | E2 Classic create | ✅ covered | Unfilled form; **Generate token** blocked until required note + `repo` checked; **No expiration** default (GitHub classic optional expiry) |
| loading | E2 Classic create | ✅ covered | **Generating…**; CTA disabled |
| error | E2 Classic create | ✅ covered | Inline field/server errors; network retry copy |
| partial | E2 Classic create | ✅ covered | Expiry date field only when **Expires on** selected |
| long-text | E2 Classic note | ✅ covered | Input scrolls; server max length if any |
| empty | E3 FG create | ✅ covered | Note/expiry empty; **Selected repositories** starts with zero repos checked (GitHub FG) |
| loading | E3 FG create | ✅ covered | Repo checklist skeleton; **Generating…** |
| error | E3 FG create | ✅ covered | **Select at least one repository** when selected-mode empty; network/server inline |
| partial | E3 FG create | ✅ covered | **All repositories** hides checklist; **Selected** shows checklist (GitHub FG repo access radios) |
| overflow | E3 FG repo list | ✅ covered | Checklist scrolls; long `owner/name` truncate + `title` |
| zero-one-many | E3 FG repo list | ✅ covered | Zero owned → empty repos body + **New repository** CTA; one/many checkboxes |
| long-text | E3 FG repo names | 🧪 backstop | `{ statement: "Long repository names truncate with title tooltip in FG checklist", verification: backstop }` |
| loading | E4 Reveal | dismissed | Reveal replaces create view synchronously after mint; copy is local clipboard — no async reveal load |
| error | E4 Copy token | ✅ covered | **Couldn’t copy…** + manual select fallback |
| overflow | E4 Token string | ✅ covered | Monospace wraps or mid-ellipsis; full value remains selectable |
| long-text | E4 Token string | ✅ covered | Same as overflow — full opaque string selectable once |
| overflow | E5 Revoke dialog | ✅ covered | Dialog body scrolls if needed; actions stay pinned |
| long-text | E5 Revoke note | 🧪 backstop | `{ statement: "Long token notes wrap in revoke dialog description without clipping CTAs", verification: backstop }` |
| empty | E6 Settings nav | dismissed | Nav is static chrome links (Profile \| Personal access tokens) — not a data collection |
| loading | E6 Settings nav | ✅ covered | Present when settings chrome mounts (no skeleton required) |
| error | E6 Settings nav | dismissed | Static links — no load-failure surface |
| populated | E6 Settings nav | ✅ covered | Active underline/border on **Personal access tokens** when on tokens routes |
| partial | E6 Settings nav | dismissed | Both links always present for signed-in settings |
| overflow | E6 Settings nav | ✅ covered | Short labels; wrap on narrow viewports |
| zero-one-many | E6 Settings nav | dismissed | Fixed two-link row — not a collection |
| long-text | E6 Settings nav | ✅ covered | Labels fixed copy; no truncation needed |
| empty | E7 How-to panel | ✅ covered | Always shows static credential steps (not data-driven empty) |
| loading | E7 How-to panel | dismissed | Static content; no async fetch |
| error | E7 How-to CTA | ✅ covered | CTA navigates to `/settings/tokens` (or login `returnTo`); clone copy errors stay on CloneBox |
| partial | E7 How-to | ✅ covered | SSH remains placeholder until Phase 9; HTTPS how-to fully specified |
| long-text | E7 How-to URLs | 🧪 backstop | `{ statement: "Long clone URLs wrap or overflow-x-auto in how-to code blocks", verification: backstop }` |
| empty | E8 Verify wall | ✅ covered | AuthShell only on create routes when unverified — no create fields |
| loading | E8 Verify wall | ✅ covered | Brief session skeleton then wall / list gate |
| error | E8 Verify wall | ✅ covered | Session error uses network error path before wall |
| populated | E8 List Generate gate | ✅ covered | Unverified: list visible; **Generate new token** disabled + **Verify your email to create a token.** |
| partial | E8 Expiry + gate | ✅ covered | Expiry date hidden when No expiration; Generate disabled while unverified |
| overflow | E8 AuthShell | ✅ covered | Wall content scrolls in viewport; CTA ≥44px |
| zero-one-many | E8 Generate dropdown | ✅ covered | Verified: dropdown items **Classic token** / **Fine-grained token**; unverified: trigger disabled (no empty menu) |
| long-text | E8 Verify hint | ✅ covered | Hint wraps under Generate control |

### Truths for plan-phase lift (explicit)

- Token list empty state shows empty hero and Generate new token entry (GitHub-shaped).
- Token list rows show note, kind, scopes, expiry, last used (+ IP per D-09), and Revoke token.
- Classic and fine-grained are **separate create flows** with distinct routes (GitHub dual model).
- Fine-grained repo access supports **Selected repositories** and **All repositories** (GitHub FG).
- One-time reveal shows plaintext once with Copy token and Back to tokens; secret never reappears in list.
- Revoke uses AlertDialog with **Revoke token** confirm and **Keep token** dismiss (no generic Cancel).
- HTTPS how-to teaches username + PAT-as-password with aliases username / `git` / `token` / `oauth2`.
- Unverified users see list but cannot mint; create routes show AuthShell verify wall.

### Backstop truths (held-out visual)

- `{ statement: "Long PAT notes ellipsis with full title tooltip on list rows", verification: backstop }`
- `{ statement: "Long repository names truncate with title tooltip in FG checklist", verification: backstop }`
- `{ statement: "Long token notes wrap in revoke dialog description without clipping CTAs", verification: backstop }`
- `{ statement: "Long clone URLs wrap or overflow-x-auto in how-to code blocks", verification: backstop }`

### Forge parity (executor checklist) — match GitHub / Forgejo as closely as possible

| Surface | Target contract (GitHub unless noted) | Oxidean lock | Intentional delta only |
|---------|----------------------------------------|---------------|------------------------|
| Settings IA | Developer settings → Personal access tokens | `/settings/tokens` + Profile \| Personal access tokens nav; Account menu item | Nested under account settings (no separate “Developer settings” hub yet) |
| Dual token types | Classic vs Fine-grained, **separate** create UIs | D-05 routes `/new` + `/new/fine-grained`; Generate dropdown split | Same |
| Classic scope | Broad `repo` for private git | Single required **repo** checkbox — Full control of private repositories | No unused classic scopes in Phase 8 UI |
| FG repo access | Only select repositories **or** All repositories | Radios Selected / All (D-06) | Same |
| FG permissions | Contents Read/Read and write (+ metadata implied) | Contents **read** / **write**; metadata auto (research) | No account/org permission matrix until Phase 10 |
| Note / name | Required token description | Required note (D-16) | Same |
| Expiry | Optional expiration | No expiration **or** date (D-07) | Same |
| Reveal | One-time plaintext + copy; never again | D-15; **Back to tokens** | Same |
| List metadata | Name, scopes, last used | + **last-used IP** (D-09) | IP is additive vs GitHub |
| Revoke | Confirm before delete | AlertDialog; **Keep token** / **Revoke token** | Verb+noun dismiss (no Cancel) |
| HTTPS credential | Username + PAT as password; never account password | How-to panel + backend D-10/D-11 | Aliases include Forgejo-friendly `token` / `oauth2` + `git` |
| Token string look | Prefixed opaque secret | `ona_pat_` / `ona_fg_` examples only | **Never** `ghp_` / `github_pat_` / `gho_` (D-08) |
| Clone UX | HTTPS clone URL + auth help | CloneBox + QuickSetup full how-to + CTA (D-13) | SSH placeholder until Phase 9 |
| Email gate | Verified email for privileged actions | Create + push require verified (D-24/D-25) | Aligns Phase 5; GitHub similarly gates some actions |
| SSH keys | Separate settings | Out — Phase 9 | Same deferral |
| OAuth apps | Separate | Out — PLAT-V2-02 remainder | Same |
| Org/FG org tokens | Later | Out — Phase 10+ | Same |
| RPC Bearer PAT | GitHub API tokens work as Bearer | **Out** Phase 8 (D-01) — git HTTPS only | Document in how-to: web stays on session cookies |

**Parity bar for executors:** If a GitHub PAT settings / HTTPS clone help screen has a control or copy pattern for the rows above, implement the Oxidean equivalent unless the Intentional delta column says otherwise. Prefer familiar labels (**Generate new token**, **Classic**, **Fine-grained**, **All repositories**, **No expiration**, one-time reveal warning) over novel IA.

<!-- Status vocabulary: ✅ covered / 🧪 backstop / dismissed / ⚠ unresolved — Rows REPLACED on probe re-run. -->

---

## Motion

Phase 3–7 motions unchanged. Phase 8 adds **at most 2** light motions:

| # | Motion | Spec |
|---|--------|------|
| 1 | AlertDialog enter (revoke) | Opacity + scale 0.98→1, 150–200ms ease-out (reuse Phase 7 dialog) |
| 2 | Generate dropdown open | Library dropdown default only |

**Reduced motion:** dialog enter disabled/instant under `prefers-reduced-motion: reduce`. No confetti on token create; no per-row list stagger.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | existing 15 ui wrappers only (AlertDialog, Radio Group, Checkbox, Badge, Dropdown already in tree) | not required — official / hand-authored Base UI |
| third-party shadcn registries | **none** | n/a |

Do not add `@registry` URLs beyond shadcn official. No new npm UI kits for Phase 8.

---

## Interaction & state matrix (executor checklist)

| Surface | Anonymous | Signed-in unverified | Signed-in verified |
|---------|-----------|----------------------|--------------------|
| `/settings/tokens` | → login | List + disabled Generate + hint | List + Generate dropdown |
| Classic / FG create | → login | AuthShell verify wall | Create forms → reveal |
| Clone how-to CTA | → login?returnTo=/settings/tokens | → `/settings/tokens` (disabled create) | → `/settings/tokens` |
| Quick setup how-to | Same as Clone | Same | Same |
| Revoke | n/a | Allowed for own tokens | Allowed |

**CSRF / cookies:** web RPC uses session cookies (`credentials: "include"`). Smart HTTP never uses cookies (backend D-12) — UI must not imply “signed-in browser = git auth.”

---

## Implementation constraints (executor checklist)

- [ ] Extend Phase 3–7 tokens/fonts/mark/chrome — do not fork a second design system
- [ ] Routes: `/settings/tokens`, `/settings/tokens/new`, `/settings/tokens/new/fine-grained`; settings link-row + Account menu item
- [ ] Two separate create flows (D-05); required note (D-16); optional expiry (D-07)
- [ ] One-time reveal with copy; never re-display secret (D-15)
- [ ] AlertDialog revoke confirm (D-17)
- [ ] List shows last-used timestamp + IP (D-09); prefixes `ona_pat_` / `ona_fg_` only in UX copy/examples
- [ ] Email verified gate for create — AuthShell wall + list Generate disabled (D-24/D-25)
- [ ] CloneBox + QuickSetup full PAT how-to panel + CTA (D-13)
- [ ] Classic scope UI: `repo` only; FG: all/selected repos + contents read/write (research catalogs)
- [ ] Titles `Page · Oxidean`; touch targets ≥ 44px; weights 400/600; spacing multiples of 4 (+ 44px exception)
- [ ] Official shadcn / existing wrappers only; no third-party registries
- [ ] Author `.tsrx` + Query; `make rpc-gen` for `pat.*` — never hand-edit api-client as source of truth

---

## Discretion locks (this phase)

Resolved without re-asking (CONTEXT + RESEARCH + inherited UI-SPEC):

| Item | Lock |
|------|------|
| Design system / spacing / type / color | Inherit Phase 7/6/5/4/3 unchanged |
| Settings IA | `/settings/tokens` + Profile \| Personal access tokens link-row |
| Create entry | Dropdown **Generate new token** → Classic / Fine-grained |
| Create routes | `/settings/tokens/new` and `/settings/tokens/new/fine-grained` |
| Classic scopes UI | Single required `repo` checkbox |
| FG permissions UI | Repo access radios + Contents read/write; no account/org perms |
| Username aliases in how-to | username, `git`, `token`, `oauth2` |
| Token prefixes in UX | `ona_pat_` / `ona_fg_` |
| Revoke confirm | AlertDialog; no type-to-confirm |
| Unverified list | Visible; Generate disabled + hint |
| Primary CTAs | **Generate token** / **Create a personal access token** / **Back to tokens** / **Revoke token** |
| Revoke dismiss | **Keep token** (not Cancel) |

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS
- [x] Dimension 7 Inventory Provenance: PASS

**Approval:** approved 2026-09-13 (revision: Keep token; UI Considerations probe + forge-parity pass)
