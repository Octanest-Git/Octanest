---
phase: 08-git-https-pats
plan: "02"
type: execute
wave: 1
depends_on:
  - "00"
files_modified:
  - .planning/phases/08-git-https-pats/08-DISCUSSION-LOG.md
requirements:
  - GIT-02
  - GIT-11
autonomous: false
user_setup: []
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "Human confirmed one-way doors for token prefixes (D-08) and public .git HTTPS URL scheme (D-18) before tracer implements them"
    - "Human confirmed git private unauth → 401+WWW-Authenticate (D-21) diverges from web 404 anti-enumeration"
    - "Phase 8 interprets GIT-11 RPC/API parenthetical as HTTPS-git-only PATs per D-01 (no Bearer for typed RPC)"
  artifacts:
    - path: .planning/phases/08-git-https-pats/08-DISCUSSION-LOG.md
      provides: "Recorded checkpoint outcomes for D-08/D-18/D-21"
  key_links:
    - from: .planning/phases/08-git-https-pats/08-CONTEXT.md
      to: .planning/phases/08-git-https-pats/08-DISCUSSION-LOG.md
      via: "Reversibility gate before 08-03/08-04"
  assumptions:
    - "ASSUME (GIT-11 adjacency): reserved Basic aliases git/token/oauth2 will be added to RESERVED_USERNAMES so alias usernames cannot register as accounts"
  prohibitions:
    - statement: "Must not mint tokens with github/ghp_/github_pat_/gho_ prefixes"
      flagged: true
    - statement: "Must not authenticate typed RPC with PAT Bearer in Phase 8"
      flagged: true
    - statement: "Must not serve Smart HTTP on bare /{owner}/{repo} without .git suffix"
      flagged: true
---

<objective>
Lock one-way/costly public contracts for PAT string format, HTTPS clone URL shape, and git-vs-web error semantics before schema and tracer implement them.

Purpose: REVERSIBILITY_GATES — undo after ship means secret-scanning hooks, shared clone URLs, and client credential behavior.
Output: Confirmed option IDs recorded; executor may proceed to 08-03 schema and 08-04 tracer.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-git-https-pats/08-CONTEXT.md
@.planning/phases/08-git-https-pats/08-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Published token format → secret scanners / docs | Prefix is a public contract (D-08 one-way) |
| Clone URL scheme → every client remote | .git URL shape is one-way (D-18) |
| Git status codes → credential helpers | 401 vs 404 changes client retry behavior (D-21) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Spoofing | token prefixes | high | mitigate | Human locks Octanest-only ona_pat_/ona_fg_ (D-08); never gh* |
| T-08-05 | Information disclosure | git 401 vs web 404 | medium | mitigate | Human confirms D-21 intentional split before Smart HTTP maps statuses |
| T-08-SC | Tampering | packages | low | accept | No package installs in this plan |
</threat_model>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1 (08-02-T1): Confirm PAT prefixes ona_pat_ / ona_fg_ (D-08 one-way)</name>
  <decision>
Classic tokens use prefix `ona_pat_` and fine-grained tokens use `ona_fg_`, followed by CSPRNG hex (32+ bytes). Never `ghp_`, `github_pat_`, `gho_`, or any github/gh* prefix (D-08).
  </decision>
  <context>
Rated one-way in CONTEXT: published token format and secret-scanning hooks. Tracer (08-04) and UI reveal copy will mint these prefixes. Undo after ship breaks every issued token and external scanners.
  </context>
  <options>
    <option id="ona_prefixes">
      <title>ona_pat_ / ona_fg_ (recommended — D-08 / RESEARCH)</title>
      <pros>Octanest-branded; distinct classic vs FG; avoids GitHub impersonation</pros>
      <cons>Custom scanner rules needed later</cons>
    </option>
    <option id="stop">
      <title>Stop — revisit prefix design</title>
      <pros>Safe if product wants different branding</pros>
      <cons>Blocks schema/tracer</cons>
    </option>
  </options>
  <resume-signal>Reply with ona_prefixes (recommended) or stop</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2 (08-02-T2): Confirm HTTPS clone URL https://{host}/{owner}/{repo}.git (D-18 one-way)</name>
  <decision>
Public clone URL shape is `https://{host}/{owner}/{repo}.git` on the same public origin; host from `OCTANEST_PUBLIC_ORIGIN` (D-18, D-19). Smart HTTP served only on `/{owner}/{repo}.git`; bare `/{owner}/{repo}` remains web UI (D-22).
  </decision>
  <context>
Rated one-way: every shared remote and docs example. Phase 7 already displays this URL via httpsCloneUrl — Phase 8 makes it a live git endpoint. Changing later requires redirects for every remote.
  </context>
  <options>
    <option id="owner_repo_git">
      <title>/{owner}/{repo}.git on public origin (recommended — D-18)</title>
      <pros>Forge parity; matches Phase 7 clone box; Traefik PathRegexp ready</pros>
      <cons>Requires Traefik priority rule so SPA does not steal .git</cons>
    </option>
    <option id="stop">
      <title>Stop — revisit URL scheme</title>
      <pros>Safe if product wants /git/ prefix forever</pros>
      <cons>Blocks Smart HTTP tracer and Traefik plan</cons>
    </option>
  </options>
  <resume-signal>Reply with owner_repo_git (recommended) or stop</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3 (08-02-T3): Confirm git private unauth → 401 (D-21 costly) + PAT-not-RPC (D-01)</name>
  <decision>
Unauthenticated git access to private/no-access repos returns 401 + WWW-Authenticate Basic (not web UI repo.not_found 404). PATs authenticate HTTPS git only in Phase 8 — not typed RPC Bearer; web stays on session cookies (D-01, D-21). GIT-11 ROADMAP parenthetical “RPC/API where applicable” is interpreted as HTTPS-git-only for Phase 8.
  </decision>
  <context>
Costly credential/error contract: git clients expect 401 challenges; web anti-enumeration stays 404. Mixing them confuses both surfaces. D-01 credential-class split is costly to reverse once clients assume PAT≠API key.
  </context>
  <options>
    <option id="git_401_pat_https_only">
      <title>401 for git private + PAT HTTPS-only (recommended)</title>
      <pros>Matches CONTEXT D-01/D-21 + git-scm Basic guidance</pros>
      <cons>Git and web differ by design — must document</cons>
    </option>
    <option id="stop">
      <title>Stop — revisit error/auth class</title>
      <pros>Safe if product wants identical 404 for git</pros>
      <cons>Blocks Smart HTTP status mapping</cons>
    </option>
  </options>
  <resume-signal>Reply with git_401_pat_https_only (recommended) or stop</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| Checkpoint outcomes in DISCUSSION-LOG | 08-02 | D-08 / D-18 / D-21 / D-01 |
| Proceed signal for schema + tracer | 08-02 | Gates 08-03 / 08-04 |

<verification>
DISCUSSION-LOG (or SUMMARY) records ona_prefixes, owner_repo_git, and git_401_pat_https_only (or stop).
</verification>

<success_criteria>
One-way/costly doors confirmed before 08-03 implements schema and 08-04 implements mint + Smart HTTP.
</success_criteria>

<output>
Create `.planning/phases/08-git-https-pats/08-02-SUMMARY.md` when done; append checkpoint replies to 08-DISCUSSION-LOG.md
</output>
