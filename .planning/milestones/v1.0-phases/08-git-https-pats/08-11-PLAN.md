---
phase: 08-git-https-pats
plan: "11"
type: execute
wave: 6
depends_on:
  - "09"
  - "10"
files_modified:
  - apps/web/src/routes/settings/tokens.new.fine-grained.tsrx
  - apps/web/src/components/settings/pat-fg-form.tsrx
  - apps/web/src/routes/settings/tokens.integration.test.ts
requirements:
  - GIT-11
autonomous: true
user_setup: []
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Fine-grained repo access supports Selected repositories and All repositories (GitHub FG) (D-04, D-06)"
    - "Classic and fine-grained are separate create flows with distinct routes (GitHub dual model)"
    - "Contents permission Read-only / Read and write matches forge-parity FG git scopes (D-04)"
    - "One-time reveal reused for FG tokens (ona_fg_)"
    - statement: "Long repository names truncate with title tooltip in FG checklist"
      verification: backstop
  artifacts:
    - path: apps/web/src/routes/settings/tokens.new.fine-grained.tsrx
      provides: "/settings/tokens/new/fine-grained"
    - path: apps/web/src/components/settings/pat-fg-form.tsrx
      provides: "FG create form"
  key_links:
    - from: apps/web/src/components/settings/pat-fg-form.tsrx
      to: packages/api-client/src/index.ts
      via: "pat.createFineGrained + repo.listMine"
  assumptions:
    - "ASSUME: repo.listMine (or equivalent Phase 7 list) supplies owned repos for FG checklist"
  prohibitions:
    - statement: "Must not add account/org FG permission matrix UI"
      flagged: true
    - statement: "Must not mix return ( JSX with Rivet @{ in same .tsrx"
      flagged: true
---

<objective>
Ship fine-grained PAT create at `/settings/tokens/new/fine-grained` with All/Selected repos, contents read/write, verify wall, and shared one-time reveal.

Purpose: Complete D-04/D-05/D-06 UI dual model (forge-parity FG scopes).
Output: Green FG create cases; build OK.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-git-https-pats/08-UI-SPEC.md
@.agents/skills/octane/SKILL.md
@apps/web/src/components/settings/pat-reveal.tsrx
@apps/web/src/routes/settings/tokens.new.tsrx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FG form → createFineGrained | Selected repo IDs must come from user’s listMine only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06 | Elevation of privilege | selected repos UI | medium | mitigate | Only show caller-owned repos; server re-validates ownership (08-05) |
| T-08-01 | Information disclosure | FG reveal | high | mitigate | Reuse pat-reveal; no list secret |
| T-08-SC | Tampering | packages | low | accept | Existing Radio/Checkbox/Badge only |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (08-11-T1): Fine-grained create form + reveal reuse</name>
  <files>apps/web/src/routes/settings/tokens.new.fine-grained.tsrx, apps/web/src/components/settings/pat-fg-form.tsrx, apps/web/src/routes/settings/tokens.integration.test.ts</files>
  <read_first>.agents/skills/octane/SKILL.md, .planning/phases/08-git-https-pats/08-UI-SPEC.md, apps/web/src/components/settings/pat-classic-form.tsrx, apps/web/src/components/settings/pat-reveal.tsrx</read_first>
  <behavior>
    - title New fine-grained token; All repositories / Only select repositories; Contents Read-only / Read and write
    - selected empty submit → Select at least one repository.
    - zero owned repos → You don’t have any repositories yet. + New repository link
    - success reuses reveal; unverified AuthShell
  </behavior>
  <action>
Add /settings/tokens/new/fine-grained route. Form: note, repo access radios, checklist from listMine with visibility Badge and title tooltip truncate (backstop), contents radios with helper copy, expiry same as classic, Generate → pat.createFineGrained. Reuse pat-reveal component. Verify wall same as classic. head title New fine-grained token · Oxidean. Green FG integration assertions.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/settings/tokens.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>FG assertions fail; or build fails; or createFineGrained not called from form</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'New fine-grained token|All repositories|Only select repositories|Contents permission|createFineGrained' apps/web/src/routes/settings/ apps/web/src/components/settings/`
    - `rg -n 'Select at least one repository|Read and write' apps/web/src/components/settings/pat-fg-form.tsrx`
  </acceptance_criteria>
  <done>FG create UI green</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `/settings/tokens/new/fine-grained` | 08-11 | D-05/D-06 |

<verification>
FG create UI tests green; full tokens.integration.test suite green.
</verification>

<success_criteria>
User can mint fine-grained PATs with selected/all repos in the UI.
</success_criteria>

<output>
Create `.planning/phases/08-git-https-pats/08-11-SUMMARY.md` when done
</output>
