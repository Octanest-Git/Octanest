---
phase: 08-git-https-pats
plan: "06"
type: execute
wave: 4
depends_on:
  - "04"
  - "05"
files_modified:
  - crates/oxidean-api/src/routes/git_smart_http.rs
  - crates/oxidean-api/src/git/http_backend.rs
  - crates/oxidean-api/src/pat/mod.rs
  - crates/oxidean-api/src/pat/rate_limit.rs
  - crates/oxidean-api/src/repo/acl.rs
  - crates/oxidean-api/tests/git_smart_http.rs
requirements:
  - GIT-02
autonomous: true
user_setup: []
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Private/no-access unauthenticated git → 401 + WWW-Authenticate (D-21); insufficient PAT scope → 403 (D-23)"
    - "receive-pack / push always requires PAT + email verified; classic repo or FG contents write required (D-20, D-24)"
    - "Failed Basic/PAT auth rate-limited 20/IP and 10/user per 15m → 429 + Retry-After (D-26)"
    - "Successful auth updates last_used_at and last_used_ip (D-09)"
  artifacts:
    - path: crates/oxidean-api/src/pat/rate_limit.rs
      provides: "In-memory failed-auth counters"
    - path: crates/oxidean-api/src/routes/git_smart_http.rs
      provides: "Full ACL/status matrix + push path"
  key_links:
    - from: crates/oxidean-api/src/routes/git_smart_http.rs
      to: crates/oxidean-api/src/repo/acl.rs
      via: "Shared ACL decision; git status mapping"
  assumptions:
    - "ASSUME (GIT-02 concurrency): in-memory rate limiter is per API process (Compose single replica); multi-replica deferred"
    - "ASSUME (Open Q2): unverified may fetch with PAT; push denied with auth.email_unverified semantics"
    - "ASSUME (A5): private + authenticated non-owner → 401 (not 404) for git"
  prohibitions:
    - statement: "Must not return 401 for insufficient scope (must be 403)"
      flagged: true
    - statement: "Must not set http.receivepack true for anonymous"
      flagged: true
    - statement: "Must not trust unvalidated X-Forwarded-For beyond existing Traefik peer pattern"
      flagged: true
---

<objective>
Expand Smart HTTP to full GIT-02 matrix: private 401, push with PAT, scope 403, rate limits, unverified push deny, last-used touch — after tracer public fetch is green.

Purpose: Complete HTTPS git auth/ACL before Traefik e2e and UI.
Output: All git_smart nextest cases green (including push subprocess where harness supports it).
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-git-https-pats/08-RESEARCH.md
@.planning/phases/08-git-https-pats/08-04-SUMMARY.md
@.planning/phases/08-git-https-pats/08-05-SUMMARY.md
@crates/oxidean-api/src/routes/git_smart_http.rs
@crates/oxidean-api/src/repo/acl.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unauth client → private git | Must challenge 401 without existence-leaking 200 |
| Read-only PAT → receive-pack | Must 403 not re-prompt forever |
| Abusive client → Basic | Rate limit before brute-force PAT hashes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02 | Elevation of privilege | receive-pack | high | mitigate | Require PAT + verified email + write scope before CGI (D-20, D-23, D-24) |
| T-08-05 | Information disclosure | private git | medium | mitigate | 401 for unauth private; do not map to web not_found (D-21) |
| T-08-08 | Denial of service | failed auth | high | mitigate | 20/IP + 10/user per 15m → 429 + Retry-After (D-26) |
| T-08-SC | Tampering | packages | low | accept | No new rate-limit crates |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (08-06-T1): ACL status matrix + scope checks + last_used</name>
  <files>crates/oxidean-api/src/routes/git_smart_http.rs, crates/oxidean-api/src/repo/acl.rs, crates/oxidean-api/src/pat/mod.rs, crates/oxidean-api/tests/git_smart_http.rs</files>
  <read_first>crates/oxidean-api/src/routes/git_smart_http.rs, crates/oxidean-api/src/repo/acl.rs, .planning/phases/08-git-https-pats/08-RESEARCH.md</read_first>
  <behavior>
    - private anon upload-pack → 401 + WWW-Authenticate
    - FG contents read cannot receive-pack → 403
    - classic repo PAT can push when verified
    - successful auth calls touch_pat_last_used with IP
  </behavior>
  <action>
Extract shared can_read/is_owner decisions from acl.rs without changing web not_found mapping. Map git failures: private/no-access unauth → 401; authenticated insufficient scope → 403; receive-pack requires PAT always. Enforce classic scope repo for fetch+push; FG: selected must include repo id OR repo_access=all AND owner; contents read → upload-pack only; write → both. On success touch last_used_at/ip. Green corresponding git_smart tests; implement push via CGI receive-pack + optional git push subprocess against test listener (mirror repo_create pattern).
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(git_smart)'</automated>
    <fails_when>exit code != 0 for ACL/scope/push tests; or insufficient scope returns 401</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n '403|WWW-Authenticate|touch_pat_last_used|receive-pack' crates/oxidean-api/src/routes/git_smart_http.rs`
    - `rg -n 'contents|repo_access|insufficient|scope' crates/oxidean-api/src/routes/git_smart_http.rs crates/oxidean-api/src/pat/`
  </acceptance_criteria>
  <done>ACL/scope/push matrix green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (08-06-T2): Failed-auth rate limit + unverified push deny</name>
  <files>crates/oxidean-api/src/pat/rate_limit.rs, crates/oxidean-api/src/routes/git_smart_http.rs, crates/oxidean-api/tests/git_smart_http.rs</files>
  <read_first>crates/oxidean-api/src/auth/verify_reset.rs, crates/oxidean-api/src/routes/git_smart_http.rs, .planning/phases/08-git-https-pats/08-RESEARCH.md</read_first>
  <behavior>
    - after 20 failed attempts from same IP within 15m → 429 with Retry-After header
    - after 10 failed attempts mapped to same user within 15m → 429
    - successful auth clears user bucket only
    - unverified user push → denied (auth.email_unverified semantics / non-CGI)
  </behavior>
  <action>
Add in-memory rate_limit helper (no new crate): keys by IP and optional user_id; windows 15 minutes; thresholds 20/IP and 10/user (D-26). On exceed return 429 + Retry-After seconds. Wire into Smart HTTP Basic failure path before expensive work. Check email_verified_at before receive-pack; allow upload-pack for unverified with valid PAT. Green remaining git_smart tests.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(git_smart)'</automated>
    <fails_when>exit code != 0; or 429 missing Retry-After; or unverified push succeeds</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f crates/oxidean-api/src/pat/rate_limit.rs`
    - `rg -n 'Retry-After|429|15' crates/oxidean-api/src/pat/rate_limit.rs crates/oxidean-api/src/routes/git_smart_http.rs`
    - `rg -n 'email_verified|email_unverified' crates/oxidean-api/src/routes/git_smart_http.rs`
  </acceptance_criteria>
  <done>Rate limit + verify push gate green</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| Full Smart HTTP ACL/push | 08-06 | GIT-02 |
| `pat/rate_limit.rs` | 08-06 | D-26 |

<verification>
All git_smart tests green.
</verification>

<success_criteria>
Clone/fetch/push over HTTPS with PAT works under full auth matrix; passwords and cookies still rejected.
</success_criteria>

<output>
Create `.planning/phases/08-git-https-pats/08-06-SUMMARY.md` when done
</output>
