---
phase: 08-git-https-pats
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/oxidean-api/tests/pat_rpc.rs
  - crates/oxidean-api/tests/git_smart_http.rs
  - crates/oxidean-db/tests/dialect_pats.rs
requirements:
  - GIT-02
  - GIT-11
autonomous: true
user_setup: []
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Wave 0 failing stubs exist for pat.createClassic/list/revoke, email_unverified create, and git Smart HTTP auth/ACL status codes (GIT-02, GIT-11)"
    - "dialect_pats stub expects tri-dialect 0008_pats (+ personal_access_tokens / personal_access_token_repos)"
  artifacts:
    - path: crates/oxidean-api/tests/pat_rpc.rs
      provides: "GIT-11 create/list/revoke failing integration stubs"
    - path: crates/oxidean-api/tests/git_smart_http.rs
      provides: "GIT-02 Smart HTTP failing integration stubs"
    - path: crates/oxidean-db/tests/dialect_pats.rs
      provides: "0008_pats migration parity stub"
  key_links:
    - from: .planning/phases/08-git-https-pats/08-VALIDATION.md
      to: crates/oxidean-api/tests/pat_rpc.rs
      via: "Wave 0 Requirements checklist"
  assumptions:
    - "ASSUME (GIT-02 concurrency): Smart HTTP requests are independent; failed-auth counters are process-local; concurrent push/fetch on one bare repo may serialize via git locks without corrupting refs"
    - "ASSUME (GIT-11 adjacency): identical notes allowed across tokens; uniqueness is token_hash only — equal note strings do not merge or collide"
    - "ASSUME (GIT-11 empty): empty/whitespace note → pat.note_required; FG selected with zero repos → validation error; null/omit expires_at means no expiration"
    - "ASSUME (GIT-11 ordering): pat.list returns active tokens ordered by created_at DESC (stable for equal timestamps by id ASC)"
    - "ASSUME (GIT-11 idempotency): each create mints a new token; revoke of already-revoked or unknown id → pat.not_found (not silent success overwrite)"
    - "ASSUME (GIT-11 concurrency): concurrent creates for one user all succeed; concurrent revoke same id → one success, other pat.not_found"
  prohibitions:
    - statement: "Must not accept account passwords for git HTTPS authentication"
      flagged: true
    - statement: "Must not store PAT plaintext at rest (hash only after create)"
      flagged: true
    - statement: "Must not use session cookies to authenticate Smart HTTP"
      flagged: true
    - statement: "Must not implement production PAT RPC or Smart HTTP handlers in this Wave 0 plan"
      flagged: true
---

<objective>
Scaffold Nyquist/Wave 0 failing Rust stubs for Phase 8 PAT RPC and Git Smart HTTP before implementation plans turn them green.

Purpose: Every later `<automated>` verify names a real test path (08-VALIDATION.md Wave 0). Web stubs live in 08-01.
Output: Discoverable RED (or intentionally failing) `pat_*` / `git_smart*` / dialect stubs matching proven nextest filters.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-git-https-pats/08-CONTEXT.md
@.planning/phases/08-git-https-pats/08-RESEARCH.md
@.planning/phases/08-git-https-pats/08-VALIDATION.md
@.planning/phases/08-git-https-pats/08-PATTERNS.md
@crates/oxidean-api/tests/repo_create.rs
@crates/oxidean-api/tests/auth_verify_gate.rs
@crates/oxidean-db/tests/dialect_repositories.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → temp DB / repos_dir | Parallel nextest must isolate state |
| Stub assertions → production auth | Password reject, cookie ignore, 401/403/429 encoded before impl |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information disclosure | pat_rpc stubs | high | mitigate | Stub asserts list never returns plaintext token; create returns token once (D-15) |
| T-08-02 | Elevation of privilege | git_smart_http stubs | high | mitigate | Stub asserts password reject + cookie ignore + private 401 (D-11, D-12, D-21) |
| T-08-SC | Tampering | cargo/npm installs | high | mitigate | Wave 0 adds test files only; no new crates.io/npm packages |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (08-00-T1): PAT RPC + Smart HTTP + dialect Wave 0 stubs</name>
  <files>crates/oxidean-api/tests/pat_rpc.rs, crates/oxidean-api/tests/git_smart_http.rs, crates/oxidean-db/tests/dialect_pats.rs</files>
  <read_first>.planning/phases/08-git-https-pats/08-VALIDATION.md, .planning/phases/08-git-https-pats/08-RESEARCH.md, crates/oxidean-api/tests/repo_create.rs, crates/oxidean-api/tests/auth_verify_gate.rs, crates/oxidean-db/tests/dialect_repositories.rs</read_first>
  <action>
Add Wave 0 integration stubs (Phase 06/07 style: compile, discoverable by nextest, intentionally fail or assert!(false, "Wave 0") / #[ignore] until later plans). pat_rpc.rs must name tests matching filter test(pat_) covering: verified createClassic returns one-time token field; list omits secret; revoke removes from list; unverified create → auth.email_unverified; empty note → pat.note_required (GIT-11, D-15, D-16, D-24). git_smart_http.rs must name tests matching test(git_smart) covering: public anon upload-pack info/refs allowed; private anon → 401 + WWW-Authenticate; Basic with account password → 401 + PAT hint; session cookie alone treated as anon; insufficient scope → 403; failed-auth over limit → 429 + Retry-After; unverified push denied; PAT push/fetch happy-path stub (GIT-02, D-11, D-12, D-20, D-21, D-23, D-26). dialect_pats.rs expects tri-dialect 0008_pats with personal_access_tokens and personal_access_token_repos (mirror dialect_repositories). Do not implement pat handlers, Smart HTTP routes, or migrations here.
  </action>
  <verify>
    <automated>test -f crates/oxidean-api/tests/pat_rpc.rs && test -f crates/oxidean-api/tests/git_smart_http.rs && test -f crates/oxidean-db/tests/dialect_pats.rs && cargo nextest list -p oxidean-api -E 'test(pat_) | test(git_smart)' | rg -q 'pat_|git_smart' && cargo nextest list -p oxidean-db -E 'test(dialect_pats)' | rg -q 'dialect_pats'</automated>
    <fails_when>any listed stub path missing; or nextest list finds zero pat_/git_smart/dialect_pats tests</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f crates/oxidean-api/tests/pat_rpc.rs && test -f crates/oxidean-api/tests/git_smart_http.rs && test -f crates/oxidean-db/tests/dialect_pats.rs`
    - `rg -n 'email_unverified|createClassic|pat.note_required|token' crates/oxidean-api/tests/pat_rpc.rs`
    - `rg -n 'WWW-Authenticate|401|403|429|password|cookie' crates/oxidean-api/tests/git_smart_http.rs`
    - `rg -n '0008_pats|personal_access_tokens' crates/oxidean-db/tests/dialect_pats.rs`
  </acceptance_criteria>
  <done>Rust Wave 0 stubs exist; filters discover RED/ignored tests</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `pat_rpc.rs` / `git_smart_http.rs` stubs | 08-00 | Nyquist GIT-11 / GIT-02 |
| `dialect_pats.rs` stub | 08-00 | Schema gate precursor |
| Web Vitest stubs | 08-01 | Parallel Wave 0 |

<verification>
Wave 0 Rust stubs exist; VALIDATION checklist items for stubs addressable by later plans.
</verification>

<success_criteria>
Phase 8 Rust requirement IDs have failing automated stub targets before green implementation (web stubs in 08-01).
</success_criteria>

<output>
Create `.planning/phases/08-git-https-pats/08-00-SUMMARY.md` when done
</output>
