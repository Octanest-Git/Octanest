---
phase: 08-git-https-pats
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/src/routes/settings/tokens.integration.test.ts
  - apps/web/src/components/repo/clone-box.pat.integration.test.ts
requirements:
  - GIT-02
  - GIT-11
autonomous: true
user_setup: []
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Wave 0 web stubs exist for /settings/tokens list/create/revoke and CloneBox PAT how-to panel (D-13, D-14, GIT-02, GIT-11)"
    - "Token list empty state shows empty hero and Generate new token entry (GitHub-shaped)"
    - "HTTPS how-to teaches username + PAT-as-password with aliases username / git / token / oauth2"
  artifacts:
    - path: apps/web/src/routes/settings/tokens.integration.test.ts
      provides: "GIT-11 settings tokens UI failing stubs"
    - path: apps/web/src/components/repo/clone-box.pat.integration.test.ts
      provides: "GIT-02 CloneBox how-to failing stubs"
  key_links:
    - from: apps/web/src/routes/settings/tokens.integration.test.ts
      to: apps/web/src/routes/settings/tokens.tsrx
      via: "Wave 0 stub greened by 08-09/08-10"
  assumptions: []
  prohibitions:
    - statement: "Must not implement production /settings/tokens or CloneBox how-to routes in this plan"
      flagged: true
    - statement: "Must not mix React return ( JSX with Rivet @{ in the same .tsrx module when later greening"
      flagged: true
---

<objective>
Wave 0 web integration stubs for `/settings/tokens` and CloneBox PAT how-to (parallel with 08-00 Rust stubs).

Purpose: Keep Rust Wave 0 and web Wave 0 parallel with thin file budgets.
Output: Discoverable Vitest stubs for D-13/D-14/D-15/D-17 UI paths.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-git-https-pats/08-UI-SPEC.md
@.planning/phases/08-git-https-pats/08-CONTEXT.md
@.agents/skills/octane/SKILL.md
@apps/web/src/routes/new.integration.test.ts
@apps/web/src/components/repo/clone-box.tsrx
@apps/web/src/routes/settings/profile.tsrx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vitest harness → route stubs | Stubs encode verify-wall and one-time reveal before production routes exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03 | Elevation of privilege | tokens stubs | medium | mitigate | Stub asserts unverified Generate disabled + AuthShell on create (D-24/D-25) |
| T-08-01 | Information disclosure | reveal stubs | high | mitigate | Stub asserts plaintext never asserted on list surface |
| T-08-SC | Tampering | packages | low | accept | No new packages; vitest stubs only |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (08-01-T1): Web Wave 0 stubs for tokens settings and CloneBox how-to</name>
  <files>apps/web/src/routes/settings/tokens.integration.test.ts, apps/web/src/components/repo/clone-box.pat.integration.test.ts</files>
  <read_first>apps/web/src/routes/new.integration.test.ts, apps/web/src/routes/settings/profile.tsrx, apps/web/src/components/repo/clone-box.tsrx, .planning/phases/08-git-https-pats/08-UI-SPEC.md, .agents/skills/octane/SKILL.md</read_first>
  <action>
Add Vitest integration stubs (Phase 06/07 pattern; @vite-ignore dynamic import if route absent). tokens.integration.test.ts must assert discoverable expectations for: list title Personal access tokens; empty hero No personal access tokens; Generate new token dropdown Classic token / Fine-grained token; unverified list visible with Generate disabled + Verify your email to create a token.; revoke AlertDialog copy Revoke token? / Keep token; one-time reveal Make sure to copy your personal access token now (GIT-11, UI-SPEC). clone-box.pat.integration.test.ts must assert Authenticate with a personal access token how-to with username aliases (git/token/oauth2), password=PAT not account password, and Create a personal access token CTA toward /settings/tokens (D-13, GIT-02). Tests may RED until production .tsrx exists. Do not implement production routes here (08-09–08-12).
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/settings/tokens.integration.test.ts src/components/repo/clone-box.pat.integration.test.ts</automated>
    <fails_when>vitest cannot find either stub file; or exit 0 with all green while production routes still absent (Wave 0 expects RED until later plans)</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/routes/settings/tokens.integration.test.ts`
    - `test -f apps/web/src/components/repo/clone-box.pat.integration.test.ts`
    - `rg -n 'Personal access tokens|Generate new token|Keep token' apps/web/src/routes/settings/tokens.integration.test.ts`
    - `rg -n 'Authenticate with a personal access token|oauth2|/settings/tokens' apps/web/src/components/repo/clone-box.pat.integration.test.ts`
  </acceptance_criteria>
  <done>Web Wave 0 stubs discoverable by vitest</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `tokens.integration.test.ts` | 08-01 | GIT-11 UI Nyquist |
| `clone-box.pat.integration.test.ts` | 08-01 | GIT-02 how-to Nyquist |

<verification>
Web Wave 0 stubs exist and are runnable by vitest discover.
</verification>

<success_criteria>
UI requirement surfaces have failing automated stub targets before green implementation.
</success_criteria>

<output>
Create `.planning/phases/08-git-https-pats/08-01-SUMMARY.md` when done
</output>
