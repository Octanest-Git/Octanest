---
phase: 08-git-https-pats
plan: "12"
type: execute
wave: 7
depends_on:
  - "09"
  - "01"
files_modified:
  - apps/web/src/components/repo/pat-how-to.tsrx
  - apps/web/src/components/repo/clone-box.tsrx
  - apps/web/src/components/repo/quick-setup.tsrx
  - apps/web/src/components/repo/clone-box.pat.integration.test.ts
requirements:
  - GIT-02
autonomous: true
user_setup: []
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 1
  confidence: medium
must_haves:
  truths:
    - "HTTPS how-to teaches username + PAT-as-password with aliases username / git / token / oauth2"
    - "CloneBox and QuickSetup include Create a personal access token CTA to /settings/tokens (D-13)"
    - "Dual remotes: HTTPS + PAT is the Phase 8 path; SSH remains muted placeholder until Phase 9 (D-02)"
    - statement: "Long clone URLs wrap or overflow-x-auto in how-to code blocks"
      verification: backstop
  artifacts:
    - path: apps/web/src/components/repo/pat-how-to.tsrx
      provides: "Shared HttpsPatGuide / PatHowTo"
    - path: apps/web/src/components/repo/clone-box.tsrx
      provides: "Extended how-to panel"
  key_links:
    - from: apps/web/src/components/repo/clone-box.tsrx
      to: apps/web/src/components/repo/pat-how-to.tsrx
      via: "D-13 how-to embed"
  assumptions: []
  prohibitions:
    - statement: "Must not imply signed-in browser session authenticates git"
      flagged: true
    - statement: "Must not change SSH placeholder into real SSH UI (Phase 9) — D-02 locks HTTPS+PAT now / SSH later"
      flagged: true
---

<objective>
Add shared PAT how-to panel to CloneBox and QuickSetup so empty-repo / clone UX teaches username+PAT (D-13, GIT-02).

Purpose: Close success criterion for HTTPS credential contract in the product UI (D-02: HTTPS+PAT now; SSH Phase 9).
Output: Green clone-box.pat.integration.test; build OK.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-git-https-pats/08-UI-SPEC.md
@.agents/skills/octane/SKILL.md
@apps/web/src/components/repo/clone-box.tsrx
@apps/web/src/components/repo/quick-setup.tsrx
@apps/web/src/lib/public-origin.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public clone UI → docs CTA | Signed-out users may see how-to; CTA may route login?returnTo=/settings/tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02 | Elevation of privilege | how-to copy | medium | mitigate | Explicit: password is PAT not account password (D-11) |
| T-08-SC | Tampering | packages | low | accept | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (08-12-T1): PatHowTo shared component + CloneBox/QuickSetup wire-up</name>
  <files>apps/web/src/components/repo/pat-how-to.tsrx, apps/web/src/components/repo/clone-box.tsrx, apps/web/src/components/repo/quick-setup.tsrx, apps/web/src/components/repo/clone-box.pat.integration.test.ts</files>
  <read_first>.agents/skills/octane/SKILL.md, .planning/phases/08-git-https-pats/08-UI-SPEC.md, apps/web/src/components/repo/clone-box.tsrx, apps/web/src/components/repo/quick-setup.tsrx, apps/web/src/lib/public-origin.ts</read_first>
  <behavior>
    - heading Authenticate with a personal access token
    - username aliases + password=PAT copy
    - CTA Create a personal access token → /settings/tokens (or login returnTo)
    - HTTPS URL still from httpsCloneUrl / OCTANEST_PUBLIC_ORIGIN (D-19)
  </behavior>
  <action>
Create shared pat-how-to.tsrx with UI-SPEC ordered content and monospace example blocks (wrap/overflow-x-auto backstop). Embed below HTTPS URL in CloneBox dropdown and in QuickSetup after HTTPS block. Keep SSH muted placeholder. Signed-out CTA may use /login?returnTo=/settings/tokens. Green clone-box.pat.integration.test.ts. Do not alter public-origin helper to use request Host.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/components/repo/clone-box.pat.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>how-to assertions fail; or build fails; or CTA missing</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/components/repo/pat-how-to.tsrx`
    - `rg -n 'Authenticate with a personal access token|oauth2|Create a personal access token' apps/web/src/components/repo/pat-how-to.tsrx apps/web/src/components/repo/clone-box.tsrx apps/web/src/components/repo/quick-setup.tsrx`
  </acceptance_criteria>
  <done>How-to panel green on clone/empty surfaces</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `pat-how-to.tsrx` | 08-12 | D-13 |
| CloneBox/QuickSetup wire-up | 08-12 | GIT-02 |

<verification>
How-to vitest green; build OK.
</verification>

<success_criteria>
Users see forge-parity HTTPS PAT instructions at clone and empty-repo surfaces.
</success_criteria>

<output>
Create `.planning/phases/08-git-https-pats/08-12-SUMMARY.md` when done
</output>
