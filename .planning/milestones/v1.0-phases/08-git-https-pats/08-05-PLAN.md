---
phase: 08-git-https-pats
plan: "05"
type: execute
wave: 4
depends_on:
  - "04"
files_modified:
  - crates/octanest-api/src/pat/mod.rs
  - crates/octanest-api/src/rpc.rs
  - crates/octanest-db/src/pats.rs
  - crates/octanest-api/tests/pat_rpc.rs
requirements:
  - GIT-11
autonomous: true
user_setup: []
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Verified user can createFineGrained with note, repo_access all|selected, contents read|write; token prefix ona_fg_ (D-04 forge-parity scopes, D-05, D-06, D-08, GIT-11)"
    - "FG selected with zero repos rejected; all does not require join rows"
    - "Classic and fine-grained are separate create RPCs (not one wizard) (D-05)"
    - "Fine-grained git scopes follow GitHub-/forge parity: selected/all repos + contents read/write (D-04); account/org FG permissions deferred"
  artifacts:
    - path: crates/octanest-api/src/pat/mod.rs
      provides: "pat.createFineGrained + FG repo binding"
  key_links:
    - from: crates/octanest-api/src/pat/mod.rs
      to: crates/octanest-db/src/pats.rs
      via: "insert FG + personal_access_token_repos rows"
  assumptions:
    - "ASSUME (GIT-11 empty): selected mode with empty repository_ids → pat.invalid_scope or dedicated validation error; all mode ignores repository_ids"
    - "ASSUME (A4 RESEARCH): FG all means all current+future repos owned by the user (not collaborators)"
  prohibitions:
    - statement: "Must not add org/account FG permissions in Phase 8"
      flagged: true
    - statement: "Must not hand-edit api-client (08-08)"
      flagged: true
---

<objective>
Expand PAT RPC with fine-grained create (selected/all repos + contents read/write) after classic tracer is green.

Purpose: Complete GIT-11 server surface for dual token model before UI and scope-enforcement expansion (D-04 forge-parity FG catalog).
Output: Green pat_rpc FG create/list kind badge fields; selected join rows persisted.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-git-https-pats/08-RESEARCH.md
@.planning/phases/08-git-https-pats/08-04-SUMMARY.md
@crates/octanest-api/src/pat/mod.rs
@crates/octanest-core/src/pat_types.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → createFineGrained | repository_ids must be owned by caller; no cross-user binding |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06 | Elevation of privilege | FG selected repos | high | mitigate | Verify each repository_id owned by token user before insert; reject foreign IDs |
| T-08-01 | Information disclosure | createFineGrained | high | mitigate | Same one-time plaintext + hash-at-rest as classic |
| T-08-SC | Tampering | packages | low | accept | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (08-05-T1): pat.createFineGrained + FG repo bindings</name>
  <files>crates/octanest-api/src/pat/mod.rs, crates/octanest-api/src/rpc.rs, crates/octanest-db/src/pats.rs, crates/octanest-api/tests/pat_rpc.rs</files>
  <read_first>crates/octanest-api/src/pat/mod.rs, crates/octanest-core/src/pat_types.rs, crates/octanest-db/src/pats.rs, .planning/phases/08-git-https-pats/08-RESEARCH.md</read_first>
  <behavior>
    - createFineGrained all + contents write returns ona_fg_ token once
    - createFineGrained selected with owned repo ids persists join rows; list shows Fine-grained kind
    - selected with empty list rejected
    - unverified createFineGrained → auth.email_unverified
  </behavior>
  <action>
Implement pat.createFineGrained with require_verified, required note, repo_access Selected|All, contents Read|Write (metadata read implied — no field). For Selected: require non-empty repository_ids owned by user; insert personal_access_token_repos. For All: no join rows. Mint ona_fg_ + hash. Register RPC arm. Extend pats.rs helpers for FG repo inserts/list summary if missing. Add/green pat_rpc tests for FG paths. Scope enforcement against Smart HTTP is 08-06.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(pat_)'</automated>
    <fails_when>exit code != 0; or createFineGrained missing from rpc.rs; or FG token lacks ona_fg_ prefix</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'createFineGrained|ona_fg_' crates/octanest-api/src/pat/mod.rs crates/octanest-api/src/rpc.rs`
    - `rg -n 'personal_access_token_repos|repo_access|contents' crates/octanest-api/src/pat/mod.rs crates/octanest-db/src/pats.rs`
  </acceptance_criteria>
  <done>FG PAT create RPC green</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `pat.createFineGrained` | 08-05 | GIT-11 D-05/D-06 |

<verification>
All pat_ nextest tests green including FG.
</verification>

<success_criteria>
Both classic and fine-grained PAT create/list/revoke work via RPC.
</success_criteria>

<output>
Create `.planning/phases/08-git-https-pats/08-05-SUMMARY.md` when done
</output>
