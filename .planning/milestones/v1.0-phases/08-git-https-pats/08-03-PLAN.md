---
phase: 08-git-https-pats
plan: "03"
type: execute
wave: 2
depends_on:
  - "00"
  - "02"
files_modified:
  - crates/octanest-db/migrations/postgres/0008_pats.sql
  - crates/octanest-db/migrations/mysql/0008_pats.sql
  - crates/octanest-db/migrations/sqlite/0008_pats.sql
  - crates/octanest-db/src/pats.rs
  - crates/octanest-db/src/lib.rs
  - crates/octanest-db/tests/dialect_pats.rs
  - crates/octanest-core/src/pat_types.rs
  - crates/octanest-core/src/auth_types.rs
  - crates/octanest-core/src/lib.rs
requirements:
  - GIT-11
autonomous: false
user_setup: []
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Tri-dialect 0008_pats exists with personal_access_tokens (token_hash CHAR(64) UNIQUE) and personal_access_token_repos join (GIT-11)"
    - "pat_types encode classic repo scope and FG selected/all + contents read/write (D-03, D-06)"
    - "RESERVED_USERNAMES includes git, token, oauth2 for Basic aliases (D-10)"
  artifacts:
    - path: crates/octanest-db/migrations/postgres/0008_pats.sql
      provides: "PAT tables migration"
    - path: crates/octanest-db/src/pats.rs
      provides: "create/find/list/revoke/touch dialect helpers"
    - path: crates/octanest-core/src/pat_types.rs
      provides: "PatKind + create/list DTOs (no secret on list)"
  key_links:
    - from: crates/octanest-db/src/pats.rs
      to: crates/octanest-db/migrations/postgres/0008_pats.sql
      via: "dialect match helpers for 0008 columns"
  assumptions:
    - "ASSUME (GIT-11 empty): expires_at NULL means no expiration; revoked_at NULL means active"
    - "ASSUME (GIT-11 ordering): list_pats_for_user orders by created_at DESC"
    - "PRECONDITION: 08-02 selected ona_prefixes + owner_repo_git + git_401_pat_https_only"
  prohibitions:
    - statement: "Must not place dialect SQL in octanest-api"
      flagged: true
    - statement: "Must not wire pat.* RPC or Smart HTTP in this plan"
      flagged: true
    - statement: "Must not store plaintext PAT in any column"
      flagged: true
---

<objective>
Confirm proceed_locked for D-08/D-18/D-21, then land PAT schema + core types + DB CRUD so the tracer (08-04) can mint and look up tokens safely.

Purpose: Blocking schema/migration parity gate (tri-dialect 0008) separate from Smart HTTP and UI.
Output: Green migration_parity + dialect_pats + pat_types unit coverage; Database facade methods ready.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-git-https-pats/08-CONTEXT.md
@.planning/phases/08-git-https-pats/08-RESEARCH.md
@.planning/phases/08-git-https-pats/08-PATTERNS.md
@.planning/phases/08-git-https-pats/08-02-SUMMARY.md
@crates/octanest-db/migrations/postgres/0002_auth.sql
@crates/octanest-db/src/sessions.rs
@crates/octanest-core/src/auth_types.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → DB | Schema defines hash-at-rest and revoke soft-delete contracts |
| Core DTOs → RPC later | List DTOs must never include plaintext token field |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information disclosure | personal_access_tokens | high | mitigate | token_hash CHAR(64) UNIQUE only; no plaintext column (D-15) |
| T-08-06 | Tampering | FG repo join | medium | mitigate | FK CASCADE on token_id/repository_id; selected repos only via join table |
| T-08-SC | Tampering | packages | high | mitigate | No new crates.io installs in this plan |
</threat_model>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 0 (08-03-T0): Confirm proceed with D-08/D-18/D-21 from 08-02</name>
  <decision>
Proceed to implement the one-way/costly doors already selected in 08-02: prefixes ona_pat_/ona_fg_ (D-08), clone URL /{owner}/{repo}.git (D-18), git private unauth 401 (D-21), PAT HTTPS-only (D-01).
  </decision>
  <context>
Same-plan checkpoint before schema work that enables the tracer. 08-02 recorded option IDs; this gate confirms the executor may walk through those doors now.
  </context>
  <options>
    <option id="proceed_locked">
      <title>Proceed with ona_prefixes + owner_repo_git + git_401_pat_https_only (recommended)</title>
      <pros>Honors locked CONTEXT + 08-02 decisions</pros>
      <cons>None if 08-02 already confirmed</cons>
    </option>
    <option id="stop">
      <title>Stop — revisit 08-02</title>
      <pros>Safe if decisions were not actually confirmed</pros>
      <cons>Blocks tracer</cons>
    </option>
  </options>
  <resume-signal>Reply with proceed_locked (recommended) or stop</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1 (08-03-T1): Migration 0008 + pat_types + DB pats API + reserved aliases</name>
  <reversibility rating="costly">PAT table shape + token_hash uniqueness become storage contracts; prefixes documented for mint in 08-04 (D-08).</reversibility>
  <precondition>08-02 human selected ona_prefixes, owner_repo_git, git_401_pat_https_only; 08-03-T0 proceed_locked</precondition>
  <files>crates/octanest-db/migrations/postgres/0008_pats.sql, crates/octanest-db/migrations/mysql/0008_pats.sql, crates/octanest-db/migrations/sqlite/0008_pats.sql, crates/octanest-db/src/pats.rs, crates/octanest-db/src/lib.rs, crates/octanest-db/tests/dialect_pats.rs, crates/octanest-core/src/pat_types.rs, crates/octanest-core/src/auth_types.rs, crates/octanest-core/src/lib.rs</files>
  <read_first>crates/octanest-db/migrations/postgres/0002_auth.sql, crates/octanest-db/src/sessions.rs, crates/octanest-db/tests/dialect_repositories.rs, crates/octanest-core/src/repo_types.rs, .planning/phases/08-git-https-pats/08-PATTERNS.md, .planning/phases/08-git-https-pats/08-RESEARCH.md</read_first>
  <behavior>
    - migration_parity finds 0008_pats on all three dialects
    - create_pat + find_pat_by_token_hash + list_pats_for_user + revoke_pat + touch_pat_last_used round-trip
    - is_reserved_username rejects git, token, oauth2
  </behavior>
  <action>
Add tri-dialect `0008_pats.sql`: table personal_access_tokens with id, user_id FK CASCADE, kind classic|fine_grained, name/note NOT NULL, token_prefix TEXT NOT NULL, token_hash CHAR(64) NOT NULL UNIQUE, scopes_json or classic scope fields, contents_perm nullable for FG, repo_access all|selected nullable for FG, expires_at NULL, revoked_at NULL, last_used_at NULL, last_used_ip NULL, created_at; join personal_access_token_repos (token_id, repository_id) UNIQUE pair with CASCADE (RESEARCH schema sketch, D-06, D-07, D-09, GIT-11). Implement pats.rs dialect match CRUD mirroring sessions.rs + Database facade methods. Add pat_types.rs: PatKind, classic scope repo only, FgRepoAccess Selected|All, ContentsPerm Read|Write, CreateClassicPatRequest, CreateFineGrainedPatRequest, PatListItem (no token field), CreatePatResponse { token, item }. Extend RESERVED_USERNAMES with git, token, oauth2 (D-10 Open Q). Turn dialect_pats Wave 0 stub green for schema presence + basic CRUD. Do not wire RPC or Smart HTTP (08-04).
  </action>
  <verify>
    <automated>cargo test -p octanest-core --lib && cargo test -p octanest-db --lib migration_parity && cargo test -p octanest-db --test dialect_pats</automated>
    <fails_when>exit code != 0; or migration_parity missing 0008_pats; or dialect_pats fails; or git/token/oauth2 absent from RESERVED_USERNAMES</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f crates/octanest-db/migrations/postgres/0008_pats.sql && test -f crates/octanest-db/migrations/mysql/0008_pats.sql && test -f crates/octanest-db/migrations/sqlite/0008_pats.sql`
    - `rg -n 'token_hash|personal_access_token_repos' crates/octanest-db/migrations/postgres/0008_pats.sql`
    - `rg -n 'create_pat|find_pat_by_token_hash|revoke_pat|touch_pat_last_used' crates/octanest-db/src/`
    - `rg -n 'CreateClassicPatRequest|PatListItem|ona_pat_|ona_fg_' crates/octanest-core/src/pat_types.rs`
    - `rg -n '"git"|"token"|"oauth2"' crates/octanest-core/src/auth_types.rs`
  </acceptance_criteria>
  <done>DB + types ready for tracer mint/lookup in 08-04</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `0008_pats` + `pats.rs` | 08-03 | GIT-11 persistence |
| `pat_types.rs` + reserved aliases | 08-03 | D-03/D-06/D-10 |

<verification>
migration_parity + dialect_pats + core lib green; proceed_locked recorded.
</verification>

<success_criteria>
Schema and validators exist; pat RPC and Smart HTTP still deferred to 08-04+.
</success_criteria>

<output>
Create `.planning/phases/08-git-https-pats/08-03-SUMMARY.md` when done
</output>
