---
phase: 08-git-https-pats
plan: "10"
type: execute
wave: 6
depends_on:
  - "09"
files_modified:
  - apps/web/src/routes/settings/tokens.new.tsrx
  - apps/web/src/components/settings/pat-classic-form.tsrx
  - apps/web/src/components/settings/pat-reveal.tsrx
  - apps/web/src/routes/settings/tokens.integration.test.ts
requirements:
  - GIT-11
autonomous: true
user_setup: []
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Classic and fine-grained are separate create flows with distinct routes (GitHub dual model)"
    - "One-time reveal shows plaintext once with Copy token and Back to tokens; secret never reappears in list"
    - "Unverified create routes show AuthShell verify wall + link to /verify (D-25); server returns auth.email_unverified (D-24)"
  artifacts:
    - path: apps/web/src/routes/settings/tokens.new.tsrx
      provides: "/settings/tokens/new classic create + reveal"
    - path: apps/web/src/components/settings/pat-reveal.tsrx
      provides: "D-15 one-time reveal"
  key_links:
    - from: apps/web/src/routes/settings/tokens.new.tsrx
      to: packages/api-client/src/index.ts
      via: "pat.createClassic"
  assumptions: []
  prohibitions:
    - statement: "Must not persist plaintext token in localStorage or Query cache beyond ephemeral create state"
      flagged: true
    - statement: "Must not mix return ( JSX with Rivet @{ in same .tsrx"
      flagged: true
---

<objective>
Ship classic PAT create form at `/settings/tokens/new` with verify wall, optional expiry, and one-time reveal (D-05, D-07, D-15, D-16, D-24, D-25).

Purpose: Complete classic half of dual create flows; D-25 locks AuthShell verify wall + /verify link for unverified create.
Output: Green classic create/reveal/verify assertions in tokens.integration.test; build OK.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-git-https-pats/08-UI-SPEC.md
@.agents/skills/octane/SKILL.md
@apps/web/src/routes/new.tsrx
@apps/web/src/routes/settings/tokens.tsrx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Create form → pat.createClassic | Verified session only; note/expiry client-validated then server-authoritative |
| Reveal panel → clipboard | Plaintext only in ephemeral React/Octane state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information disclosure | reveal panel | high | mitigate | Replace form with reveal once; Back to tokens clears secret from view (D-15) |
| T-08-03 | Elevation of privilege | create route | high | mitigate | AuthShell when !email_verified (D-24/D-25) |
| T-08-SC | Tampering | packages | low | accept | Existing Input/Checkbox/Radio/Button only |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (08-10-T1): Classic create + reveal + verify wall</name>
  <files>apps/web/src/routes/settings/tokens.new.tsrx, apps/web/src/components/settings/pat-classic-form.tsrx, apps/web/src/components/settings/pat-reveal.tsrx, apps/web/src/routes/settings/tokens.integration.test.ts</files>
  <read_first>.agents/skills/octane/SKILL.md, .planning/phases/08-git-https-pats/08-UI-SPEC.md, apps/web/src/routes/new.tsrx, apps/web/src/routes/settings/tokens.tsrx</read_first>
  <behavior>
    - title New classic token; required Note; required repo checkbox Full control of private repositories
    - Expiration No expiration default or Expires on date
    - success shows Make sure to copy… + Copy token + Back to tokens
    - unverified shows Verify your email AuthShell
  </behavior>
  <action>
Add file route for /settings/tokens/new (TanStack file naming per project convention — tokens.new.tsrx or nested folder matching existing settings layout). Classic form: note onInput; repo checkbox required; expiry radios; Generate token → pat.createClassic; pending Generating…. On success replace with pat-reveal (monospace token, Copy with Copied 2s / fail copy). Unverified: AuthShell wall UI-SPEC copy linking /verify — do not render form. head title New classic token · Octanest. Green classic create/reveal/verify test cases. FG route deferred to 08-11.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/settings/tokens.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>classic create/reveal assertions fail; or build fails; or reveal title copy missing</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'New classic token|Note is required|Full control of private repositories|Make sure to copy' apps/web/src/routes/settings/ apps/web/src/components/settings/`
    - `rg -n 'createClassic|Copy token|Back to tokens' apps/web/src/components/settings/ apps/web/src/routes/settings/`
  </acceptance_criteria>
  <done>Classic create + reveal green</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `/settings/tokens/new` | 08-10 | Classic D-05 |
| `pat-reveal.tsrx` | 08-10 | D-15 |

<verification>
Classic create/reveal/verify UI tests green.
</verification>

<success_criteria>
User can mint a classic PAT in UI and copy it once.
</success_criteria>

<output>
Create `.planning/phases/08-git-https-pats/08-10-SUMMARY.md` when done
</output>
