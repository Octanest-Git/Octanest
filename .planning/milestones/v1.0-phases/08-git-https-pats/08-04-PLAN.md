---
phase: 08-git-https-pats
plan: "04"
type: tracer
wave: 3
depends_on:
  - "03"
files_modified:
  - crates/oxidean-api/src/pat/mod.rs
  - crates/oxidean-api/src/rpc.rs
  - crates/oxidean-api/src/routes/git_smart_http.rs
  - crates/oxidean-api/src/routes/mod.rs
  - crates/oxidean-api/src/git/http_backend.rs
  - crates/oxidean-api/src/git/mod.rs
  - crates/oxidean-api/src/app.rs
  - crates/oxidean-api/src/auth/session.rs
  - crates/oxidean-api/tests/pat_rpc.rs
  - crates/oxidean-api/tests/git_smart_http.rs
requirements:
  - GIT-02
  - GIT-11
autonomous: true
user_setup: []
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  # low: uncalibrated absolute size; 10 files intentional for end-to-end tracer (RPC mint ↔ Smart HTTP), not split
  confidence: low
must_haves:
  truths:
    - "Verified user can createClassic PAT with note + repo scope; response includes one-time plaintext ona_pat_ token; list omits secret; revoke hides token (D-15, D-16, GIT-11)"
    - "Unverified pat.createClassic returns auth.email_unverified (D-24)"
    - "Public repo anonymous upload-pack info/refs succeeds; Basic username+classic PAT authenticates; account password as Basic password returns 401 with PAT hint (D-10, D-11, D-20, GIT-02)"
    - "Session cookie alone does not authenticate Smart HTTP (D-12)"
  artifacts:
    - path: crates/oxidean-api/src/pat/mod.rs
      provides: "pat.createClassic / list / revoke RPC"
    - path: crates/oxidean-api/src/routes/git_smart_http.rs
      provides: "Smart HTTP auth gate + CGI spawn for upload-pack"
    - path: crates/oxidean-api/src/git/http_backend.rs
      provides: "git-http-backend CGI helper"
  key_links:
    - from: crates/oxidean-api/src/routes/git_smart_http.rs
      to: crates/oxidean-api/src/pat/mod.rs
      via: "Basic password hashed → find_pat_by_token_hash"
  assumptions:
    - "ASSUME (GIT-02 concurrency): tracer proves single-request auth; multi-client lock behavior deferred to 08-06 expansion notes"
    - "ASSUME (Open Q2 RESEARCH): unverified owner may fetch with valid PAT; push blocked — tracer covers create+fetch auth; push deny in 08-06"
    - "PRECONDITION: 08-03 schema + proceed_locked complete; doors D-08/D-18/D-21 locked"
  prohibitions:
    - statement: "Must not call verify_password as a success path for git auth"
      flagged: true
    - statement: "Must not read Cookie oxidean_session for Smart HTTP authorization"
      flagged: true
    - statement: "Must not enable dumb HTTP /objects/ aliases"
      flagged: true
    - statement: "Must not implement fine-grained create UI or FG scope matrix in this tracer (08-05)"
      flagged: true
---

<objective>
Tracer: one production-quality path — verified user creates a classic PAT, then clones/fetches a public repo over HTTPS Smart HTTP using username+PAT (password rejected; cookies ignored).

Purpose: Catch architecture dead-ends (CGI spawn, Basic parse, hash lookup, Traefik-ready routes) before FG/rate-limit/UI expansion. Keep classic RPC mint + Smart HTTP route in one plan (~10 files) so the tracer slice is end-to-end; do not split mint vs CGI unless this wave fails.
Output: Green pat_rpc classic create/list/revoke + git_smart public upload-pack + password reject + cookie ignore.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-git-https-pats/08-CONTEXT.md
@.planning/phases/08-git-https-pats/08-RESEARCH.md
@.planning/phases/08-git-https-pats/08-PATTERNS.md
@.planning/phases/08-git-https-pats/08-03-SUMMARY.md
@crates/oxidean-api/src/auth/session.rs
@crates/oxidean-api/src/auth/gate.rs
@crates/oxidean-api/src/routes/repo_raw.rs
@crates/oxidean-api/src/repo/mod.rs
@crates/oxidean-git/src/cli.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → pat.createClassic | Session cookie + require_verified; note/scopes untrusted |
| Git client → Smart HTTP | Basic credentials untrusted; Cookie must be ignored |
| API → git-http-backend CGI | PATH_INFO must stay under repos_dir; no shell string concat |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information disclosure | pat.createClassic | high | mitigate | Return plaintext once; persist SHA-256 hex only; list omits token (D-15) |
| T-08-02 | Elevation of privilege | Smart HTTP Basic | high | mitigate | PAT-prefix/hash lookup only; reject non-PAT secrets with hint (D-11); ignore cookies (D-12) |
| T-08-07 | Tampering | http_backend CGI | high | mitigate | argv to git-http-backend binary; validate owner/repo via bare_repo_path; GIT_HTTP_EXPORT_ALL |
| T-08-SC | Tampering | crates | medium | accept | No new crates.io deps — tokio process already available |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (08-04-T1): pat.createClassic / list / revoke RPC</name>
  <reversibility rating="costly">Published ona_pat_ mint format (D-08).</reversibility>
  <files>crates/oxidean-api/src/pat/mod.rs, crates/oxidean-api/src/rpc.rs, crates/oxidean-api/src/auth/session.rs, crates/oxidean-api/tests/pat_rpc.rs</files>
  <read_first>crates/oxidean-api/src/repo/mod.rs, crates/oxidean-api/src/auth/gate.rs, crates/oxidean-api/src/auth/session.rs, crates/oxidean-api/src/rpc.rs, crates/oxidean-core/src/pat_types.rs, .agents/skills/tdd/SKILL.md</read_first>
  <behavior>
    - verified createClassic with note + scopes containing repo returns token starting with ona_pat_ and PatListItem metadata
    - list after create has item without token field / secret
    - revoke removes from subsequent list
    - unverified create → auth.email_unverified
    - empty note → pat.note_required
  </behavior>
  <action>
Implement pat module: createClassic requires require_verified; validate non-empty note (pat.note_required); require classic scope repo (pat.invalid_scope otherwise); mint CSPRNG hex prefixed ona_pat_ (reuse session sha256_hex / bytes_to_hex style — extract shared helper if clean); store token_hash only; return CreatePatResponse once. list requires session (not necessarily verified); returns PatListItem with last_used_* nullable, kind, note, expires, prefix — never plaintext. revoke by id for owning user → pat.not_found if missing/other user. Register pat.createClassic, pat.list, pat.revoke in rpc.rs. Green the matching Wave 0 tests in pat_rpc.rs. Leave createFineGrained stub Err or unimplemented for 08-05. Do not hand-edit packages/api-client (08-08 rpc-gen).
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(pat_)'</automated>
    <fails_when>exit code != 0; or any pat_ test still Wave-0-assert-false; or create response missing ona_pat_ prefix</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'pat\.createClassic|pat\.list|pat\.revoke' crates/oxidean-api/src/rpc.rs`
    - `rg -n 'ona_pat_|require_verified|pat.note_required' crates/oxidean-api/src/pat/mod.rs`
    - `rg -n 'sha256|token_hash' crates/oxidean-api/src/pat/mod.rs`
  </acceptance_criteria>
  <done>Classic PAT lifecycle RPC green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (08-04-T2): Smart HTTP upload-pack tracer with PAT Basic + password reject</name>
  <reversibility rating="one-way">Live /{owner}/{repo}.git Smart HTTP surface (D-18/D-22).</reversibility>
  <files>crates/oxidean-api/src/routes/git_smart_http.rs, crates/oxidean-api/src/routes/mod.rs, crates/oxidean-api/src/git/http_backend.rs, crates/oxidean-api/src/git/mod.rs, crates/oxidean-api/src/app.rs, crates/oxidean-api/tests/git_smart_http.rs</files>
  <read_first>crates/oxidean-api/src/routes/repo_raw.rs, crates/oxidean-api/src/git/mod.rs, crates/oxidean-api/src/repo/acl.rs, crates/oxidean-git/src/cli.rs, .planning/phases/08-git-https-pats/08-RESEARCH.md</read_first>
  <behavior>
    - GET /{owner}/{repo}.git/info/refs?service=git-upload-pack on public repo without auth → 200 CGI success
    - same with Basic username (owner or git/token/oauth2) + valid classic PAT → 200
    - Basic with account password → 401 body/header hint mentioning personal access token
    - request with only session cookie → treated as anonymous (public OK; private would 401 — assert cookie does not elevate)
  </behavior>
  <action>
Mount Smart HTTP routes for info/refs, git-upload-pack, git-receive-pack under /{owner}/{repo}.git (Axum path shape that keeps .git in repo segment). Ignore Cookie for auth (D-12). Parse Authorization Basic; identity from PAT password via SHA-256 lookup; accept username aliases per RESEARCH (account username case-insensitive, git, token, oauth2); empty username → 401. If password does not look like ona_pat_/ona_fg_ or hash miss → 401 + WWW-Authenticate: Basic realm="Oxidean Git" and hint to create a PAT (never Argon2 success). For public upload-pack allow anon; spawn git-http-backend with GIT_PROJECT_ROOT=repos_dir, PATH_INFO, GIT_HTTP_EXPORT_ALL=1, forward Git-Protocol → GIT_PROTOCOL, set REMOTE_USER when authenticated. Use bare_repo_path validation. Green tracer subset of git_smart_http tests (public anon, PAT auth, password reject, cookie ignore). Private 401, push, 403, 429, unverified push may remain RED until 08-06 — mark those #[ignore] or leave failing only if Wave 0 still requires them discoverable; prefer turning tracer cases green and keep expansion cases failing with clear names.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(git_smart)'</automated>
    <fails_when>exit code != 0 for non-ignored tracer cases; or password-accept path exists; or cookie elevates private access</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'git_smart_http|git-upload-pack|WWW-Authenticate' crates/oxidean-api/src/routes/git_smart_http.rs crates/oxidean-api/src/app.rs`
    - `rg -n 'GIT_HTTP_EXPORT_ALL|git-http-backend|REMOTE_USER' crates/oxidean-api/src/git/http_backend.rs`
    - `rg -n 'oxidean_session|Cookie' crates/oxidean-api/src/routes/git_smart_http.rs` shows ignore/not-used-for-auth pattern
  </acceptance_criteria>
  <done>Tracer Smart HTTP + classic PAT path production-quality and verified</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `pat/mod.rs` classic RPC | 08-04 | GIT-11 tracer |
| `git_smart_http.rs` + `http_backend.rs` | 08-04 | GIT-02 tracer |
| Green pat_rpc + partial git_smart | 08-04 | Nyquist |

<verification>
Tracer nextest filters green for classic PAT lifecycle and public upload-pack + password/cookie negatives.
</verification>

<success_criteria>
Users (via API tests) can mint a classic PAT and fetch a public repo over HTTPS Smart HTTP with that PAT; passwords rejected.
</success_criteria>

<output>
Create `.planning/phases/08-git-https-pats/08-04-SUMMARY.md` when done
</output>
