---
phase: 08-git-https-pats
plan: "08"
type: execute
wave: 5
depends_on:
  - "04"
  - "05"
files_modified:
  - packages/api-client/src/index.ts
  - packages/api-client/src/types.ts
  - docs/API.md
requirements:
  - GIT-11
autonomous: true
user_setup: []
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 1
  confidence: medium
must_haves:
  truths:
    - "make rpc-gen regenerates @oxidean/api-client with pat.createClassic, pat.createFineGrained, pat.list, pat.revoke"
    - "docs/API.md states PATs are not RPC Bearer in Phase 8 (D-01)"
  artifacts:
    - path: packages/api-client/src/index.ts
      provides: "Generated pat.* client methods"
    - path: docs/API.md
      provides: "pat.* + Smart HTTP auth notes"
  key_links:
    - from: crates/oxidean-api/src/rpc.rs
      to: packages/api-client/src/index.ts
      via: "make rpc-gen"
  assumptions: []
  prohibitions:
    - statement: "Must not hand-edit api-client as lasting source of truth without rpc-gen"
      flagged: true
---

<objective>
Regenerate typed TS client for pat.* RPCs and document Smart HTTP + PAT auth contracts in API.md.

Purpose: Unlock Octane UI plans that import @oxidean/api-client.
Output: rpc-sync-check clean; API.md updated.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@crates/oxidean-api/src/rpc.rs
@crates/oxidean-core/src/pat_types.rs
@docs/API.md
@Makefile
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated client → browser | Must not imply PAT works as RPC Bearer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-10 | Spoofing | docs/client misuse | medium | mitigate | API.md explicitly: session cookies for RPC; PAT for HTTPS git only (D-01) |
| T-08-SC | Tampering | packages | low | accept | rpc-gen only |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (08-08-T1): make rpc-gen + API.md PAT/Smart HTTP docs</name>
  <files>packages/api-client/src/index.ts, packages/api-client/src/types.ts, docs/API.md</files>
  <read_first>crates/oxidean-api/src/rpc.rs, crates/oxidean-core/src/pat_types.rs, docs/API.md, Makefile</read_first>
  <action>
Run make rpc-gen (or cargo run -p oxidean-api --bin rpc-gen) so packages/api-client exports pat.createClassic, pat.createFineGrained, pat.list, pat.revoke and DTOs. Update docs/API.md: document pat.* procedures, error codes (pat.not_found, pat.note_required, pat.invalid_scope, auth.email_unverified); document Smart HTTP paths and Basic+PAT (not session); state D-01 PATs are not Bearer for typed RPC. Run make rpc-sync-check. Do not implement UI here.
  </action>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && rg -n 'pat\.createClassic|pat\.createFineGrained|pat\.list|pat\.revoke' packages/api-client/src/ && rg -n 'Smart HTTP|personal access token|Bearer' docs/API.md</automated>
    <fails_when>rpc-sync-check fails; or pat procedures missing from api-client; or API.md lacks PAT/Smart HTTP notes</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'createClassic|createFineGrained' packages/api-client/src/`
    - `rg -n 'not.*Bearer|HTTPS git|Smart HTTP' docs/API.md`
    - `make rpc-sync-check` exits 0
  </acceptance_criteria>
  <done>Generated client + API docs ready for UI</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `@oxidean/api-client` pat.* | 08-08 | GIT-11 |
| `docs/API.md` updates | 08-08 | D-01 |

<verification>
rpc-sync-check green; client includes pat procedures.
</verification>

<success_criteria>
Web can call pat.* via generated client without hand patches.
</success_criteria>

<output>
Create `.planning/phases/08-git-https-pats/08-08-SUMMARY.md` when done
</output>
