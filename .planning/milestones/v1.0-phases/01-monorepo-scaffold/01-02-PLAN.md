---
phase: 01-monorepo-scaffold
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - crates/oxidean-api/
  - crates/oxidean-core/
  - crates/oxidean-db/
  - packages/api-client/
  - Makefile
  - scripts/check-rpc-sync.sh
requirements:
  - PLAT-05
  - PLAT-06
autonomous: true
user_setup: []
must_haves:
  truths:
    - "API serves system.health and system.echo over HTTP /api/rpc and WS /api/rpc/ws"
    - "Wrong or missing Oxidean-RPC-Version is rejected"
    - "packages/api-client is regenerated from Rust and CI sync script fails on drift"
    - "Typed AppError with code+message(+data) is exported to TS"
  artifacts:
    - path: crates/oxidean-api/src/main.rs
      provides: "Axum server with RPC mounts"
    - path: packages/api-client/src
      provides: "generated client + queryOptions helpers"
  key_links:
    - from: crates/oxidean-api
      to: packages/api-client
      via: "specta-typescript codegen / make rpc-gen"
---

<objective>
Implement Rust API with Oxidean thin RPC (specta-style, not unmaintained rspc as primary), HTTP+WS mounts, version header, CORS, health/echo procedures, codegen into packages/api-client with TanStack Query helpers, and automated tests.

Purpose: Satisfy PLAT-05/PLAT-06 and CONTEXT D-11–D-21 per RESEARCH recommendation.
Output: Running API binary, generated client, cargo+vitest proofs for echo over HTTP and WS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-monorepo-scaffold/01-CONTEXT.md
@.planning/phases/01-monorepo-scaffold/01-RESEARCH.md
@.planning/phases/01-monorepo-scaffold/01-VALIDATION.md
@.planning/phases/01-monorepo-scaffold/01-01-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/client → `/api/rpc` + `/api/rpc/ws` | Untrusted JSON/WS frames; public in Phase 1 |
| Env `OXIDEAN_CORS_ORIGINS` → CORS layer | Misconfig can open origins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Spoofing | RPC version header | mitigate | Middleware requires `Oxidean-RPC-Version: 1`; reject otherwise with typed error |
| T-01-04 | Information disclosure | CORS | mitigate | `OXIDEAN_ENV=development` allows reflective/any-origin policy; production/Compose requires non-empty `OXIDEAN_CORS_ORIGINS` allowlist or API refuses to start |
| T-01-05 | Denial of service | public echo | accept | Phase 1 no auth/rate-limit; echo payload size capped (e.g. max 8KiB) as cheap mitigate |
| T-01-06 | Tampering | generated client | mitigate | `scripts/check-rpc-sync.sh` runs regen + `git diff --exit-code -- packages/api-client` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Core types, DB stub, Axum RPC HTTP+WS</name>
  <files>crates/oxidean-core/**, crates/oxidean-db/**, crates/oxidean-api/**</files>
  <read_first>.planning/phases/01-monorepo-scaffold/01-CONTEXT.md, .planning/phases/01-monorepo-scaffold/01-RESEARCH.md, crates/oxidean-api/Cargo.toml</read_first>
  <action>
Per RESEARCH: use **Axum 0.8 + specta + specta-typescript** with Oxidean-owned router (do **not** take rspc as primary dependency).

1. `oxidean-core`: define `AppError { code: String, message: String, data: Option&lt;serde_json::Value&gt; }`, `HealthResponse { status: String, version: String }`, `EchoRequest { message: String }`, `EchoResponse { message: String }`. Specta Type derives as needed.
2. `oxidean-db`: `Database` wrapper with Postgres URL from `DATABASE_URL`; Phase 1 `ping()` via sqlx (postgres feature). SQLite/MySQL feature flags may exist but need not be proven (Phase 2).
3. `oxidean-api`: Axum app binding `API_BIND` (default `0.0.0.0:8080`):
   - `GET /healthz` → 200 JSON `{ "ok": true }` for probes
   - `POST /api/rpc` JSON body `{ "procedure": "system.health"|"system.echo", "input": ... }` returning `{ "ok": true, "data": ... }` or `{ "ok": false, "error": AppError }`
   - `GET /api/rpc/ws` WebSocket: same procedure protocol over messages (D-17 transport only)
   - Middleware: require header `Oxidean-RPC-Version` equal to `1` (D-18); on failure return typed error code `rpc.version_mismatch`
   - CORS via tower-http: if `OXIDEAN_ENV=development` permissive (reflect origin or allow any with credentials-safe approach); else require `OXIDEAN_CORS_ORIGINS` comma-list or exit 1 on boot (D-19/D-20)
4. Procedures: `system.health` returns ok + crate/version string; `system.echo` echoes message (reject if &gt; 8192 bytes).
5. Wire `oxidean-db` ping optionally into health payload as `"database": "ok"|"skipped"` without failing health if DB down in Phase 1 — prefer include ping result field but keep HTTP 200 for API liveness; document choice in SUMMARY.
  </action>
  <verify>cargo test -p oxidean-api --tests</verify>
  <acceptance_criteria>
    - crates/oxidean-api contains route string `/api/rpc`
    - crates/oxidean-api contains `/api/rpc/ws`
    - crates/oxidean-api contains `Oxidean-RPC-Version`
    - crates/oxidean-api contains `system.health`
    - crates/oxidean-api contains `system.echo`
    - crates/oxidean-core contains `AppError` or struct AppError
    - `cargo test -p oxidean-api` exits 0
  </acceptance_criteria>
  <done>HTTP+WS RPC works with version gate and CORS policy</done>
</task>

<task type="auto">
  <name>Task 2: Codegen to api-client + TanStack Query helpers + sync script</name>
  <files>packages/api-client/**, Makefile, scripts/check-rpc-sync.sh, crates/oxidean-api/**</files>
  <read_first>packages/api-client/package.json, crates/oxidean-api/src/main.rs</read_first>
  <action>
1. Add codegen path: `make rpc-gen` runs a Rust binary or `cargo run -p oxidean-api --bin rpc-gen` that emits TypeScript types + client into `packages/api-client/src/` using specta-typescript (or equivalent emit).
2. Client must:
   - Send `Oxidean-RPC-Version: 1` on HTTP requests
   - Support `credentials: 'include'`
   - Expose `createClient({ baseUrl })` and procedures `system.health` / `system.echo`
   - Export TanStack Query `queryOptions` / `mutationOptions` helpers (D-21) — depend on `@tanstack/react-query` as peerDep
3. Implement watch-friendly path for `make dev` documentation: e.g. `cargo watch -x 'run -p oxidean-api --bin rpc-gen'` noted in Makefile `dev` recipe composition (full concurrent dev in plan 03/04).
4. Finalize `scripts/check-rpc-sync.sh`: regen then `git diff --exit-code -- packages/api-client`.
5. Add vitest test that imports client helpers (can mock fetch) asserting RPC_VERSION/header constant is 1.
6. Add cargo tests: HTTP health+echo; WS health+echo; version mismatch rejected (VALIDATION Wave 0).
  </action>
  <verify>make rpc-gen &amp;&amp; make rpc-sync-check &amp;&amp; bunx vitest run packages/api-client &amp;&amp; cargo test -p oxidean-api</verify>
  <acceptance_criteria>
    - packages/api-client/src contains generated or hand-emitted client exporting health or system.health
    - packages/api-client contains `queryOptions` or `mutationOptions`
    - scripts/check-rpc-sync.sh contains `git diff --exit-code`
    - Makefile `rpc-gen` target runs without error
    - cargo test output includes passing rpc tests
  </acceptance_criteria>
  <done>Codegen + CI sync + HTTP/WS tests green</done>
</task>

</tasks>

<verification>
- [ ] `cargo test -p oxidean-api` passes
- [ ] `make rpc-sync-check` passes on clean tree
- [ ] vitest for api-client passes
</verification>

<success_criteria>
- D-11–D-21 implemented
- PLAT-05 and PLAT-06 demonstrably true
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-scaffold/01-02-SUMMARY.md`
</output>
