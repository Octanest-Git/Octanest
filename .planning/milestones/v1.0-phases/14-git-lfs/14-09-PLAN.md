---
phase: 14-git-lfs
plan: "09"
type: execute
wave: 7
depends_on:
  - "01"
  - "08"
files_modified:
  - apps/web/src/routes/$owner.$repo.settings.tsrx
  - apps/web/src/routes/$owner.$repo.settings.lfs.integration.test.ts
  - apps/web/src/components/repo
requirements:
  - GIT-12
autonomous: true
user_setup: []
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Repo Settings shows LFS enable toggle (Admin only), status, docs link for .gitattributes, and usage breakdown (D-LFS-10, D-LFS-16, D-LFS-17, D-LFS-19)"
  artifacts:
    - path: apps/web/src/routes/$owner.$repo.settings.tsrx
      provides: "LFS settings section"
  key_links:
    - from: apps/web/src/routes/$owner.$repo.settings.tsrx
      to: packages/api-client
      via: "repo.lfs.setEnabled / getUsage Query"
  assumptions:
    - "ASSUME: Settings copy Enable Git LFS for this repository + docs link (discretion)"
  prohibitions:
    - statement: "Must not auto-commit .gitattributes from the server"
      flagged: true
    - statement: "Must not author React JSX; use Octane .tsrx"
      flagged: true
---

<objective>
Ship repo Settings LFS toggle, status, and usage breakdown in Octane.

Purpose: Deliver D-LFS-16/19 repo-side operator UX for GIT-12 enablement.
Output: Green settings.lfs Vitest + buildable settings route.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-git-lfs/14-CONTEXT.md
@.planning/phases/14-git-lfs/14-08-SUMMARY.md
@apps/web/src/routes/$owner.$repo.settings.tsrx
@.agents/skills/octane/SKILL.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser session → setEnabled | Server enforces Admin; UI hides for non-Admin |
| Usage RPC → metrics | Read for viewers with settings access patterns existing today |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-02 | Elevation of privilege | enable toggle UI | high | mitigate | Only render toggle when can_admin; server re-checks (D-LFS-10) |
| T-14-SC | Tampering | npm | medium | accept | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (14-09-T1): Settings LFS enable + status section</name>
  <files>apps/web/src/routes/$owner.$repo.settings.tsrx, apps/web/src/routes/$owner.$repo.settings.lfs.integration.test.ts, apps/web/src/components/repo</files>
  <read_first>.agents/skills/octane/SKILL.md, apps/web/src/routes/$owner.$repo.settings.tsrx, apps/web/src/routes/$owner.$repo.settings.collaborators.integration.test.ts</read_first>
  <behavior>
    - Admin sees enable toggle and status; non-Admin does not get toggle control
    - Docs link to .gitattributes / git lfs track guidance present
    - Toggle calls repo.lfs.setEnabled and refreshes status
  </behavior>
  <action>
Extend repo settings with LFS section per D-LFS-10, D-LFS-16, D-LFS-17 using Octane .tsrx + TanStack Query. Link to docs; do not auto-commit attributes. Green settings.lfs integration tests for Admin vs non-Admin.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/\$owner.\$repo.settings.lfs.integration.test.ts</automated>
  </verify>
  <done>Repo Settings LFS enable/status works for Admins</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (14-09-T2): Repo usage breakdown dashboard</name>
  <files>apps/web/src/routes/$owner.$repo.settings.tsrx, apps/web/src/routes/$owner.$repo.settings.lfs.integration.test.ts</files>
  <read_first>apps/web/src/routes/$owner.$repo.settings.tsrx</read_first>
  <behavior>
    - Settings shows this-repo usage breakdown (OID count, logical bytes, useful top OIDs/paths as available)
  </behavior>
  <action>
Wire repo.lfs.getUsage into Settings per D-LFS-19 (repo dashboard). Match existing settings visual language (no new design system). Green usage assertions in settings.lfs tests.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/\$owner.\$repo.settings.lfs.integration.test.ts && bun --cwd apps/web run build</automated>
  </verify>
  <done>Repo LFS usage breakdown visible in Settings</done>
</task>

</tasks>

<verification>
settings.lfs Vitest green; apps/web build succeeds.
</verification>

<success_criteria>
Admins can enable LFS and see repo usage in Settings with docs link.
</success_criteria>

<output>
Create `.planning/phases/14-git-lfs/14-09-SUMMARY.md` when done
</output>
