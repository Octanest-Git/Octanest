---
phase: 14-git-lfs
plan: "11"
type: execute
wave: 8
depends_on:
  - "01"
  - "08"
  - "09"
files_modified:
  - apps/web/src/lib/lfs-pointer.ts
  - apps/web/src/lib/lfs-pointer.test.ts
  - apps/web/src/components/repo/blob-viewer.tsrx
  - apps/web/src/components/repo/blob-viewer.lfs.integration.test.ts
  - apps/web/src/components/repo/lfs-browser.tsrx
  - apps/web/src/components/repo/lfs-browser.integration.test.ts
  - apps/web/src/routes/$owner.$repo.settings.tsrx
requirements:
  - GIT-12
autonomous: true
user_setup: []
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Blob view detects LFS pointer, shows badge, and offers Download via session Read path (D-LFS-18)"
    - "In-app LFS browser lists objects for enabled repos; tree listings link to blob badges (D-LFS-16 locked clarification)"
  artifacts:
    - path: apps/web/src/components/repo/blob-viewer.tsrx
      provides: "Pointer badge + Download"
    - path: apps/web/src/components/repo/lfs-browser.tsrx
      provides: "In-app LFS object browser"
  key_links:
    - from: apps/web/src/components/repo/blob-viewer.tsrx
      to: packages/api-client
      via: "repo.lfs.download session RPC"
  assumptions:
    - "ASSUME: Download never uses Cookie against .git/info/lfs; session RPC only (RESEARCH A2 RESOLVED)"
    - "LOCKED (D-LFS-16 CONTEXT 2026-09-14): blob badge + LFS browser; no separate tree-row badge required"
  prohibitions:
    - statement: "Must not embed PAT Basic credentials in the browser for Download"
      flagged: true
    - statement: "Must not author React JSX; use Octane .tsrx"
      flagged: true
---

<objective>
Ship pointer detection badge, session Download, and in-app LFS browser.

Purpose: Complete rich client UX from D-LFS-16/18 for GIT-12 (blob badges + browser per locked D-LFS-16 clarification).
Output: Green pointer/blob/browser Vitest suites.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-git-lfs/14-CONTEXT.md
@.planning/phases/14-git-lfs/14-08-SUMMARY.md
@apps/web/src/components/repo/blob-viewer.tsrx
@.agents/skills/octane/SKILL.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Blob content → pointer parse | Untrusted file text; size &lt; 1024 rule |
| Download RPC → bytes | Capability Read; private repo soft-deny patterns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-05 | Information disclosure | Download | high | mitigate | Session RPC + Read only; no PAT in JS (D-LFS-18, A2) |
| T-14-04 | Tampering | pointer parse | low | accept | Strict parse; non-pointer falls through to normal blob view |
| T-14-SC | Tampering | npm | medium | accept | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (14-11-T1): Pointer parse + blob badge + Download</name>
  <files>apps/web/src/lib/lfs-pointer.ts, apps/web/src/lib/lfs-pointer.test.ts, apps/web/src/components/repo/blob-viewer.tsrx, apps/web/src/components/repo/blob-viewer.lfs.integration.test.ts</files>
  <read_first>.agents/skills/octane/SKILL.md, apps/web/src/components/repo/blob-viewer.tsrx, .planning/phases/14-git-lfs/14-RESEARCH.md</read_first>
  <behavior>
    - Valid pointer text detected; badge shown; Download fetches via session RPC with Read
    - Non-pointer UTF-8 blob unchanged
  </behavior>
  <action>
Implement parseLfsPointer fully per spec rules; integrate into BlobViewer for utf-8 non-binary content (D-LFS-18). Download uses repo.lfs.download (session), not .git/info/lfs Cookie. Tree listings continue to link into blob view where the badge appears (D-LFS-16 locked: no separate FileTree badge). Green lfs-pointer.test.ts and blob-viewer.lfs.integration.test.ts.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/lib/lfs-pointer.test.ts src/components/repo/blob-viewer.lfs.integration.test.ts</automated>
  </verify>
  <done>Pointer badge and Download work on blob view</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (14-11-T2): In-app LFS browser surface</name>
  <files>apps/web/src/components/repo/lfs-browser.tsrx, apps/web/src/components/repo/lfs-browser.integration.test.ts, apps/web/src/routes/$owner.$repo.settings.tsrx</files>
  <read_first>apps/web/src/components/repo/blob-viewer.tsrx, apps/web/src/routes/$owner.$repo.settings.tsrx</read_first>
  <behavior>
    - Enabled repo exposes LFS browser listing linked OIDs/sizes (and download affordance where Read)
    - Disabled/empty state is clear
  </behavior>
  <action>
Build lfs-browser.tsrx per D-LFS-16 wired to list RPC; mount from Settings and/or repo chrome as fits existing navigation. Green lfs-browser.integration.test.ts; run bun --cwd apps/web run build.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/components/repo/lfs-browser.integration.test.ts && bun --cwd apps/web run build</automated>
  </verify>
  <done>In-app LFS browser is available for enabled repos</done>
</task>

</tasks>

<verification>
Pointer, blob LFS, and browser Vitest green; web build succeeds.
</verification>

<success_criteria>
Users see LFS badges, can Download via session, and browse LFS objects in-app.
</success_criteria>

<output>
Create `.planning/phases/14-git-lfs/14-11-SUMMARY.md` when done
</output>
