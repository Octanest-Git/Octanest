---
phase: 14-git-lfs
plan: "06"
type: execute
wave: 5
depends_on:
  - "05"
files_modified:
  - crates/octanest-api/src/jobs/lfs_gc.rs
  - crates/octanest-api/src/jobs/mod.rs
  - crates/octanest-api/src/jobs/schedule.rs
  - crates/octanest-api/src/auth/admin.rs
  - crates/octanest-api/src/app.rs
  - crates/octanest-api/tests/factory_reset_scope.rs
  - crates/octanest-api/tests/lfs_store.rs
requirements:
  - GIT-13
autonomous: true
user_setup: []
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Periodic GC deletes only refcount=0 OIDs after grace with lockfile safety (D-LFS-15)"
    - "Factory reset database_and_repositories also wipes OCTANEST_LFS_DIR children (D-LFS-04)"
  artifacts:
    - path: crates/octanest-api/src/jobs/lfs_gc.rs
      provides: "Unreferenced OID GC job"
    - path: crates/octanest-api/src/auth/admin.rs
      provides: "Factory reset LFS_DIR wipe"
  key_links:
    - from: crates/octanest-api/src/auth/admin.rs
      to: crates/octanest-api/src/lfs/store.rs
      via: "wipe_lfs_dir_contents mirrors wipe_repos_dir_contents"
  assumptions:
    - "ASSUME: GC interval default 24h; grace 7 days; OCTANEST_LFS_GC_INTERVAL_SECS=0 disables; lock at LFS_DIR/.gc.lock"
  prohibitions:
    - statement: "Must not delete OIDs with refcount greater than zero"
      flagged: true
---

<objective>
Add refcount-safe LFS GC and extend factory reset to wipe the LFS volume.

Purpose: Close D-LFS-15 and D-LFS-04 operator lifecycle for GIT-13 storage.
Output: Green factory_reset LFS wipe + lfs_gc tests.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-git-lfs/14-CONTEXT.md
@.planning/phases/14-git-lfs/14-RESEARCH.md
@.planning/phases/14-git-lfs/14-05-SUMMARY.md
@crates/octanest-api/src/jobs/schedule.rs
@crates/octanest-api/src/auth/admin.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GC job → LFS_DIR | Must not delete live OID |
| Factory reset → volumes | Destructive; confirm gate already required |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | Tampering | lfs_gc | high | mitigate | Refcount=0 + grace + .gc.lock before unlink (D-LFS-15) |
| T-14-03 | Denial of service | factory reset wipe | medium | mitigate | Existing confirm phrase; wipe only children of LFS_DIR |
| T-14-SC | Tampering | crates | medium | accept | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (14-06-T1): Periodic unreferenced OID GC</name>
  <reversibility rating="costly">GC deletes physical objects (D-LFS-15).</reversibility>
  <files>crates/octanest-api/src/jobs/lfs_gc.rs, crates/octanest-api/src/jobs/mod.rs, crates/octanest-api/src/jobs/schedule.rs, crates/octanest-api/src/app.rs, crates/octanest-api/tests/lfs_store.rs</files>
  <read_first>crates/octanest-api/src/jobs/schedule.rs, crates/octanest-api/src/jobs/reconcile.rs</read_first>
  <behavior>
    - OID with refcount 0 older than grace is deleted from disk and DB
    - OID with refcount greater than 0 is never deleted
    - Concurrent GC serialized via LFS_DIR/.gc.lock
  </behavior>
  <action>
Implement D-LFS-15 GC job mirroring orphan reconcile scheduling: interval from env (default 24h), grace default 7 days, disable with 0. Only delete refcount=0 after grace; use lockfile under LFS_DIR. Spawn from schedule alongside existing jobs (pass lfs_dir). Green lfs_gc unit/integration stubs.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(lfs_gc) | test(lfs_store)'</automated>
  </verify>
  <done>GC removes only safely unreferenced OIDs</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (14-06-T2): Factory reset wipes LFS_DIR</name>
  <files>crates/octanest-api/src/auth/admin.rs, crates/octanest-api/src/lfs/store.rs, crates/octanest-api/tests/factory_reset_scope.rs</files>
  <read_first>crates/octanest-api/src/auth/admin.rs, crates/octanest-api/tests/factory_reset_scope.rs</read_first>
  <behavior>
    - database_and_repositories factory reset removes LFS_DIR children and keeps directory root
    - database-only scope does not wipe LFS_DIR
  </behavior>
  <action>
Extend factory_reset per D-LFS-04: when wiping repositories, also wipe_lfs_dir_contents(lfs_dir) mirroring wipe_repos_dir_contents. Thread lfs_dir through RpcCtx/AppState into admin factory_reset. Green factory_reset_scope LFS assertions.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(factory_reset)'</automated>
  </verify>
  <done>Factory reset clears LFS volume with repositories</done>
</task>

</tasks>

<verification>
GC and factory-reset LFS wipe covered by nextest.
</verification>

<success_criteria>
Unreferenced OIDs are GCed safely; factory reset clears LFS_DIR with repos.
</success_criteria>

<output>
Create `.planning/phases/14-git-lfs/14-06-SUMMARY.md` when done
</output>
