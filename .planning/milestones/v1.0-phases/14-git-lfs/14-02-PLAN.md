---
phase: 14-git-lfs
plan: "02"
type: tracer
wave: 1
depends_on:
  - "00"
files_modified:
  - crates/octanest-db/migrations/sqlite
  - crates/octanest-db/migrations/postgres
  - crates/octanest-db/migrations/mysql
  - crates/octanest-db/src
  - crates/octanest-api/src/lfs/mod.rs
  - crates/octanest-api/src/lfs/store.rs
  - crates/octanest-api/src/lfs/batch.rs
  - crates/octanest-api/src/routes/git_lfs.rs
  - crates/octanest-api/src/routes/mod.rs
  - crates/octanest-api/src/app.rs
  - crates/octanest-api/src/main.rs
  - crates/octanest-api/tests/lfs_batch.rs
  - crates/octanest-api/tests/lfs_store.rs
  - crates/octanest-db/tests/dialect_lfs.rs
requirements:
  - GIT-12
  - GIT-13
autonomous: true
user_setup: []
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Operator-configured OCTANEST_LFS_DIR receives OID-sharded objects ab/cd/oid on successful PUT (D-LFS-01, D-LFS-03, GIT-13)"
    - "Enabled repo can Batch upload then basic PUT then GET download for one OID with transfer=basic (D-LFS-05, D-LFS-06, D-LFS-07 gate=basic, GIT-12)"
    - "Clone without smudge remains valid — LFS routes do not alter git object store pointers (D-LFS-08)"
  artifacts:
    - path: crates/octanest-api/src/routes/git_lfs.rs
      provides: "Batch + basic transfer handlers under .git/info/lfs"
    - path: crates/octanest-api/src/lfs/store.rs
      provides: "Content-addressed OID store on LFS_DIR"
  key_links:
    - from: crates/octanest-api/src/routes/git_lfs.rs
      to: crates/octanest-api/src/lfs/store.rs
      via: "batch hrefs → PUT/GET stream to shard path"
    - from: crates/octanest-api/src/app.rs
      to: docker-compose.yml
      via: "AppState.lfs_dir from OCTANEST_LFS_DIR (Compose bind in 14-07)"
  assumptions:
    - "ASSUME: tracer may force lfs_enabled=true in test fixture SQL until 14-03 Admin toggle RPC lands"
    - "ASSUME: tracer auth may reuse minimal PAT helper; full matrix greens in 14-03"
    - "LOCKED (D-LFS-07 CONTEXT 2026-09-14): phase gate is Batch+basic + streaming PUT; official transfer=multipart deferred; Range/verify land in 14-05"
  prohibitions:
    - statement: "Must not implement transfer=multipart as required path (stock clients speak basic only; D-LFS-07 locked deviation)"
      flagged: true
    - statement: "Must not store LFS objects under each bare repo path"
      flagged: true
    - statement: "Must not add new crates.io packages (reuse sha2/tempfile/tokio)"
      flagged: true
---

<objective>
Tracer: one production-quality path — LFS Batch + basic PUT/GET for a single OID landing under OCTANEST_LFS_DIR with DB rows, mounted at /{owner}/{repo}.git/info/lfs/….

Purpose: Prove Axum LFS routing beside Smart HTTP, body streaming past 2 MiB default, and volume-backed shard layout before auth/quota/UI expansion. Delivers D-LFS-07 basic gate (locked CONTEXT interpretation).
Output: Green lfs_batch happy-path + lfs_store layout tests; dialect migration parity green.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-git-lfs/14-CONTEXT.md
@.planning/phases/14-git-lfs/14-RESEARCH.md
@.planning/phases/08-git-https-pats/08-CONTEXT.md
@crates/octanest-api/src/routes/git_smart_http.rs
@crates/octanest-api/src/app.rs
@crates/octanest-api/src/git/http_backend.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git-lfs client → Axum LFS | Untrusted JSON batch + raw PUT body |
| API → LFS_DIR filesystem | OID must be strict 64-hex before path join |
| API → DB | Refcount/link rows for OID ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Spoofing | git_lfs routes | high | mitigate | Minimal PAT Basic on tracer path; expand in 14-03 (D-LFS-09) |
| T-14-03 | Denial of service | PUT body | high | mitigate | Stream to disk; DefaultBodyLimit disable/raise on LFS PUT; enforce size while hashing |
| T-14-04 | Tampering | store path join | high | mitigate | Reject non-^[a-f0-9]{64}$ OIDs before join (D-LFS-03) |
| T-14-SC | Tampering | crates | medium | accept | No new crates — sha2/tempfile already in workspace |
</threat_model>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (14-02-T1): End-to-end Batch + basic transfer into LFS_DIR</name>
  <reversibility rating="costly">Instance LFS_DIR + OID shard layout + DB tables (D-LFS-01, D-LFS-02, D-LFS-03).</reversibility>
  <files>crates/octanest-db/migrations/sqlite, crates/octanest-db/migrations/postgres, crates/octanest-db/migrations/mysql, crates/octanest-db/src, crates/octanest-api/src/lfs/mod.rs, crates/octanest-api/src/lfs/store.rs, crates/octanest-api/src/lfs/batch.rs, crates/octanest-api/src/routes/git_lfs.rs, crates/octanest-api/src/routes/mod.rs, crates/octanest-api/src/app.rs, crates/octanest-api/src/main.rs, crates/octanest-api/tests/lfs_batch.rs, crates/octanest-api/tests/lfs_store.rs, crates/octanest-db/tests/dialect_lfs.rs</files>
  <read_first>crates/octanest-api/src/routes/git_smart_http.rs, crates/octanest-api/src/app.rs, crates/octanest-api/src/routes/avatar.rs, .planning/phases/14-git-lfs/14-RESEARCH.md</read_first>
  <behavior>
    - POST /{owner}/{repo}.git/info/lfs/objects/batch with operation upload and transfers including basic returns transfer basic and upload href under same repo .git/info/lfs
    - PUT object body streams to {LFS_DIR}/{oid[0:2]}/{oid[2:4]}/{oid}; SHA-256 must match oid or reject
    - GET object returns stored bytes for download batch action
    - dialect_lfs migration parity passes for lfs tables + lfs_enabled column
  </behavior>
  <action>
Implement the tracer vertical slice per D-LFS-01, D-LFS-02, D-LFS-03, D-LFS-05, D-LFS-06, D-LFS-07 (locked: basic gate + streaming PUT; multipart adapter deferred), D-LFS-08: add next-free tri-dialect migration for repositories.lfs_enabled (default false in prod; tests may set true), lfs_objects (oid, size, created_at), lfs_object_links (repository_id, oid, refcount attribution). Wire AppState.lfs_dir from OCTANEST_LFS_DIR (default var/lfs). Create lfs/store with shard path, atomic tempfile+rename put, get stream, verify hash with sha2. Add batch JSON types Accept application/vnd.git-lfs+json. Mount Axum routes sibling to Smart HTTP: objects/batch, objects/{oid} GET/PUT (and stub verify route optional for 14-05). Raise/disable DefaultBodyLimit on LFS PUT; stream body — do not buffer whole OID. For tracer auth, accept valid classic PAT with contents/repo scope sufficient for upload/download on the fixture repo; full cookie/private matrix in 14-03. Green lfs_batch happy path + lfs_store layout + dialect_lfs. Do not implement quotas, GC, UI, or Compose bind (14-04/14-06/14-07). Do not negotiate or require transfer=multipart (D-LFS-07 locked deviation).
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(lfs_batch) | test(lfs_store)' && cargo test -p octanest-db --test dialect_lfs</automated>
  </verify>
  <done>One OID can Batch+PUT+GET end-to-end into OCTANEST_LFS_DIR shard with DB rows</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (14-02-T2): Hash mismatch and path-safety negatives on tracer store</name>
  <files>crates/octanest-api/src/lfs/store.rs, crates/octanest-api/tests/lfs_store.rs</files>
  <read_first>crates/octanest-api/src/lfs/store.rs, crates/octanest-api/tests/lfs_store.rs</read_first>
  <behavior>
    - PUT with body whose SHA-256 differs from oid path → reject and leave no final shard object
    - OID with uppercase or non-hex → reject before filesystem join
  </behavior>
  <action>
Harden store put/get validation per RESEARCH anti-patterns: only lowercase 64-hex OIDs; on hash mismatch delete temp and return LFS error; never write outside LFS_DIR. Green remaining lfs_store negative stubs from Wave 0. Keep transfer=multipart out of scope per locked D-LFS-07.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(lfs_store)'</automated>
  </verify>
  <done>Store rejects bad OID and hash mismatch without corrupting LFS_DIR</done>
</task>

</tasks>

<verification>
Tracer nextest filters green for batch happy path and store layout/safety; dialect_lfs green.
</verification>

<success_criteria>
Users (via API tests) can push and fetch one LFS object over HTTPS basic transfer into volume-backed LFS_DIR.
</success_criteria>

<output>
Create `.planning/phases/14-git-lfs/14-02-SUMMARY.md` when done
</output>
