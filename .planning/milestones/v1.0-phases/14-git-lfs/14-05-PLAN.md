---
phase: 14-git-lfs
plan: "05"
type: execute
wave: 4
depends_on:
  - "04"
files_modified:
  - crates/octanest-api/src/lfs/store.rs
  - crates/octanest-api/src/lfs/batch.rs
  - crates/octanest-api/src/routes/git_lfs.rs
  - crates/octanest-db/src
  - crates/octanest-api/tests/lfs_batch.rs
  - crates/octanest-api/tests/lfs_store.rs
requirements:
  - GIT-12
autonomous: true
user_setup: []
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Instance-wide OID dedup: second repo linking existing OID omits upload actions (D-LFS-02)"
    - "Optional verify endpoint checks size/oid; GET supports Range for resumable download within basic (D-LFS-07 locked resumable-within-basic)"
  artifacts:
    - path: crates/octanest-api/src/routes/git_lfs.rs
      provides: "verify + Range GET + dedup batch responses"
  key_links:
    - from: crates/octanest-api/src/lfs/batch.rs
      to: crates/octanest-db/src
      via: "refcount/link lookup before upload actions"
  assumptions:
    - "LOCKED (D-LFS-07 CONTEXT 2026-09-14): no multipart transfer adapter; this plan completes resumable-within-basic via verify + Range GET (streaming PUT already in 14-02)"
  prohibitions:
    - statement: "Must not require clients to negotiate transfer=multipart for success (D-LFS-07 locked)"
      flagged: true
---

<objective>
Complete dedup/refcount linking, verify endpoint, and Range GET so basic transfer is robust and D-LFS-07 locked resumable-within-basic lands without a multipart adapter.

Purpose: Close D-LFS-02 dedup behavior and D-LFS-07 Range/verify resumability per locked CONTEXT + RESEARCH RESOLVED.
Output: Green lfs_dedup / lfs_verify tests.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-git-lfs/14-CONTEXT.md
@.planning/phases/14-git-lfs/14-RESEARCH.md
@.planning/phases/14-git-lfs/14-04-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → verify | Client-supplied oid/size must match store |
| Cross-repo dedup | Linking requires Write on target repo; never skip ACL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-02 | Elevation of privilege | dedup link | high | mitigate | Require Write on repo before creating link to existing OID |
| T-14-04 | Tampering | verify | medium | mitigate | Re-hash or size check against store before success |
| T-14-SC | Tampering | crates | medium | accept | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (14-05-T1): Global OID dedup + refcount links</name>
  <files>crates/octanest-api/src/lfs/batch.rs, crates/octanest-api/src/lfs/store.rs, crates/octanest-db/src, crates/octanest-api/tests/lfs_batch.rs</files>
  <read_first>crates/octanest-api/src/lfs/batch.rs, .planning/phases/14-git-lfs/14-RESEARCH.md</read_first>
  <behavior>
    - If OID already on disk and linked to this repo, batch upload omits actions
    - If OID exists globally but not linked, Write can link without re-upload (omit upload action) and increment refcount
    - Logical quota accounts for new link size; physical bytes unchanged when OID already stored
  </behavior>
  <action>
Implement D-LFS-02 refcount linking: lfs_object_links per (repository_id, oid); batch upload skips actions when object present; still require authz and enable checks. Green lfs_dedup tests. Coordinate quota accounting with 14-04 logical vs physical rules.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(lfs_dedup)'</automated>
  </verify>
  <done>Dedup omits redundant uploads; refcounts track repo links</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (14-05-T2): Verify endpoint + Range GET (D-LFS-07 locked)</name>
  <files>crates/octanest-api/src/routes/git_lfs.rs, crates/octanest-api/src/lfs/store.rs, crates/octanest-api/tests/lfs_store.rs, crates/octanest-api/tests/lfs_batch.rs</files>
  <read_first>https://github.com/git-lfs/git-lfs/blob/main/docs/api/basic-transfers.md, .planning/phases/14-git-lfs/14-CONTEXT.md</read_first>
  <behavior>
    - POST …/objects/verify validates oid+size against store
    - GET supports HTTP Range for partial content when feasible
    - Stock basic single-PUT path remains the phase gate (no transfer=multipart)
  </behavior>
  <action>
Per locked D-LFS-07 (CONTEXT 2026-09-14 + RESEARCH Open Questions RESOLVED): ship verify action href in batch when appropriate; implement verify handler; support Range on GET downloads for resumable download within basic. Document in code comments that open-source git-lfs multipart transfer adapter remains deferred. Green lfs_verify stubs discoverable under test(lfs_verify). Do not implement tus or transfer=multipart negotiation.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(lfs_verify) | test(lfs_store) | test(lfs_batch)'</automated>
  </verify>
  <done>Verify + Range GET work; basic clients remain fully supported per D-LFS-07</done>
</task>

</tasks>

<verification>
Dedup and verify/Range behaviors covered by nextest.
</verification>

<success_criteria>
Shared OIDs dedup across repos; basic transfer gains verify and resumable GET per locked D-LFS-07 (no multipart adapter).
</success_criteria>

<output>
Create `.planning/phases/14-git-lfs/14-05-SUMMARY.md` when done
</output>
