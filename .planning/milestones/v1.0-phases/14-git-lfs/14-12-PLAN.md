---
phase: 14-git-lfs
plan: "12"
type: execute
wave: 9
depends_on:
  - "06"
  - "07"
  - "10"
  - "11"
files_modified:
  - scripts/smoke-git-lfs.sh
  - Makefile
  - docs/CONFIGURATION.md
  - .planning/phases/14-git-lfs/14-VALIDATION.md
requirements:
  - GIT-12
  - GIT-13
autonomous: true
user_setup: []
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "make smoke-git-lfs exercises git lfs push/pull over Traefik .git/info/lfs when Docker available (GIT-12)"
    - "Phase gate: nextest test(lfs) + dialect_lfs + rpc-sync-check + web LFS Vitest green"
    - "Docs include .gitattributes guidance without server auto-commit (D-LFS-17)"
  artifacts:
    - path: scripts/smoke-git-lfs.sh
      provides: "Compose Traefik LFS client smoke"
  key_links:
    - from: scripts/smoke-git-lfs.sh
      to: docker-compose.yml
      via: "HTTPS remote through Traefik PathRegexp *.git"
  assumptions:
    - "ASSUME: smoke skips exit 0 when Docker/git-lfs unavailable (mirror smoke-git-https)"
  prohibitions:
    - statement: "Must not claim LFS-over-SSH in smoke or docs"
      flagged: true
---

<objective>
Phase gate: real git-lfs client smoke through Traefik, docs polish, VALIDATION task map refresh.

Purpose: Prove GIT-12/13 end-to-end outside unit tests and close the phase checklist.
Output: Working smoke target + gate commands green (or skip-documented).
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-git-lfs/14-CONTEXT.md
@.planning/phases/14-git-lfs/14-VALIDATION.md
@scripts/smoke-git-https.sh
@scripts/smoke-git-lfs.sh
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Smoke → stack | Uses test PAT; must not print secrets |
| Docs → users | Accurate auth story |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Spoofing | smoke | medium | mitigate | PAT Basic only in smoke; no password auth |
| T-14-05 | Information disclosure | smoke logs | medium | mitigate | Redact token values in script output |
| T-14-SC | Tampering | scripts | low | accept | No package installs |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (14-12-T1): smoke-git-lfs push/pull through Traefik</name>
  <files>scripts/smoke-git-lfs.sh, Makefile, docs/CONFIGURATION.md</files>
  <read_first>scripts/smoke-git-https.sh, scripts/smoke-git-lfs.sh, docker-compose.yml</read_first>
  <action>
Implement smoke-git-lfs.sh: create/enable repo, track a binary via git lfs, push and pull over HTTPS remote through Compose Traefik using PAT Basic (D-LFS-05, D-LFS-06, GIT-12). Skip exit 0 when Docker or git-lfs missing. Confirm Makefile target. Ensure docs mention .gitattributes patterns only (D-LFS-17). Do not test LFS-over-SSH.
  </action>
  <verify>
    <automated>test -x scripts/smoke-git-lfs.sh && rg -n 'smoke-git-lfs' Makefile && make smoke-git-lfs</automated>
  </verify>
  <done>Smoke script exists and runs (or skips cleanly) proving client path</done>
</task>

<task type="auto">
  <name>Task 2 (14-12-T2): Phase gate sweep + VALIDATION refresh</name>
  <files>.planning/phases/14-git-lfs/14-VALIDATION.md</files>
  <read_first>.planning/phases/14-git-lfs/14-VALIDATION.md</read_first>
  <action>
Run phase gate: cargo nextest run -p oxidean-api -E 'test(lfs)'; cargo test -p oxidean-db --test dialect_lfs; make rpc-sync-check; bun --cwd apps/web exec vitest run for LFS-related tests; refresh 14-VALIDATION.md per-task status rows to reflect executed results (do not set nyquist_compliant true — owned by validate-phase).
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(lfs)' && cargo test -p oxidean-db --test dialect_lfs && make rpc-sync-check</automated>
  </verify>
  <done>Phase automated gate commands pass; VALIDATION map updated</done>
</task>

</tasks>

<verification>
Smoke + nextest + rpc-sync-check constitute the phase gate for GIT-12/13.
</verification>

<success_criteria>
Users can push/fetch LFS over HTTPS; operators have volume-backed storage; phase validation map is current.
</success_criteria>

<output>
Create `.planning/phases/14-git-lfs/14-12-SUMMARY.md` when done
</output>
