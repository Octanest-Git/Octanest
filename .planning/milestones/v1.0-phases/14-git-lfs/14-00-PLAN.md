---
phase: 14-git-lfs
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/octanest-api/tests/lfs_batch.rs
  - crates/octanest-api/tests/lfs_store.rs
  - crates/octanest-api/tests/factory_reset_scope.rs
  - crates/octanest-db/tests/dialect_lfs.rs
  - scripts/smoke-git-lfs.sh
  - Makefile
requirements:
  - GIT-12
  - GIT-13
autonomous: true
user_setup: []
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Wave 0 discoverable stubs exist for lfs_batch/lfs_store/lfs_enable/lfs_quota/lfs_dedup/lfs_verify/lfs_gc and dialect_lfs (GIT-12, GIT-13)"
    - "factory_reset_scope gains named expectation that LFS_DIR children are wiped (D-LFS-04)"
    - "smoke-git-lfs.sh + make smoke-git-lfs scaffold exist (may exit 0 when Docker absent)"
  artifacts:
    - path: crates/octanest-api/tests/lfs_batch.rs
      provides: "GIT-12 batch/auth/enable/quota/dedup failing stubs"
    - path: crates/octanest-api/tests/lfs_store.rs
      provides: "GIT-13 shard layout + verify/gc named stubs"
    - path: crates/octanest-db/tests/dialect_lfs.rs
      provides: "LFS migration parity stub"
  key_links:
    - from: .planning/phases/14-git-lfs/14-VALIDATION.md
      to: crates/octanest-api/tests/lfs_batch.rs
      via: "Wave 0 Requirements checklist"
  assumptions:
    - "ASSUME: migration id is next free at execute (after 0010_orgs_acl or later if Phases 11–13 land first) — RESEARCH Open Questions RESOLVED item 4"
    - "ASSUME: Wave 0 stubs may #[ignore] or assert!(false) until later plans green them"
  prohibitions:
    - statement: "Must not implement production LFS handlers, migrations, or Compose volume binds in this Wave 0 plan"
      flagged: true
    - statement: "Must not add new crates.io or npm packages"
      flagged: true
---

<objective>
Scaffold Nyquist/Wave 0 failing Rust stubs and smoke scaffold for Phase 14 Git LFS before implementation plans turn them green.

Purpose: Every later `<automated>` verify names a real test path (14-VALIDATION.md Wave 0). Web stubs live in 14-01.
Output: Discoverable RED/ignored nextest filters matching lfs_batch/lfs_store/lfs_enable/lfs_quota/lfs_dedup/lfs_verify/lfs_gc / dialect_lfs, plus smoke script stub.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-git-lfs/14-CONTEXT.md
@.planning/phases/14-git-lfs/14-RESEARCH.md
@.planning/phases/14-git-lfs/14-VALIDATION.md
@crates/octanest-api/tests/git_smart_http.rs
@crates/octanest-api/tests/factory_reset_scope.rs
@crates/octanest-db/tests/dialect_pats.rs
@scripts/smoke-git-https.sh
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → temp DB / LFS_DIR | Parallel nextest must isolate state |
| Stub assertions → production LFS auth | Cookie ignore, PAT Basic, 401/403 encoded before impl |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Spoofing | lfs_batch stubs | high | mitigate | Stub asserts Cookie ignored; PAT Basic only; LFS-Authenticate on 401 (D-LFS-09) |
| T-14-02 | Elevation of privilege | lfs_batch stubs | high | mitigate | Stub asserts Read download / Write upload / Admin enable (D-LFS-09, D-LFS-10, D-LFS-11) |
| T-14-SC | Tampering | cargo/npm installs | high | mitigate | Wave 0 adds test/smoke files only; no new packages |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (14-00-T1): LFS batch/store Wave 0 API stubs</name>
  <files>crates/octanest-api/tests/lfs_batch.rs, crates/octanest-api/tests/lfs_store.rs</files>
  <read_first>.planning/phases/14-git-lfs/14-VALIDATION.md, .planning/phases/14-git-lfs/14-RESEARCH.md, crates/octanest-api/tests/git_smart_http.rs</read_first>
  <action>
Add Wave 0 integration stubs (Phase 08/10 style: compile, discoverable by nextest, intentionally fail or assert!(false, "Wave 0") / #[ignore] until later plans). lfs_batch.rs must name tests matching filters test(lfs_batch), test(lfs_enable), test(lfs_quota), test(lfs_dedup) covering: POST …/info/lfs/objects/batch upload then PUT then GET download happy path (GIT-12); Cookie alone does not auth LFS; private unauth → 401 with LFS-Authenticate Basic; insufficient PAT scope → 403; Write+verified required for upload; disabled repo rejects batch named lfs_enable (D-LFS-09, D-LFS-10, D-LFS-11); over-quota reject named lfs_quota (D-LFS-14); existing OID omits upload actions named lfs_dedup (D-LFS-02). lfs_store.rs must cover OID-sharded path under OCTANEST_LFS_DIR ab/cd/oid (D-LFS-01, D-LFS-03), hash mismatch reject on PUT, plus discoverable stubs named lfs_verify (verify endpoint / size check) and lfs_gc (refcount GC / unreferenced OID) so later plans 14-05/14-06 nextest filters are non-vacuous. Do not implement routes, store, or migrations here.
  </action>
  <verify>
    <automated>test -f crates/octanest-api/tests/lfs_batch.rs && test -f crates/octanest-api/tests/lfs_store.rs && cargo nextest list -p octanest-api -E 'test(lfs)' | rg -q 'lfs' && cargo nextest list -p octanest-api -E 'test(lfs_enable)' | rg -q 'lfs_enable' && cargo nextest list -p octanest-api -E 'test(lfs_verify)' | rg -q 'lfs_verify' && cargo nextest list -p octanest-api -E 'test(lfs_gc)' | rg -q 'lfs_gc'</automated>
  </verify>
  <done>API Wave 0 lfs stubs exist and are discoverable under lfs_batch/lfs_store/lfs_enable/lfs_quota/lfs_dedup/lfs_verify/lfs_gc</done>
</task>

<task type="auto">
  <name>Task 2 (14-00-T2): Dialect + factory reset + smoke scaffolds</name>
  <files>crates/octanest-db/tests/dialect_lfs.rs, crates/octanest-api/tests/factory_reset_scope.rs, scripts/smoke-git-lfs.sh, Makefile</files>
  <read_first>crates/octanest-db/tests/dialect_pats.rs, crates/octanest-api/tests/factory_reset_scope.rs, scripts/smoke-git-https.sh, Makefile</read_first>
  <action>
Add dialect_lfs.rs expecting tri-dialect next-free LFS migration with repositories.lfs_enabled (or equivalent), lfs_objects, and lfs_object_links (refcount) tables — mirror dialect_pats pattern; do not hard-code migration number if Phases 11–13 may ship first (per RESEARCH A3). Extend factory_reset_scope.rs with a named stub/assertion that database_and_repositories wipe also clears OCTANEST_LFS_DIR children while keeping the root (D-LFS-04). Add scripts/smoke-git-lfs.sh that documents Traefik .git/info/lfs push/pull intent and exits 0 when Docker unavailable (mirror smoke-git-https skip pattern); wire make smoke-git-lfs. Do not implement wipe or Compose binds here.
  </action>
  <verify>
    <automated>test -f crates/octanest-db/tests/dialect_lfs.rs && test -f scripts/smoke-git-lfs.sh && cargo nextest list -p octanest-db -E 'test(dialect_lfs)' | rg -q 'dialect_lfs' && rg -n 'smoke-git-lfs' Makefile</automated>
  </verify>
  <done>Dialect, factory-reset, and smoke Wave 0 scaffolds exist</done>
</task>

</tasks>

<verification>
Wave 0 Rust/smoke stubs exist; VALIDATION checklist items for stubs addressable by later plans.
</verification>

<success_criteria>
Phase 14 requirement IDs have failing automated stub targets before green implementation (web stubs in 14-01).
</success_criteria>

<output>
Create `.planning/phases/14-git-lfs/14-00-SUMMARY.md` when done
</output>
