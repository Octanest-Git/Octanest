---
phase: 14-git-lfs
plan: "07"
type: execute
wave: 5
depends_on:
  - "02"
files_modified:
  - docker-compose.yml
  - .env.example
  - docs/CONFIGURATION.md
  - docs/API.md
  - docs/ARCHITECTURE.md
requirements:
  - GIT-13
autonomous: true
user_setup: []
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Compose binds OXIDEAN_LFS_DIR volume like uploads/repos for the API service (D-LFS-01, GIT-13)"
    - "CONFIGURATION/API docs describe LFS env, HTTPS discovery path, PAT auth, and SSH-remote HTTPS LFS credential note (D-LFS-05, D-LFS-09)"
  artifacts:
    - path: docker-compose.yml
      provides: "var/lfs volume bind + OXIDEAN_LFS_DIR"
    - path: docs/CONFIGURATION.md
      provides: "Operator LFS configuration"
  key_links:
    - from: docker-compose.yml
      to: crates/oxidean-api/src/app.rs
      via: "OXIDEAN_LFS_DIR=/var/lfs matches AppState"
  assumptions:
    - "ASSUME: existing Traefik PathRegexp for *.git already covers /info/lfs — document, do not duplicate router"
  prohibitions:
    - statement: "Must not claim LFS-over-SSH is supported in Phase 14"
      flagged: true
---

<objective>
Wire Compose volume + operator documentation for filesystem-backed LFS storage.

Purpose: Make GIT-13 operable in Compose and document contracts for clients/operators.
Output: docker-compose + docs updated for OXIDEAN_LFS_DIR and LFS HTTPS.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-git-lfs/14-CONTEXT.md
@.planning/phases/14-git-lfs/14-RESEARCH.md
@docker-compose.yml
@docs/CONFIGURATION.md
@docs/API.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → container volume | LFS_DIR bind must not expose secrets in docs examples |
| Docs → operators | Must not document cookie auth for LFS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Spoofing | docs/API.md | medium | mitigate | Document PAT Basic only; cookies ignored (D-LFS-09) |
| T-14-SC | Tampering | compose | low | accept | No new packages; volume path only |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (14-07-T1): Compose OXIDEAN_LFS_DIR volume bind</name>
  <reversibility rating="costly">Ops path + backup story for LFS volume (D-LFS-01).</reversibility>
  <files>docker-compose.yml, .env.example</files>
  <read_first>docker-compose.yml, .env.example, crates/oxidean-api/src/app.rs</read_first>
  <action>
Per D-LFS-01 / GIT-13: add ./var/lfs:/var/lfs (or equivalent) volume on api service; set OXIDEAN_LFS_DIR=/var/lfs absolute like REPOS_DIR. Document optional quota env keys in .env.example with safe non-secret defaults. Do not change Traefik .git PathRegexp unless broken — RESEARCH says it already matches info/lfs.
  </action>
  <verify>
    <automated>rg -n 'OXIDEAN_LFS_DIR|/var/lfs' docker-compose.yml .env.example</automated>
  </verify>
  <done>Compose exposes volume-backed LFS_DIR to the API</done>
</task>

<task type="auto">
  <name>Task 2 (14-07-T2): CONFIGURATION + API docs for LFS HTTPS</name>
  <files>docs/CONFIGURATION.md, docs/API.md, docs/ARCHITECTURE.md</files>
  <read_first>docs/CONFIGURATION.md, docs/API.md, .planning/phases/14-git-lfs/14-RESEARCH.md</read_first>
  <action>
Document OXIDEAN_LFS_DIR, max object / quota envs, Admin overrides, Batch+basic under /{owner}/{repo}.git/info/lfs, PAT Basic auth matching Smart HTTP, per-repo enable, and that SSH git remotes still use HTTPS LFS with credential helper (LFS-over-SSH deferred). Include .gitattributes / git lfs track guidance only (D-LFS-17) — no auto-commit. Link from ARCHITECTURE briefly.
  </action>
  <verify>
    <automated>rg -n 'OXIDEAN_LFS_DIR|info/lfs|LFS-Authenticate|personal access token|gitattributes' docs/CONFIGURATION.md docs/API.md</automated>
  </verify>
  <done>Operators and users have documented LFS storage and HTTPS client setup</done>
</task>

</tasks>

<verification>
Compose and docs mention LFS_DIR and HTTPS LFS contracts.
</verification>

<success_criteria>
Operator can configure filesystem LFS storage via Compose/env and docs.
</success_criteria>

<output>
Create `.planning/phases/14-git-lfs/14-07-SUMMARY.md` when done
</output>
