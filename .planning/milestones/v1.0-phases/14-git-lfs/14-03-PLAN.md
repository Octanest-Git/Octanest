---
phase: 14-git-lfs
plan: "03"
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - crates/oxidean-api/src/routes/git_lfs.rs
  - crates/oxidean-api/src/lfs/auth.rs
  - crates/oxidean-api/src/repo
  - crates/oxidean-api/src/rpc.rs
  - crates/oxidean-api/tests/lfs_batch.rs
requirements:
  - GIT-12
autonomous: true
user_setup: []
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "LFS endpoints use PAT Basic only; session Cookie never authorizes LFS (D-LFS-09)"
    - "Download requires Capability Read; upload requires Write + verified email; scopes reuse classic repo / FG contents (D-LFS-09, D-LFS-11)"
    - "Only Admin can enable/disable per-repo LFS; disabled repos reject batch/object with clear LFS JSON error (D-LFS-10)"
  artifacts:
    - path: crates/oxidean-api/src/lfs/auth.rs
      provides: "Shared PAT/ACL helpers mirroring Smart HTTP"
    - path: crates/oxidean-api/src/routes/git_lfs.rs
      provides: "AuthZ gates on batch and object routes"
  key_links:
    - from: crates/oxidean-api/src/routes/git_lfs.rs
      to: crates/oxidean-api/src/routes/git_smart_http.rs
      via: "Reuse authenticate_pat / pat_allows_operation / effective_capability"
  assumptions:
    - "ASSUME: enable RPC registered in rpc.rs here; api-client regeneration deferred to 14-08"
  prohibitions:
    - statement: "Must not authenticate LFS with oxidean_session Cookie"
      flagged: true
    - statement: "Must not invent a dedicated lfs PAT fine-grained permission"
      flagged: true
---

<objective>
Expand LFS tracer with Smart HTTP-parity auth/ACL and Admin-only per-repo enable toggle (session RPC).

Purpose: Close GIT-12 security matrix and D-LFS-09/10/11 before quotas/UI.
Output: Green lfs auth/enable nextest cases.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-git-lfs/14-CONTEXT.md
@.planning/phases/14-git-lfs/14-RESEARCH.md
@.planning/phases/14-git-lfs/14-02-SUMMARY.md
@crates/oxidean-api/src/routes/git_smart_http.rs
@crates/oxidean-api/src/repo/acl.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git-lfs → LFS HTTP | Basic credentials untrusted; Cookie must be ignored |
| Session RPC → enable toggle | Must require Capability Admin |
| Public vs private repo | Unauth private → 401; public download may be anon |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Spoofing | LFS Basic | high | mitigate | PAT hash lookup only; LFS-Authenticate header; ignore Cookie (D-LFS-09) |
| T-14-02 | Elevation of privilege | enable RPC + upload | high | mitigate | Admin for toggle; Write+verified for upload; 403 on insufficient scope (D-LFS-10, D-LFS-11) |
| T-14-SC | Tampering | crates | medium | accept | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (14-03-T1): PAT Basic + ACL on batch and object routes</name>
  <files>crates/oxidean-api/src/lfs/auth.rs, crates/oxidean-api/src/routes/git_lfs.rs, crates/oxidean-api/tests/lfs_batch.rs</files>
  <read_first>crates/oxidean-api/src/routes/git_smart_http.rs, crates/oxidean-api/src/repo/acl.rs, .planning/phases/14-git-lfs/14-RESEARCH.md</read_first>
  <behavior>
    - Cookie-only request treated as anonymous (public download OK if enabled+public; private → 401)
    - Private unauth → 401 with LFS-Authenticate Basic realm
    - Insufficient PAT scope → 403 not 401
    - Upload without Write or unverified email denied mirroring receive-pack
    - Classic repo and FG contents scopes authorize; no new lfs scope
  </behavior>
  <action>
Extract or wrap Smart HTTP PAT auth helpers into lfs/auth.rs per D-LFS-09 and D-LFS-11. Apply on batch and object GET/PUT: ignore Cookie for auth; map download→Capability Read, upload→Write + require_verified equivalent; reuse pat_allows_operation for classic repo / FG contents. Prefer LFS-Authenticate on JSON 401 responses. Green auth-matrix stubs in lfs_batch.rs. Do not implement quotas here.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(lfs)'</automated>
  </verify>
  <done>LFS auth/ACL matrix matches Smart HTTP semantics for Read/Write and cookie ignore</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (14-03-T2): Admin-only per-repo LFS enable + disabled reject</name>
  <files>crates/oxidean-api/src/repo, crates/oxidean-api/src/rpc.rs, crates/oxidean-api/src/routes/git_lfs.rs, crates/oxidean-api/tests/lfs_batch.rs</files>
  <read_first>crates/oxidean-api/src/repo/acl.rs, crates/oxidean-api/src/rpc.rs</read_first>
  <behavior>
    - Session RPC repo.lfs.setEnabled (or equivalent) succeeds only with Capability Admin
    - Non-Admin toggle → permission error
    - When lfs_enabled=false, batch and object routes return consistent LFS JSON error (403 or 404 with clear message)
  </behavior>
  <action>
Implement per-repo enable/disable per D-LFS-10: persist repositories.lfs_enabled; gate all LFS wire routes on enabled; add session RPC for Admin toggle (register in rpc.rs; leave api-client to 14-08). Green lfs_enable / disabled-repo stubs. Settings UI deferred to 14-09.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(lfs_enable) | test(lfs)'</automated>
  </verify>
  <done>Admin can toggle LFS; disabled repos reject LFS protocol consistently</done>
</task>

</tasks>

<verification>
lfs nextest filter green for auth matrix and enable/disable gates.
</verification>

<success_criteria>
PAT Basic LFS auth mirrors Smart HTTP; Admin-only enable works; disabled repos cannot transfer.
</success_criteria>

<output>
Create `.planning/phases/14-git-lfs/14-03-SUMMARY.md` when done
</output>
