---
phase: 14-git-lfs
plan: "10"
type: execute
wave: 7
depends_on:
  - "01"
  - "08"
files_modified:
  - apps/web/src/routes/admin
  - apps/web/src/routes/admin/lfs.integration.test.ts
requirements:
  - GIT-13
autonomous: true
user_setup: []
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Admin UI can view/override instance max object and per-repo/per-user quotas (D-LFS-12, D-LFS-13)"
    - "Admin UI shows instance usage breakdown by repo/user plus physical bytes (D-LFS-19)"
  artifacts:
    - path: apps/web/src/routes/admin
      provides: "Admin LFS quotas/usage surface"
  key_links:
    - from: apps/web/src/routes/admin
      to: packages/api-client
      via: "admin.lfs.getSettings / updateSettings / getUsage"
  assumptions:
    - "ASSUME: place under existing admin settings navigation patterns"
  prohibitions:
    - statement: "Must not author React JSX; use Octane .tsrx"
      flagged: true
---

<objective>
Ship Admin LFS quotas overrides and instance usage breakdown UI.

Purpose: Deliver D-LFS-13/19 operator controls for GIT-13.
Output: Green admin/lfs Vitest + buildable admin route.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-git-lfs/14-CONTEXT.md
@.planning/phases/14-git-lfs/14-08-SUMMARY.md
@apps/web/src/routes/admin
@.agents/skills/octane/SKILL.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → admin.lfs | Server is_admin gate; UI only linked from admin area |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-02 | Elevation of privilege | admin LFS UI | high | mitigate | Route behind existing admin gate; RPC re-checks is_admin |
| T-14-03 | Denial of service | quota form | medium | mitigate | Validate numeric inputs client-side; server enforces |
| T-14-SC | Tampering | npm | medium | accept | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (14-10-T1): Admin quota settings form</name>
  <files>apps/web/src/routes/admin, apps/web/src/routes/admin/lfs.integration.test.ts</files>
  <read_first>.agents/skills/octane/SKILL.md, apps/web/src/routes/admin/auth.tsrx</read_first>
  <behavior>
    - Instance admin can load and save max object / per-repo / per-user quota overrides
    - Non-admin cannot access
  </behavior>
  <action>
Add Admin LFS settings UI per D-LFS-12/D-LFS-13 using admin.lfs.getSettings/updateSettings from api-client. Follow existing admin Octane patterns. Green admin/lfs tests for load/save.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/admin/lfs.integration.test.ts</automated>
  </verify>
  <done>Admin can configure instance LFS limits in UI</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (14-10-T2): Admin instance usage breakdown</name>
  <files>apps/web/src/routes/admin, apps/web/src/routes/admin/lfs.integration.test.ts</files>
  <read_first>apps/web/src/routes/admin</read_first>
  <behavior>
    - Admin sees instance breakdown: by repo, by user, physical bytes, OID count
  </behavior>
  <action>
Wire admin.lfs.getUsage into Admin LFS page per D-LFS-19. Green usage breakdown assertions; ensure bun --cwd apps/web run build still passes when combined with 14-09 (no shared file conflict — admin routes only).
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/admin/lfs.integration.test.ts && bun --cwd apps/web run build</automated>
  </verify>
  <done>Admin LFS usage dashboard shows useful breakdown</done>
</task>

</tasks>

<verification>
admin/lfs Vitest green; web build succeeds.
</verification>

<success_criteria>
Operators can manage LFS quotas and view instance usage in Admin UI.
</success_criteria>

<output>
Create `.planning/phases/14-git-lfs/14-10-SUMMARY.md` when done
</output>
