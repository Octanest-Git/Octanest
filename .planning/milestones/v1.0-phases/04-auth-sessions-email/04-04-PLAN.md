---
phase: 04-auth-sessions-email
plan: "04"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - crates/octanest-api/src/auth/local.rs
  - crates/octanest-api/src/auth/mod.rs
  - crates/octanest-api/src/rpc.rs
  - crates/octanest-api/src/app.rs
  - crates/octanest-api/src/main.rs
  - crates/octanest-api/src/bin/rpc_gen.rs
  - crates/octanest-api/tests/auth_signup.rs
  - crates/octanest-api/tests/auth_session.rs
  - packages/api-client/src/index.ts
  - .env.example
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
autonomous: true
user_setup:
  - service: admin-seed
    why: "Optional first admin before Phase 6 wizard (discretion)"
    env_vars:
      - name: OCTANEST_ADMIN_EMAIL
        source: "Dev/self-host bootstrap email"
      - name: OCTANEST_ADMIN_PASSWORD
        source: "Dev/self-host bootstrap password"
must_haves:
  truths:
    - "User can sign up with email+username+password when provider mode is local (D-01, AUTH-01)"
    - "User can log in with email or username + password; session cookie persists auth.me across requests (D-02, AUTH-02)"
    - "auth.logout deletes current session and clears cookie; auth.logout_all revokes all sessions (D-13, AUTH-03)"
    - "Local signup sends welcome email via EmailSender (D-20); mode≠local rejects signup/login local RPCs"
  artifacts:
    - path: crates/octanest-api/src/auth/local.rs
      provides: "local signup/login"
    - path: crates/octanest-api/tests/auth_signup.rs
      provides: "AUTH-01 integration"
    - path: crates/octanest-api/tests/auth_session.rs
      provides: "AUTH-02/03 cookie round-trip"
  key_links:
    - from: crates/octanest-api/src/app.rs
      to: crates/octanest-api/src/auth/session.rs
      via: "rpc_http reads Cookie + attaches Set-Cookie"
    - from: crates/octanest-api/src/auth/local.rs
      to: crates/octanest-api/src/email/mod.rs
      via: "welcome email on signup"
---

<objective>
Wire local auth end-to-end: `auth.signup|login|logout|logout_all|me|provider_config` RPC, session-aware `rpc_http` with Set-Cookie, welcome email on local signup, optional `OCTANEST_ADMIN_*` seed, rpc-gen client updates, and integration tests for AUTH-01/02/03.

Purpose: Deliver roadmap success criteria 1–2 for local mode (D-04 Rust-native; D-14–16 provider_config for UI).
Output: Working cookie sessions via `/api/rpc`, generated TS client auth namespace, green `auth_signup` + `auth_session` tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-auth-sessions-email/04-01-SUMMARY.md
@.planning/phases/04-auth-sessions-email/04-02-SUMMARY.md
@.planning/phases/04-auth-sessions-email/04-03-SUMMARY.md
@.planning/phases/04-auth-sessions-email/04-RESEARCH.md
@crates/octanest-api/src/rpc.rs
@crates/octanest-api/src/app.rs
@crates/octanest-api/tests/rpc_db_probe.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/rpc auth.* | Untrusted credentials and session cookies |
| ENV admin seed → users.is_admin | Boot-time privilege creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Spoofing | auth.login | mitigate | Generic error `auth.invalid_credentials` with UI copy “Incorrect email/username or password…” — no user enumeration via distinct codes for missing vs bad password |
| T-04-12 | Elevation of privilege | auth.* when mode≠local | mitigate | Server rejects `auth.signup`/`auth.login` unless `provider_mode==local` (Pitfall 9) |
| T-04-13 | Elevation of privilege | OCTANEST_ADMIN_* seed | mitigate | Only when both env set AND `count_users()==0`; create single is_admin user; document Phase 6 owns wizard |
| T-04-14 | Information disclosure | signup duplicate | mitigate | Stable code `auth.taken` for email/username conflict; never leak which field in production message (UI-SPEC allows combined “email or username already taken”) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (04-04-T1): Local provider + session-aware RPC dispatch</name>
  <files>crates/octanest-api/src/auth/local.rs, crates/octanest-api/src/auth/mod.rs, crates/octanest-api/src/rpc.rs, crates/octanest-api/src/app.rs</files>
  <read_first>crates/octanest-api/src/rpc.rs, crates/octanest-api/src/app.rs, crates/octanest-api/src/auth/session.rs, crates/octanest-api/src/email/mod.rs, .planning/phases/04-auth-sessions-email/04-PATTERNS.md</read_first>
  <action>
1. Expand `AppState` to hold `db`, `email: Arc&lt;dyn EmailSender&gt;`, `uploads_dir: PathBuf` (default `var/uploads`), and load auth settings (ENV bootstrap + DB row).
2. Introduce `RpcCtx` (RESEARCH Pattern 1) with `session: Option&lt;ResolvedSession&gt;`, `set_cookie: Option&lt;CookieChange&gt;` (Set or Clear).
3. `auth/local.rs`:
   - signup: validate username (core), normalize email lowercase, password min 8, unique checks, hash_password, insert user (display_name default = username), SessionService::create(remember_me=false), send welcome `OutboundEmail` subject `Welcome to Octanest` text body mentioning username (D-20) — only local signup.
   - login: find by email if identifier contains `@` else by username (D-02); verify_password; reject if password_hash NULL; create session with remember_me flag (D-12).
4. RPC procedures:
   - `auth.signup`, `auth.login`, `auth.logout` (require session; revoke current; clear cookie), `auth.logout_all` (require session; revoke_all; clear cookie), `auth.me` (UserPublic or null/anonymous err `auth.unauthenticated`), `auth.provider_config` (public, no auth).
5. `rpc_http`: parse Cookie header for `octanest_session`, resolve session, dispatch, on response attach `Set-Cookie` when `set_cookie` present. Map auth errors to BAD_REQUEST; unauthenticated may be 401 if you introduce status mapping — prefer consistent JSON err with HTTP 401 for `auth.unauthenticated` and 200/400 for others matching existing style (document choice in SUMMARY). Keep VERSION_HEADER check.
6. Reject local signup/login when settings.provider_mode ≠ Local with code `auth.provider_mismatch`.
7. Own sessions in Rust (`octanest-api` + `sessions` table) only — **reject Better Auth / `@octanejs/better-auth` as session owner (D-10)**; do not add a Node/Better Auth session path alongside these RPCs.
  </action>
  <verify>
    <automated>cargo test -p octanest-api --lib</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'auth.signup' crates/octanest-api/src/rpc.rs`
    - `grep -q 'auth.login' crates/octanest-api/src/rpc.rs`
    - `grep -q 'auth.logout' crates/octanest-api/src/rpc.rs`
    - `grep -q 'auth.logout_all' crates/octanest-api/src/rpc.rs`
    - `grep -q 'auth.me' crates/octanest-api/src/rpc.rs`
    - `grep -q 'auth.provider_config' crates/octanest-api/src/rpc.rs`
    - `grep -q 'octanest_session\|SESSION_COOKIE' crates/octanest-api/src/app.rs`
    - `grep -q 'Welcome to Octanest' crates/octanest-api/src/auth/local.rs`
  </acceptance_criteria>
  <done>Local auth RPC + cookie attachment compile</done>
</task>

<task type="auto">
  <name>Task 2 (04-04-T2): Admin seed, rpc-gen, integration tests AUTH-01/02/03</name>
  <files>crates/octanest-api/src/main.rs, crates/octanest-api/src/bin/rpc_gen.rs, crates/octanest-api/tests/auth_signup.rs, crates/octanest-api/tests/auth_session.rs, packages/api-client/src/index.ts, .env.example</files>
  <read_first>crates/octanest-api/src/main.rs, crates/octanest-api/src/bin/rpc_gen.rs, crates/octanest-api/tests/rpc_db_probe.rs, packages/api-client/src/index.ts, .env.example</read_first>
  <action>
1. After migrate in `main.rs`: if `OCTANEST_ADMIN_EMAIL` and `OCTANEST_ADMIN_PASSWORD` both set and `count_users==0`, create admin user (username `admin` if free else `admin1`; is_admin=true). Log info once. Do not implement Phase 6 wizard.
2. Update `rpc_gen.rs` string template to emit `auth: { signup, login, logout, logoutAll, me, providerConfig }` with types; regenerate `packages/api-client/src/index.ts` (run whatever make/cargo bin the repo uses — typically `cargo run -p octanest-api --bin rpc_gen` or existing make target). Keep `credentials: "include"` default.
3. `.env.example` document: `OCTANEST_ADMIN_EMAIL`, `OCTANEST_ADMIN_PASSWORD`, `OCTANEST_SMTP_URL`, `OCTANEST_RESEND_API_KEY`, `OCTANEST_MAIL_FROM`, `WORKOS_API_KEY`, `WORKOS_CLIENT_ID`, OIDC vars placeholders.
4. `tests/auth_signup.rs` (SQLite tempfile + LogSink): signup → 200 ok + Set-Cookie; duplicate → auth.taken; reserved username → auth.reserved_username; assert welcome path invoked (inject recording EmailSender or assert tracing — prefer `Arc&lt;RecordingSender&gt;` in test AppState if AppState is constructible).
5. `tests/auth_session.rs`: login → Cookie → auth.me returns user; logout → auth.me unauthenticated; second device session + logout_all clears both.
6. Analog harness from `rpc_db_probe.rs` / `rpc_http.rs` with version header.
  </action>
  <verify>
    <automated>cargo test -p octanest-api --test auth_signup --test auth_session</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'OCTANEST_ADMIN_EMAIL' crates/octanest-api/src/main.rs`
    - `grep -q 'auth.signup\|providerConfig\|provider_config' crates/octanest-api/src/bin/rpc_gen.rs packages/api-client/src/index.ts`
    - `grep -q 'credentials' packages/api-client/src/index.ts`
    - `cargo test -p octanest-api --test auth_signup` exits 0
    - `cargo test -p octanest-api --test auth_session` exits 0
    - `grep -q 'OCTANEST_ADMIN_EMAIL' .env.example`
  </acceptance_criteria>
  <done>AUTH-01/02/03 proven by integration tests; client regenerated</done>
</task>

</tasks>

<verification>
- [ ] auth_signup + auth_session green
- [ ] rpc-gen includes auth namespace
- [ ] Welcome email only on local signup
</verification>

<success_criteria>
- Signup/login/logout/me work with HttpOnly cookie sessions
- Admin seed optional path documented
</success_criteria>

<output>
After completion, create `.planning/phases/04-auth-sessions-email/04-04-SUMMARY.md`
</output>
