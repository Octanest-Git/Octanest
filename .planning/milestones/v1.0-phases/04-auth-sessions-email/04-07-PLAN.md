---
phase: 04-auth-sessions-email
plan: "07"
type: execute
wave: 6
depends_on:
  - "04"
  - "06"
files_modified:
  - apps/web/src/components/ui/label.tsx
  - apps/web/src/components/ui/checkbox.tsx
  - apps/web/src/components/ui/textarea.tsx
  - apps/web/src/components/ui/dropdown-menu.tsx
  - apps/web/src/components/chrome.tsx
  - apps/web/src/routes/login.tsx
  - apps/web/src/routes/signup.tsx
  - apps/web/src/routes/dashboard.tsx
  - apps/web/src/routes/index.tsx
  - apps/web/src/styles.css
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
autonomous: true
user_setup: []
must_haves:
  truths:
    - "/login and /signup render mode-exclusive UI from auth.provider_config (D-14, D-16)"
    - "Local signup/login forms call auth.* with credentials include; success redirects per D-15"
    - "Header Sign in/Sign up enabled; signed-in account menu offers Profile, Dashboard, Log out this device (D-17)"
    - "Landing Get started links to /signup; /dashboard thin shell per UI-SPEC"
  artifacts:
    - path: apps/web/src/routes/login.tsx
      provides: "Sign in page"
    - path: apps/web/src/routes/signup.tsx
      provides: "Sign up page"
    - path: apps/web/src/routes/dashboard.tsx
      provides: "Thin signed-in home"
    - path: apps/web/src/components/chrome.tsx
      provides: "Auth header affordances"
  key_links:
    - from: apps/web/src/routes/login.tsx
      to: packages/api-client/src/index.ts
      via: "auth.login / auth.providerConfig"
    - from: apps/web/src/components/chrome.tsx
      to: apps/web/src/routes/login.tsx
      via: "Sign in navigation"
---

<objective>
Ship auth UI shell: shadcn Label/Checkbox/Textarea/Dropdown Menu, `/login` `/signup` `/dashboard`, enable header + landing CTAs, signed-in account menu with this-device logout — following 04-UI-SPEC copy and layout exactly.

Purpose: AUTH-01/02/03 user-visible success criteria; D-14–D-17.
Output: Mode-exclusive auth pages + chrome wired to generated client.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-auth-sessions-email/04-UI-SPEC.md
@.planning/phases/04-auth-sessions-email/04-06-SUMMARY.md
@apps/web/src/components/chrome.tsx
@apps/web/src/routes/status.tsx
@apps/web/components.json
@packages/api-client/src/index.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form inputs → auth RPC | XSS if unsafely rendered error/HTML |
| returnTo query → redirect | Open redirect risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-23 | Spoofing | returnTo redirect | mitigate | Only allow same-origin relative paths starting with `/` and not `//`; reject `http:`, `https:`, `javascript:`; if `/` or homepage → `/dashboard` (D-15) |
| T-04-24 | Injection XSS | error banners | mitigate | Render error strings as text nodes only; no `dangerouslySetInnerHTML`; use UI-SPEC copy for generic login errors |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (04-07-T1): shadcn form primitives + auth pages</name>
  <files>apps/web/src/components/ui/label.tsx, apps/web/src/components/ui/checkbox.tsx, apps/web/src/components/ui/textarea.tsx, apps/web/src/components/ui/dropdown-menu.tsx, apps/web/src/routes/login.tsx, apps/web/src/routes/signup.tsx, apps/web/src/routes/dashboard.tsx, apps/web/src/styles.css</files>
  <read_first>apps/web/components.json, apps/web/src/routes/status.tsx, apps/web/src/components/ui/button.tsx, apps/web/src/components/ui/input.tsx, .planning/phases/04-auth-sessions-email/04-UI-SPEC.md</read_first>
  <action>
1. Add official shadcn Base UI components only: `label`, `checkbox`, `textarea`, `dropdown-menu` (preset base-nova). Match `h-11` / ≥44px touch targets; weights 400/600 only.
2. Shared auth column: max-w-md, OctanestMark size={48}, Heading title, Body muted support, optional auth panel enter motion (opacity+translateY 8px, 200–300ms) with `prefers-reduced-motion` disable.
3. `/login` (`title: Sign in · Octanest`):
   - Fetch `auth.providerConfig`
   - **local**: fields Email or username, Password, Remember me (default unchecked), CTA **Sign in**; errors use exact UI-SPEC generic login copy; cross-link to signup
   - **workos**: CTA **Continue with WorkOS** → `window.location` to `/api/auth/workos/start?returnTo=...`
   - **oidc**: CTA **Continue with SSO** → `/api/auth/oidc/start?...`
   - Never show local fields when mode ≠ local; never show both SSO CTAs
4. `/signup`: local fields Email, Username (+ helper), Password, Confirm password (client mismatch); CTA **Create account**; SSO modes per UI-SPEC titles/bodies; no forgot/verify copy (D-21)
5. On success: redirect via D-15 helper
6. If `auth.me` already signed in on login/signup → redirect dashboard/returnTo
7. `/dashboard`: greeting `Welcome, {displayName||@username}`; body exact UI-SPEC; links Profile/Status/(Auth settings if admin); incomplete profile banner if `profile_incomplete`; redirect anonymous to `/login?returnTo=/dashboard`
8. Copy strings must match UI-SPEC Copywriting Contract verbatim for titles, CTAs, helpers, errors.
9. Do not import or wire Better Auth / `@octanejs/better-auth` on auth pages — Octanest owns sessions in Rust (D-10); keep the better-auth exclusion AC below.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/routes/login.tsx`
    - `test -f apps/web/src/routes/signup.tsx`
    - `test -f apps/web/src/routes/dashboard.tsx`
    - `test -f apps/web/src/components/ui/checkbox.tsx`
    - `test -f apps/web/src/components/ui/dropdown-menu.tsx`
    - `grep -q 'Sign in · Octanest' apps/web/src/routes/login.tsx`
    - `grep -q 'Continue with WorkOS' apps/web/src/routes/login.tsx`
    - `grep -q 'Continue with SSO' apps/web/src/routes/login.tsx`
    - `grep -q 'Remember me' apps/web/src/routes/login.tsx`
    - `grep -q 'Create account' apps/web/src/routes/signup.tsx`
    - `grep -q 'You.re signed in. Profile and admin tools' apps/web/src/routes/dashboard.tsx`
    - `grep -v '^#' apps/web/src/routes/login.tsx apps/web/src/routes/signup.tsx | grep -ci 'forgot password\\|better-auth\\|verify email' | grep -q '^0$'`
  </acceptance_criteria>
  <done>Auth routes build with mode-exclusive UI and UI-SPEC copy</done>
</task>

<task type="auto">
  <name>Task 2 (04-07-T2): Chrome account menu + landing Get started</name>
  <files>apps/web/src/components/chrome.tsx, apps/web/src/routes/index.tsx</files>
  <read_first>apps/web/src/components/chrome.tsx, apps/web/src/routes/index.tsx, packages/api-client/src/index.ts, .planning/phases/04-auth-sessions-email/04-UI-SPEC.md</read_first>
  <action>
1. Replace disabled Sign in/Sign up (remove `title="Coming soon"` / disabled) with links/buttons to `/login` and `/signup` (ghost / secondary per UI-SPEC).
2. When `auth.me` returns user: replace with Dropdown Menu — trigger ≥44px; avatar 28×28 squircle or initials; `@username` visible md+; `aria-label="Account menu"` when username hidden; items: Profile → `/settings/profile`, Dashboard → `/dashboard`, Auth settings → `/admin/auth` if admin, separator, **Log out** calling `auth.logout` then navigate home/login (this device only — D-13).
3. ThemeSelect placement unchanged (before account group).
4. Landing: enable **Get started** → `/signup` (hero + closing); drop Coming soon.
5. Keep SW `/api/` bypass untouched.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v 'Coming soon' apps/web/src/components/chrome.tsx | grep -q 'Sign in'`
    - `grep -q '/login' apps/web/src/components/chrome.tsx`
    - `grep -q '/signup' apps/web/src/components/chrome.tsx`
    - `grep -q 'logout\\|Log out' apps/web/src/components/chrome.tsx`
    - `grep -q 'Account menu' apps/web/src/components/chrome.tsx`
    - `grep -q '/signup' apps/web/src/routes/index.tsx`
    - `grep -v '^#' apps/web/src/routes/index.tsx | grep -c 'Coming soon' | grep -q '^0$'`
  </acceptance_criteria>
  <done>Header and landing auth CTAs live; logout this device from menu</done>
</task>

</tasks>

<verification>
- [ ] `bun run build` in apps/web succeeds
- [ ] No verify/forgot-password UI
- [ ] Mode-exclusive branches present
</verification>

<success_criteria>
- Users can reach signup/login/dashboard from chrome
- AUTH-01/02/03 UI paths exist for local mode
</success_criteria>

<output>
After completion, create `.planning/phases/04-auth-sessions-email/04-07-SUMMARY.md`
</output>
