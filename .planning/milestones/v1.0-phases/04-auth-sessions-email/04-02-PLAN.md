---
phase: 04-auth-sessions-email
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/octanest-api/Cargo.toml
  - crates/octanest-api/src/email/mod.rs
  - crates/octanest-api/src/email/log_sink.rs
  - crates/octanest-api/src/email/smtp.rs
  - crates/octanest-api/src/email/resend.rs
  - crates/octanest-api/src/lib.rs
requirements:
  - AUTH-09
  - AUTH-10
  - AUTH-11
autonomous: true
user_setup:
  - service: smtp
    why: "Optional live SMTP delivery (AUTH-10); CI uses log sink / mock"
    env_vars:
      - name: OCTANEST_SMTP_URL
        source: "Operator SMTP URL e.g. smtp://user:pass@host:587 (lettre from_url)"
      - name: OCTANEST_MAIL_FROM
        source: "From address e.g. Octanest <noreply@example.com>"
  - service: resend
    why: "Optional live Resend delivery (AUTH-11); CI uses wiremock"
    env_vars:
      - name: OCTANEST_RESEND_API_KEY
        source: "Resend Dashboard → API Keys"
must_haves:
  truths:
    - "With no email provider configured, send() logs via target octanest.mail and performs no network I/O (AUTH-09)"
    - "SMTP adapter builds/sends via lettre AsyncSmtpTransport when OCTANEST_SMTP_URL is set (AUTH-10)"
    - "Resend adapter POSTs to https://api.resend.com/emails with Bearer auth and User-Agent octanest-api/0.1 (AUTH-11)"
  artifacts:
    - path: crates/octanest-api/src/email/mod.rs
      provides: "EmailSender trait + OutboundEmail + build_email_sender()"
    - path: crates/octanest-api/src/email/log_sink.rs
      provides: "LogSink EmailSender"
    - path: crates/octanest-api/src/email/smtp.rs
      provides: "SmtpSender"
    - path: crates/octanest-api/src/email/resend.rs
      provides: "ResendSender"
  key_links:
    - from: crates/octanest-api/src/email/mod.rs
      to: crates/octanest-api/src/email/log_sink.rs
      via: "default when no SMTP/Resend env"
    - from: crates/octanest-api/src/email/resend.rs
      to: "https://api.resend.com/emails"
      via: "reqwest POST with User-Agent"
---

<objective>
Implement the `EmailSender` trait with log-sink, SMTP (`lettre`), and Resend (`reqwest`) adapters so local signup can exercise outbound mail without Phase 5 verify/reset flows.

Purpose: AUTH-09/10/11; D-19. Welcome send is wired in 04-04 (D-20).
Output: `crates/octanest-api/src/email/*` with unit tests proving log sink, SMTP message construction, and Resend headers (User-Agent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-auth-sessions-email/04-CONTEXT.md
@.planning/phases/04-auth-sessions-email/04-RESEARCH.md
@crates/octanest-api/Cargo.toml
@crates/octanest-api/src/lib.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → SMTP / Resend | Outbound email crosses to third-party networks with credentials from ENV |
| Operator ENV → EmailSender | Secrets `OCTANEST_SMTP_URL`, `OCTANEST_RESEND_API_KEY` never logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information disclosure | email logs / errors | mitigate | LogSink logs to/subject/body only via `tracing::info!(target: "octanest.mail", ...)`; never log SMTP URL password or Resend API key; redact in error strings |
| T-04-05 | Tampering | email header injection | mitigate | Use lettre typed `Mailbox` parsing for To/From; reject invalid addresses before send; Resend JSON uses structured fields not raw headers |
| T-04-06 | Spoofing | From address | accept | Operator configures From via ENV/settings; Phase 4 does not SPF/DKIM — ops concern |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (04-02-T1): EmailSender trait + LogSink (AUTH-09)</name>
  <files>crates/octanest-api/Cargo.toml, crates/octanest-api/src/email/mod.rs, crates/octanest-api/src/email/log_sink.rs, crates/octanest-api/src/lib.rs</files>
  <read_first>crates/octanest-api/Cargo.toml, crates/octanest-api/src/lib.rs, .planning/phases/04-auth-sessions-email/04-RESEARCH.md</read_first>
  <action>
1. Add dependencies to `octanest-api` (versions per RESEARCH): `lettre` with features `tokio1,tokio1-rustls,smtp-transport,builder,hostname`; `reqwest` with `rustls-tls` + `json`; `async-trait` if not already present; `thiserror` 2.x.
2. Create `email/mod.rs`:
```rust
pub struct OutboundEmail {
    pub to: String,
    pub subject: String,
    pub text: String,
    pub html: Option&lt;String&gt;,
}

#[async_trait::async_trait]
pub trait EmailSender: Send + Sync {
    async fn send(&amp;self, msg: OutboundEmail) -&gt; Result&lt;(), EmailError&gt;;
}

pub fn build_email_sender_from_env() -&gt; Arc&lt;dyn EmailSender&gt; {
    // Prefer Resend if OCTANEST_RESEND_API_KEY set; else SMTP if OCTANEST_SMTP_URL set; else LogSink
    // from: OCTANEST_MAIL_FROM default "Octanest &lt;noreply@localhost&gt;"
}
```
3. `log_sink.rs`: implement `EmailSender` with `tracing::info!(target: "octanest.mail", to=%msg.to, subject=%msg.subject, body=%msg.text, "outbound email (log sink)")` — no network.
4. `pub mod email;` in `lib.rs`.
5. Unit test `email::log_sink::tests::send_logs_without_network` that constructs LogSink and `send()` succeeds.
  </action>
  <verify>
    <automated>cargo test -p octanest-api --lib email::</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'trait EmailSender' crates/octanest-api/src/email/mod.rs`
    - `grep -q 'octanest.mail' crates/octanest-api/src/email/log_sink.rs`
    - `grep -q 'pub mod email' crates/octanest-api/src/lib.rs`
    - `cargo test -p octanest-api --lib email::` exits 0
  </acceptance_criteria>
  <done>Unconfigured mail uses log sink only (AUTH-09)</done>
</task>

<task type="auto">
  <name>Task 2 (04-02-T2): SMTP + Resend adapters (AUTH-10, AUTH-11)</name>
  <files>crates/octanest-api/src/email/smtp.rs, crates/octanest-api/src/email/resend.rs, crates/octanest-api/src/email/mod.rs, crates/octanest-api/Cargo.toml</files>
  <read_first>crates/octanest-api/src/email/mod.rs, .planning/phases/04-auth-sessions-email/04-RESEARCH.md</read_first>
  <action>
1. `smtp.rs`: `SmtpSender { from: String, transport URL }` using `AsyncSmtpTransport::&lt;Tokio1Executor&gt;::from_url` + `Message::builder()` per RESEARCH lettre example. Parse addresses with lettre — invalid To returns `EmailError::InvalidAddress`.
2. `resend.rs`: `ResendSender { api_key, from }` — POST `https://api.resend.com/emails` with headers:
   - `Authorization: Bearer {api_key}`
   - `User-Agent: octanest-api/0.1` (REQUIRED — Resend error 1010 without it)
   - JSON body `{ "from", "to": [to], "subject", "html" or "text" }`
3. Wire `build_email_sender_from_env` selection order: Resend key → SMTP URL → LogSink.
4. Unit tests:
   - `smtp_builds_message` — construct Message with valid from/to (may use lettre without connecting; or mock — if live connect avoided, test address validation Err on `"not-an-email"`).
   - `resend_includes_user_agent` — use `wiremock` or `httpmock` as **dev-dependency** to assert request has `User-Agent` containing `octanest-api` and Authorization Bearer. Add `wiremock` to `[dev-dependencies]` if chosen.
5. Do **not** implement verify/reset templates (D-21).
  </action>
  <verify>
    <automated>cargo test -p octanest-api --lib email::</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'AsyncSmtpTransport' crates/octanest-api/src/email/smtp.rs`
    - `grep -q 'api.resend.com/emails' crates/octanest-api/src/email/resend.rs`
    - `grep -q 'User-Agent' crates/octanest-api/src/email/resend.rs`
    - `grep -q 'octanest-api/0.1' crates/octanest-api/src/email/resend.rs`
    - `cargo test -p octanest-api --lib email::` exits 0
  </acceptance_criteria>
  <done>SMTP and Resend adapters exist with User-Agent and typed addresses</done>
</task>

</tasks>

<verification>
- [ ] `cargo test -p octanest-api --lib email::` covers log_sink + resend UA + smtp validation
- [ ] No verify/reset email templates introduced
</verification>

<success_criteria>
- AUTH-09 default log sink
- AUTH-10 SMTP path available
- AUTH-11 Resend path with User-Agent
</success_criteria>

<output>
After completion, create `.planning/phases/04-auth-sessions-email/04-02-SUMMARY.md`
</output>
