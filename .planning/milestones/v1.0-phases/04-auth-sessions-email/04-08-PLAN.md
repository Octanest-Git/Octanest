---
phase: 04-auth-sessions-email
plan: "08"
type: execute
wave: 7
depends_on:
  - "06"
  - "07"
files_modified:
  - apps/web/src/routes/settings/profile.tsx
  - apps/web/src/routes/admin/auth.tsx
  - apps/web/src/components/avatar-preview.tsx
  - apps/web/public/sw.js
requirements:
  - AUTH-08
  - AUTH-09
  - AUTH-10
  - AUTH-11
autonomous: false
user_setup: []
must_haves:
  truths:
    - "Signed-in user can view/edit display name, username, bio, and upload avatar on /settings/profile (AUTH-08, D-18)"
    - "Profile offers Log out (this device) and Log out all devices with confirm copy from UI-SPEC (D-13)"
    - "Admins manage provider mode and email delivery on /admin/auth; secrets shown as Configured via ENV only (D-09)"
    - "Non-admin hitting /admin/auth sees forbidden messaging"
  artifacts:
    - path: apps/web/src/routes/settings/profile.tsx
      provides: "Profile settings page"
    - path: apps/web/src/routes/admin/auth.tsx
      provides: "Admin auth settings page"
    - path: apps/web/src/components/avatar-preview.tsx
      provides: "Squircle avatar preview"
  key_links:
    - from: apps/web/src/routes/settings/profile.tsx
      to: "/api/user/avatar"
      via: "multipart fetch credentials include"
    - from: apps/web/src/routes/admin/auth.tsx
      to: packages/api-client/src/index.ts
      via: "admin.auth.getSettings/updateSettings"
---

<objective>
Finish Phase 4 UI: `/settings/profile` (fields, avatar, logout-all) and `/admin/auth` (provider + email selects with ENV badges), plus a human verification checkpoint for the four roadmap success criteria.

Purpose: AUTH-08 user-visible; D-09/D-13/D-18; operator email provider UI for AUTH-09/10/11.
Output: Profile + admin routes matching UI-SPEC; checkpoint confirms end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-auth-sessions-email/04-UI-SPEC.md
@.planning/phases/04-auth-sessions-email/04-07-SUMMARY.md
@.planning/phases/04-auth-sessions-email/04-06-SUMMARY.md
@apps/web/src/routes/dashboard.tsx
@apps/web/src/components/chrome.tsx
@apps/web/public/sw.js
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → admin.auth UI | Only admins must reach controls; UI is not the security boundary (API enforces) |
| File picker → avatar POST | Malicious files handled by API limits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-25 | Elevation of privilege | /admin/auth UI | mitigate | Client redirects non-admin to dashboard/forbidden copy; API still returns admin.forbidden — never trust UI alone |
| T-04-26 | Information disclosure | admin form | mitigate | No password-style inputs for SMTP/Resend/WorkOS/OIDC secrets — show **Configured via ENV** badges only (UI-SPEC) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (04-08-T1): Profile page + avatar preview</name>
  <files>apps/web/src/routes/settings/profile.tsx, apps/web/src/components/avatar-preview.tsx</files>
  <read_first>apps/web/src/routes/dashboard.tsx, apps/web/src/components/oxidean-mark.tsx, apps/web/src/components/ui/input.tsx, packages/api-client/src/index.ts, .planning/phases/04-auth-sessions-email/04-UI-SPEC.md</read_first>
  <action>
1. Route `/settings/profile`, title `Profile · Oxidean`, max-w-xl column.
2. `AvatarPreview` 64×64 squircle (reuse `oxidean-squircle`); **Change avatar** opens file picker; helper “JPEG, PNG, or WebP · max 2 MB”; upload via `fetch('/api/user/avatar', { method:'POST', credentials:'include', body: formData })`.
3. Fields: Display name, Username, Bio (Textarea rows~4, max 160, helper “Optional · up to 160 characters”); **Save profile** → `user.updateProfile`; success “Profile saved.”
4. Separator; **Log out** ghost → `auth.logout`; **Log out all devices** destructive with confirm dialog exact UI-SPEC copy (“Sign out everywhere?…”) actions **Log out all devices** / **Stay signed in** → `auth.logoutAll`.
5. Anonymous → redirect `/login?returnTo=/settings/profile`.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/routes/settings/profile.tsx`
    - `grep -q 'Profile · Oxidean' apps/web/src/routes/settings/profile.tsx`
    - `grep -q 'Change avatar' apps/web/src/routes/settings/profile.tsx`
    - `grep -q 'Log out all devices' apps/web/src/routes/settings/profile.tsx`
    - `grep -q 'Stay signed in' apps/web/src/routes/settings/profile.tsx`
    - `grep -q '/api/user/avatar' apps/web/src/routes/settings/profile.tsx`
    - `grep -q '160' apps/web/src/routes/settings/profile.tsx`
  </acceptance_criteria>
  <done>Profile edit + avatar + logout-all UI complete</done>
</task>

<task type="auto">
  <name>Task 2 (04-08-T2): Admin auth settings page</name>
  <files>apps/web/src/routes/admin/auth.tsx, apps/web/public/sw.js</files>
  <read_first>apps/web/src/components/ui/select.tsx, packages/api-client/src/index.ts, apps/web/public/sw.js, .planning/phases/04-auth-sessions-email/04-UI-SPEC.md</read_first>
  <action>
1. Route `/admin/auth`, title `Auth settings · Oxidean`, max-w-2xl.
2. Gate: unauthenticated → login redirect; authenticated non-admin → Body “You need admin access to manage auth settings.” (or redirect dashboard).
3. Support copy exact: “Instance provider mode and email delivery. Secrets stay in environment variables.”
4. Selects: Auth provider Local/WorkOS/OIDC; Email delivery Log sink/SMTP/Resend; mode-exclusive non-secret fields; ENV badges **Configured via ENV** when flags true.
5. **Save auth settings** → admin.auth.updateSettings; success “Auth settings saved.”
6. Confirm `sw.js` still bypasses any pathname starting with `/api/` (do not narrow); if `/uploads/` caching added, keep network-first — default leave uploads uncached or short cache, never cache `/api/*`.
7. No verify/reset UI; no secret paste fields.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/routes/admin/auth.tsx`
    - `grep -q 'Auth settings · Oxidean' apps/web/src/routes/admin/auth.tsx`
    - `grep -q 'Configured via ENV' apps/web/src/routes/admin/auth.tsx`
    - `grep -q 'Save auth settings' apps/web/src/routes/admin/auth.tsx`
    - `grep -q 'You need admin access' apps/web/src/routes/admin/auth.tsx`
    - `grep -q 'pathname.startsWith(\"/api/\")\\|startsWith(\"/api/\")' apps/web/public/sw.js`
  </acceptance_criteria>
  <done>Admin auth settings UI matches UI-SPEC without secret inputs</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (04-08-T3): Human verify Phase 4 success criteria</name>
  <files>.planning/phases/04-auth-sessions-email/04-UI-SPEC.md</files>
  <action>
Pause for human UAT of Phase 4 success criteria after Tasks 1–2. Do not change code unless the human reports issues. Present the how-to-verify steps below and wait for "approved" or a defect list.
  </action>
  <what-built>
    Full Phase 4 auth sessions + email stack: local signup/login/logout, cookie sessions, profile/avatar, admin auth settings, email log-sink (and SMTP/Resend when configured), WorkOS/OIDC routes for non-local modes.
  </what-built>
  <how-to-verify>
    1. Start stack (`make up` or documented Compose/dev). Ensure provider mode is **local**.
    2. Open `/signup` — create account with email+username+password; confirm redirect to `/dashboard` (or returnTo).
    3. Refresh browser — still signed in (header account menu). Log out from menu — session cleared.
    4. Sign in again; open `/settings/profile` — edit display name/bio; upload small PNG avatar; save; confirm preview updates.
    5. With no SMTP/Resend env: signup welcome appears in API logs (`oxidean.mail`). Optionally set SMTP or Resend and confirm adapter path (or trust unit tests if keys absent).
    6. Spot-check light/dark on `/login` and `/signup`; confirm no “Forgot password” / verify UI.
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint: awaiting human approval for Phase 4 UAT"</automated>
  </verify>
  <resume-signal>Type "approved" or list issues</resume-signal>
  <done>Human confirms four ROADMAP success criteria</done>
</task>

</tasks>

<verification>
- [ ] Profile and admin routes build
- [ ] Checkpoint approved
- [ ] SW still bypasses /api/
</verification>

<success_criteria>
- AUTH-08 UI complete
- Admin can select email delivery mode (AUTH-09/10/11 operator surface)
- Phase 4 UAT checkpoint passed
</success_criteria>

<output>
After completion, create `.planning/phases/04-auth-sessions-email/04-08-SUMMARY.md`
</output>
