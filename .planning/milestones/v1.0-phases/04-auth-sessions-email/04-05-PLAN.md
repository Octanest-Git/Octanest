---
phase: 04-auth-sessions-email
plan: "05"
type: execute
wave: 4
depends_on:
  - "04"
files_modified:
  - crates/octanest-api/Cargo.toml
  - crates/octanest-api/src/auth/workos.rs
  - crates/octanest-api/src/auth/oidc.rs
  - crates/octanest-api/src/auth/mod.rs
  - crates/octanest-api/src/routes/auth_callbacks.rs
  - crates/octanest-api/src/routes/mod.rs
  - crates/octanest-api/src/app.rs
  - crates/octanest-api/src/rpc.rs
requirements:
  - AUTH-02
autonomous: true
user_setup:
  - service: workos
    why: "Live WorkOS AuthKit E2E optional; CI uses unit tests/mocks"
    env_vars:
      - name: WORKOS_API_KEY
        source: "WorkOS Dashboard → API Keys"
      - name: WORKOS_CLIENT_ID
        source: "WorkOS Dashboard → AuthKit"
  - service: oidc
    why: "Generic OIDC IdP for enterprise SSO"
    env_vars:
      - name: OCTANEST_OIDC_ISSUER
        source: "IdP issuer URL (https)"
      - name: OCTANEST_OIDC_CLIENT_ID
        source: "OIDC client id"
      - name: OCTANEST_OIDC_CLIENT_SECRET
        source: "OIDC client secret"
must_haves:
  truths:
    - "When mode=workos, /api/auth/workos/start redirects to WorkOS AuthKit; callback exchanges code and mints Octanest session cookie (D-07)"
    - "When mode=oidc, /api/auth/oidc/start begins auth-code+PKCE; callback verifies ID token and mints Octanest session (D-08)"
    - "External identity links via auth_identities; same SessionService as local (D-05)"
    - "Local signup/login remain rejected when mode is workos or oidc"
  artifacts:
    - path: crates/octanest-api/src/auth/workos.rs
      provides: "WorkOS AuthKit start + authenticate_with_code"
    - path: crates/octanest-api/src/auth/oidc.rs
      provides: "OIDC discover + PKCE + code exchange"
    - path: crates/octanest-api/src/routes/auth_callbacks.rs
      provides: "HTTP start/callback routes"
  key_links:
    - from: crates/octanest-api/src/routes/auth_callbacks.rs
      to: crates/octanest-api/src/auth/session.rs
      via: "mint session after external finish"
    - from: crates/octanest-api/src/auth/workos.rs
      to: workos
      via: "user_management().authenticate_with_code"
---

<objective>
Add WorkOS (official `workos` crate AuthKit) and generic OIDC (`openidconnect`) provider adapters with HTTP start/callback routes that mint the same Octanest session cookie as local login.

Purpose: D-05/D-07/D-08 enterprise SSO paths; mode-exclusive UI in later plans reads `auth.provider_config`.
Output: `/api/auth/workos/{start,callback}` and `/api/auth/oidc/{start,callback}`; identity linking; provider unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-auth-sessions-email/04-04-SUMMARY.md
@.planning/phases/04-auth-sessions-email/04-RESEARCH.md
@crates/octanest-api/src/auth/session.rs
@crates/octanest-api/src/app.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → WorkOS / OIDC IdP → `/api/auth/*/callback` | Authorization codes and state/PKCE verifiers cross external IdP boundary |
| Admin-configured OIDC issuer URL → discovery HTTP | SSRF risk if issuer is attacker-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Spoofing | OIDC/WorkOS callback | mitigate | Validate `state` (and OIDC `nonce`) against server-side pending auth store (in-memory map keyed by state, TTL 10m); reject missing/mismatch; use PKCE |
| T-04-16 | Spoofing / SSRF | OIDC issuer discovery | mitigate | Require `https://` issuer; reject hosts `localhost`, `127.0.0.1`, `169.254.0.0/16`, `10.0.0.0/8`, `metadata` — document residual risk if admin is compromised (accept residual for ASVS L1 with admin-only settings) |
| T-04-17 | Elevation of privilege | WorkOS sealed session | mitigate | Do **not** use WorkOS sealed cookies as app session — always mint Octanest `sessions` row (D-07) |
| T-04-18 | Information disclosure | IdP errors | mitigate | Log details server-side; browser redirect to `/login?error=sso` with generic message |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (04-05-T1): WorkOS AuthKit adapter + routes</name>
  <files>crates/octanest-api/Cargo.toml, crates/octanest-api/src/auth/workos.rs, crates/octanest-api/src/routes/auth_callbacks.rs, crates/octanest-api/src/routes/mod.rs, crates/octanest-api/src/app.rs, crates/octanest-api/src/auth/mod.rs</files>
  <read_first>crates/octanest-api/src/app.rs, crates/octanest-api/src/auth/mod.rs, crates/octanest-api/Cargo.toml, .planning/phases/04-auth-sessions-email/04-RESEARCH.md</read_first>
  <action>
1. Add `workos = "3.4"` dependency. Ensure rustc ≥1.88 (document in SUMMARY if toolchain pin needed).
2. `workos.rs`:
   - `start(return_to: &amp;str) -&gt; Result&lt;Url, AuthError&gt;` — AuthKit authorization URL with `provider=authkit` + PKCE; stash verifier/state/`return_to` in pending map.
   - `finish(code, state) -&gt; Result&lt;ExternalIdentity, AuthError&gt;` — `Client::builder().api_key().client_id().build()` then `user_management().authenticate_with_code(AuthenticateWithCodeParams { code, ..Default::default() })`; map WorkOS user id → `provider_subject`, email claim.
3. After finish: find or create user via `auth_identities` (provider=`workos`); if new user, derive username from email local-part + collision suffix; if invalid/reserved set `profile_incomplete` path (username placeholder that forces completion — e.g. store temp unique `u{shortid}` and flag profile_incomplete=true on UserPublic).
4. Mint SessionService session (remember_me=false default for SSO); Set-Cookie; redirect to safe `return_to` or `/dashboard` (D-15).
5. Mount `GET /api/auth/workos/start` and `GET /api/auth/workos/callback` in `app.rs` router.
6. Unit test: mode gate — when settings force workos, `auth.signup` still rejected (may live in rpc test); workos start URL contains expected host or query `provider=authkit` when keys present — if keys absent, test that start returns `auth.not_configured`.
7. Do not add WorkOS AuthKit JS widget (UI-SPEC).
  </action>
  <verify>
    <automated>cargo test -p octanest-api --lib auth::workos</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'workos' crates/octanest-api/Cargo.toml`
    - `grep -q 'authenticate_with_code' crates/octanest-api/src/auth/workos.rs`
    - `grep -q '/api/auth/workos/start' crates/octanest-api/src/app.rs crates/octanest-api/src/routes/auth_callbacks.rs`
    - `grep -q '/api/auth/workos/callback' crates/octanest-api/src/app.rs crates/octanest-api/src/routes/auth_callbacks.rs`
    - `cargo test -p octanest-api --lib auth::` exits 0
  </acceptance_criteria>
  <done>WorkOS AuthKit start/callback mints Octanest sessions</done>
</task>

<task type="auto">
  <name>Task 2 (04-05-T2): Generic OIDC adapter + routes</name>
  <files>crates/octanest-api/src/auth/oidc.rs, crates/octanest-api/src/routes/auth_callbacks.rs, crates/octanest-api/src/app.rs, crates/octanest-api/src/rpc.rs</files>
  <read_first>crates/octanest-api/src/auth/workos.rs, crates/octanest-api/src/routes/auth_callbacks.rs, .planning/phases/04-auth-sessions-email/04-RESEARCH.md</read_first>
  <action>
1. Add `openidconnect = "4.0"` (and HTTP client alignment with rustls).
2. `oidc.rs`: discover `CoreProviderMetadata` from issuer; `PkceCodeChallenge::new_random_sha256()`; authorize_url with nonce; exchange_code + set_pkce_verifier; verify id_token claims; extract `sub` + email.
3. Link `auth_identities` provider=`oidc`; same user create/username rules as WorkOS; mint Octanest session.
4. Mount `GET /api/auth/oidc/start` and `GET /api/auth/oidc/callback`.
5. SSRF mitigation on issuer as T-04-16.
6. Ensure `auth.provider_config` returns current mode so UI can show Continue with WorkOS vs Continue with SSO (D-16).
7. Unit tests: reject `http://` issuer; reject link-local host; PKCE state mismatch → error.
8. No welcome email for SSO signup (D-20 local only).
  </action>
  <verify>
    <automated>cargo test -p octanest-api --lib auth::oidc</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'openidconnect' crates/octanest-api/Cargo.toml`
    - `grep -q 'PkceCodeChallenge\|pkce' crates/octanest-api/src/auth/oidc.rs`
    - `grep -q '/api/auth/oidc/start' crates/octanest-api/src/app.rs crates/octanest-api/src/routes/auth_callbacks.rs`
    - `grep -q '/api/auth/oidc/callback' crates/octanest-api/src/app.rs crates/octanest-api/src/routes/auth_callbacks.rs`
    - `cargo test -p octanest-api --lib auth::oidc` exits 0
  </acceptance_criteria>
  <done>OIDC auth-code+PKCE path mints Octanest sessions</done>
</task>

</tasks>

<verification>
- [ ] WorkOS and OIDC routes mounted
- [ ] Sessions always Octanest-native after callback
- [ ] No Better Auth / consumer OAuth buttons
</verification>

<success_criteria>
- D-05/D-07/D-08 provider adapters operational
- AUTH-02 session minting shared with SSO
</success_criteria>

<output>
After completion, create `.planning/phases/04-auth-sessions-email/04-05-SUMMARY.md`
</output>
