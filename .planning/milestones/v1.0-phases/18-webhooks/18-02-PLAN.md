---
phase: 18-webhooks
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - crates/oxidean-api/src/webhook/deliver.rs
  - crates/oxidean-api/src/webhook/worker.rs
  - crates/oxidean-api/src/webhook/mod.rs
  - crates/oxidean-api/src/jobs/schedule.rs
  - crates/oxidean-api/src/jobs/mod.rs
  - crates/oxidean-api/src/app.rs
  - crates/oxidean-db/src/webhooks.rs
  - crates/oxidean-api/tests/webhook_delivery.rs
  - crates/oxidean-api/tests/webhook_rpc.rs
  - crates/oxidean-api/Cargo.toml
  - docs/CONFIGURATION.md
  - .env.example
requirements:
  - HOOK-02
  - HOOK-03
autonomous: true
user_setup: []
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Deliveries include X-Hub-Signature-256 matching HMAC-SHA256 of raw body (HOOK-02, D-HOOK-12)"
    - "Transient failures retry with bounded backoff and each attempt is recorded (HOOK-03, D-HOOK-14, D-HOOK-13)"
    - "SSRF-unsafe URLs are rejected on create/update and/or at delivery time (D-HOOK-04, D-HOOK-18)"
    - "Admin can list recent deliveries with status via RPC (HOOK-03, D-HOOK-20, D-HOOK-21)"
  artifacts:
    - path: crates/oxidean-api/src/webhook/worker.rs
      provides: "retry drain loop"
    - path: crates/oxidean-api/src/webhook/deliver.rs
      provides: "SSRF guards + signing + timeouts"
    - path: crates/oxidean-api/src/webhook/mod.rs
      provides: "deliveries.list/get, ping, redeliver RPC"
  key_links:
    - from: crates/oxidean-api/src/jobs/schedule.rs
      to: crates/oxidean-api/src/webhook/worker.rs
      via: "spawn webhook worker beside existing jobs"
    - from: crates/oxidean-api/src/webhook/mod.rs
      to: crates/oxidean-db/src/webhooks.rs
      via: "deliveries.list persistence"
  assumptions:
    - "ASSUME: Discretion defaults — max attempts 5, timeout 10s, retention 30d ENV knobs"
    - "ASSUME: Prefer existing transitive hmac/sha2; avoid new crates.io deps unless necessary (T-18-SC)"
  prohibitions:
    - statement: "Must not emit X-Hub-Signature SHA-1 header"
      flagged: true
    - statement: "Must not introduce Redis or external brokers"
      flagged: true
---

<objective>
Harden delivery: SSRF policy, HMAC verification contract tests, retry worker, deliveries/ping/redeliver RPC for HOOK-02/03.

Purpose: Make the tracer production-safe and expose history APIs the Settings UI will bind (D-HOOK-12..18, D-HOOK-20, D-HOOK-21).
Output: Green delivery HMAC/SSRF/retry tests; webhook.deliveries.* + ping/redeliver; worker scheduled from app boot.
</objective>

<execution_context>
@/workspace/worktrees/phase-18/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/worktrees/phase-18/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-webhooks/18-CONTEXT.md
@.planning/phases/18-webhooks/18-RESEARCH.md
@.planning/phases/18-webhooks/18-01-PLAN.md
@crates/oxidean-api/src/jobs/schedule.rs
@crates/oxidean-api/src/webhook/deliver.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API outbound → arbitrary URL | SSRF / metadata endpoints |
| Delivery logs → Admin UI | Response snippets may leak third-party data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Elevation / SSRF | deliver.rs URL fetch | critical | mitigate | Block link-local, metadata IPs, non-https (D-HOOK-04/18); timeouts; limit redirects |
| T-18-02 | Information Disclosure | secret + response_snippet | high | mitigate | Never log secret; truncate response bodies stored (D-HOOK-16/20) |
| T-18-03 | Spoofing | signature | high | mitigate | Tests assert sha256= HMAC only (D-HOOK-12) |
| T-18-SC | Tampering | crates.io installs | high | mitigate | Prefer sha2/hmac already in lockfile; no new package without crates.io legitimacy check |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (18-02-T1): SSRF policy + HMAC contract + timeouts</name>
  <files>crates/oxidean-api/src/webhook/deliver.rs, crates/oxidean-api/Cargo.toml, crates/oxidean-api/tests/webhook_delivery.rs, docs/CONFIGURATION.md, .env.example</files>
  <behavior>
    - Reject http non-loopback and blocked IP ranges on create/update and delivery (D-HOOK-04, D-HOOK-18)
    - Signature header equals sha256= hex HMAC of exact POST bytes (D-HOOK-12)
    - Attempt records timeout/connection errors without panicking worker
  </behavior>
  <action>
  Harden deliver.rs per D-HOOK-04, D-HOOK-12, D-HOOK-18: URL scheme/host policy, DNS/IP deny for metadata/link-local, reqwest timeout, read bounded response for status/snippet. Prefer HMAC via existing workspace sha2/hmac (T-18-SC) — only add a direct hmac dep after confirming crates.io/crates/hmac is RustCrypto. Document ENV knobs in CONFIGURATION.md and .env.example. Expand webhook_delivery tests for HMAC match and SSRF rejection.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(webhook_hmac) | test(webhook_ssrf) | test(webhook_timeout)'</automated>
  </verify>
  <done>HMAC/SSRF/timeout automated tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (18-02-T2): Retry worker + deliveries.list/ping/redeliver RPC</name>
  <files>crates/oxidean-api/src/webhook/worker.rs, crates/oxidean-api/src/jobs/schedule.rs, crates/oxidean-api/src/webhook/mod.rs, crates/oxidean-db/src/webhooks.rs, crates/oxidean-api/tests/webhook_delivery.rs, crates/oxidean-api/tests/webhook_rpc.rs, packages/api-client/src/index.ts</files>
  <behavior>
    - 5xx/timeout schedules another attempt up to max; each attempt row persisted (D-HOOK-14, D-HOOK-13)
    - webhook.deliveries.list returns recent deliveries with status for Admin (HOOK-03, D-HOOK-20)
    - webhook.ping and webhook.redeliver enqueue new deliveries (D-HOOK-21)
  </behavior>
  <action>
  Add DB-backed retry worker and spawn from jobs/schedule.rs (D-HOOK-14, D-HOOK-23). Implement webhook.deliveries.list/get, webhook.ping, webhook.redeliver Admin RPC (HOOK-03, D-HOOK-20, D-HOOK-21). make rpc-gen. Retention trim optional but preferred on list/write.
  </action>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && cargo nextest run -p oxidean-api -E 'test(webhook_retry) | test(webhook_deliveries) | test(webhook_ping) | test(webhook_redeliver)'</automated>
  </verify>
  <done>Retries and delivery history/ping/redeliver RPC green.</done>
</task>

</tasks>

<verification>
HMAC/SSRF/retry/deliveries filters green; worker registered at boot; docs list ENV knobs.
</verification>

<success_criteria>
Production-safe delivery path with inspectable attempts via RPC (HOOK-02/03).
</success_criteria>

<output>
Create `.planning/phases/18-webhooks/18-02-SUMMARY.md` when done
</output>
