---
phase: 18-webhooks
plan: "04"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - apps/web/src/routes/$owner.$repo.settings.tsrx
  - apps/web/src/components/repo/webhooks-panel.tsrx
  - apps/web/src/components/repo/webhook-form.tsrx
  - apps/web/src/components/repo/webhook-deliveries.tsrx
  - apps/web/src/routes/$owner.$repo.settings.webhooks.integration.test.ts
  - packages/api-client/src/index.ts
requirements:
  - HOOK-01
  - HOOK-03
autonomous: true
user_setup: []
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Repo Admin sees Webhooks section in settings and can create/edit/delete/toggle hooks (HOOK-01, D-HOOK-19)"
    - "Admin can view recent delivery attempts with response status and trigger ping/redeliver (HOOK-03, D-HOOK-20, D-HOOK-21)"
    - "Non-admin settings visitors do not see webhook management (D-HOOK-02)"
  artifacts:
    - path: apps/web/src/components/repo/webhooks-panel.tsrx
      provides: "Admin Webhooks list + entry points"
    - path: apps/web/src/components/repo/webhook-form.tsrx
      provides: "create/edit form with events + secret reveal"
    - path: apps/web/src/components/repo/webhook-deliveries.tsrx
      provides: "delivery history table"
  key_links:
    - from: apps/web/src/routes/$owner.$repo.settings.tsrx
      to: apps/web/src/components/repo/webhooks-panel.tsrx
      via: "Admin-gated section render"
    - from: apps/web/src/components/repo/webhook-deliveries.tsrx
      to: packages/api-client/src/index.ts
      via: "webhook.deliveries.list RPC"
  assumptions:
    - "ASSUME: Discretion — inline Settings section acceptable vs nested /settings/hooks routes"
    - "ASSUME: Octane .tsrx only; load octane skill before editing"
  prohibitions:
    - statement: "Must author Octane Rivet .tsrx only — follow .agents/skills/octane/SKILL.md"
      flagged: true
    - statement: "Must not show full webhook secret after leave-create except rotate reveal"
      flagged: true
---

<objective>
Ship Admin Settings Webhooks UI: CRUD, event subscription checkboxes, secret reveal, and delivery history with ping/redeliver for HOOK-01/03.

Purpose: Complete ROADMAP UI success criteria using RPC from 18-01/18-02 (D-HOOK-19..21).
Output: Green Vitest settings.webhooks integration tests; Octane panel on repo settings.
</objective>

<execution_context>
@/workspace/worktrees/phase-18/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/worktrees/phase-18/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-webhooks/18-CONTEXT.md
@.planning/phases/18-webhooks/18-RESEARCH.md
@.agents/skills/octane/SKILL.md
@apps/web/src/routes/$owner.$repo.settings.tsrx
@apps/web/src/components/settings/pat-reveal.tsrx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → RPC | Admin session cookies; secret typed in form |
| UI display | Masked secrets; truncated delivery errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-02 | Information Disclosure | secret field UI | high | mitigate | One-time reveal pattern like PAT; list shows masked only (D-HOOK-16) |
| T-18-06 | Elevation | settings route | medium | mitigate | Render Webhooks only when can_admin; reuse RepoNotFound anti-enum (D-HOOK-02) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (18-04-T1): Webhooks panel CRUD on repo settings</name>
  <files>apps/web/src/components/repo/webhooks-panel.tsrx, apps/web/src/components/repo/webhook-form.tsrx, apps/web/src/routes/$owner.$repo.settings.tsrx, apps/web/src/routes/$owner.$repo.settings.webhooks.integration.test.ts</files>
  <behavior>
    - Admin sees Webhooks heading and can create hook with URL, secret, event checkboxes, active toggle (HOOK-01, D-HOOK-03, D-HOOK-19)
    - Edit/delete/toggle work via webhook.* RPC
    - Create shows one-time secret reveal then masks (D-HOOK-16)
  </behavior>
  <action>
  Author Octane .tsrx components (Rivet templates, no JSX return mix) per octane skill. Add Admin-only Webhooks section to repo settings (D-HOOK-19, D-HOOK-02). Wire webhook.create/update/delete/list. Reuse PAT reveal UX for secret (D-HOOK-16). Event checkboxes for push, pull_request, issues (D-HOOK-05). Turn Wave 0 Vitest stub green for list/create discoverability.
  </action>
  <verify>
    <automated>bun --filter @oxidean/web test -- src/routes/\$owner.\$repo.settings.webhooks.integration.test.ts</automated>
  </verify>
  <done>Admin can CRUD webhooks from Settings; Vitest green for CRUD discoverability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (18-04-T2): Delivery history + ping/redeliver UI</name>
  <files>apps/web/src/components/repo/webhook-deliveries.tsrx, apps/web/src/components/repo/webhooks-panel.tsrx, apps/web/src/routes/$owner.$repo.settings.webhooks.integration.test.ts</files>
  <behavior>
    - Selecting a hook shows recent deliveries with event, time, HTTP status/success (HOOK-03, D-HOOK-20)
    - Ping and Redeliver actions call RPC and refresh list (D-HOOK-21)
  </behavior>
  <action>
  Add deliveries table UI bound to webhook.deliveries.list and actions for ping/redeliver (D-HOOK-20, D-HOOK-21, HOOK-03). Keep status-focused; truncate any response snippet. Extend integration tests for history visibility.
  </action>
  <verify>
    <automated>bun --filter @oxidean/web test -- src/routes/\$owner.\$repo.settings.webhooks.integration.test.ts && make web-lint && make web-format-check</automated>
  </verify>
  <done>Delivery history and ping/redeliver visible for Admin; web lint/format clean.</done>
</task>

</tasks>

<verification>
Settings webhooks Vitest green; make web-lint and web-format-check pass.
</verification>

<success_criteria>
HOOK-01 and HOOK-03 usable from repo Settings UI for Admins.
</success_criteria>

<output>
Create `.planning/phases/18-webhooks/18-04-SUMMARY.md` when done
</output>
