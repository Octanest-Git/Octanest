---
phase: 18-webhooks
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - crates/oxidean-api/src/webhook/dispatch.rs
  - crates/oxidean-api/src/webhook/payloads.rs
  - crates/oxidean-api/src/routes/git_smart_http.rs
  - crates/oxidean-api/src/ssh
  - crates/oxidean-api/src/issue/mod.rs
  - crates/oxidean-api/tests/webhook_delivery.rs
requirements:
  - HOOK-02
autonomous: true
user_setup: []
estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Successful HTTPS and SSH pushes enqueue push webhook deliveries for subscribed hooks (HOOK-02, D-HOOK-10, D-HOOK-22)"
    - "PR open/edit/close/reopen/synchronize/merge enqueue pull_request deliveries with Phase 12 #N and fork-capable head fields (HOOK-02, D-HOOK-09)"
  artifacts:
    - path: crates/oxidean-api/src/webhook/payloads.rs
      provides: "GitHub-shaped push/issues/pull_request builders (D-HOOK-07)"
    - path: crates/oxidean-api/src/routes/git_smart_http.rs
      provides: "push emit after successful receive-pack"
  key_links:
    - from: crates/oxidean-api/src/routes/git_smart_http.rs
      to: crates/oxidean-api/src/webhook/dispatch.rs
      via: "emit push after success"
    - from: Phase 12 PR module
      to: crates/oxidean-api/src/webhook/dispatch.rs
      via: "emit pull_request actions"
  assumptions:
    - "ASSUME: Phase 12 PR RPC/tables exist on the integration branch before turning PR delivery tests green"
    - "ASSUME: Push payload may truncate commit list like GitHub (discretion)"
  prohibitions:
    - statement: "Must not invent a separate PR number space in payloads"
      flagged: true
    - statement: "Must not fail git push when webhook delivery fails"
      flagged: true
---

<objective>
Complete HOOK-02 event coverage: push emitters on HTTPS+SSH receive paths and pull_request emitters aligned to Phase 12 CONTEXT identity.

Purpose: Satisfy ROADMAP success criterion 2 beyond the issues tracer (D-HOOK-09, D-HOOK-10, D-HOOK-22).
Output: Green webhook_push_* and webhook_pull_request_* delivery tests against mock sinks.
</objective>

<execution_context>
@/workspace/worktrees/phase-18/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/worktrees/phase-18/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-webhooks/18-CONTEXT.md
@.planning/phases/18-webhooks/18-RESEARCH.md
@.planning/phases/12-pull-requests/12-CONTEXT.md
@crates/oxidean-api/src/routes/git_smart_http.rs
@crates/oxidean-api/src/ssh
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git push auth → event emit | Only after successful authorized receive |
| PR mutate → external POST | Payload may include PR body markdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-04 | Spoofing | push emit on failed receive | medium | mitigate | Emit only after successful receive-pack/SSH update (D-HOOK-10) |
| T-18-05 | Information Disclosure | PR/issue body in payload | low | accept | Same data Admin already routes to configured URL (document) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (18-03-T1): push event after HTTPS + SSH receive success</name>
  <files>crates/oxidean-api/src/webhook/payloads.rs, crates/oxidean-api/src/webhook/dispatch.rs, crates/oxidean-api/src/routes/git_smart_http.rs, crates/oxidean-api/src/ssh, crates/oxidean-api/tests/webhook_delivery.rs</files>
  <behavior>
    - After successful HTTPS git-receive-pack ref update, subscribed push hooks receive X-GitHub-Event: push (D-HOOK-10, D-HOOK-06)
    - SSH receive path emits the same event (D-HOOK-22)
    - Failed auth/receive does not emit (T-18-04)
    - User push succeeds even if sink is down (D-HOOK-11)
  </behavior>
  <action>
  Build GitHub-shaped push payloads (D-HOOK-07, D-HOOK-10) and emit from Smart HTTP receive success and SSH receive success paths (D-HOOK-22). Prefer parsing updated refs only when the receive completes successfully. Keep delivery async. Add nextest coverage with mock HTTP sink.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(webhook_push)'</automated>
  </verify>
  <done>HTTPS and SSH successful pushes produce push webhook deliveries; failures do not.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (18-03-T2): pull_request emitters using Phase 12 PR model</name>
  <files>crates/oxidean-api/src/webhook/payloads.rs, crates/oxidean-api/src/webhook/dispatch.rs, crates/oxidean-api/tests/webhook_delivery.rs</files>
  <precondition>Phase 12 pull request RPC module and DB tables exist on this branch (ROADMAP depends_on Phase 12; D-HOOK-09).</precondition>
  <behavior>
    - PR open → pull_request opened; edit → edited; close/reopen; synchronize on head push; merge maps to GitHub-compatible closed+merged or merged action (D-HOOK-09)
    - Payload number equals shared per-repo #N; head may indicate fork vs same-repo (D-PR-01..03 / D-HOOK-09)
  </behavior>
  <action>
  Wire WebhookDispatcher into Phase 12 PR create/update/merge/close handlers (D-HOOK-09, D-HOOK-22). Payload builders MUST follow 12-CONTEXT identity (shared #N, fork heads, draft if present) — do not invent alternate numbering. Cover actions listed in D-HOOK-09 with nextest against mock sink. If Phase 12 code is absent, halt on precondition rather than stubbing fake PR tables.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(webhook_pull_request)'</automated>
  </verify>
  <done>PR lifecycle emits pull_request webhook deliveries with Phase 12-aligned identity fields.</done>
  <reversibility rating="costly">Couples webhook payload contract to Phase 12 PR schema (D-HOOK-09).</reversibility>
</task>

</tasks>

<verification>
webhook_push and webhook_pull_request nextest filters green; no user-facing git/PR failures when sinks down.
</verification>

<success_criteria>
HOOK-02 covers push + pull_request + issues (issues already from 18-01).
</success_criteria>

<output>
Create `.planning/phases/18-webhooks/18-03-SUMMARY.md` when done
</output>
