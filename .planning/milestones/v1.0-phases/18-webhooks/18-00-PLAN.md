---
phase: 18-webhooks
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/octanest-api/tests/webhook_rpc.rs
  - crates/octanest-api/tests/webhook_delivery.rs
  - crates/octanest-db/tests/dialect_webhooks.rs
  - apps/web/src/routes/$owner.$repo.settings.webhooks.integration.test.ts
  - .planning/phases/18-webhooks/18-VALIDATION.md
requirements:
  - HOOK-01
  - HOOK-02
  - HOOK-03
autonomous: true
user_setup: []
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Wave 0 discoverable stubs exist for webhook RPC, delivery/HMAC, dialect_webhooks, and settings Webhooks UI (HOOK-01..03, D-HOOK-02, D-HOOK-19)"
    - "18-VALIDATION.md Wave 0 gap files are present on disk"
  artifacts:
    - path: crates/octanest-api/tests/webhook_rpc.rs
      provides: "HOOK-01 Admin CRUD failing stubs"
    - path: crates/octanest-api/tests/webhook_delivery.rs
      provides: "HOOK-02/03 delivery + signature stubs"
    - path: crates/octanest-db/tests/dialect_webhooks.rs
      provides: "webhooks migration parity stub"
    - path: apps/web/src/routes/$owner.$repo.settings.webhooks.integration.test.ts
      provides: "Settings Webhooks discoverability stub"
  key_links:
    - from: .planning/phases/18-webhooks/18-VALIDATION.md
      to: crates/octanest-api/tests/webhook_rpc.rs
      via: "Wave 0 Requirements checklist"
  assumptions:
    - "ASSUME: Wave 0 stubs may #[ignore] or assert!(false) until later plans turn green"
    - "ASSUME: Migration number is next free 00NN after packages at execute time"
  prohibitions:
    - statement: "Must not implement production webhook RPC, migrations, or Settings UI in this Wave 0 plan"
      flagged: true
    - statement: "Must not add new crates.io or npm packages"
      flagged: true
---

<objective>
Scaffold Nyquist/Wave 0 failing stubs for Phase 18 webhook CRUD, delivery/HMAC, dialect migrations, and Admin Settings Webhooks UI before implementation plans turn them green.

Purpose: Every later automated verify names a real test path (18-VALIDATION.md Wave 0). Covers HOOK-01..03 stub discoverability per D-HOOK-02 / D-HOOK-19.
Output: Discoverable RED/ignored nextest + Vitest filters matching `webhook`, `dialect_webhooks`.
</objective>

<execution_context>
@/workspace/worktrees/phase-18/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/worktrees/phase-18/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-webhooks/18-CONTEXT.md
@.planning/phases/18-webhooks/18-RESEARCH.md
@.planning/phases/18-webhooks/18-VALIDATION.md
@.planning/phases/15-releases-transfer/15-00-PLAN.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A (stubs only) | No production surfaces yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-00 | Tampering | stub tests | low | accept | Stubs assert false only; no network |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (18-00-T1): Rust Wave 0 stubs for webhook RPC + delivery + dialect</name>
  <files>crates/octanest-api/tests/webhook_rpc.rs, crates/octanest-api/tests/webhook_delivery.rs, crates/octanest-db/tests/dialect_webhooks.rs</files>
  <action>
  Create discoverable failing/ignored nextest stubs mirroring Phase 15 Wave 0 style:
  - webhook_rpc.rs: tests named for create/list/update/delete/Admin denial (HOOK-01, D-HOOK-02)
  - webhook_delivery.rs: tests named for issues emit delivery, HMAC header, deliveries.list status (HOOK-02/03, D-HOOK-12, D-HOOK-13)
  - dialect_webhooks.rs: migration parity stub for webhooks tables
  Do not implement production code or migrations.
  </action>
  <verify>
    <automated>test -f crates/octanest-api/tests/webhook_rpc.rs && test -f crates/octanest-api/tests/webhook_delivery.rs && test -f crates/octanest-db/tests/dialect_webhooks.rs && cargo nextest list -p octanest-api -E 'test(webhook)' | rg -q 'webhook' && cargo nextest list -p octanest-db -E 'test(dialect_webhooks)' | rg -q 'dialect_webhooks'</automated>
  </verify>
  <done>Webhook/dialect stub test files exist and are listable by nextest filters.</done>
</task>

<task type="auto">
  <name>Task 2 (18-00-T2): Web Wave 0 stub for Settings Webhooks UI</name>
  <files>apps/web/src/routes/$owner.$repo.settings.webhooks.integration.test.ts</files>
  <action>
  Add Vitest integration stub that fails until Settings exposes a Webhooks heading/section for Admin (HOOK-01/03, D-HOOK-19). Follow `$owner.$repo.settings.rename-transfer.integration.test.ts` discoverability pattern. No production UI.
  </action>
  <verify>
    <automated>test -f apps/web/src/routes/\$owner.\$repo.settings.webhooks.integration.test.ts</automated>
  </verify>
  <done>Settings webhooks integration stub file exists on disk.</done>
</task>

</tasks>

<verification>
Wave 0 files present; nextest list finds webhook filters; no production webhook module yet.
</verification>

<success_criteria>
HOOK-01..03 have named stub targets in 18-VALIDATION.md Wave 0 table.
</success_criteria>

<output>
Create `.planning/phases/18-webhooks/18-00-SUMMARY.md` when done
</output>
