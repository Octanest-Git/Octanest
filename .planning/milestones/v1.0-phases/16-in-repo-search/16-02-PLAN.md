---
phase: 16-in-repo-search
plan: "02"
type: execute
wave: 3
depends_on:
  - "01"
files_modified:
  - crates/octanest-git/src/backend.rs
  - crates/octanest-git/src/cli.rs
  - crates/octanest-db/src/issues.rs
  - crates/octanest-db/src/pulls.rs
  - crates/octanest-api/src/repo/search.rs
  - crates/octanest-api/src/repo/search_query.rs
  - crates/octanest-api/tests/repo_search.rs
requirements:
  - GIT-18
autonomous: true
user_setup: []
estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "repo.search type=commits finds commits by message and author: qualifier (GIT-18, D-SRCH-07, D-SRCH-12)"
    - "repo.search type=issues finds issues by title/body with is:open/is:closed (D-SRCH-09, D-SRCH-12)"
    - "repo.search type=pulls finds PRs by title/body separately from issues (D-SRCH-10, D-SRCH-11)"
  artifacts:
    - path: crates/octanest-api/src/repo/search_query.rs
      provides: "qualifier parser"
    - path: crates/octanest-git/src/backend.rs
      provides: "GitBackend::log_search"
    - path: crates/octanest-api/src/repo/search.rs
      provides: "commits/issues/pulls dispatch"
  key_links:
    - from: crates/octanest-api/src/repo/search.rs
      to: crates/octanest-db/src/issues.rs
      via: "issue text search filters"
    - from: crates/octanest-api/src/repo/search.rs
      to: crates/octanest-db/src/pulls.rs
      via: "PR text search (Phase 12 schema)"
  assumptions:
    - "ASSUME: Phase 12 pull_requests (or equivalent) tables and helpers exist at execute time per D-SRCH-10 / D-PR-02; path may be pulls.rs or issues kind filter — follow landed Phase 12"
    - "ASSUME: Unknown qualifiers are stripped (RESEARCH resolution)"
  prohibitions:
    - statement: "Must not return mixed issue and PR rows in one type tab"
      flagged: true
    - statement: "Must not implement full GitHub search grammar"
      flagged: true
---

<objective>
Expand `repo.search` to commits, issues, and pulls with the modest qualifier subset, completing GIT-18 backend coverage beyond code.

Purpose: Deliver remaining search types behind the same ACL and RPC (D-SRCH-07…12, D-SRCH-14).
Output: Green `repo_search_commits`, `repo_search_issues`, `repo_search_pulls`.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-in-repo-search/16-CONTEXT.md
@.planning/phases/16-in-repo-search/16-RESEARCH.md
@.planning/phases/12-pull-requests/12-CONTEXT.md
@.planning/phases/11-issues/11-CONTEXT.md
@crates/octanest-db/src/issues.rs
</context>

<precondition>
Phase 12 PR persistence and list helpers are present on the execution branch (D-SRCH-10). Halt and rebase onto Phase 12 if pulls tables/RPC are missing.
</precondition>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Qualifier strings | author:/path:/is: parsed from untrusted q |
| DB LIKE patterns | Escape/wildcard safety for user q |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Information disclosure | issues/pulls search | high | mitigate | Same Read gate; never cross-repo (D-SRCH-05) |
| T-16-04 | Information disclosure | author: resolution | medium | mitigate | Resolve author usernames like issue.list; do not leak existence across private repos |
| T-16-05 | Denial of service | log_search | medium | mitigate | Reuse timeout/caps (D-SRCH-08) |
| T-16-SC | Tampering | packages | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (16-02-T1): Qualifier parser + commit log_search</name>
  <files>crates/octanest-api/src/repo/search_query.rs, crates/octanest-git/src/backend.rs, crates/octanest-git/src/cli.rs, crates/octanest-api/src/repo/search.rs, crates/octanest-api/tests/repo_search.rs</files>
  <read_first>.planning/phases/16-in-repo-search/16-RESEARCH.md, crates/octanest-git/src/cli.rs</read_first>
  <behavior>
    - Parser extracts is:/author:/path: and leftover keywords; strips unknown qualifiers (D-SRCH-12)
    - type=commits matches message grep and author:login (D-SRCH-07)
  </behavior>
  <action>
Add in-tree `search_query` parser for D-SRCH-12. Extend `GitBackend` with `log_search` (message grep + optional author, paging). Wire `repo.search` type=commits. Turn `repo_search_commits` green. Also apply `path:` to code grep pathspec when present.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-git -E 'test(log_search)' &amp;&amp; cargo nextest run -p octanest-api -E 'test(repo_search_commits)'</automated>
  </verify>
  <done>Commit search and qualifier parsing work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (16-02-T2): Issues search type</name>
  <files>crates/octanest-db/src/issues.rs, crates/octanest-api/src/repo/search.rs, crates/octanest-api/tests/repo_search.rs</files>
  <read_first>crates/octanest-db/src/issues.rs, crates/octanest-api/src/issue/mod.rs</read_first>
  <behavior>
    - type=issues matches title/body substring; is:open/is:closed and author: apply (D-SRCH-09, D-SRCH-12)
    - Does not return PR rows (D-SRCH-11)
  </behavior>
  <action>
Extend db issue search helpers as needed (title/body only per RESEARCH discretion). Dispatch type=issues from `repo.search` using parsed qualifiers. Ensure pulls are excluded from issues results (D-SRCH-11). Green `repo_search_issues`.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(repo_search_issues)'</automated>
  </verify>
  <done>Issues search type returns correct filtered hits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (16-02-T3): Pulls search type</name>
  <files>crates/octanest-db/src/pulls.rs, crates/octanest-api/src/repo/search.rs, crates/octanest-api/tests/repo_search.rs</files>
  <read_first>.planning/phases/12-pull-requests/12-CONTEXT.md</read_first>
  <precondition>Phase 12 PR tables/helpers exist on this branch</precondition>
  <behavior>
    - type=pulls matches PR title/body with is:/author: (D-SRCH-10, D-SRCH-12)
    - Separate from issues tab (D-SRCH-11)
  </behavior>
  <action>
Implement pulls search against Phase 12 PR persistence (follow landed schema — dedicated table or kind discriminator). Wire type=pulls. Green `repo_search_pulls`. Cite D-PR-02 shared numbering only for link targets, not for mixing result sets.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(repo_search_pulls) | test(repo_search_issues)'</automated>
  </verify>
  <done>Pulls search works and stays isolated from issues results.</done>
</task>

</tasks>

<verification>
All four search types return permission-aware hits via repo.search.
</verification>

<success_criteria>
repo_search_commits/issues/pulls green; qualifiers honored; no mixed tabs.
</success_criteria>

<output>
Create `.planning/phases/16-in-repo-search/16-02-SUMMARY.md` when done
</output>
