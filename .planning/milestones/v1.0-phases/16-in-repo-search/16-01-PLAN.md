---
phase: 16-in-repo-search
plan: "01"
type: execute
wave: 2
depends_on:
  - "00"
files_modified:
  - crates/octanest-git/src/backend.rs
  - crates/octanest-git/src/cli.rs
  - crates/octanest-core/src/lib.rs
  - crates/octanest-api/src/repo/search.rs
  - crates/octanest-api/src/repo/mod.rs
  - crates/octanest-api/src/rpc.rs
  - crates/octanest-api/src/app.rs
  - crates/octanest-api/tests/repo_search.rs
  - packages/api-client/src/index.ts
  - apps/web/src/routes/$owner.$repo.search.tsrx
  - apps/web/src/lib/repo-chrome-active.ts
requirements:
  - GIT-18
autonomous: true
user_setup: []
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Read user can repo.search type=code and find a seeded text line on the default branch (GIT-18, D-SRCH-04, D-SRCH-06, D-SRCH-14)"
    - "Unauthorized private search returns soft repo.not_found (D-SRCH-04)"
    - "Thin Octane /search route calls repo.search for type=code and shows at least one hit (D-SRCH-01, D-SRCH-15)"
  artifacts:
    - path: crates/octanest-git/src/backend.rs
      provides: "GitBackend::grep"
    - path: crates/octanest-api/src/repo/search.rs
      provides: "repo.search handler (code path)"
    - path: apps/web/src/routes/$owner.$repo.search.tsrx
      provides: "tracer search page"
  key_links:
    - from: crates/octanest-api/src/repo/search.rs
      to: crates/octanest-api/src/repo/acl.rs
      via: "resolve_repo_for_read"
    - from: crates/octanest-api/src/repo/search.rs
      to: crates/octanest-git/src/backend.rs
      via: "grep"
  assumptions:
    - "ASSUME: Default timeout 8000ms and max matches 100 from RESEARCH ENV knobs may be hardcoded first then documented in plan 03"
    - "ASSUME: Commits/issues/pulls types return empty or rpc.bad_input until plan 02 — prefer empty hits with type accepted for tracer stability"
  prohibitions:
    - statement: "Must not add Zoekt Elasticsearch Meilisearch or similar indexer services"
      flagged: true
    - statement: "Must not enable apps/web GlobalSearch"
      flagged: true
---

<objective>
Ship the end-to-end tracer for GIT-18 code search: `GitBackend::grep` → `repo.search` type=code behind Read ACL → thin Octane `/search` page showing code hits.

Purpose: Prove architecture before commits/issues/PRs expansion (D-SRCH-01, D-SRCH-04, D-SRCH-06, D-SRCH-14, D-SRCH-15).
Output: Green `repo_search_code` + `repo_search_acl`; rpc-gen client; minimal search route.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-in-repo-search/16-CONTEXT.md
@.planning/phases/16-in-repo-search/16-RESEARCH.md
@.planning/phases/10-orgs-permissions/10-CONTEXT.md
@crates/octanest-git/src/backend.rs
@crates/octanest-api/src/repo/mod.rs
@crates/octanest-api/src/repo/acl.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → RPC | Untrusted `q`, `ref`, `type`, path qualifiers |
| RPC → git subprocess | Args must not allow option injection |
| ACL | Private repo enumeration via search |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Information disclosure | repo.search | high | mitigate | resolve_repo_for_read + soft not_found (D-SRCH-04, D-SRCH-05) |
| T-16-02 | Tampering | git grep argv | high | mitigate | Reuse validate_treeish / path validators; pass patterns as args not shell |
| T-16-03 | Denial of service | git grep | medium | mitigate | Timeout + max matches (D-SRCH-08); full ENV wiring may land in 16-03 |
| T-16-SC | Tampering | packages | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (16-01-T1): End-to-end repo.search type=code via git grep</name>
  <files>crates/octanest-git/src/backend.rs, crates/octanest-git/src/cli.rs, crates/octanest-core/src/lib.rs, crates/octanest-api/src/repo/search.rs, crates/octanest-api/src/repo/mod.rs, crates/octanest-api/src/rpc.rs, crates/octanest-api/tests/repo_search.rs, packages/api-client/src/index.ts</files>
  <read_first>.planning/phases/16-in-repo-search/16-RESEARCH.md, crates/octanest-git/src/cli.rs, crates/octanest-api/src/repo/mod.rs, crates/octanest-api/src/rpc.rs</read_first>
  <behavior>
    - repo.search type=code returns path+line hits for seeded unique string on default branch (D-SRCH-06, D-SRCH-14)
    - git grep exit with no matches yields empty hits not error
    - private unauthorized actor gets soft repo.not_found (D-SRCH-04)
  </behavior>
  <action>
Add `GitBackend::grep` (and CliGitBackend impl using `git grep -n -I` on bare repo + treeish; map no-match exit to empty). Add core DTOs for search request/response. Implement `repo.search` in `repo/search.rs`: require session when needed, `resolve_repo_for_read`, accept `type`/`q`/`ref`/`offset`/`limit`, dispatch `code` through grep on default branch when ref omitted (D-SRCH-06). Register `"repo.search"` in rpc.rs; run `make rpc-gen`. Turn Wave 0 `repo_search_code` and `repo_search_acl` green. Other types may return empty hits for now. Do not add indexer crates (D-SRCH-06).
  </action>
  <verify>
    <automated>make rpc-gen &amp;&amp; make rpc-sync-check &amp;&amp; cargo nextest run -p octanest-git -E 'test(grep)' &amp;&amp; cargo nextest run -p octanest-api -E 'test(repo_search_code) | test(repo_search_acl)'</automated>
  </verify>
  <done>Code search RPC works end-to-end with Read ACL; client regenerated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (16-01-T2): Thin Octane /search tracer page for code hits</name>
  <files>apps/web/src/routes/$owner.$repo.search.tsrx, apps/web/src/lib/repo-chrome-active.ts, apps/web/src/routes/$owner.$repo.search.integration.test.ts</files>
  <read_first>.agents/skills/octane/SKILL.md, apps/web/src/routes/$owner.$repo.issues.tsrx, apps/web/src/lib/repo-chrome-active.ts, apps/web/src/components/repo/repo-chrome.tsrx</read_first>
  <behavior>
    - /{owner}/{repo}/search renders query field and shows code hits from repo.search (D-SRCH-01, D-SRCH-15)
    - repoChromeActiveFromPath maps /search to search active (D-SRCH-16)
  </behavior>
  <action>
Add Octane route `$owner.$repo.search.tsrx` using TanStack Query calling `repo.search` with type=code (D-SRCH-14, D-SRCH-15). Extend `RepoChromeActive` with `"search"` and path mapping (D-SRCH-16). Keep page thin: q input + code hit list linking toward blob routes when possible. Do not wire GlobalSearch (D-SRCH-01). Turn a minimal Vitest assertion green for code results presence.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/\$owner.\$repo.search.integration.test.ts</automated>
  </verify>
  <done>Tracer search page shows code hits for the happy path.</done>
</task>

<task type="auto">
  <name>Task 3 (16-01-T3): Binary skip + basic match cap on code path</name>
  <files>crates/octanest-git/src/cli.rs, crates/octanest-api/src/repo/search.rs, crates/octanest-api/tests/repo_search.rs</files>
  <read_first>.planning/phases/16-in-repo-search/16-RESEARCH.md</read_first>
  <action>
Ensure grep uses binary-skipping flag (`-I`) per D-SRCH-08. Enforce a soft max-matches cap on the code path and expose `truncated` in the response when capped. Add/adjust `repo_search_limits` coverage for truncation (full timeout ENV may wait for 16-03).
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(repo_search_limits) | test(repo_search_code)'</automated>
  </verify>
  <done>Binary skip and truncated flag behave as specified for code search.</done>
</task>

</tasks>

<verification>
Code search tracer green: API + thin UI + ACL.
</verification>

<success_criteria>
GIT-18 code path works for Read users; private soft-404; truncated/binary safeguards present.
</success_criteria>

<output>
Create `.planning/phases/16-in-repo-search/16-01-SUMMARY.md` when done
</output>
