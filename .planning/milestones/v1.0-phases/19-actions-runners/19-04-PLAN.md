---
phase: 19-actions-runners
plan: "04"
type: execute
wave: 3
depends_on:
  - "03"
files_modified:
  - crates/octanest-api/src/actions/dispatch.rs
  - crates/octanest-api/src/actions/runner_proto.rs
  - crates/octanest-api/src/actions/logs.rs
  - crates/octanest-api/src/actions/mod.rs
  - crates/octanest-api/src/routes/git_smart_http.rs
  - crates/octanest-api/src/ssh/pack.rs
  - crates/octanest-api/src/app.rs
  - crates/octanest-api/tests/actions_triggers.rs
  - crates/octanest-api/tests/actions_runner_protocol.rs
  - crates/octanest-api/tests/actions_dispatch_policy.rs
  - crates/octanest-api/tests/actions_rpc.rs
requirements:
  - ACT-01
  - ACT-02
  - ACT-06
  - ACT-07
autonomous: true
user_setup: []
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A successful push can enqueue a workflow run from .github/workflows when on.push matches (ACT-02, D-ACT-04, D-ACT-05)"
    - "A registered runner can FetchTask a queued job matching its labels over the Actions protocol mount (ACT-06, D-ACT-07, D-ACT-09)"
    - "Jobs without a matching runner remain queued; API does not execute steps (ACT-07, D-ACT-10, D-ACT-20)"
  artifacts:
    - path: crates/octanest-api/src/actions/dispatch.rs
      provides: "Push dispatcher"
    - path: crates/octanest-api/src/actions/runner_proto.rs
      provides: "Minimal FetchTask path"
    - path: crates/octanest-api/src/app.rs
      provides: "/api/actions mount"
  key_links:
    - from: crates/octanest-api/src/routes/git_smart_http.rs
      to: crates/octanest-api/src/actions/dispatch.rs
      via: "post-receive-pack async dispatch"
    - from: runner FetchTask
      to: action_jobs queued
      via: "label match"
  assumptions:
    - "ASSUME: Tracer may use a simplified protobuf/JSON handler stub that is replaced by full prost Connect wiring in 19-05 — architecture mount and auth must remain stable"
  prohibitions:
    - statement: "Must not run workflow steps inside octanest-api"
      flagged: true
---

<objective>
Tracer bullet: wire one push → parse → enqueue → runner FetchTask path end-to-end through receive-pack hook, dispatcher, DB jobs, and `/api/actions` protocol mount (ACT-01, ACT-02, ACT-06, ACT-07).

Purpose: Prove control-plane architecture before expanding PR triggers, full proto, and UI.
Output: Working happy-path tests for push dispatch + FetchTask + queued-without-runner policy.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-actions-runners/19-CONTEXT.md
@.planning/phases/19-actions-runners/19-RESEARCH.md
@crates/octanest-api/src/actions/parse.rs
@crates/octanest-api/src/routes/git_smart_http.rs
@crates/octanest-api/src/app.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| receive-pack success → dispatcher | Must not fail the git push if dispatch errors |
| Runner → FetchTask | Runner token required |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-08 | Elevation of privilege | FetchTask | high | mitigate | Only registered runner token; label + scope match (D-ACT-10, D-ACT-18) |
| T-19-09 | Denial of Service | push hook | medium | mitigate | Async dispatch after pack success (D-ACT-05) |
| T-19-10 | Spoofing | protocol mount | high | mitigate | Ignore session cookies on /api/actions (D-ACT-18) |
</threat_model>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (19-04-T1): End-to-end push dispatch tracer</name>
  <files>crates/octanest-api/src/actions/dispatch.rs, crates/octanest-api/src/routes/git_smart_http.rs, crates/octanest-api/src/ssh/pack.rs, crates/octanest-api/tests/actions_triggers.rs, crates/octanest-api/tests/actions_dispatch_policy.rs</files>
  <behavior>
    - After successful HTTPS receive-pack, matching on.push workflow creates queued run+jobs
    - OCTANEST_ACTIONS_ENABLED false or repo actions_enabled false skips enqueue (D-ACT-06)
    - No matching runner leaves jobs queued (D-ACT-20)
    - Dispatch errors do not convert pack success into push failure
  </behavior>
  <read_first>crates/octanest-api/src/routes/git_smart_http.rs, crates/octanest-api/tests/actions_triggers.rs, .planning/phases/19-actions-runners/19-CONTEXT.md</read_first>
  <action>
    Implement async push dispatcher invoked after successful receive-pack (HTTPS and SSH) per D-ACT-05 / D-ACT-04 / ACT-02. Respect instance + repo enable gates (D-ACT-06). Create run/job rows from parsed workflows (ACT-01). Enforce registered-only / no in-process execution policy in actions_dispatch_policy (ACT-07 / D-ACT-10 / D-ACT-20). Green trigger + policy tests for the push path only.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && cargo nextest run -p octanest-api -E 'test(actions_triggers)|test(actions_dispatch_policy)'</automated>
  </verify>
  <done>Push enqueue + queued-without-runner policy proven by nextest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (19-04-T2): Minimal runner FetchTask mount</name>
  <files>crates/octanest-api/src/actions/runner_proto.rs, crates/octanest-api/src/app.rs, crates/octanest-api/tests/actions_runner_protocol.rs</files>
  <behavior>
    - Registered runner with matching label can FetchTask one queued job
    - Unauthenticated FetchTask rejected
    - Session cookie alone does not authorize protocol
  </behavior>
  <read_first>crates/octanest-api/src/app.rs, crates/octanest-api/tests/actions_runner_protocol.rs, .planning/phases/19-actions-runners/19-RESEARCH.md</read_first>
  <action>
    Mount `/api/actions` runner endpoints and implement the thinnest FetchTask (plus seed Register if required for the test) that assigns a queued job by label match per D-ACT-07 / D-ACT-09 / ACT-06. Keep wire shape compatible with the Gitea protocol direction so 19-05 can complete prost/Connect without remounting. Green the happy-path subset of actions_runner_protocol.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && cargo nextest run -p octanest-api -E 'test(actions_runner_protocol)'</automated>
  </verify>
  <done>FetchTask happy path + auth rejection covered.</done>
</task>

<task type="auto">
  <name>Task 3 (19-04-T3): Tracer log append path</name>
  <files>crates/octanest-api/src/actions/logs.rs, crates/octanest-api/src/actions/mod.rs, crates/octanest-api/tests/actions_rpc.rs</files>
  <read_first>crates/octanest-api/src/config.rs, .planning/phases/19-actions-runners/19-CONTEXT.md</read_first>
  <action>
    Add log file writer under OCTANEST_ACTIONS_LOG_DIR for a job (D-ACT-13) and a minimal session RPC or test helper to read logs for the tracer run (ACT-03 foundation). Expand actions_rpc with a failing-or-partial assertion greened enough to prove log bytes round-trip; full UI RPC surface completes in 19-09.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && cargo nextest run -p octanest-api -E 'test(actions_rpc)'</automated>
  </verify>
  <done>Log bytes for a job can be written and read back in tests.</done>
</task>

</tasks>

<verification>
End-to-end push → queue → FetchTask → log path proven.
</verification>

<success_criteria>
Architecture for Actions control plane is committed and tested on one path.
</success_criteria>

<output>
Create `.planning/phases/19-actions-runners/19-04-SUMMARY.md` when done
</output>
