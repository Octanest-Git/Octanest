---
phase: 19-actions-runners
plan: "02"
type: execute
wave: 1
depends_on:
  - "00"
files_modified:
  - crates/oxidean-db/migrations/sqlite/0016_actions.sql
  - crates/oxidean-db/migrations/postgres/0016_actions.sql
  - crates/oxidean-db/migrations/mysql/0016_actions.sql
  - crates/oxidean-db/src/lib.rs
  - crates/oxidean-db/src/actions.rs
  - crates/oxidean-db/tests/dialect_actions.rs
  - crates/oxidean-db/tests/factory_reset_actions.rs
  - crates/oxidean-api/src/config.rs
  - docker-compose.yml
  - docs/CONFIGURATION.md
requirements:
  - ACT-03
  - ACT-07
autonomous: true
user_setup: []
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Dialect migrations create actions runners/runs/jobs/statuses/secrets tables with parity (D-ACT-13, D-ACT-17)"
    - "OXIDEAN_ACTIONS_LOG_DIR is configured and mounted in Compose like other volume stores (D-ACT-13)"
    - "Factory reset clears actions runs/logs/runner registrations per D-ACT-19"
  artifacts:
    - path: crates/oxidean-db/migrations/sqlite/0016_actions.sql
      provides: "Actions schema"
    - path: docker-compose.yml
      provides: "ACTIONS_LOG_DIR volume on api"
  key_links:
    - from: crates/oxidean-api/src/config.rs
      to: OXIDEAN_ACTIONS_LOG_DIR
      via: "env parse + default path"
  assumptions:
    - "ASSUME: Migration number may be renumbered if another phase lands 0016 first — keep dialect trio in sync"
  prohibitions:
    - statement: "Must not implement runner protocol handlers or workflow parser in this plan"
      flagged: true
---

<objective>
Create Actions DB schema across three dialects, wire OXIDEAN_ACTIONS_LOG_DIR + Compose volume, and factory-reset wipe for runs/logs/runners (D-ACT-13, D-ACT-17, D-ACT-19).

Purpose: Persistence foundation before parse/dispatch tracer.
Output: Migrations + db module + config/compose/docs env rows; dialect_actions greening started.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-actions-runners/19-CONTEXT.md
@.planning/phases/19-actions-runners/19-RESEARCH.md
@crates/oxidean-db/migrations/sqlite/0015_packages.sql
@crates/oxidean-db/src/packages.rs
@docker-compose.yml
@docs/CONFIGURATION.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator env → LOG_DIR | Path must be absolute in containers |
| Secrets table ciphertext | Never store plaintext secret values |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-04 | Information Disclosure | action_secrets | high | mitigate | Store only ciphertext + name; no plaintext columns (D-ACT-17) |
| T-19-05 | Tampering | LOG_DIR paths | medium | mitigate | Internal run/job IDs only; reject path separators in IDs |
| T-19-SC | Tampering | cargo installs | high | mitigate | No new packages in this plan |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (19-02-T1): Actions migrations + db module</name>
  <files>crates/oxidean-db/migrations/sqlite/0016_actions.sql, crates/oxidean-db/migrations/postgres/0016_actions.sql, crates/oxidean-db/migrations/mysql/0016_actions.sql, crates/oxidean-db/src/actions.rs, crates/oxidean-db/src/lib.rs, crates/oxidean-db/tests/dialect_actions.rs</files>
  <behavior>
    - dialect_actions applies migration on sqlite/postgres/mysql fixtures
    - Tables exist for runners, runner_tokens, runs, jobs, commit_statuses, action_secrets; repos gain actions_enabled
  </behavior>
  <read_first>crates/oxidean-db/migrations/sqlite/0015_packages.sql, crates/oxidean-db/tests/dialect_packages.rs, .planning/phases/19-actions-runners/19-RESEARCH.md</read_first>
  <action>
    Add synchronized dialect migrations for the Actions data model in RESEARCH (runners, registration tokens, runs, jobs, commit_statuses, action_secrets, repo actions_enabled) per D-ACT-13 / D-ACT-17. Implement thin db accessors in actions.rs and export from lib.rs. Green dialect_actions parity assertions. Do not implement API handlers.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && cargo nextest run -p oxidean-db -E 'test(dialect_actions)'</automated>
  </verify>
  <done>dialect_actions passes on all dialects for Actions schema.</done>
</task>

<task type="auto">
  <name>Task 2 (19-02-T2): ACTIONS_LOG_DIR config + Compose</name>
  <files>crates/oxidean-api/src/config.rs, docker-compose.yml, docs/CONFIGURATION.md</files>
  <read_first>crates/oxidean-api/src/config.rs, docker-compose.yml, docs/CONFIGURATION.md</read_first>
  <action>
    Add OXIDEAN_ACTIONS_LOG_DIR env (absolute path in Compose, e.g. /var/actions-logs) mirroring PACKAGES_DIR / LFS_DIR patterns per D-ACT-13. Document in CONFIGURATION.md. Mount volume on api service. Include OXIDEAN_ACTIONS_ENABLED default true per D-ACT-06.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && rg -n 'OXIDEAN_ACTIONS_LOG_DIR' crates/oxidean-api/src/config.rs docker-compose.yml docs/CONFIGURATION.md | rg -v '^#' | rg -q . && rg -n 'OXIDEAN_ACTIONS_ENABLED' crates/oxidean-api/src/config.rs docs/CONFIGURATION.md | rg -v '^#' | rg -q .</automated>
  </verify>
  <done>Env vars present in config, Compose, and docs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (19-02-T3): Factory reset wipe Actions</name>
  <files>crates/oxidean-db/tests/factory_reset_actions.rs, crates/oxidean-db/src/lib.rs</files>
  <behavior>
    - Factory reset deletes action runs/jobs/runners/secrets rows and clears ACTIONS_LOG_DIR contents (D-ACT-19)
  </behavior>
  <read_first>crates/oxidean-db/tests/factory_reset_issues.rs, crates/oxidean-db/src/lib.rs</read_first>
  <action>
    Extend factory reset to wipe Actions tables and log directory contents per D-ACT-19. Add factory_reset_actions test. Keep registration bootstrap env token as operator-owned (not DB wipe concern).
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && cargo nextest run -p oxidean-db -E 'test(factory_reset_actions)'</automated>
  </verify>
  <done>factory_reset_actions test passes.</done>
</task>

</tasks>

<verification>
Schema + LOG_DIR + factory reset verified by nextest and rg gates.
</verification>

<success_criteria>
Persistence and ops volume ready for dispatcher and runner protocol.
</success_criteria>

<output>
Create `.planning/phases/19-actions-runners/19-02-SUMMARY.md` when done
</output>
