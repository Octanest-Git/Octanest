---
phase: 19-actions-runners
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/octanest-api/tests/actions_workflow_parse.rs
  - crates/octanest-api/tests/actions_triggers.rs
  - crates/octanest-api/tests/actions_rpc.rs
  - crates/octanest-api/tests/actions_runner_protocol.rs
  - crates/octanest-api/tests/actions_dispatch_policy.rs
  - crates/octanest-api/tests/commit_statuses.rs
  - crates/octanest-db/tests/dialect_actions.rs
requirements:
  - ACT-01
  - ACT-02
  - ACT-03
  - ACT-04
  - ACT-05
  - ACT-06
  - ACT-07
autonomous: true
user_setup: []
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Wave 0 discoverable stubs exist for workflow parse, triggers, RPC, runner protocol, dispatch policy, commit statuses, and dialect_actions (ACT-01..07, D-ACT-15)"
  artifacts:
    - path: crates/octanest-api/tests/actions_workflow_parse.rs
      provides: "ACT-01 failing stubs"
    - path: crates/octanest-api/tests/actions_runner_protocol.rs
      provides: "ACT-06 failing stubs"
    - path: crates/octanest-api/tests/actions_dispatch_policy.rs
      provides: "ACT-07 failing stubs"
    - path: crates/octanest-db/tests/dialect_actions.rs
      provides: "actions migration parity stub"
  key_links:
    - from: .planning/phases/19-actions-runners/19-VALIDATION.md
      to: crates/octanest-api/tests/actions_workflow_parse.rs
      via: "Wave 0 Requirements checklist"
  assumptions:
    - "ASSUME: Wave 0 stubs may #[ignore] or assert!(false) until later plans turn green"
    - "ASSUME: Migration id resolved at execute against latest dialect (after 0015_packages)"
  prohibitions:
    - statement: "Must not implement production Actions handlers, migrations, or runner image in this Wave 0 plan"
      flagged: true
    - statement: "Must not add new crates.io packages in Wave 0"
      flagged: true
---

<objective>
Scaffold Nyquist/Wave 0 failing Rust stubs for Phase 19 Actions parse, triggers, RPC, runner protocol, dispatch policy, commit statuses, and dialect parity before implementation plans turn them green.

Purpose: Every later `<automated>` verify names a real test path (19-VALIDATION.md Wave 0).
Output: Discoverable RED/ignored nextest filters matching actions_*, runner_*, commit_status, dialect_actions.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-actions-runners/19-CONTEXT.md
@.planning/phases/19-actions-runners/19-RESEARCH.md
@.planning/phases/19-actions-runners/19-VALIDATION.md
@crates/octanest-api/tests/package_rpc.rs
@crates/octanest-db/tests/dialect_packages.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → temp DB / ACTIONS_LOG_DIR | Parallel nextest isolation |
| Stub expectations → production auth | Runner tokens vs session cookies encoded before impl |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Spoofing | runner protocol stubs | high | mitigate | Stubs expect registration/runner tokens only; session Cookie ignored on `/api/actions` (D-ACT-18) |
| T-19-02 | Elevation of privilege | dispatch_policy stubs | high | mitigate | Stubs expect jobs never execute in-process; unmatched labels stay queued (D-ACT-10, D-ACT-20) |
| T-19-SC | Tampering | cargo/npm installs | high | mitigate | Wave 0 adds test files only; no new packages |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (19-00-T1): Actions API Wave 0 stubs</name>
  <files>crates/octanest-api/tests/actions_workflow_parse.rs, crates/octanest-api/tests/actions_triggers.rs, crates/octanest-api/tests/actions_rpc.rs, crates/octanest-api/tests/actions_runner_protocol.rs, crates/octanest-api/tests/actions_dispatch_policy.rs, crates/octanest-api/tests/commit_statuses.rs</files>
  <read_first>crates/octanest-api/tests/package_rpc.rs, crates/octanest-api/tests/generic_registry.rs, .planning/phases/19-actions-runners/19-VALIDATION.md</read_first>
  <action>
    Create six discoverable nextest integration stub files mirroring Phase 20 Wave 0 style. Each file documents the locked behavior it will enforce later and fails or is #[ignore] with a clear message. Cover: (1) workflow discover under .github/workflows for ACT-01 / D-ACT-01; (2) push + pull_request dispatch for ACT-02 / D-ACT-04; (3) session RPC list/detail/logs for ACT-03 / D-ACT-12; (4) Register/Declare/FetchTask/label match for ACT-06 / D-ACT-07 / D-ACT-09; (5) registered-only + no in-process exec for ACT-07 / D-ACT-10 / D-ACT-20; (6) commit status context `{workflow_name} / {job_id}` for D-ACT-15. Do not implement handlers or add dependencies.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && cargo nextest list -p octanest-api 2>/dev/null | rg -c 'actions_workflow_parse|actions_triggers|actions_rpc|actions_runner_protocol|actions_dispatch_policy|commit_statuses' | rg -v '^0$' ; test -f crates/octanest-api/tests/actions_workflow_parse.rs && test -f crates/octanest-api/tests/actions_dispatch_policy.rs && test -f crates/octanest-api/tests/commit_statuses.rs</automated>
  </verify>
  <done>All six API stub test files exist and are discoverable by name.</done>
</task>

<task type="auto">
  <name>Task 2 (19-00-T2): dialect_actions Wave 0 stub</name>
  <files>crates/octanest-db/tests/dialect_actions.rs</files>
  <read_first>crates/octanest-db/tests/dialect_packages.rs, crates/octanest-db/tests/dialect_lfs.rs</read_first>
  <action>
    Add dialect_actions stub that will later assert postgres/mysql/sqlite migration parity for runners, runs, jobs, statuses, secrets, and repo actions_enabled (D-ACT-13, D-ACT-17, D-ACT-19). Fail or ignore until 19-02. No production migrations in this plan.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && test -f crates/octanest-db/tests/dialect_actions.rs && rg -n 'actions|runner|commit_status' crates/octanest-db/tests/dialect_actions.rs | rg -v '^#' | rg -q .</automated>
  </verify>
  <done>dialect_actions stub file exists and references actions domain tables.</done>
</task>

</tasks>

<verification>
Wave 0 Rust stubs present per 19-VALIDATION.md checklist (API six + dialect).
</verification>

<success_criteria>
Later plans can reference real test file paths in `<automated>` verifies.
</success_criteria>

<output>
Create `.planning/phases/19-actions-runners/19-00-SUMMARY.md` when done
</output>
