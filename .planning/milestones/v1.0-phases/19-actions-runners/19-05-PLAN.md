---
phase: 19-actions-runners
plan: "05"
type: execute
wave: 4
depends_on:
  - "04"
files_modified:
  - crates/oxidean-api/Cargo.toml
  - crates/oxidean-api/build.rs
  - crates/oxidean-api/proto
  - crates/oxidean-api/src/actions/runner_proto.rs
  - crates/oxidean-api/src/actions/tokens.rs
  - crates/oxidean-api/tests/actions_runner_protocol.rs
requirements:
  - ACT-04
  - ACT-06
  - ACT-07
autonomous: false
user_setup: []
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Runner protocol implements Register, Declare, FetchTask, UpdateTask, UpdateLog compatible with act_runner expectations (ACT-06, D-ACT-07)"
    - "Custom runs-on labels match via label[:schema[:args]] semantics (D-ACT-09)"
    - "Registration tokens (instance/env bootstrap) create runners; jobs only assigned to registered runners (D-ACT-08, D-ACT-10)"
  artifacts:
    - path: crates/oxidean-api/src/actions/runner_proto.rs
      provides: "Full runner service handlers"
    - path: crates/oxidean-api/proto
      provides: "Pinned actions-proto definitions"
  key_links:
    - from: Register
      to: action_runners token_hash
      via: "registration token validation"
  assumptions:
    - "ASSUME: Pin a specific actions-proto-def commit/tag in-repo under crates/oxidean-api/proto"
  prohibitions:
    - statement: "Must not accept session cookies as runner credentials"
      flagged: true
---

<objective>
Complete Gitea Actions–compatible runner registration and job-dispatch protocol with label matching and registration tokens (ACT-06, ACT-04 foundation, ACT-07).

Purpose: Third-party and official runners can register and complete jobs against Oxidean.
Output: prost/Connect-compatible handlers; actions_runner_protocol fully green.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-actions-runners/19-RESEARCH.md
@.planning/phases/19-actions-runners/19-CONTEXT.md
@crates/oxidean-api/src/actions/runner_proto.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registration token → Register | Issues long-lived runner token |
| Runner token → Fetch/Update | Job assignment + log write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-11 | Spoofing | Register | critical | mitigate | Validate active registration token; hash runner tokens at rest (D-ACT-08) |
| T-19-12 | Elevation of privilege | FetchTask scope | high | mitigate | Enforce instance/org/repo scope on task pickup (D-ACT-08) |
| T-19-SC | Tampering | prost install | high | mitigate | Package Legitimacy [VERIFIED]; checkpoint if any [ASSUMED] helper added |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0 (19-05-T0): Confirm prost/protobuf deps</name>
  <files>crates/oxidean-api/Cargo.toml</files>
  <action>
    Before cargo add: confirm RESEARCH legitimacy table — prost/prost-build are [VERIFIED]. If executor adds any [ASSUMED] package (e.g. pbjson), pause and verify crates.io page legitimacy with the operator. Skip pause when only [VERIFIED] prost crates are added.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && rg -n 'Legitimacy|prost|ASSUMED' .planning/phases/19-actions-runners/19-RESEARCH.md | rg -v '^#' | rg -q prost</automated>
  </verify>
  <done>Operator acknowledges verified deps (or auto-advance when only VERIFIED crates).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1 (19-05-T1): Pin proto + prost codegen</name>
  <files>crates/oxidean-api/proto, crates/oxidean-api/build.rs, crates/oxidean-api/Cargo.toml</files>
  <behavior>
    - build.rs generates Rust types from pinned runner service protos
  </behavior>
  <read_first>.planning/phases/19-actions-runners/19-RESEARCH.md, crates/oxidean-api/Cargo.toml</read_first>
  <action>
    Vendor/pin actions-proto (RunnerService) definitions under crates/oxidean-api/proto and wire prost-build. Prefer keeping codegen in oxidean-api (RESEARCH discretion). Add only [VERIFIED] prost crates unless checkpoint approved otherwise.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && test -d crates/oxidean-api/proto && rg -n 'prost' crates/oxidean-api/Cargo.toml | rg -v '^#' | rg -q . && cargo check -p oxidean-api</automated>
  </verify>
  <done>Proto sources present; api crate builds with prost codegen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (19-05-T2): Full Register/Declare/Fetch/Update + labels</name>
  <files>crates/oxidean-api/src/actions/runner_proto.rs, crates/oxidean-api/src/actions/tokens.rs, crates/oxidean-api/tests/actions_runner_protocol.rs</files>
  <behavior>
    - Register with registration token returns runner identity + token (D-ACT-08)
    - Declare updates labels including custom label:schema:args (D-ACT-09)
    - FetchTask assigns matching queued job only
    - UpdateTask/UpdateLog persist status and log bytes
    - OXIDEAN_RUNNER_REGISTRATION_TOKEN env bootstrap works for instance scope
  </behavior>
  <read_first>crates/oxidean-api/src/actions/runner_proto.rs, crates/oxidean-api/tests/actions_runner_protocol.rs</read_first>
  <action>
    Expand runner_proto to full ACT-06 surface per D-ACT-07…10: Register, Declare, FetchTask, UpdateTask, UpdateLog; registration token helper module; env bootstrap token; custom label matching. Green entire actions_runner_protocol suite. Keep cookie auth rejected.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && cargo nextest run -p oxidean-api -E 'test(actions_runner_protocol)'</automated>
  </verify>
  <done>Full runner protocol tests green including labels and token bootstrap.</done>
</task>

</tasks>

<verification>
ACT-06 protocol complete; ACT-07 assignment rules enforced in protocol tests.
</verification>

<success_criteria>
act_runner-class clients can register and complete a job against documented endpoints.
</success_criteria>

<output>
Create `.planning/phases/19-actions-runners/19-05-SUMMARY.md` when done
</output>
