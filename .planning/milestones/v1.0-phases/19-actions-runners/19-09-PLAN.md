---
phase: 19-actions-runners
plan: "09"
type: execute
wave: 6
depends_on:
  - "07"
files_modified:
  - apps/web/src/lib/repo-chrome-active.ts
  - apps/web/src/components/repo/repo-chrome.tsrx
  - apps/web/src/routes/$owner.$repo.actions.tsrx
  - apps/web/src/routes/$owner.$repo.actions.$run.tsrx
  - apps/web/src/lib/actions-queries.ts
  - apps/web/src/routes/$owner.$repo.actions.integration.test.ts
  - apps/web/src/routes/$owner.$repo.actions.$run.integration.test.ts
requirements:
  - ACT-03
autonomous: true
user_setup: []
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "User can open repo Actions tab and see workflow runs with status (ACT-03, D-ACT-12)"
    - "User can open a run detail page and view job logs (ACT-03, D-ACT-13)"
  artifacts:
    - path: apps/web/src/routes/$owner.$repo.actions.tsrx
      provides: "Actions list route"
    - path: apps/web/src/routes/$owner.$repo.actions.$run.tsrx
      provides: "Run detail + logs"
  key_links:
    - from: RepoChrome Actions tab
      to: /$owner/$repo/actions
      via: "active: actions"
    - from: run detail
      to: actions RPC logs
      via: "TanStack Query"
  assumptions:
    - "ASSUME: Octane .tsrx only; load Octane skill before editing"
  prohibitions:
    - statement: "Must not remount RepoChrome inside leaf routes"
      flagged: true
---

<objective>
Ship Actions list and run-detail UI with logs in Octane, including RepoChrome Actions tab (ACT-03 / D-ACT-12 / D-ACT-13).

Purpose: Users can observe CI without CLI.
Output: Routes + queries; Vitest integration tests green.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-actions-runners/19-CONTEXT.md
@.agents/skills/octane/SKILL.md
@apps/web/src/components/repo/repo-chrome.tsrx
@apps/web/src/lib/repo-chrome-active.ts
@apps/web/src/routes/$owner.$repo.packages.tsrx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Actions RPC | Session auth + Read ACL |
| Logs view | May contain workflow output — still Read-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-19 | Information Disclosure | run logs UI | medium | mitigate | Enforce repo Read; private repos keep not_found anti-enumeration (D-ACT-18) |
| T-19-20 | Elevation of privilege | Actions tab | low | accept | Tab visible whenever repo readable; no Admin-only for viewing runs |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (19-09-T1): RepoChrome Actions tab</name>
  <files>apps/web/src/lib/repo-chrome-active.ts, apps/web/src/components/repo/repo-chrome.tsrx</files>
  <read_first>apps/web/src/lib/repo-chrome-active.ts, apps/web/src/components/repo/repo-chrome.tsrx, .agents/skills/octane/SKILL.md</read_first>
  <action>
    Extend RepoChromeActive with `actions` and add Actions nav link to `/{owner}/{repo}/actions` per D-ACT-12 / D-QH-01. Do not remount chrome in leaves. Follow existing Packages/Issues link styling.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && rg -n '"actions"|actionsHref|/actions' apps/web/src/lib/repo-chrome-active.ts apps/web/src/components/repo/repo-chrome.tsrx | rg -v '^#' | rg -q .</automated>
  </verify>
  <done>Actions tab present and path mapping works.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (19-09-T2): Actions list + detail routes</name>
  <files>apps/web/src/routes/$owner.$repo.actions.tsrx, apps/web/src/routes/$owner.$repo.actions.$run.tsrx, apps/web/src/lib/actions-queries.ts, apps/web/src/routes/$owner.$repo.actions.integration.test.ts, apps/web/src/routes/$owner.$repo.actions.$run.integration.test.ts</files>
  <behavior>
    - List shows runs with status badges
    - Detail shows jobs and log text for selected job
    - Loading/empty/error states covered
  </behavior>
  <read_first>.agents/skills/octane/SKILL.md, apps/web/src/routes/$owner.$repo.packages.tsrx, apps/web/src/lib/session-queries.ts</read_first>
  <action>
    Implement Octane routes and TanStack Query helpers for Actions list/detail/logs per ACT-03 / D-ACT-12 / D-ACT-13. Use session RPC from prior plans. Green both integration tests. Polling interval is Claude discretion.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && make web-lint && cd apps/web && bun run test:unit -- src/routes/\$owner.\$repo.actions.integration.test.ts src/routes/\$owner.\$repo.actions.\$run.integration.test.ts</automated>
  </verify>
  <done>Actions UI tests green; web-lint clean for touched files.</done>
</task>

<task type="auto">
  <name>Task 3 (19-09-T3): Format check</name>
  <files>apps/web/src/routes/$owner.$repo.actions.tsrx, apps/web/src/routes/$owner.$repo.actions.$run.tsrx, apps/web/src/components/repo/repo-chrome.tsrx</files>
  <read_first>AGENTS.md</read_first>
  <action>
    Run make web-format-check (or format) and fix any oxfmt issues on touched web files before commit.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && make web-format-check</automated>
  </verify>
  <done>web-format-check passes.</done>
</task>

</tasks>

<verification>
ACT-03 UI observable via Vitest + chrome link.
</verification>

<success_criteria>
Users can browse runs and logs in the forge UI.
</success_criteria>

<output>
Create `.planning/phases/19-actions-runners/19-09-SUMMARY.md` when done
</output>
