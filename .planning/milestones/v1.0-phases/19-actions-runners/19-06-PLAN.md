---
phase: 19-actions-runners
plan: "06"
type: execute
wave: 4
depends_on:
  - "04"
files_modified:
  - crates/octanest-api/src/actions/dispatch.rs
  - crates/octanest-api/src/actions/events.rs
  - crates/octanest-api/tests/actions_triggers.rs
  - crates/octanest-api/src/pr
requirements:
  - ACT-02
autonomous: true
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "pull_request events (open, synchronize, reopened) enqueue matching workflows (ACT-02, D-ACT-04)"
    - "Phase 12 PR lifecycle has a stable call site into actions::dispatch_pull_request (D-ACT-05)"
  artifacts:
    - path: crates/octanest-api/src/actions/events.rs
      provides: "PR event payload + dispatcher entry"
  key_links:
    - from: Phase 12 PR service
      to: crates/octanest-api/src/actions/events.rs
      via: "dispatch_pull_request"
  assumptions:
    - "ASSUME: If Phase 12 PR module is not yet merged on the execution branch, implement the public dispatch API + tests with a harness stub, and leave a clearly named hook module Phase 12 must call — do not invent full PR domain here"
  prohibitions:
    - statement: "Must not implement Phase 13 branch protection enforcement in this plan"
      flagged: true
---

<objective>
Add pull_request trigger dispatch and a stable Phase 12 integration hook (ACT-02 / D-ACT-04 / D-ACT-05).

Purpose: Complete event surface required by ACT-02 beyond push tracer.
Output: PR event dispatcher + greened actions_triggers PR cases; documented hook for Phase 12.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-actions-runners/19-CONTEXT.md
@.planning/phases/12-pull-requests/12-CONTEXT.md
@crates/octanest-api/src/actions/dispatch.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR service → Actions | Trusted internal call; still ACL-gated by repo existence |
| Fork PR head SHA | Workflows read from correct head commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-13 | Spoofing | PR event | medium | mitigate | Only internal PR service may call dispatch; no public unauthenticated trigger endpoint |
| T-19-14 | Information Disclosure | fork workflows | medium | mitigate | Use head SHA readable under PR ACL; respect private fork rules from Phase 12 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (19-06-T1): pull_request dispatch</name>
  <files>crates/octanest-api/src/actions/events.rs, crates/octanest-api/src/actions/dispatch.rs, crates/octanest-api/tests/actions_triggers.rs</files>
  <behavior>
    - open/synchronize/reopened enqueue workflows with on.pull_request
    - Workflows without pull_request trigger are skipped
    - Enable gates (D-ACT-06) apply
  </behavior>
  <read_first>crates/octanest-api/tests/actions_triggers.rs, .planning/phases/19-actions-runners/19-CONTEXT.md</read_first>
  <action>
    Implement actions::dispatch_pull_request(event) covering open, synchronize, and reopened per D-ACT-04 / D-ACT-05 / ACT-02. Reuse parse + enqueue from push path. Green PR cases in actions_triggers. Optional cancel-on-close is Claude discretion.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && cargo nextest run -p octanest-api -E 'test(actions_triggers)'</automated>
  </verify>
  <done>PR trigger cases green alongside push cases.</done>
</task>

<task type="auto">
  <name>Task 2 (19-06-T2): Phase 12 hook wiring</name>
  <files>crates/octanest-api/src/pr, crates/octanest-api/src/actions/events.rs</files>
  <read_first>.planning/phases/12-pull-requests/12-CONTEXT.md, crates/octanest-api/src/lib.rs</read_first>
  <action>
    Wire or publish a stable internal hook that Phase 12 PR lifecycle calls after open/sync/reopen (D-ACT-05). If PR module absent on branch, add `actions::hooks` documentation module + `#[cfg(test)]` harness proving the function is invocable, and a short comment in events.rs listing the Phase 12 call sites required. Do not build full PR UI/domain here.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && rg -n 'dispatch_pull_request' crates/octanest-api/src | rg -v '^#' | rg -q . && cargo nextest run -p octanest-api -E 'test(actions_triggers)'</automated>
  </verify>
  <done>dispatch_pull_request is the named Phase 12 integration entry point.</done>
</task>

</tasks>

<verification>
ACT-02 pull_request path tested; Phase 12 hook named.
</verification>

<success_criteria>
Push and pull_request both enqueue runs under enable gates.
</success_criteria>

<output>
Create `.planning/phases/19-actions-runners/19-06-SUMMARY.md` when done
</output>
