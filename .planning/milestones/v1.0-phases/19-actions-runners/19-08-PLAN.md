---
phase: 19-actions-runners
plan: "08"
type: execute
wave: 5
depends_on:
  - "05"
files_modified:
  - docker/oxidean-runner/Dockerfile
  - docker/oxidean-runner/README.md
  - docker-compose.yml
  - scripts/smoke-actions.sh
  - docs/DEPLOYMENT.md
  - docs/CONFIGURATION.md
requirements:
  - ACT-04
  - ACT-05
autonomous: true
user_setup:
  - service: docker
    why: "Build/run official Oxidean runner image for smoke"
    env_vars:
      - name: OXIDEAN_RUNNER_REGISTRATION_TOKEN
        source: "Admin UI or instance bootstrap env for runner register"
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Official Oxidean runner image exists and documents register+daemon against an instance (ACT-04, D-ACT-11)"
    - "Compose sidecar (or profile) and standalone bring-up are documented (ACT-05, D-ACT-14)"
  artifacts:
    - path: docker/oxidean-runner/Dockerfile
      provides: "Official runner image"
    - path: docker-compose.yml
      provides: "Optional actions runner service"
  key_links:
    - from: runner container
      to: api /api/actions
      via: "OXIDEAN_PUBLIC_ORIGIN registration"
  assumptions:
    - "ASSUME: Base image may be gitea/act_runner or pinned digest; document version"
  prohibitions:
    - statement: "Must not embed Docker socket into the api service for job execution"
      flagged: true
---

<objective>
Ship the official Oxidean runner image and Compose sidecar / standalone operator docs (ACT-04, ACT-05 / D-ACT-11 / D-ACT-14).

Purpose: Operators can register compute without managed cloud minutes.
Output: Dockerfile + compose service/profile + docs + smoke-actions checks for image presence.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-actions-runners/19-CONTEXT.md
@docker-compose.yml
@docs/DEPLOYMENT.md
@scripts/smoke-actions.sh
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runner host → forge | Needs network to PUBLIC_ORIGIN |
| Job containers → forge | Checkout must not use loopback registration URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-17 | Spoofing | registration token in compose | high | mitigate | Token via env/secret file; never commit real tokens; document rotation (D-ACT-08) |
| T-19-18 | Elevation of privilege | Docker socket on runner | medium | accept | Runner needs Docker for label:docker:// jobs; isolate runner host from api |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (19-08-T1): Official runner image</name>
  <files>docker/oxidean-runner/Dockerfile, docker/oxidean-runner/README.md</files>
  <read_first>.planning/phases/19-actions-runners/19-RESEARCH.md, .planning/phases/19-actions-runners/19-CONTEXT.md</read_first>
  <action>
    Create official Oxidean runner image based on act_runner lineage per D-ACT-11 / ACT-04. README covers register flags (instance URL, token, name, labels) and warns against localhost registration when jobs need checkout (Gitea Connection 2 lesson). Default labels include at least one docker-mapped label suitable for ubuntu-latest or document the mapping.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && test -f docker/oxidean-runner/Dockerfile && rg -n 'register|label|ORIGIN|token' docker/oxidean-runner/README.md | rg -v '^#' | rg -q .</automated>
  </verify>
  <done>Dockerfile + README exist with register instructions.</done>
</task>

<task type="auto">
  <name>Task 2 (19-08-T2): Compose sidecar + docs + smoke</name>
  <files>docker-compose.yml, docs/DEPLOYMENT.md, docs/CONFIGURATION.md, scripts/smoke-actions.sh</files>
  <read_first>docker-compose.yml, docs/DEPLOYMENT.md, scripts/smoke-actions.sh</read_first>
  <action>
    Add Compose runner service (profile `actions` or equivalent) using the official image, OXIDEAN_RUNNER_REGISTRATION_TOKEN, and non-loopback origin guidance per D-ACT-14 / ACT-05. Document standalone `docker run` path in DEPLOYMENT/CONFIGURATION. Expand smoke-actions to verify Dockerfile exists and, when Docker available, optionally build/start profile (skip-ok otherwise).
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && rg -n 'oxidean-runner|actions' docker-compose.yml | rg -v '^#' | rg -q . && rg -n 'runner|Actions|OXIDEAN_RUNNER' docs/DEPLOYMENT.md docs/CONFIGURATION.md | rg -v '^#' | rg -q . && bash scripts/smoke-actions.sh</automated>
  </verify>
  <done>Compose + docs + smoke cover official runner bring-up.</done>
</task>

</tasks>

<verification>
ACT-04/05 operator path documented and smoke-gated.
</verification>

<success_criteria>
Operators can run official runners without Oxidean-managed minutes.
</success_criteria>

<output>
Create `.planning/phases/19-actions-runners/19-08-SUMMARY.md` when done
</output>
