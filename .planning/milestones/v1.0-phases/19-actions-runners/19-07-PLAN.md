---
phase: 19-actions-runners
plan: "07"
type: execute
wave: 5
depends_on:
  - "05"
  - "06"
files_modified:
  - crates/octanest-api/src/actions/statuses.rs
  - crates/octanest-api/src/rpc.rs
  - crates/octanest-core/src
  - packages/api-client
  - crates/octanest-api/tests/commit_statuses.rs
requirements:
  - ACT-03
autonomous: true
user_setup: []
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Each job publishes commit status context `{workflow_name} / {job_id}` through queued→terminal states (D-ACT-15)"
    - "Statuses for a SHA are queryable via RPC/API for Phase 13 required checks (D-ACT-16)"
  artifacts:
    - path: crates/octanest-api/src/actions/statuses.rs
      provides: "Status publisher"
    - path: crates/octanest-api/tests/commit_statuses.rs
      provides: "Status contract tests"
  key_links:
    - from: action_jobs status transitions
      to: commit_statuses rows
      via: "statuses::upsert"
    - from: Phase 13 protection
      to: repo statuses RPC
      via: "list by sha + context"
  assumptions:
    - "ASSUME: Phase 13 enforcement consumes this API later; this plan only publishes + queries"
  prohibitions:
    - statement: "Must not implement branch protection rule CRUD or merge blocking UI here"
      flagged: true
---

<objective>
Publish and query commit status / check contexts for Actions jobs so Phase 13 can require them (D-ACT-15, D-ACT-16).

Purpose: Integration surface with branch protection (ORG-05/06, PR-08) without implementing Phase 13.
Output: Status writer on job transitions; list RPC; commit_statuses tests green; rpc-gen updated.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-actions-runners/19-CONTEXT.md
@.planning/REQUIREMENTS.md
@crates/octanest-api/src/rpc.rs
@crates/octanest-api/tests/commit_statuses.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Status write | Only Actions control plane / runner updates |
| Status read | Repo Read ACL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-15 | Tampering | commit_statuses | high | mitigate | No public unauthenticated status forge endpoint; writes from trusted job transitions only |
| T-19-16 | Spoofing | context names | medium | mitigate | Server-generated context `{workflow_name} / {job_id}` only (D-ACT-15) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (19-07-T1): Status publish on job transitions</name>
  <files>crates/octanest-api/src/actions/statuses.rs, crates/octanest-api/src/actions/runner_proto.rs, crates/octanest-api/src/actions/dispatch.rs, crates/octanest-api/tests/commit_statuses.rs</files>
  <behavior>
    - Enqueue writes pending status per job with context `{workflow_name} / {job_id}`
    - UpdateTask success/failure/cancelled updates state
    - target_url points at Actions run route when origin known
  </behavior>
  <read_first>crates/octanest-api/tests/commit_statuses.rs, .planning/phases/19-actions-runners/19-CONTEXT.md</read_first>
  <action>
    Implement statuses::upsert hooked from dispatch enqueue and runner UpdateTask per D-ACT-15. Green commit_statuses publish cases. Document context naming for Phase 13 in test comments / docs snippet pointer.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && cargo nextest run -p octanest-api -E 'test(commit_statuses)'</automated>
  </verify>
  <done>Status publish/update cases green with locked context format.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (19-07-T2): Query RPC for Phase 13</name>
  <files>crates/octanest-api/src/rpc.rs, crates/octanest-core/src, packages/api-client, crates/octanest-api/tests/commit_statuses.rs</files>
  <behavior>
    - Authenticated Read can list statuses for repo+sha
    - Private repo anti-enumeration consistent with existing patterns
  </behavior>
  <read_first>crates/octanest-api/src/rpc.rs, crates/octanest-api/tests/package_rpc.rs</read_first>
  <action>
    Add session RPC (e.g. repo.statuses.list or checks.list) returning contexts/states for a SHA per D-ACT-16. Run make rpc-gen. Extend commit_statuses tests for query ACL. Do not implement protection evaluation.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && make rpc-gen && make rpc-sync-check && cargo nextest run -p octanest-api -E 'test(commit_statuses)'</automated>
  </verify>
  <done>Statuses queryable via generated client; rpc-sync-check clean.</done>
</task>

</tasks>

<verification>
D-ACT-15/16 satisfied; Phase 13 can consume statuses later.
</verification>

<success_criteria>
Required-check contexts exist as a stable forge API.
</success_criteria>

<output>
Create `.planning/phases/19-actions-runners/19-07-SUMMARY.md` when done
</output>
