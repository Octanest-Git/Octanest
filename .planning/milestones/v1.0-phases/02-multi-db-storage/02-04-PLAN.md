---
phase: 02-multi-db-storage
plan: "04"
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - docker-compose.sqlite.yml
  - docker-compose.mysql.yml
  - Makefile
  - scripts/compose-smoke.sh
  - scripts/db-switch-dialect.sh
  - .gitignore
requirements:
  - PLAT-07
  - PLAT-08
autonomous: true
user_setup: []
must_haves:
  truths:
    - "make up, make up-mysql, make up-sqlite each bring up the stack on their dialect (D-14)"
    - "The sqlite overlay starts no database container and no postgres dependency (D-19)"
    - "SQLite data lives in host ./var bind-mounted to /app/var, and var/ is gitignored (D-17, D-19)"
    - "make smoke / smoke-mysql / smoke-sqlite assert system.db_probe returned the expected dialect (D-13)"
    - "make db-migrate runs migrations explicitly for OCTANEST_AUTO_MIGRATE=false (D-07)"
    - "make db-switch-dialect refuses a non-empty target database unless --force-empty (D-03, D-16)"
  artifacts:
    - path: docker-compose.sqlite.yml
      provides: "DB-container-free overlay with ./var bind mount"
    - path: scripts/compose-smoke.sh
      provides: "dialect-parameterised bring-up smoke with db_probe assertion"
    - path: scripts/db-switch-dialect.sh
      provides: "empty-target-only dialect switch helper"
  key_links:
    - from: Makefile
      to: scripts/compose-smoke.sh
      via: "COMPOSE_FILES + EXPECT_DIALECT env"
    - from: scripts/db-switch-dialect.sh
      to: crates/octanest-db/src/bin/migrate.rs
      via: "cargo run -p octanest-db --bin migrate -- --assert-empty"
---

<objective>
Give operators the three concrete bring-up paths and the guarded dialect-switch helper: a SQLite Compose overlay with no database container, Make targets for each dialect, and a smoke script that proves which dialect actually served the write.

Purpose: ROADMAP success criterion #3 (core write/read flow per dialect in local Compose) and CONTEXT D-03, D-13, D-14, D-16, D-17, D-19.
Output: `docker-compose.sqlite.yml`, expanded Makefile, dialect-aware smoke, `scripts/db-switch-dialect.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-multi-db-storage/02-CONTEXT.md
@.planning/phases/02-multi-db-storage/02-RESEARCH.md
@.planning/phases/02-multi-db-storage/02-03-SUMMARY.md
@.planning/phases/01-monorepo-scaffold/01-04-PLAN.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host `./var` → api container `/app/var` | Bind-mounted SQLite database file crosses the container boundary |
| Operator shell env (`DATABASE_URL`) → Make/scripts | Connection strings with credentials pass through command lines and logs |
| `make db-switch-dialect` → target database | A destructive-looking operation pointed at a possibly populated database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Information disclosure | SQLite file committed to git | mitigate | `.gitignore` gains `var/`; `make up-sqlite` creates `./var` up front so Docker does not create a root-owned directory |
| T-02-12 | Information disclosure | credentials echoed by smoke/switch scripts | mitigate | Scripts print the dialect and target host only; `DATABASE_URL` is never echoed and `set -x` is not used |
| T-02-13 | Tampering | dialect switch against a populated database | mitigate | `scripts/db-switch-dialect.sh` delegates to `migrate --assert-empty`, exits non-zero on a non-empty target, and never issues DROP/TRUNCATE — `--force-empty` is an explicit operator opt-in that still only runs migrations |
| T-02-14 | Spoofing | smoke passing against the wrong database | mitigate | Smoke asserts `"dialect":"<expected>"` from the `system.db_probe` response body, not merely HTTP 200 |
| T-02-15 | Denial of service | stale volumes/containers between dialect runs | accept | Local-only; smoke `trap`s a `compose down --remove-orphans` for the exact overlay set it started |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (02-04-T1): SQLite Compose overlay with no database container</name>
  <files>docker-compose.sqlite.yml, .gitignore</files>
  <read_first>docker-compose.yml, docker-compose.mysql.yml, .gitignore, .planning/phases/02-multi-db-storage/02-RESEARCH.md</read_first>
  <action>
Locks RESEARCH open item 1. Compose merges mappings and cannot delete keys, so removing the Postgres dependency requires the Compose `!reset` tag (Compose v2.24+) rather than a naive redeclaration.

1. Create `docker-compose.sqlite.yml`:

```yaml
# SQLite overlay (D-07, D-19): no DB container, file bind-mounted from ./var.
#   docker compose -f docker-compose.yml -f docker-compose.sqlite.yml up --build
# Requires Docker Compose v2.24+ for the !reset tag.
services:
  postgres:
    profiles: ["postgres"]

  api:
    depends_on: !reset null
    environment:
      DATABASE_URL: sqlite:/app/var/octanest.db
      OCTANEST_DB_DIALECT: sqlite
    volumes:
      - ./var:/app/var
```

   Two mechanisms are needed together: `profiles: ["postgres"]` keeps the Postgres service out of the default `up` set for this overlay, and `depends_on: !reset null` drops the api→postgres dependency so Compose does not auto-enable the profiled service. The base `docker-compose.yml` is left untouched, so plain `docker compose up` and `make up` keep the Phase 1 Postgres default (D-06).
2. Verify the resolved config before trusting it (RESEARCH pitfall 8): `docker compose -f docker-compose.yml -f docker-compose.sqlite.yml config` must succeed, must **not** list a `postgres` service, and the `api` service must have no `depends_on` on postgres and must carry the `./var:/app/var` bind mount. If the installed Compose rejects `!reset`, record the version in the SUMMARY and fall back to `depends_on: !override {}`; do not fall back to `--scale postgres=0`.
3. Add `var/` to `.gitignore` (next to the existing `data/` entry) — the SQLite database and WAL/SHM sidecars must never be committed (T-02-11).
4. `docker-compose.mysql.yml`: add `OCTANEST_DB_DIALECT: mysql` to the `api` environment so the mysql path exercises the D-01/D-02 agreement check too. No other change.
  </action>
  <verify>docker compose -f docker-compose.yml -f docker-compose.sqlite.yml config &amp;&amp; docker compose -f docker-compose.yml -f docker-compose.sqlite.yml config | grep -q '/app/var'</verify>
  <acceptance_criteria>
    - `test -f docker-compose.sqlite.yml`
    - `grep -q 'depends_on: !reset' docker-compose.sqlite.yml`
    - `grep -q './var:/app/var' docker-compose.sqlite.yml`
    - `grep -q 'sqlite:/app/var/octanest.db' docker-compose.sqlite.yml`
    - `docker compose -f docker-compose.yml -f docker-compose.sqlite.yml config` exits 0
    - `docker compose -f docker-compose.yml -f docker-compose.sqlite.yml config | grep -c 'postgres:'` is 0
    - `grep -q '^var/$' .gitignore`
    - `grep -q 'OCTANEST_DB_DIALECT: mysql' docker-compose.mysql.yml`
  </acceptance_criteria>
  <done>`docker compose config` proves a SQLite stack with no database service and no postgres dependency</done>
</task>

<task type="auto">
  <name>Task 2 (02-04-T2): Make targets for all three dialects, migrate, and dialect switch</name>
  <files>Makefile, scripts/db-switch-dialect.sh</files>
  <read_first>Makefile, docker-compose.sqlite.yml, crates/octanest-db/src/bin/migrate.rs, .planning/phases/02-multi-db-storage/02-CONTEXT.md</read_first>
  <action>
1. Extend the `Makefile` `.PHONY` list and add targets (D-14, D-07, D-16), keeping the existing `COMPOSE ?= docker compose` indirection:
   - `up-mysql`: `$(COMPOSE) -f docker-compose.yml -f docker-compose.mysql.yml --profile mysql up --build -d`
   - `up-sqlite`: `mkdir -p var` first (so the bind-mount source is owned by the operator, T-02-11), then `$(COMPOSE) -f docker-compose.yml -f docker-compose.sqlite.yml up --build -d`
   - `down-mysql` / `down-sqlite`: matching `down --remove-orphans` with the same `-f` sets
   - `smoke`: unchanged entrypoint, now `EXPECT_DIALECT=postgres ./scripts/compose-smoke.sh`
   - `smoke-mysql`: `COMPOSE_FILES="-f docker-compose.yml -f docker-compose.mysql.yml" COMPOSE_PROFILES=mysql EXPECT_DIALECT=mysql ./scripts/compose-smoke.sh`
   - `smoke-sqlite`: `mkdir -p var` then `COMPOSE_FILES="-f docker-compose.yml -f docker-compose.sqlite.yml" EXPECT_DIALECT=sqlite ./scripts/compose-smoke.sh`
   - `db-migrate`: `cargo run -q -p octanest-db --bin migrate` (the explicit path when `OCTANEST_AUTO_MIGRATE=false`, D-07)
   - `db-switch-dialect`: `./scripts/db-switch-dialect.sh` (passes `$(ARGS)` through so `make db-switch-dialect ARGS=--force-empty` works)
   - `db-matrix`: `cargo test -p octanest-db --test dialect_probe -- --nocapture` (the same command CI drives, so operators can reproduce a matrix leg locally by setting `DATABASE_URL`)
2. Extend `make help` with one line per new target plus copy-pasteable sample URLs (D-04 — Make help is a canonical operator doc surface):

```
	@echo "  make up-mysql       - compose up with the MySQL profile (D-07)"
	@echo "  make up-sqlite      - compose up with SQLite file in ./var (D-19)"
	@echo "  make smoke-mysql    - bring-up smoke asserting dialect=mysql"
	@echo "  make smoke-sqlite   - bring-up smoke asserting dialect=sqlite"
	@echo "  make db-migrate     - apply migrations for DATABASE_URL"
	@echo "  make db-switch-dialect - migrate an EMPTY target DB to a new dialect"
	@echo ""
	@echo "Sample DATABASE_URLs:"
	@echo "  postgres://octanest:octanest@localhost:5432/octanest"
	@echo "  mysql://octanest:octanest@127.0.0.1:3306/octanest"
	@echo "  sqlite:./var/octanest.db"
```

3. Create `scripts/db-switch-dialect.sh` (`chmod +x`), `set -euo pipefail`:
   - Require `DATABASE_URL`; exit 1 with `"set DATABASE_URL to the NEW dialect target before running"` when unset.
   - Print the detected dialect (derived from the URL prefix in-script) but never the URL itself (T-02-12).
   - Run `cargo run -q -p octanest-db --bin migrate -- --assert-empty "$@"` — the empty check and the migration both live in the Rust binary, so there is exactly one implementation (D-16 refuses unless empty; `--force-empty` is forwarded from `"$@"`).
   - On success print the follow-up step: persist `DATABASE_URL` in `.env`, and note that Phase 2 supports switching only an empty target (D-03) — no data is copied between dialects.
  </action>
  <verify>make help | grep -q 'up-sqlite' &amp;&amp; test -x scripts/db-switch-dialect.sh &amp;&amp; DATABASE_URL=sqlite:./target/tmp/switch/octanest.db ./scripts/db-switch-dialect.sh</verify>
  <acceptance_criteria>
    - `grep -q '^up-mysql:' Makefile && grep -q '^up-sqlite:' Makefile`
    - `grep -q '^smoke-mysql:' Makefile && grep -q '^smoke-sqlite:' Makefile`
    - `grep -q '^db-migrate:' Makefile && grep -q '^db-switch-dialect:' Makefile`
    - `make help` output contains `sqlite:./var/octanest.db` and `mysql://octanest:octanest@127.0.0.1:3306/octanest`
    - `test -x scripts/db-switch-dialect.sh`
    - `grep -q 'assert-empty' scripts/db-switch-dialect.sh`
    - `DATABASE_URL=sqlite:./target/tmp/switch/octanest.db ./scripts/db-switch-dialect.sh` exits 0 on an empty target, and re-running it against the now-migrated file exits non-zero without `--force-empty`
  </acceptance_criteria>
  <done>Every operator action named in D-14 and D-16 is a single make target with printable sample URLs</done>
</task>

<task type="auto">
  <name>Task 3 (02-04-T3): Dialect-asserting Compose smoke</name>
  <files>scripts/compose-smoke.sh</files>
  <read_first>scripts/compose-smoke.sh, docker-compose.sqlite.yml, Makefile</read_first>
  <action>
Extend the existing script rather than forking a second one (its Docker-Desktop/WSL discovery block stays untouched).

1. Parameterise the compose invocation: `COMPOSE_FILES="${COMPOSE_FILES:--f docker-compose.yml}"` and use `"${DOCKER[@]}" compose $COMPOSE_FILES ...` for `config`, `up --build -d --wait`, and the `cleanup` trap. Keep `COMPOSE_FILE` honoured for backward compatibility when `COMPOSE_FILES` is unset.
2. Add `EXPECT_DIALECT="${EXPECT_DIALECT:-postgres}"`.
3. After the existing `system.health` check, add the probe round-trip (D-13, ROADMAP criterion #3):

```bash
echo "==> RPC system.db_probe (expect dialect=$EXPECT_DIALECT)"
curl -fsS \
  -H 'Content-Type: application/json' \
  -H 'Octanest-RPC-Version: 1' \
  -d '{"procedure":"system.db_probe","input":{}}' \
  "$BASE_URL/api/rpc" > /tmp/octanest-smoke-probe.json
grep -Eq "\"dialect\"[[:space:]]*:[[:space:]]*\"$EXPECT_DIALECT\"" /tmp/octanest-smoke-probe.json
```

   Then call it a second time and assert `probe_count` increased, so the smoke proves a real write, not a cached read: extract with `grep -o '"probe_count"[^,}]*' | grep -o '[0-9]\+'` and compare the two values with a numeric test that fails the script when not strictly greater.
4. Print only the dialect and the two counts — never `DATABASE_URL`, and do not `tee` the raw response to stdout (T-02-12).
5. Final line stays `echo "==> smoke OK ($EXPECT_DIALECT)"`.
  </action>
  <verify>bash -n scripts/compose-smoke.sh &amp;&amp; grep -q 'system.db_probe' scripts/compose-smoke.sh &amp;&amp; make smoke-sqlite</verify>
  <acceptance_criteria>
    - `grep -q 'EXPECT_DIALECT' scripts/compose-smoke.sh`
    - `grep -q 'system.db_probe' scripts/compose-smoke.sh`
    - `grep -q 'probe_count' scripts/compose-smoke.sh`
    - `! grep -q 'echo .*DATABASE_URL' scripts/compose-smoke.sh`
    - `bash -n scripts/compose-smoke.sh` exits 0
    - `make smoke-sqlite` exits 0 on a machine with Docker Engine reachable; when Docker is unavailable the script exits with `docker engine not reachable` and the SUMMARY records it as an environment blocker (same convention as 01-04)
  </acceptance_criteria>
  <done>`make smoke*` fails loudly if the write landed on a different dialect than the operator asked for</done>
</task>

</tasks>

<verification>
- [ ] `docker compose -f docker-compose.yml -f docker-compose.sqlite.yml config` succeeds with no postgres service
- [ ] `docker compose -f docker-compose.yml config` still shows postgres as the default (D-06 unchanged)
- [ ] `make help` lists `up-mysql`, `up-sqlite`, `smoke-mysql`, `smoke-sqlite`, `db-migrate`, `db-switch-dialect`
- [ ] `make smoke`, `make smoke-mysql`, `make smoke-sqlite` each pass locally with Docker running (record any environment blocker in the SUMMARY)
- [ ] `scripts/db-switch-dialect.sh` refuses a non-empty target
- [ ] `git status --porcelain var/` is empty (gitignored)
</verification>

<success_criteria>
- D-03, D-13, D-14, D-16, D-17, D-19 delivered as operator-runnable commands
- ROADMAP criterion #3 provable locally on all three dialects
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-db-storage/02-04-SUMMARY.md`
</output>
