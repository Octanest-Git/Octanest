---
phase: 15-releases-transfer
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - crates/oxidean-api/src/repo/rename_transfer.rs
  - crates/oxidean-api/src/repo/mod.rs
  - crates/oxidean-api/src/rpc.rs
  - crates/oxidean-db/src/redirects.rs
  - crates/oxidean-db/src/repositories.rs
  - crates/oxidean-api/src/routes/git_smart_http.rs
  - crates/oxidean-api/src/ssh
  - crates/oxidean-api/src/jobs/reconcile.rs
  - crates/oxidean-api/src/jobs/schedule.rs
  - crates/oxidean-api/tests/repo_rename_transfer.rs
  - packages/api-client/src/index.ts
  - docs/CONFIGURATION.md
  - .env.example
requirements:
  - GIT-16
autonomous: true
user_setup: []
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Admin can rename a repository; bare dir moves; DB name updates (GIT-16, D-REL-07)"
    - "Old /{owner}/{repo} and Smart HTTP/SSH paths redirect within OXIDEAN_REPO_REDIRECT_RETENTION_DAYS (default 90) (D-REL-08)"
    - "Live repo at old path supersedes redirect; expired redirects purge via job"
    - "Non-admin rename returns soft repo.not_found"
  artifacts:
    - path: crates/oxidean-api/src/repo/rename_transfer.rs
      provides: "repo.rename + shared redirect resolve helper"
    - path: crates/oxidean-db/src/redirects.rs
      provides: "insert/lookup/delete/purge redirects"
  key_links:
    - from: crates/oxidean-api/src/routes/git_smart_http.rs
      to: crates/oxidean-db/src/redirects.rs
      via: "resolve old owner/name → current bare path or 302"
    - from: crates/oxidean-api/src/jobs/reconcile.rs
      to: repository_redirects.expires_at
      via: "purge expired rows"
  assumptions:
    - "ASSUME: Default redirect retention 90 days"
    - "ASSUME: Rename uses simple Admin form (no type-confirm); transfer type-confirm stays in 15-04"
    - "ASSUME: Shared resolve helper used by web/get, Smart HTTP, and SSH when cheap"
  prohibitions:
    - statement: "Must not leave redirects as the only resolution surface for git clients"
      flagged: true
    - statement: "Must not add new crates.io or npm packages"
      flagged: true
---

<objective>
Implement Admin repository rename with bare-disk move, finite-retention redirects, live-path supersede, purge job, and honor redirects on web + Smart HTTP (+ SSH via shared helper) (GIT-16).

Purpose: Deliver D-REL-07/08 rename semantics without breaking remotes during the retention window.
Output: Green repo_rename / redirect_* nextest; ENV docs for retention days.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-releases-transfer/15-CONTEXT.md
@.planning/phases/15-releases-transfer/15-RESEARCH.md
@crates/oxidean-api/src/git/mod.rs
@crates/oxidean-api/src/repo/collaborators.rs
@crates/oxidean-api/src/repo/mod.rs
@crates/oxidean-api/src/jobs/reconcile.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin mutate RPC | Rename name input; capability forge attempt |
| Redirect resolution | Open-redirect risk if target not server-resolved |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-09 | Elevation of privilege | repo.rename | high | mitigate | resolve_repo_for_admin only; soft not_found (D-REL-07) |
| T-15-10 | Spoofing | redirect target | medium | mitigate | Redirect rows store repo_id only; resolve current owner/name server-side |
| T-15-11 | Tampering | recreate at old path | medium | mitigate | Live repo wins; delete redirect on repo.create for that slug/name |
| T-15-SC | Tampering | packages | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (15-03-T1): repo.rename + disk move + redirect insert</name>
  <files>crates/oxidean-api/src/repo/rename_transfer.rs, crates/oxidean-api/src/repo/mod.rs, crates/oxidean-api/src/rpc.rs, crates/oxidean-db/src/redirects.rs, crates/oxidean-db/src/repositories.rs, crates/oxidean-api/tests/repo_rename_transfer.rs, packages/api-client/src/index.ts</files>
  <read_first>crates/oxidean-api/src/git/mod.rs, crates/oxidean-api/src/repo/mod.rs, crates/oxidean-api/src/repo/collaborators.rs, .planning/phases/15-releases-transfer/15-RESEARCH.md</read_first>
  <behavior>
    - Admin rename validates name, rejects name_taken, fs::rename bare dir before DB commit, updates repositories.name (GIT-16, D-REL-07)
    - Inserts repository_redirects with expires_at = now + retention days (D-REL-08)
    - Non-admin → soft not_found
    - Compensates disk on DB failure (mirror username migrate)
  </behavior>
  <reversibility rating="costly">Redirect table + retention purge job (D-REL-08)</reversibility>
  <action>
Implement repo.rename behind resolve_repo_for_admin (D-REL-07). Use bare_repo_path + validate_repo_name; disk rename before DB update; insert redirect with OXIDEAN_REPO_REDIRECT_RETENTION_DAYS default 90 (D-REL-08). Register RPC; make rpc-gen. Green rename tests from Wave 0. UI settings form lands in 15-05.
  </action>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && cargo nextest run -p oxidean-api -E 'test(repo_rename)'</automated>
    <fails_when>rpc-gen/rpc-sync-check exits non-zero or client drifts; or repo_rename nextest exits non-zero</fails_when>
  </verify>
  <done>Admin rename moves disk+DB and inserts retention-bounded redirect</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (15-03-T2): Redirect resolve on web get + Smart HTTP + SSH helper</name>
  <files>crates/oxidean-api/src/repo/rename_transfer.rs, crates/oxidean-api/src/repo/mod.rs, crates/oxidean-api/src/routes/git_smart_http.rs, crates/oxidean-api/src/ssh, crates/oxidean-api/tests/repo_rename_transfer.rs</files>
  <read_first>crates/oxidean-api/src/routes/git_smart_http.rs, crates/oxidean-api/src/repo/mod.rs</read_first>
  <behavior>
    - repo.get / resolve helper returns redirect target when old slug/name hit and redirect unexpired
    - Smart HTTP follows/serves redirected bare path (301/302 or internal resolve)
    - SSH resolution uses same helper when applicable
    - redirect_supersede: creating repo at old path deletes redirect
  </behavior>
  <action>
Add shared resolve_repo_or_redirect(owner, name) used by RPC get path, Smart HTTP, and SSH tracer. Honor live repo over redirect. On repo.create success for a slug/name, delete matching redirect row. Green redirect and redirect_supersede tests.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(redirect) | test(repo_rename) | test(git_smart)'</automated>
    <fails_when>cargo/nextest exits non-zero or expected redirect/rename/git_smart tests fail</fails_when>
  </verify>
  <done>Web + git clients honor redirects; live create supersedes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (15-03-T3): Redirect purge job + retention ENV docs</name>
  <files>crates/oxidean-api/src/jobs/reconcile.rs, crates/oxidean-api/src/jobs/schedule.rs, crates/oxidean-db/src/redirects.rs, docs/CONFIGURATION.md, .env.example, crates/oxidean-api/tests/repo_rename_transfer.rs</files>
  <read_first>crates/oxidean-api/src/jobs/reconcile.rs, crates/oxidean-api/src/jobs/schedule.rs, docs/CONFIGURATION.md</read_first>
  <behavior>
    - Expired redirects are deleted by scheduled/reconcile extension
    - OXIDEAN_REPO_REDIRECT_RETENTION_DAYS documented (default 90)
  </behavior>
  <action>
Extend orphan reconcile or sibling job to purge repository_redirects where expires_at &lt; now (D-REL-08). Document ENV in CONFIGURATION.md and .env.example. Add/green a purge unit or integration assertion in repo_rename_transfer tests.
  </action>
  <verify>
    <automated>rg -n 'OXIDEAN_REPO_REDIRECT_RETENTION' docs/CONFIGURATION.md .env.example && cargo nextest run -p oxidean-api -E 'test(redirect)'</automated>
    <fails_when>rg finds no OXIDEAN_REPO_REDIRECT_RETENTION in docs/ENV; or redirect nextest exits non-zero</fails_when>
  </verify>
  <done>Expired redirects purge; retention ENV documented</done>
</task>

</tasks>

<verification>
repo_rename + redirect + supersede + purge tests green; rpc-sync-check; retention ENV documented.
</verification>

<success_criteria>
Admin rename works with disk move and finite redirects honored by web and git (GIT-16).
</success_criteria>

<output>
Create `.planning/phases/15-releases-transfer/15-03-SUMMARY.md` when done
</output>
