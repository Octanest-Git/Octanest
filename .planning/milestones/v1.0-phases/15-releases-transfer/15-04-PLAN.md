---
phase: 15-releases-transfer
plan: "04"
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - crates/oxidean-api/src/repo/rename_transfer.rs
  - crates/oxidean-api/src/repo/mod.rs
  - crates/oxidean-api/src/rpc.rs
  - crates/oxidean-db/src/repositories.rs
  - crates/oxidean-db/src/redirects.rs
  - crates/oxidean-api/tests/repo_rename_transfer.rs
  - packages/api-client/src/index.ts
requirements:
  - GIT-17
autonomous: true
user_setup: []
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Admin can transfer a repository to another user or org after typing the repo name exactly (GIT-17, D-REL-09, D-REL-10)"
    - "Transfer rewrites owner_type/owner_id, moves bare git dir, inserts redirect; issues and LFS associations stay on repo_id (D-REL-10)"
    - "Phase 15 does not invent webhook/package stubs (D-REL-11)"
    - "Caller must be allowed to create under destination org when dest is org; name_taken rejected"
  artifacts:
    - path: crates/oxidean-api/src/repo/rename_transfer.rs
      provides: "repo.transfer with type-confirm"
  key_links:
    - from: repo.transfer
      to: repositories.owner_type/owner_id + bare path
      via: "ownership rewrite + fs::rename"
    - from: issues/lfs tables
      to: repositories.id
      via: "repo_id FK — no content copy (D-REL-10)"
  assumptions:
    - "ASSUME: Immediate transfer after type-confirm (no accept-email)"
    - "ASSUME: On user→user transfer, former personal owner is added as admin collaborator (GitHub parity discretion)"
    - "ASSUME: If issues/LFS tables absent at execute time, cascade tests assert ownership rewrite only and skip missing-table branches"
  prohibitions:
    - statement: "Must not invent stub webhooks or package rows for transfer cascade"
      flagged: true
    - statement: "Must not copy LFS OID files on transfer"
      flagged: true
    - statement: "Must not add new crates.io or npm packages"
      flagged: true
---

<objective>
Implement Admin repository transfer to user or organization with type-confirm, disk ownership move, redirect, and repo_id-preserving cascade for issues/LFS associations (GIT-17).

Purpose: Deliver D-REL-09/10/11 transfer without OID copies or webhook stubs.
Output: Green repo_transfer / cascade nextest; rpc-gen client.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-releases-transfer/15-CONTEXT.md
@.planning/phases/15-releases-transfer/15-RESEARCH.md
@.planning/phases/10-orgs-permissions/10-CONTEXT.md
@.planning/phases/11-issues/11-CONTEXT.md
@.planning/phases/14-git-lfs/14-CONTEXT.md
@crates/oxidean-api/src/repo/mod.rs
@crates/oxidean-api/src/repo/rename_transfer.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Transfer destination slug | Dumping repos into foreign orgs |
| Type-confirm string | Accidental destructive transfer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-12 | Elevation of privilege | repo.transfer | high | mitigate | resolve_repo_for_admin; dest org create permission same as repo.create; soft not_found (D-REL-09) |
| T-15-13 | Tampering | confirm_name | medium | mitigate | Exact name match or repo.confirm_mismatch / transfer_confirm_mismatch (D-REL-10) |
| T-15-14 | Information disclosure | transfer errors | medium | mitigate | Soft not_found for forbidden; structured name_taken for collisions |
| T-15-SC | Tampering | packages | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (15-04-T1): repo.transfer with type-confirm + disk/owner rewrite + redirect</name>
  <files>crates/oxidean-api/src/repo/rename_transfer.rs, crates/oxidean-api/src/repo/mod.rs, crates/oxidean-api/src/rpc.rs, crates/oxidean-db/src/repositories.rs, crates/oxidean-db/src/redirects.rs, crates/oxidean-api/tests/repo_rename_transfer.rs, packages/api-client/src/index.ts</files>
  <read_first>crates/oxidean-api/src/repo/mod.rs, crates/oxidean-api/src/repo/rename_transfer.rs, .planning/phases/15-releases-transfer/15-RESEARCH.md</read_first>
  <behavior>
    - Admin transfer requires confirm_name exact match (D-REL-10)
    - Destination user or org; ensure dest parent dir; fs::rename bare repo; update owner_type/owner_id; insert redirect for old slug/name (GIT-17, D-REL-09, D-REL-10)
    - Reject when dest already has lower(name); require create permission on dest org
  </behavior>
  <reversibility rating="costly">Ownership rewrite across tables + disk move (D-REL-10)</reversibility>
  <action>
Implement repo.transfer reusing soft-delete confirm pattern (D-REL-10) and Admin gate (D-REL-09). Move bare dir under destination owner slug; rewrite polymorphic owner; insert redirect like rename. Reuse redirect resolve from 15-03. make rpc-gen. Green transfer happy-path and confirm_mismatch tests. Per D-REL-11, only move existing ownership/git/issues/LFS-association concerns — do not create webhook or package stub tables.
  </action>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && cargo nextest run -p oxidean-api -E 'test(repo_transfer)'</automated>
    <fails_when>rpc-gen/rpc-sync-check exits non-zero or client drifts; or repo_transfer nextest exits non-zero</fails_when>
  </verify>
  <done>Admin transfer to user/org with type-confirm, disk move, and redirect green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (15-04-T2): Cascade policy — collaborators + issues/LFS by repo_id</name>
  <files>crates/oxidean-api/src/repo/rename_transfer.rs, crates/oxidean-api/tests/repo_rename_transfer.rs</files>
  <read_first>.planning/phases/14-git-lfs/14-CONTEXT.md, .planning/phases/11-issues/11-CONTEXT.md, crates/oxidean-api/src/repo/rename_transfer.rs</read_first>
  <behavior>
    - repository_collaborators rows remain keyed by repo_id
    - user→user: former owner becomes admin collaborator when not already (discretion)
    - When issues / lfs association tables exist, rows stay on same repo_id with no OID file copy (D-REL-10)
    - No webhook/package invent (D-REL-11)
  </behavior>
  <action>
After successful ownership rewrite: keep collaborator rows; on personal→personal transfer add former owner as admin collaborator if missing. Assert cascade tests: issue/LFS association counts unchanged by repo_id when schema present; never cp LFS OID store. Document behavior in brief code comments only where needed. Green repo_transfer_cascade stubs.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(repo_transfer) | test(repo_transfer_cascade)'</automated>
    <fails_when>cargo/nextest exits non-zero or expected transfer/cascade tests fail</fails_when>
  </verify>
  <done>Transfer cascade preserves repo_id associations; no LFS OID copy; no webhook stubs</done>
</task>

</tasks>

<verification>
repo_transfer + cascade nextest green; rpc-sync-check.
</verification>

<success_criteria>
Admin can transfer repos to user/org with type-confirm; git data moves; issues/LFS associations follow via repo_id (GIT-17).
</success_criteria>

<output>
Create `.planning/phases/15-releases-transfer/15-04-SUMMARY.md` when done
</output>
