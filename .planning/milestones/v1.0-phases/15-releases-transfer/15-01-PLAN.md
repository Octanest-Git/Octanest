---
phase: 15-releases-transfer
plan: "01"
type: execute
wave: 1
depends_on:
  - "00"
files_modified:
  - crates/oxidean-core/src/release_types.rs
  - crates/oxidean-core/src/lib.rs
  - crates/oxidean-db/migrations/sqlite
  - crates/oxidean-db/migrations/postgres
  - crates/oxidean-db/migrations/mysql
  - crates/oxidean-db/src/releases.rs
  - crates/oxidean-db/src/redirects.rs
  - crates/oxidean-db/src/lib.rs
  - crates/oxidean-api/src/release/mod.rs
  - crates/oxidean-api/src/rpc.rs
  - crates/oxidean-api/src/app.rs
  - crates/oxidean-api/tests/release_rpc.rs
  - crates/oxidean-db/tests/dialect_releases.rs
  - packages/api-client/src/index.ts
requirements:
  - GIT-14
autonomous: true
user_setup: []
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Write+ user can create a release bound to an existing tag with notes, draft, and prerelease flags via RPC (GIT-14, D-REL-01, D-REL-02, D-REL-12)"
    - "Missing tag returns release.tag_missing; drafts omitted from Read/anon list/get (D-REL-01, D-REL-12)"
    - "Author or Write+ can update notes/flags; Admin can delete a release (D-REL-03)"
  artifacts:
    - path: crates/oxidean-api/src/release/mod.rs
      provides: "release.create/list/get/update/delete RPC"
    - path: crates/oxidean-db/src/releases.rs
      provides: "releases + release_assets table helpers"
    - path: crates/oxidean-db/src/redirects.rs
      provides: "repository_redirects helpers (schema ready for rename plans)"
    - path: packages/api-client/src/index.ts
      provides: "generated release.* client after rpc-gen"
  key_links:
    - from: crates/oxidean-api/src/release/mod.rs
      to: crates/oxidean-api/src/repo/acl.rs
      via: "meets(Write) / resolve_repo_for_admin for delete"
    - from: crates/oxidean-api/src/release/mod.rs
      to: crates/oxidean-db/src/releases.rs
      via: "create/list/get/update/delete persistence"
  assumptions:
    - "ASSUME: Migration filename is next free 00NN_releases_redirects across sqlite/postgres/mysql at execute time"
    - "ASSUME: Discretion — drafts Write+-only visibility (GitHub-like)"
    - "ASSUME: release_assets + repository_redirects tables ship empty-ready in this migration; asset HTTP and rename mutate in later plans"
    - "ASSUME: Octane Releases tab UI lands in plan 15-06 (follow-up after this RPC tracer)"
  prohibitions:
    - statement: "Must not invent create-tag-from-target_commitish behavior"
      flagged: true
    - statement: "Must not store release binaries under LFS OID dir"
      flagged: true
    - statement: "Must not add new crates.io or npm packages"
      flagged: true
---

<objective>
Ship the end-to-end backend tracer for tag-bound releases with notes (create/list/get/update/delete) and land the shared `releases` / `release_assets` / `repository_redirects` migration for later asset, UI, and rename/transfer plans.

Purpose: Prove GIT-14 notes path through DB → RPC (D-REL-01/02/03/12) before Octane UI (15-06) and assets (15-02).
Output: Green release_rpc create/list/get/update/delete + draft ACL tests; rpc-gen client; dialect_releases green for schema.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-releases-transfer/15-CONTEXT.md
@.planning/phases/15-releases-transfer/15-RESEARCH.md
@.planning/phases/15-releases-transfer/15-VALIDATION.md
@.planning/phases/10-orgs-permissions/10-CONTEXT.md
@crates/oxidean-api/src/repo/acl.rs
@crates/oxidean-api/src/repo/collaborators.rs
@crates/oxidean-api/src/repo/mod.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → RPC session | Untrusted release title/notes/tag_name |
| Capability ladder | Write vs Admin vs Read for draft visibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-03 | Elevation of privilege | release.create/update/delete | high | mitigate | Write+ for create/update; Admin for delete via resolve_repo_for_admin; soft not_found (D-REL-03, D-REL-12) |
| T-15-04 | Information disclosure | release.list/get drafts | medium | mitigate | Filter draft=true unless meets(Write) (D-REL-12 discretion) |
| T-15-05 | Tampering | tag_name binding | medium | mitigate | Verify refs/tags/{tag} exists before insert; stable release.tag_missing (D-REL-01) |
| T-15-SC | Tampering | packages | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (15-01-T1): End-to-end create/list release for existing tag with notes</name>
  <files>crates/oxidean-core/src/release_types.rs, crates/oxidean-core/src/lib.rs, crates/oxidean-db/migrations/sqlite, crates/oxidean-db/migrations/postgres, crates/oxidean-db/migrations/mysql, crates/oxidean-db/src/releases.rs, crates/oxidean-db/src/redirects.rs, crates/oxidean-db/src/lib.rs, crates/oxidean-api/src/release/mod.rs, crates/oxidean-api/src/rpc.rs, crates/oxidean-api/src/app.rs, crates/oxidean-api/tests/release_rpc.rs, crates/oxidean-db/tests/dialect_releases.rs, packages/api-client/src/index.ts</files>
  <read_first>.planning/phases/15-releases-transfer/15-RESEARCH.md, crates/oxidean-api/src/repo/mod.rs, crates/oxidean-api/src/repo/collaborators.rs, crates/oxidean-db/migrations/sqlite/0010_orgs_acl.sql</read_first>
  <behavior>
    - release.create succeeds when refs/tags/{tag} exists; stores title/body/draft/prerelease (D-REL-01, D-REL-02)
    - release.create fails with release.tag_missing when tag absent (D-REL-01)
    - release.list returns published releases to Read; Write+ required for create (D-REL-12)
  </behavior>
  <action>
Land next-free tri-dialect migration `00NN_releases_redirects` with `releases` (repo_id, tag_name, title, body, draft, prerelease, author_id, timestamps), `release_assets` (release_id, filename, content_type, byte_size, uploader_id — ready for 15-02), and `repository_redirects` (old_owner_slug, old_name, repo_id, expires_at, UNIQUE slug+name — ready for 15-03) per D-REL-04/08 schema needs. Add core DTOs + db helpers. Implement release.create/list/get RPC: require_verified; resolve_repo_for_read; create path meets(Write); prove tag via existing GitBackend/repo.refs listing (D-REL-01); support draft+prerelease (D-REL-02). Register in rpc.rs; run make rpc-gen. Prefer ENV stubs OXIDEAN_RELEASE_ASSETS_DIR default var/release-assets and OXIDEAN_RELEASE_ASSET_MAX_BYTES default 536870912 (512 MiB) and OXIDEAN_REPO_REDIRECT_RETENTION_DAYS default 90 in AppState/config even if unused until later plans (discretion locks from RESEARCH). Turn Wave 0 release_create / dialect_releases stubs green for this slice. Octane Releases UI is plan 15-06 — do not add apps/web routes here.
  </action>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && cargo test -p oxidean-db --test dialect_releases && cargo nextest run -p oxidean-api -E 'test(release_create) | test(release_tag)'</automated>
    <fails_when>rpc-gen/rpc-sync-check exits non-zero or client drifts; dialect_releases fails; or release_create/release_tag nextest exits non-zero</fails_when>
  </verify>
  <done>Happy path create+list for existing tag with notes works via RPC; dialect parity green; tag_missing covered</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (15-01-T2): Draft visibility + update + Admin delete</name>
  <files>crates/oxidean-api/src/release/mod.rs, crates/oxidean-db/src/releases.rs, crates/oxidean-api/tests/release_rpc.rs</files>
  <read_first>crates/oxidean-api/src/release/mod.rs, crates/oxidean-api/src/repo/acl.rs</read_first>
  <behavior>
    - Draft list/get invisible to anon and Read-only; visible to Write+ (D-REL-12)
    - Author or Write+ can update notes/flags (D-REL-03)
    - Admin delete removes release row (asset file cleanup may no-op until 15-02) (D-REL-03, D-REL-12)
  </behavior>
  <action>
Implement release.update (Write+ or author) and release.delete (resolve_repo_for_admin) per D-REL-03 and D-REL-12. Enforce draft visibility on list/get. Green release_draft and delete tests from Wave 0. No Octane detail route in this plan (15-06).
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(release_draft) | test(release_update) | test(release_delete)'</automated>
    <fails_when>cargo/nextest exits non-zero or expected draft/update/delete tests fail</fails_when>
  </verify>
  <done>Draft ACL, update, and Admin delete green on RPC</done>
</task>

</tasks>

<verification>
release_create/tag/draft/update/delete nextest green; dialect_releases green; rpc-sync-check.
</verification>

<success_criteria>
Users with Write+ can publish tag-bound release notes with draft/prerelease via RPC; drafts hidden from Read; Admin can delete (UI in 15-06; assets in 15-02).
</success_criteria>

<output>
Create `.planning/phases/15-releases-transfer/15-01-SUMMARY.md` when done
</output>
