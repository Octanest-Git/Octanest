---
phase: 22-compose-ci-deploy
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - .railway/railway.ts
  - .railway/package.json
  - .railway/README.md
  - deploy/cloud/Caddyfile
  - deploy/cloud/Dockerfile
  - deploy/cloud/README.md
  - Makefile
  - docs/DEPLOYMENT.md
  - .env.example
requirements:
  - PLAT-02
autonomous: false
user_setup:
  - service: railway
    why: "Operator-linked Railway project for Oxidean Cloud IaC apply (not required for PR CI)"
    env_vars:
      - name: RAILWAY_TOKEN
        source: "Railway account token — dashboard / CLI login; never commit; never expose to fork PRs"
    dashboard_config:
      - task: "Create or link Oxidean Cloud project + custom domain"
        location: "Railway Dashboard → Project → Settings / Domains"
estimate:
  tokens: 70000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Operator can deploy api and web from the existing Dockerfiles on a Railway-class host (D-CLOUD-01)"
    - "Cloud default database is managed Postgres (D-CLOUD-02)"
    - "Cloud HTTP ingress uses a file-configured gateway mirroring Compose path priorities without a Docker socket (D-CLOUD-03)"
    - "Forge data paths are volume-mounted in the cloud topology (D-CLOUD-04)"
    - "IaC lives in .railway/railway.ts with secrets preserved out of git (D-CLOUD-05, D-CLOUD-06)"
  artifacts:
    - path: .railway/railway.ts
      provides: "Railway IaC for postgres + api + web + gateway (+ volumes)"
    - path: deploy/cloud/Caddyfile
      provides: "Path-priority reverse proxy for single public origin"
    - path: docs/DEPLOYMENT.md
      provides: "Concrete Railway-class deploy steps replacing VERIFY stubs"
  key_links:
    - from: .railway/railway.ts
      to: crates/oxidean-api/Dockerfile
      via: "DOCKERFILE builder / dockerfilePath"
    - from: .railway/railway.ts
      to: apps/web/Dockerfile
      via: "DOCKERFILE builder / dockerfilePath"
    - from: deploy/cloud/Caddyfile
      to: docker-compose.yml
      via: "mirrored path priority rules"
---

<objective>
Deliver a Railway-class Oxidean Cloud deploy path that runs the same api/web images as Compose, with file-based ingress, managed Postgres, volumes, and operator docs (per D-CLOUD-01…D-CLOUD-08).

Purpose: Close PLAT-02 without forking a cloud-only architecture.
Output: `.railway/` IaC, `deploy/cloud/` gateway, DEPLOYMENT/.env.example updates, Make helpers.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-compose-ci-deploy/22-CONTEXT.md
@.planning/phases/22-compose-ci-deploy/22-RESEARCH.md
@.agents/skills/use-railway/SKILL.md
@.agents/skills/use-railway/references/iac.md
@.agents/skills/use-railway/references/deploy.md
@docs/DEPLOYMENT.md
@docs/CONFIGURATION.md
@docker-compose.yml
@crates/oxidean-api/Dockerfile
@apps/web/Dockerfile
@.env.example
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: verify railway npm package + caddy image legitimacy</name>
  <action>
RESEARCH.md lists npm package railway and Docker image caddy as ASSUMED. Before installing or pinning them, open https://www.npmjs.com/package/railway and the Docker Hub/caddy image page; confirm publisher/ownership matches Railway Inc / official Caddy. Reply with APPROVED or names of rejected packages. Do not add deprecated railway.json.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-22 && test ! -f railway.json && test ! -f railway.toml; rg -n "ASSUMED" .planning/phases/22-compose-ci-deploy/22-RESEARCH.md | head -5</automated>
  </verify>
  <done>Human approved package/image legitimacy (or documented rejection with alternate).</done>
</task>

<task type="tracer">
  <name>End-to-end cloud path — gateway + api/web Dockerfiles in IaC</name>
  <files>.railway/railway.ts, .railway/package.json, .railway/README.md, deploy/cloud/Caddyfile, deploy/cloud/Dockerfile, deploy/cloud/README.md</files>
  <read_first>docker-compose.yml, crates/oxidean-api/Dockerfile, apps/web/Dockerfile, .agents/skills/use-railway/references/iac.md</read_first>
  <action>
Per D-CLOUD-01, D-CLOUD-02, D-CLOUD-03, D-CLOUD-05: After legitimacy approval, create nested .railway/package.json depending on railway (IaC SDK) isolated from the Bun monorepo root if practical. Author .railway/railway.ts defining managed postgres, api service (DOCKERFILE → crates/oxidean-api/Dockerfile, root context), web service (DOCKERFILE → apps/web/Dockerfile), and a gateway service built from deploy/cloud that reverse-proxies to private api/web hostnames. Implement deploy/cloud/Caddyfile (or Traefik file provider) mirroring Compose priorities: git PathRegexp .git, packages /v2|/npm|/generic, /api|/uploads|/health, then SPA to web. Prefer public Host catch-all suitable for Railway domains (not Host localhost). Wire DATABASE_URL from postgres plugin to api. Document in .railway/README.md and deploy/cloud/README.md that operators run railway config plan and only apply with explicit approval (D-CLOUD-07). Do not commit secrets; use preserve() for sensitive vars.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-22 && test -f .railway/railway.ts && test -f deploy/cloud/Caddyfile && test ! -f railway.json && rg -n "dockerfile|postgres|gateway|Caddyfile" .railway/railway.ts deploy/cloud/README.md | head -40</automated>
  </verify>
  <done>IaC references existing Dockerfiles + Postgres + gateway file config; no railway.json; READMEs describe plan/apply discipline.</done>
</task>

<task type="auto">
  <name>Task 3: Volumes, cloud env docs, Make helpers, SSH TCP note</name>
  <files>.railway/railway.ts, docs/DEPLOYMENT.md, .env.example, Makefile, deploy/cloud/README.md</files>
  <read_first>docs/DEPLOYMENT.md, docs/CONFIGURATION.md, .env.example, docker-compose.yml</read_first>
  <action>
Per D-CLOUD-04, D-CLOUD-06, D-CLOUD-07, D-CLOUD-08: Extend IaC with volume mounts for repos, lfs, packages, release-assets, uploads, and SSH host keys on the api service (paths aligned with Compose /var/*). Update docs/DEPLOYMENT.md to replace Railway VERIFY stubs with concrete steps: link project, set OXIDEAN_ENV=production, OXIDEAN_CORS_ORIGINS, OXIDEAN_PUBLIC_ORIGIN, OXIDEAN_ALLOW_SIGNUP=true, prefer OXIDEAN_AUTO_MIGRATE=false + migrate/preDeploy, gateway domain, optional TCP for OXIDEAN_SSH_PORT, rollback via Railway redeploy. Add a short cloud section to .env.example (commented). Add Make helpers such as cloud-plan that document or run railway config plan when CLI is available (no auto-apply). Note HTTPS Smart HTTP as the always-on cloud git path.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-22 && rg -n "Railway|OXIDEAN_ALLOW_SIGNUP|volume|SSH|cloud-plan" docs/DEPLOYMENT.md .env.example Makefile .railway/railway.ts | head -50</automated>
  </verify>
  <done>DEPLOYMENT.md describes Railway-class deploy of same images; volumes and cloud env documented; Make helper exists; SSH TCP optional note present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Railway API | IaC apply mutates cloud infrastructure |
| Public internet → gateway | Untrusted HTTP(S); must not expose Docker socket |
| Secrets store → api/web | Env vars must not appear in git |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-10 | Information disclosure | .railway / docs | high | mitigate | preserve() secrets; .env.example comments only; no tokens in CI |
| T-22-11 | Spoofing | X-Forwarded-For / rate limits | medium | mitigate | Document trusted gateway as sole public edge; api stays private |
| T-22-12 | Tampering | IaC apply | high | mitigate | Human-only railway config apply (D-CLOUD-07); plan before apply |
| T-22-13 | Elevation of privilege | Open signup | medium | mitigate | Document OXIDEAN_ALLOW_SIGNUP=true only for intentional cloud; code default remains fail-closed |
| T-22-14 | Denial of service | Public forge volumes | medium | accept | Quotas already exist in product; Phase 22 does not add WAF |
| T-22-SC | Tampering | npm railway / caddy image | high | mitigate | Blocking legitimacy checkpoint before install; pin versions |
</threat_model>

<verification>
Files exist for IaC + gateway; DEPLOYMENT.md no longer relies on empty Railway VERIFY stubs for the happy path; Makefile documents cloud-plan.
</verification>

<success_criteria>
An operator following DEPLOYMENT.md can deploy the same api/web images behind a file-configured gateway with managed Postgres on Railway-class hosting (PLAT-02).
</success_criteria>

<output>
Create `.planning/phases/22-compose-ci-deploy/22-02-SUMMARY.md` when done
</output>
