---
phase: 22-compose-ci-deploy
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - Makefile
  - docs/TESTING.md
  - scripts/ci-compose-smoke.sh
requirements:
  - PLAT-03
  - PLAT-09
autonomous: true
user_setup: []
estimate:
  tokens: 55000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every PR CI run builds and brings up Compose and asserts web/health/db_probe for Postgres (D-CI-01, D-CI-02)"
    - "Every PR CI run brings up Compose for SQLite and asserts dialect=sqlite (D-CI-02)"
    - "Every PR CI run brings up Compose for MySQL and asserts dialect=mysql (D-CI-03)"
    - "Compose bring-up CI fails closed when Docker is unavailable (D-CI-04)"
    - "Existing smoke-protocol job remains a separate job (D-CI-05)"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "compose-smoke matrix (or sibling jobs) invoking Make smoke targets"
    - path: scripts/ci-compose-smoke.sh
      provides: "optional thin CI wrapper around make smoke* with artifact-friendly logging"
    - path: docs/TESTING.md
      provides: "CI table documents compose bring-up matrix"
  key_links:
    - from: .github/workflows/ci.yml
      to: scripts/compose-smoke.sh
      via: "make smoke / smoke-sqlite / smoke-mysql"
    - from: docs/TESTING.md
      to: .github/workflows/ci.yml
      via: "compose-smoke job row"
---

<objective>
Wire existing Compose dialect smokes into PR CI so every pull request builds images and proves bring-up health on Postgres, SQLite, and MySQL (per D-CI-01…D-CI-05).

Purpose: Close PLAT-03 and PLAT-09 without inventing a parallel smoke stack.
Output: GHA compose-smoke coverage + TESTING.md CI table update.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-compose-ci-deploy/22-CONTEXT.md
@.planning/phases/22-compose-ci-deploy/22-RESEARCH.md
@.github/workflows/ci.yml
@Makefile
@scripts/compose-smoke.sh
@scripts/ci-smoke-protocol.sh
@docs/TESTING.md
@docker-compose.yml
@docker-compose.sqlite.yml
@docker-compose.mysql.yml
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>End-to-end CI Compose bring-up — Postgres path only</name>
  <files>.github/workflows/ci.yml, scripts/ci-compose-smoke.sh, Makefile, docs/TESTING.md</files>
  <read_first>.github/workflows/ci.yml, scripts/compose-smoke.sh, scripts/ci-smoke-protocol.sh, Makefile</read_first>
  <action>
Per D-CI-01 and D-CI-04: Add a GitHub Actions job (name like compose-smoke) that on pull_request and push to main: checks out the repo, optionally frees disk like the api-rust job, sets CI=true, and runs the existing Postgres bring-up smoke via make smoke (or a thin scripts/ci-compose-smoke.sh wrapper that only exports CI / SMOKE_REQUIRE_STACK and invokes make smoke). Do not rewrite dialect assertions — reuse scripts/compose-smoke.sh. On failure, upload compose logs /tmp/oxidean-smoke*.json as artifacts (mirror smoke-protocol pattern). Keep the existing config-only compose job and the separate smoke-protocol job (D-CI-05). Document the new job in docs/TESTING.md CI table in the same edit if the table is touched; otherwise Task 3 may finish the docs row.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-22 && rg -n "compose-smoke|make smoke" .github/workflows/ci.yml docs/TESTING.md Makefile scripts/ci-compose-smoke.sh 2>/dev/null | head -40; test -f scripts/compose-smoke.sh</automated>
  </verify>
  <done>CI workflow contains a job that invokes Postgres compose bring-up smoke; config-only compose and smoke-protocol jobs still present; smoke scripts unchanged in dialect contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SQLite Compose bring-up matrix leg</name>
  <files>.github/workflows/ci.yml, scripts/ci-compose-smoke.sh, Makefile</files>
  <read_first>docker-compose.sqlite.yml, Makefile, scripts/compose-smoke.sh, scripts/sqlite-host-dir.sh</read_first>
  <action>
Per D-CI-02: Expand the compose-smoke job into a matrix (or sibling job) that also runs make smoke-sqlite on ubuntu-latest. Ensure OXIDEAN_SQLITE_HOST_DIR resolves to a Linux path under the workspace (e.g. ./var) — do not use WSL docker.exe helpers in GHA. Prefer strategy.fail-fast: false so one dialect failure does not cancel others. Reuse Make’s existing smoke-sqlite recipe.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-22 && rg -n "smoke-sqlite|dialect: sqlite|sqlite" .github/workflows/ci.yml | head -30</automated>
  </verify>
  <done>PR CI includes a SQLite Compose bring-up path that calls make smoke-sqlite (or equivalent wrapper with EXPECT_DIALECT=sqlite).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: MySQL Compose bring-up in PR CI + TESTING docs</name>
  <files>.github/workflows/ci.yml, docs/TESTING.md, Makefile</files>
  <read_first>docker-compose.mysql.yml, docs/TESTING.md, .github/workflows/ci.yml</read_first>
  <action>
Per D-CI-03: Add MySQL Compose bring-up to the same PR workflow via make smoke-mysql (third matrix leg or sibling job with fail-fast false). Do not treat db-matrix dialect_probe as a substitute. Update docs/TESTING.md CI jobs table to list compose-smoke (postgres/sqlite/mysql bring-up), keep compose config-only and smoke-protocol and db-matrix rows accurate. Note D-CI-06: image build occurs via compose up --build inside smoke.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-22 && rg -n "smoke-mysql|compose-smoke" .github/workflows/ci.yml docs/TESTING.md | head -40</automated>
  </verify>
  <done>PR CI runs MySQL Compose bring-up smoke; TESTING.md documents the three-dialect compose-smoke coverage alongside existing jobs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fork PR → GHA runner | Untrusted code builds local images; no production deploy secrets |
| CI → ephemeral Compose | Localhost Traefik only; tear down after smoke |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Elevation of privilege | GHA secrets | high | mitigate | Compose-smoke uses no Railway/production tokens (D-CLOUD-07 / D-CI-06) |
| T-22-02 | Denial of service | compose-smoke matrix disk | medium | mitigate | Free-disk step; fail-fast false; artifact upload bounded to smoke logs |
| T-22-03 | Tampering | Smoke skip greenwash | high | mitigate | CI=true / fail-closed; Docker-missing must fail the job (D-CI-04) |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | This plan adds no new package-manager dependencies |
</threat_model>

<verification>
Workflow YAML lists three dialect bring-up invocations; TESTING.md CI table matches; smoke-protocol job still independent.
</verification>

<success_criteria>
PLAT-03 and PLAT-09 are satisfied by PR CI Compose bring-up for Postgres, SQLite, and MySQL using existing Make smoke targets.
</success_criteria>

<output>
Create `.planning/phases/22-compose-ci-deploy/22-01-SUMMARY.md` when done
</output>
