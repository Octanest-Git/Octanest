---
phase: 21-social-explore
plan: "05"
type: execute
wave: 5
depends_on:
  - "21-01"
files_modified:
  - crates/oxidean-git/src/backend.rs
  - crates/oxidean-git/src/cli.rs
  - crates/oxidean-db/src/repositories.rs
  - crates/oxidean-db/src/lib.rs
  - crates/oxidean-core/src/repo_types.rs
  - crates/oxidean-api/src/repo/mod.rs
  - crates/oxidean-api/src/repo/fork_network.rs
  - crates/oxidean-api/src/rpc.rs
  - crates/oxidean-api/tests/repo_fork.rs
  - packages/api-client
requirements:
  - SOC-04
autonomous: true
user_setup: []
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Signed-in user with Read can fork a public repo into a creatable owner and get a bare git copy with all refs (D-SOC-12…15)"
    - "fork_network_id equals the upstream root; head_valid_for_base matches D-PR-01…03 (D-SOC-14, D-SOC-17)"
    - "Private source and duplicate (owner, network) forks are rejected (D-SOC-18, D-SOC-19)"
  artifacts:
    - path: crates/oxidean-api/src/repo/fork_network.rs
      provides: "Phase 12 head_valid_for_base helper"
    - path: crates/oxidean-api/tests/repo_fork.rs
      provides: "Green SOC-04 cases"
  key_links:
    - from: repo.fork
      to: GitBackend.clone_bare + repositories.fork_*
      via: "DB insert then bare copy with compensate on failure"
---

<objective>
Tracer for SOC-04: GitBackend bare copy, repo.fork RPC, fork_network helpers for Phase 12 D-PR-01…03 (D-SOC-12…19).

Purpose: Durable fork graph PR plans will consume.
Output: Green repo_fork nextest including network validation helper.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/oxidean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-social-explore/21-CONTEXT.md
@.planning/phases/12-pull-requests/12-CONTEXT.md
@.planning/phases/21-social-explore/21-RESEARCH.md
@crates/oxidean-git/src/backend.rs
@crates/oxidean-api/src/repo/mod.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session → repo.fork | Auth + Read on public source only |
| Fork → filesystem repos_dir | Must not escape bare_repo_path layout |
| Fork metadata → Phase 12 | Network id must be trustworthy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Information disclosure | repo.fork private source | high | mitigate | Public-only + not_found (D-SOC-12, D-SOC-19) |
| T-21-07 | Tampering | fork_network_id | high | mitigate | Server sets from source; client cannot supply network id |
| T-21-08 | Denial of service | bare clone large repo | medium | accept | Same class as normal clone; no new limiter beyond existing |
| T-21-SC | Tampering | package installs | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (21-05-T1): GitBackend clone_bare</name>
  <files>crates/oxidean-git/src/backend.rs, crates/oxidean-git/src/cli.rs</files>
  <behavior>
    - clone_bare(src, dst) produces a bare repo at dst with source refs
  </behavior>
  <read_first>crates/oxidean-git/src/backend.rs, crates/oxidean-git/src/cli.rs, .planning/phases/21-social-explore/21-RESEARCH.md</read_first>
  <action>
Add GitBackend::clone_bare using git clone --bare per D-SOC-15 / RESEARCH Pattern 3. Add unit/integration coverage in oxidean-git tests if a pattern exists; otherwise cover via repo_fork API test. Do not invent a second storage root.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-git -E 'test(clone_bare) | test(cli)' --no-fail-fast 2>/dev/null; cargo check -p oxidean-git</automated>
  </verify>
  <done>clone_bare available on GitBackend</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2 (21-05-T2): repo.fork end-to-end</name>
  <files>crates/oxidean-db/src/repositories.rs, crates/oxidean-db/src/lib.rs, crates/oxidean-core/src/repo_types.rs, crates/oxidean-api/src/repo/mod.rs, crates/oxidean-api/src/rpc.rs, crates/oxidean-api/tests/repo_fork.rs, packages/api-client</files>
  <behavior>
    - Fork public repo under self → new RepoPublic with forked_from / is_fork metadata
    - fork_network_id equals source.fork_network_id (root id)
    - Private source → repo.not_found or explicit denied matching ACL style
    - Second fork same owner+network → conflict error
    - name_taken when destination name exists
    - Compensate DB if clone_bare fails
  </behavior>
  <read_first>crates/oxidean-api/src/repo/mod.rs, crates/oxidean-api/tests/repo_fork.rs, .planning/phases/12-pull-requests/12-CONTEXT.md</read_first>
  <action>
Implement repo.fork per D-SOC-12…16 and D-SOC-18. Resolve destination owner like repo.create (self or creatable org). Copy description/default_branch; visibility public. Set forked_from_id and fork_network_id from source. Enforce one active fork per (owner_id, fork_network_id). Extend RepoPublic with fork fields. On git failure, compensate like create. make rpc-gen. Turn repo_fork Wave 0 tests green for these behaviors.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(repo_fork)'</automated>
  </verify>
  <done>repo.fork green with bare copy and network ids</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (21-05-T3): fork_network head_valid_for_base</name>
  <files>crates/oxidean-api/src/repo/fork_network.rs, crates/oxidean-api/tests/repo_fork.rs</files>
  <behavior>
    - Same repo id → true
    - Head with fork_network_id == base.id → true (D-PR-01, D-PR-03)
    - Unrelated repo → false
  </behavior>
  <read_first>.planning/phases/12-pull-requests/12-CONTEXT.md, crates/oxidean-api/tests/repo_fork.rs</read_first>
  <action>
Add fork_network.rs helper head_valid_for_base(base_repo_id, head_repo_row) implementing D-SOC-17 / D-PR-01…03. Cover in repo_fork tests. Document in a short module comment that Phase 12 compare/open must call this — do not implement PR UI.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(repo_fork)'</automated>
  </verify>
  <done>Phase 12 fork-head helper tested</done>
</task>

</tasks>

<verification>
SOC-04 core fork + network helper complete.
</verification>

<success_criteria>
Public forks work; network ids align with D-PR-01…03 (D-SOC-12…18).
</success_criteria>

<output>
Create `.planning/phases/21-social-explore/21-05-SUMMARY.md` when done
</output>
