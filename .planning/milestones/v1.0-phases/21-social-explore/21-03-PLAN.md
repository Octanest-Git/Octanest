---
phase: 21-social-explore
plan: "03"
type: execute
wave: 3
depends_on:
  - "21-01"
files_modified:
  - crates/octanest-core/src/auth_types.rs
  - crates/octanest-api/src/profile.rs
  - crates/octanest-api/src/user.rs
  - crates/octanest-api/src/rpc.rs
  - crates/octanest-api/tests/user_public_profile.rs
  - apps/web/src/routes/$owner.index.tsrx
  - apps/web/src/lib/ssr-org.ts
  - apps/web/src/lib/ssr-user-profile.ts
  - apps/web/src/routes/$owner.user-profile.integration.test.ts
  - packages/api-client
requirements:
  - SOC-02
autonomous: true
user_setup: []
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Anonymous or signed-in viewer can open /{username} and see public profile fields without email (D-SOC-05, D-SOC-06)"
    - "Profile lists repositories the viewer can Read under that user (D-SOC-07)"
    - "Org overview at /{org} continues to work (D-SOC-05)"
  artifacts:
    - path: apps/web/src/routes/$owner.index.tsrx
      provides: "User vs org branched owner index"
    - path: crates/octanest-api/tests/user_public_profile.rs
      provides: "Green SOC-02 RPC cases"
  key_links:
    - from: $owner.index loader
      to: user.getPublicProfile / org.get
      via: "shared slug resolver (D-SOC-05)"
---

<objective>
Deliver SOC-02 public user profiles at /{username} with ACL-filtered repos, without breaking org overview (D-SOC-05…08).

Purpose: Discovery of people and their public work.
Output: Green user_public_profile nextest + owner index user branch UI.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-social-explore/21-CONTEXT.md
@apps/web/src/routes/$owner.index.tsrx
@apps/web/src/lib/ssr-org.ts
@crates/octanest-api/src/rpc.rs
@.agents/skills/octane/SKILL.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public profile RPC → user row | Must strip email and secrets |
| Profile repo list → private repos | Viewer ACL only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-03 | Information disclosure | user.getPublicProfile | medium | mitigate | DTO without email (D-SOC-06) |
| T-21-01 | Information disclosure | profile repo list | high | mitigate | listByOwner ACL (D-SOC-07, D-SOC-19) |
| T-21-SC | Tampering | package installs | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (21-03-T1): user.getPublicProfile end-to-end</name>
  <files>crates/octanest-core/src/auth_types.rs, crates/octanest-api/src/profile.rs, crates/octanest-api/src/user.rs, crates/octanest-api/src/rpc.rs, crates/octanest-api/tests/user_public_profile.rs, packages/api-client</files>
  <behavior>
    - getPublicProfile(username) returns username, display_name, bio, avatar_url
    - Response JSON has no email key
    - Unknown username → not-found style error
  </behavior>
  <read_first>crates/octanest-api/src/profile.rs, crates/octanest-core/src/auth_types.rs, crates/octanest-api/tests/user_public_profile.rs</read_first>
  <action>
Implement user.getPublicProfile per D-SOC-06 and D-SOC-08. Reuse existing user row fields from AUTH-08. Wire rpc.rs; make rpc-gen; turn user_public_profile stubs green for DTO shape and not-found. Do not change settings/profile edit flow.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(user_public_profile)'</automated>
  </verify>
  <done>Public profile RPC green without email leakage</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (21-03-T2): Profile repo list ACL</name>
  <files>crates/octanest-api/src/repo/mod.rs, crates/octanest-api/tests/user_public_profile.rs</files>
  <behavior>
    - Anonymous sees only public repos for that user owner
    - Collaborator sees granted private repos on that profile list
  </behavior>
  <read_first>crates/octanest-api/src/repo/mod.rs, crates/octanest-api/src/repo/acl.rs</read_first>
  <action>
Ensure repo.listByOwner (or profile-bundled repos) filters by viewer Read under the profiled user owner per D-SOC-07. Extend user_public_profile tests accordingly.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(user_public_profile)'</automated>
  </verify>
  <done>ACL-filtered profile repos covered</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (21-03-T3): $owner.index user vs org UI</name>
  <files>apps/web/src/routes/$owner.index.tsrx, apps/web/src/lib/ssr-user-profile.ts, apps/web/src/lib/ssr-org.ts, apps/web/src/routes/$owner.user-profile.integration.test.ts</files>
  <behavior>
    - User slug renders profile heading with display name / @username and repo list
    - Org slug still renders org overview
    - Stars tab/section can call listStarred when viewing self or public stars list when appropriate (D-SOC-04)
  </behavior>
  <read_first>.agents/skills/octane/SKILL.md, apps/web/src/routes/$owner.index.tsrx, apps/web/src/lib/ssr-org.ts</read_first>
  <action>
Per D-SOC-05, branch $owner.index loader: org.get success → existing OrgOverviewPage; else user.getPublicProfile → new UserProfilePage UI (avatar, bio, Repositories list, Stars list using user.listStarred for the profiled user when API allows — if listStarred is caller-only, show Stars only when viewer.username matches profile). Prefer tabs on the same page. Preserve org behavior. Add integration test. Run make web-lint and make web-format-check.
  </action>
  <verify>
    <automated>bun --cwd apps/web test src/routes/\$owner.user-profile.integration.test.ts && make web-lint && make web-format-check</automated>
  </verify>
  <done>User and org owner index both work; lint clean</done>
</task>

</tasks>

<verification>
SOC-02 satisfied for profiles + org coexistence.
</verification>

<success_criteria>
/{username} public profile works; no email; ACL repos; orgs unbroken (D-SOC-05…08).
</success_criteria>

<output>
Create `.planning/phases/21-social-explore/21-03-SUMMARY.md` when done
</output>
