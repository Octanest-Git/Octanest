---
phase: 21-social-explore
plan: "01"
type: execute
wave: 1
depends_on:
  - "21-00"
files_modified:
  - crates/octanest-db/migrations/sqlite/0016_social.sql
  - crates/octanest-db/migrations/postgres/0016_social.sql
  - crates/octanest-db/migrations/mysql/0016_social.sql
  - crates/octanest-db/src/repositories.rs
  - crates/octanest-db/src/lib.rs
  - crates/octanest-db/tests/dialect_social.rs
  - crates/octanest-core/src/repo_types.rs
  - crates/octanest-api/src/repo/mod.rs
  - crates/octanest-api/src/rpc.rs
  - crates/octanest-api/tests/repo_stars.rs
  - packages/api-client
requirements:
  - SOC-01
autonomous: true
user_setup: []
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Signed-in user with Read can star and unstar a repository via RPC and see star_count / viewer_has_starred on repo.get (D-SOC-01, D-SOC-03)"
    - "Anonymous star is rejected; private without Read returns repo.not_found (D-SOC-19)"
  artifacts:
    - path: crates/octanest-db/migrations/sqlite/0016_social.sql
      provides: "repository_stars + star_count (+ fork columns if co-shipped)"
    - path: crates/octanest-api/tests/repo_stars.rs
      provides: "Green SOC-01 star/unstar cases"
  key_links:
    - from: repo.star RPC
      to: repository_stars + repositories.star_count
      via: "transactional insert/delete (D-SOC-02)"
---

<objective>
Tracer for SOC-01: dialect social migration for stars, RPC star/unstar wired end-to-end through DB → core DTO → rpc dispatcher → nextest (D-SOC-01…03, D-SOC-19).

Purpose: Prove star membership + counters before UI and explore sort depend on them.
Output: Green `test(repo_stars)` happy path; `make rpc-gen` client updated.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-social-explore/21-CONTEXT.md
@.planning/phases/21-social-explore/21-RESEARCH.md
@crates/octanest-api/src/repo/mod.rs
@crates/octanest-db/migrations/sqlite/0015_packages.sql
@crates/octanest-core/src/repo_types.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session cookie → repo.star | Untrusted actor; must be authenticated + Read |
| Star RPC → private repo metadata | Anti-enumeration via repo.not_found |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Information disclosure | repo.star / unstar | high | mitigate | ACL Read + not_found (D-SOC-19) |
| T-21-04 | Tampering | star_count | medium | mitigate | Same transaction as membership row (D-SOC-02) |
| T-21-SC | Tampering | package installs | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (21-01-T1): End-to-end star/unstar — one path</name>
  <files>crates/octanest-db/migrations/sqlite/0016_social.sql, crates/octanest-db/migrations/postgres/0016_social.sql, crates/octanest-db/migrations/mysql/0016_social.sql, crates/octanest-db/src/repositories.rs, crates/octanest-db/src/lib.rs, crates/octanest-core/src/repo_types.rs, crates/octanest-api/src/repo/mod.rs, crates/octanest-api/src/rpc.rs, crates/octanest-api/tests/repo_stars.rs, packages/api-client</files>
  <behavior>
    - Signed-in Read user stars public repo → star_count 1, viewer_has_starred true
    - Second star is idempotent (still count 1)
    - Unstar → count 0, viewer_has_starred false
    - Anonymous star fails auth
    - Private without access → repo.not_found
  </behavior>
  <read_first>.planning/phases/21-social-explore/21-RESEARCH.md, crates/octanest-api/src/repo/acl.rs, crates/octanest-api/src/repo/mod.rs, crates/octanest-db/tests/dialect_packages.rs</read_first>
  <action>
Implement the single star path across layers per D-SOC-01…03 and D-SOC-19. Confirm migration number after 0015_packages (prefer 0016_social). Create repository_stars (user_id, repository_id, created_at) with PK; add repositories.star_count default 0. Also add forked_from_id and fork_network_id columns now if that avoids a second migration — if added, backfill fork_network_id = id for existing rows and keep forked_from_id null (supports D-SOC-14 for later fork plan without changing create semantics yet: on insert_repository set fork_network_id = new id). Wire db helpers star/unstar + count; extend RepoPublic with star_count and viewer_has_starred; map in to_public; add repo.star and repo.unstar procedures; run make rpc-gen. Turn Wave 0 repo_stars tests green for the behaviors listed. Do not build Explore UI or Fork UI here.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(repo_stars)' --no-fail-fast</automated>
  </verify>
  <done>repo_stars nextest green for star/unstar path; rpc client regenerated</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (21-01-T2): dialect_social parity green</name>
  <files>crates/octanest-db/tests/dialect_social.rs</files>
  <behavior>
    - All three dialects include repository_stars and star_count
    - Fork columns present if co-shipped in 0016_social
  </behavior>
  <read_first>crates/octanest-db/tests/dialect_packages.rs, crates/octanest-db/migrations/sqlite/0016_social.sql</read_first>
  <action>
Turn dialect_social assertions green against the shipped 0016_social SQL for postgres/mysql/sqlite per RESEARCH Pattern 1–2 and D-SOC-02 / D-SOC-14.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-db -E 'test(dialect_social)'</automated>
  </verify>
  <done>dialect_social nextest green</done>
</task>

<task type="auto">
  <name>Task 3 (21-01-T3): Existing create sets fork_network_id</name>
  <files>crates/octanest-db/src/repositories.rs, crates/octanest-api/src/repo/mod.rs</files>
  <read_first>crates/octanest-api/src/repo/mod.rs</read_first>
  <action>
If fork_network_id column exists after T1, ensure repo.create / insert_repository initializes fork_network_id to the new repository id and forked_from_id null per D-SOC-14 so Phase 12 roots are well-defined before forks ship. Add or extend a focused assertion in dialect_social or an existing repo create test if cheap.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-db -E 'test(dialect_social)'</automated>
  </verify>
  <done>New repositories receive fork_network_id = id when column present</done>
</task>

</tasks>

<verification>
SOC-01 RPC path works; dialect migration parity holds.
</verification>

<success_criteria>
Star/unstar observable via RPC with correct counts and ACL behavior (D-SOC-01…03, D-SOC-19).
</success_criteria>

<output>
Create `.planning/phases/21-social-explore/21-01-SUMMARY.md` when done
</output>
