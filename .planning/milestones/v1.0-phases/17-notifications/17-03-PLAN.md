---
phase: 17-notifications
plan: "03"
type: execute
wave: 3
depends_on:
  - "17-01"
  - "17-02"
files_modified:
  - crates/oxidean-api/src/notify/mod.rs
  - crates/oxidean-api/src/pull/mod.rs
  - crates/oxidean-api/tests/notification_rpc.rs
requirements:
  - NOTF-01
autonomous: true
user_setup: []
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PR open/close/reopen/merge, reviews, comments, and review requests emit in-app notifications per D-02 / D-03 (NOTF-01)"
    - "PR actor is never notified; subject_kind is pull_request with /pull/{n} deep-link fields (D-05)"
  artifacts:
    - path: crates/oxidean-api/src/notify/mod.rs
      provides: "PR reason codes + recipient helpers"
  key_links:
    - from: crates/oxidean-api/src/pull/mod.rs
      to: crates/oxidean-api/src/notify/mod.rs
      via: "PR write success → fanout"
  assumptions:
    - "ASSUME: Phase 12 PR domain module exists at execute time (parallel-track rule)"
---

<objective>
Wire PR-domain notification emitters using Phase 12 CONTEXT event set (D-02 / D-03 / NOTF-01).

Purpose: Complete NOTF-01 for PR activity once Phase 12 handlers exist.
Output: Green tests proving PR events create pull_request notifications for participants/reviewers/mentions.
</objective>

<execution_context>
@/workspace/worktrees/phase-17/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/worktrees/phase-17/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-notifications/17-CONTEXT.md
@.planning/phases/12-pull-requests/12-CONTEXT.md
@.planning/phases/17-notifications/17-RESEARCH.md
@.planning/parallel-tracks/phase-17.md
@.planning/phases/17-notifications/17-01-SUMMARY.md
@.planning/phases/17-notifications/17-02-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR write → fanout | Same as issue; session actor excluded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-02 | Spoofing | PR fanout | medium | mitigate | Actor excluded; requested reviewers from domain tables only |
| T-17-01 | Information Disclosure | notification list | high | mitigate | Own-rows filter unchanged |
</threat_model>

<tasks>

<task type="auto" id="17-03-T1" tdd="true">
  <name>Task 1: PR open/close/merge/review/comment fan-out</name>
  <files>crates/oxidean-api/src/notify/mod.rs, crates/oxidean-api/src/pull/mod.rs, crates/oxidean-api/tests/notification_rpc.rs</files>
  <precondition>Phase 12 pull/PR API module and tables exist on the execute branch (rebase onto Phase 12 if needed per parallel-tracks/phase-17.md).</precondition>
  <behavior>
    - PR opened notifies requested reviewers and @mentions (not author/actor)
    - Review submit (Approve / Request changes / Comment) notifies PR author excluding reviewer
    - General and line comments notify author + participants excluding actor
    - Merge / close / reopen notify author + participants excluding actor (D-02 / D-03)
    - subject_kind pull_request and subject_number set for D-05 /pull/{n} links
  </behavior>
  <action>
  After confirming Phase 12 PR handlers exist, hook notify::fanout into PR create, close, reopen, merge, review submit, conversation comments, and line comments (D-02). Reuse participant rules (author, assignees if any, prior commenters, requested reviewers) minus actor (D-03). Skip force-push/retarget/draft/label noise (D-04). Extend notification_rpc (or PR-adjacent test) coverage. If PR module path differs from pull/mod.rs, use the actual Phase 12 module path.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(notification)'</automated>
  </verify>
  <done>PR core events emit pull_request notifications for non-actor participants.</done>
</task>

<task type="auto" id="17-03-T2" tdd="true">
  <name>Task 2: Review-requested + PR @mentions</name>
  <files>crates/oxidean-api/src/notify/mod.rs, crates/oxidean-api/src/pull/mod.rs, crates/oxidean-api/tests/notification_rpc.rs</files>
  <precondition>Phase 12 requested-reviewers API exists (D-PR-09).</precondition>
  <behavior>
    - Requesting a reviewer creates pr_review_requested for that user
    - @mentions in PR body/comments notify existing users (D-03)
  </behavior>
  <action>
  Hook review-request mutations to emit pr_review_requested (D-02 / D-03). Reuse mention parser from plan 17-02 on PR bodies/comments. Cover with automated tests.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(notification)'</automated>
  </verify>
  <done>Review requests and PR mentions notify the right users only.</done>
</task>

</tasks>

<verification>
notification nextest includes PR event cases; halt if Phase 12 precondition unmet.
</verification>

<success_criteria>
NOTF-01 covers PR activity per D-02/D-03 after Phase 12 is available.
</success_criteria>

<output>
Create `.planning/phases/17-notifications/17-03-SUMMARY.md` when done
</output>
