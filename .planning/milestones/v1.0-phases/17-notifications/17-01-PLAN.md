---
phase: 17-notifications
plan: "01"
type: execute
wave: 1
depends_on:
  - "17-00"
files_modified:
  - crates/oxidean-db/migrations/postgres/00NN_notifications.sql
  - crates/oxidean-db/migrations/mysql/00NN_notifications.sql
  - crates/oxidean-db/migrations/sqlite/00NN_notifications.sql
  - crates/oxidean-db/src/notifications.rs
  - crates/oxidean-db/src/lib.rs
  - crates/oxidean-core/src/notification_types.rs
  - crates/oxidean-core/src/lib.rs
  - crates/oxidean-api/src/notification/mod.rs
  - crates/oxidean-api/src/notify/mod.rs
  - crates/oxidean-api/src/rpc.rs
  - crates/oxidean-api/src/issue/mod.rs
  - crates/oxidean-api/tests/notification_rpc.rs
  - crates/oxidean-db/tests/dialect_notifications.rs
  - packages/api-client/
requirements:
  - NOTF-01
  - NOTF-02
autonomous: true
user_setup: []
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Signed-in user can call notification.list, unreadCount, markRead, markAllRead for their own rows only (NOTF-02 / D-12 / D-15)"
    - "When user B comments on user A's issue, A gets an unread in-app notification and B does not (NOTF-01 / D-01 / D-03)"
    - "make rpc-gen regenerated client exposes notification.* procedures"
  artifacts:
    - path: crates/oxidean-api/src/notification/mod.rs
      provides: "notification RPC handlers"
    - path: crates/oxidean-api/src/notify/mod.rs
      provides: "fanout helper"
    - path: crates/oxidean-db/src/notifications.rs
      provides: "notifications dialect SQL"
  key_links:
    - from: crates/oxidean-api/src/issue/mod.rs
      to: crates/oxidean-api/src/notify/mod.rs
      via: "issue.comments.create success → fanout"
    - from: crates/oxidean-api/src/rpc.rs
      to: crates/oxidean-api/src/notification/mod.rs
      via: "dispatch notification.*"
---

<objective>
End-to-end tracer for in-app notifications: schema + notification.* RPCs + one issue.comments.create fan-out path that notifies the issue author (NOTF-01 / NOTF-02 / D-01 / D-03 / D-12 / D-15).

Purpose: Prove architecture before expanding remaining issue/PR events and UI.
Output: Green notification_rpc nextest for list/unread/mark + comment→author notify; rpc-gen client updated.
</objective>

<execution_context>
@/workspace/worktrees/phase-17/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/worktrees/phase-17/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-notifications/17-CONTEXT.md
@.planning/phases/17-notifications/17-RESEARCH.md
@.planning/phases/17-notifications/17-VALIDATION.md
@crates/oxidean-api/src/issue/mod.rs
@crates/oxidean-api/src/rpc.rs
@crates/oxidean-api/tests/issue_comments.rs
@crates/oxidean-db/migrations/postgres/0011_issues.sql
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → notification.* RPC | Session required; recipient always session user |
| issue write → notify fanout | Actor excluded from recipients |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Information Disclosure / Elevation | notification.list / markRead | high | mitigate | Force recipient_id = session.user_id; never accept client recipient |
| T-17-02 | Spoofing | notify fanout | medium | mitigate | Actor id from session only; never notify actor (D-03) |
| T-17-SC | Tampering | package installs | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="tracer" id="17-01-T1" tdd="true">
  <name>End-to-end comment→notification→list/mark path</name>
  <files>crates/oxidean-db/migrations/postgres/00NN_notifications.sql, crates/oxidean-db/migrations/mysql/00NN_notifications.sql, crates/oxidean-db/migrations/sqlite/00NN_notifications.sql, crates/oxidean-db/src/notifications.rs, crates/oxidean-db/src/lib.rs, crates/oxidean-core/src/notification_types.rs, crates/oxidean-core/src/lib.rs, crates/oxidean-api/src/notification/mod.rs, crates/oxidean-api/src/notify/mod.rs, crates/oxidean-api/src/rpc.rs, crates/oxidean-api/src/issue/mod.rs, crates/oxidean-api/tests/notification_rpc.rs, crates/oxidean-db/tests/dialect_notifications.rs, packages/api-client/</files>
  <behavior>
    - Signed-in notification.list returns empty initially
    - User B comments on A's issue → A unreadCount becomes 1; B unreadCount stays 0 (D-01 / D-03)
    - notification.list filter unread returns the row with reason issue_comment and subject_kind issue
    - markRead on that id clears unread for A; markAllRead clears remaining
    - markRead with another user's notification id does not change that row / returns safe no-op or not_found without leaking
  </behavior>
  <action>
  Resolve next free migration number at execute time (after 0015_packages or later if Phase 12 added PR migrations). Create dialect-identical notifications tables per 17-RESEARCH schema with read_at null meaning unread (D-12).

  Add oxidean-db insert/list/mark/unread_count helpers filtering by recipient_id.

  Add core types and notification.* RPC handlers: list (Unread|All, offset/limit), unreadCount, markRead(ids), markAllRead — session required; own rows only (D-15 / T-17-01). Prefer namespace notification.* (discretion).

  Add notify::fanout helper: insert one row per recipient excluding actor (D-03); soft-fail log on insert errors after domain write succeeds.

  Wire ONE hook: after successful issue.comments.create, notify issue author if author != actor (tracer path for NOTF-01 / D-01). No other events yet.

  Do not call EmailSender for activity mail (D-06). Do not add notification preference RPCs (D-07). Do not add delete/archive/Done inbox RPCs (D-13) — list + mark read only.

  Run make rpc-gen. Turn Wave 0 notification_rpc and dialect_notifications stubs green for this tracer behavior.
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(notification)' &amp;&amp; cargo nextest run -p oxidean-db -E 'test(dialect_notifications)' &amp;&amp; make rpc-sync-check</automated>
  </verify>
  <done>Comment on another's issue creates unread for author; list/mark/unreadCount work for own rows; rpc client in sync.</done>
</task>

<task type="auto" id="17-01-T2">
  <name>Task 2: Harden markRead IDOR + list pagination contracts</name>
  <files>crates/oxidean-api/src/notification/mod.rs, crates/oxidean-api/tests/notification_rpc.rs</files>
  <behavior>
    - Unauthorized calls return auth.unauthenticated
    - List defaults newest-first with offset pagination matching Issues spirit (D-09)
    - markRead ignores foreign ids without mutating them (T-17-01)
  </behavior>
  <action>
  Expand notification_rpc tests and handlers so markRead only updates rows where recipient_id equals session user (D-15 / T-17-01). Confirm unauthenticated list/unreadCount fail closed. Keep reason/subject fields needed for later UI deep links (D-05).
  </action>
  <verify>
    <automated>cargo nextest run -p oxidean-api -E 'test(notification)'</automated>
  </verify>
  <done>IDOR and auth cases covered green for notification RPCs.</done>
</task>

<task type="auto" id="17-01-T3">
  <name>Task 3: Document subject payload fields for UI deep links</name>
  <files>crates/oxidean-core/src/notification_types.rs, docs/API.md</files>
  <action>
  Ensure NotificationPublic (or equivalent) exposes reason, subject_kind, owner/repo identifiers or repo_id+lookup fields, subject_number, subject_title, actor username, created_at, read_at so /notifications can build D-05 URLs without extra round trips where cheap. Update docs/API.md notification section. Re-run make rpc-gen if types change.
  </action>
  <verify>
    <automated>make rpc-sync-check &amp;&amp; cargo nextest run -p oxidean-api -E 'test(notification)'</automated>
  </verify>
  <done>Public notification payload supports subject deep links per D-05; API docs mention notification.*</done>
</task>

</tasks>

<verification>
cargo nextest notification + dialect_notifications green; rpc-sync-check clean.
</verification>

<success_criteria>
Tracer path proves NOTF-01 (comment→author) and NOTF-02 (list/mark) with session-bound rows.
</success_criteria>

<output>
Create `.planning/phases/17-notifications/17-01-SUMMARY.md` when done
</output>
