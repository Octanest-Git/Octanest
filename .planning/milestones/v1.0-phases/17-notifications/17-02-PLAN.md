---
phase: 17-notifications
plan: "02"
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - crates/octanest-api/src/notify/mod.rs
  - crates/octanest-api/src/issue/mod.rs
  - crates/octanest-api/tests/notification_rpc.rs
requirements:
  - NOTF-01
autonomous: true
user_setup: []
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Issue opened/closed/reopened/assigned/unassigned and comments notify participants per D-01 / D-03 (NOTF-01)"
    - "@username mentions in issue bodies/comments add mentioned users as recipients (D-03)"
    - "Reactions and label-only changes do not emit notifications (D-04)"
  artifacts:
    - path: crates/octanest-api/src/notify/mod.rs
      provides: "participant + mention recipient resolution"
  key_links:
    - from: crates/octanest-api/src/issue/mod.rs
      to: crates/octanest-api/src/notify/mod.rs
      via: "create/close/reopen/assignees/comments → fanout"
---

<objective>
Expand issue-side notification emitters beyond the tracer comment→author path to full D-01 events plus @mention recipients (NOTF-01 / D-03 / D-04).

Purpose: Complete issue activity coverage before PR hooks and UI.
Output: Green notification_rpc coverage for open/close/reopen/assign/mention cases; reactions/labels silent.
</objective>

<execution_context>
@/workspace/worktrees/phase-17/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/worktrees/phase-17/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-notifications/17-CONTEXT.md
@.planning/phases/17-notifications/17-RESEARCH.md
@.planning/phases/17-notifications/17-01-SUMMARY.md
@crates/octanest-api/src/issue/mod.rs
@crates/octanest-db/src/lib.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| issue mutation → fanout | Recipients must already be participants or resolvable usernames |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-02 | Spoofing | notify fanout | medium | mitigate | Actor excluded; mention only via find_user_by_username |
| T-17-04 | Information Disclosure | mention of private usernames | low | accept | Mentions require knowing the handle; no new search surface |
</threat_model>

<tasks>

<task type="auto" id="17-02-T1" tdd="true">
  <name>Task 1: Issue lifecycle + assignee fan-out</name>
  <files>crates/octanest-api/src/notify/mod.rs, crates/octanest-api/src/issue/mod.rs, crates/octanest-api/tests/notification_rpc.rs</files>
  <behavior>
    - issue.create notifies assignees if any were set in same flow, and participants from body mentions; author is actor so not self-notified on create except mentions of others
    - close/reopen notifies author + assignees + prior commenters excluding actor (D-01 / D-03)
    - assignees.set notifies newly assigned users (issue_assigned) and optionally unassigned (issue_unassigned)
    - issue.reactions.toggle and issue.labels.set produce no new notification rows (D-04)
  </behavior>
  <action>
  Extend notify::fanout callers for issue.create, close, reopen, and assignees.set after successful domain writes (D-01). Build recipient sets: author, current assignees, distinct prior commenter ids; always remove actor (D-03). Do not hook reactions or label mutations (D-04). Expand notification_rpc tests accordingly using existing issue test support patterns.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(notification)'</automated>
  </verify>
  <done>D-01 issue events emit participant notifications; D-04 events stay silent.</done>
</task>

<task type="auto" id="17-02-T2" tdd="true">
  <name>Task 2: @mention recipient parsing</name>
  <files>crates/octanest-api/src/notify/mod.rs, crates/octanest-api/tests/notification_rpc.rs</files>
  <behavior>
    - Body containing @otheruser notifies that user with reason issue_mention (or issue_comment with mention) when they exist (D-03)
    - Unknown @handles are ignored
    - Actor @self does not create a self-notification
  </behavior>
  <action>
  Implement server-side @username extraction on issue/comment bodies (plain text; do not rely on client markdown autolinks — mentions are disabled in renderGfm per research). Resolve via find_user_by_username; add to recipient set excluding actor (D-03). Keep regex simple GitHub-like handles. Cover with notification_rpc tests.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(notification)'</automated>
  </verify>
  <done>@mentions create notifications for existing users; actor never self-notified.</done>
</task>

</tasks>

<verification>
notification nextest covers lifecycle, assign, mention, and D-04 silence.
</verification>

<success_criteria>
NOTF-01 issue activity fan-out matches D-01/D-03/D-04.
</success_criteria>

<output>
Create `.planning/phases/17-notifications/17-02-SUMMARY.md` when done
</output>
