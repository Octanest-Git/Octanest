# Deferred polish (Class C) — integrate completeness campaign

Tracked after honesty + CONTEXT must-haves + Phase 19. Not required for umbrella ship once Class A/B land.

| Item | Notes |
|------|--------|
| Sitewide multi-entity GlobalSearch | Header search now routes to `/explore?q=` (SOC-03). Full users/orgs/commits/PRs/code search is a future phase. |
| `issue_comment` webhook event | D-HOOK-08 discretionary; notify fan-out exists, webhook emit does not. |
| Commit-status viewer UI | Configure contexts + merge blockers exist; dedicated Checks tab deferred. |
| Follow users | Deferred in 21-CONTEXT. |
| Watch repos | Deferred (notifications watch matrix). |
| Live `railway config apply` | Operator-only (D-CLOUD-07); CI must not hold tokens. |

Created: 2026-09-16
