---
phase: 20-packages-registry
plan: "02"
type: execute
wave: 1
depends_on:
  - "00"
files_modified:
  - crates/octanest-db/migrations/postgres/00xx_packages.sql
  - crates/octanest-db/migrations/mysql/00xx_packages.sql
  - crates/octanest-db/migrations/sqlite/00xx_packages.sql
  - crates/octanest-db/src/lib.rs
  - crates/octanest-db/tests/dialect_packages.rs
  - crates/octanest-core/src/auth_types.rs
  - docker-compose.yml
  - .env.example
  - docs/CONFIGURATION.md
  - apps/web/vite.config.ts
requirements:
  - PKG-04
  - PKG-01
  - PKG-02
  - PKG-03
autonomous: true
user_setup: []
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Tri-dialect packages migration applies with packages, versions, blobs, blob_refs, quota override tables (D-PKG-07, D-PKG-08, D-PKG-09)"
    - "OCTANEST_PACKAGES_DIR is configured separately from LFS and release-assets volumes (D-PKG-07)"
    - "Traefik and Vite route /v2, /npm, /generic to the API before the SPA (D-PKG-01)"
    - "Usernames v2, npm, and generic are reserved (pitfall 2)"
  artifacts:
    - path: crates/octanest-db/migrations/postgres/00xx_packages.sql
      provides: "Packages schema (number resolved at execute)"
    - path: docker-compose.yml
      provides: "api-packages Traefik router + packages volume"
    - path: crates/octanest-core/src/auth_types.rs
      provides: "RESERVED_USERNAMES includes v2/npm/generic"
  key_links:
    - from: docker-compose.yml
      to: crates/octanest-api/src/app.rs
      via: "PathPrefix /v2|/npm|/generic → api service"
    - from: .env.example
      to: OCTANEST_PACKAGES_DIR
      via: "operator env contract D-PKG-07"
  assumptions:
    - "ASSUME: Pick next free migration number after latest on disk at execute (may not be 0011 if Phases 11–19 claim numbers first)"
  prohibitions:
    - statement: "Must not share OCTANEST_PACKAGES_DIR with OCTANEST_LFS_DIR or release-asset paths"
      flagged: true
    - statement: "Must not implement OCI/npm/generic handlers in this plan — routing and schema only"
      flagged: true
---

<objective>
Land packages DB schema, separate PACKAGES_DIR volume/env, reserved registry path slugs, and Traefik/Vite edge routing so later protocol plans can mount handlers safely (D-PKG-01, D-PKG-07).

Purpose: Unblock store/ACL and protocol tracers without SPA stealing /v2.
Output: Applied migrations, Compose volume + api-packages router, Vite proxies, reserved usernames, CONFIGURATION note.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-packages-registry/20-CONTEXT.md
@.planning/phases/20-packages-registry/20-RESEARCH.md
@crates/octanest-db/migrations/postgres/0010_orgs_acl.sql
@crates/octanest-core/src/auth_types.rs
@docker-compose.yml
@apps/web/vite.config.ts
@docs/CONFIGURATION.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public edge → API | /v2|/npm|/generic must not hit SPA HTML |
| Operator env → blob root | PACKAGES_DIR path isolation from LFS/assets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Spoofing | Traefik/Vite | high | mitigate | PathPrefix priority ≥110 for /v2|/npm|/generic → api (D-PKG-01); Vite proxy for make dev |
| T-20-09 | Tampering | reserved slugs | medium | mitigate | Add v2, npm, generic to RESERVED_USERNAMES |
| T-20-SC | Tampering | packages | low | accept | No new crates.io deps |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (20-02-T1): Tri-dialect packages migration</name>
  <files>crates/octanest-db/migrations/postgres/00xx_packages.sql, crates/octanest-db/migrations/mysql/00xx_packages.sql, crates/octanest-db/migrations/sqlite/00xx_packages.sql, crates/octanest-db/src/lib.rs, crates/octanest-db/tests/dialect_packages.rs</files>
  <read_first>crates/octanest-db/migrations/postgres/0010_orgs_acl.sql, crates/octanest-db/tests/dialect_orgs.rs, crates/octanest-db/src/lib.rs, .planning/phases/20-packages-registry/20-RESEARCH.md</read_first>
  <behavior>
    - dialect_packages: migration applies on postgres/mysql/sqlite
    - Tables exist for packages (owner_type, owner_id, name, format, visibility, optional repository_id), package_versions, package_blobs (digest, size, refcount), package_blob_refs, package_quota_overrides
  </behavior>
  <action>
Resolve next free migration id at execute. Create identical-capability 00xx_packages.sql for postgres/mysql/sqlite per D-PKG-02 ownership (owner + name), optional repository link, D-PKG-08 blob digests + refcounts, D-PKG-09 per-owner quota overrides, format enum covering oci/npm/generic. Linked packages inherit visibility via repository_id; unlinked packages carry independent public|private defaulting private (RESEARCH discretion). Green dialect_packages.rs. All SQL stays inside octanest-db only.
  </action>
  <verify>
    <automated>cargo test -p octanest-db --lib migration_parity && cargo test -p octanest-db --test dialect_packages</automated>
  </verify>
  <done>Tri-dialect packages migration applies; dialect_packages green</done>
</task>

<task type="auto">
  <name>Task 2 (20-02-T2): OCTANEST_PACKAGES_DIR + Compose volume</name>
  <files>docker-compose.yml, .env.example, docs/CONFIGURATION.md</files>
  <read_first>docker-compose.yml, .env.example, docs/CONFIGURATION.md, .planning/phases/14-git-lfs/14-CONTEXT.md</read_first>
  <action>
Per D-PKG-07 add OCTANEST_PACKAGES_DIR env (Compose bind e.g. /var/packages), document defaults for max blob size and per-owner quota (RESEARCH discretion: 2 GiB blob / 10 GiB owner) in .env.example and docs/CONFIGURATION.md. Volume must be distinct from repos, LFS, and release-assets. Do not implement quota enforcement here.
  </action>
  <verify>
    <automated>rg -n 'OCTANEST_PACKAGES_DIR' docker-compose.yml .env.example docs/CONFIGURATION.md && rg -n 'PathPrefix\(`/v2`\)|PathPrefix\(`/npm`\)|PathPrefix\(`/generic`\)|api-packages' docker-compose.yml</automated>
  </verify>
  <done>PACKAGES_DIR env + volume documented; Compose labels present for registry prefixes</done>
</task>

<task type="auto">
  <name>Task 3 (20-02-T3): Reserved usernames + Vite proxies + Traefik labels</name>
  <files>crates/octanest-core/src/auth_types.rs, apps/web/vite.config.ts, docker-compose.yml</files>
  <read_first>crates/octanest-core/src/auth_types.rs, apps/web/vite.config.ts, docker-compose.yml</read_first>
  <action>
Add v2, npm, and generic to RESERVED_USERNAMES (pitfall 2). Add Traefik router api-packages with Host localhost and PathPrefix /v2 or /npm or /generic at priority 110 service api (D-PKG-01; mirror api-git). Add Vite proxy entries for /v2, /npm, /generic to apiProxyTarget for make dev. Finish any Compose label pieces not completed in T2.
  </action>
  <verify>
    <automated>rg -n '"v2"|"npm"|"generic"' crates/octanest-core/src/auth_types.rs && rg -n '"/v2"|"/npm"|"/generic"' apps/web/vite.config.ts && rg -n 'api-packages' docker-compose.yml</automated>
  </verify>
  <done>Reserved slugs and edge proxies/routers route registry prefixes to API</done>
</task>

</tasks>

<verification>
dialect_packages green; PACKAGES_DIR + Traefik/Vite + reserved names in place.
</verification>

<success_criteria>
Schema and edge routing ready for package store and protocol mounts; no protocol handlers yet.
</success_criteria>

<output>
Create `.planning/phases/20-packages-registry/20-02-SUMMARY.md` when done
</output>
