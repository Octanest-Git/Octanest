---
phase: 20-packages-registry
plan: "05"
type: execute
wave: 4
depends_on:
  - "04"
files_modified:
  - crates/octanest-api/src/packages/oci.rs
  - crates/octanest-api/src/packages/auth.rs
  - crates/octanest-api/tests/oci_registry.rs
requirements:
  - PKG-01
  - PKG-04
autonomous: true
user_setup: []
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "User can push and pull OCI images at /v2/{owner}/{image}/… with digests immutable and tags mutable (PKG-01, D-PKG-10, D-PKG-15)"
    - "Docker-style Bearer token challenge works with PAT-as-password at realm (D-PKG-04)"
    - "Anonymous pull works for public images; private requires auth (D-PKG-05)"
  artifacts:
    - path: crates/octanest-api/src/packages/oci.rs
      provides: "OCI Distribution Spec v1.1.1 end-1…10 handlers"
    - path: crates/octanest-api/src/packages/auth.rs
      provides: "Bearer realm + opaque token mint"
  key_links:
    - from: crates/octanest-api/src/packages/oci.rs
      to: crates/octanest-api/src/packages/store.rs
      via: "Layer/config/manifest bytes CA-deduped"
    - from: WWW-Authenticate challenge
      to: /v2/token (or chosen realm path)
      via: "PAT Basic → short-lived Bearer scoped to repository"
  assumptions:
    - "ASSUME: Pin OCI Distribution Spec v1.1.1; defer end-12 Referrers (404); cosign deferred"
    - "ASSUME: Nested OCI names owner/a/b allowed; repo link is metadata not path-encoded"
  prohibitions:
    - statement: "Must not implement Referrers API or cosign as a Phase 20 requirement"
      flagged: true
    - statement: "Must not mutate blob bytes for an existing digest"
      flagged: true
---

<objective>
Expand the tracer with fully featured OCI Distribution Spec push/pull, tags, and delete (GHCR-basic parity) plus Bearer token auth (PKG-01, D-PKG-15).

Purpose: Deliver container registry on same host under /v2.
Output: oci.rs complete for end-1…10; oci_registry tests green.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-packages-registry/20-RESEARCH.md
@crates/octanest-api/src/packages/oci.rs
@crates/octanest-api/src/packages/auth.rs
@crates/octanest-api/src/packages/store.rs
@crates/octanest-api/tests/oci_registry.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docker/podman → /v2 | Bearer challenge; Cookie ignored |
| Manifest put → digest store | Immutable digest content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-05 | Spoofing | token realm | high | mitigate | Short-lived opaque Bearer; scope ∩ ACL ∩ PAT; Basic uses PAT password |
| T-20-04 | Tampering | manifests/blobs | high | mitigate | Digest immutable; tag mutable retarget only (D-PKG-10) |
| T-20-02 | Elevation of privilege | push/delete | high | mitigate | Write+ publish; Admin delete; package:write required |
| T-20-SC | Tampering | installs | low | accept | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (20-05-T1): OCI Bearer token realm</name>
  <files>crates/octanest-api/src/packages/auth.rs, crates/octanest-api/src/packages/oci.rs, crates/octanest-api/tests/oci_registry.rs</files>
  <read_first>.planning/phases/20-packages-registry/20-RESEARCH.md Pitfall 3, crates/octanest-api/src/packages/auth.rs</read_first>
  <behavior>
    - Unauth /v2/* protected op returns 401 with WWW-Authenticate Bearer realm/service/scope
    - GET realm with Basic PAT mints token usable for subsequent pull/push
    - Cookie alone does not authenticate
  </behavior>
  <action>
Implement Docker Distribution token auth flow per RESEARCH (realm path /v2/token or /auth/token under API — discretion with docker/podman compatibility). Intersect requested scope with package ACL and PAT package scopes (D-PKG-04). Green oci_registry auth challenge cases.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(oci_registry)'</automated>
  </verify>
  <done>Bearer challenge + token mint works for OCI clients</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (20-05-T2): OCI blob upload + manifest push/pull</name>
  <files>crates/octanest-api/src/packages/oci.rs, crates/octanest-api/tests/oci_registry.rs</files>
  <read_first>.planning/phases/20-packages-registry/20-RESEARCH.md OCI endpoint subset, crates/octanest-api/src/packages/store.rs</read_first>
  <behavior>
    - end-1 GET /v2/; end-2/3 blob+manifest GET/HEAD; end-4..6 upload session; end-7 manifest PUT by tag or digest
    - Cross-package layer dedup via CA store
    - Public anon pull; private denied without token
  </behavior>
  <action>
Implement Distribution Spec v1.1.1 endpoints end-1 through end-7 for repository name {owner}/{image} (nested allowed) per D-PKG-01/03/15. Use uuid upload sessions and store.rs for blob bytes. Mount from existing packages/mod — do not remount prefixes. Green push/pull oci_registry cases.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(oci_registry)'</automated>
  </verify>
  <done>OCI push/pull manifests and blobs work</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (20-05-T3): OCI tags list + delete + immutability</name>
  <files>crates/octanest-api/src/packages/oci.rs, crates/octanest-api/tests/oci_registry.rs</files>
  <read_first>crates/octanest-api/src/packages/oci.rs</read_first>
  <behavior>
    - end-8 tags/list with optional n/last pagination
    - end-9/10 delete manifest by digest and blob delete with Admin + package:write
    - Re-PUT same digest content OK; conflicting digest body rejected; tag may retarget
  </behavior>
  <action>
Implement tags/list and deletes per D-PKG-10/15. Optional end-11 mount if cheap else document skip. Return 404 for end-12 referrers. Green remaining oci_registry Wave 0 cases for tags/delete/immutability.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(oci_registry)'</automated>
  </verify>
  <done>OCI tags/list/delete and digest immutability green — PKG-01 complete at protocol layer</done>
</task>

</tasks>

<verification>
oci_registry fully green for PKG-01 protocol surface.
</verification>

<success_criteria>
Fully featured OCI registry under /v2 with Bearer auth and GitHub-aligned immutability.
</success_criteria>

<output>
Create `.planning/phases/20-packages-registry/20-05-SUMMARY.md` when done
</output>
