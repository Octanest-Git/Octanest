---
phase: 20-packages-registry
plan: "08"
type: execute
wave: 5
depends_on:
  - "04"
  - "05"
  - "06"
files_modified:
  - crates/octanest-api/src/packages/rpc.rs
  - crates/octanest-api/src/rpc.rs
  - crates/octanest-core/src/package_types.rs
  - crates/octanest-api/tests/package_rpc.rs
  - packages/api-client/src/index.ts
requirements:
  - PKG-05
autonomous: true
user_setup: []
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Session RPC lists packages for an owner and for a linked repository (PKG-05, D-PKG-11)"
    - "packages.deleteVersion requires Admin capability, package permission, and confirm string name@version (D-PKG-06, D-PKG-12)"
  artifacts:
    - path: crates/octanest-api/src/packages/rpc.rs
      provides: "packages.list / packages.deleteVersion procedures"
    - path: crates/octanest-core/src/package_types.rs
      provides: "Package DTOs for RPC"
  key_links:
    - from: packages.deleteVersion
      to: packages/acl.rs
      via: "Admin + confirm string gate"
    - from: make rpc-gen
      to: packages/api-client
      via: "Generated client only"
  assumptions:
    - "ASSUME: UI type-to-confirm is enforced server-side via confirm field matching name@version (or OCI tag/digest equivalent)"
  prohibitions:
    - statement: "Must not hand-edit packages/api-client as source of truth"
      flagged: true
    - statement: "Must not allow Member/Write-only users to delete versions"
      flagged: true
---

<objective>
Add session-authenticated package list and delete RPCs so the web UI can manage versions (PKG-05, D-PKG-11, D-PKG-12).

Purpose: Protocol DELETEs exist; session RPC is the UI path with type-to-confirm.
Output: package_rpc tests green; rpc-gen synced.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-packages-registry/20-CONTEXT.md
@.planning/phases/20-packages-registry/20-RESEARCH.md
@crates/octanest-api/src/rpc.rs
@crates/octanest-api/src/packages/acl.rs
@crates/octanest-api/tests/package_rpc.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser session → RPC | Cookie OK for RPC only; not registry paths |
| deleteVersion confirm → destructive | Server validates confirm string |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-07 | Elevation of privilege | deleteVersion | high | mitigate | Admin ACL + confirm name@version (D-PKG-06, D-PKG-12) |
| T-20-03 | Information disclosure | list | medium | mitigate | Soft-not_found / empty for callers lacking Read on private packages |
| T-20-SC | Tampering | installs | low | accept | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (20-08-T1): packages.list RPC</name>
  <files>crates/octanest-core/src/package_types.rs, crates/octanest-api/src/packages/rpc.rs, crates/octanest-api/src/rpc.rs, packages/api-client/src/index.ts, crates/octanest-api/tests/package_rpc.rs</files>
  <read_first>crates/octanest-api/src/rpc.rs, crates/octanest-db/src/packages.rs</read_first>
  <behavior>
    - list by owner slug returns formats/names/versions caller may read
    - list by repository returns packages with matching repository_id
  </behavior>
  <action>
Add package DTOs and packages.list procedure wired through specta RPC. Run make rpc-gen. Green list cases in package_rpc.rs. Session auth only (no PAT Bearer for RPC — Phase 8 D-01 preserved).
  </action>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && cargo nextest run -p octanest-api -E 'test(package_rpc)'</automated>
  </verify>
  <done>packages.list works and client regenerated</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (20-08-T2): packages.deleteVersion with confirm</name>
  <files>crates/octanest-api/src/packages/rpc.rs, crates/octanest-api/tests/package_rpc.rs, packages/api-client/src/index.ts</files>
  <read_first>crates/octanest-api/src/packages/acl.rs, crates/octanest-api/src/packages/rpc.rs</read_first>
  <behavior>
    - deleteVersion with wrong/missing confirm → validation error
    - Non-Admin → forbidden
    - Admin + correct confirm removes version and decrements blob refs
  </behavior>
  <action>
Implement packages.deleteVersion requiring Admin Capability and confirm string equal to name@version (or documented OCI equivalent) per D-PKG-12 and D-PKG-06. After delete, npm/generic may republish same version. make rpc-gen. Green delete package_rpc cases.
  </action>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && cargo nextest run -p octanest-api -E 'test(package_rpc)'</automated>
  </verify>
  <done>package_rpc fully green for list/delete — PKG-05 API complete</done>
</task>

</tasks>

<verification>
package_rpc green; rpc-sync-check clean.
</verification>

<success_criteria>
Users with Admin can list and delete package versions via session RPC with type-to-confirm.
</success_criteria>

<output>
Create `.planning/phases/20-packages-registry/20-08-SUMMARY.md` when done
</output>
