---
phase: 20-packages-registry
plan: "09"
type: execute
wave: 6
depends_on:
  - "04"
  - "05"
  - "06"
files_modified:
  - crates/octanest-api/src/packages/store.rs
  - crates/octanest-api/src/packages/quota.rs
  - crates/octanest-api/src/jobs/mod.rs
  - crates/octanest-api/src/packages/rpc.rs
  - crates/octanest-api/tests/generic_registry.rs
  - crates/octanest-api/tests/package_acl.rs
  - packages/api-client/src/index.ts
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
autonomous: true
user_setup: []
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Uploads exceeding max blob size or per-owner quota are rejected (D-PKG-09)"
    - "Admin can read/override per-owner package quotas via RPC (D-PKG-09)"
    - "GC removes only blobs with refcount 0 after grace; live layers survive (pitfall 5)"
  artifacts:
    - path: crates/octanest-api/src/packages/quota.rs
      provides: "Quota check + Admin override RPCs"
    - path: crates/octanest-api/src/jobs/mod.rs
      provides: "Periodic package blob GC job"
  key_links:
    - from: store commit path
      to: quota.rs
      via: "Pre-commit size + owner usage check"
  assumptions:
    - "ASSUME: Defaults max blob 2 GiB and per-owner 10 GiB from env; Admin overrides in DB"
    - "ASSUME: GC every 24h with 7d grace after refcount 0; never GC open upload sessions"
  prohibitions:
    - statement: "Must not GC blobs still referenced by any package version or OCI manifest"
      flagged: true
---

<objective>
Enforce max blob size and per-owner quotas on all format uploads, expose Admin quota RPC, and add refcount-safe blob GC (D-PKG-09, D-PKG-08).

Purpose: Operators can bound storage; dedup GC must not break live images.
Output: Reject-over-quota behavior; Admin quota RPCs; GC job; tests covering reject + refcount safety.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-packages-registry/20-RESEARCH.md
@.planning/phases/14-git-lfs/14-CONTEXT.md
@crates/octanest-api/src/packages/store.rs
@crates/octanest-api/src/jobs/mod.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upload stream → quota | Enforce before commit; parallel upload races accounted |
| GC job → blob files | Only refcount 0 past grace |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-08 | Denial of service | uploads | high | mitigate | Max blob + owner quota reject; stream size enforcement (D-PKG-09) |
| T-20-11 | Tampering | GC | high | mitigate | Refcount + grace; never delete open upload sessions (pitfall 5) |
| T-20-SC | Tampering | installs | low | accept | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (20-09-T1): Quota enforcement on store commit</name>
  <files>crates/octanest-api/src/packages/quota.rs, crates/octanest-api/src/packages/store.rs, crates/octanest-api/tests/generic_registry.rs, crates/octanest-api/tests/package_acl.rs</files>
  <read_first>crates/octanest-api/src/packages/store.rs, .env.example</read_first>
  <behavior>
    - Over max blob size → reject with clear error
    - Over owner quota → reject even if blob size OK
    - Under quota → commit succeeds
  </behavior>
  <action>
Per D-PKG-09 wire env defaults and DB overrides into quota checks called from store commit used by generic/OCI/npm. Add tests via generic_registry and/or package_acl filters proving reject behavior.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(generic_registry) | test(package_acl) | test(package_quota)'</automated>
  </verify>
  <done>Over-quota uploads rejected across formats</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (20-09-T2): Admin quota/usage RPC</name>
  <files>crates/octanest-api/src/packages/rpc.rs, crates/octanest-api/src/packages/quota.rs, packages/api-client/src/index.ts</files>
  <read_first>crates/octanest-api/src/packages/rpc.rs</read_first>
  <behavior>
    - Instance Admin can get usage breakdown by format and package
    - Instance Admin can set per-owner override
    - Non-admin denied
  </behavior>
  <action>
Add Admin-gated RPC for package storage usage and quota overrides (breakdown dimensions per RESEARCH discretion). make rpc-gen. Keep Octane UI for 20-11.
  </action>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && cargo nextest run -p octanest-api -E 'test(package_rpc) | test(package_quota)'</automated>
  </verify>
  <done>Admin quota/usage RPC available and synced</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (20-09-T3): Refcount GC job</name>
  <files>crates/octanest-api/src/jobs/mod.rs, crates/octanest-api/src/packages/store.rs</files>
  <read_first>crates/octanest-api/src/jobs/mod.rs, crates/octanest-api/src/packages/store.rs</read_first>
  <behavior>
    - Blobs with refcount 0 older than grace are deleted from disk and DB
    - Referenced blobs remain after unrelated package deletes
  </behavior>
  <action>
Add periodic GC job (24h / 7d grace per RESEARCH discretion) for package blobs only under OCTANEST_PACKAGES_DIR. Include unit/integration proving cross-package OCI layer survival after delete of another package (pitfall 5).
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(package_gc) | test(oci_registry) | test(package_store)'</automated>
  </verify>
  <done>GC is refcount-safe; dry-run metrics optional in Admin later</done>
</task>

</tasks>

<verification>
Quota rejects work; Admin RPC synced; GC does not orphan live layers.
</verification>

<success_criteria>
D-PKG-09 quotas and D-PKG-08 GC safety delivered.
</success_criteria>

<output>
Create `.planning/phases/20-packages-registry/20-09-SUMMARY.md` when done
</output>
