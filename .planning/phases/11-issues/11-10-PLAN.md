---
phase: 11-issues
plan: "10"
type: execute
wave: 5
depends_on:
  - "05"
files_modified:
  - apps/web/package.json
  - apps/web/src/lib/markdown.ts
  - apps/web/src/lib/markdown.issues.test.ts
  - apps/web/src/lib/markdown.test.ts
  - apps/web/src/components/repo/markdown-write-preview.tsrx
requirements:
  - ISS-04
autonomous: true
user_setup: []
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "renderGfm autolinks #N and owner/repo#N to /{owner}/{repo}/issues/{n} with rehype-sanitize last (ISS-04, D-ISS-13, D-ISS-10)"
    - "remark-github@12.0.0 installed; Package Legitimacy OK; mentions/commits disabled via buildUrl false"
  artifacts:
    - path: apps/web/src/lib/markdown.ts
      provides: "remark-github extended renderGfm"
    - path: apps/web/src/lib/markdown.issues.test.ts
      provides: "Autolink + XSS regression green"
  key_links:
    - from: apps/web/src/components/repo/markdown-write-preview.tsrx
      to: apps/web/src/lib/markdown.ts
      via: "Preview passes owner/repo context for autolink"
  assumptions:
    - "ASSUME: remark-github buildUrl field names confirmed against installed 12.x types at implement time (RESEARCH A1)"
  prohibitions:
    - statement: "Must not hand-roll regex HTML post-processors for #N"
      flagged: true
    - statement: "Must not reorder pipeline to sanitize before remark-github transforms complete"
      flagged: true
---

<objective>
Install remark-github and extend renderGfm for `#N` / `owner/repo#N` autolinks while keeping sanitize-last (ISS-04, D-ISS-13, D-ISS-10).

Purpose: Markdown reference linking independent of the links table.
Output: Green markdown.issues tests; Preview/detail render autolinks.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-issues/11-RESEARCH.md
@apps/web/src/lib/markdown.ts
@apps/web/src/components/repo/markdown-write-preview.tsrx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User markdown → HTML | XSS via crafted links/HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03 | Tampering / XSS | renderGfm | high | mitigate | Pipeline remarkParse→remarkGfm→remarkGithub→remarkRehype→rehypeSanitize→rehypeStringify; sanitize last (D-ISS-10) |
| T-11-SC | Tampering | npm remark-github | high | mitigate | Legitimacy OK in RESEARCH; pin 12.0.0; no postinstall scripts |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (11-10-T1): Install remark-github@12.0.0</name>
  <files>apps/web/package.json</files>
  <read_first>.planning/phases/11-issues/11-RESEARCH.md</read_first>
  <action>
From apps/web run bun add remark-github@12.0.0. RESEARCH Package Legitimacy Audit verdict is OK/Approved — no blocking human legitimacy checkpoint required. Do not add other new packages.
  </action>
  <verify>
    <automated>rg -q '"remark-github"' apps/web/package.json && bun --cwd apps/web pm ls remark-github 2>/dev/null || bun --cwd apps/web exec node -e "require('remark-github/package.json')"</automated>
  </verify>
  <done>remark-github@12.x present in apps/web dependencies</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (11-10-T2): Extend renderGfm + green autolink tests</name>
  <files>apps/web/src/lib/markdown.ts, apps/web/src/lib/markdown.issues.test.ts, apps/web/src/lib/markdown.test.ts, apps/web/src/components/repo/markdown-write-preview.tsrx</files>
  <read_first>apps/web/src/lib/markdown.ts, apps/web/src/components/repo/markdown-write-preview.tsrx, .planning/phases/11-issues/11-RESEARCH.md</read_first>
  <behavior>
    - #42 with owner/repo context → href /owner/repo/issues/42
    - owner/repo#7 → /owner/repo/issues/7
    - script tags in markdown remain sanitized
    - mention/commit refs do not become links (buildUrl false)
  </behavior>
  <action>
Extend renderGfm with optional owner/repo context and remarkGithub buildUrl for issue refs only (D-ISS-13). Keep rehype-sanitize last (D-ISS-10). Thread owner/repo into MarkdownWritePreview Preview path so issue/comment surfaces autolink without editing detail route files in this plan. Green markdown.issues.test.ts and existing sanitize regressions in markdown.test.ts.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/lib/markdown.issues.test.ts src/lib/markdown.test.ts</automated>
  </verify>
  <done>Autolink + sanitize regressions green</done>
</task>

</tasks>

<verification>
ISS-04 markdown autolink complete with sanitize-last.
</verification>

<success_criteria>
#N and owner/repo#N autolink in issue/comment markdown; XSS regressions hold.
</success_criteria>

<output>
Create `.planning/phases/11-issues/11-10-SUMMARY.md` when done
</output>
