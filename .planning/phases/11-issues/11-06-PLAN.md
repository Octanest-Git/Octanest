---
phase: 11-issues
plan: "06"
type: execute
wave: 5
depends_on:
  - "05"
files_modified:
  - crates/octanest-api/src/issue/mod.rs
  - crates/octanest-api/src/label/mod.rs
  - crates/octanest-api/src/rpc.rs
  - crates/octanest-db/src/issue_labels.rs
  - crates/octanest-api/tests/issue_labels.rs
  - packages/api-client/src/index.ts
  - apps/web/src/components/repo/issue-labels-panel.tsrx
  - apps/web/src/routes/$owner.$repo.issues.labels.tsrx
  - apps/web/src/routes/$owner.settings.labels.tsrx
  - apps/web/src/routes/$owner.$repo.issues.$number.tsrx
  - apps/web/src/routes/$owner.$repo.issues.integration.test.ts
requirements:
  - ISS-03
autonomous: true
user_setup: []
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Admin can CRUD org + repo label definitions; Write+ can set labels on an issue (ISS-03, D-ISS-05, D-ISS-07)"
    - "Effective label set = inherited org labels (− hidden) ∪ repo-local; assign by label id (D-ISS-05)"
  artifacts:
    - path: crates/octanest-api/src/label/mod.rs
      provides: "label.* definition RPC"
    - path: apps/web/src/routes/$owner.$repo.issues.labels.tsrx
      provides: "Repo Admin label settings"
    - path: apps/web/src/routes/$owner.settings.labels.tsrx
      provides: "Org label catalog settings"
  key_links:
    - from: apps/web/src/components/repo/issue-labels-panel.tsrx
      to: crates/octanest-api/src/issue/mod.rs
      via: "issue.labels.set"
  assumptions:
    - "ASSUME (discretion): inherit + hide org label ids + local-only repo labels; unique name within scope"
  prohibitions:
    - statement: "Must not allow Write-only users to create/edit/delete label definitions"
      flagged: true
---

<objective>
Ship dual-scope labels (org defaults + repo overrides) with Admin defs and Write+ issue assignment (ISS-03, D-ISS-05, D-ISS-07).

Purpose: Label catalog + issue sidebar assignment.
Output: Green issue_labels tests; org/repo Labels settings + issue label picker.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-issues/11-CONTEXT.md
@.planning/phases/11-issues/11-RESEARCH.md
@apps/web/src/routes/$owner.settings.members.tsrx
@crates/octanest-api/src/org/mod.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Label definition CRUD | Admin-only |
| Issue label assignment | Write+ |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-02 | Elevation of privilege | label.create/update/delete | high | mitigate | meets(Admin) on repo/org as appropriate (D-ISS-07, D-ISS-20) |
| T-11-11 | Tampering | label color/name | low | mitigate | Validate color hex + name uniqueness per scope; rpc.bad_input |
| T-11-SC | Tampering | packages | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (11-06-T1): label.* defs + effective listForRepo</name>
  <files>crates/octanest-api/src/label/mod.rs, crates/octanest-api/src/rpc.rs, crates/octanest-db/src/issue_labels.rs, crates/octanest-api/tests/issue_labels.rs, packages/api-client/src/index.ts</files>
  <read_first>.planning/phases/11-issues/11-RESEARCH.md, crates/octanest-db/src/issue_labels.rs</read_first>
  <behavior>
    - Admin create/update/delete org and repo labels; non-Admin denied
    - listForRepo returns effective set with hide overrides applied
    - duplicate names within same scope rejected
  </behavior>
  <action>
Implement label.listForRepo, label.create|update|delete covering org catalog and repo local/hide overrides per D-ISS-05 discretion (inherit + hide + local-only). Admin-only defs (D-ISS-07). make rpc-gen. Start turning green issue_labels definition cases.
  </action>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && cargo nextest run -p octanest-api -E 'test(issue_labels)'</automated>
  </verify>
  <done>Label definition RPC + effective list green for covered cases</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (11-06-T2): issue.labels.set Write+</name>
  <files>crates/octanest-api/src/issue/mod.rs, crates/octanest-api/tests/issue_labels.rs, packages/api-client/src/index.ts</files>
  <read_first>crates/octanest-api/src/issue/mod.rs</read_first>
  <behavior>
    - Write+ can replace issue label id set from effective repo labels
    - Read-only cannot set labels; invalid/hidden ids rejected
  </behavior>
  <action>
Implement issue.labels.set requiring Write+ (D-ISS-07, D-ISS-20). Validate each label id is in the effective set for the repo. make rpc-gen. Green remaining assignment stubs in issue_labels.
  </action>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && cargo nextest run -p octanest-api -E 'test(issue_labels)'</automated>
  </verify>
  <done>Issue label assignment RPC green</done>
</task>

<task type="auto">
  <name>Task 3 (11-06-T3): Org/repo Labels settings + issue picker UI</name>
  <files>apps/web/src/routes/$owner.settings.labels.tsrx, apps/web/src/routes/$owner.$repo.issues.labels.tsrx, apps/web/src/components/repo/issue-labels-panel.tsrx, apps/web/src/routes/$owner.$repo.issues.$number.tsrx, apps/web/src/routes/$owner.$repo.issues.integration.test.ts</files>
  <read_first>apps/web/src/routes/$owner.settings.members.tsrx, apps/web/src/routes/$owner.$repo.settings.tsrx</read_first>
  <action>
Add org settings Labels page for org catalog and repo Issues→Labels (or settings) Admin UI for local/hide (D-ISS-05, D-ISS-07). Issue detail labels panel for Write+ assignment. Gate Admin UIs on can_admin. Turn green related integration stub cases. Octane .tsrx only.
  </action>
  <verify>
    <automated>bun --cwd apps/web run build && bun --cwd apps/web exec vitest run src/routes/\$owner.\$repo.issues.integration.test.ts</automated>
  </verify>
  <done>Label settings + issue assignment UI shipped</done>
</task>

</tasks>

<verification>
ISS-03 label half complete: defs Admin, assign Write+, effective merge rules.
</verification>

<success_criteria>
Org+repo labels work; issues can be labeled by Write+.
</success_criteria>

<output>
Create `.planning/phases/11-issues/11-06-SUMMARY.md` when done
</output>
