---
phase: 11-issues
plan: "12"
type: execute
wave: 10
depends_on:
  - "08"
  - "10"
  - "11"
files_modified:
  - crates/octanest-db/src/lib.rs
  - crates/octanest-db/tests/factory_reset_issues.rs
  - crates/octanest-api/tests/factory_reset_scope.rs
  - docs/ARCHITECTURE.md
  - docs/API.md
  - docs/CONFIGURATION.md
  - .planning/phases/11-issues/11-VALIDATION.md
requirements:
  - ISS-01
  - ISS-02
  - ISS-03
  - ISS-04
autonomous: true
user_setup: []
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "factory_reset_instance wipes issue domain tables via CASCADE or explicit order (ISS-01..04)"
    - "Docs describe issue.* / label.* , #N numbering, ACL gates, autolink, stub links; VALIDATION maps Wave 0 closed"
    - "Phase gate: issue_ filters + dialect_issues + rpc-sync + web build green"
  artifacts:
    - path: docs/API.md
      provides: "issue.* / label.* docs"
    - path: .planning/phases/11-issues/11-VALIDATION.md
      provides: "nyquist_compliant refresh"
  key_links:
    - from: crates/octanest-db/src/lib.rs
      to: factory_reset_instance
      via: "DELETE repositories cascades issue tables"
  assumptions:
    - "ASSUME: No new env vars for issues"
  prohibitions:
    - statement: "Must not leave VALIDATION Wave 0 gaps unchecked when suites are green"
      flagged: true
---

<objective>
Close Phase 11 with factory-reset coverage, documentation, rpc-sync confirmation, VALIDATION refresh, and full automated gate (ISS-01..04).

Purpose: Ship-ready issues with durable ops/docs.
Output: Green phase gate; nyquist map marks Wave 0 gaps closed.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-issues/11-RESEARCH.md
@.planning/phases/11-issues/11-VALIDATION.md
@crates/octanest-db/src/lib.rs
@docs/ARCHITECTURE.md
@docs/API.md
@crates/octanest-db/tests/factory_reset_orgs.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| factory_reset → all tenant data | Must not leave orphan issue rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05 | Tampering / residual data | factory_reset | high | mitigate | Prove CASCADE/wipe for issues/comments/labels/reactions/links; green factory_reset_issues |
| T-11-SC | Tampering | packages | high | mitigate | No new packages in closeout |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (11-12-T1): Factory reset issue tables + tests</name>
  <files>crates/octanest-db/src/lib.rs, crates/octanest-db/tests/factory_reset_issues.rs, crates/octanest-api/tests/factory_reset_scope.rs</files>
  <read_first>crates/octanest-db/src/lib.rs, crates/octanest-db/tests/factory_reset_orgs.rs, .planning/phases/11-issues/11-RESEARCH.md</read_first>
  <behavior>
    - After factory_reset, issues/comments/labels/assignees/reactions/links/counters counts are zero
    - Repository delete cascade path leaves no orphans
  </behavior>
  <action>
Ensure factory_reset_instance (via DELETE FROM repositories cascades and/or explicit deletes) wipes Phase 11 issue domain tables. Green factory_reset_issues and extend factory_reset_scope if needed.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-db -E 'test(factory_reset)' && cargo nextest run -p octanest-api -E 'test(factory_reset)'</automated>
  </verify>
  <done>Factory reset wipes issue domain rows</done>
</task>

<task type="auto">
  <name>Task 2 (11-12-T2): Docs + VALIDATION phase gate</name>
  <files>docs/ARCHITECTURE.md, docs/API.md, docs/CONFIGURATION.md, .planning/phases/11-issues/11-VALIDATION.md</files>
  <read_first>docs/API.md, docs/ARCHITECTURE.md, .planning/phases/11-issues/11-VALIDATION.md</read_first>
  <action>
Document issue.* / label.* RPCs, per-repo #N, ACL Capability gates, Write|Preview + autolink, Linked PR stubs, and deferred closing keywords (D-ISS-15). Refresh 11-VALIDATION.md: mark Wave 0 gaps closed, set nyquist_compliant true / wave_0_complete true, File Exists? ✅. Run phase gate commands and fix any residual drift.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(issue_)' && cargo test -p octanest-db --lib migration_parity && cargo nextest run -p octanest-db -E 'test(dialect_issues)' && make rpc-sync-check && bun --cwd apps/web run build</automated>
  </verify>
  <done>Docs updated; VALIDATION nyquist-compliant; phase gate green</done>
</task>

</tasks>

<verification>
ISS-01..04 phase gate green; factory reset + docs complete.
</verification>

<success_criteria>
Phase 11 issues ship-ready with docs, reset coverage, and VALIDATION closed.
</success_criteria>

<output>
Create `.planning/phases/11-issues/11-12-SUMMARY.md` when done
</output>
