# Phase 22.1: v1.0 Milestone Closure - Research

**Researched:** 2026-09-19
**Domain:** Branch-protection packaging (Docker/Compose/SSH/Smart HTTP), fork hook install, GSD verification/Nyquist/WINDOWS/REQUIREMENTS hygiene
**Confidence:** HIGH (product seams verified in-repo); MEDIUM (Compose smoke script shape / boot-sweep concurrency — discretionary)

<user_constraints>
## User Constraints (from CONTEXT.md)

### Locked Decisions

#### A — ORG-06 packaging & fail-open
- **D-PKG-01:** Full audit close — build and copy `octanest-protection-hook` into the API Docker image; set `OCTANEST_PROTECTION_HELPER` in Compose; Smart HTTP falls back to `default_helper_path()` when env unset; SSH `receive-pack` passes helper + DB + repos (+ actor capability) env into hooks so update hooks can run — **Reversibility:** costly — image + Compose + HTTPS/SSH receive paths
- **D-PKG-02:** When the helper is missing or not executable: **fail-closed in production** (`OCTANEST_ENV=production` or equivalent); **fail-open (or soft warn) in development** so local git without the helper still works — **Reversibility:** reversible
- **D-PKG-03:** Prove in shipped image via **Compose smoke** — after bring-up, a protected-branch direct push is denied over HTTPS; include SSH when the smoke stack exposes SSH TCP — **Reversibility:** reversible
- **D-PKG-04:** On API boot, **sweep `OCTANEST_REPOS_DIR`** and install missing update hooks on existing bare repos (upgrade safety net beyond lazy reconcile) — **Reversibility:** costly — boot path / large repos_dir walks

#### B — Fork / `clone_bare` hooks
- **D-FORK-01:** Fix the audit WARNING in this phase — forks must get protection hooks — **Reversibility:** reversible
- **D-FORK-02:** Install hooks **inside `clone_bare`** (same pattern as `init_bare`) so every bare copy gets hooks; boot sweep remains a safety net for older forks — **Reversibility:** reversible
- **D-FORK-03:** Prove with unit/integration assert that `hooks/update` exists after `clone_bare` (reuse `hooks_installed` from Phase 13) — **Reversibility:** reversible
- **D-FORK-04:** If hook install fails after a successful `clone_bare`, **fail the fork** and compensate/rollback like other post-insert storage errors — **Reversibility:** reversible

#### C — VERIFICATION & Nyquist
- **D-VER-01:** **Hybrid backfill** — full `/gsd-verify-work` (or equivalent thorough VERIFICATION) for phases **13** and **22**; evidence-based VERIFICATION from SUMMARYs + integration wiring for **01, 02, 03, 11.1, 12, 16, 17, 18, 21** — **Reversibility:** reversible
- **D-VER-02:** Include **`/gsd-validate-phase` for 8, 14, 15, and 20** in this phase (Nyquist / coverage honesty) — **Reversibility:** reversible
- **D-VER-03:** Lightweight VERIFICATION may be **`passed`** when evidence supports the phase goal (residual caveats OK in the body). If real gaps are found → **`gaps_found` / `partial`** and **block milestone until fixed**. Do **not** use `human_needed` — **Reversibility:** reversible
- **D-VER-04:** **Package ORG-06 first**, then thoroughly verify **13** and **22**. Other lightweight VERIFICATIONs may run in parallel anytime — **Reversibility:** reversible

#### D — WINDOWS & REQUIREMENTS hygiene
- **D-HYG-01:** **Mass-waive** clearly obsolete Wave 0 WINDOWS stubs whose owning plan already shipped; reason must cite superseded plan; spot-check a sample — **Reversibility:** reversible
- **D-HYG-02:** Flip Pending **PLAT-01, PLAT-04–08, PLAT-10–11** only after matching phase VERIFICATION exists and is **`passed`**. Leave already-Complete PLAT-02/03/09 as-is — **Reversibility:** reversible
- **D-HYG-03:** For non-stub WINDOWS (deviations, skipped-tests, unrun-verify): waive historical noise; **keep only entries that still describe current debt** — **Reversibility:** reversible
- **D-HYG-04:** Leave **ORG-06 `[x]`** in REQUIREMENTS; fix packaging this phase; honesty via Phase 13 VERIFICATION + re-audit — **Reversibility:** reversible
- **D-HYG-05:** Refresh **STATE.md** and **`.planning/parallel-tracks/README.md`** stale pointers as part of hygiene — **Reversibility:** reversible

### Claude's Discretion
- Exact Compose smoke script shape (extend `scripts/compose-smoke.sh` vs sibling script) as long as D-PKG-03 holds
- Exact production env signal for D-PKG-02 (`OCTANEST_ENV=production` vs additional flag)
- Boot-sweep concurrency / progress logging for large `OCTANEST_REPOS_DIR`
- Whether `clone_bare` hook-install failure maps to existing `repo.fork_failed` or a more specific code
- Batching of waive commands / commit granularity for WINDOWS sweep
- How to sequence validate-phase 8/14/15/20 relative to lightweight VERIFICATION writers

### Deferred Ideas (OUT OF SCOPE)
None — discussion stayed within phase scope (Nyquist validate-phase was explicitly folded into 22.1 by user choice).
</user_constraints>

<phase_requirements>
## Phase Requirements

| ID | Description | Research Support |
|----|-------------|------------------|
| ORG-06 (push half) | Protected branch rules enforced on direct pushes (HTTPS + SSH) in shipped image | Package helper into API image; Compose `OCTANEST_PROTECTION_HELPER`; Smart HTTP `default_helper_path()` fallback; SSH env pass-through + actor capability; fail-closed in production; Compose smoke denial; boot sweep; `clone_bare` hooks |
| PLAT-01 | Operator can run full stack with Docker Compose locally | Flip checkbox only after Phase 01 VERIFICATION `passed` (evidence: Compose/smoke already wired) |
| PLAT-04 | Web UI OctaneJS / TanStack Start | Flip after Phase 01 VERIFICATION `passed` |
| PLAT-05 | Backend forge/API in Rust | Flip after Phase 01 VERIFICATION `passed` |
| PLAT-06 | Typed RPC + generated TS client | Flip after Phase 01 VERIFICATION `passed` |
| PLAT-07 | SQLite / Postgres / MySQL config | Flip after Phase 02 VERIFICATION `passed` |
| PLAT-08 | Migrations/flows on three dialects | Flip after Phase 02 VERIFICATION `passed` |
| PLAT-10 | ShadCN + Base UI | Flip after Phase 01 VERIFICATION `passed` |
| PLAT-11 | Tailwind CSS v4 CSS config | Flip after Phase 01 VERIFICATION `passed` |
| Process: unverified 01–03, 11.1, 12–13, 16–18, 21–22 | Write VERIFICATION.md (hybrid D-VER-01) | Mirror Phase 04 frontmatter; thorough for 13/22 after packaging |
| Process: Nyquist 8, 14, 15, 20 | `/gsd-validate-phase` | Flip VALIDATION frontmatter `nyquist_compliant` / status per validate-phase |
</phase_requirements>

## Summary

Phase 22.1 is a **packaging + honesty** closure, not a new product surface. ORG-06 merge enforcement already works via `evaluate_merge`; the audit blocker is that **direct push** in the shipped Compose/API path is fail-open because (1) `octanest-protection-hook` is never built/copied into the API image, (2) Compose never sets `OCTANEST_PROTECTION_HELPER`, (3) Smart HTTP only reads the env and does not call `default_helper_path()`, (4) SSH `receive-pack` does not inject helper/DB/repos/capability env into the git child, and (5) the installed `hooks/update` script exits 0 when the helper is missing. Forks via `clone_bare` never call `install_protection_hooks` (unlike `init_bare`), so even a packaged helper leaves forks unprotected until reconcile.

In parallel, eleven executed phases lack `*-VERIFICATION.md`, PLAT-* checkboxes lag evidence, WINDOWS `open_count` is 55 (mostly obsolete Wave 0 stubs), and Nyquist honesty for phases 8/14/15/20 needs `/gsd-validate-phase`. Sequence locked by D-VER-04: **ship ORG-06 packaging first**, then thorough verify 13 + 22; lightweight VERIFICATIONs and WINDOWS/REQUIREMENTS hygiene can interleave.

**Primary recommendation:** Extend the existing API Dockerfile to build/copy `octanest-protection-hook` beside `octanest-api`, wire Compose + Smart HTTP fallback + SSH env (including capability), change the update-hook script for production fail-closed, install hooks in `clone_bare`, add boot sweep + Compose protected-push smoke, then run the hybrid VERIFICATION / validate-phase / WINDOWS waive / PLAT flip program.

## Architectural Responsibility Map

| Capability | Primary Tier | Secondary Tier | Rationale |
|------------|-------------|----------------|-----------|
| Ship protection helper binary | CDN / Static (image build) | API / Backend | Dockerfile owns packaging; runtime only consumes path |
| Compose helper env | API / Backend (ops config) | — | `docker-compose.yml` injects `OCTANEST_PROTECTION_HELPER` into API service |
| Smart HTTP CGI protection env | API / Backend | — | `git_smart_http` + `http_backend` forward env into `git-http-backend` |
| SSH receive-pack protection env | API / Backend | — | `ssh/pack` + `ssh/server` must set env on `git receive-pack` child |
| Update hook script semantics | Database / Storage (bare repos) | API / Backend | Script lives under `repos_dir/**/hooks/update`; installer in `octanest-git` |
| Boot hook sweep | API / Backend | Database / Storage | Walk `OCTANEST_REPOS_DIR` at process start |
| Fork hook install | API / Backend | Database / Storage | `clone_bare` in `octanest-git`; fork RPC compensates on failure |
| Compose protected-push smoke | API / Backend (CI/ops) | Browser / Client (git client) | Host runs git against Traefik HTTPS / SSH TCP |
| VERIFICATION / Nyquist / WINDOWS / REQUIREMENTS | Planning process | — | GSD artifacts under `.planning/`; no runtime product tier |

## Standard Stack

### Core

| Library / Tool | Version | Purpose | Why Standard |
|----------------|---------|---------|--------------|
| `octanest-protection-hook` bin | in-tree `[[bin]]` | Evaluate update hook against DB rules | Already implemented; missing from image only `[VERIFIED: crates/octanest-api/Cargo.toml:19-21]` — verbatim: `name = "octanest-protection-hook"` / `path = "src/bin/protection_hook.rs"` |
| `octanest-api` Dockerfile | current | Single image for Compose + Railway | Phase 22 D-CLOUD-01 — same Dockerfile `[VERIFIED: crates/octanest-api/Dockerfile:9-16]` — builds only `octanest-api` today |
| Docker Compose | repo `docker-compose.yml` | Local/CI ship path | Sets `OCTANEST_ENV` default `compose`, SSH TCP `2222`, repos volume — no `OCTANEST_PROTECTION_HELPER` yet `[VERIFIED: docker-compose.yml:48]` — `OCTANEST_ENV: ${OCTANEST_ENV:-compose}` |
| cargo-nextest | workspace CI | Unit/integration for push/fork | Existing `branch_protect_push`, `repo_fork` tests |
| Vitest / Bun | apps/web | Not primary for this phase | No UI work in locked decisions |
| GSD `gsd-tools` windows / verification | project tooling | Waive WINDOWS; VERIFICATION frontmatter | `windows waive\|fixed\|status` |

### Supporting

| Library / Tool | Version | Purpose | When to Use |
|----------------|---------|---------|-------------|
| `scripts/compose-smoke.sh` | in-repo | Bring-up health smoke | Extend or sibling for D-PKG-03 |
| `scripts/smoke-git-https.sh` / `smoke-git-ssh.sh` | in-repo | Protocol smoke patterns | Reuse auth/PAT/SSH patterns for protected-push denial |
| `make smoke` / `make smoke-protocol-ci` | Makefile | Operator/CI entrypoints | Wire new assert into existing Make surface |
| Phase 04 `04-VERIFICATION.md` | frontmatter pattern | Template for backfill | Mirror YAML + must-haves structure |

### Alternatives Considered

| Instead of | Could Use | Tradeoff |
|------------|-----------|----------|
| Same API image + sibling helper | Sidecar container for hook | Rejected — Phase 22 same-image; hook must be on receive path filesystem |
| Lazy reconcile only | Boot sweep (locked D-PKG-04) | Lazy misses upgrade of existing forks until touched |
| Uncheck ORG-06 | Keep `[x]` + fix packaging (locked D-HYG-04) | Honesty via VERIFICATION + re-audit |

**Installation:** No new npm/crates packages. Build second existing binary into the image:

```dockerfile
# Builder (extend existing Dockerfile)
RUN cargo build --release -p octanest-api --bin octanest-api --bin octanest-protection-hook
# Runtime
COPY --from=builder /app/target/release/octanest-api /usr/local/bin/octanest-api
COPY --from=builder /app/target/release/octanest-protection-hook /usr/local/bin/octanest-protection-hook
```

**Version verification:** No external package installs this phase. Existing Cargo bins verified via `Cargo.toml` `[[bin]]` entries (see Standard Stack table). Docker multi-bin COPY is the established multi-stage pattern `[CITED: github.com/block/buzz Dockerfile multi-bin COPY; rustprojectprimer.com/releasing/containers]`.

## Package Legitimacy Audit

> **No new external packages** are required for this phase. Packaging uses an existing in-tree binary and Docker/Compose/Make already in the repo.

| Package | Registry | Age | Downloads | Source Repo | Verdict | Disposition |
|---------|----------|-----|-----------|-------------|---------|-------------|
| _(none)_ | — | — | — | — | N/A | No install |

**Packages removed due to [SLOP] verdict:** none
**Packages flagged as suspicious [SUS]:** none

*Legitimacy gate skipped (no installs). Do not add crates/npm deps for this phase.*

## Architecture Patterns

### System Architecture Diagram

```text
                    ┌─────────────────┐
  git push HTTPS    │  Traefik :80    │
 ──────────────────►│  /.git → API    │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐     env_clear CGI
                    │ Smart HTTP      │──────────────────────┐
                    │ git_smart_http  │  ProtectionCgiEnv    │
                    └────────┬────────┘  helper|DB|repos|cap │
                             │                               ▼
                             │                    ┌──────────────────┐
  git push SSH :2222         │                    │ git-http-backend │
 ──────────────────┐         │                    │ → receive-pack   │
                   ▼         │                    └────────┬─────────┘
            ┌──────────────┐ │                             │
            │ SSH server   │ │                             │
            │ authorize +  │ │                             ▼
            │ run_pack_cmd │─┼──► git receive-pack ──► hooks/update
            └──────────────┘ │         ▲                    │
                             │         │ inherit/set env    │ OCTANEST_PROTECTION_HELPER
                    ┌────────┴─────────┴────────┐           ▼
                    │ API process (Compose)     │   octanest-protection-hook
                    │ OCTANEST_PROTECTION_HELPER│───► check_ref_update(DB)
                    │ OCTANEST_REPOS_DIR        │
                    │ OCTANEST_DATABASE_URL     │
                    └───────────┬───────────────┘
                                │ boot sweep
                                ▼
                    ┌───────────────────────────┐
                    │ OCTANEST_REPOS_DIR/**/*.git│
                    │ install_protection_hooks  │
                    └───────────────────────────┘

  repo.fork → clone_bare → install_protection_hooks (NEW)
  repo.create → init_bare → install_protection_hooks (EXISTING)
```

### Recommended Project Structure

```text
crates/octanest-api/
├── Dockerfile                    # build + COPY both bins
├── src/bin/protection_hook.rs    # unchanged entrypoint
├── src/protection/mod.rs         # default_helper_path; boot sweep helper
├── src/routes/git_smart_http.rs  # env OR default_helper_path()
├── src/git/http_backend.rs       # ProtectionCgiEnv (pattern)
├── src/ssh/pack.rs               # env on receive-pack + capability
├── src/ssh/server.rs             # pass capability into pack spawn
├── src/main.rs / app.rs          # boot sweep call
└── tests/
    ├── branch_protect_push.rs    # existing evaluator tests
    └── repo_fork.rs              # extend hooks_installed assert

crates/octanest-git/src/cli.rs    # update hook script + clone_bare install

docker-compose.yml                # OCTANEST_PROTECTION_HELPER=/usr/local/bin/...
scripts/compose-smoke.sh          # or sibling: protected-push denial
.planning/phases/*/…-VERIFICATION.md
.planning/WINDOWS.md              # waive/fixed
.planning/REQUIREMENTS.md         # PLAT flips only
```

### Pattern 1: Sibling helper path + Compose explicit env
**What:** Build helper next to API; set Compose env to absolute path; Smart HTTP falls back to `default_helper_path()` when env unset.
**When to use:** Always for ship path (D-PKG-01).
**Example:**

```rust
// Source: crates/octanest-api/src/protection/mod.rs:410-419 (existing)
pub fn default_helper_path() -> Option<PathBuf> {
    std::env::current_exe().ok().and_then(|p| {
        let sibling = p.parent()?.join("octanest-protection-hook");
        if sibling.is_file() {
            Some(sibling)
        } else {
            None
        }
    })
}

// Planned Smart HTTP resolution (planner/executor):
let helper = std::env::var("OCTANEST_PROTECTION_HELPER")
    .ok()
    .filter(|s| !s.is_empty())
    .or_else(|| default_helper_path().map(|p| p.display().to_string()));
```

### Pattern 2: Install hooks on every bare creation path
**What:** `init_bare` already installs; `clone_bare` must call the same installer; boot sweep catches pre-existing bares.
**When to use:** All bare repo births + API boot (D-FORK-02, D-PKG-04).
**Example:**

```rust
// Source: crates/octanest-git/src/cli.rs:396-397 (init_bare today)
install_protection_hooks(path).await?;

// Planned end of clone_bare after successful `git clone --bare`:
install_protection_hooks(&dest_abs).await?;
```

### Pattern 3: Production fail-closed update hook
**What:** Change the embedded `hooks/update` script so missing/non-executable helper denies updates when `OCTANEST_ENV` is production-like; keep fail-open for `development`/`dev`/`compose` (discretion: treat `compose` as fail-open so local without helper still works; Compose smoke proves helper is present).
**When to use:** `install_protection_hooks` overwrite (boot + create/clone).
**Recommended production-like set:** `production` and `cloud` (matches webhook URL hardening in `webhook/deliver.rs`). `[ASSUMED]` for whether `compose` should also fail-closed in CI — recommend **no** (rely on smoke asserting binary + denial).

### Anti-Patterns to Avoid
- **Only setting Compose env without building the binary:** env points at missing file → still fail-open in compose.
- **Relying on process env for Smart HTTP:** `http_backend` uses `env_clear()` — must re-inject `[VERIFIED: crates/octanest-api/src/git/http_backend.rs:64-90]`.
- **SSH inherit-only without capability:** Allow enum has no capability field today — helper defaults to `read`, breaking Admin bypass (D-16) over SSH `[VERIFIED: crates/octanest-api/src/ssh/pack.rs:28-35]`.
- **Unchecking ORG-06 mid-fix:** Forbidden by D-HYG-04.
- **`human_needed` VERIFICATION status:** Forbidden by D-VER-03.
- **Flipping PLAT checkboxes before VERIFICATION `passed`:** Forbidden by D-HYG-02.
- **Hand-editing `@octanest/api-client`:** N/A unless RPC changes (none expected).
- **Inventing a second Dockerfile for cloud:** Violates Phase 22 D-CLOUD-01.

## Don't Hand-Roll

| Problem | Don't Build | Use Instead | Why |
|---------|-------------|-------------|-----|
| Protection evaluation | New merge/push gate | Existing `check_ref_update` / `evaluate_merge` | Same evaluator already unit-tested |
| Hook install | Custom shell per repo | `install_protection_hooks` / `hooks_installed` | Idempotent Phase 13 seam |
| Fork storage rollback | Ad-hoc delete | `compensate_failed_create` + `repo.fork_failed` | Existing fork error path `[VERIFIED: crates/octanest-api/src/repo/mod.rs:1663-1669]` |
| WINDOWS ledger edits | Hand-edit JSON only | `gsd-tools windows waive <id> "<reason>"` / `fixed` | Keeps frontmatter counts consistent |
| Milestone honesty | Skip verify | `/gsd-verify-work` / evidence VERIFICATION + `/gsd-validate-phase` | Audit scores depend on these artifacts |
| Multi-bin Docker caching | cargo-chef rewrite | Minimal Dockerfile delta (second `--bin` + COPY) | Phase is packaging gap, not build-perf |

**Key insight:** The product code for ORG-06 push already exists; the ship path never wires it. Planning should maximize reuse of Phase 13 tests and Phase 22 smoke, not reinvent protection.

## Common Pitfalls

### Pitfall 1: Smart HTTP `env_clear` drops helper
**What goes wrong:** Compose sets helper on API process, but CGI child never sees it.
**Why it happens:** `run_git_http_backend` clears env then selectively re-adds `[VERIFIED: crates/octanest-api/src/git/http_backend.rs:64-90]`.
**How to avoid:** Always pass `helper_path` in `ProtectionCgiEnv` (env var or `default_helper_path()`).
**Warning signs:** HTTPS push succeeds on protected branch while unit tests pass.

### Pitfall 2: SSH Admin treated as Read
**What goes wrong:** `OCTANEST_ACTOR_CAPABILITY` unset → helper uses `read` → Admin cannot bypass when `enforce_admins=false`.
**Why it happens:** `AuthzDecision::Allow` omits capability; `run_pack_command` sets no env `[VERIFIED: crates/octanest-api/src/ssh/pack.rs:28-35,196-204]`.
**How to avoid:** Extend Allow with capability (or parallel arg); set `OCTANEST_ACTOR_CAPABILITY`, `OCTANEST_PROTECTION_HELPER`, `OCTANEST_DATABASE_URL`, `OCTANEST_REPOS_DIR` on receive-pack only.
**Warning signs:** Admin SSH push to protected branch denied unexpectedly; Write user push allowed when helper missing.

### Pitfall 3: Fail-open script survives packaging
**What goes wrong:** Image has helper but old repos keep old hook script that exits 0 when helper unset.
**Why it happens:** `reconcile_protection_hooks` skips if `hooks/update` exists `[VERIFIED: crates/octanest-git/src/cli.rs:52-58]`.
**How to avoid:** Boot sweep should **overwrite** via `install_protection_hooks` (not reconcile-only) so script upgrades apply; new installs already overwrite.
**Warning signs:** Pre-phase repos still fail-open after upgrade.

### Pitfall 4: `clone_bare` without hooks
**What goes wrong:** Forked repos accept direct pushes on protected patterns until touched by reconcile.
**Why it happens:** `clone_bare` ends after `git clone --bare` with no hook install `[VERIFIED: crates/octanest-git/src/cli.rs:1045-1064]`.
**How to avoid:** Install hooks inside `clone_bare`; on failure, compensate fork like clone failure (D-FORK-04). Prefer reuse `repo.fork_failed` unless a more specific code is needed (discretion).
**Warning signs:** Audit WARNING SOC-04 / ORG-06 fork path remains.

### Pitfall 5: Compose smoke without Docker / without protected rule setup
**What goes wrong:** Smoke only checks `/health` and never proves denial.
**Why it happens:** Current `compose-smoke.sh` is dialect/Traefik health oriented.
**How to avoid:** After bring-up: assert helper executable in container; create repo + protection rule requiring reviews; attempt HTTPS push; expect non-zero. Include SSH when TCP published (Compose already maps `2222:2222`).
**Warning signs:** Re-audit still flags packaging blocker.

### Pitfall 6: Flipping PLAT before VERIFICATION
**What goes wrong:** REQUIREMENTS claim Complete without evidence artifact.
**Why it happens:** Integration is wired but checkbox lag was intentional process debt.
**How to avoid:** Strict D-HYG-02 order: VERIFICATION `passed` → then flip PLAT-01/04–08/10–11. Leave PLAT-02/03/09 Complete.

### Pitfall 7: Mass-waive without spot-check
**What goes wrong:** Real remaining debt waived.
**How to avoid:** D-HYG-01/03 — waive only Wave 0 stubs whose owning plan shipped; spot-check sample files; keep current deviations (e.g. coverage floor 11.1 if still true).

## Code Examples

### Current fail-open update hook (must change per D-PKG-02)

```sh
# Source: crates/octanest-git/src/cli.rs:30-40
#!/bin/sh
refname="$1"
oldrev="$2"
newrev="$3"
helper="${OCTANEST_PROTECTION_HELPER:-}"
if [ -z "$helper" ] || [ ! -x "$helper" ]; then
  exit 0
fi
exec "$helper" update "$refname" "$oldrev" "$newrev"
```

### Recommended production-aware script shape

```sh
#!/bin/sh
refname="$1"
oldrev="$2"
newrev="$3"
helper="${OCTANEST_PROTECTION_HELPER:-}"
env_name="${OCTANEST_ENV:-development}"
if [ -z "$helper" ] || [ ! -x "$helper" ]; then
  case "$env_name" in
    production|cloud)
      echo "octanest: protection helper missing; refusing update" >&2
      exit 1
      ;;
    *)
      exit 0
      ;;
  esac
fi
exec "$helper" update "$refname" "$oldrev" "$newrev"
```

### Dockerfile delta (prescriptive)

```dockerfile
# Source baseline: crates/octanest-api/Dockerfile:9,16
RUN cargo build --release -p octanest-api --bin octanest-api --bin octanest-protection-hook
...
COPY --from=builder /app/target/release/octanest-api /usr/local/bin/octanest-api
COPY --from=builder /app/target/release/octanest-protection-hook /usr/local/bin/octanest-protection-hook
```

### Compose env (prescriptive)

```yaml
# Under api.environment — beside OCTANEST_REPOS_DIR
OCTANEST_PROTECTION_HELPER: /usr/local/bin/octanest-protection-hook
```

### Fork hooks assert (extend existing test helpers)

```rust
// Pattern from branch_protect_push.rs using hooks_installed
assert!(
    hooks_installed(&dest_bare).await,
    "clone_bare / fork must install hooks/update"
);
```

### WINDOWS waive

```bash
gsd_run windows waive <id> "Obsolete Wave 0 stub; superseded by plan <NN-XX> (shipped)"
gsd_run windows status   # confirm open_count drop
```

## State of the Art

| Old Approach | Current Approach | When Changed | Impact |
|--------------|------------------|--------------|--------|
| Soft-protect default branch only | Classic branch protection + hooks (Phase 13) | Phase 13 | ORG-06 evaluator exists |
| Helper local-only / tests | Ship helper in API image (this phase) | 22.1 | Closes audit blocker |
| Lazy reconcile on touch | Boot sweep + clone_bare install | 22.1 | Upgrade + fork safety |
| VERIFICATION optional for early phases | Hybrid backfill required for milestone | Audit 2026-09-19 | Honest complete-milestone |

**Deprecated/outdated:**
- Fail-open-when-helper-missing in production-like envs (replace per D-PKG-02)
- Assuming REQUIREMENTS `[x]` implies ship-path enforcement (ORG-06 merge ≠ push)

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A1 | Treat `production` and `cloud` as fail-closed; keep `compose`/`development`/`dev` fail-open | Pattern 3 / D-PKG-02 | Compose may still fail-open if helper packaging regresses unless smoke asserts helper presence |
| A2 | Reuse error code `repo.fork_failed` when hook install fails after clone | D-FORK-04 discretion | Callers may want a more specific code later |
| A3 | Extend `compose-smoke.sh` (or sibling invoked from Make) rather than inventing a new CI job | D-PKG-03 | CI may need a longer timeout for protection setup |
| A4 | Boot sweep should overwrite hooks (`install_protection_hooks`) not only reconcile-missing | Pitfall 3 | Slightly more I/O on large repos_dir |
| A5 | No Railway IaC change required beyond image rebuild (helper path can rely on `default_helper_path` if env unset in cloud) | Architecture | Cloud may still want explicit env for clarity |

## Open Questions

1. **Should Compose `OCTANEST_ENV=compose` ever fail-closed on missing helper?**
   - What we know: D-PKG-02 names production; Compose defaults to `compose`.
   - What's unclear: CI desire to catch packaging regression without full push smoke.
   - Recommendation: Keep compose fail-open; **require** smoke to `test -x` helper + denial (A1).

2. **Boot-sweep concurrency for large repos_dir?**
   - What we know: User wants boot sweep (D-PKG-04).
   - What's unclear: Serial vs bounded parallel walk.
   - Recommendation: Serial + `tracing::info` progress every N repos first; add concurrency only if measured slow (discretion).

3. **SSH smoke in D-PKG-03 when `SMOKE_SKIP_LS_REMOTE=1`?**
   - What we know: Phase 09 caveats skip client push in some CI.
   - What's unclear: Whether protected-push SSH is mandatory in CI or local-only.
   - Recommendation: HTTPS denial mandatory in compose smoke; SSH denial when TCP reachable / not skip-flagged.

## Environment Availability

| Dependency | Required By | Available | Version | Fallback |
|------------|------------|-----------|---------|----------|
| Docker engine | Image build + Compose smoke (D-PKG-03) | ✗ (research host) | — | Executor host with Docker; CI already runs compose-smoke |
| cargo / rustc | Rebuild API + hook; nextest | ✓ | cargo 1.100.0-nightly | — |
| bun | Web tests (unlikely needed) | ✓ | 1.4.0 | Skip if no web changes |
| make | smoke / test targets | ✓ | 4.4.1 | — |
| gsd-tools | windows waive, commit docs | ✓ | `.cursor/gsd-core` | — |
| git | Bare repos / smoke client | ✓ | system | — |

**Missing dependencies with no fallback:**
- Docker on this research machine — planners must schedule Compose smoke on a Docker-capable executor/CI, not assume local Docker.

**Missing dependencies with fallback:**
- None critical beyond Docker.

Step 2.6 note: Phase has external Docker dependency for success criterion 1; unit tests can green without Docker but milestone gate cannot.

## Validation Architecture

### Test Framework

| Property | Value |
|----------|-------|
| Framework | Rust: cargo-nextest (`make test`); Web: Vitest via Bun (minimal this phase); Compose: bash smoke |
| Config file | `.config/nextest.toml`; `apps/web` Vitest; `scripts/compose-smoke.sh` |
| Quick run command | `cargo nextest run -p octanest-api -E 'test(branch_protect)|test(repo_fork)'` |
| Full suite command | `make test` + Docker-capable `make smoke` (protected-push assert) |

### Phase Requirements → Test Map

| Req ID | Behavior | Test Type | Automated Command | File Exists? |
|--------|----------|-----------|-------------------|-------------|
| ORG-06 push | Direct push denied when reviews required | unit/integration | `cargo nextest run -p octanest-api -E 'test(branch_protect_push)'` | ✅ `branch_protect_push.rs` |
| ORG-06 packaging | Helper binary present in image | smoke | `docker compose exec -T api test -x /usr/local/bin/octanest-protection-hook` | ❌ Wave 0 — add to smoke |
| ORG-06 HTTPS ship | Protected branch HTTPS push denied in Compose | smoke | Extend compose / sibling smoke | ❌ Wave 0 |
| ORG-06 SSH ship | Protected branch SSH push denied when TCP up | smoke | Extend smoke-git-ssh or sibling | ❌ Wave 0 |
| ORG-06 Smart HTTP fallback | `default_helper_path` used when env unset | unit | New test near http backend / smart http | ❌ Wave 0 |
| ORG-06 fail-closed | Missing helper + production env → deny | unit | Hook script / installer test | ❌ Wave 0 |
| ORG-06 boot sweep | Missing hooks installed on boot | unit/integration | Temp repos_dir + boot helper | ❌ Wave 0 |
| SOC-04 / fork hooks | `hooks/update` after `clone_bare` / fork | integration | Extend `repo_fork.rs` + git unit | ❌ Wave 0 (assert missing) |
| D-FORK-04 | Hook failure fails fork + compensates | integration | Force install error path | ❌ Wave 0 |
| PLAT-* hygiene | VERIFICATION passed then checkbox flip | process | Manual/gsd verify + REQUIREMENTS edit | ❌ until VERIFICATION written |
| Nyquist 8/14/15/20 | validate-phase compliance | process | `/gsd-validate-phase` | ⚠️ VALIDATION.md exists; status draft/partial |
| WINDOWS | open_count reduced | process | `gsd-tools windows status` | ✅ ledger exists |

### Sampling Rate
- **Per task commit:** `cargo nextest run -p octanest-api -E 'test(branch_protect)|test(repo_fork)'` (and targeted new tests)
- **Per wave merge:** `make test`
- **Phase gate:** Docker Compose protected-push smoke green + VERIFICATION for 13/22 `passed` + PLAT flips only after matching VERIFICATION + WINDOWS open_count reduced + re-audit no ORG-06 packaging blocker

### Wave 0 Gaps
- [ ] Compose/image assert: helper executable in API container
- [ ] Compose smoke: protected-branch HTTPS push denial (setup rule + push)
- [ ] Optional SSH protected-push denial when TCP exposed
- [ ] Unit: Smart HTTP resolves `default_helper_path()` when env unset
- [ ] Unit: production fail-closed hook script behavior
- [ ] Unit/integration: boot sweep installs missing hooks
- [ ] `clone_bare` / `repo_fork` assert `hooks_installed`
- [ ] Hook-install failure after clone compensates fork
- [ ] Lightweight `*-VERIFICATION.md` for phases 01, 02, 03, 11.1, 12, 16, 17, 18, 21
- [ ] Thorough VERIFICATION for 13 and 22 (after packaging)
- [ ] `/gsd-validate-phase` for 8, 14, 15, 20

## Security Domain

### Applicable ASVS Categories

| ASVS Category | Applies | Standard Control |
|---------------|---------|-----------------|
| V2 Authentication | yes (push paths) | Existing session/PAT/SSH key auth before receive-pack |
| V3 Session Management | no (no session changes) | — |
| V4 Access Control | yes | Capability gates + protection evaluator; Admin bypass only when `enforce_admins=false` and capability=admin |
| V5 Input Validation | yes | Existing ref/path validation; hook args from git |
| V6 Cryptography | no | — |

### Known Threat Patterns for forge push protection

| Pattern | STRIDE | Standard Mitigation |
|---------|--------|---------------------|
| Fail-open missing helper in production | Elevation / Tampering | D-PKG-02 fail-closed for production/cloud |
| Bypass via SSH env omission | Elevation | Pass helper + capability into receive-pack |
| Fork without hooks | Tampering | `clone_bare` install + boot sweep |
| Path escape via GIT_DIR | Tampering | Existing `owner_name_from_git_dir` under `OCTANEST_REPOS_DIR` |
| Helper binary substitution | Tampering | Image-owned path `/usr/local/bin/octanest-protection-hook`; no user-writable helper path in Compose |

`security_enforcement: true` in `.planning/config.json` — packaging must not weaken ACL; fail-closed is the hardening.

## Project Constraints (from .cursor/rules/)

| Rule | Directive for this phase |
|------|--------------------------|
| `octanest-core.mdc` | One product path; same API Dockerfile; no parallel app structure; `tmp/` for scratch; prefer extending Make/smoke patterns |
| `rust-crates.mdc` | Dialect SQL only in `octanest-db`; Result/errors no unwrap outside tests; tests in `crates/*/tests/`; nextest CI |
| `rpc-codegen.mdc` | No hand-edit of `packages/api-client`; `make rpc-gen` only if RPC/DTO changes (not expected) |
| `no-ai-attribution.mdc` | No AI Co-authored-by / Generated-with / Made-with on commits or PRs |
| `octane-ui.mdc` | Load Octane skill before UI — **N/A** (no UI in locked scope) |
| `clean-user-facing-text.mdc` | Apply when writing user-facing docs prose in hygiene refreshes |

Also honor `AGENTS.md`: `make test` / relevant smoke before claiming done; no secrets in commits.

## Sources

### Primary (HIGH confidence)
- In-repo reads: `crates/octanest-api/Dockerfile`, `Cargo.toml` bins, `protection/mod.rs`, `git/http_backend.rs`, `routes/git_smart_http.rs`, `ssh/pack.rs`, `ssh/server.rs`, `octanest-git/src/cli.rs`, `repo/mod.rs` fork path, `docker-compose.yml`, `scripts/compose-smoke.sh`, `.planning/v1.0-MILESTONE-AUDIT.md`, `22.1-CONTEXT.md`, `WINDOWS.md`, `REQUIREMENTS.md`
- Official git hooks docs — update hook args + exit semantics `[CITED: https://git-scm.com/docs/githooks]` `[CITED: https://git-scm.com/docs/git-receive-pack]`

### Secondary (MEDIUM confidence)
- Docker multi-bin COPY community/pattern docs `[CITED: rustprojectprimer.com/releasing/containers]` / example multi-bin Dockerfiles
- Phase 04 VERIFICATION frontmatter as template
- Phase 22 CONTEXT cloud `OCTANEST_ENV=production` via `.railway/railway.ts`

### Tertiary (LOW confidence)
- Exact Compose smoke orchestration duration / CI flake profile `[ASSUMED]`
- Whether cloud deploy docs need an explicit `OCTANEST_PROTECTION_HELPER` line vs sibling default `[ASSUMED]` (A5)

## Metadata

**Confidence breakdown:**
- Standard stack: HIGH — in-tree binary + Docker/Compose; no new packages
- Architecture: HIGH — seams verified by reading source; SSH capability gap explicit
- Pitfalls: HIGH — mapped to concrete fail-open paths from audit + code
- Hygiene process: MEDIUM — VERIFICATION authorship quality depends on executor rigor

**Research date:** 2026-09-19
**Valid until:** 2026-10-19 (30 days; stack stable)
