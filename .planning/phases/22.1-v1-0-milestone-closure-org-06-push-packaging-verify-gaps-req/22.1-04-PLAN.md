---
phase: 22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req
plan: "04"
type: execute
wave: 3
depends_on:
  - "22.1-01"
  - "22.1-02"
files_modified:
  - crates/octanest-api/src/protection/mod.rs
  - crates/octanest-api/src/main.rs
  - crates/octanest-api/tests/branch_protect_push.rs
requirements:
  - ORG-06
autonomous: true
user_setup: []
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "On API boot, OCTANEST_REPOS_DIR is walked and missing/outdated update hooks are overwritten via install_protection_hooks (D-PKG-04)"
    - "Boot sweep uses install (overwrite) not reconcile-only so D-PKG-02 script upgrades apply to existing bares"
    - "Single-repo sweep errors are logged and counted without aborting listen indefinitely"
    - statement: "ORG-06 unclassified — boot sweep upgrade path reviewed for pre-existing forks"
      verification: backstop
  artifacts:
    - path: crates/octanest-api/src/protection/mod.rs
      provides: "sweep_protection_hooks(repos_dir) overwrite walker"
    - path: crates/octanest-api/src/main.rs
      provides: "boot-time sweep call after AppState::new"
  key_links:
    - from: crates/octanest-api/src/main.rs
      to: crates/octanest-api/src/protection/mod.rs
      via: "boot sweep → install_hooks overwrite"
    - from: crates/octanest-api/src/protection/mod.rs
      to: crates/octanest-git/src/cli.rs
      via: "install_protection_hooks"
  assumptions:
    - "ASSUME: Serial walk + tracing progress every N repos first (RESEARCH open Q2 / A4)"
  prohibitions:
    - statement: "Must not implement boot safety net with reconcile_protection_hooks only"
      flagged: true
    - statement: "Must not abort API listen forever without progress logs on large repos_dir"
      flagged: true
---

<objective>
Add API boot sweep over OCTANEST_REPOS_DIR that installs/overwrites protection hooks on existing bare repos (D-PKG-04).

Purpose: Upgrade safety net for pre-phase forks and old fail-open hook scripts after packaging.
Output: sweep_protection_hooks + main.rs boot call + nextest coverage.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req/22.1-CONTEXT.md
@.planning/phases/22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req/22.1-RESEARCH.md
@.planning/phases/22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req/22.1-PATTERNS.md
@crates/octanest-api/src/protection/mod.rs
@crates/octanest-api/src/main.rs
@crates/octanest-api/src/jobs/reconcile.rs
@crates/octanest-git/src/cli.rs
</context>

## Artifacts this phase produces

- Boot-time repos_dir hook overwrite sweep (D-PKG-04)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Boot → repos_dir filesystem | Must only touch bare repos under OCTANEST_REPOS_DIR; overwrite known hook script |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22.1-03 | Tampering | Pre-existing forks without hooks | high | mitigate | Boot sweep install_protection_hooks overwrite (D-PKG-04) |
| T-22.1-01 | Elevation of privilege | Stale fail-open scripts | high | mitigate | Overwrite scripts so D-PKG-02 semantics land on upgrade |
| T-22.1-SC | Tampering | packages | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sweep_protection_hooks overwrite walker</name>
  <reversibility rating="costly">D-PKG-04: boot path walks all bares and rewrites hooks; undo needs coordinated main/protection changes and may re-touch large repos_dir.</reversibility>
  <files>crates/octanest-api/src/protection/mod.rs, crates/octanest-api/tests/branch_protect_push.rs</files>
  <read_first>crates/octanest-api/src/protection/mod.rs, crates/octanest-api/src/jobs/reconcile.rs, crates/octanest-git/src/cli.rs, crates/octanest-api/tests/branch_protect_push.rs</read_first>
  <behavior>
    - Bare under temp repos_dir without hooks becomes hooks_installed after sweep
    - Bare with outdated hook script is overwritten (install, not reconcile-skip)
    - Non-bare entries are skipped without panic
  </behavior>
  <action>
Per D-PKG-04: Implement sweep_protection_hooks(repos_dir) modeled on jobs/reconcile.rs owner/repo walk. Detect bare repos (HEAD/objects) and call install_hooks / install_protection_hooks (overwrite), never reconcile-only. Serial walk with tracing::info progress every N repos. Count/log per-repo errors; do not treat one failure as fatal for the whole sweep. Add nextest covering missing-hooks install and overwrite path.
  </action>
  <verify>
    <automated>rg -n "sweep_protection_hooks|install_hooks" crates/octanest-api/src/protection/mod.rs | head -20; cargo nextest run -p octanest-api -E 'test(sweep) | test(boot_hook) | test(branch_protect)'</automated>
    <fails_when>sweep function missing or uses only reconcile; nextest non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - sweep_protection_hooks exists and calls install overwrite
    - Automated test proves hooks appear after sweep on a bare without hooks
  </acceptance_criteria>
  <done>Boot sweep helper installs/overwrites hooks with tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Invoke sweep on API boot</name>
  <reversibility rating="costly">D-PKG-04: wires sweep into process startup for every API instance.</reversibility>
  <files>crates/octanest-api/src/main.rs</files>
  <read_first>crates/octanest-api/src/main.rs, crates/octanest-api/src/protection/mod.rs</read_first>
  <action>
Per D-PKG-04: After AppState::new (repos_dir normalized) and before or alongside spawn_background_jobs, await/call sweep_protection_hooks on state.repos_dir. Log aggregate errors; do not prevent listen solely because one repo failed. Keep SSH spawn and migrate ordering intact.
  </action>
  <verify>
    <automated>rg -n "sweep_protection_hooks" crates/octanest-api/src/main.rs | head -10</automated>
    <fails_when>main.rs has no sweep_protection_hooks call</fails_when>
  </verify>
  <acceptance_criteria>
    - main.rs calls sweep after AppState construction
    - Boot continues if individual repo install fails (logged)
  </acceptance_criteria>
  <done>API boot runs the hook overwrite sweep.</done>
</task>

</tasks>

<verification>
sweep_protection_hooks overwrite + main boot call present; nextest covers sweep install.
</verification>

<success_criteria>
D-PKG-04 upgrade safety net is live on API start.
</success_criteria>

<output>
Create `.planning/phases/22.1-v1-0-milestone-closure-org-06-push-packaging-verify-gaps-req/22.1-04-SUMMARY.md` when done
</output>
