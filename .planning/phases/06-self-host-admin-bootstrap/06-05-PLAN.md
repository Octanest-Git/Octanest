---
phase: 06-self-host-admin-bootstrap
plan: "05"
type: execute
wave: 5
depends_on:
  - "03"
  - "04"
files_modified:
  - apps/web/src/lib/ssr-auth.ts
  - apps/web/src/lib/api-client.ts
  - apps/web/src/lib/bootstrap.ts
  - apps/web/src/routes/index.tsx
  - apps/web/src/routes/dashboard.tsx
  - apps/web/src/routes/setup.tsx
  - apps/web/src/routes/login.tsx
  - apps/web/src/routes/signup.tsx
  - packages/api-client/src/index.ts
requirements:
  - AUTH-06
  - AUTH-07
autonomous: true
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "`/` SSR priority: needs_setup → redirect /setup; else session → SignedInHome; else marketing — first HTML matches final tree (D-18, D-20)"
    - "Direct /dashboard → notFound 404 — not a soft redirect (D-19)"
    - "While needs_setup, app UI routes gate to /setup except /status (and health) (D-09, D-10)"
    - "SSR uses Cookie-forward createServerFn to Octanest RPC — not TanStack Start useSession (D-11 phase-4 session ownership)"
    - "Presence hint octanest_signed_in remains progressive enhancement only (D-21 discretion: keep unless it fights SSR)"
    - "UI Consideration: SSR/home gate unclassified row covered — no marketing flash for signed-in"
  artifacts:
    - path: apps/web/src/lib/ssr-auth.ts
      provides: "fetchBootstrapStatus / fetchSessionMe / fetchProviderConfig cookie-forward"
    - path: apps/web/src/routes/index.tsx
      provides: "SSR home tree selection"
    - path: apps/web/src/routes/dashboard.tsx
      provides: "notFound()"
  key_links:
    - from: apps/web/src/lib/ssr-auth.ts
      to: packages/api-client/src/index.ts
      via: "createClient fetch override with Cookie header"
    - from: apps/web/src/routes/index.tsx
      to: apps/web/src/lib/ssr-auth.ts
      via: "beforeLoad/loader priority chain"
  assumptions:
    - "ASSUME (RESEARCH A1): CreateClientOptions.fetch override is enough for Cookie forward"
    - "ASSUME: tracked .tsx paths are canonical; executor reconciles in-flight .tsrx rename when landing edits"
  prohibitions:
    - statement: "Must not use client-only redirectIfNeedsSetup as the security/first-paint boundary"
      flagged: true
    - statement: "Must not soft-redirect /dashboard to / or login"
      flagged: true
---

<objective>
Replace client-only bootstrap/home gating with SSR Cookie-forward session + needs_setup priority, and hard-404 `/dashboard`.

Purpose: Eliminate signed-in `/` flicker (folded todo) and enforce empty-instance lock before paint (D-09).
Output: `ssr-auth` helpers; index/setup/login/signup gates; dashboard notFound.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-self-host-admin-bootstrap/06-04-SUMMARY.md
@.planning/phases/06-self-host-admin-bootstrap/06-CONTEXT.md
@.planning/phases/06-self-host-admin-bootstrap/06-UI-SPEC.md
@.planning/phases/06-self-host-admin-bootstrap/06-RESEARCH.md
@.planning/phases/06-self-host-admin-bootstrap/06-PATTERNS.md
@apps/web/src/routes/index.tsx
@apps/web/src/lib/api-client.ts
@packages/api-client/src/index.ts
@apps/web/src/lib/return-to.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser Cookie → SSR server fn → API | Session forwarded; must not log cookie values |
| SSR gate → route tree | UX redirect only; API allowlist remains authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-11 | Information disclosure | SSR Cookie forward | high | mitigate | Forward Cookie header only to configured API origin; never log cookies; credentials include |
| T-06-12 | Spoofing | Open redirect after gates | medium | mitigate | Reuse safeReturnTo for post-login paths (D-22) — no raw returnTo |
| T-06-SC | Tampering | cargo/npm installs | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (06-05-T1): ssr-auth Cookie-forward server fns</name>
  <files>apps/web/src/lib/ssr-auth.ts, apps/web/src/lib/api-client.ts, packages/api-client/src/index.ts</files>
  <read_first>apps/web/src/lib/api-client.ts, packages/api-client/src/index.ts, .planning/phases/06-self-host-admin-bootstrap/06-RESEARCH.md, .planning/phases/06-self-host-admin-bootstrap/06-PATTERNS.md</read_first>
  <action>
Per RESEARCH Pattern 2 (D-18/D-20): create intentionally new `apps/web/src/lib/ssr-auth.ts` using `createServerFn` from `@octanejs/tanstack-start` and `getRequestHeader("cookie")` from `@octanejs/tanstack-start/server`. Build createClient with fetch override that injects the incoming Cookie. Export helpers: fetchBootstrapStatus, fetchSessionMe (auth.me), fetchProviderConfig (allow_signup). Extend CreateClientOptions only if fetch override proves insufficient (A1). Do not adopt Start useSession.
  </action>
  <verify>
    <automated>bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or TypeScript errors mentioning ssr-auth / createServerFn unresolved</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/lib/ssr-auth.ts`
    - `rg -n 'createServerFn|getRequestHeader|bootstrapStatus|provider_config|providerConfig' apps/web/src/lib/ssr-auth.ts`
  </acceptance_criteria>
  <done>SSR auth helpers compile and forward Cookie to API</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (06-05-T2): `/` SSR priority + demote client bootstrap helper</name>
  <files>apps/web/src/routes/index.tsx, apps/web/src/lib/bootstrap.ts, apps/web/src/routes/setup.tsx, apps/web/src/routes/login.tsx</files>
  <read_first>apps/web/src/routes/index.tsx, apps/web/src/components/signed-in-home.tsx, apps/web/src/lib/return-to.ts, .planning/phases/06-self-host-admin-bootstrap/06-UI-SPEC.md</read_first>
  <behavior>
    - needs_setup → redirect /setup before paint
    - valid session → SignedInHome in first HTML
    - anonymous → marketing without SignedInHome flash
    - must_change_credentials session → redirect /setup/credentials (gate may stub route module until 06-06)
  </behavior>
  <action>
Implement index beforeLoad/loader priority per UI-SPEC / D-18–D-20. Prefer tracked `index.tsx` (reconcile `.tsrx` if rename in flight). Create minimal `setup.tsx` route shell if missing so redirect target exists (full wizard UI in 06-06). Demote/delete client `redirectIfNeedsSetup` as security boundary — PE only. Keep presence hint unless it causes hydration races (D-21). Honor safeReturnTo on login success (D-22).
  </action>
  <verify>
    <automated>bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'needs_setup|SignedInHome|fetchBootstrapStatus|fetchSessionMe' apps/web/src/routes/index.tsx apps/web/src/routes/index.tsrx 2>/dev/null | head`
    - Marketing Get started links gated or deferred to 06-06 when allow_signup false
  </acceptance_criteria>
  <done>`/` first HTML matches session/setup/marketing decision</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (06-05-T3): `/dashboard` notFound + setup/signup route gates</name>
  <files>apps/web/src/routes/dashboard.tsx, apps/web/src/routes/signup.tsx, apps/web/src/routes/setup.tsx</files>
  <read_first>apps/web/src/routes/dashboard.tsx, apps/web/src/routes/signup.tsx, .planning/phases/06-self-host-admin-bootstrap/06-UI-SPEC.md</read_first>
  <behavior>
    - /dashboard → notFound()
    - /signup beforeLoad: needs_setup → /setup; allow_signup false → notFound; else existing signup
    - /setup beforeLoad: !needs_setup → redirect /
  </behavior>
  <action>
Per D-06/D-19: replace dashboard soft-redirect with `notFound()` from `@octanejs/tanstack-router`. Gate signup/setup via ssr-auth. Green Wave 0 dashboard.integration stub as far as unit-testable without full Start server (mock beforeLoad result or route options). Status route remains public (D-10).
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/dashboard.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'notFound' apps/web/src/routes/dashboard.tsx apps/web/src/routes/dashboard.tsrx 2>/dev/null`
    - dashboard integration test green
  </acceptance_criteria>
  <done>Dashboard 404 and signup/setup SSR gates land</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `apps/web/src/lib/ssr-auth.ts` | 06-05 | Cookie-forward |
| SSR `/` + setup/signup gates | 06-05 | D-18–D-20 |
| `/dashboard` notFound | 06-05 | D-19 |

<verification>
web build green; dashboard integration green; index uses ssr-auth priority.
</verification>

<success_criteria>
First paint for `/` matches final UI; empty-instance redirects before paint; dashboard is not public.
</success_criteria>

<output>
Create `.planning/phases/06-self-host-admin-bootstrap/06-05-SUMMARY.md` when done
</output>
