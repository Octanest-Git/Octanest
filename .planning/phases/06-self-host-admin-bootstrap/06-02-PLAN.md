---
phase: 06-self-host-admin-bootstrap
plan: "02"
type: execute
wave: 2
depends_on:
  - "00"
  - "01"
files_modified:
  - crates/octanest-api/src/auth/seed.rs
  - crates/octanest-api/src/auth/bootstrap.rs
  - crates/octanest-api/src/auth/local.rs
  - crates/octanest-api/src/auth/mod.rs
  - crates/octanest-api/src/rpc.rs
  - crates/octanest-api/src/main.rs
  - crates/octanest-api/tests/auth_signup.rs
  - crates/octanest-api/tests/auth_forced_credentials.rs
requirements:
  - AUTH-06
autonomous: false
user_setup: []
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "When both OCTANEST_ADMIN_EMAIL and OCTANEST_ADMIN_PASSWORD are set and users is empty, first boot seeds sys-admin username system-administrator, auto-verified, must_change_credentials true (AUTH-06, D-15, D-16)"
    - "Partial ENV (either or both unset/empty) does not seed — adjacency separates to wizard path (D-13)"
    - "OCTANEST_ALLOW_SIGNUP true/1 at seed writes allow_signup; unset defaults false (D-07, D-08)"
    - "When both ENV set and seed returns Err, process does not serve the app — fail-closed boot (D-14)"
    - "auth.confirm_admin_credentials clears must_change after rejecting default username; ENV email/password may be kept (D-16, D-17)"
    - "Seed second run with existing users is no-op — ordering/idempotency edge (AUTH-06 ordering + AUTH-07 idempotency adjacency)"
    - "user_to_public maps must_change_credentials for auth.me / login response"
  artifacts:
    - path: crates/octanest-api/src/auth/seed.rs
      provides: "system-administrator ENV seed + allow_signup + must_change"
    - path: crates/octanest-api/src/auth/bootstrap.rs
      provides: "confirm_admin_credentials RPC handler (intentionally new / stabilize untracked WIP)"
    - path: crates/octanest-api/tests/auth_forced_credentials.rs
      provides: "E2E confirm coverage"
  key_links:
    - from: crates/octanest-api/src/main.rs
      to: crates/octanest-api/src/auth/seed.rs
      via: "maybe_seed_admin fail → exit(1)"
    - from: crates/octanest-api/src/rpc.rs
      to: crates/octanest-api/src/auth/bootstrap.rs
      via: "auth.confirm_admin_credentials"
    - from: crates/octanest-api/src/auth/local.rs
      to: crates/octanest-db/src/users.rs
      via: "user_to_public must_change_credentials"
  assumptions:
    - "RESOLVED (RESEARCH Q1): login may mint session while must_change_credentials; SSR/UI gate forces /setup/credentials (06-05/06-06)"
    - "ASSUME (AUTH-06 empty): null/unset ENV vars treated as absent — same as D-13"
  prohibitions:
    - statement: "Must not seed an admin when only one of OCTANEST_ADMIN_EMAIL / OCTANEST_ADMIN_PASSWORD is set"
      flagged: true
    - statement: "Must not leave ENV-seeded default username without must_change_credentials=true"
      flagged: true
---

<objective>
Prove AUTH-06 end-to-end: ENV seed creates `system-administrator` with forced-credential flag, fail-closed boot stays locked, and confirm-credentials clears the flag — one production-quality tracer through DB → seed → RPC → tests.

Purpose: Catch architectural gaps before wizard/SSR expansion.
Output: Green seeded_admin + forced_credentials tests; confirm RPC live.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-self-host-admin-bootstrap/06-CONTEXT.md
@.planning/phases/06-self-host-admin-bootstrap/06-RESEARCH.md
@.planning/phases/06-self-host-admin-bootstrap/06-PATTERNS.md
@.planning/phases/06-self-host-admin-bootstrap/06-00-SUMMARY.md
@.planning/phases/06-self-host-admin-bootstrap/06-01-SUMMARY.md
@crates/octanest-api/src/auth/seed.rs
@crates/octanest-api/src/main.rs
@crates/octanest-api/tests/auth_signup.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator ENV → boot seed | Secrets create first sys-admin |
| Authenticated admin → confirm credentials | Must not skip default-username change |
| Seed failure → process lifecycle | Fail-closed prevents empty/open serve |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Elevation of privilege | ENV default credentials | high | mitigate | must_change_credentials=true on seed; confirm rejects username equal to system-administrator case-insensitive (D-16) |
| T-06-04 | Elevation of privilege | Partial ENV | high | mitigate | Both vars required; else no seed (D-13) |
| T-06-05 | Denial of service / Tampering | Seed failure with both ENV set | high | mitigate | main exits 1; do not bind listeners (D-14) |
| T-06-SC | Tampering | cargo/npm installs | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1 (06-02-T1): Confirm fail-closed boot (D-14 one-way)</name>
  <decision>
Keep fail-closed API boot when both `OCTANEST_ADMIN_EMAIL` and `OCTANEST_ADMIN_PASSWORD` are set and `maybe_seed_admin` returns Err: print error and `std::process::exit(1)` — do not serve HTTP.
  </decision>
  <context>
CONTEXT D-14 rates this one-way: operators rely on fail-closed boot. Undo would mean serving an empty/unseeded instance after a broken ENV seed (security + ops surprise). `main.rs` already exits on seed Err; this phase strengthens seed behavior (username/flags) while preserving that door.
  </context>
  <options>
    <option id="fail_closed">
      <title>Keep fail-closed (recommended)</title>
      <pros>Matches D-14; prevents serving without the intended admin; operators see boot error immediately</pros>
      <cons>Misconfigured ENV takes the process down until fixed</cons>
    </option>
    <option id="serve_wizard_fallback">
      <title>On seed failure, serve wizard instead</title>
      <pros>Instance still comes up</pros>
      <cons>Violates D-14; masks operator ENV errors; ambiguous empty-instance state</cons>
    </option>
  </options>
  <resume-signal>Reply with fail_closed (recommended) or serve_wizard_fallback</resume-signal>
</task>

<task type="tracer">
  <name>Task 2 (06-02-T2): E2E ENV seed → must_change → confirm credentials</name>
  <reversibility rating="one-way">Fail-closed boot when seed fails with both ADMIN ENV set (D-14); operators depend on exit(1).</reversibility>
  <files>crates/octanest-api/src/auth/seed.rs, crates/octanest-api/src/auth/bootstrap.rs, crates/octanest-api/src/auth/local.rs, crates/octanest-api/src/auth/mod.rs, crates/octanest-api/src/rpc.rs, crates/octanest-api/src/main.rs, crates/octanest-api/tests/auth_signup.rs, crates/octanest-api/tests/auth_forced_credentials.rs</files>
  <read_first>crates/octanest-api/src/auth/seed.rs, crates/octanest-api/src/main.rs, crates/octanest-api/src/auth/local.rs, crates/octanest-api/src/rpc.rs, crates/octanest-db/src/users.rs, .planning/phases/06-self-host-admin-bootstrap/06-RESEARCH.md, .planning/phases/06-self-host-admin-bootstrap/06-PATTERNS.md</read_first>
  <precondition>06-01 delivered 0006 migrations, allow_signup/must_change DB helpers, and DTO fields; human selected fail_closed for D-14</precondition>
  <action>
Wire ONE AUTH-06 happy path through every layer (D-01–D-04 empty-instance path — no deployment-mode forks; D-13–D-17):

1. Update `maybe_seed_admin`: both ENV required else Ok no-op; fixed username `system-administrator`; set must_change_credentials true; parse OCTANEST_ALLOW_SIGNUP like AUTO_MIGRATE (`true`/`1`, default false) and persist on instance_auth_settings; keep auto-verify via set_email_verified_at; preserve Err propagation for main fail-closed (D-14).
2. Stabilize/create tracked `bootstrap.rs` (WIP may exist untracked — land as intentional new/tracked module per PATTERNS; wire `mod bootstrap` if missing on clean HEAD). Implement `auth.confirm_admin_credentials`: session required; only when must_change_credentials; reject username case-insensitive equal to system-administrator; allow keeping ENV email; Keep-password path skips password rewrite; otherwise hash new password; update email/username/password helpers; clear must_change_credentials; return UserPublic.
3. Map `user_to_public` must_change_credentials; register RPC arm; keep main seed fail → exit(1).
4. Turn Wave 0 seeded_admin + forced_credentials tests green for this single path. Do not implement wizard allow_signup Switch UI or SSR gates here.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(seeded_admin) | test(forced_credentials) | test(confirm_admin)'</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"; or tests still assert username admin/admin1</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'system-administrator' crates/octanest-api/src/auth/seed.rs`
    - `rg -n 'must_change_credentials' crates/octanest-api/src/auth/seed.rs`
    - `rg -n 'OCTANEST_ALLOW_SIGNUP|allow_signup' crates/octanest-api/src/auth/seed.rs`
    - `rg -n 'confirm_admin_credentials' crates/octanest-api/src/rpc.rs crates/octanest-api/src/auth/bootstrap.rs`
    - `rg -n 'process::exit\(1\)' crates/octanest-api/src/main.rs`
    - nextest filter exits 0
  </acceptance_criteria>
  <done>ENV seed + confirm-credentials path works end-to-end under tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (06-02-T3): Partial ENV + seed idempotency expansion</name>
  <files>crates/octanest-api/src/auth/seed.rs, crates/octanest-api/tests/auth_signup.rs, crates/octanest-api/tests/auth_bootstrap.rs</files>
  <read_first>crates/octanest-api/src/auth/seed.rs, crates/octanest-api/tests/auth_signup.rs, .planning/phases/06-self-host-admin-bootstrap/06-CONTEXT.md</read_first>
  <behavior>
    - Email-only ENV → no user created
    - Password-only ENV → no user created
    - Second maybe_seed_admin with users present → count stays 1 (idempotent)
  </behavior>
  <action>
Expand tracer with D-13 partial-ENV tests (may live in auth_bootstrap or auth_signup). Ensure no cloud/self-host branches (D-04). Keep fail-closed semantics untouched.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(bootstrap) | test(seeded_admin) | test(signup)'</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"</fails_when>
  </verify>
  <acceptance_criteria>
    - Tests cover email-only and password-only ENV non-seed
    - Second seed remains no-op when users exist
  </acceptance_criteria>
  <done>Partial ENV and seed idempotency covered</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `maybe_seed_admin` system-administrator + flags | 06-02 | AUTH-06 |
| `auth.confirm_admin_credentials` | 06-02 | D-16/D-17 |
| Fail-closed boot retained | 06-02 | D-14 |

<verification>
nextest seeded_admin + forced_credentials + bootstrap partial ENV green; main still exits on seed Err.
</verification>

<success_criteria>
AUTH-06 success criterion 1 satisfied at API layer; forced credential confirm RPC ready for UI.
</success_criteria>

<output>
Create `.planning/phases/06-self-host-admin-bootstrap/06-02-SUMMARY.md` when done
</output>
