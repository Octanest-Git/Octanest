---
phase: 06-self-host-admin-bootstrap
plan: "04"
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - crates/octanest-api/src/auth/local.rs
  - crates/octanest-api/src/auth/admin.rs
  - crates/octanest-api/tests/auth_signup.rs
  - crates/octanest-api/tests/admin_auth_settings.rs
  - crates/octanest-api/src/bin/rpc_gen.rs
requirements:
  - AUTH-06
  - AUTH-07
autonomous: true
user_setup: []
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "After bootstrap, auth.signup is rejected when allow_signup is false (D-05); UI 404 alone is insufficient"
    - "auth.provider_config returns allow_signup for public chrome (fail closed if unset)"
    - "admin.auth.get_settings / update_settings round-trip allow_signup for sys-admin (D-08)"
    - "AUTH-05 open-signup product rule is now governed by allow_signup — no invite codes (deferred)"
  artifacts:
    - path: crates/octanest-api/src/auth/local.rs
      provides: "signup allow_signup gate + provider_config.allow_signup"
    - path: crates/octanest-api/src/auth/admin.rs
      provides: "settings DTO map for allow_signup"
  key_links:
    - from: crates/octanest-api/src/auth/local.rs
      to: crates/octanest-db/src/auth_settings.rs
      via: "read allow_signup before signup"
    - from: crates/octanest-api/src/auth/admin.rs
      to: crates/octanest-db/src/auth_settings.rs
      via: "update_auth_settings allow_signup"
  assumptions:
    - "ASSUME (RESEARCH Open Q2): cloud Compose/Railway should set OCTANEST_ALLOW_SIGNUP=true at deploy — documented in 06-07, code default remains false"
  prohibitions:
    - statement: "Must not implement invite-code issuance when allow_signup is false (deferred)"
      flagged: true
    - statement: "Must not rely on UI-only hiding of Sign up as the security control"
      flagged: true
---

<objective>
Enforce post-bootstrap `allow_signup` at the RPC seam and expose it on public provider config + admin settings.

Purpose: Close AUTH-05 interaction / D-05–D-08 without invite codes.
Output: Signup blocked when closed; admin can toggle; provider_config carries the flag for UI.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-self-host-admin-bootstrap/06-03-SUMMARY.md
@.planning/phases/06-self-host-admin-bootstrap/06-CONTEXT.md
@.planning/phases/06-self-host-admin-bootstrap/06-RESEARCH.md
@crates/octanest-api/src/auth/local.rs
@crates/octanest-api/src/auth/admin.rs
@crates/octanest-api/tests/admin_auth_settings.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous client → auth.signup | Closed signup must reject |
| Sys-admin → admin.auth.update_settings | Only admins flip allow_signup |
| Anonymous → provider_config | Public non-secret policy read |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-09 | Elevation of privilege | auth.signup when closed | high | mitigate | Server-side allow_signup check with stable error code; default false |
| T-06-10 | Elevation of privilege | admin settings | medium | mitigate | Existing require_admin gate; thread allow_signup only through that path |
| T-06-SC | Tampering | cargo/npm installs | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (06-04-T1): provider_config.allow_signup + signup reject when closed</name>
  <reversibility rating="costly">Closed-signup RPC contract revises AUTH-05 always-open behavior (D-05/D-06).</reversibility>
  <files>crates/octanest-api/src/auth/local.rs, crates/octanest-api/tests/auth_signup.rs</files>
  <read_first>crates/octanest-api/src/auth/local.rs, crates/octanest-api/tests/auth_signup.rs, .planning/phases/06-self-host-admin-bootstrap/06-RESEARCH.md</read_first>
  <behavior>
    - provider_config includes allow_signup from settings (false if missing)
    - After users exist and allow_signup false → auth.signup fails
    - allow_signup true → signup succeeds (existing welcome/verify behavior retained)
    - needs_setup still blocks signup before bootstrap
  </behavior>
  <action>
Per D-05/D-06/D-07: extend `provider_config` with allow_signup; in `signup`, after setup gate, reject when allow_signup is false with a clear auth error code (product closed-signup — not invite). Green Wave 0 closed-signup tests. Do not add invite schema.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(signup) | test(bootstrap) | test(seeded_admin)'</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'allow_signup' crates/octanest-api/src/auth/local.rs`
    - signup tests assert closed reject + open success
  </acceptance_criteria>
  <done>Signup RPC honors allow_signup; public config exposes it</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (06-04-T2): admin.auth settings allow_signup round-trip</name>
  <files>crates/octanest-api/src/auth/admin.rs, crates/octanest-api/tests/admin_auth_settings.rs, crates/octanest-api/src/bin/rpc_gen.rs</files>
  <read_first>crates/octanest-api/src/auth/admin.rs, crates/octanest-api/tests/admin_auth_settings.rs, crates/octanest-core/src/auth_types.rs</read_first>
  <behavior>
    - get_settings returns allow_signup
    - update_settings persists allow_signup for sys-admin
    - non-admin still forbidden
  </behavior>
  <action>
Thread allow_signup through admin settings map / update / AuthSettingsPublic (D-08). Update rpc_gen TypeScript shapes for new DTO fields if hand-maintained stubs exist; full client regen may wait for 06-07 `make rpc-gen` — ensure compile-safe stubs here if needed for API tests only.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(admin_auth) | test(signup)'</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'allow_signup' crates/octanest-api/src/auth/admin.rs`
    - admin_auth_settings tests cover allow_signup update
  </acceptance_criteria>
  <done>Sys-admin can persist allow_signup via admin.auth</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `ProviderConfigPublic.allow_signup` wired | 06-04 | chrome/SSR input |
| `auth.signup` closed gate | 06-04 | D-05 |
| `admin.auth` allow_signup | 06-04 | D-08 |

<verification>
signup + admin_auth nextest green for allow_signup.
</verification>

<success_criteria>
Closed signup enforced server-side; public + admin surfaces expose the flag.
</success_criteria>

<output>
Create `.planning/phases/06-self-host-admin-bootstrap/06-04-SUMMARY.md` when done
</output>
