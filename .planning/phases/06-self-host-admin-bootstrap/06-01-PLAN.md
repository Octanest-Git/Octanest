---
phase: 06-self-host-admin-bootstrap
plan: "01"
type: execute
wave: 1
depends_on:
  - "00"
files_modified:
  - crates/octanest-db/migrations/sqlite/0006_bootstrap_flags.sql
  - crates/octanest-db/migrations/postgres/0006_bootstrap_flags.sql
  - crates/octanest-db/migrations/mysql/0006_bootstrap_flags.sql
  - crates/octanest-db/src/auth_settings.rs
  - crates/octanest-db/src/users.rs
  - crates/octanest-db/src/lib.rs
  - crates/octanest-db/tests/dialect_auth.rs
  - crates/octanest-core/src/auth_types.rs
requirements:
  - AUTH-06
  - AUTH-07
autonomous: true
user_setup: []
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All three dialects migrate instance_auth_settings.allow_signup default false and users.must_change_credentials default false"
    - "octanest-db exposes allow_signup on auth settings CRUD and must_change_credentials on users plus dialect-safe email update / clear-must-change helpers"
    - "UserPublic.must_change_credentials, ProviderConfigPublic.allow_signup, BootstrapSetupRequest.allow_signup, AuthSettingsPublic/UpdateAuthSettingsRequest.allow_signup exist; system-administrator is reserved"
    - "Empty users table with no new columns unset behaves as defaults (false/false) after migrate — empty/null edge (AUTH-06 empty)"
  artifacts:
    - path: crates/octanest-db/migrations/sqlite/0006_bootstrap_flags.sql
      provides: "allow_signup + must_change_credentials columns"
    - path: crates/octanest-db/src/auth_settings.rs
      provides: "AuthSettingsRow.allow_signup"
    - path: crates/octanest-db/src/users.rs
      provides: "must_change_credentials + update_user_email / clear flag helpers"
    - path: crates/octanest-core/src/auth_types.rs
      provides: "DTO fields + reserved username"
  key_links:
    - from: crates/octanest-db/src/lib.rs
      to: crates/octanest-db/src/users.rs
      via: "Database facades for new helpers"
    - from: crates/octanest-db/migrations/sqlite/0006_bootstrap_flags.sql
      to: crates/octanest-db/migrations/postgres/0006_bootstrap_flags.sql
      via: "migration_parity identical filename"
  assumptions:
    - "ASSUME (RESEARCH A2): allow_signup public surface is ProviderConfigPublic (not BootstrapStatus)"
    - "Migration id is 0006 (live dialects end at 0005_user_role; next free id)"
  prohibitions:
    - statement: "Must not invent a separate instance_flags table for a single allow_signup bool"
      flagged: true
    - statement: "Must not add OCTANEST_DEPLOYMENT_MODE or cloud/self-host schema forks"
      flagged: true
---

<objective>
Ship dialect-parity persistence and core DTOs for `allow_signup` and `must_change_credentials` so the AUTH-06 tracer can write/read them.

Purpose: Keep schema/CRUD within context budget before seed/wizard/RPC wiring.
Output: `0006_bootstrap_flags`, DB helpers, and auth_types fields ready for 06-02.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-self-host-admin-bootstrap/06-CONTEXT.md
@.planning/phases/06-self-host-admin-bootstrap/06-RESEARCH.md
@.planning/phases/06-self-host-admin-bootstrap/06-PATTERNS.md
@crates/octanest-db/migrations/sqlite/0004_email_token_issue_count.sql
@crates/octanest-db/migrations/sqlite/0002_auth.sql
@crates/octanest-db/src/auth_settings.rs
@crates/octanest-db/src/users.rs
@crates/octanest-core/src/auth_types.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → live schema | New flags become security policy inputs |
| DTO surface → clients | must_change_credentials / allow_signup become public contracts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation of privilege | `allow_signup` default | high | mitigate | Column DEFAULT false (D-07); fail closed when unset at read |
| T-06-02 | Elevation of privilege | `must_change_credentials` | high | mitigate | Default false; only ENV seed sets true (enforced in 06-02) |
| T-06-SC | Tampering | cargo/npm installs | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (06-01-T1): 0006_bootstrap_flags + DB row helpers</name>
  <reversibility rating="costly">Persisted instance setting + user flag become operator/data contracts (D-08); changing shape later needs migrations across dialects.</reversibility>
  <files>crates/octanest-db/migrations/sqlite/0006_bootstrap_flags.sql, crates/octanest-db/migrations/postgres/0006_bootstrap_flags.sql, crates/octanest-db/migrations/mysql/0006_bootstrap_flags.sql, crates/octanest-db/src/auth_settings.rs, crates/octanest-db/src/users.rs, crates/octanest-db/src/lib.rs</files>
  <read_first>crates/octanest-db/migrations/sqlite/0004_email_token_issue_count.sql, crates/octanest-db/migrations/sqlite/0002_auth.sql, crates/octanest-db/src/auth_settings.rs, crates/octanest-db/src/users.rs, crates/octanest-db/src/lib.rs, .planning/phases/06-self-host-admin-bootstrap/06-PATTERNS.md, .planning/phases/06-self-host-admin-bootstrap/06-RESEARCH.md</read_first>
  <action>
Per D-07/D-08 and RESEARCH Pattern 3 / PATTERNS migration analog:

1. Add dialect-parity `0006_bootstrap_flags.sql` (sqlite/postgres/mysql) with `-- logical:` header: ALTER `instance_auth_settings` ADD `allow_signup` boolean/integer NOT NULL DEFAULT false/0; ALTER `users` ADD `must_change_credentials` boolean/integer NOT NULL DEFAULT false/0. Mirror boolean typing used for other auth flags in `0002_auth.sql`.
2. Extend `AuthSettingsRow`, map_settings!, SELECT constants, and `update` signature with `allow_signup: bool`.
3. Extend `UserRow`, map_user!, USER_SELECT_* with `must_change_credentials`. Add dialect-safe `update_user_email` (or combined credentials update) beside `set_password_hash` / `update_user_profile`; add helper to set/clear `must_change_credentials`. Expose via `Database` facades — no raw SQL in API crates.
4. Do not wire seed/RPC/UI here.
  </action>
  <verify>
    <automated>cargo test -p octanest-core --lib && cargo test -p octanest-db --lib migration_parity</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"; or migration_parity reports missing 0006 file</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f crates/octanest-db/migrations/sqlite/0006_bootstrap_flags.sql`
    - `test -f crates/octanest-db/migrations/postgres/0006_bootstrap_flags.sql`
    - `test -f crates/octanest-db/migrations/mysql/0006_bootstrap_flags.sql`
    - `rg -n 'allow_signup' crates/octanest-db/src/auth_settings.rs`
    - `rg -n 'must_change_credentials' crates/octanest-db/src/users.rs`
    - `rg -n 'update_user_email|fn update_.*email' crates/octanest-db/src/users.rs`
  </acceptance_criteria>
  <done>Migrations and DB helpers for bootstrap flags exist</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (06-01-T2): Core DTOs + dialect_auth column coverage</name>
  <files>crates/octanest-core/src/auth_types.rs, crates/octanest-db/tests/dialect_auth.rs</files>
  <read_first>crates/octanest-core/src/auth_types.rs, crates/octanest-db/tests/dialect_auth.rs, .planning/phases/06-self-host-admin-bootstrap/06-RESEARCH.md</read_first>
  <behavior>
    - UserPublic includes must_change_credentials: bool
    - ProviderConfigPublic includes allow_signup: bool
    - BootstrapSetupRequest includes allow_signup: bool
    - AuthSettingsPublic + UpdateAuthSettingsRequest include allow_signup
    - is_reserved_username("system-administrator") is true
    - dialect_auth after migrate can read/write allow_signup and must_change_credentials
  </behavior>
  <action>
1. Extend auth_types DTOs per RESEARCH recommendations (D-08 wizard request field; public config for chrome). Keep BootstrapStatus lean (needs_setup only).
2. Add `"system-administrator"` to reserved username list (Pitfall 5 / A4).
3. Turn Wave 0 dialect_auth stubs green for new columns (round-trip set allow_signup / must_change_credentials).
  </action>
  <verify>
    <automated>cargo test -p octanest-db --lib migration_parity && cargo test -p octanest-db --test dialect_auth && cargo test -p octanest-core --lib</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"; or migration_parity missing 0006</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'must_change_credentials' crates/octanest-core/src/auth_types.rs`
    - `rg -n 'allow_signup' crates/octanest-core/src/auth_types.rs`
    - `rg -n 'system-administrator' crates/octanest-core/src/auth_types.rs`
    - `cargo test -p octanest-db --test dialect_auth` exits 0
  </acceptance_criteria>
  <done>DTOs and dialect coverage for bootstrap flags are green</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `0006_bootstrap_flags.sql` (3 dialects) | 06-01 | Next after live 0005_user_role |
| `AuthSettingsRow.allow_signup` | 06-01 | |
| `UserRow.must_change_credentials` + email update helpers | 06-01 | |
| DTO fields + reserved `system-administrator` | 06-01 | |

<verification>
migration_parity + dialect_auth + core lib green for new columns/DTOs.
</verification>

<success_criteria>
Tracer plan can persist and type-check allow_signup / must_change_credentials without further schema work.
</success_criteria>

<output>
Create `.planning/phases/06-self-host-admin-bootstrap/06-01-SUMMARY.md` when done
</output>
