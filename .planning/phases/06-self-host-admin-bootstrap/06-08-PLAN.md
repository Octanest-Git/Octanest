---
phase: 06-self-host-admin-bootstrap
plan: "08"
type: execute
wave: 7
depends_on:
  - "06"
files_modified:
  - apps/web/src/components/chrome.tsx
  - apps/web/src/routes/index.tsx
  - apps/web/src/routes/login.tsx
  - apps/web/src/routes/signup.tsx
  - apps/web/src/routes/admin/auth.tsx
  - apps/web/src/components/chrome.integration.test.ts
requirements:
  - AUTH-06
  - AUTH-07
autonomous: true
user_setup: []
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "allow_signup false → /signup notFound; logged-out chrome/landing/login omit all Sign up / Get started→/signup / Create an account references (D-06)"
    - "While needs_setup, header omits Sign in/Sign up account CTAs; mark+theme OK (UI-SPEC)"
    - "While allow_signup unknown, omit Sign up fail-closed (UI Considerations header loading)"
    - "admin/auth exposes Allow open signup Switch near provider mode (D-08)"
  artifacts:
    - path: apps/web/src/components/chrome.tsx
      provides: "omit Sign up when closed"
    - path: apps/web/src/routes/admin/auth.tsx
      provides: "Allow open signup Switch"
  key_links:
    - from: apps/web/src/components/chrome.tsx
      to: auth.provider_config.allow_signup
      via: "public config fetch"
    - from: apps/web/src/routes/admin/auth.tsx
      to: apps/web/src/components/ui/switch.tsx
      via: "reuse Switch from 06-06"
  assumptions:
    - "ASSUME: Switch primitive already landed in 06-06"
    - "ASSUME: reconcile .tsrx rename when editing routes/components"
  prohibitions:
    - statement: "Must not show an AuthShell soft page for closed /signup — 404 only"
      flagged: true
    - statement: "Must not show disabled warm Sign up control when closed — omit entirely"
      flagged: true
---

<objective>
Ship closed-signup chrome/landing/login omissions and admin `allow_signup` Switch (split from wizard plan for context budget).

Purpose: Complete D-06 UI contract and post-bootstrap admin control on top of 06-06 Switch.
Output: Green chrome.integration.test.ts; UI matches UI-SPEC omit rules.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-self-host-admin-bootstrap/06-06-SUMMARY.md
@.planning/phases/06-self-host-admin-bootstrap/06-UI-SPEC.md
@.planning/phases/06-self-host-admin-bootstrap/06-CONTEXT.md
@apps/web/src/components/chrome.tsx
@apps/web/src/routes/admin/auth.tsx
@apps/web/src/routes/login.tsx
@apps/web/src/components/ui/switch.tsx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chrome public config | Fail closed on unknown allow_signup |
| Admin settings form → update_settings | Sys-admin toggles signup policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-13 | Elevation of privilege | Closed signup UI bypass | medium | mitigate | UI omit + SSR 404; API gate from 06-04 remains authority |
| T-06-SC | Tampering | cargo/npm installs | high | mitigate | No new npm packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (06-08-T1): Closed signup chrome + landing + login omit</name>
  <reversibility rating="costly">Closed-signup chrome/routing contract (D-06).</reversibility>
  <files>apps/web/src/components/chrome.tsx, apps/web/src/routes/index.tsx, apps/web/src/routes/login.tsx, apps/web/src/routes/signup.tsx, apps/web/src/components/chrome.integration.test.ts</files>
  <read_first>apps/web/src/components/chrome.tsx, apps/web/src/routes/login.tsx, apps/web/src/routes/index.tsx, .planning/phases/06-self-host-admin-bootstrap/06-UI-SPEC.md</read_first>
  <behavior>
    - allow_signup false/unknown → omit header Sign up; omit landing Get started→/signup; omit login Create an account
    - needs_setup → omit Sign in/Sign up account CTAs
    - allow_signup true → restore Phase 4 Sign up / Get started / cross-link
  </behavior>
  <action>
Per D-06 and UI-SPEC closed signup + UI Considerations: fail-closed omit Sign up until public config resolves. Update chrome, marketing index CTAs, and login cross-links. Signup route remains notFound when closed (SSR gate from 06-05). Green chrome.integration.test.ts. No invite UI.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/components/chrome.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'allow_signup' apps/web/src/components/chrome.tsx apps/web/src/components/chrome.tsrx 2>/dev/null`
    - chrome integration test exits 0
  </acceptance_criteria>
  <done>Closed signup UX omit rules match UI-SPEC</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (06-08-T2): admin/auth Allow open signup Switch</name>
  <files>apps/web/src/routes/admin/auth.tsx</files>
  <read_first>apps/web/src/routes/admin/auth.tsx, apps/web/src/components/ui/switch.tsx, .planning/phases/06-self-host-admin-bootstrap/06-UI-SPEC.md</read_first>
  <behavior>
    - admin/auth Switch Allow open signup + helper persists via update_settings
    - Label/helper match wizard Switch copy (UI-SPEC)
  </behavior>
  <action>
Per D-08: extend admin auth form after provider mode with same Switch/label/helper as wizard. Wire to existing admin.auth update_settings allow_signup from 06-04. Prefer tracked `.tsx`; reconcile `.tsrx` if rename in flight.
  </action>
  <verify>
    <automated>bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Allow open signup|allow_signup' apps/web/src/routes/admin/auth.tsx apps/web/src/routes/admin/auth.tsrx 2>/dev/null`
  </acceptance_criteria>
  <done>Admin allow_signup Switch matches UI-SPEC</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| chrome/login/landing omissions | 06-08 | D-06 |
| admin auth Switch | 06-08 | D-08 |

<verification>
chrome vitest green; web build green.
</verification>

<success_criteria>
Closed-signup omit rules and admin Switch implemented; Wave 0 chrome stub green.
</success_criteria>

<output>
Create `.planning/phases/06-self-host-admin-bootstrap/06-08-SUMMARY.md` when done
</output>
