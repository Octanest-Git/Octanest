---
phase: 06-self-host-admin-bootstrap
plan: "06"
type: execute
wave: 6
depends_on:
  - "05"
files_modified:
  - apps/web/src/components/ui/switch.tsx
  - apps/web/src/routes/setup.tsx
  - apps/web/src/routes/setup.credentials.tsx
  - apps/web/src/routes/login.tsx
  - apps/web/src/routes/signup.tsx
  - apps/web/src/routes/index.tsx
  - apps/web/src/routes/admin/auth.tsx
  - apps/web/src/components/chrome.tsx
  - apps/web/src/routes/setup.integration.test.ts
  - apps/web/src/routes/setup.credentials.integration.test.ts
  - apps/web/src/components/chrome.integration.test.ts
requirements:
  - AUTH-06
  - AUTH-07
autonomous: true
user_setup: []
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "/setup AuthShell wizard: Set up this instance; fields Email→Username→Password→Confirm→Allow open signup Switch→Create system admin; success → / (D-08, D-12, UI-SPEC)"
    - "/setup/credentials for ENV must_change only: Confirm admin account; must change username away from system-administrator; Keep current password Switch default on (D-16, D-17)"
    - "allow_signup false → /signup notFound; logged-out chrome/landing/login omit all Sign up / Get started→/signup / Create an account references (D-06)"
    - "While needs_setup, header omits Sign in/Sign up account CTAs; mark+theme OK (UI-SPEC)"
    - "While allow_signup unknown, omit Sign up fail-closed (UI Considerations header loading)"
    - "admin/auth exposes Allow open signup Switch near provider mode (D-08)"
    - "Post confirm/login honors safeReturnTo else / (D-22)"
    - statement: "Long-text support/helpers wrap in max-w-md on setup surfaces"
      verification: backstop
  artifacts:
    - path: apps/web/src/components/ui/switch.tsx
      provides: "Base UI Switch wrapper"
    - path: apps/web/src/routes/setup.tsx
      provides: "wizard UI"
    - path: apps/web/src/routes/setup.credentials.tsx
      provides: "forced credential change UI"
    - path: apps/web/src/components/chrome.tsx
      provides: "omit Sign up when closed"
  key_links:
    - from: apps/web/src/routes/setup.tsx
      to: apps/web/src/lib/ssr-auth.ts
      via: "beforeLoad needs_setup"
    - from: apps/web/src/routes/setup.credentials.tsx
      to: auth.confirm_admin_credentials
      via: "apiClient RPC"
    - from: apps/web/src/components/chrome.tsx
      to: auth.provider_config.allow_signup
      via: "public config fetch"
  assumptions:
    - "ASSUME: hand-author Switch like Checkbox; shadcn official only if it emits Octane-compatible .tsx/.tsrx"
    - "ASSUME: reconcile .tsrx rename when editing routes/components"
  prohibitions:
    - statement: "Must not show an AuthShell soft page for closed /signup — 404 only"
      flagged: true
    - statement: "Must not show disabled warm Sign up control when closed — omit entirely"
      flagged: true
    - statement: "Must not force ENV email/password change when Keep current password is on"
      flagged: true
---

<objective>
Ship UI-SPEC surfaces: Switch primitive, `/setup` wizard, `/setup/credentials`, closed-signup chrome/landing/login omissions, and admin allow_signup control.

Purpose: Complete AUTH-06/07 user-visible flows on top of SSR + API.
Output: Green setup/credentials/chrome integration tests; UI matches copy contract.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-self-host-admin-bootstrap/06-05-SUMMARY.md
@.planning/phases/06-self-host-admin-bootstrap/06-UI-SPEC.md
@.planning/phases/06-self-host-admin-bootstrap/06-CONTEXT.md
@.planning/phases/06-self-host-admin-bootstrap/06-PATTERNS.md
@apps/web/src/components/ui/checkbox.tsx
@apps/web/src/routes/signup.tsx
@apps/web/src/routes/reset-password.tsx
@apps/web/src/components/auth-shell.tsx
@apps/web/src/components/chrome.tsx
@apps/web/src/routes/admin/auth.tsx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wizard form → bootstrap_setup | Unauthenticated first-admin create |
| Credentials form → confirm RPC | ENV admin must leave default username |
| Chrome public config | Fail closed on unknown allow_signup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-13 | Elevation of privilege | Closed signup UI bypass | medium | mitigate | UI omit + SSR 404; API gate from 06-04 remains authority |
| T-06-14 | Spoofing | returnTo after confirm | medium | mitigate | safeReturnTo only (D-22) |
| T-06-SC | Tampering | cargo/npm installs | high | mitigate | No new npm packages; hand-author Switch from existing @octanejs/base-ui |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (06-06-T1): Switch UI + /setup wizard</name>
  <reversibility rating="costly">Closed-signup chrome/routing contract (D-06) begins here for logged-out UI.</reversibility>
  <files>apps/web/src/components/ui/switch.tsx, apps/web/src/routes/setup.tsx, apps/web/src/routes/setup.integration.test.ts</files>
  <read_first>apps/web/src/components/ui/checkbox.tsx, apps/web/src/routes/signup.tsx, apps/web/src/components/auth-shell.tsx, .planning/phases/06-self-host-admin-bootstrap/06-UI-SPEC.md, .planning/phases/06-self-host-admin-bootstrap/06-PATTERNS.md</read_first>
  <action>
1. Hand-author Switch wrapper from `@octanejs/base-ui/switch` like Checkbox (data-slot=switch; ≥44px hit; primary checked track). Prefer tracked `.tsx` path; reconcile `.tsrx` if rename in flight.
2. Implement `/setup` per UI-SPEC: AuthShell titles/copy; field order; Allow open signup Switch default off + helper; Create system admin CTA; Working… pending; AuthErrorBanner mappings; document title Set up · Octanest; success → `/`. SSR beforeLoad when !needs_setup → `/`. If untracked setup.tsrx exists, stabilize into tracked module rather than dual files.
3. Green setup.integration.test.ts Wave 0 assertions.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/setup.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Allow open signup|Create system admin|Set up this instance' apps/web/src/routes/setup.tsx apps/web/src/routes/setup.tsrx 2>/dev/null`
    - `test -f apps/web/src/components/ui/switch.tsx -o -f apps/web/src/components/ui/switch.tsrx`
    - setup integration test exits 0
  </acceptance_criteria>
  <done>Wizard UI with allow_signup Switch matches UI-SPEC</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (06-06-T2): /setup/credentials forced change</name>
  <files>apps/web/src/routes/setup.credentials.tsx, apps/web/src/routes/setup.credentials.integration.test.ts, apps/web/src/routes/login.tsx</files>
  <read_first>apps/web/src/routes/reset-password.tsx, apps/web/src/lib/return-to.ts, .planning/phases/06-self-host-admin-bootstrap/06-UI-SPEC.md</read_first>
  <behavior>
    - Title Confirm admin account; Keep current password Switch default on
    - Client rejects username equal to system-administrator before submit
    - Success clears flag path via confirm RPC; safeReturnTo else /
    - Wizard-created admins never see this route (SSR redirects if flag clear)
  </behavior>
  <action>
Implement `/setup/credentials` per D-16/D-17 + UI-SPEC (AuthShell, prefill email/username, password fields when Keep off). Wire confirm_admin_credentials. Ensure SSR/home gate from 06-05 redirects flagged users here. Login post-success: if must_change → credentials with returnTo; else safeReturnTo (D-22). Green credentials integration tests.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/setup.credentials.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Confirm admin account|Keep current password|system-administrator' apps/web/src/routes/setup.credentials.tsx apps/web/src/routes/setup.credentials.tsrx 2>/dev/null`
    - credentials integration test exits 0
  </acceptance_criteria>
  <done>ENV forced credential change UI works end-to-end with API</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (06-06-T3): Closed signup chrome + admin Switch</name>
  <files>apps/web/src/components/chrome.tsx, apps/web/src/routes/index.tsx, apps/web/src/routes/login.tsx, apps/web/src/routes/signup.tsx, apps/web/src/routes/admin/auth.tsx, apps/web/src/components/chrome.integration.test.ts</files>
  <read_first>apps/web/src/components/chrome.tsx, apps/web/src/routes/admin/auth.tsx, apps/web/src/routes/login.tsx, .planning/phases/06-self-host-admin-bootstrap/06-UI-SPEC.md</read_first>
  <behavior>
    - allow_signup false/unknown → omit header Sign up; omit landing Get started→/signup; omit login Create an account
    - needs_setup → omit Sign in/Sign up account CTAs
    - allow_signup true → restore Phase 4 Sign up / Get started / cross-link
    - admin/auth Switch Allow open signup + helper persists via update_settings
  </behavior>
  <action>
Per D-06 and UI-SPEC closed signup + UI Considerations: fail-closed omit Sign up until public config resolves. Extend admin auth form after provider mode with same Switch/label/helper. Green chrome.integration.test.ts. No invite UI.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/components/chrome.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'allow_signup|Allow open signup' apps/web/src/components/chrome.tsx apps/web/src/routes/admin/auth.tsx apps/web/src/components/chrome.tsrx apps/web/src/routes/admin/auth.tsrx 2>/dev/null`
    - chrome integration test exits 0
  </acceptance_criteria>
  <done>Closed signup UX and admin toggle match UI-SPEC</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `components/ui/switch` | 06-06 | |
| `/setup` + `/setup/credentials` | 06-06 | AUTH-07 + AUTH-06 UX |
| chrome/login/landing omissions | 06-06 | D-06 |
| admin auth Switch | 06-06 | D-08 |

<verification>
setup + credentials + chrome vitest green; web build green.
</verification>

<success_criteria>
All UI-SPEC screens/rules for this phase implemented; Wave 0 web stubs green.
</success_criteria>

<output>
Create `.planning/phases/06-self-host-admin-bootstrap/06-06-SUMMARY.md` when done
</output>
