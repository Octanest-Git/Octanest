---
phase: 06-self-host-admin-bootstrap
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/octanest-api/tests/auth_bootstrap.rs
  - crates/octanest-api/tests/auth_signup.rs
  - crates/octanest-api/tests/auth_forced_credentials.rs
  - crates/octanest-db/tests/dialect_auth.rs
  - apps/web/src/routes/setup.integration.test.ts
  - apps/web/src/routes/setup.credentials.integration.test.ts
  - apps/web/src/routes/dashboard.integration.test.ts
  - apps/web/src/routes/index.integration.test.ts
  - apps/web/src/components/chrome.integration.test.ts
requirements:
  - AUTH-06
  - AUTH-07
autonomous: true
user_setup: []
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Wave 0 API stubs exist for partial ENV, seeded system-administrator + must_change_credentials, strict RPC allowlist, allow_signup closed signup, and forced credential confirm"
    - "Wave 0 web stubs exist for /setup Switch, /setup/credentials, /dashboard notFound, index/home SSR tree gate, and chrome omit Sign up"
    - "dialect_auth stub covers 0006_bootstrap_flags columns (allow_signup, must_change_credentials)"
    - "Wave 0 index/home SSR stub asserts needs_setup → /setup vs session → SignedInHome vs marketing tree selection (D-18/D-20)"
  artifacts:
    - path: crates/octanest-api/tests/auth_bootstrap.rs
      provides: "AUTH-07 + allowlist + partial ENV failing stubs (intentionally new if untracked WIP present)"
    - path: crates/octanest-api/tests/auth_forced_credentials.rs
      provides: "forced credential confirm failing stubs"
    - path: apps/web/src/routes/setup.integration.test.ts
      provides: "wizard Switch failing stubs"
  key_links:
    - from: .planning/phases/06-self-host-admin-bootstrap/06-VALIDATION.md
      to: crates/octanest-api/tests/auth_bootstrap.rs
      via: "Wave 0 Requirements checklist"
  assumptions:
    - "ASSUME (AUTH-06 adjacency): partial ENV does not seed — wizard path; confirmed by CONTEXT D-13"
    - "ASSUME (AUTH-07 concurrency): bootstrap_setup re-checks count_users before create (existing pattern to stub)"
  prohibitions:
    - statement: "Must not leave ENV-seeded default username system-administrator without a forced credential confirm gate"
      flagged: true
    - statement: "Must not introduce cloud vs self-host deployment-mode conditionals in bootstrap tests or helpers"
      flagged: true
---

<objective>
Scaffold failing Nyquist/Wave 0 tests for AUTH-06/07 gates before implementation plans turn them green.

Purpose: Per VALIDATION.md / RESEARCH Wave 0 Gaps — every later `<automated>` must have a real test file path.
Output: Failing (or compile-gated) API + web + dialect stubs matching the phase filter commands.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-self-host-admin-bootstrap/06-CONTEXT.md
@.planning/phases/06-self-host-admin-bootstrap/06-RESEARCH.md
@.planning/phases/06-self-host-admin-bootstrap/06-VALIDATION.md
@.planning/phases/06-self-host-admin-bootstrap/06-PATTERNS.md
@crates/octanest-api/tests/auth_signup.rs
@crates/octanest-db/tests/dialect_auth.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → ENV | Parallel nextest must not race OCTANEST_ADMIN_* |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-00 | Elevation of privilege | Wave 0 stubs only | low | accept | Stubs assert secure outcomes; no production code change |
| T-06-SC | Tampering | cargo/npm installs | high | mitigate | No new packages in this plan |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (06-00-T1): API Wave 0 stubs — bootstrap, seed, allowlist, credentials</name>
  <files>crates/octanest-api/tests/auth_bootstrap.rs, crates/octanest-api/tests/auth_signup.rs, crates/octanest-api/tests/auth_forced_credentials.rs</files>
  <read_first>crates/octanest-api/tests/auth_signup.rs, crates/octanest-api/tests/support/, .planning/phases/06-self-host-admin-bootstrap/06-VALIDATION.md, .planning/phases/06-self-host-admin-bootstrap/06-RESEARCH.md, .planning/phases/06-self-host-admin-bootstrap/06-PATTERNS.md</read_first>
  <behavior>
    - Partial ENV (email-only or password-only) → no seed; needs_setup true (D-13)
    - Both ENV + empty users → username system-administrator, must_change_credentials true, allow_signup from OCTANEST_ALLOW_SIGNUP (D-07/D-08/D-15/D-16)
    - While needs_setup, non-allowlisted RPC (e.g. auth.login) fails; auth.bootstrap_status / auth.bootstrap_setup / system.health succeed (D-11)
    - Second bootstrap_setup → auth.setup_unavailable (AUTH-07 idempotency)
    - After bootstrap with allow_signup false, auth.signup rejected
    - Forced confirm rejects username equal to system-administrator; keep-password path OK (D-16/D-17)
  </behavior>
  <action>
Per VALIDATION Wave 0 and RESEARCH Validation Architecture (D-01–D-04 empty-instance, not self-host-only):

1. Create or stabilize `crates/octanest-api/tests/auth_bootstrap.rs` (intentionally new — currently untracked WIP; do not treat WIP blob as source of truth; follow auth_signup harness + PATTERNS). Include failing assertions for partial ENV, wizard allow_signup persistence, strict RPC allowlist, and second-setup unavailable.
2. Extend `auth_signup.rs` `seeded_admin_is_auto_verified` (or sibling tests) with failing expectations for username `system-administrator`, `must_change_credentials`, and `OCTANEST_ALLOW_SIGNUP` default false / true parse — use existing ENV mutex helper pattern from support/ or bootstrap lock.
3. Add new `auth_forced_credentials.rs` with failing cases for confirm RPC (name per RESEARCH: `auth.confirm_admin_credentials`) — reject default username; keep current password when Switch-equivalent flag true.
4. Stubs must compile and fail for the right reason (assert not yet true / unimplemented), not fail to compile from missing modules — `#[ignore]` only if a module literally does not exist yet; prefer `todo!`/`assert!` red tests that later plans un-ignore by implementing.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(bootstrap) | test(seeded_admin) | test(signup) | test(forced_credentials) | test(confirm_admin)'</automated>
    <fails_when>exit code == 0 with all green (Wave 0 must show RED/ignored unfinished); or exit code != 0 solely due to compile errors preventing stub files from existing</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f crates/octanest-api/tests/auth_bootstrap.rs`
    - `test -f crates/octanest-api/tests/auth_forced_credentials.rs`
    - `rg -n 'system-administrator|must_change|allow_signup|setup_unavailable|partial' crates/octanest-api/tests/auth_bootstrap.rs crates/octanest-api/tests/auth_signup.rs crates/octanest-api/tests/auth_forced_credentials.rs`
  </acceptance_criteria>
  <done>API Wave 0 stubs exist and are runnable under the phase nextest filter</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (06-00-T2): Web + dialect Wave 0 stubs</name>
  <files>crates/octanest-db/tests/dialect_auth.rs, apps/web/src/routes/setup.integration.test.ts, apps/web/src/routes/setup.credentials.integration.test.ts, apps/web/src/routes/dashboard.integration.test.ts, apps/web/src/routes/index.integration.test.ts, apps/web/src/components/chrome.integration.test.ts</files>
  <read_first>crates/octanest-db/tests/dialect_auth.rs, apps/web/src/routes/signup.integration.test.ts, apps/web/src/components/signed-in-home.integration.test.ts, .planning/phases/06-self-host-admin-bootstrap/06-UI-SPEC.md, .planning/phases/06-self-host-admin-bootstrap/06-VALIDATION.md</read_first>
  <behavior>
    - dialect_auth expects allow_signup + must_change_credentials columns after migrate
    - /setup renders Allow open signup Switch + Create system admin CTA copy
    - /setup/credentials has Keep current password Switch + rejects default username copy
    - /dashboard route resolves to notFound
    - index/home: needs_setup → /setup redirect; session → SignedInHome in first tree; anonymous → marketing without SignedInHome (D-18/D-20)
    - chrome omits Sign up when allow_signup false/unknown
  </behavior>
  <action>
1. Extend `dialect_auth.rs` with failing checks for `0006_bootstrap_flags` columns once migration lands (stub asserts column presence helpers or SELECT after migrate — leave RED until 06-01). Live dialects already have `0005_user_role.sql`; next free id is **0006**.
2. Add vitest integration stubs mirroring existing signup/signed-in-home patterns for setup, setup.credentials, dashboard notFound, **index/home SSR tree gate**, and chrome omit — assert UI-SPEC copy strings and Switch labels. Prefer tracked route module names (`setup.tsx` / `.tsrx` per in-flight rename); note in SUMMARY which extension landed.
3. For `index.integration.test.ts`: assert loader/beforeLoad priority selects the correct tree (mock ssr-auth helpers or route options) so wrong marketing-vs-SignedInHome-vs-/setup selection fails the test — do not rely on `bun build` alone.
4. Do not implement production UI here.
  </action>
  <verify>
    <automated>bun --cwd apps/web run test:unit -- setup.integration setup.credentials dashboard.integration index.integration chrome.integration 2>/dev/null || bun --cwd apps/web exec vitest run src/routes/setup.integration.test.ts src/routes/setup.credentials.integration.test.ts src/routes/dashboard.integration.test.ts src/routes/index.integration.test.ts src/components/chrome.integration.test.ts</automated>
    <fails_when>all listed tests pass green (Wave 0 expects RED); or vitest cannot find any of the stub files including index.integration.test.ts</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/routes/setup.integration.test.ts`
    - `test -f apps/web/src/routes/setup.credentials.integration.test.ts`
    - `test -f apps/web/src/routes/dashboard.integration.test.ts`
    - `test -f apps/web/src/routes/index.integration.test.ts`
    - `test -f apps/web/src/components/chrome.integration.test.ts`
    - `rg -n 'allow_signup|must_change_credentials|0006' crates/octanest-db/tests/dialect_auth.rs`
    - `rg -n 'needs_setup|SignedInHome|/setup' apps/web/src/routes/index.integration.test.ts`
  </acceptance_criteria>
  <done>Web + dialect Wave 0 stubs exist for later plans to turn green</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| Wave 0 API/web/dialect stubs | 06-00 | RED until later waves |
| `0006_bootstrap_flags` + DB/DTO fields | 06-01 | Schema foundation (after live `0005_user_role`) |
| ENV seed tracer + confirm credentials RPC | 06-02 | AUTH-06 E2E |
| Wizard + strict RPC allowlist | 06-03 | AUTH-07 |
| Closed signup + admin allow_signup API | 06-04 | AUTH-05 interaction |
| Shared root SSR access gate + `/` tree | 06-05 | D-09/D-10, D-18–D-20, must_change |
| `/setup`, `/setup/credentials`, Switch | 06-06 | UI-SPEC wizard/credentials |
| `/dashboard` notFound + closed `/signup` | 06-09 | D-19 / D-06 |
| chrome omit + admin Switch | 06-08 | D-06 / D-08 UI |
| Docs + REQUIREMENTS reframe + rpc-gen | 06-07 | D-02 |

<verification>
Wave 0 files exist; nextest/vitest filters discover stubs; VALIDATION Wave 0 checkboxes can be ticked as file-present (still RED).
</verification>

<success_criteria>
Every VALIDATION Wave 0 gap has a concrete test path referenced by later plans.
</success_criteria>

<output>
Create `.planning/phases/06-self-host-admin-bootstrap/06-00-SUMMARY.md` when done
</output>
