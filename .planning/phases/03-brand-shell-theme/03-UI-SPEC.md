---
phase: 3
slug: brand-shell-theme
status: approved
shadcn_initialized: true
preset: base-nova
created: 2026-09-09
reviewed_at: 2026-09-09
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for Brand Shell & Theme. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
> Locked upstream: `03-CONTEXT.md` D-01…D-30. Do not reopen those decisions.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (already initialized) |
| Preset / style | **base-nova** (`apps/web/components.json`) |
| Component library | Base UI (shadcn Base UI path) |
| Icon library | lucide |
| CSS variables | enabled (`tailwind.cssVariables: true`, baseColor `neutral`) |
| Font display | **Sora Variable** (`--font-display`) — locked |
| Font body / UI | **Source Sans 3 Variable** (`--font-sans`) — locked |
| Variant utility | **CVA** on reusable components when variants apply (D-17) |

**Standing chrome rule (D-09):** Prefer reusable ShadCN + Base UI components over plain HTML controls for project UI chrome.

**Sources:** CONTEXT D-08–D-09, D-16–D-19; `components.json`; existing `styles.css` fonts.

---

## Screens in scope

| Route | Purpose | Focal point (eye first) |
|-------|---------|-------------------------|
| Global header | Product identity + theme + placeholder search/auth | Squircle mark (+ “Octanest” wordmark on wide viewports) |
| `/` | Multi-section marketing landing | Large squircle mark, then headline (no duplicate “Octanest” text), then CTA group |
| `/status` | Marketed live system-status page | Status hero message (operational / degraded / unreachable), then version + database detail |

**Out of scope (deferred):** real search, auth flows, status history, light/transparent mark variants, aggressive offline API caching.

---

## Visual hierarchy & layout contracts

### Global header (D-03, D-10, D-11, D-18, D-19)

**Order (LTR):** `[Mark ± wordmark]` → flex spacer → `[Search Input]` → `[Theme Select]` → `[Sign in]` → `[Sign up]`

| Element | Contract |
|---------|----------|
| Mark | Shared `OctanestMark` component; macOS **squircle** clip of `brand/octanest-mark.png` (via `public/`); **no** padded plate; same treatment light/dark; `alt="Octanest"`; header size **32×32** |
| Wordmark | Text “Octanest” in display font; **visible ≥ `sm`**; **hidden on narrow** (mark-only) |
| Search | ShadCN/Base UI **Input**, disabled, placeholder below; height ≥ 44px |
| Theme | ShadCN/Base UI **Select** (not native `<select>`); after search, before Sign in |
| Auth | ShadCN **Button** group: Sign in = `ghost` disabled; Sign up = `secondary` disabled; `title="Coming soon"` |

**Theme Select interaction (D-07, D-11):**
- Preference model: `system` \| `light` \| `dark`; persist `localStorage` key `octanest-theme` (existing `theme.ts`)
- Closed trigger: **icon + short label** for current choice (`System` / `Light` / `Dark`)
- Menu items: **icon + text** for all three
- Icons (default from CONTEXT discretion): `Monitor` = System, `Sun` = Light, `Moon` = Dark (lucide)
- Height ≥ 44px; label/trigger text uses Label role (14px / 400)

**FOUC (D-12):** Inline boot script in document head **before paint** reads `octanest-theme`, resolves system via `prefers-color-scheme`, toggles `html.dark`. React hydrate must not flash the opposite theme.

### Footer (D-06)

- Text-only: `© Octanest` + link **Status** → `/status`
- **No** footer mark

### `/` Landing — section map (D-22…D-25)

**Layout language:** editorial bands / split rows — **no card grid**, no card clutter in hero or marketing bands.

| # | Band | Job | Focal / content |
|---|------|-----|-----------------|
| 1 | **Hero** (first viewport) | Brand-first composition | Large squircle mark (**96×96** default; discretionary ±16 within scale) + **one** Display headline + one Body support + CTA group. **No** duplicate “Octanest” wordmark/text in hero (mark carries brand). Full-bleed cool↔warm radial atmosphere on bg (not flat). No stats, schedules, chips, overlays, or inset media cards. |
| 2 | **Dual-mode** (`id="explore"` target) | Cloud + self-host | Split editorial row: left “Octanest Cloud”, right “Self-host” — one headline each + short body; **not** a card grid |
| 3 | **Collaboration pillars** | Why nest here | Horizontal or stacked editorial pillars (3 max): host git / collaborate / ship — typography + spacing, not bordered cards |
| 4 | **Closing CTA** | Reaffirm action | Short Display/Heading line + CTA group mirroring hero rules |

**CTA rules (D-24):**
- **Get started** — primary Button, **disabled** placeholder until Auth phase (`title="Coming soon"`)
- **Explore** — secondary Button/link; **scrolls** to `#explore` (dual-mode band); enabled

### `/status` (D-26, D-27)

- Marketed system-status page with **clear status hero** (not a small muted badge)
- Data: live `system.health` **only** (no history)
- Below hero: version + database detail in Label/muted text
- Status panel may use surface (`card`/`surface`) for readability — this is an interactive status container, not a marketing card grid

| State | Hero treatment |
|-------|----------------|
| Loading | Muted body: “Checking Octanest services…” — no accent fill |
| Healthy | Cool **primary** accent on hero line “All systems operational” |
| Unhealthy / degraded | **Destructive** on “Degraded or failing” |
| Unreachable | **Destructive** on “Can’t reach the API” + recovery body |

---

## Mark & PWA / icons (D-01, D-02, D-05, D-20, D-28…D-30)

### Shared mark component

- Single `OctanestMark` (name discretionary) used in header, landing hero, and any marketing empty spots
- Clip: **macOS app-icon squircle** (CSS `clip-path` or SVG mask — executor discretion)
- Never recolor the PNG ad hoc; brand colors live in the asset + semantic tokens

### Document titles (D-21)

| Route | `<title>` |
|-------|-----------|
| `/` | `Octanest` |
| `/status` | `Status · Octanest` |
| Future pages | `Page · Octanest` pattern |

### Icon / favicon set (from `brand/octanest-mark.png`)

Generate and wire at minimum:
- `favicon.ico`
- PNG favicons (16, 32, 180+)
- `apple-touch-icon` (squircle where platform expects)
- Web manifest icons (192, 512) with squircle where it matters

### Vite PWA

| Field | Value |
|-------|-------|
| `name` / `short_name` | Octanest |
| `display` | `standalone` |
| `start_url` | `/` |
| `theme_color` | Follow brand surfaces — ≈ dark surface / primary cool when dark; light surface when light; use `theme-color` meta with `media` for light/dark where supported |
| `background_color` | App background (`--background` / dominant) |
| Service worker | **Assets-only / precache app shell**; **network-first or bypass** for `/api/*`, RPC, auth — never cache API responses |

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline tight padding |
| sm | 8px | Compact control padding, mark↔wordmark gap |
| md | 16px | Default element spacing, header horizontal padding unit |
| lg | 24px | Section inner padding, CTA group gap rhythm |
| xl | 32px | Layout gaps, header height rhythm companion |
| 2xl | 48px | Major section vertical breaks between landing bands |
| 3xl | 64px | Page-level padding (status / landing vertical) |

**Exceptions:**
- Touch targets (Search, Theme Select, Buttons, CTAs): **≥ 44px** height (a11y; not on 8-pt scale)
- Header bar height: **64px** (`3xl` / `h-16`) — locked from existing chrome
- Content max width: **72rem** (`max-w-6xl`) for chrome + landing; status may use **48rem** (`max-w-3xl`) for readable hero column

**Source:** Phase 1 UI-SPEC + existing chrome; confirmed as default for Phase 3.

---

## Typography

Exactly **4** sizes and **2** weights project-wide for this phase:

| Role | Size | Weight | Line Height | Font |
|------|------|--------|-------------|------|
| Label | 14px | 400 | 1.4 | Source Sans 3 |
| Body | 16px | 400 | 1.5 | Source Sans 3 |
| Heading | 24px | 600 | 1.2 | Sora |
| Display | 40px | 600 | 1.15 | Sora |

**Weights used:** **400** and **600** only — no 500/700 in Phase 3 UI.

**Application map:**
- Header wordmark → Heading (24 / 600) on wide; mark-only on narrow
- Landing hero headline → Display
- Landing support / dual-mode / pillars body → Body
- Band section titles → Heading
- Status page title → Heading
- Status hero line → Heading or Display (prefer **Heading 24** if viewport feels dense; **Display 40** allowed for healthy/degraded hero — pick one consistently; default **Display** for status hero)
- Meta (version · database), placeholders, footer → Label
- Buttons / Select trigger text → Label size **14px**; weight **600** on primary/secondary CTA labels, **400** on ghost/muted chrome (both weights are in the declared pair)

**Source:** Phase 1 UI-SPEC + existing `styles.css` / chrome sizes; locked fonts from CONTEXT.

---

## Color

### Semantic token mapping (D-13…D-15)

Migrate off legacy `--color-accent-cool` / `--color-accent-warm`. Map Octanest brand into full ShadCN semantic layer:

| Semantic token | Brand meaning | Notes |
|----------------|---------------|-------|
| `--primary` | **Cool / left** (blues–cyans) | Must read as logo cool side |
| `--secondary` | **Warm / right** (oranges) | Must read as logo warm side |
| `--background` | Dominant page bg | |
| `--card` / surface | Secondary chrome panels | Header, status panel |
| `--muted` / `--muted-foreground` | Supporting copy | |
| `--border` / `--input` / `--ring` | Chrome + focus | Ring uses cool/primary family |
| `--destructive` | Errors / degraded / unreachable | Not for warm brand accent |
| `--foreground` | Primary text | |

**Remove** all call sites of `--color-accent-cool` / `--color-accent-warm` in this phase.

### Light theme values

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#F4F6F8` | `--background` — page canvas |
| Secondary (30%) | `#FFFFFF` | `--card` / header/footer / status surface |
| Primary / accent cool (≤10% with warm) | `#1B6FE8` | `--primary` — see reserved list |
| Secondary warm | `#F07818` | `--secondary` — see reserved list |
| Text primary | `#12141A` | `--foreground` |
| Text muted | `#5C6570` | `--muted-foreground` |
| Destructive | `#D92D20` | Degraded/unreachable hero + future destructive only |
| Border | `#D8DEE6` | `--border` |

### Dark theme values

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0B0C0E` | Mark-friendly near-black bg |
| Secondary (30%) | `#16181D` | Header/footer / status surface |
| Primary / accent cool | `#3B8CFF` | Lifted for contrast on dark |
| Secondary warm | `#FF8A2B` | Lifted warm |
| Text primary | `#F2F4F7` | |
| Text muted | `#9AA3B2` | |
| Destructive | `#F97066` | |
| Border | `#2A2F3A` | |

**Hex source:** existing `styles.css` (tuned tokens that already match logo cool/warm split). Executor may micro-tune toward mark samples but must keep primary=cool-left, secondary=warm-right and avoid purple-on-white / generic indigo slop.

### 60 / 30 / 10

- **60%** dominant background
- **30%** secondary surfaces (header, footer, status panel)
- **10%** combined primary cool + secondary warm accents

### Accent reserved for (explicit — never “all interactive”)

**Primary (cool) reserved for:**
1. Primary CTA fill (**Get started**) when enabled in later phases; disabled state still uses primary token at reduced opacity
2. Focus rings (`--ring`) on interactive controls
3. Healthy status hero line on `/status`
4. Theme Select selected-item indicator / check affordance (subtle)
5. Dual-mode band cool-side accent rule or underline (Cloud column only)

**Secondary (warm) reserved for:**
1. Secondary CTA outline / text accent (**Explore**)
2. Dual-mode band warm-side accent (Self-host column only)
3. Closing CTA secondary treatment paired with primary
4. Brand atmosphere gradients (radial cool/warm mesh — atmospheric only, not UI chrome fill)

**Destructive reserved for:**
1. Status degraded / unreachable hero lines
2. Future destructive actions (none confirmed this phase)

**Not accent:** footer links, disabled search chrome, disabled auth ghost/secondary beyond their defined variants, body links (use foreground + underline).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Product name | **Octanest** (never bare “Octane”; never market as “GitHub clone”) |
| Mark `alt` | Octanest |
| Landing headline | Where repositories nest — cloud or yours |
| Landing supporting | Social coding forge: host git, collaborate, and ship — one product for Octanest Cloud and self-host |
| Dual-mode Cloud title | Octanest Cloud |
| Dual-mode Cloud body | Hosted forge with the same product you run yourself — one release train. |
| Dual-mode Self-host title | Self-host |
| Dual-mode Self-host body | Run Octanest on your machines with Docker Compose — your data, your network. |
| Pillars titles | Host git · Collaborate · Ship |
| Pillars bodies (defaults) | Browse repos and history in the browser. / Issues and pull requests for teams. / Releases and CI when you’re ready to ship. |
| Closing headline | One forge. Cloud or yours. |
| Primary CTA | **Get started** (disabled placeholder) |
| Secondary CTA | **Explore** (scroll to `#explore`; visible label may be “Explore” or “Explore Octanest” — prefer **Explore** to avoid hero brand duplication; header already says Octanest) |
| Header Search placeholder | Search public code (soon) |
| Header Auth | Sign in / Sign up |
| Auth / Get started hint | Coming soon |
| Theme aria-label | Theme |
| Theme options | System / Light / Dark |
| Footer | © Octanest |
| Footer link | Status |
| Status document title | Status · Octanest |
| Status page title | System status |
| Status loading | Checking Octanest services… |
| Status healthy heading | All systems operational |
| Status healthy body | API health check succeeded. This page reflects live `system.health` — history arrives in a later release. |
| Status unhealthy heading | Degraded or failing |
| Status unhealthy body | The API reported an unhealthy state. Retry in a moment or check your Compose/API logs. |
| Status unreachable heading | Can’t reach the API |
| Status unreachable body | Octanest couldn’t complete a health check. Confirm the stack is up (`docker compose` / `make dev`) and try again. |
| Status unreachable hint | Something blocked this request. Refresh the page or verify the API is running. |
| Status meta | `version {v} · database {dialect}` |
| Empty state | No empty-list UI in Phase 3; status always resolves to a health outcome |
| Destructive confirmation | **None** in Phase 3 |

**Source:** Phase 1 copy retained where still valid; dual-mode / pillars / closing defaults from CONTEXT Claude’s Discretion + BRAND-02.

---

## Motion (D-25)

Ship **exactly 3** intentional motions (CSS keyframes preferred; Motion library optional):

| # | Motion | Spec |
|---|--------|------|
| 1 | Hero entrance | Mark → headline → support/CTAs stagger fade+translateY(8px→0), 200–400ms ease-out, ≤3 steps |
| 2 | Section reveal | Dual-mode / pillars enter on scroll (IntersectionObserver or CSS) — opacity + 12px rise, once |
| 3 | CTA hover | Primary/secondary Buttons: brightness or border-color micro-transition ≤150ms (no glow pulse loops) |

**Reduced motion:** all three disabled or instant under `prefers-reduced-motion: reduce`.

**Status:** soft opacity when health resolves (counts as part of reveal polish; do not add decorative loops).

---

## Component inventory (Phase 3)

| Component | Action | Notes |
|-----------|--------|-------|
| `OctanestMark` | **Add** | Shared squircle-clipped mark |
| Button | **Replace** hand-rolled with ShadCN/Base UI + **CVA** | Variants: `default`/`primary`, `secondary`, `ghost`; h ≥ 44px |
| Input | **Add** ShadCN | Disabled header search |
| Select | **Add** ShadCN | Theme control (trigger + items with lucide icons) |
| Link | Keep / ShadCN if needed | Footer Status, Explore hash |
| Theme boot script | **Add** | Inline before paint |
| PWA plugin + manifest | **Add** | Assets-only SW |
| Favicon / icon set | **Add** | From brand mark |

Initialize only components required by chrome + landing + status. No third-party blocks.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | `button`, `input`, `select` (and their Base UI primitives / helpers as pulled by CLI) | not required — official registry |
| third-party | **none** | n/a |

No third-party registries. Do not add `@registry` URLs beyond shadcn official unless a future CONTEXT decision requires vetting.

---

## Implementation constraints (executor checklist)

- [ ] Shared squircle mark everywhere the mark appears (not one-off `<img>` in header only)
- [ ] Header: mark + wordmark wide; mark-only narrow; footer text-only
- [ ] Landing hero: large mark + headline — **no** duplicate “Octanest” text
- [ ] Theme: Select + persistence + FOUC boot; default system
- [ ] Full semantic token layer; delete `--color-accent-cool` / `--color-accent-warm`
- [ ] Primary = cool left; Secondary = warm right
- [ ] Landing bands: Hero → Dual-mode → Pillars → Closing CTA; editorial, no card grid
- [ ] Get started disabled; Explore scrolls to dual-mode
- [ ] Status branded hero states; live health only
- [ ] Titles: `Octanest` / `Status · Octanest`
- [ ] Full favicon/PWA icon set + Vite PWA assets-only SW
- [ ] CVA on variant components; ShadCN over native controls
- [ ] 2–3 motions with reduced-motion respect
- [ ] Fonts remain Sora + Source Sans 3; avoid purple-on-white AI default look

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved 2026-09-09
