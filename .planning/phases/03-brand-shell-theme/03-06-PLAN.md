---
phase: 03-brand-shell-theme
plan: "06"
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05"]
files_modified: []
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03
  - BRAND-04
  - BRAND-05
autonomous: false
user_setup: []
must_haves:
  truths:
    - "Zero `--color-accent-cool` / `--color-accent-warm` references remain anywhere under `apps/web/src`, closing D-15"
    - "No native `<select>`, `<option>`, or hand-rolled button/input element remains in app chrome (D-08, D-09, D-16, D-18)"
    - "The whole web app builds and the generated service worker still caches no API traffic (D-30)"
    - "A human confirms light and dark render correctly, system default is followed, and an explicit light/dark override persists across refresh (BRAND-03, BRAND-04, BRAND-05)"
    - "A human confirms the mark, four landing bands, status hero states, and the favicon/installable manifest read as Octanest (BRAND-01, BRAND-02)"
  artifacts:
    - path: .planning/phases/03-brand-shell-theme/03-06-SUMMARY.md
      provides: "phase-wide gate results plus recorded human verification outcome"
  key_links:
    - from: .planning/phases/03-brand-shell-theme/03-06-SUMMARY.md
      to: .planning/phases/03-brand-shell-theme/03-UI-SPEC.md
      via: "executor checklist items confirmed one by one"
      pattern: "Implementation constraints"
---

<objective>
Close Phase 3 with the checks that cannot live inside a single implementation plan: the cross-file gates that only hold once every wave has landed, and the human verification of the inherently visual requirements (light/dark correctness, system default, persisted override, brand read).

Purpose: BRAND-01…BRAND-05 sign-off against the UI-SPEC "Implementation constraints (executor checklist)". D-15 in particular spans four files owned by four different plans and can only be proven here.
Output: `03-06-SUMMARY.md` recording gate results and the human verdict. No source files are modified by this plan; any defect found is fixed by a gap-closure plan (`/gsd-plan-phase 3 --gaps`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-brand-shell-theme/03-CONTEXT.md
@.planning/phases/03-brand-shell-theme/03-UI-SPEC.md
@.planning/phases/03-brand-shell-theme/03-01-SUMMARY.md
@.planning/phases/03-brand-shell-theme/03-02-SUMMARY.md
@.planning/phases/03-brand-shell-theme/03-03-SUMMARY.md
@.planning/phases/03-brand-shell-theme/03-04-SUMMARY.md
@.planning/phases/03-brand-shell-theme/03-05-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Built client bundle + service worker → browser | Final shipped artifact; last point where a caching or token regression can be caught |
| Local dev stack (`make dev` / `docker compose`) → human verifier | Verification traffic only; no production data involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-21 | Information disclosure | service worker regression across waves | mitigate | The phase gate re-greps the built `sw.js` for `/api` and `/health` precache entries and for `CacheFirst`/`NetworkFirst`/`StaleWhileRevalidate` handlers, so a later change that reintroduces API caching fails the phase rather than shipping (re-check of T-03-16) |
| T-03-22 | Tampering | leftover legacy token call sites masking a broken theme | mitigate | Phase-wide grep for `accent-cool`/`accent-warm` and for `var(--color-` arbitrary values across `apps/web/src` must return zero, proving every surface is on the semantic layer that light/dark switching drives |
| T-03-23 | Repudiation | undocumented human verification | mitigate | The checkpoint verdict, including any rejected item, is written into `03-06-SUMMARY.md` before the phase is marked complete |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (03-06-T1): Phase-wide gates across all five plans</name>
  <files>.planning/phases/03-brand-shell-theme/03-06-SUMMARY.md</files>
  <read_first>.planning/phases/03-brand-shell-theme/03-UI-SPEC.md, apps/web/src/styles.css, apps/web/src/components/chrome.tsx, apps/web/src/routes/index.tsx, apps/web/src/routes/status.tsx</read_first>
  <action>
Run each gate below and record the actual command output in the SUMMARY. These are cross-plan invariants; do not modify source to make one pass — a failure means the phase needs a gap-closure plan.

1. **Legacy token eradication (D-15).** `grep -rn 'accent-cool\|accent-warm' apps/web/src apps/web/public/brand 2>/dev/null` must print nothing.
2. **Semantic layer only.** `grep -rn 'var(--color-' apps/web/src --include='*.tsx'` must print nothing; component files use Tailwind semantic utilities (`bg-card`, `text-muted-foreground`, …), and the only permitted raw `var(--…)` usages in TSX are `var(--font-display)` for the display font and `var(--primary)` / `var(--secondary)` / `var(--background)` / `var(--card)` inside the landing's `color-mix` atmosphere gradient.
3. **ShadCN over native controls (D-08, D-09, D-16, D-18).** `grep -rn '<select\|<option\|<input \|<button ' apps/web/src/components/chrome.tsx apps/web/src/routes` must print nothing. Native elements are allowed only inside `apps/web/src/components/ui/*` (the primitives themselves).
4. **Mark is shared (D-01).** `grep -rn 'octanest-mark.png' apps/web/src` must match `apps/web/src/components/octanest-mark.tsx` and nothing else.
5. **Titles (D-21).** `grep -q 'title: "Octanest"' apps/web/src/routes/__root.tsx` and `grep -qF 'title: "Status · Octanest"' apps/web/src/routes/status.tsx` both exit 0.
6. **Naming (BRAND-02).** `grep -rni 'github clone' apps/web/src .planning/phases/03-brand-shell-theme/*-SUMMARY.md` must print nothing, and no user-visible string names the product bare "Octane" — inspect every match of `grep -rn '>Octane<\|"Octane"' apps/web/src` (framework imports from the `octane` package are expected and fine).
7. **Typography discipline (UI-SPEC Typography).** `grep -rnE 'font-medium|font-bold|text-\[(12|18|20|28|32|48)px\]' apps/web/src` must print nothing.
8. **Motion budget (D-25).** `grep -rc '@keyframes' apps/web/src/routes/index.tsx` outputs `1`, `apps/web/src/routes/status.tsx` outputs `1`, and `grep -rnE 'infinite|animate-pulse|animate-spin|animate-ping|animate-bounce' apps/web/src` prints nothing.
9. **Build + service worker (D-30, T-03-21).** `bun install --frozen-lockfile && bun run --filter @octanest/web build`, then locate the emitted worker with `find apps/web -name 'sw.js' -not -path '*/node_modules/*' -not -path '*/dev-dist/*' | head -1` and assert it contains no `/api` or `/health` precache URL and no `CacheFirst`/`NetworkFirst`/`StaleWhileRevalidate` handler.
10. **Icon set (D-20).** All of `apps/web/public/favicon.ico`, `favicon-16.png`, `favicon-32.png`, `apple-touch-icon.png`, `icons/icon-192.png`, `icons/icon-512.png`, `icons/icon-512-maskable.png` exist and are non-empty.
11. **UI-SPEC executor checklist.** Walk the 13 items under "Implementation constraints (executor checklist)" in `03-UI-SPEC.md` and record each as met or not met, citing the file that satisfies it.

Write the results into `.planning/phases/03-brand-shell-theme/03-06-SUMMARY.md` as a table of gate → command → result before starting task 2.
  </action>
  <verify>
    <automated>set -e; test -z "$(grep -rn 'accent-cool\|accent-warm' apps/web/src || true)"; test -z "$(grep -rn '<select\|<option' apps/web/src/components/chrome.tsx apps/web/src/routes || true)"; bun install --frozen-lockfile; bun run --filter @octanest/web build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'accent-cool\|accent-warm' apps/web/src` prints nothing (exit status 1)
    - `grep -rn '<select\|<option' apps/web/src/components/chrome.tsx apps/web/src/routes` prints nothing
    - `grep -rn 'octanest-mark.png' apps/web/src | grep -vc 'octanest-mark.tsx'` outputs `0`
    - `grep -rnE 'font-medium|font-bold' apps/web/src` prints nothing
    - `grep -rnE 'infinite|animate-pulse|animate-spin|animate-ping|animate-bounce' apps/web/src` prints nothing
    - `bun install --frozen-lockfile` and `bun run --filter @octanest/web build` both exit 0
    - The built `sw.js` matches neither `"url":"[^"]*/api/` nor `CacheFirst|NetworkFirst|StaleWhileRevalidate`
    - All seven icon files exist and are non-empty
    - `03-06-SUMMARY.md` contains a gate table with the real command output for gates 1–11
  </acceptance_criteria>
  <done>Every cross-plan invariant of Phase 3 is proven mechanically, with results recorded</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 3 brand shell and theme, across five plans:
- Full ShadCN semantic token layer with cool `--primary` / warm `--secondary`, legacy `accent-cool`/`accent-warm` deleted; CVA `Button`, `Input`, Base UI `Select`; shared squircle `OctanestMark` (03-01)
- Branded header (mark + responsive wordmark, disabled search Input, theme Select, disabled auth Button group), text-only footer, pre-paint theme boot script (03-02)
- Four-band editorial landing — Hero → Dual-mode → Pillars → Closing CTA — with three reduced-motion-aware motions (03-03)
- Branded `/status` with Display hero states and the `Status · Octanest` title (03-04)
- Favicon/app-icon set generated from `brand/octanest-mark.png`, installable Octanest manifest, assets-only service worker (03-05)
  </what-built>
  <how-to-verify>
Bring the stack up: `make dev` (API on 127.0.0.1:8080 plus `bun run --filter @octanest/web dev`), or `make up` for the Compose path. Then:

1. **System default (BRAND-04):** In a fresh profile or after `localStorage.removeItem("octanest-theme")` + reload, set your OS to dark. Expected: `http://localhost:3000/` loads dark, the theme trigger reads "System", and there is no white flash during load. Switch the OS to light and reload — the page follows without touching the control.
2. **Override + persistence (BRAND-05):** Open the theme Select in the header. Expected: three items, System / Light / Dark, each with an icon, and the closed trigger shows the current choice as icon + label. Pick Dark, then refresh. Expected: still dark, trigger still reads "Dark", `localStorage.getItem("octanest-theme")` returns `"dark"`. Repeat with Light.
3. **Light/dark correctness (BRAND-03):** In each mode visit `/` and `/status`. Expected: no unreadable text, no invisible borders, no white-on-white or black-on-black panel, and blue reads as the primary accent while orange only appears as the secondary/atmosphere accent.
4. **Brand read (BRAND-01, BRAND-02):** Expected: the header mark is a rounded-square (macOS-style squircle) clip with no visible plate behind it; the "Octanest" wordmark appears at desktop width and disappears when you narrow the window below ~640px, leaving the mark alone; the browser tab shows the Octanest favicon and the title `Octanest` on `/` and `Status · Octanest` on `/status`.
5. **Landing bands (D-22, D-23, D-24, D-25):** Expected: four bands in order — hero (large mark, one headline, no duplicate "Octanest" text), Cloud/self-host split row, three pillars, closing CTA. Nothing looks like a grid of bordered cards. `Get started` is visibly disabled and tooltips "Coming soon"; `Explore` scrolls to the dual-mode band (`#explore`). Motions: hero content staggers in on load, dual-mode and pillars fade/rise once as you scroll, CTA hover is a quick color/brightness change with no pulsing. Enable OS "reduce motion" and reload — all content appears immediately with no animation.
6. **Status states (D-26, D-27):** With the API up, `/status` shows `All systems operational` as a large line in the cool primary color plus `version … · database …` beneath. Stop the API (`docker compose stop api`, or kill the cargo process) and reload — expect the large destructive `Can't reach the API` line with the recovery copy. Restart the API and reload to return to healthy.
7. **PWA (D-28, D-29, D-30):** Run `bun run --filter @octanest/web build` and serve the build (`bun run --filter @octanest/web preview`). In Chrome DevTools → Application: Manifest shows name/short name `Octanest`, `standalone`, start URL `/`, and the 192/512/maskable icons; an install affordance is offered. In Application → Service Workers the worker is active; in Cache Storage, confirm no entry exists for `/api/rpc` or `/health`. Reload `/status` with the Network tab open and confirm the health request is served from the network, not "(ServiceWorker)".
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 3, or list the numbered items that failed (with what you saw) so a gap-closure plan can be created</resume-signal>
</task>

</tasks>

<verification>
- [ ] All 11 automated gates in task 1 pass, with output recorded in `03-06-SUMMARY.md`
- [ ] Human verification covers BRAND-01 through BRAND-05 and returns "approved"
- [ ] Any rejected item is written into `03-06-SUMMARY.md` verbatim for `/gsd-plan-phase 3 --gaps`
</verification>

<success_criteria>
- D-15 proven closed across all four consumer files
- BRAND-01…BRAND-05 confirmed by a human against the ROADMAP Phase 3 success criteria
- No source file changed by this plan; defects route to gap closure rather than in-place patching
</success_criteria>

<output>
After completion, create `.planning/phases/03-brand-shell-theme/03-06-SUMMARY.md`
</output>
