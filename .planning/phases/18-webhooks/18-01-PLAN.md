---
phase: 18-webhooks
plan: "01"
type: execute
wave: 1
depends_on:
  - "00"
files_modified:
  - crates/octanest-core/src/webhook_types.rs
  - crates/octanest-core/src/lib.rs
  - crates/octanest-db/migrations/sqlite
  - crates/octanest-db/migrations/postgres
  - crates/octanest-db/migrations/mysql
  - crates/octanest-db/src/webhooks.rs
  - crates/octanest-db/src/lib.rs
  - crates/octanest-db/tests/dialect_webhooks.rs
  - crates/octanest-api/src/webhook/mod.rs
  - crates/octanest-api/src/webhook/dispatch.rs
  - crates/octanest-api/src/webhook/deliver.rs
  - crates/octanest-api/src/rpc.rs
  - crates/octanest-api/src/app.rs
  - crates/octanest-api/src/issue/mod.rs
  - crates/octanest-api/tests/webhook_rpc.rs
  - crates/octanest-api/tests/webhook_delivery.rs
  - packages/api-client/src/index.ts
requirements:
  - HOOK-01
  - HOOK-02
  - HOOK-03
autonomous: true
user_setup: []
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Admin can create/list a repo webhook with URL, secret, events, active via RPC (HOOK-01, D-HOOK-01..03, D-HOOK-02)"
    - "After issue.create, subscribed issues webhook receives async POST and a delivery attempt row records HTTP status (HOOK-02/03, D-HOOK-05, D-HOOK-08, D-HOOK-11, D-HOOK-13)"
    - "Non-Admin cannot manage webhooks (D-HOOK-02 anti-enumeration)"
  artifacts:
    - path: crates/octanest-api/src/webhook/mod.rs
      provides: "webhook.create/list/get RPC + Admin gate"
    - path: crates/octanest-api/src/webhook/dispatch.rs
      provides: "WebhookDispatcher.emit seam (D-HOOK-23)"
    - path: crates/octanest-db/src/webhooks.rs
      provides: "webhooks + deliveries persistence"
    - path: packages/api-client/src/index.ts
      provides: "generated webhook.* client after rpc-gen"
  key_links:
    - from: crates/octanest-api/src/issue/mod.rs
      to: crates/octanest-api/src/webhook/dispatch.rs
      via: "emit issues opened after successful create"
    - from: crates/octanest-api/src/webhook/deliver.rs
      to: crates/octanest-db/src/webhooks.rs
      via: "record attempt status after POST"
  assumptions:
    - "ASSUME: Migration filename is next free 00NN_webhooks across sqlite/postgres/mysql"
    - "ASSUME: Discretion — RPC namespace webhook.* (not repo.webhook.*)"
    - "ASSUME: Tracer delivery may be fire-and-forget spawn without full retry worker yet (retries land in 18-02)"
    - "ASSUME: HMAC signing lands in tracer path (D-HOOK-12) even if retries expand later"
  prohibitions:
    - statement: "Must not block issue.create on outbound HTTP failure"
      flagged: true
    - statement: "Must not hand-edit packages/api-client as source of truth"
      flagged: true
    - statement: "Must not invent org-level webhooks"
      flagged: true
---

<objective>
Ship the end-to-end tracer: Admin webhook create/list, WebhookDispatcher seam, issues→delivery attempt for one happy path, and shared DB schema for later push/PR/UI plans.

Purpose: Prove HOOK-01..03 vertically on issues before expanding emitters and Settings UI (D-HOOK-05, D-HOOK-08, D-HOOK-11, D-HOOK-23).
Output: Green webhook_rpc create/list + Admin denial; green webhook_delivery issues path with attempt status; dialect_webhooks green; rpc-gen client.
</objective>

<execution_context>
@/workspace/worktrees/phase-18/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/worktrees/phase-18/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-webhooks/18-CONTEXT.md
@.planning/phases/18-webhooks/18-RESEARCH.md
@.planning/phases/18-webhooks/18-VALIDATION.md
@.planning/phases/11-issues/11-CONTEXT.md
@.planning/phases/10-orgs-permissions/10-CONTEXT.md
@crates/octanest-api/src/repo/acl.rs
@crates/octanest-api/src/issue/mod.rs
@crates/octanest-api/src/release/mod.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin session → webhook URL/secret | Untrusted URL + secret config |
| API → external HTTPS endpoint | Outbound POST of issue payload |
| Capability ladder | Admin-only management |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Information Disclosure / SSRF | webhook URL | high | mitigate | Enforce https (D-HOOK-04); basic SSRF blocklist in deliver.rs; full hardening in 18-02 |
| T-18-02 | Information Disclosure | webhook secret | high | mitigate | Mask secret on list/get; one-time plaintext only on create response (D-HOOK-16) |
| T-18-03 | Spoofing | delivery signature | high | mitigate | Always HMAC-SHA256 sign raw body (D-HOOK-12) |
| T-18-SC | Tampering | crates.io hmac (if added) | high | mitigate | Prefer existing sha2+hmac; if direct hmac dep added, legitimacy checkpoint in 18-02 |
</threat_model>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (18-01-T1): End-to-end Admin create webhook → issue.opened delivery attempt</name>
  <files>crates/octanest-core/src/webhook_types.rs, crates/octanest-db/migrations/sqlite, crates/octanest-db/migrations/postgres, crates/octanest-db/migrations/mysql, crates/octanest-db/src/webhooks.rs, crates/octanest-api/src/webhook/mod.rs, crates/octanest-api/src/webhook/dispatch.rs, crates/octanest-api/src/webhook/deliver.rs, crates/octanest-api/src/rpc.rs, crates/octanest-api/src/issue/mod.rs, crates/octanest-api/tests/webhook_rpc.rs, crates/octanest-api/tests/webhook_delivery.rs, crates/octanest-db/tests/dialect_webhooks.rs, packages/api-client/src/index.ts</files>
  <behavior>
    - Admin webhook.create returns public hook with masked secret and one-time secret field on create (D-HOOK-16)
    - webhook.list returns hooks for repo; Write-only user denied (D-HOOK-02)
    - Subscribing to issues then issue.create enqueues delivery; mock HTTP sink receives POST with X-GitHub-Event: issues and X-Hub-Signature-256 (D-HOOK-06, D-HOOK-12)
    - deliveries query shows attempt with HTTP status (D-HOOK-13) without failing issue.create if sink down (D-HOOK-11)
  </behavior>
  <action>
  Implement the thin vertical slice for HOOK-01..03 on the issues path only (per D-HOOK-01, D-HOOK-05, D-HOOK-08, D-HOOK-23):
  1. Core DTOs + triple-dialect migration for webhooks + deliveries (+ attempts if needed); FK webhooks.repository_id so rename/transfer keep hooks (D-HOOK-24)
  2. DB helpers in octanest-db/src/webhooks.rs; store signing secret in DB so API can HMAC (D-HOOK-17)
  3. webhook.create/list/get RPC Admin-gated; secret masked except create reveal
  4. WebhookDispatcher.emit + deliver POST with Hookshot headers and HMAC-SHA256 over raw JSON bytes
  5. Call emit from issue.create (action opened) after successful persist — never fail create on delivery errors
  6. make rpc-gen; turn Wave 0 stubs green for create/list/Admin denial + issues delivery happy path
  Avoid push/PR emitters and Settings UI (later plans). Avoid org hooks.
  </action>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && cargo test -p octanest-db --test dialect_webhooks && cargo nextest run -p octanest-api -E 'test(webhook_create) | test(webhook_list) | test(webhook_admin) | test(webhook_issues_deliver)'</automated>
  </verify>
  <done>Issues happy path delivers signed POST and records attempt; Admin CRUD list/create green; dialect migration green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (18-01-T2): webhook.update / delete / active flag + ping stub path</name>
  <files>crates/octanest-api/src/webhook/mod.rs, crates/octanest-db/src/webhooks.rs, crates/octanest-api/tests/webhook_rpc.rs</files>
  <behavior>
    - Admin can update URL/events/active and rotate secret with one-time reveal (D-HOOK-03, D-HOOK-15, D-HOOK-16)
    - Delete removes hook; further issue events do not deliver to it (D-HOOK-15)
    - Inactive hook skips enqueue (D-HOOK-15)
  </behavior>
  <action>
  Complete HOOK-01 mutate/delete for the tracer schema: webhook.update, webhook.delete, honor active=false skip in dispatcher (D-HOOK-03, D-HOOK-15, D-HOOK-16). Optional webhook.ping may enqueue a ping delivery if cheap; otherwise leave ping to 18-02. Keep Admin gate identical to create.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(webhook_update) | test(webhook_delete) | test(webhook_inactive)'</automated>
  </verify>
  <done>Update/delete/inactive behaviors covered by green nextest filters.</done>
</task>

<task type="auto">
  <name>Task 3 (18-01-T3): Map issue.update lifecycle actions to issues events</name>
  <files>crates/octanest-api/src/issue/mod.rs, crates/octanest-api/src/webhook/dispatch.rs, crates/octanest-api/tests/webhook_delivery.rs</files>
  <behavior>
    - issue close → issues closed; reopen → reopened; title/body edit → edited (D-HOOK-08)
  </behavior>
  <action>
  Extend emitter mapping from issue.update state transitions to GitHub-like issues actions opened/edited/closed/reopened (D-HOOK-08, D-HOOK-22). Keep payload GitHub-shaped with repository + sender + issue number from Phase 11 shared #N (D-HOOK-07). Do not add issue_comment unless trivial.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(webhook_issues_)'</automated>
  </verify>
  <done>Issue lifecycle actions emit corresponding issues webhook actions with recorded attempts.</done>
</task>

</tasks>

<verification>
rpc-sync-check clean; dialect_webhooks + webhook create/list/update/delete/issues delivery filters green.
</verification>

<success_criteria>
Admin can manage hooks via RPC; issue events deliver signed payloads asynchronously; attempts visible via DB/RPC path used by later UI.
</success_criteria>

<output>
Create `.planning/phases/18-webhooks/18-01-SUMMARY.md` when done
</output>
