---
phase: 08-git-https-pats
plan: "09"
type: execute
wave: 6
depends_on:
  - "08"
  - "01"
files_modified:
  - apps/web/src/routes/settings/tokens.tsrx
  - apps/web/src/components/settings/pat-list.tsrx
  - apps/web/src/components/settings/pat-revoke-dialog.tsrx
  - apps/web/src/components/settings/settings-nav.tsrx
  - apps/web/src/components/chrome.tsrx
  - apps/web/src/routes/settings/profile.tsrx
  - apps/web/src/routes/settings/tokens.integration.test.ts
requirements:
  - GIT-11
autonomous: true
user_setup: []
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Token list empty state shows empty hero and Generate new token entry (GitHub-shaped)"
    - "Token list rows show note, kind, scopes, expiry, last used (+ IP per D-09), and Revoke token"
    - "Revoke uses AlertDialog with Revoke token confirm and Keep token dismiss (no generic Cancel)"
    - "Unverified users see list but cannot mint; Generate disabled + Verify your email to create a token."
    - statement: "Long PAT notes ellipsis with full title tooltip on list rows"
      verification: backstop
    - statement: "Long token notes wrap in revoke dialog description without clipping CTAs"
      verification: backstop
  artifacts:
    - path: apps/web/src/routes/settings/tokens.tsrx
      provides: "/settings/tokens list + Generate dropdown"
    - path: apps/web/src/components/settings/pat-revoke-dialog.tsrx
      provides: "D-17 revoke confirm"
  key_links:
    - from: apps/web/src/routes/settings/tokens.tsrx
      to: packages/api-client/src/index.ts
      via: "pat.list / pat.revoke Query"
  assumptions: []
  prohibitions:
    - statement: "Must not show plaintext PAT on list or after reload"
      flagged: true
    - statement: "Must not mix return ( JSX with Rivet @{ in same .tsrx"
      flagged: true
---

<objective>
Ship `/settings/tokens` list, settings secondary nav, Account menu link, Generate dropdown entry, and revoke AlertDialog (create forms in 08-10/08-11).

Purpose: GIT-11 manage surface per UI-SPEC D-14/D-17.
Output: Green tokens.integration.test list/revoke/nav cases; build passes.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-git-https-pats/08-UI-SPEC.md
@.agents/skills/octane/SKILL.md
@apps/web/src/routes/settings/profile.tsrx
@apps/web/src/components/chrome.tsrx
@apps/web/src/routes/$owner.$repo.settings.tsrx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser session → pat.list/revoke | Cookie auth only; never display full secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information disclosure | token list UI | high | mitigate | Render prefix/metadata only; no token field (D-15) |
| T-08-03 | Elevation of privilege | Generate CTA | medium | mitigate | Disable Generate when email_verified false (D-24/D-25) |
| T-08-SC | Tampering | packages | low | accept | Existing AlertDialog/Badge/Dropdown only |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (08-09-T1): /settings/tokens list + nav + Generate gate</name>
  <files>apps/web/src/routes/settings/tokens.tsrx, apps/web/src/components/settings/pat-list.tsrx, apps/web/src/components/settings/settings-nav.tsrx, apps/web/src/components/chrome.tsrx, apps/web/src/routes/settings/profile.tsrx, apps/web/src/routes/settings/tokens.integration.test.ts</files>
  <read_first>.agents/skills/octane/SKILL.md, .planning/phases/08-git-https-pats/08-UI-SPEC.md, apps/web/src/routes/settings/profile.tsrx, apps/web/src/components/chrome.tsrx, apps/web/src/lib/session-queries.ts</read_first>
  <behavior>
    - route title Personal access tokens · Octanest
    - empty hero No personal access tokens + Generate new token
    - Profile | Personal access tokens secondary nav; Account menu item
    - unverified: list visible; Generate disabled + hint
  </behavior>
  <action>
Author Octane .tsrx route /settings/tokens (max-w-2xl, UI-SPEC copy). Use TanStack Query for pat.list; skeletons for loading; empty/error states per UI-SPEC. Dropdown Generate new token → Classic token links /settings/tokens/new, Fine-grained → /settings/tokens/new/fine-grained (pages may 404 until 08-10/11 — links required). Settings nav link-row on profile + tokens. Chrome Account menu Personal access tokens. Unauthenticated redirect /login?returnTo=/settings/tokens. Green list/nav/unverified portions of tokens.integration.test.ts.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/settings/tokens.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>vitest fails list/nav assertions; or build fails; or tokens.tsrx missing</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/routes/settings/tokens.tsrx`
    - `rg -n 'Personal access tokens|Generate new token|Verify your email to create a token' apps/web/src/routes/settings/tokens.tsrx apps/web/src/components/settings/`
    - `rg -n '/settings/tokens' apps/web/src/components/chrome.tsrx`
  </acceptance_criteria>
  <done>List + nav green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (08-09-T2): Revoke AlertDialog</name>
  <files>apps/web/src/components/settings/pat-revoke-dialog.tsrx, apps/web/src/components/settings/pat-list.tsrx, apps/web/src/routes/settings/tokens.integration.test.ts</files>
  <read_first>apps/web/src/routes/$owner.$repo.settings.tsrx, apps/web/src/components/ui/alert-dialog.tsrx, .planning/phases/08-git-https-pats/08-UI-SPEC.md</read_first>
  <behavior>
    - Revoke token opens dialog Revoke token? with Keep token / Revoke token
    - confirm calls pat.revoke and refreshes list
    - server error shows Couldn’t revoke token… inline; dialog stays open
  </behavior>
  <action>
Implement pat-revoke-dialog using existing AlertDialog (branch-delete pattern, no type-to-confirm). Wire row Revoke token. Pending Revoking…; dismiss Keep token. Long note wraps without clipping CTAs (backstop). Green revoke stub assertions.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/settings/tokens.integration.test.ts</automated>
    <fails_when>revoke dialog copy missing; or vitest fails revoke cases</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Revoke token\?|Keep token|Revoking' apps/web/src/components/settings/pat-revoke-dialog.tsrx`
  </acceptance_criteria>
  <done>Revoke confirm green</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `/settings/tokens` list UI | 08-09 | GIT-11 D-14 |
| Revoke AlertDialog | 08-09 | D-17 |

<verification>
tokens.integration.test list/nav/revoke green; web build OK.
</verification>

<success_criteria>
Users can list and revoke PATs in the UI with settings IA per UI-SPEC.
</success_criteria>

<output>
Create `.planning/phases/08-git-https-pats/08-09-SUMMARY.md` when done
</output>
