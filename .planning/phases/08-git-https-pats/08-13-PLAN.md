---
phase: 08-git-https-pats
plan: "13"
type: execute
wave: 7
depends_on:
  - "06"
  - "07"
  - "08"
  - "11"
  - "12"
files_modified:
  - docs/CONFIGURATION.md
  - docs/ARCHITECTURE.md
  - .planning/phases/08-git-https-pats/08-VALIDATION.md
requirements:
  - GIT-02
  - GIT-11
autonomous: true
user_setup: []
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "CONFIGURATION/ARCHITECTURE document Smart HTTP .git routing, PAT prefixes, and cookie-vs-PAT split"
    - "Phase verification commands green: pat_ + git_smart nextest, rpc-sync-check, web build"
  artifacts:
    - path: docs/CONFIGURATION.md
      provides: "Operator notes for .git Traefik + PUBLIC_ORIGIN"
    - path: .planning/phases/08-git-https-pats/08-VALIDATION.md
      provides: "Updated task map statuses toward validate-phase"
  key_links:
    - from: docs/CONFIGURATION.md
      to: docker-compose.yml
      via: "Traefik PathRegexp documentation"
  assumptions: []
  prohibitions:
    - statement: "Must not claim PAT works as RPC Bearer in docs"
      flagged: true
    - statement: "Must not put secrets in docs examples (use ona_pat_REDACTED style)"
      flagged: true
---

<objective>
Close Phase 8 with operator docs + full automated gate sweep across PAT RPC, Smart HTTP, rpc-sync, and web build.

Purpose: Prove success criteria before /gsd-verify-work; refresh VALIDATION task map.
Output: Docs updated; make test (or scoped filters) + rpc-sync-check green.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-git-https-pats/08-CONTEXT.md
@.planning/phases/08-git-https-pats/08-VALIDATION.md
@docs/CONFIGURATION.md
@docs/ARCHITECTURE.md
@docs/API.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → operators | Mis-doc of cookie-for-git would teach insecure patterns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-10 | Spoofing | docs | medium | mitigate | Document PAT HTTPS-only + ignore cookies on .git (D-01/D-12) |
| T-08-SC | Tampering | packages | low | accept | Docs + verify only |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (08-13-T1): CONFIGURATION + ARCHITECTURE Smart HTTP/PAT notes</name>
  <files>docs/CONFIGURATION.md, docs/ARCHITECTURE.md</files>
  <read_first>docs/CONFIGURATION.md, docs/ARCHITECTURE.md, docs/API.md, docker-compose.yml, .planning/phases/08-git-https-pats/08-RESEARCH.md</read_first>
  <action>
Update CONFIGURATION.md: OCTANEST_PUBLIC_ORIGIN drives clone URLs (D-19); Traefik PathRegexp for .git → API; no new env required for CGI beyond OCTANEST_REPOS_DIR; note in-memory failed-auth limits single-replica. Update ARCHITECTURE.md: Smart HTTP via git-http-backend CGI; PAT hash-at-rest; session cookies not used for git; classic/FG scope summary; point to API.md for procedures. Use redacted token examples only (ona_pat_… / ona_fg_…).
  </action>
  <verify>
    <automated>rg -n 'Smart HTTP|PathRegexp|ona_pat_|personal access token|PUBLIC_ORIGIN' docs/CONFIGURATION.md docs/ARCHITECTURE.md</automated>
    <fails_when>docs lack Smart HTTP/PAT/.git routing notes</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'git-http-backend|\\.git|PAT' docs/ARCHITECTURE.md`
    - `rg -n 'OCTANEST_PUBLIC_ORIGIN|PathRegexp|REPOS_DIR' docs/CONFIGURATION.md`
  </acceptance_criteria>
  <done>Operator docs describe Phase 8 surfaces</done>
</task>

<task type="auto">
  <name>Task 2 (08-13-T2): Phase gate verify + VALIDATION map refresh</name>
  <files>.planning/phases/08-git-https-pats/08-VALIDATION.md</files>
  <read_first>.planning/phases/08-git-https-pats/08-VALIDATION.md, Makefile</read_first>
  <action>
Run cargo nextest run -p octanest-api -E 'test(pat_)|test(git_smart)'; cargo test -p octanest-db --lib migration_parity; make rpc-sync-check; bun --cwd apps/web run build. Optionally make test if time allows. Update 08-VALIDATION.md Per-Task Verification Map rows to concrete plan/task IDs 08-00…08-13 with status reflecting green where verified. Do not set nyquist_compliant true (validate-phase owns that).
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(pat_) | test(git_smart)' && cargo test -p octanest-db --lib migration_parity && make rpc-sync-check && bun --cwd apps/web run build</automated>
    <fails_when>any command exit != 0</fails_when>
  </verify>
  <acceptance_criteria>
    - nextest pat_/git_smart exit 0
    - migration_parity exit 0
    - rpc-sync-check exit 0
    - web build exit 0
    - `rg -n '08-04|08-06|08-09' .planning/phases/08-git-https-pats/08-VALIDATION.md`
  </acceptance_criteria>
  <done>Phase automated gates green; VALIDATION map concrete</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| CONFIGURATION/ARCHITECTURE updates | 08-13 | Operator docs |
| VALIDATION task map refresh | 08-13 | Nyquist prep |

<verification>
Scoped nextest + migration_parity + rpc-sync-check + web build green.
</verification>

<success_criteria>
Phase 8 success criteria supported by automated gates and docs; ready for verify-work / UAT.
</success_criteria>

<output>
Create `.planning/phases/08-git-https-pats/08-13-SUMMARY.md` when done
</output>
