---
phase: 08-git-https-pats
plan: "07"
type: execute
wave: 5
depends_on:
  - "06"
files_modified:
  - docker-compose.yml
  - scripts/smoke-git-https.sh
  - Makefile
requirements:
  - GIT-02
autonomous: true
user_setup: []
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 1
  confidence: medium
must_haves:
  truths:
    - "Traefik routes PathRegexp ^/[^/]+/[^/]+\\.git to API with priority 110 so SPA does not steal Smart HTTP (D-18, D-22)"
    - "Smoke script can git ls-remote (and optionally push) through public origin against Compose"
  artifacts:
    - path: docker-compose.yml
      provides: "Traefik .git → API router"
    - path: scripts/smoke-git-https.sh
      provides: "Compose Smart HTTP smoke"
  key_links:
    - from: docker-compose.yml
      to: crates/octanest-api/src/routes/git_smart_http.rs
      via: "PathRegexp forwards to Axum Smart HTTP"
  assumptions:
    - "ASSUME: Docker available on operator machine for make up smoke; CI may skip if no Docker (document)"
  prohibitions:
    - statement: "Must not lower web catch-all priority above API .git rule"
      flagged: true
---

<objective>
Wire Traefik so `/{owner}/{repo}.git` reaches the API (not the SPA) and add a Compose smoke for git ls-remote/push.

Purpose: Pitfall 1 from RESEARCH — without this, clones get HTML 200.
Output: Updated compose labels + smoke script/Make target.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-git-https-pats/08-RESEARCH.md
@docker-compose.yml
@Makefile
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Traefik → API | .git paths must not hit unauthenticated SPA HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-09 | Spoofing | Traefik routing | high | mitigate | Priority 110 PathRegexp to API; verify smoke does not return text/html |
| T-08-SC | Tampering | packages | low | accept | Compose/Make only |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (08-07-T1): Traefik .git PathRegexp + smoke-git-https</name>
  <files>docker-compose.yml, scripts/smoke-git-https.sh, Makefile</files>
  <read_first>docker-compose.yml, Makefile, .planning/phases/08-git-https-pats/08-RESEARCH.md</read_first>
  <action>
Add Traefik API router (or widen existing) with priority 110 and rule Host(`localhost`) && PathRegexp(`^/[^/]+/[^/]+\.git`) merging to API service port 8080 (RESEARCH Traefik section). Keep existing API priority 100 and web priority 1. Add scripts/smoke-git-https.sh that: waits for health; creates or uses a known public repo if stack helpers exist OR documents prerequisite; runs git ls-remote against http://localhost/{owner}/{repo}.git and fails if response looks like HTML; optional PAT push path if env SMOKE_PAT set. Wire make target smoke-git-https or extend make smoke. Do not change OCTANEST_PUBLIC_ORIGIN semantics (D-19).
  </action>
  <verify>
    <automated>rg -n 'PathRegexp|\\.git|priority=110' docker-compose.yml && test -f scripts/smoke-git-https.sh && rg -n 'ls-remote|smoke-git-https' Makefile scripts/smoke-git-https.sh</automated>
    <fails_when>PathRegexp or priority 110 missing from docker-compose.yml; or smoke script missing; or Makefile has no reference</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'PathRegexp.*\\.git|priority=110' docker-compose.yml`
    - `test -f scripts/smoke-git-https.sh`
    - `rg -n 'smoke-git-https' Makefile`
  </acceptance_criteria>
  <done>Traefik .git routing + smoke script landed</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| Traefik `.git` rule | 08-07 | D-18/D-22 |
| `smoke-git-https.sh` | 08-07 | GIT-02 e2e |

<verification>
Compose labels and smoke script present; operator can run smoke when Docker up.
</verification>

<success_criteria>
.git requests reach API through Traefik; SPA cannot steal Smart HTTP paths.
</success_criteria>

<output>
Create `.planning/phases/08-git-https-pats/08-07-SUMMARY.md` when done
</output>
