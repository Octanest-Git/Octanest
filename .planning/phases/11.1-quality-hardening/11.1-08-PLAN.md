---
phase: 11.1-quality-hardening
plan: "08"
type: execute
wave: 4
gap_closure: true
gap_ids:
  - G-11.1-15
depends_on:
  - "07"
files_modified:
  - scripts/route-coverage-check.sh
  - apps/web/src/test/route-coverage.manifest.ts
  - docs/TESTING.md
  - .github/workflows/ci.yml
  - Makefile
requirements:
  - ISSUE-3
autonomous: true
---

<objective>
CI gate: every user-facing apps/web `src/routes/**/*.tsrx` page must appear in a coverage manifest with at least one of happy-dom render, stack-browser, or documented skip rationale — so pages like /admin/lfs cannot ship with only raw-source stubs.
</objective>

<tasks>

<task id="11.1-08-T1">
  <name>Route coverage manifest + checker script</name>
  <files>apps/web/src/test/route-coverage.manifest.ts, scripts/route-coverage-check.sh</files>
  <verify>
    <automated>./scripts/route-coverage-check.sh</automated>
  </verify>
  <done>Script lists all route .tsrx files (exclude __root / layouts Outlet-only if marked), fails if any lack coverage entry; initial manifest covers forge + admin LFS/packages/auth + auth pages from 11.1-04.</done>
</task>

<task id="11.1-08-T2">
  <name>Wire Make + CI + TESTING.md</name>
  <files>Makefile, .github/workflows/ci.yml, docs/TESTING.md</files>
  <verify>
    <automated>rg -n "route-coverage" Makefile .github/workflows/ci.yml docs/TESTING.md</automated>
  </verify>
  <done>`make route-coverage-check` exists; CI job or step runs it; TESTING.md documents the gate and how to add entries.</done>
</task>

</tasks>

<summary>
Create `.planning/phases/11.1-quality-hardening/11.1-08-SUMMARY.md` when done
</summary>
