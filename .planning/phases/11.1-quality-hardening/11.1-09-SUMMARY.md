---
phase: 11.1-quality-hardening
plan: "09"
subsystem: ci
tags: [route-coverage, g-11.1-15, happy-dom, stack-browser, ci-gate]
status: complete

requires:
  - phase: 11.1-quality-hardening
    provides: "Forge stack-browser matrix + admin LFS/packages/auth (11.1-04 / 07 / 08)"
provides:
  - "CI route-coverage gate for every apps/web .tsrx page"
  - "Manifest + make route-coverage-check entrypoint"
affects:
  - future route additions (must update manifest)
  - G-11.1-15 closure

actuals:
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Declarative routeCoverageManifest + bun checker discovers .tsrx files"
    - "Evidence kinds: happy-dom | stack-browser | skip(rationale); layoutOnly excludes shells"

key-files:
  created:
    - apps/web/src/test/route-coverage.manifest.ts
    - scripts/route-coverage-check.sh
    - scripts/route-coverage-check.ts
  modified:
    - Makefile
    - .github/workflows/ci.yml
    - docs/TESTING.md

key-decisions:
  - "Checker lives in scripts/route-coverage-check.ts; shell wrapper is Make/CI entrypoint"
  - "TS manifest under apps/web/src/test/ instead of e2e JSON (plan path deviation — keep working solution)"

patterns-established:
  - "New .tsrx page → append manifest row → make route-coverage-check"

requirements-completed: [ISSUE-3]

gaps_closed:
  - G-11.1-15

deviations:
  - "Plan listed apps/web/e2e/route-coverage.manifest.json + scripts/check-route-coverage.sh; shipped TS manifest + route-coverage-check.{sh,ts}"

duration: 6min
completed: 2026-09-15
---

# Phase 11.1 Plan 09: Route coverage CI gate Summary

CI fails when any user-facing `apps/web` route `.tsrx` page lacks happy-dom, stack-browser, or documented skip evidence in the coverage manifest.

## Outcomes

- Manifest covers all discovered route files; checker reports PASS.
- `make route-coverage-check` and CI job `route-coverage` run the gate; `docs/TESTING.md` documents how to add entries.
- Admin LFS/packages/auth reference `forge-admin.stack.browser.test.tsx` from 11.1-07/08.

## Task commits

| Task | Commit | Summary |
|------|--------|---------|
| Manifest + checker | `6f0a74e` | Originally labeled 11.1-08 on old plan numbering |
| Make + CI + docs | `b311586` | Same |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Bun heredoc cannot run the checker**
- **Issue:** `bun <<'EOF'` printed usage instead of executing stdin as a script.
- **Fix:** Added `scripts/route-coverage-check.ts` and thin shell wrapper.
- **Commit:** `6f0a74e`

**2. [Rule 1 - Bug] JSDoc `**/` closed the manifest block comment early**
- **Fix:** Reworded the comment to avoid `*/` inside the glob.
- **Commit:** `6f0a74e`

**3. [Rule 3] Artifact paths**
- Plan asked for JSON under `e2e/` + `check-route-coverage.sh`; kept working TS manifest + `route-coverage-check` names.

## Self-Check: PASS (`./scripts/route-coverage-check.sh`)
