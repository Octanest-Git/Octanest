---
phase: 13-branch-protection
plan: "08"
type: execute
wave: 7
depends_on:
  - "06"
  - "07"
files_modified:
  - apps/web/src/routes
  - apps/web/src/components
  - apps/web/src/routes/$owner.$repo.pull.protection.integration.test.ts
  - docs/API.md
  - docs/ARCHITECTURE.md
requirements:
  - PR-08
  - ORG-06
autonomous: true
user_setup: []
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PR detail shows structured merge blockers when protection unsatisfied (PR-08, D-24)"
    - "docs/API.md documents branchProtection + commitStatus + merge block error shape"
  artifacts:
    - path: apps/web/src/routes/$owner.$repo.pull.protection.integration.test.ts
      provides: "PR blocker UI coverage"
    - path: docs/API.md
      provides: "Protection RPC + error contract"
  key_links:
    - from: PR detail UI
      to: pull.merge / merge preview reasons
      via: "D-22 structured reasons"
  assumptions:
    - "PRECONDITION: Phase 12 PR detail routes exist to extend"
  prohibitions:
    - statement: "Notifications for blocked merges are deferred to Phase 17"
      flagged: true
---

<objective>
Surface protection merge blockers on PR detail and document protection/status/merge-block contracts (PR-08, D-24).

Purpose: Users understand why merge is blocked; operators have API docs.
Output: Green pull.protection Vitest; API/ARCHITECTURE docs updated.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-branch-protection/13-CONTEXT.md
@.planning/phases/12-pull-requests/12-CONTEXT.md
@.agents/skills/octane/SKILL.md
@docs/API.md
@docs/ARCHITECTURE.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR UI → merge reasons | Reasons must not leak private repo existence beyond ACL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-18 | Information disclosure | merge reasons | medium | mitigate | Only show to callers who can already view the PR |
| T-13-19 | Tampering | client-only gate | high | mitigate | Server merge still enforces; UI is advisory display |
| T-13-SC | Tampering | npm | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (13-08-T1): PR merge blockers UI</name>
  <precondition>Phase 12 PR detail route exists on the integration branch.</precondition>
  <files>apps/web/src/routes, apps/web/src/components, apps/web/src/routes/$owner.$repo.pull.protection.integration.test.ts</files>
  <read_first>.agents/skills/octane/SKILL.md, .planning/phases/12-pull-requests/12-CONTEXT.md</read_first>
  <behavior>
    - When merge would be blocked, UI lists unmet requirements (reviews, checks, conversations, up-to-date, locked)
    - Merge control disabled or errors with same reasons; after requirements met, merge allowed per Phase 12 methods
  </behavior>
  <action>
Extend Phase 12 PR detail (Octane `.tsrx`) to display D-24 structured reasons from merge preview or failed merge (D-22). Turn pull.protection Vitest green. Run make web-lint and make web-format-check on touched web files before done. Do not add notification toasts beyond existing patterns; Phase 17 owns notifications.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/\$owner.\$repo.pull.protection.integration.test.ts && make web-lint && make web-format-check</automated>
  </verify>
  <done>PR protection UI test green; web lint/format clean.</done>
</task>

<task type="auto">
  <name>Task 2 (13-08-T2): API + architecture docs</name>
  <files>docs/API.md, docs/ARCHITECTURE.md</files>
  <read_first>docs/API.md, docs/ARCHITECTURE.md, .planning/phases/13-branch-protection/13-CONTEXT.md</read_first>
  <action>
Document repo.branchProtection.* and repo.commitStatus.* procedures, Admin/Write gates, merge-block error code + reasons shape, and that HTTPS/SSH pushes share bare-repo hooks (ORG-06). Note soft-protect coexistence (D-04) and Phase 19 Actions consuming the status store. No secrets in examples.
  </action>
  <verify>
    <automated>rg -n "branchProtection|commitStatus|branch_protection|merge_blocked" docs/API.md docs/ARCHITECTURE.md | head -20</automated>
  </verify>
  <done>Docs mention protection CRUD, statuses, merge block, and hook enforcement.</done>
</task>

</tasks>

<verification>
PR-08 UX + docs complete; phase product surface closed.
</verification>

<success_criteria>
Users see why merges are blocked; docs match shipped RPC/hook behavior.
</success_criteria>

<output>
Create `.planning/phases/13-branch-protection/13-08-SUMMARY.md` when done
</output>
