---
phase: 13-branch-protection
plan: "03"
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - crates/octanest-db/migrations/sqlite
  - crates/octanest-db/migrations/postgres
  - crates/octanest-db/migrations/mysql
  - crates/octanest-db/src
  - crates/octanest-api/src/protection
  - crates/octanest-api/src/repo
  - crates/octanest-api/src/bin/rpc_gen.rs
  - crates/octanest-api/tests/branch_protection_rpc.rs
  - packages/api-client
requirements:
  - ORG-05
autonomous: true
user_setup: []
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Admin can CRUD multiple rules with wildcard patterns (ORG-05, D-02, D-03)"
    - "Matching multi-rule union uses max review count and OR of restriction flags (D-02)"
  artifacts:
    - path: crates/octanest-api/src/protection
      provides: "Pattern match + union evaluate"
    - path: packages/api-client
      provides: "Generated branchProtection client types"
  key_links:
    - from: crates/octanest-api/src/repo
      to: packages/api-client
      via: "make rpc-gen after RPC registration"
  assumptions:
    - "ASSUME: Tracer exact-match schema is migrated forward without data loss"
  prohibitions:
    - statement: "Hand-editing packages/api-client as source of truth is forbidden"
      flagged: true
---

<objective>
Expand protection rules to full Admin CRUD with GitHub-style branch patterns and multi-rule union evaluation (ORG-05, D-02).

Purpose: Admins can target release/* etc. and stack overlapping rules correctly.
Output: Green branch_protection_rpc suite; rpc-gen client updated.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-branch-protection/13-CONTEXT.md
@.planning/phases/13-branch-protection/13-02-SUMMARY.md
@crates/octanest-api/src/protection
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin RPC → rules table | Untrusted pattern strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Tampering | pattern input | medium | mitigate | Reject empty/overlong patterns; validate UTF-8; no path traversal in patterns |
| T-13-06 | Elevation of privilege | CRUD | high | mitigate | Admin-only (D-03); private soft not_found for non-Admin |
| T-13-SC | Tampering | crates | high | mitigate | No new packages; hand-roll * / ? matcher if needed |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (13-03-T1): Pattern matcher + multi-rule union</name>
  <files>crates/octanest-api/src/protection, crates/octanest-api/tests/branch_protection_rpc.rs</files>
  <read_first>crates/octanest-api/src/protection, .planning/phases/13-branch-protection/13-RESEARCH.md</read_first>
  <behavior>
    - Pattern main matches only main; release/* matches release/1.0; ? single-char
    - Two matching rules: review counts 1 and 2 → required count 2; any allow_force_pushes false wins as disallow
  </behavior>
  <action>
Implement GitHub-style * / ? branch pattern matching and D-02 union semantics inside protection::evaluate. Prefer no new crates.io dependency (hand-rolled matcher acceptable). Add unit/integration coverage in branch_protection_rpc or colocated tests. Do not implement status-context union here beyond storing the field if already present from tracer — status enforcement is 13-04.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(branch_protect)'</automated>
  </verify>
  <done>Wildcard match and multi-rule union behaviors covered and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (13-03-T2): Full branchProtection CRUD + rpc-gen</name>
  <files>crates/octanest-api/src/repo, crates/octanest-api/src/bin/rpc_gen.rs, crates/octanest-db/migrations/sqlite, crates/octanest-db/migrations/postgres, crates/octanest-db/migrations/mysql, crates/octanest-db/src, packages/api-client, crates/octanest-api/tests/branch_protection_rpc.rs</files>
  <read_first>crates/octanest-api/src/repo/collaborators.rs, docs/API.md</read_first>
  <behavior>
    - Admin create/list/update/delete rules with flags from D-05..18 schema columns
    - Non-Admin mutate → soft not_found / forbidden consistent with settings ACL
  </behavior>
  <action>
Complete repo.branchProtection.create/list/update/delete (D-26) with Admin gate (D-03). Expand migration columns for dismiss_stale_reviews, require_conversation_resolution, require_last_push_approval, required_status_checks JSON/contexts, strict, allow_force_pushes, allow_deletions, enforce_admins, required_linear_history, lock_branch (D-05..18). Run make rpc-gen; never hand-edit api-client SoT. Green branch_protection_rpc CRUD cases.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(branch_protection_rpc)' && make rpc-sync-check</automated>
  </verify>
  <done>CRUD green; rpc-sync-check passes.</done>
</task>

</tasks>

<verification>
ORG-05 rule configuration complete at RPC layer.
</verification>

<success_criteria>
Admins can manage patterned rules with union evaluation ready for enforcement consumers.
</success_criteria>

<output>
Create `.planning/phases/13-branch-protection/13-03-SUMMARY.md` when done
</output>
