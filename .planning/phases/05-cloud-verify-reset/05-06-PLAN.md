---
phase: 05-cloud-verify-reset
plan: "06"
type: execute
wave: 6
depends_on:
  - "04"
  - "05"
files_modified:
  - crates/octanest-api/src/bin/rpc_gen.rs
  - packages/api-client/src/index.ts
  - apps/web/package.json
  - apps/web/src/components/ui/input-otp.tsx
  - apps/web/src/routes/verify.tsx
  - apps/web/src/components/verify-banner.tsx
  - apps/web/src/routes/__root.tsx
  - apps/web/src/styles.css
requirements:
  - AUTH-04
autonomous: false
user_setup: []
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Generated api-client UserPublic includes email_verified; verify/resend/request_verify RPCs are typed"
    - "Unfilled /verify form shows empty 8 OTP slots + Verify email CTA; no empty illustration"
    - "Token consume / submit / resend on /verify: primary disabled + Working…/Verifying…; AuthShell stays visible"
    - "AuthErrorBanner on /verify with problem + next step (invalid/expired, wrong user, network)"
    - "Token query vs code form on /verify; incomplete OTP does not submit until 8 digits or button with full code"
    - "Verify banner resend shows disabled + Working… / Email sent.; rate-limit helper when cooling down"
    - "Inline muted destructive Label in banner on resend failure; not toast-only"
    - statement: "Support/error Body on /verify wraps in max-w-md auth column"
      verification: backstop
    - statement: "Support and error copy on /verify wrap; no ellipsis on critical instructions"
      verification: backstop
    - statement: "Long verify banner message wraps; actions wrap under message on narrow viewports"
      verification: backstop
    - statement: "Banner message wraps within strip; no mid-word truncate"
      verification: backstop
  artifacts:
    - path: apps/web/src/components/ui/input-otp.tsx
      provides: "local input-otp wrapper"
    - path: apps/web/src/routes/verify.tsx
      provides: "/verify page"
    - path: apps/web/src/components/verify-banner.tsx
      provides: "persistent verify banner"
    - path: packages/api-client/src/index.ts
      provides: "regenerated client with email_verified"
  key_links:
    - from: apps/web/src/routes/verify.tsx
      to: packages/api-client/src/index.ts
      via: "auth.verify / auth.resendVerify"
    - from: apps/web/src/components/verify-banner.tsx
      to: packages/api-client/src/index.ts
      via: "auth.me.email_verified + resend"
    - from: apps/web/src/routes/__root.tsx
      to: apps/web/src/components/verify-banner.tsx
      via: "mount under SiteHeader"
---

<objective>
Regenerate the typed client, confirm `input-otp@1.5.0`, and ship `/verify` plus the persistent verify banner wired to `auth.me.email_verified`.

Purpose: User-visible AUTH-04 verification experience (D-11, D-13, D-16, D-17).
Output: rpc-gen client, InputOtp wrapper, `/verify`, VerifyBanner in root chrome.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cloud-verify-reset/05-UI-SPEC.md
@.planning/phases/05-cloud-verify-reset/05-CONTEXT.md
@.planning/phases/05-cloud-verify-reset/05-04-SUMMARY.md
@.planning/phases/05-cloud-verify-reset/05-05-SUMMARY.md
@apps/web/src/routes/login.tsx
@apps/web/src/components/auth-shell.tsx
@apps/web/src/routes/__root.tsx
@crates/octanest-api/src/bin/rpc_gen.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install → apps/web | Supply-chain risk for OTP widget |
| Browser → verify RPC | Token in query/referrer |
| Banner resend → API | Authenticated flood already rate-limited server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | `input-otp` npm install | high | mitigate | Blocking-human package-legitimacy checkpoint before `bun add`; pin `input-otp@1.5.0`; no postinstall scripts expected |
| T-05-15 | Information disclosure | magic token in URL | medium | mitigate | Never echo raw token into UI fields; set Referrer-Policy no-referrer on `/verify` (and reset in 05-06); safe returnTo only |
| T-05-16 | Spoofing | returnTo after verify | medium | mitigate | Reuse Phase 4 `safeReturnTo` / sanitize rules (D-16 success → dashboard or returnTo) |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1 (05-06-T1): Confirm input-otp@1.5.0 legitimacy</name>
  <what-built>RESEARCH Package Legitimacy Audit flagged `input-otp` as seam SUS (too-new on latest publish) while CONTEXT/UI-SPEC lock the package. No install yet.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/input-otp and confirm version **1.5.0**, MIT license, repo `guilhermerodz/input-otp`, ~high weekly downloads.
    2. Confirm no unexpected install scripts beyond normal publish.
    3. Reply **approved** to allow `bun add input-otp@1.5.0` in apps/web, or describe concerns.
  </how-to-verify>
  <resume-signal>Type "approved" or describe package concerns</resume-signal>
</task>

<task type="auto">
  <name>Task 2 (05-06-T2): rpc-gen + InputOtp + /verify page</name>
  <reversibility rating="costly">Regenerated client permanently exposes email_verified and verify RPC names to the web app (D-13).</reversibility>
  <files>crates/octanest-api/src/bin/rpc_gen.rs, packages/api-client/src/index.ts, apps/web/package.json, apps/web/src/components/ui/input-otp.tsx, apps/web/src/routes/verify.tsx, apps/web/src/styles.css</files>
  <read_first>crates/octanest-api/src/bin/rpc_gen.rs, packages/api-client/src/index.ts, apps/web/src/routes/login.tsx, apps/web/src/components/auth-shell.tsx, apps/web/src/components/ui/input.tsx, .planning/phases/05-cloud-verify-reset/05-UI-SPEC.md</read_first>
  <action>
After legitimacy approval (D-17):
1. Run `cargo run -p octanest-api --bin rpc-gen` so `UserPublic.email_verified` and auth verify/resend/request_verify/reset procedures appear in `packages/api-client` — do not hand-edit the generated index.
2. `bun add input-otp@1.5.0` in apps/web. Create thin `components/ui/input-otp.tsx` wrapping `OTPInput` with Octane/Tailwind slots: maxLength 8, inputMode numeric, autoComplete one-time-code, onComplete auto-submit; also keep explicit primary button. Do not install shadcn third-party OTP registry blocks.
3. Route `/verify` (`Verify email · Octanest`) per UI-SPEC: AuthShell title/support copy exact; token query auto-consume when signed-in as target; anonymous → Sign in with returnTo preserving token; wrong user error copy; InputOtp + Verify email; Resend email with cooldown helper; already-verified panel; success → safeReturnTo/dashboard; never echo raw token; reuse `oct-auth-enter`; Referrer-Policy no-referrer.
4. Exact copy strings from 05-UI-SPEC Copywriting Contract.
  </action>
  <verify>
    <automated>cargo run -p octanest-api --bin rpc-gen && bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or build stderr contains "error TS"; or generated client missing email_verified</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -q 'email_verified' packages/api-client/src/index.ts`
    - `grep -q 'input-otp' apps/web/package.json`
    - `test -f apps/web/src/routes/verify.tsx`
    - `test -f apps/web/src/components/ui/input-otp.tsx`
    - `bun --cwd apps/web run build` exits 0
  </acceptance_criteria>
  <done>/verify builds with InputOtp and regenerated client exposing email_verified</done>
</task>

<task type="auto">
  <name>Task 3 (05-06-T3): Persistent VerifyBanner under header</name>
  <files>apps/web/src/components/verify-banner.tsx, apps/web/src/routes/__root.tsx, apps/web/src/styles.css</files>
  <read_first>apps/web/src/routes/__root.tsx, apps/web/src/components/chrome.tsx, apps/web/src/routes/dashboard.tsx, .planning/phases/05-cloud-verify-reset/05-UI-SPEC.md</read_first>
  <action>
Per D-11 and D-13: create `VerifyBanner` — full-width strip under SiteHeader, `border-b border-border` + `bg-card`, role=status, Body/Label message exact UI-SPEC, Resend email + Enter code → /verify, pending Working…, rate-limit helper, soft opacity 150–200ms appear with prefers-reduced-motion instant. Mount between SiteHeader and main in `__root.tsx`. Visible only when signed-in and `auth.me.email_verified === false`. Do not steal focus on navigation.
  </action>
  <verify>
    <automated>bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or stderr contains "error TS"</fails_when>
  </verify>
  <human-check>
    Sign in as unverified local user: banner under header with Resend + Enter code; after verify, banner hidden. Light and dark.
  </human-check>
  <acceptance_criteria>
    - `test -f apps/web/src/components/verify-banner.tsx`
    - `grep -q 'VerifyBanner\|verify-banner' apps/web/src/routes/__root.tsx`
    - `bun --cwd apps/web run build` exits 0
  </acceptance_criteria>
  <done>Verify banner mounts for unverified sessions and builds clean</done>
</task>

</tasks>

## Artifacts this phase produces

Typed api-client with `email_verified`, `/verify` route, InputOtp wrapper, VerifyBanner chrome.

<verification>
rpc-gen + `bun --cwd apps/web run build`; human-check banner at end-of-phase UAT.
</verification>

<success_criteria>
Users can verify via /verify OTP/magic UX; banner prompts unverified sessions.
</success_criteria>

<output>
Create `.planning/phases/05-cloud-verify-reset/05-06-SUMMARY.md` when done
</output>
