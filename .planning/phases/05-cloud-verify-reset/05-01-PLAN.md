---
phase: 05-cloud-verify-reset
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/octanest-db/migrations/postgres/0003_email_tokens.sql
  - crates/octanest-db/migrations/mysql/0003_email_tokens.sql
  - crates/octanest-db/migrations/sqlite/0003_email_tokens.sql
  - crates/octanest-db/src/email_tokens.rs
  - crates/octanest-db/src/users.rs
  - crates/octanest-db/src/lib.rs
  - crates/octanest-db/tests/dialect_auth.rs
  - crates/octanest-core/src/auth_types.rs
requirements:
  - AUTH-04
autonomous: true
user_setup: []
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All three dialects migrate auth_email_tokens with purpose, token_hash, otp_hash, and UNIQUE(user_id, purpose)"
    - "octanest-db exposes email token CRUD and set/clear email_verified_at helpers"
    - "UserPublic includes email_verified boolean field in octanest-core (D-13 contract shape)"
  artifacts:
    - path: crates/octanest-db/migrations/sqlite/0003_email_tokens.sql
      provides: "auth_email_tokens schema (sqlite)"
      contains: "CREATE TABLE"
    - path: crates/octanest-db/src/email_tokens.rs
      provides: "token CRUD via DbPool match"
    - path: crates/octanest-core/src/auth_types.rs
      provides: "UserPublic.email_verified"
  key_links:
    - from: crates/octanest-db/src/lib.rs
      to: crates/octanest-db/src/email_tokens.rs
      via: "Database email_token facades"
    - from: crates/octanest-db/migrations/postgres/0003_email_tokens.sql
      to: crates/octanest-db/migrations/mysql/0003_email_tokens.sql
      via: "identical filename for migration_parity"
---

<objective>
Lay the Phase 5 persistence foundation: dialect-parity `0003_email_tokens`, token CRUD, verified-at helpers, and `UserPublic.email_verified` DTO field — before the tracer wires gate/RPC.

Purpose: Keep schema/CRUD within context budget so 05-02 can prove AUTH-04 end-to-end without ≥15 files in one plan.
Output: Migrating token schema, DB helpers, and core DTO field ready for tracer wiring.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cloud-verify-reset/05-CONTEXT.md
@.planning/phases/05-cloud-verify-reset/05-RESEARCH.md
@.planning/phases/05-cloud-verify-reset/05-PATTERNS.md
@crates/octanest-db/migrations/sqlite/0002_auth.sql
@crates/octanest-db/src/sessions.rs
@crates/octanest-core/src/auth_types.rs

<interfaces>
<!-- 05-02 tracer consumes these. -->

UserPublic gains:
```
email_verified: bool  // wired in user_to_public in 05-02 — field exists after this plan (D-13)
```

auth_email_tokens (logical):
```
id, user_id FK, purpose ('verify'|'reset'), token_hash CHAR(64), otp_hash CHAR(64),
expires_at, attempt_count DEFAULT 0, created_at, UNIQUE(user_id, purpose)
```

DB helpers:
```
upsert_by_user_purpose / find_by_token_hash / find_by_otp_hash / increment_attempts / delete
set_email_verified_at / clear_email_verified_at
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → DB token hashes | Secrets cross into persistence; plaintext must not be stored |
| Migration SQL → live schema | UNIQUE(user_id, purpose) enforces replace-on-resend model |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Information disclosure | `auth_email_tokens` | high | mitigate | Columns are hash-only (token_hash, otp_hash CHAR/TEXT 64); never store plaintext OTP/magic in DB |
| T-05-SC | Tampering | cargo/npm installs | high | mitigate | No new packages in this plan |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (05-01-T1): 0003_email_tokens + CRUD + UserPublic.email_verified</name>
  <files>crates/octanest-db/migrations/postgres/0003_email_tokens.sql, crates/octanest-db/migrations/mysql/0003_email_tokens.sql, crates/octanest-db/migrations/sqlite/0003_email_tokens.sql, crates/octanest-db/src/email_tokens.rs, crates/octanest-db/src/users.rs, crates/octanest-db/src/lib.rs, crates/octanest-core/src/auth_types.rs</files>
  <read_first>crates/octanest-db/migrations/sqlite/0002_auth.sql, crates/octanest-db/src/sessions.rs, crates/octanest-db/src/users.rs, crates/octanest-core/src/auth_types.rs, .planning/phases/05-cloud-verify-reset/05-RESEARCH.md, .planning/phases/05-cloud-verify-reset/05-PATTERNS.md</read_first>
  <action>
Per RESEARCH schema + PATTERNS sessions analog (supports D-13 field, D-18/D-20 storage shape):
1. Ship dialect-parity `0003_email_tokens.sql` (postgres/mysql/sqlite) for `auth_email_tokens` with purpose, token_hash, otp_hash, expires_at, attempt_count, UNIQUE(user_id, purpose), index on token_hash — match 0002 timestamp styles per dialect.
2. Add `email_tokens` CRUD (`upsert_by_user_purpose`, find by token_hash / otp_hash, increment attempts, delete) and `users::set_email_verified_at` / `clear_email_verified_at`; expose via `Database` facades (no raw SQL in API).
3. Add `UserPublic.email_verified: bool` in octanest-core (D-13). Do not wire `user_to_public` or RPCs here — that is 05-02 tracer.
  </action>
  <verify>
    <automated>cargo test -p octanest-core --lib && cargo test -p octanest-db --lib migration_parity</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"; or migration_parity reports missing 0003 file</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -q 'email_verified' crates/octanest-core/src/auth_types.rs`
    - `test -f crates/octanest-db/migrations/postgres/0003_email_tokens.sql`
    - `test -f crates/octanest-db/migrations/mysql/0003_email_tokens.sql`
    - `test -f crates/octanest-db/migrations/sqlite/0003_email_tokens.sql`
    - `grep -q 'upsert_by_user_purpose\|fn upsert' crates/octanest-db/src/email_tokens.rs`
    - `grep -q 'set_email_verified_at' crates/octanest-db/src/users.rs`
    - `cargo test -p octanest-db --lib migration_parity` exits 0
  </acceptance_criteria>
  <done>Token migrations, CRUD helpers, and UserPublic.email_verified field exist</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (05-01-T2): Dialect token CRUD + verified helpers</name>
  <files>crates/octanest-db/tests/dialect_auth.rs, crates/octanest-db/src/email_tokens.rs, crates/octanest-db/src/users.rs</files>
  <read_first>crates/octanest-db/tests/dialect_auth.rs, crates/octanest-db/src/lib.rs, .planning/phases/05-cloud-verify-reset/05-PATTERNS.md</read_first>
  <behavior>
    - After migrate, upsert auth_email_tokens for purpose verify and find by token_hash / otp_hash
    - set_email_verified_at then clear_email_verified_at round-trip on UserRow
    - migration_parity still greps matching 0003 filenames across dialects
  </behavior>
  <action>
Extend `dialect_auth` so each dialect with DATABASE_URL: migrate → create user → upsert email token → find by hashes → set/clear `email_verified_at`. Confirm `cargo test -p octanest-db --lib migration_parity` still passes for `0003_email_tokens.sql` filename parity.
  </action>
  <verify>
    <automated>cargo test -p octanest-db --lib migration_parity && cargo test -p octanest-db --test dialect_auth</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"; or migration_parity reports missing 0003 file</fails_when>
  </verify>
  <acceptance_criteria>
    - `cargo test -p octanest-db --lib migration_parity` exits 0
    - `cargo test -p octanest-db --test dialect_auth` exits 0
    - dialect_auth covers email token upsert/find and verified flag helpers
  </acceptance_criteria>
  <done>Token schema and verified helpers proven on dialect_auth + migration_parity</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `0003_email_tokens` + DB helpers + `UserPublic.email_verified` field | 05-01 | Persistence foundation |
| `require_verified` + verify OTP tracer | 05-02 | AUTH-04 gate proof |
| Verify/reset expansion + IdP + UI | 05-03…05-07 | Channel, AUTH-12, UX |

<verification>
`cargo test -p octanest-db --lib migration_parity && cargo test -p octanest-db --test dialect_auth` green; core lib compiles with email_verified.
</verification>

<success_criteria>
Dialects migrate tokens; CRUD + verified helpers work; DTO field present for tracer.
</success_criteria>

<output>
Create `.planning/phases/05-cloud-verify-reset/05-01-SUMMARY.md` when done
</output>
