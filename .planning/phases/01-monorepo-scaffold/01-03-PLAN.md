---
phase: 01-monorepo-scaffold
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - apps/web/
  - brand/octanest-mark.png
requirements:
  - PLAT-04
  - PLAT-10
  - PLAT-11
autonomous: false
user_setup: []
must_haves:
  truths:
    - "Operator can open marketing landing at / with Octanest brand and dual CTAs"
    - "Public /status shows live system.health via api-client + TanStack Query"
    - "Theme defaults to system and can force light/dark with persistence"
    - "UI uses shadcn/Base UI + Tailwind v4 CSS-based config"
  artifacts:
    - path: apps/web
      provides: "Octane TanStack Start app"
    - path: apps/web/src/routes
      provides: "/ and /status routes"
  key_links:
    - from: apps/web
      to: packages/api-client
      via: "workspace import + QueryClient"
---

<objective>
Build `apps/web` on `@octanejs/tanstack-start` with shadcn+Base UI+Tailwind v4, theme system, marketing landing, and public `/status` per UI-SPEC and CONTEXT D-22–D-26.

Purpose: PLAT-04/10/11 + credible scaffold UX.
Output: Runnable web app (Vite dev) matching UI-SPEC copy and tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/01-monorepo-scaffold/01-UI-SPEC.md
@.planning/phases/01-monorepo-scaffold/01-CONTEXT.md
@.planning/phases/01-monorepo-scaffold/01-RESEARCH.md
@.planning/phases/01-monorepo-scaffold/01-01-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API client | XSS in landing copy/status rendering |
| localStorage/cookie theme pref | Tampering only affects UX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | XSS | status/error rendering | mitigate | Render API strings as text (React default escaping); do not use dangerouslySetInnerHTML for health payloads |
| T-01-08 | Spoofing | Auth placeholders | accept | Non-functional Sign in/Sign up; no fake session cookies |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Octane Start app + shadcn/Base UI + Tailwind v4 + theme</name>
  <files>apps/web/**</files>
  <read_first>.planning/phases/01-monorepo-scaffold/01-UI-SPEC.md, brand/README.md, apps/web/package.json</read_first>
  <action>
1. Scaffold `apps/web` using `@octanejs/tanstack-start` Vite plugin per RESEARCH (`tanstackStart()` from `@octanejs/tanstack-start/plugin/vite`). Depend on `@octanejs/tanstack-start`, `@octanejs/tanstack-router`, React 19, `@tanstack/react-query`, `@octanest/api-client`.
2. Initialize shadcn with **Base UI** path (UI-SPEC); add components: button, input, link (and primitives they need). Record preset string in `apps/web/components.json`.
3. Tailwind CSS v4 with CSS-based config (`@import "tailwindcss"` in `apps/web/src/styles.css` or equivalent) — PLAT-11; map UI-SPEC color tokens as CSS variables for light and dark.
4. Fonts: Sora + Source Sans 3 via `@fontsource-variable/sora` and `@fontsource-variable/source-sans-3`.
5. Theme: default **system**; user can force light/dark; persist preference (localStorage key `octanest-theme`); apply class/`data-theme` on `html`.
6. Vite proxy for `make dev`: `/api/rpc` → `http://127.0.0.1:8080`, `/api/rpc/ws` with `ws: true` (D-10).
7. Copy or reference `brand/octanest-mark.png` into web public assets for header/favicon.
Do not implement real auth/search.
  </action>
  <verify>cd apps/web &amp;&amp; bun run build</verify>
  <acceptance_criteria>
    - apps/web/package.json contains `@octanejs/tanstack-start`
    - apps/web contains `components.json` or shadcn config
    - apps/web CSS contains `@import "tailwindcss"` or `@import 'tailwindcss'`
    - apps/web contains `octanest-theme` or theme persistence key
    - apps/web vite config contains `/api/rpc`
  </acceptance_criteria>
  <done>Web app builds with design system wired</done>
</task>

<task type="auto">
  <name>Task 2: Landing `/` and `/status` routes per UI-SPEC</name>
  <files>apps/web/src/**</files>
  <read_first>.planning/phases/01-monorepo-scaffold/01-UI-SPEC.md</read_first>
  <action>
Implement routes exactly per UI-SPEC copywriting:
1. Header: mark + wordmark “Octanest”; Search input placeholder `Search public code (soon)` disabled; Auth buttons `Sign in` / `Sign up` disabled with hint Coming soon.
2. `/` first viewport: headline `Where repositories nest — cloud or yours`; supporting sentence from UI-SPEC; CTAs **Get started** (primary) and **Explore Octanest** (secondary); atmospheric blue↔orange background; no stats/dashboard cards in hero.
3. Footer includes link text `Status` → `/status` (D-26).
4. `/status`: title `System status`; use TanStack Query + api-client helpers to call `system.health` over HTTP only; show loading/healthy/unhealthy/unreachable copy from UI-SPEC; no echo UI (D-23/D-24).
5. Motion: subtle hero stagger + status opacity; respect `prefers-reduced-motion`.
6. Wire QueryClientProvider at app root.
  </action>
  <verify>bunx turbo run build --filter=@octanest/web</verify>
  <acceptance_criteria>
    - apps/web source contains `Where repositories nest`
    - apps/web source contains `Get started`
    - apps/web source contains `Explore Octanest`
    - apps/web source contains `System status`
    - apps/web source contains `Search public code (soon)`
    - apps/web source contains route path `/status` or `status`
  </acceptance_criteria>
  <done>Landing and status routes match UI-SPEC</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Octane web app with landing and /status (API may need plan 02 running for live health)</what-built>
  <how-to-verify>
    1. Run web dev server; open `/` — confirm brand mark, headline, both CTAs, placeholder search/auth.
    2. Confirm footer Status link.
    3. Toggle theme system/light/dark and refresh — preference persists.
    4. If API up, `/status` shows live health; if not, unreachable copy appears.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] `apps/web` production build passes
- [ ] UI-SPEC strings present in source
- [ ] Human checkpoint approved
</verification>

<success_criteria>
- PLAT-04, PLAT-10, PLAT-11 met for Phase 1 UI slice
- D-22–D-26 honored
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-scaffold/01-03-SUMMARY.md`
</output>
