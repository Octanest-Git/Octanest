---
phase: 01-monorepo-scaffold
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - turbo.json
  - Makefile
  - .gitignore
  - .env.example
  - README.md
  - Cargo.toml
  - crates/octanest-api/Cargo.toml
  - crates/octanest-core/Cargo.toml
  - crates/octanest-db/Cargo.toml
  - apps/web/package.json
  - packages/api-client/package.json
  - scripts/check-rpc-sync.sh
requirements:
  - PLAT-01
  - PLAT-04
  - PLAT-05
  - PLAT-06
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Repo root has Bun/Turborepo JS workspace and Cargo workspace recognized by tooling"
    - "Makefile exposes make help listing dev, rpc-gen, and compose targets (stubs OK if bodies land later)"
    - "packages/api-client exists as a workspace package ready for codegen output"
  artifacts:
    - path: Cargo.toml
      provides: "workspace members octanest-api, octanest-core, octanest-db"
    - path: package.json
      provides: "workspaces apps/* packages/* and packageManager bun"
    - path: Makefile
      provides: "dev rpc-gen up targets"
  key_links:
    - from: packages/api-client
      to: apps/web
      via: "workspace dependency declaration"
---

<objective>
Create the greenfield monorepo skeleton: Bun+Turborepo JS workspace, Cargo workspace crates, Makefile entrypoints, env example, and api-client package stub.

Purpose: Establish D-01–D-05 layout so API, web, and Compose plans have a stable home.
Output: Root tooling files + empty/stub crates and packages that build/typecheck at a minimal level.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-scaffold/01-CONTEXT.md
@.planning/phases/01-monorepo-scaffold/01-RESEARCH.md
@.planning/phases/01-monorepo-scaffold/01-VALIDATION.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer workstation → git repo | Scaffold commits must not include secrets |
| `.env.example` → runtime `.env` | Example values only; no real credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information disclosure | `.env.example` | mitigate | Document placeholders only; add `.env` to `.gitignore`; never commit real `OCTANEST_*` secrets |
| T-01-02 | Tampering | lockfiles | accept | Phase 1 accepts normal lockfile churn; CI later pins installs |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Root JS + Cargo workspace skeleton</name>
  <files>package.json, turbo.json, Cargo.toml, crates/octanest-api/Cargo.toml, crates/octanest-core/Cargo.toml, crates/octanest-db/Cargo.toml, apps/web/package.json, packages/api-client/package.json, .gitignore, README.md</files>
  <read_first>.planning/phases/01-monorepo-scaffold/01-CONTEXT.md, .planning/phases/01-monorepo-scaffold/01-RESEARCH.md</read_first>
  <action>
Create monorepo per D-01–D-05:
1. Root `package.json` with `"private": true`, `"workspaces": ["apps/*", "packages/*"]`, `"packageManager": "bun@1.2.5"` (or current Corepack Bun version available — pin exact), scripts delegating to turbo (`dev`, `build`, `test`, `lint`).
2. `turbo.json` with pipeline tasks `build`, `dev`, `test` (dev persistent/cache false).
3. Root `Cargo.toml` workspace members: `crates/octanest-api`, `crates/octanest-core`, `crates/octanest-db`. Editions 2021+, shared workspace deps for serde/tokio as needed.
4. Stub crates: `octanest-core` lib with `pub fn crate_name() -> &'static str { "octanest-core" }`; `octanest-db` lib with a `Database` trait or `connect_stub()` returning Ok(()); `octanest-api` bin `main` that prints boot message (full server in plan 02).
5. `apps/web/package.json` name `@octanest/web` private; depend on `@octanest/api-client` workspace:`*`; placeholder until plan 03.
6. `packages/api-client/package.json` name `@octanest/api-client`, `"type": "module"`, export `./src/index.ts` stub exporting `export const RPC_VERSION = 1 as const`.
7. `.gitignore`: `node_modules`, `target`, `.env`, `dist`, `.turbo`, bun/pnpm lock conflicts handled — commit `bun.lock` when using Bun.
8. Root `README.md` with product name **Octanest**, link to Compose later, and Corepack Bun note + pnpm fallback (D-04/D-05).
Do NOT implement RPC, UI routes, or Compose in this plan.
  </action>
  <verify>test -f Cargo.toml &amp;&amp; test -f package.json &amp;&amp; cargo metadata --no-deps -q &amp;&amp; bun install</verify>
  <acceptance_criteria>
    - Cargo.toml contains `octanest-api`
    - Cargo.toml contains `octanest-core`
    - Cargo.toml contains `octanest-db`
    - package.json contains `"workspaces"`
    - package.json contains `bun@`
    - packages/api-client/package.json contains `"name": "@octanest/api-client"`
    - .gitignore contains `.env`
    - README.md contains `Octanest`
  </acceptance_criteria>
  <done>Workspace installs and `cargo metadata` succeed</done>
</task>

<task type="auto">
  <name>Task 2: Makefile + env example + rpc sync script stub</name>
  <files>Makefile, .env.example, scripts/check-rpc-sync.sh</files>
  <read_first>package.json, Cargo.toml</read_first>
  <action>
1. `Makefile` with `.PHONY` targets at minimum: `help`, `dev`, `rpc-gen`, `rpc-sync-check`, `up`, `down`, `test`. Bodies may call echo/TODO for `dev`/`up` if later plans fill them, but `help` must print target names; `rpc-sync-check` must invoke `scripts/check-rpc-sync.sh`.
2. `.env.example` with keys: `DATABASE_URL=postgres://octanest:octanest@localhost:5432/octanest`, `OCTANEST_CORS_ORIGINS=http://localhost:3000`, `OCTANEST_ENV=development`, `API_BIND=0.0.0.0:8080`, `OCTANEST_RPC_VERSION=1`.
3. `scripts/check-rpc-sync.sh`: `set -euo pipefail`; run `make rpc-gen` (or cargo run -p octanest-api --bin rpc-gen if that binary exists later — for now stub with exit 0 after checking packages/api-client exists); eventually must `git diff --exit-code -- packages/api-client`. For this plan: script exists, executable bit set, contains `packages/api-client`.
Per D-03, D-15, D-20.
  </action>
  <verify>make help &amp;&amp; test -x scripts/check-rpc-sync.sh &amp;&amp; grep -q OCTANEST_CORS_ORIGINS .env.example</verify>
  <acceptance_criteria>
    - Makefile contains `rpc-gen`
    - Makefile contains `dev`
    - Makefile contains `up`
    - .env.example contains `OCTANEST_CORS_ORIGINS=`
    - .env.example contains `DATABASE_URL=`
    - scripts/check-rpc-sync.sh contains `packages/api-client`
  </acceptance_criteria>
  <done>Operator entrypoints and env template exist</done>
</task>

</tasks>

<verification>
- [ ] `bun install` succeeds
- [ ] `cargo metadata -q` succeeds
- [ ] `make help` lists targets
</verification>

<success_criteria>
- D-01–D-05 skeleton on disk
- No application secrets committed
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-scaffold/01-01-SUMMARY.md`
</output>
