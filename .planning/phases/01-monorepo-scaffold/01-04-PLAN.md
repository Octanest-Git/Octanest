---
phase: 01-monorepo-scaffold
plan: "04"
type: execute
wave: 4
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - docker-compose.yml
  - docker-compose.mysql.yml
  - deploy/
  - apps/web/Dockerfile
  - crates/octanest-api/Dockerfile
  - Makefile
  - .env.example
requirements:
  - PLAT-01
autonomous: true
user_setup: []
must_haves:
  truths:
    - "docker compose up brings Traefik, web, api, postgres to healthy"
    - "Operator reaches web UI and API through Traefik ingress"
    - "MySQL available via Compose profile; SQLite documented via env"
  artifacts:
    - path: docker-compose.yml
      provides: "web api postgres traefik services"
  key_links:
    - from: traefik
      to: api
      via: "/api/* router"
    - from: traefik
      to: web
      via: "/* router"
---

<objective>
Ship Docker Compose stack with Traefik reverse proxy, web+api+postgres default, MySQL profile, healthchecks, and Makefile up/down — proving PLAT-01.

Purpose: Operator-local full stack per D-06–D-10.
Output: Compose files, Dockerfiles, documented SQLite/MySQL paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-monorepo-scaffold/01-CONTEXT.md
@.planning/phases/01-monorepo-scaffold/01-RESEARCH.md
@.planning/phases/01-monorepo-scaffold/01-02-SUMMARY.md
@.planning/phases/01-monorepo-scaffold/01-03-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet/host → Traefik → web/api | All ingress |
| Compose env files | Secrets for DB password |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Information disclosure | compose DB password | mitigate | Use non-default only via env; `.env` gitignored; `.env.example` uses local-only password `octanest` |
| T-01-10 | Spoofing | CORS in Compose | mitigate | Set `OCTANEST_ENV=production` (or `compose`) and require `OCTANEST_CORS_ORIGINS` to Traefik public origin |
| T-01-11 | Denial of service | exposed ports | accept | Local-dev binding; document not to expose to WAN without auth (auth later phases) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Dockerfiles + Compose + Traefik routing</name>
  <files>docker-compose.yml, apps/web/Dockerfile, crates/octanest-api/Dockerfile, deploy/traefik/**, Makefile, .env.example</files>
  <read_first>.planning/phases/01-monorepo-scaffold/01-CONTEXT.md, Makefile</read_first>
  <action>
1. `docker-compose.yml` services: `traefik` (pin `traefik:v3.3` or current stable v3), `web`, `api`, `postgres:16-alpine`.
2. Traefik routes host `localhost` (or `octanest.localhost`): web for `/`, api for `/api/` including WebSocket upgrade for `/api/rpc/ws`.
3. API env: `DATABASE_URL`, `OCTANEST_ENV`, `OCTANEST_CORS_ORIGINS`, `API_BIND=0.0.0.0:8080`.
4. HEALTHCHECK on api using `curl -f http://localhost:8080/healthz` (or wget).
5. Postgres volume + credentials matching `.env.example`.
6. Dockerfiles: multi-stage Rust api release build; web Bun/Vite build + static/SSR runner as required by Octane Start server output.
7. Makefile `up`/`down`/`logs` wrapping compose.
8. Document SQLite: set `DATABASE_URL=sqlite:./data/octanest.db` (or sqlx form) without DB service — README section.
9. MySQL: Compose profile `mysql` adding mysql service and override DATABASE_URL (D-07) — file `docker-compose.mysql.yml` or profiles in main file.
  </action>
  <verify>docker compose -f docker-compose.yml config</verify>
  <acceptance_criteria>
    - docker-compose.yml contains `traefik`
    - docker-compose.yml contains `postgres`
    - docker-compose.yml contains service name `api` or equivalent
    - docker-compose.yml contains `web`
    - API Dockerfile or compose healthcheck references `healthz` or HEALTHCHECK
    - Makefile contains `up`
  </acceptance_criteria>
  <done>Compose config validates</done>
</task>

<task type="auto">
  <name>Task 2: Compose bring-up smoke</name>
  <files>scripts/compose-smoke.sh, Makefile</files>
  <read_first>docker-compose.yml</read_first>
  <action>
1. Add `scripts/compose-smoke.sh`: `docker compose up --build -d --wait` (or wait-for-healthy), curl Traefik URL for `/` (expect 200) and `/api` healthz or RPC health with header `Octanest-RPC-Version: 1`, then `docker compose down`.
2. Wire `make smoke` to that script.
3. Run smoke once in execution environment if Docker available; if Docker unavailable, mark SUMMARY with blocker note but leave script green-path ready.
  </action>
  <verify>docker compose config &amp;&amp; test -x scripts/compose-smoke.sh</verify>
  <acceptance_criteria>
    - scripts/compose-smoke.sh contains `docker compose`
    - scripts/compose-smoke.sh contains `Octanest-RPC-Version` or `healthz`
    - Makefile contains `smoke`
  </acceptance_criteria>
  <done>PLAT-01 smoke path automated</done>
</task>

</tasks>

<verification>
- [ ] `docker compose config` succeeds
- [ ] Smoke script exists and is executable
</verification>

<success_criteria>
- PLAT-01 operator path documented and scripted
- D-06–D-10 reflected in Compose
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-scaffold/01-04-SUMMARY.md`
</output>
