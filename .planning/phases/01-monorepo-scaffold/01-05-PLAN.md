---
phase: 01-monorepo-scaffold
plan: "05"
type: execute
wave: 4
depends_on:
  - "01-04"
files_modified:
  - .github/workflows/
  - Makefile
  - .planning/phases/01-monorepo-scaffold/01-VALIDATION.md
requirements:
  - PLAT-01
  - PLAT-04
  - PLAT-05
  - PLAT-06
  - PLAT-10
  - PLAT-11
autonomous: true
user_setup: []
must_haves:
  truths:
    - "CI runs cargo tests, vitest, rpc sync check, and compose config validation on PR"
    - "VALIDATION.md task map updated to match implemented tests"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "PR checks for Phase 1 quality gates"
  key_links:
    - from: .github/workflows/ci.yml
      to: scripts/check-rpc-sync.sh
      via: "rpc sync job step"
---

<objective>
Add CI workflow covering Rust tests, JS tests, codegen drift, and Compose config validation; refresh VALIDATION.md sign-off fields.

Purpose: Prevent regressions of PLAT-* Phase 1 gates.
Output: GitHub Actions workflow (or compatible) + updated validation doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-monorepo-scaffold/01-VALIDATION.md
@.planning/phases/01-monorepo-scaffold/01-RESEARCH.md
@.planning/phases/01-monorepo-scaffold/01-04-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR fork → CI secrets | Untrusted code in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Elevation | CI secrets | mitigate | Do not expose deploy secrets to fork PRs; Phase 1 CI uses no production secrets |
| T-01-13 | Tampering | generated client | mitigate | Mandatory `make rpc-sync-check` (or script) in CI |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml, Makefile</files>
  <read_first>scripts/check-rpc-sync.sh, package.json, Cargo.toml</read_first>
  <action>
Create `.github/workflows/ci.yml` on pull_request + push to main:
1. Job `rust`: install stable Rust; `cargo test --workspace`.
2. Job `js`: Corepack enable; Bun install; `bunx vitest run`; `bunx turbo run build --filter=@octanest/web`.
3. Job `rpc-sync`: install both toolchains as needed; `make rpc-sync-check`.
4. Job `compose`: `docker compose config` (and optional build if runners allow — at minimum config).
5. No `--watch` flags. Cache cargo and bun where practical.
6. Document pnpm fallback note in workflow comments if Bun unavailable on runner — prefer Bun via official setup action.
  </action>
  <verify>test -f .github/workflows/ci.yml &amp;&amp; grep -q rpc-sync .github/workflows/ci.yml</verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml contains `cargo test`
    - .github/workflows/ci.yml contains `vitest`
    - .github/workflows/ci.yml contains `rpc-sync` or `check-rpc-sync`
    - .github/workflows/ci.yml contains `docker compose config` or `compose config`
  </acceptance_criteria>
  <done>CI gates encoded</done>
</task>

<task type="auto">
  <name>Task 2: Update VALIDATION.md to nyquist_compliant</name>
  <files>.planning/phases/01-monorepo-scaffold/01-VALIDATION.md</files>
  <read_first>.planning/phases/01-monorepo-scaffold/01-VALIDATION.md</read_first>
  <action>
Update the Per-Task Verification Map with real commands/paths from implemented tests; check off Wave 0 items that exist; set frontmatter `nyquist_compliant: true` and Approval date when all automated mappings exist; leave manual-only table as-is.
  </action>
  <verify>grep -q 'nyquist_compliant: true' .planning/phases/01-monorepo-scaffold/01-VALIDATION.md</verify>
  <acceptance_criteria>
    - 01-VALIDATION.md contains `nyquist_compliant: true`
    - 01-VALIDATION.md Wave 0 checkboxes or map references `rpc_http` or `rpc_ws` or equivalent real paths
  </acceptance_criteria>
  <done>Validation strategy reflects reality</done>
</task>

</tasks>

<verification>
- [ ] CI workflow file present with four gate categories
- [ ] VALIDATION.md marked nyquist_compliant
</verification>

<success_criteria>
- All Phase 1 requirement IDs covered by CI and/or prior plan tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-scaffold/01-05-SUMMARY.md`
</output>
