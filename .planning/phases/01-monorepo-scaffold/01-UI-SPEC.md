---
phase: 1
slug: monorepo-scaffold
status: draft
shadcn_initialized: false
preset: pending-phase-1-init
created: 2026-09-09
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (initialize during Phase 1 scaffold) |
| Preset | TBD at init — prefer Base UI + CSS variables + Tailwind v4; record preset string in repo after `shadcn init` |
| Component library | Base UI (via shadcn Base UI path) |
| Icon library | Lucide (shadcn default) |
| Font | Display/headings: **Sora**; Body/UI: **Source Sans 3** |

**Theme behavior (locked for Phase 1):**
- Default = **system** (`prefers-color-scheme`)
- User can force **light** or **dark**; preference persists (localStorage / cookie — executor choice)
- Both themes ship complete token sets in Phase 1 (not dark-only)

**Sources:** CONTEXT D-23, PROJECT theme requirements (BRAND theme → Phase 3 polish; tokens land now), user UI-phase answers 2026-09-09

---

## Screens in scope

| Route | Purpose | Focal point (eye first) |
|-------|---------|-------------------------|
| `/` | Marketing landing | Brand wordmark/mark in header + hero brand name, then primary CTA group |
| `/status` | Public live health | Current health status indicator (ok / degraded / unreachable) |

**Out of scope UI:** status history, echo UI, real search results, real auth flows (placeholders only).

---

## Layout contracts

### Global chrome
- **Header:** mark (`brand/octanest-mark.png`) + wordmark “Octanest”; public **Search** control (disabled/placeholder); **Auth** placeholders (e.g. “Sign in” / “Sign up” non-functional or `disabled` with title “Coming soon”)
- **Footer:** includes **Status** link → `/status` (CONTEXT D-26)
- No cards in the hero. Cards allowed only if needed for interactive status details on `/status` (health payload readability)

### `/` Landing (first viewport)
- One composition: brand-forward hero (not a dashboard)
- Contents: brand (mark + name), one headline, one short supporting sentence, CTA group (**Get started** + **Explore Octanest**), full-bleed atmospheric background (gradient/mesh derived from blue↔orange brand — not flat single color)
- Do **not** put stats, schedules, address blocks, or secondary marketing grids in the first viewport
- Search/Auth remain chrome-only, not hero overlays/badges

### `/status`
- Page title + live `system.health` via TanStack Query helpers
- States: loading, healthy, unhealthy/error, unreachable
- HTTP only in UI; WS covered by tests
- No echo controls

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: touch targets for Search/Auth/CTA ≥ **44px** height (may use 44px — not on spacing scale; justify a11y)

---

## Typography

| Role | Size | Weight | Line Height | Font |
|------|------|--------|-------------|------|
| Label | 14px | 400 | 1.4 | Source Sans 3 |
| Body | 16px | 400 | 1.5 | Source Sans 3 |
| Heading | 24px | 600 | 1.2 | Sora |
| Display | 40px | 600 | 1.15 | Sora |

Weights used project-wide: **400** and **600** only.

---

## Color

### Light theme

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#F4F6F8` | Page background, large surfaces |
| Secondary (30%) | `#FFFFFF` | Header/footer bars, status panel surface |
| Accent cool (brand) | `#1B6FE8` | Primary CTA fill, focus ring, healthy status |
| Accent warm (brand) | `#F07818` | Secondary CTA outline/text accent, mark pairing, exploratory emphasis |
| Text primary | `#12141A` | Body and headings |
| Text muted | `#5C6570` | Supporting copy, placeholders |
| Destructive | `#D92D20` | Reserved; unused in Phase 1 UI (no destructive actions) |
| Border | `#D8DEE6` | Hairlines, input placeholder borders |

### Dark theme

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0B0C0E` | Page background (mark-friendly black) |
| Secondary (30%) | `#16181D` | Header/footer, status panel |
| Accent cool (brand) | `#3B8CFF` | Primary CTA, focus ring, healthy status (lifted for contrast) |
| Accent warm (brand) | `#FF8A2B` | Secondary CTA / exploratory accent |
| Text primary | `#F2F4F7` | Body and headings |
| Text muted | `#9AA3B2` | Supporting copy, placeholders |
| Destructive | `#F97066` | Reserved; unused in Phase 1 UI |
| Border | `#2A2F3A` | Hairlines |

**60/30/10:** Dominant surfaces ~60%, secondary chrome/panels ~30%, brand accents together ~10%.

**Accent reserved for (only):**
1. Primary CTA (**Get started**) fill
2. Secondary CTA (**Explore Octanest**) accent treatment (outline or text+underline — not solid fill competing with primary)
3. Focus rings on interactive controls
4. Healthy status indicator on `/status`
5. Brand mark color pairing (asset itself; do not recolor ad hoc)

**Not accent:** default links in footer (use text primary/muted + underline), placeholder Search field chrome, disabled Auth controls.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Product name | Octanest (never bare “Octane”) |
| Landing headline | Where repositories nest — cloud or yours |
| Landing supporting | Social coding forge: host git, collaborate, and ship — one product for Octanest Cloud and self-host |
| Primary CTA | Get started |
| Secondary CTA | Explore Octanest |
| Header Search placeholder | Search public code (soon) |
| Header Auth — Sign in | Sign in |
| Header Auth — Sign up | Sign up |
| Auth placeholder hint | Coming soon |
| Footer Status link | Status |
| Status page title | System status |
| Status loading | Checking Octanest services… |
| Status healthy heading | All systems operational |
| Status healthy body | API health check succeeded. This page reflects live `system.health` — history arrives in a later release. |
| Status unhealthy heading | Degraded or failing |
| Status unhealthy body | The API reported an unhealthy state. Retry in a moment or check your Compose/API logs. |
| Status unreachable heading | Can’t reach the API |
| Status unreachable body | Octanest couldn’t complete a health check. Confirm the stack is up (`docker compose` / `make dev`) and try again. |
| Empty state (N/A) | No empty-list UI in Phase 1; status always shows a health outcome |
| Error (generic fetch) | Something blocked this request. Refresh the page or verify the API is running. |
| Destructive confirmation | None in Phase 1 |

---

## Motion (Phase 1 minimum)

- Hero: subtle fade/slide-in of brand + headline + CTAs (stagger ≤ 3 steps, 200–400ms, respect `prefers-reduced-motion`)
- Status: soft opacity transition when health query resolves
- No decorative perpetual loops or glow pulses on CTAs

---

## Component inventory (Phase 1)

| Component | Notes |
|-----------|-------|
| Button | Primary + secondary variants for CTA pair |
| Input | Search placeholder (readOnly/disabled OK) |
| Link | Footer Status, secondary nav text |
| Badge or StatusDot | Healthy / degraded / unreachable |
| Separator | Optional header/footer rules — prefer spacing over heavy chrome |

Initialize only what the landing + status need; avoid dumping the full shadcn catalog.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | button, input, link (and primitives required by those) | not required — official registry |
| third-party | none | n/a |

No third-party registries in Phase 1.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
