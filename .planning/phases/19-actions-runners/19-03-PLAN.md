---
phase: 19-actions-runners
plan: "03"
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - crates/octanest-api/Cargo.toml
  - crates/octanest-api/src/actions/mod.rs
  - crates/octanest-api/src/actions/workflow.rs
  - crates/octanest-api/src/actions/parse.rs
  - crates/octanest-api/tests/actions_workflow_parse.rs
  - crates/octanest-db/src/actions.rs
requirements:
  - ACT-01
autonomous: true
user_setup: []
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Forge can load and parse .github/workflows YAML from a commit tree into run/job candidates (ACT-01, D-ACT-01, D-ACT-02)"
    - "Invalid YAML yields a structured parse error suitable for a failed run log (D-ACT-02)"
  artifacts:
    - path: crates/octanest-api/src/actions/parse.rs
      provides: "Workflow YAML subset parser"
    - path: crates/octanest-api/src/actions/workflow.rs
      provides: "Tree discovery under .github/workflows"
  key_links:
    - from: crates/octanest-api/src/actions/workflow.rs
      to: crates/octanest-git
      via: "blob read at head SHA"
  assumptions:
    - "ASSUME: serde_yaml [VERIFIED] may be added per RESEARCH Package Legitimacy Audit"
  prohibitions:
    - statement: "Must not execute workflow steps inside the API process"
      flagged: true
---

<objective>
Implement GitHub Actions–compatible workflow discovery and YAML subset parsing from git trees (ACT-01 / D-ACT-01 / D-ACT-02), creating the domain types later plans enqueue.

Purpose: Deterministic parse layer before event dispatcher tracer.
Output: actions::{workflow,parse} modules; actions_workflow_parse tests green.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-actions-runners/19-CONTEXT.md
@.planning/phases/19-actions-runners/19-RESEARCH.md
@crates/octanest-git/src
@crates/octanest-api/Cargo.toml
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted workflow YAML from repo | Parse with size limits |
| Tree paths | Confine to .github/workflows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-06 | Denial of Service | YAML parse | medium | mitigate | Max workflow file size + bounded parse; reject path escape outside .github/workflows (D-ACT-01) |
| T-19-07 | Tampering | workflow path | medium | mitigate | Only *.yml/*.yaml under .github/workflows |
| T-19-SC | Tampering | serde_yaml install | high | mitigate | Package Legitimacy Audit [VERIFIED]; no other new deps |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (19-03-T1): Add serde_yaml dependency</name>
  <files>crates/octanest-api/Cargo.toml</files>
  <read_first>.planning/phases/19-actions-runners/19-RESEARCH.md, crates/octanest-api/Cargo.toml</read_first>
  <action>
    Add serde_yaml per RESEARCH Package Legitimacy Audit ([VERIFIED]). Do not add prost yet (plan 05). Pin a current 0.9.x compatible with workspace edition.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && rg -n 'serde_yaml' crates/octanest-api/Cargo.toml | rg -v '^#' | rg -q .</automated>
  </verify>
  <done>serde_yaml listed in octanest-api Cargo.toml.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (19-03-T2): Discover + parse workflow subset</name>
  <files>crates/octanest-api/src/actions/mod.rs, crates/octanest-api/src/actions/workflow.rs, crates/octanest-api/src/actions/parse.rs, crates/octanest-api/tests/actions_workflow_parse.rs, crates/octanest-db/src/actions.rs</files>
  <behavior>
    - Discovers .github/workflows/*.{yml,yaml} at a commit SHA (D-ACT-01)
    - Parses name, on.push, on.pull_request, jobs.*.runs-on, jobs.*.steps subset (D-ACT-02)
    - Invalid YAML returns error object usable as failed-run message
    - Does not spawn containers or run steps
  </behavior>
  <read_first>crates/octanest-api/tests/actions_workflow_parse.rs, .planning/phases/19-actions-runners/19-CONTEXT.md</read_first>
  <action>
    Implement workflow discovery via octanest-git tree/blob reads and YAML subset parser per D-ACT-01 / D-ACT-02 / ACT-01. Emit structured WorkflowDocument / JobSpec types. Persist helpers only as needed for creating run/job rows (may be thin). Green actions_workflow_parse. Explicitly do not execute steps (D-ACT-03 / ACT-07).
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && cargo nextest run -p octanest-api -E 'test(actions_workflow_parse)'</automated>
  </verify>
  <done>actions_workflow_parse tests green for discover + subset parse + invalid YAML error.</done>
</task>

</tasks>

<verification>
ACT-01 parse path covered by nextest.
</verification>

<success_criteria>
Workflow documents can be produced from a repo commit without executing jobs.
</success_criteria>

<output>
Create `.planning/phases/19-actions-runners/19-03-SUMMARY.md` when done
</output>
