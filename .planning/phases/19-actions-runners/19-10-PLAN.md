---
phase: 19-actions-runners
plan: "10"
type: execute
wave: 6
depends_on:
  - "05"
  - "02"
files_modified:
  - crates/octanest-api/src/actions/secrets.rs
  - crates/octanest-api/src/actions/tokens.rs
  - crates/octanest-api/src/rpc.rs
  - packages/api-client
  - apps/web/src/routes/$owner.$repo.settings.actions.tsrx
  - apps/web/src/routes/admin/runners.tsrx
  - crates/octanest-api/tests/actions_secrets.rs
requirements:
  - ACT-06
  - ACT-07
autonomous: true
user_setup: []
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Admin can create/rotate registration tokens; env bootstrap still works (D-ACT-08, ACT-06)"
    - "Repo Admin can toggle Actions enable and manage Actions secrets without reading back values (D-ACT-06, D-ACT-17)"
    - "Cloud/self-host have no managed minutes path — only registered runners (ACT-07, D-ACT-10)"
  artifacts:
    - path: crates/octanest-api/src/actions/secrets.rs
      provides: "Secrets encrypt/store"
    - path: apps/web/src/routes/admin/runners.tsrx
      provides: "Instance runner token UI"
  key_links:
    - from: job task payload
      to: decrypted secrets
      via: "FetchTask secrets injection"
  assumptions:
    - "ASSUME: New test file actions_secrets.rs added in this plan (Wave 0 named policy coverage already via dispatch_policy)"
  prohibitions:
    - statement: "Must not return secret values after create"
      flagged: true
    - statement: "Must not add Octanest-hosted runner minute billing or cloud executor"
      flagged: true
---

<objective>
Deliver registration-token Admin UX, repo Actions enable + secrets, and reinforce no-managed-minutes policy (ACT-06, ACT-07 / D-ACT-06, D-ACT-08, D-ACT-10, D-ACT-17).

Purpose: Operators and repo admins can run CI safely on their own runners.
Output: Secrets module + RPC + Settings/Admin UI; tests for secrets and policy.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-actions-runners/19-CONTEXT.md
@apps/web/src/routes/$owner.$repo.settings.tsrx
@crates/octanest-api/src/actions/tokens.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → registration token | One-time display on create |
| Secrets → job | Injected only into assigned task |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-21 | Information Disclosure | action_secrets | critical | mitigate | Encrypt at rest; never echo value on GET; Admin-only write (D-ACT-17) |
| T-19-22 | Elevation of privilege | registration tokens | high | mitigate | Admin-only create/rotate; hash storage (D-ACT-08) |
| T-19-23 | Elevation of privilege | managed minutes | high | mitigate | No cloud executor code paths (D-ACT-10 / ACT-07) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (19-10-T1): Actions secrets + enable RPC</name>
  <files>crates/octanest-api/src/actions/secrets.rs, crates/octanest-api/src/rpc.rs, packages/api-client, crates/octanest-api/tests/actions_secrets.rs</files>
  <behavior>
    - Admin can put/delete secret by name; get returns names only
    - Secrets appear in FetchTask job data for matching repo jobs
    - Repo actions_enabled toggle persists (D-ACT-06)
  </behavior>
  <read_first>crates/octanest-api/src/actions/tokens.rs, .planning/phases/19-actions-runners/19-CONTEXT.md</read_first>
  <action>
    Implement repo Actions secrets encryption using OCTANEST_ACTIONS_SECRETS_KEY (or derived instance secret) per D-ACT-17. Wire RPC + make rpc-gen. Ensure FetchTask includes secrets for the job. Green actions_secrets tests. Confirm no plaintext GET.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && make rpc-gen && cargo nextest run -p octanest-api -E 'test(actions_secrets)|test(actions_dispatch_policy)'</automated>
  </verify>
  <done>Secrets + enable behavior tested; rpc-gen updated.</done>
</task>

<task type="auto">
  <name>Task 2 (19-10-T2): Admin runners + repo Settings Actions UI</name>
  <files>apps/web/src/routes/admin/runners.tsrx, apps/web/src/routes/$owner.$repo.settings.actions.tsrx</files>
  <read_first>.agents/skills/octane/SKILL.md, apps/web/src/routes/admin/packages.tsrx, apps/web/src/routes/$owner.$repo.settings.tsrx</read_first>
  <action>
    Add Admin page to create/show (once) instance registration tokens and list runners (D-ACT-08). Add repo Settings Actions section for enable toggle + secrets CRUD (names only) per D-ACT-06 / D-ACT-17 / D-ACT-18. Octane only. Copy must state operators bring their own runners — Octanest does not sell managed minutes (ACT-07 / D-ACT-10).
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && make web-lint && make web-format-check && rg -n 'registration|secret|actions_enabled|managed' apps/web/src/routes/admin/runners.tsrx apps/web/src/routes/\$owner.\$repo.settings.actions.tsrx | rg -v '^#' | rg -q .</automated>
  </verify>
  <done>Admin + Settings Actions UIs exist and lint/format clean.</done>
</task>

<task type="auto">
  <name>Task 3 (19-10-T3): Policy assertion — no managed executor</name>
  <files>crates/octanest-api/tests/actions_dispatch_policy.rs, docs/CONFIGURATION.md</files>
  <read_first>crates/octanest-api/tests/actions_dispatch_policy.rs, docs/CONFIGURATION.md</read_first>
  <action>
    Strengthen ACT-07 / D-ACT-10 tests and docs: document that Octanest Cloud and self-host require registered runners; there is no managed-minutes product switch. Ensure dispatch_policy still fails closed if someone attempts in-process execution helpers.
  </action>
  <verify>
    <automated>cd /workspace/worktrees/phase-19 && cargo nextest run -p octanest-api -E 'test(actions_dispatch_policy)' && rg -n 'managed|registration|runner' docs/CONFIGURATION.md | rg -v '^#' | rg -q .</automated>
  </verify>
  <done>Policy tests + docs state registered-runners-only.</done>
</task>

</tasks>

<verification>
ACT-06 tokens/secrets operable; ACT-07 policy explicit.
</verification>

<success_criteria>
Admins can register runners and configure repo secrets without cloud minutes.
</success_criteria>

<output>
Create `.planning/phases/19-actions-runners/19-10-SUMMARY.md` when done
</output>
