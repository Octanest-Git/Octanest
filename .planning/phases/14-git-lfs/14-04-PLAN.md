---
phase: 14-git-lfs
plan: "04"
type: execute
wave: 3
depends_on:
  - "03"
files_modified:
  - crates/octanest-db/migrations/sqlite
  - crates/octanest-db/migrations/postgres
  - crates/octanest-db/migrations/mysql
  - crates/octanest-db/src
  - crates/octanest-api/src/lfs
  - crates/octanest-api/src/routes/git_lfs.rs
  - crates/octanest-api/src/auth/admin.rs
  - crates/octanest-api/src/rpc.rs
  - crates/octanest-api/tests/lfs_batch.rs
requirements:
  - GIT-12
  - GIT-13
autonomous: true
user_setup: []
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Configurable max object size and per-repo + per-user quotas enforce on upload with clear reject (D-LFS-12, D-LFS-14)"
    - "Instance env defaults apply with Admin UI/RPC overrides possible (D-LFS-13)"
    - "Logical bytes charged per repo link; physical bytes tracked for disk (RESEARCH pitfall 3)"
  artifacts:
    - path: crates/octanest-api/src/lfs
      provides: "Quota check before issuing upload actions / accepting PUT"
  key_links:
    - from: crates/octanest-api/src/routes/git_lfs.rs
      to: crates/octanest-db/src
      via: "batch upload preflight reads quota counters"
  assumptions:
    - "ASSUME defaults: max object 2 GiB; per-repo 10 GiB logical; per-user 50 GiB logical; 0 or -1 = unlimited (RESEARCH discretion)"
    - "ASSUME: enforce both repo logical quota and repo-owner user quota; attribute new OID to pushing user for breakdown (RESEARCH open Q2)"
  prohibitions:
    - statement: "Must not soft-warn-only on over-quota uploads"
      flagged: true
---

<objective>
Add max-object and quota enforcement with env defaults and Admin override storage for LFS uploads.

Purpose: Deliver D-LFS-12/13/14 operator limits before GC/UI.
Output: Green lfs_quota tests; Admin get/update settings RPC stubs ready for 14-08/14-10.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-git-lfs/14-CONTEXT.md
@.planning/phases/14-git-lfs/14-RESEARCH.md
@.planning/phases/14-git-lfs/14-03-SUMMARY.md
@crates/octanest-api/src/auth/admin.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → batch upload | Client-claimed size untrusted until PUT verifies |
| Admin RPC → quota settings | Instance admin only |
| Uploader → disk | Quotas prevent unbounded fill |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03 | Denial of service | LFS PUT / batch | high | mitigate | Max object + per-repo/user quotas; reject 507/422 with LFS error JSON (D-LFS-12, D-LFS-14) |
| T-14-02 | Elevation of privilege | admin.lfs settings | high | mitigate | is_admin gate on overrides (D-LFS-13) |
| T-14-SC | Tampering | crates | medium | accept | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (14-04-T1): Env defaults + upload reject on max size / quota</name>
  <reversibility rating="costly">Quota schema + enforcement counters (D-LFS-12).</reversibility>
  <files>crates/octanest-db/migrations/sqlite, crates/octanest-db/migrations/postgres, crates/octanest-db/migrations/mysql, crates/octanest-db/src, crates/octanest-api/src/lfs, crates/octanest-api/src/routes/git_lfs.rs, crates/octanest-api/tests/lfs_batch.rs</files>
  <read_first>.planning/phases/14-git-lfs/14-RESEARCH.md, crates/octanest-api/src/lfs/store.rs</read_first>
  <behavior>
    - Object size above max → batch and/or PUT reject with clear LFS error (no silent accept)
    - Upload that would exceed per-repo logical quota → reject
    - Upload that would exceed per-user (repo owner) quota → reject
    - Unlimited sentinel (0 or -1) disables that limit
  </behavior>
  <action>
Implement D-LFS-12 and D-LFS-14: read OCTANEST_LFS_MAX_OBJECT_BYTES, OCTANEST_LFS_QUOTA_REPO_BYTES, OCTANEST_LFS_QUOTA_USER_BYTES (defaults per RESEARCH discretion). Persist usage counters for logical bytes per repo and per user; check before issuing upload actions and again during PUT (abort stream if exceeded). Separate physical unique OID bytes from logical linked sizes. Green lfs_quota stubs. Do not build Admin UI yet.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(lfs_quota)'</automated>
  </verify>
  <done>Over-size and over-quota uploads are rejected with clear errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (14-04-T2): Admin override RPC for instance LFS limits</name>
  <files>crates/octanest-api/src/auth/admin.rs, crates/octanest-api/src/rpc.rs, crates/octanest-db/src, crates/octanest-api/tests/lfs_batch.rs</files>
  <read_first>crates/octanest-api/src/auth/admin.rs, crates/octanest-api/src/rpc.rs</read_first>
  <behavior>
    - admin.lfs.getSettings / updateSettings (names flexible) require instance admin
    - Overrides take precedence over env defaults when set
    - Non-admin cannot update
  </behavior>
  <action>
Implement D-LFS-13 Admin override storage (instance_settings row or dedicated table) and RPC get/update gated by is_admin. Env remains default when override null. Register in rpc.rs; rpc-gen in 14-08. Add integration coverage for admin override affecting subsequent upload reject threshold.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(lfs_quota) | test(lfs)'</automated>
  </verify>
  <done>Admin can override instance LFS limits used by enforcement</done>
</task>

</tasks>

<verification>
lfs_quota nextest green; admin override affects enforcement.
</verification>

<success_criteria>
Operators have configurable max object and quotas; uploads exceeding limits are rejected.
</success_criteria>

<output>
Create `.planning/phases/14-git-lfs/14-04-SUMMARY.md` when done
</output>
