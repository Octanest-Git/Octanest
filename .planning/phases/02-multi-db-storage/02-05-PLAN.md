---
phase: 02-multi-db-storage
plan: "05"
type: execute
wave: 5
depends_on:
  - "02-04"
files_modified:
  - .github/workflows/ci.yml
  - docs/database.md
  - README.md
  - .env.example
  - .planning/phases/02-multi-db-storage/02-VALIDATION.md
requirements:
  - PLAT-07
  - PLAT-08
autonomous: true
user_setup: []
must_haves:
  truths:
    - "CI proves migrate + probe on postgres, mysql, and sqlite in one matrix job on every PR (D-15)"
    - "CI validates the sqlite Compose overlay config alongside the existing default and mysql checks"
    - "docs/database.md, README, .env.example, and make help all describe the same three dialect URLs (D-04)"
    - ".env.example SQLite line points at ./var/octanest.db, not the stale ./data path (D-17)"
    - "Empty-target-only dialect switch is documented as a recipe, not just a make target (D-03, D-16)"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "db-matrix job across three dialects"
    - path: docs/database.md
      provides: "canonical operator database documentation"
  key_links:
    - from: .github/workflows/ci.yml
      to: crates/octanest-db/tests/dialect_probe.rs
      via: "cargo test -p octanest-db --test dialect_probe per matrix leg"
---

<objective>
Close Phase 2 with mechanized proof and operator documentation: a three-dialect CI matrix, Compose config validation for the SQLite overlay, and the canonical database docs set.

Purpose: D-04, D-15 and the ROADMAP criterion #2 gate ("migrations apply cleanly on each supported dialect") on every PR.
Output: extended `ci.yml`, `docs/database.md`, corrected `README.md` and `.env.example`, and a VALIDATION map that matches reality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-multi-db-storage/02-CONTEXT.md
@.planning/phases/02-multi-db-storage/02-RESEARCH.md
@.planning/phases/02-multi-db-storage/02-VALIDATION.md
@.planning/phases/02-multi-db-storage/02-04-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fork PR → CI runner | Untrusted code executing with runner-local database services |
| CI service containers → job steps | Default credentials on localhost-bound Postgres/MySQL |
| Public docs → operators | Documented defaults become production defaults if written carelessly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Elevation | CI secrets exposure to fork PRs | mitigate | `db-matrix` uses no `secrets.*`; all credentials are the literal throwaway value `octanest` on runner-local services (continues T-01-12) |
| T-02-17 | Information disclosure | CI logs leaking connection strings | accept | Matrix `database_url` values are non-secret localhost throwaways; the application itself redacts passwords in its own error output (T-02-01) |
| T-02-18 | Information disclosure | docs encouraging weak production credentials | mitigate | `docs/database.md` and `.env.example` label `octanest`/`octanest` as local-only and instruct operators to set real credentials plus a non-public bind before exposing an instance |
| T-02-19 | Tampering | undocumented destructive dialect switch | mitigate | `docs/database.md` states the switch applies to an empty target only, copies no data, and that cross-dialect data migration is out of scope for this phase |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (02-05-T1): CI db-matrix job across three dialects</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml, crates/octanest-db/tests/dialect_probe.rs, .planning/phases/02-multi-db-storage/02-RESEARCH.md</read_first>
  <action>
Locks RESEARCH open items 2 and 3: **one matrix job** (the literal reading of D-15, and shorter than three near-identical job blocks), and **no live Compose bring-up in CI** (that stays Phase 22 / PLAT-03; Phase 2 CI stops at `cargo test` plus `docker compose config`).

1. Append a `db-matrix` job to `.github/workflows/ci.yml`, following the existing job style (`actions/checkout@v4`, `dtolnay/rust-toolchain@stable`, `Swatinem/rust-cache@v2`):

```yaml
  db-matrix:
    name: db-${{ matrix.dialect }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - dialect: postgres
            database_url: postgres://octanest:octanest@localhost:5432/octanest
          - dialect: mysql
            database_url: mysql://octanest:octanest@127.0.0.1:3306/octanest
          - dialect: sqlite
            database_url: sqlite:./var/octanest.db
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: octanest
          POSTGRES_PASSWORD: octanest
          POSTGRES_DB: octanest
        ports: ["5432:5432"]
        options: >-
          --health-cmd="pg_isready -U octanest" --health-interval=5s --health-timeout=5s --health-retries=10
      mysql:
        image: mysql:8.4
        env:
          MYSQL_USER: octanest
          MYSQL_PASSWORD: octanest
          MYSQL_DATABASE: octanest
          MYSQL_ROOT_PASSWORD: octanest
        ports: ["3306:3306"]
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -uroot -poctanest" --health-interval=5s --health-timeout=5s --health-retries=20
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: migrate + probe (${{ matrix.dialect }})
        env:
          DATABASE_URL: ${{ matrix.database_url }}
          OCTANEST_DB_DIALECT: ${{ matrix.dialect }}
        run: cargo test -p octanest-db --test dialect_probe -- --nocapture
```

   Both service containers boot on every leg because GitHub Actions cannot condition `services:` on `matrix.*` (RESEARCH pitfall 7); that is an accepted few-seconds cost on the sqlite leg. Add a comment above the job saying so, so the next reader does not "fix" it.
2. Set `OCTANEST_DB_DIALECT` on every leg deliberately: it exercises the D-01/D-02 agreement path in CI, not just the URL inference.
3. Extend the existing `compose` job with a third step validating the new overlay (do **not** add bring-up):

```yaml
      - name: docker compose sqlite overlay config
        run: docker compose -f docker-compose.yml -f docker-compose.sqlite.yml config
```

4. Leave the `rust`, `js`, `rpc-sync`, and existing `compose` steps unchanged; `cargo test --workspace` in the `rust` job continues to run with no `DATABASE_URL`, exercising the graceful-skip path.
  </action>
  <verify>grep -q 'db-matrix' .github/workflows/ci.yml &amp;&amp; docker compose -f docker-compose.yml -f docker-compose.sqlite.yml config &gt;/dev/null</verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains `db-matrix`
    - `.github/workflows/ci.yml` contains all three of `dialect: postgres`, `dialect: mysql`, `dialect: sqlite`
    - `.github/workflows/ci.yml` contains `cargo test -p octanest-db --test dialect_probe`
    - `.github/workflows/ci.yml` contains `docker-compose.sqlite.yml`
    - `! grep -q 'secrets\.' .github/workflows/ci.yml`
    - `! grep -q 'compose .*up --build' .github/workflows/ci.yml` (no live bring-up in CI this phase)
    - the workflow parses: `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0
  </acceptance_criteria>
  <done>Every PR proves migrations and the probe round-trip on all three dialects</done>
</task>

<task type="auto">
  <name>Task 2 (02-05-T2): Canonical operator documentation for dialects</name>
  <files>docs/database.md, README.md, .env.example</files>
  <read_first>README.md, .env.example, Makefile, docker-compose.sqlite.yml, .planning/phases/02-multi-db-storage/02-CONTEXT.md</read_first>
  <action>
D-04 names four surfaces: `.env.example`, README, `make help` (done in 02-04), and `docs/database.md`. All must agree on the same URLs.

1. Create `docs/database.md` with these sections:
   - **Supported dialects** — table of dialect, sample `DATABASE_URL`, and bring-up command (`make up`, `make up-mysql`, `make up-sqlite`).
   - **How the dialect is chosen (D-01/D-02)** — inferred from the `DATABASE_URL` scheme (`postgres://`, `postgresql://`, `mysql://`, `sqlite:`); optional `OCTANEST_DB_DIALECT` must agree or the API exits at boot with `does not match DATABASE_URL scheme`. `mariadb://` is not supported.
   - **Migrations (D-05/D-06/D-07)** — one hand-synced `.sql` set per dialect under `crates/octanest-db/migrations/{postgres,mysql,sqlite}/`, applied by the sqlx migrator with `_sqlx_migrations` bookkeeping; `OCTANEST_AUTO_MIGRATE` defaults to `true`, and setting it to `false` requires `make db-migrate`.
   - **SQLite specifics (D-17/D-18/D-19/D-20)** — default file `./var/octanest.db`, parent directories created automatically at startup, `journal_mode=WAL` and `foreign_keys=ON` applied on every connection, `./var` bind-mounted to `/app/var` in Compose, `var/` gitignored, single-writer expectations. State plainly that deeper tuning and multi-replica guidance are out of scope for now.
   - **Diagnostic probe (D-09–D-13)** — the `instances` table (`id`, `dialect`, `probe_count`, `probed_at`) and the `system.db_probe` RPC, with a copy-pasteable curl including `Octanest-RPC-Version: 1`. Note it is unauthenticated in this phase and that admin-only gating arrives with the system dashboard.
   - **Switching dialect on an empty database (D-03/D-16)** — numbered recipe: stop the stack; point `DATABASE_URL` at the new empty target; run `make db-switch-dialect` (refuses a non-empty target; `make db-switch-dialect ARGS=--force-empty` overrides the check); persist the URL in `.env`; bring the stack up with the matching `make up*` target. State explicitly that no data is copied between dialects and that migrating a populated database across dialects is not supported.
   - **Security note** — `octanest`/`octanest` are local-only credentials; set real ones and do not expose an instance without auth (T-02-18).
2. `README.md`: replace the stale SQLite paragraph (`DATABASE_URL=sqlite:./data/octanest.db`, "full SQLite proof is Phase 2") with a short three-dialect block showing `make up`, `make up-mysql`, `make up-sqlite` and the matching `make smoke`, `make smoke-mysql`, `make smoke-sqlite`, linking to `docs/database.md` as the canonical reference.
3. `.env.example`: correct and expand the database section —
   - keep the default Postgres line as-is;
   - add `OCTANEST_AUTO_MIGRATE=true` with a comment that prod-like deployments set `false` and run `make db-migrate`;
   - replace the commented SQLite placeholder with `# DATABASE_URL=sqlite:./var/octanest.db` (D-17 path, no more `./data`) and drop the "full SQLite proof is Phase 2" wording;
   - add a commented `# OCTANEST_DB_DIALECT=postgres` line noting it is optional and must agree with the URL.
4. Do not document any diagnostics UI, admin gating, or cross-dialect data migration — those are explicitly deferred.
  </action>
  <verify>test -f docs/database.md &amp;&amp; grep -q 'sqlite:./var/octanest.db' .env.example &amp;&amp; ! grep -q 'sqlite:./data/octanest.db' .env.example README.md</verify>
  <acceptance_criteria>
    - `test -f docs/database.md`
    - `docs/database.md` contains all of `OCTANEST_DB_DIALECT`, `OCTANEST_AUTO_MIGRATE`, `system.db_probe`, `make db-switch-dialect`, `journal_mode`, `instances`
    - `grep -q 'sqlite:./var/octanest.db' .env.example`
    - `grep -q 'OCTANEST_AUTO_MIGRATE' .env.example`
    - `! grep -rq 'sqlite:./data/octanest.db' .env.example README.md docs/`
    - `README.md` contains `make up-sqlite` and a link to `docs/database.md`
    - `! grep -riq 'diagnostics dashboard\|admin UI' docs/database.md`
  </acceptance_criteria>
  <done>All four operator doc surfaces name the same three URLs and the same switch recipe</done>
</task>

<task type="auto">
  <name>Task 3 (02-05-T3): Close out the Phase 2 validation contract</name>
  <files>.planning/phases/02-multi-db-storage/02-VALIDATION.md</files>
  <read_first>.planning/phases/02-multi-db-storage/02-VALIDATION.md, .planning/phases/02-multi-db-storage/02-01-SUMMARY.md, .planning/phases/02-multi-db-storage/02-02-SUMMARY.md, .planning/phases/02-multi-db-storage/02-03-SUMMARY.md, .planning/phases/02-multi-db-storage/02-04-SUMMARY.md</read_first>
  <action>
1. Walk the Per-Task Verification Map (already populated with task IDs `02-01-T1` … `02-05-T3`) and set each row's Status to ✅ green once its automated command has been run and passed, or ❌ red with a note when blocked (for example, Compose smoke rows when Docker Engine is unavailable in the execution environment — the same convention 01-04 used).
2. Tick the Wave 0 Requirements checkboxes that now have real files behind them, correcting any command that drifted during execution so every listed command is copy-pasteable.
3. Complete the Validation Sign-Off checklist and set frontmatter `nyquist_compliant: true` and `wave_0_complete: true` only when every row has either a passing automated command or an explicit manual/blocked note.
4. Leave the Manual-Only Verifications table intact — live three-profile Compose bring-up stays a local proof this phase by design (RESEARCH Layer 4 recommendation, locked in 02-05-T1).
  </action>
  <verify>grep -q 'nyquist_compliant: true' .planning/phases/02-multi-db-storage/02-VALIDATION.md &amp;&amp; ! grep -q '| TBD |' .planning/phases/02-multi-db-storage/02-VALIDATION.md</verify>
  <acceptance_criteria>
    - `02-VALIDATION.md` contains `nyquist_compliant: true`
    - `02-VALIDATION.md` contains no `TBD` rows
    - `02-VALIDATION.md` references `02-01-T1`, `02-03-T2`, `02-04-T3`, and `02-05-T1`
    - every Per-Task Verification Map row has a Status other than ⬜ pending
  </acceptance_criteria>
  <done>The validation contract reflects what was actually built and proven</done>
</task>

</tasks>

<verification>
- [ ] `.github/workflows/ci.yml` parses and contains a three-dialect `db-matrix` job with no secrets
- [ ] `docker compose -f docker-compose.yml -f docker-compose.sqlite.yml config` passes (the exact CI step)
- [ ] `docs/database.md` exists and matches `make help` sample URLs
- [ ] `.env.example` no longer references `./data/octanest.db`
- [ ] `02-VALIDATION.md` is `nyquist_compliant: true` with no TBD rows
</verification>

<success_criteria>
- D-04 and D-15 satisfied; RESEARCH open items 1–4 all resolved in-plan
- PLAT-07 and PLAT-08 are enforced by CI on every PR
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-db-storage/02-05-SUMMARY.md`
</output>
