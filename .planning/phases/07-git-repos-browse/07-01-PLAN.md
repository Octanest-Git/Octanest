---
phase: 07-git-repos-browse
plan: "01"
type: execute
wave: 1
depends_on:
  - "00"
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/phases/07-git-repos-browse/07-COVERAGE.md
requirements:
  - GIT-09
  - GIT-10
autonomous: false
user_setup: []
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "REQUIREMENTS GIT-09 text describes system git CLI primary with documented future gitoxide path (D-32)"
    - "ROADMAP Phase 7 Goal and success criterion 4 match CLI-primary + GitBackend swap wording (D-32)"
    - "07-COVERAGE.md records git CLI capability INTEGRATE/OPT-OUT matrix"
    - "Human confirmed one-way doors for URL scheme (D-14) and git boot floor (D-33) before tracer implements them"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "Amended GIT-09 / GIT-10 wording"
    - path: .planning/phases/07-git-repos-browse/07-COVERAGE.md
      provides: "API coverage matrix for system git"
  key_links:
    - from: .planning/phases/07-git-repos-browse/07-CONTEXT.md
      to: .planning/REQUIREMENTS.md
      via: "D-32 REQUIREMENT AMENDMENT"
  assumptions:
    - "ASSUME (assumption-delta promote): primary noun is GitBackend; CliGitBackend is the shipped adapter; GixGitBackend is a future adapter — not gitoxide-primary with CLI fallback"
  prohibitions:
    - statement: "Must not leave ROADMAP/REQUIREMENTS claiming gitoxide-first for Phase 7 implementation after this plan"
      flagged: true
---

<objective>
Lock one-way operator/URL contracts and amend planning docs so Phase 7 implements CLI-first git (D-32/D-33) with an explicit API coverage matrix.

Purpose: CONTEXT overrides stale ROADMAP/REQUIREMENTS; reversibility gates before tracer.
Output: Confirmed D-14/D-33 decisions; amended REQUIREMENTS/ROADMAP/PROJECT; COVERAGE.md present.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@.planning/phases/07-git-repos-browse/07-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/07-git-repos-browse/07-COVERAGE.md
</context>

<assumption_delta_decision>
noun: GitBackend (swappable git operations seam)
decision: promote
rationale: CONTEXT D-32 flips ROADMAP gitoxide-primary+CLI-fallback to CLI-primary+future-gix; promote GitBackend as the identity noun and demote any concrete engine to an adapter (Cli now, Gix later). Do not keep both as co-equal required primaries.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs → implementers | Stale gitoxide-first text would ship wrong backend |
| Operator boot contract | Missing/old git must not silently continue |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Tampering | GIT-09 wording drift | medium | mitigate | Amend REQUIREMENTS/ROADMAP/PROJECT in this plan (D-32) |
| T-07-04 | Denial of service | Boot without git | high | mitigate | Human confirms fail-boot door D-33 before 07-02 implements exit(1) |
| T-07-SC | Tampering | package installs | high | mitigate | No package installs in this plan |
</threat_model>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1 (07-01-T1): Confirm public URL scheme /{owner}/{repo} (D-14 one-way)</name>
  <decision>
Adopt public forge URLs `/{owner}/{repo}` (plus tree/blob/raw/blame/commits/commit/branches/tags/compare nested paths per D-16/D-17) with a reserved-name denylist that includes flat app routes such as `login`, `setup`, `new`, `status`, and existing reserved usernames — per D-14.
  </decision>
  <context>
Rated one-way in CONTEXT: public URL scheme. Undo after ship requires redirects/migrations for every shared link. Tracer (07-02) and browse plans will implement this shape.
  </context>
  <options>
    <option id="owner_repo_path">
      <title>/{owner}/{repo} (recommended — D-14)</title>
      <pros>GitHub/Gitea familiar; matches UI-SPEC; reserved denylist protects /new</pros>
      <cons>Collides with flat routes unless denylist is complete</cons>
    </option>
    <option id="repos_prefix">
      <title>/repos/{owner}/{repo} prefix forever</title>
      <pros>No collision with /new</pros>
      <cons>Violates locked D-14; weaker forge parity</cons>
    </option>
  </options>
  <resume-signal>Reply with owner_repo_path (recommended) or repos_prefix</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2 (07-01-T2): Confirm fail-boot when git missing or &lt; 2.5 (D-33 one-way)</name>
  <decision>
API process must refuse to serve if system `git` is missing or reports version older than 2.5.0: print error and `std::process::exit(1)` during boot (D-33). Floor stays 2.5 even though `--initial-branch` needs 2.28 — create uses `symbolic-ref` after `init --bare` (RESEARCH Pattern 2).
  </decision>
  <context>
One-way operator contract: Compose/host images without git will not start the API. Dockerfile must install git (07-02). Soft-continue would violate D-33 and produce runtime failures on every repo op.
  </context>
  <options>
    <option id="fail_boot_git">
      <title>Fail boot if git missing/old (recommended — D-33)</title>
      <pros>Matches locked decision; fail-fast ops; clear Compose requirement</pros>
      <cons>Local/dev must install git ≥2.5</cons>
    </option>
    <option id="warn_and_serve">
      <title>Log warning and serve without git</title>
      <pros>API comes up for non-git routes</pros>
      <cons>Violates D-33; forge APIs fail late</cons>
    </option>
  </options>
  <resume-signal>Reply with fail_boot_git (recommended) or warn_and_serve</resume-signal>
</task>

<task type="auto">
  <name>Task 3 (07-01-T3): Amend GIT-09/ROADMAP/PROJECT + ensure COVERAGE.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/PROJECT.md, .planning/phases/07-git-repos-browse/07-COVERAGE.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/PROJECT.md, .planning/phases/07-git-repos-browse/07-CONTEXT.md, .planning/phases/07-git-repos-browse/07-COVERAGE.md</read_first>
  <action>
Per D-32: rewrite **GIT-09** to state Phase 7 git operations use the system `git` CLI (≥2.5) behind a `GitBackend` abstraction, with a documented future gitoxide/`gix` adapter when feature coverage allows — remove gitoxide-first wording. Adjust **GIT-10** to match (architecture allows swapping backends; CLI is current; gitoxide later). Update ROADMAP Phase 7 **Goal** and success criterion 4 to the same CLI-primary + swap-boundary language (drop "powered primarily by gitoxide"). Update PROJECT.md mental model if it still says gitoxide object layer as current. Ensure `07-COVERAGE.md` exists with the git CLI INTEGRATE/OPT-OUT table (create/update if planner draft incomplete). Do not implement Rust code here.
  </action>
  <verify>
    <automated>rg -n 'GIT-09' .planning/REQUIREMENTS.md | head -5 && test -f .planning/phases/07-git-repos-browse/07-COVERAGE.md && rg -n 'CliGitBackend|system .git. CLI|GitBackend' .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/07-git-repos-browse/07-COVERAGE.md && ! rg -n 'gitoxide-first|primarily via gitoxide' .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
    <fails_when>REQUIREMENTS still describes Phase 7 as gitoxide-primary; or COVERAGE.md missing; or ROADMAP Phase 7 Goal still says powered primarily by gitoxide</fails_when>
  </verify>
  <acceptance_criteria>
    - GIT-09 bullet no longer claims gitoxide as the primary Phase 7 implementation
    - ROADMAP Phase 7 Goal mentions system git CLI / GitBackend (or equivalent CLI-primary language)
    - `07-COVERAGE.md` has a markdown table with INTEGRATE and at least one OPT-OUT row
  </acceptance_criteria>
  <done>Planning docs and COVERAGE match D-32; checkpoints resolved</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| Amended GIT-09 / GIT-10 | 07-01 | D-32 |
| ROADMAP Phase 7 Goal rewrite | 07-01 | D-32 |
| `07-COVERAGE.md` | 07-01 | API coverage gate |
| Human decisions D-14 / D-33 | 07-01 | reversibility |

<verification>
Docs amended; COVERAGE present; human selected recommended one-way options.
</verification>

<success_criteria>
No planning artifact still mandates gitoxide-first for Phase 7; one-way doors confirmed for tracer.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-01-SUMMARY.md` when done
</output>
