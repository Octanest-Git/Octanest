---
phase: 07-git-repos-browse
plan: "06"
type: execute
wave: 10
depends_on:
  - "05"
  - "15"
files_modified:
  - crates/octanest-git/src/backend.rs
  - crates/octanest-git/src/cli.rs
  - crates/octanest-api/src/repo/mod.rs
  - crates/octanest-api/src/rpc.rs
  - apps/web/src/routes/$owner.$repo.commits.$.tsrx
  - apps/web/src/routes/$owner.$repo.commit.$sha.tsrx
  - apps/web/src/routes/$owner.$repo.compare.$.tsrx
  - apps/web/src/routes/$owner.$repo.blame.$.tsrx
  - apps/web/src/components/repo
  - packages/api-client
requirements:
  - GIT-05
autonomous: true
user_setup: []
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "User can list commits for a ref, open /commit/{sha} with unified diffs, compare base...head, and view blame (D-16)"
    - "Empty history and Nothing to compare empty states use UI-SPEC copy (UI E5/E7)"
    - "Soft file-size caps apply to blame/diff display (D-20)"
    - statement: "Long ref names in compare pickers truncate — held-out visual"
      verification: backstop
  artifacts:
    - path: apps/web/src/routes/$owner.$repo.commits.$.tsrx
      provides: "Commits list"
    - path: apps/web/src/routes/$owner.$repo.compare.$.tsrx
      provides: "Compare view"
    - path: apps/web/src/routes/$owner.$repo.blame.$.tsrx
      provides: "Blame view"
  key_links:
    - from: crates/octanest-api/src/repo/mod.rs
      to: crates/octanest-git/src/cli.rs
      via: "log / show_commit / diff / blame"
  assumptions:
    - "ASSUME (RESEARCH Q4): ship functional unified diff + blame with soft caps; pagination via skip/limit or Load more"
  prohibitions:
    - statement: "Must not expose private commit metadata to non-owners"
      flagged: true
---

<objective>
Expand browse history surfaces: commits list, commit detail diffs, compare, and blame (remainder of D-16 / GIT-05).

Purpose: GitHub-like history IA on top of tree/blob.
Output: History RPC + Octane routes; ACL reused from 07-05.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-UI-SPEC.md
@.planning/phases/07-git-repos-browse/07-05-SUMMARY.md
@.planning/phases/07-git-repos-browse/07-15-SUMMARY.md
@crates/octanest-git/src/backend.rs
@crates/octanest-api/src/repo/acl.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client ref/sha → git log/diff/blame | Validate before argv |
| ACL → history RPCs | Same unified not_found |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-17 | Tampering | ref/sha inputs | high | mitigate | Allowlist ref characters; reject path escapes before Command |
| T-07-18 | Denial of service | unbounded diff | medium | mitigate | Limit patch size / file count in response; soft UI caps |
| T-07-13 | Information disclosure | history ACL | high | mitigate | Reuse acl.rs on every history procedure |
| T-07-SC | Tampering | packages | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (07-06-T1): log / show_commit / diff / blame backend</name>
  <files>crates/octanest-git/src/backend.rs, crates/octanest-git/src/cli.rs, crates/octanest-api/src/repo/mod.rs, crates/octanest-api/src/rpc.rs, packages/api-client</files>
  <read_first>crates/octanest-git/src/backend.rs, crates/octanest-api/src/repo/acl.rs, .planning/phases/07-git-repos-browse/07-RESEARCH.md</read_first>
  <behavior>
    - log returns paged CommitSummary for ref
    - show_commit returns files + unified patch
    - diff(base,head) empty → empty result not 500
    - blame returns per-line meta for text files
  </behavior>
  <action>
Implement GitBackend methods `log`, `show_commit`, `diff`, `blame` via CLI argv (`git log`, `git show`, `git diff`, `git blame`). Wire RPC under ACL. Cap payload sizes. rpc-gen. Unit/integration tests in octanest-git and/or api filters covering non-empty seeded repo.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-git --lib && cargo nextest run -p octanest-api -E 'test(repo_)'</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'fn log|fn show_commit|fn diff|fn blame' crates/octanest-git/src/backend.rs`
    - `rg -n 'repo\.(log|commits|commit|diff|compare|blame)' crates/octanest-api/src/rpc.rs`
  </acceptance_criteria>
  <done>History git ops exposed via ACL’d RPC</done>
</task>

<task type="auto">
  <name>Task 2 (07-06-T2): Commits / commit / compare / blame UI</name>
  <files>apps/web/src/routes/$owner.$repo.commits.$.tsrx, apps/web/src/routes/$owner.$repo.commit.$sha.tsrx, apps/web/src/routes/$owner.$repo.compare.$.tsrx, apps/web/src/routes/$owner.$repo.blame.$.tsrx, apps/web/src/components/repo</files>
  <read_first>.planning/phases/07-git-repos-browse/07-UI-SPEC.md, apps/web/src/routes/$owner.$repo.index.tsrx</read_first>
  <action>
Add Octane routes matching D-16 paths: commits list (SHA, message truncate+title, author, relative time, Load more); commit detail header + unified diffs monospace; compare base/head + empty "Nothing to compare for these refs."; blame gutter short SHA/author/date linking to commit; per-file history entry point from blob if natural. Titles per UI-SPEC. Link-row nav highlights Commits when active. Skeletons/error/retry per UI Considerations.
  </action>
  <verify>
    <automated>bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Nothing to compare|Commits ·|Blame' apps/web/src/routes/`
    - Route files for commits, commit, compare, blame exist
  </acceptance_criteria>
  <done>History IA browsable in UI</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `log`/`show_commit`/`diff`/`blame` | 07-06 | GIT-05 |
| commits/commit/compare/blame routes | 07-06 | D-16 |

<verification>
git lib + api repo_ tests + web build green.
</verification>

<success_criteria>
GIT-05 history surfaces complete beyond tree/blob.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-06-SUMMARY.md` when done
</output>
