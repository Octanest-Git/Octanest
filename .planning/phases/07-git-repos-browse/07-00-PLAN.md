---
phase: 07-git-repos-browse
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/octanest-git/src/lib.rs
  - crates/octanest-git/src/version.rs
  - crates/octanest-git/Cargo.toml
  - Cargo.toml
  - crates/octanest-api/tests/repo_create.rs
  - crates/octanest-api/tests/repo_private_404.rs
  - crates/octanest-api/tests/repo_branch_soft_protect.rs
  - crates/octanest-api/tests/repo_fs_layout.rs
  - crates/octanest-db/tests/dialect_repositories.rs
  - apps/web/src/routes/new.integration.test.ts
  - apps/web/src/components/signed-in-home.integration.test.ts
requirements:
  - GIT-01
  - GIT-05
  - GIT-06
  - GIT-07
  - GIT-08
  - GIT-09
  - GIT-10
autonomous: true
user_setup: []
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Wave 0 failing stubs exist for git version gate, CliGitBackend archive formats, repo.create verified/unverified/duplicate, private→not_found, branch soft-protect, fs layout under repos_dir"
    - "Wave 0 web stubs exist for /new verify wall + New repository CTA enablement on SignedInHome"
    - "dialect_repositories stub expects 0007_repositories (+ default_branch / default_visibility columns)"
    - statement: "Syntax highlighting visual fidelity for .tsrx/.ripple held for human UAT"
      verification: backstop
  artifacts:
    - path: crates/octanest-git/src/version.rs
      provides: "git_version_gate failing unit stubs (intentionally new crate scaffold)"
    - path: crates/octanest-api/tests/repo_create.rs
      provides: "GIT-01 create/unverified/duplicate failing integration stubs"
    - path: apps/web/src/routes/new.integration.test.ts
      provides: "D-11 /new wall failing stubs"
  key_links:
    - from: .planning/phases/07-git-repos-browse/07-VALIDATION.md
      to: crates/octanest-api/tests/repo_create.rs
      via: "Wave 0 Requirements checklist"
  assumptions:
    - "ASSUME (GIT-01 empty): empty repository name is rejected; optional description may be empty string; JSON null name is invalid input"
    - "ASSUME (GIT-01 encoding): validate_repo_name operates on UTF-8 Unicode scalar string; allowed charset letters/digits/hyphen/underscore/period per D-06; length measured in Unicode scalars with same bounds style as username (document exact max in core)"
    - "ASSUME (GIT-01 idempotency): second create same owner+name among non-deleted returns stable duplicate error — never overwrite disk or row"
    - "ASSUME (GIT-01 concurrency): unique index on (owner_id, lower(name)) where deleted_at IS NULL; concurrent creates → one success one duplicate"
    - "ASSUME (GIT-05 concurrency): browse RPCs are read-only; parallel reads allowed; no exclusive lock required for ls-tree/log"
    - "ASSUME (GIT-06 idempotency): create existing branch → stable error; delete missing branch → stable not_found-style error; rename onto existing → error"
    - "ASSUME (GIT-06 concurrency): concurrent branch mutations on one bare repo may fail one caller with git lock/error; no corrupted refs"
    - "ASSUME (GIT-07 empty): no commits → archive returns structured empty/unavailable; UI disables Download ZIP/tar.gz"
    - "ASSUME (GIT-07 encoding): refs validated before argv; archive prefix uses repo name bytes as git archive --prefix"
    - "FLAGGED unresolved (GIT-09 unclassified): Phase 7 ships CliGitBackend only; no GixGitBackend implementation body"
    - "FLAGGED unresolved (GIT-10 unclassified): GitBackend trait + ARCHITECTURE docs are the swap seam; AppState holds Arc<dyn GitBackend> registered as Cli only"
  prohibitions:
    - statement: "Must not implement Phase 7 production git ops via gitoxide/gix as the primary backend"
      flagged: true
    - statement: "Must not return distinct private-vs-missing error codes that reveal private repository existence"
      flagged: true
    - statement: "Must not place multi-megabyte archive or blob payloads in JSON RPC responses"
      flagged: true
    - statement: "Must not call live gitignore.io or other network catalog APIs during repository create"
      flagged: true
    - statement: "Must not enable repository create for unverified email sessions"
      flagged: true
---

<objective>
Scaffold Nyquist/Wave 0 failing tests and crate stubs for Phase 7 git forge surfaces before implementation plans turn them green.

Purpose: Every later `<automated>` verify names a real test path (07-VALIDATION.md Wave 0).
Output: Failing (or compile-gated) octanest-git + API + web + dialect stubs matching proven nextest/vitest filters.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@.planning/phases/07-git-repos-browse/07-RESEARCH.md
@.planning/phases/07-git-repos-browse/07-VALIDATION.md
@.planning/phases/07-git-repos-browse/07-PATTERNS.md
@crates/octanest-api/tests/auth_verify_gate.rs
@crates/octanest-db/tests/dialect_auth.rs
@apps/web/src/routes/setup.integration.test.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → temp repos_dir | Parallel nextest must isolate OCTANEST_REPOS_DIR / temp dirs |
| Stub assertions → production ACL | Private 404 and require_verified behaviors encoded before impl |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Elevation of privilege | repo.create stubs | high | mitigate | Stub asserts auth.email_unverified without verified session (D-11) |
| T-07-02 | Information disclosure | repo_private_404 stubs | high | mitigate | Stub asserts identical not_found for missing and non-owner private (D-25) |
| T-07-SC | Tampering | cargo/npm installs | high | mitigate | Wave 0 adds workspace member only; no new npm packages yet |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (07-00-T1): octanest-git + API + dialect Wave 0 stubs</name>
  <files>Cargo.toml, crates/octanest-git/Cargo.toml, crates/octanest-git/src/lib.rs, crates/octanest-git/src/version.rs, crates/octanest-api/tests/repo_create.rs, crates/octanest-api/tests/repo_private_404.rs, crates/octanest-api/tests/repo_branch_soft_protect.rs, crates/octanest-api/tests/repo_fs_layout.rs, crates/octanest-db/tests/dialect_repositories.rs</files>
  <read_first>.planning/phases/07-git-repos-browse/07-VALIDATION.md, .planning/phases/07-git-repos-browse/07-RESEARCH.md, crates/octanest-api/tests/auth_verify_gate.rs, crates/octanest-db/tests/dialect_auth.rs, Cargo.toml, crates/octanest-api/Cargo.toml</read_first>
  <action>
Add workspace member `crates/octanest-git` with minimal lib exporting placeholders for `assert_git_version` / version parse tests named so filters `git_version_gate` and `git_archive_formats` match (tests may `#[ignore]` or `assert!(false, "Wave 0")` until later plans). Mirror Phase 06 Wave 0 style: API integration tests under `crates/octanest-api/tests/repo_*.rs` using existing test support patterns — cover verified create happy-path stub, unverified → auth.email_unverified, duplicate name stable code, private non-owner → repo.not_found, default-branch rename/delete blocked, bare path `{repos_dir}/{owner}/{name}.git` (D-30, D-33, GIT-01/05/06/07/08/09). Add `dialect_repositories.rs` expecting tri-dialect `0007_repositories` (+ user default_branch / instance default_visibility columns per RESEARCH). Do not implement CliGitBackend or repo RPC handlers here — stubs only. Do not amend REQUIREMENTS wording here (plan 01).
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(repo_create) | test(repo_private) | test(repo_branch) | test(repo_fs)' ; cargo test -p octanest-git --lib || true</automated>
    <fails_when>stub test files missing from crates/octanest-api/tests/; or cargo cannot resolve package octanest-git after workspace member add; or all repo_* tests pass green without Wave 0 RED/ignore markers</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f crates/octanest-git/src/lib.rs && test -f crates/octanest-git/src/version.rs`
    - `rg -n 'repo_create|email_unverified|not_found' crates/octanest-api/tests/repo_create.rs crates/octanest-api/tests/repo_private_404.rs`
    - `rg -n '0007_repositories|default_branch|default_visibility' crates/octanest-db/tests/dialect_repositories.rs`
    - `rg -n 'octanest-git' Cargo.toml`
  </acceptance_criteria>
  <done>Rust Wave 0 stubs and octanest-git member exist; filters discover RED/ignored tests</done>
</task>

<task type="auto">
  <name>Task 2 (07-00-T2): Web Wave 0 stubs for /new and home CTA</name>
  <files>apps/web/src/routes/new.integration.test.ts, apps/web/src/components/signed-in-home.integration.test.ts</files>
  <read_first>apps/web/src/routes/setup.integration.test.ts, apps/web/src/components/signed-in-home.tsrx, apps/web/src/routes/index.integration.test.ts, .planning/phases/07-git-repos-browse/07-UI-SPEC.md</read_first>
  <action>
Add Vitest integration stubs (Phase 06 pattern) asserting: unverified session on `/new` shows verify wall copy (UI-SPEC Unverified `/new`); verified enabled **New repository** navigates to `/new` (D-01, D-11); disabled CTA + hint when unverified on SignedInHome. Tests may fail until routes exist — file must exist and be runnable by vitest discover. Do not implement production `.tsrx` routes here.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/new.integration.test.ts src/components/signed-in-home.integration.test.ts</automated>
    <fails_when>vitest cannot find either stub file; or exit 0 with all green while routes still absent (Wave 0 expects RED until later plans)</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/routes/new.integration.test.ts`
    - `test -f apps/web/src/components/signed-in-home.integration.test.ts`
    - `rg -n 'Verify your email|New repository|/new' apps/web/src/routes/new.integration.test.ts apps/web/src/components/signed-in-home.integration.test.ts`
  </acceptance_criteria>
  <done>Web Wave 0 stubs discoverable by vitest</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `crates/octanest-git` workspace member scaffold | 07-00 | GIT-09/10 |
| `repo_*.rs` / `dialect_repositories.rs` stubs | 07-00 | Nyquist |
| `new.integration.test.ts` / `signed-in-home.integration.test.ts` | 07-00 | D-01/D-11 |

<verification>
Wave 0 files exist; VALIDATION checklist items for stubs addressable by later plans.
</verification>

<success_criteria>
All Phase 7 requirement IDs have failing automated stub targets before green implementation.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-00-SUMMARY.md` when done
</output>
