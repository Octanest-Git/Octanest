---
phase: 07-git-repos-browse
plan: "07"
type: execute
wave: 7
depends_on:
  - "05"
  - "06"
files_modified:
  - crates/octanest-git/src/backend.rs
  - crates/octanest-git/src/cli.rs
  - crates/octanest-api/src/repo/mod.rs
  - crates/octanest-api/src/rpc.rs
  - apps/web/src/routes/$owner.$repo.branches.tsrx
  - apps/web/src/routes/$owner.$repo.tags.tsrx
  - apps/web/src/components/repo
  - apps/web/src/components/ui/dialog.tsx
  - apps/web/src/components/ui/alert-dialog.tsx
  - crates/octanest-api/tests/repo_branch_soft_protect.rs
  - packages/api-client
requirements:
  - GIT-06
  - GIT-05
autonomous: true
user_setup: []
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Owner can create/rename/delete branches from UI; non-owner read-only (D-27, GIT-06)"
    - "Default branch rename/delete controls disabled with soft-protect copy (D-28)"
    - "Tags list + archive download actions; no create-tag UI (UI-SPEC lock)"
    - "UI E6 empty/loading/error/populated/partial states honored"
  artifacts:
    - path: apps/web/src/routes/$owner.$repo.branches.tsrx
      provides: "Branch management UI"
    - path: crates/octanest-api/tests/repo_branch_soft_protect.rs
      provides: "Soft-protect + owner ACL tests"
  key_links:
    - from: apps/web/src/routes/$owner.$repo.branches.tsrx
      to: crates/octanest-api/src/rpc.rs
      via: "repo.branch_*"
  assumptions:
    - "ASSUME (GIT-06 idempotency/concurrency): duplicate create and concurrent mutate return stable errors without corrupting refs"
  prohibitions:
    - statement: "Must not allow non-owners to mutate branches"
      flagged: true
    - statement: "Must not allow delete or rename of the repository default branch from Phase 7 UI/API"
      flagged: true
---

<objective>
Branch CRUD (owner-only, soft-protect default) and tags list surface (GIT-06 + tags half of GIT-05).

Purpose: Mutable refs where permitted without full branch-protection product.
Output: Green soft-protect tests; Branches/Tags pages.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-UI-SPEC.md
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@.planning/phases/07-git-repos-browse/07-05-SUMMARY.md
@.planning/phases/07-git-repos-browse/07-06-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner session → branch mutate | Non-owners must not mutate |
| Branch name → git argv | Injection via ref names |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-19 | Elevation of privilege | branch mutate | high | mitigate | Server checks caller is owner; UI hides actions for non-owners (D-27) |
| T-07-20 | Tampering | default branch | medium | mitigate | API rejects rename/delete of default branch even if UI bypassed (D-28) |
| T-07-17 | Tampering | branch names | high | mitigate | validate ref names; argv arrays only |
| T-07-SC | Tampering | shadcn dialog | medium | mitigate | Official shadcn Base UI only — no third-party registries |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (07-07-T1): branch create/rename/delete + soft-protect API</name>
  <files>crates/octanest-git/src/backend.rs, crates/octanest-git/src/cli.rs, crates/octanest-api/src/repo/mod.rs, crates/octanest-api/src/rpc.rs, crates/octanest-api/tests/repo_branch_soft_protect.rs, packages/api-client</files>
  <read_first>crates/octanest-git/src/backend.rs, crates/octanest-api/src/repo/acl.rs, .planning/phases/07-git-repos-browse/07-RESEARCH.md</read_first>
  <behavior>
    - owner create branch from start ref succeeds
    - non-owner mutate → not_found or auth error without leaking
    - rename/delete default branch → stable soft-protect error
  </behavior>
  <action>
Implement `branch_create`, `branch_rename`, `branch_delete` on CliGitBackend. RPC requires owner (D-27). Block rename/delete when name equals repo default_branch (D-28). List branches/tags via list_refs. Green `repo_branch_soft_protect`. rpc-gen.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(repo_branch)'</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'branch_create|branch_rename|branch_delete|default_branch' crates/octanest-api/src/repo/ crates/octanest-git/src/`
    - nextest repo_branch filter exits 0
  </acceptance_criteria>
  <done>Branch mutate API enforces owner + soft-protect</done>
</task>

<task type="auto">
  <name>Task 2 (07-07-T2): Branches / Tags UI + delete confirm dialog</name>
  <files>apps/web/src/routes/$owner.$repo.branches.tsrx, apps/web/src/routes/$owner.$repo.tags.tsrx, apps/web/src/components/repo, apps/web/src/components/ui/dialog.tsx, apps/web/src/components/ui/alert-dialog.tsx</files>
  <read_first>.planning/phases/07-git-repos-browse/07-UI-SPEC.md, apps/web/components.json</read_first>
  <action>
Add Branches page: list name/tip/updated; owner New branch, rename, delete; default soft-protect disabled controls with title "Default branch can’t be renamed or deleted here."; AlertDialog confirm typing branch name; destructive Delete branch. Tags page: list + archive download actions only — no create tag UI. Add official shadcn Dialog/AlertDialog if missing. Link-row nav. Titles per UI-SPEC.
  </action>
  <verify>
    <automated>bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'New branch|Default branch can.t be renamed|Delete branch' apps/web/src/routes/`
    - `test -f apps/web/src/routes/\$owner.\$repo.branches.tsrx`
    - Tags route has no create-tag primary CTA
  </acceptance_criteria>
  <done>GIT-06 UI complete; tags list shipped</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| branch_* GitBackend + RPC | 07-07 | GIT-06 |
| Branches/Tags pages | 07-07 | D-27/D-28 |

<verification>
repo_branch nextest + web build green.
</verification>

<success_criteria>
Owner branch CRUD with soft-protect; tags list only.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-07-SUMMARY.md` when done
</output>
