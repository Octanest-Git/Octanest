---
phase: 07-git-repos-browse
plan: "08"
type: execute
wave: 8
depends_on:
  - "05"
  - "07"
files_modified:
  - crates/octanest-git/src/backend.rs
  - crates/octanest-git/src/cli.rs
  - crates/octanest-api/src/routes/repo_raw.rs
  - crates/octanest-api/src/app.rs
  - apps/web/src/components/repo/clone-box.tsrx
  - apps/web/src/routes/$owner.$repo.index.tsrx
  - crates/octanest-api/tests/repo_archive.rs
requirements:
  - GIT-07
autonomous: true
user_setup: []
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "User can download zip and tar.gz archives for current ref via HTTP after ACL (D-29, GIT-07)"
    - "Clone box shows HTTPS URL + Copy HTTPS URL, SSH placeholder, Download ZIP / tar.gz (D-22)"
    - "Empty repos disable/hide archive downloads (GIT-07 empty edge)"
    - "UI E12 copy HTTPS success/error/overflow behaviors honored"
  artifacts:
    - path: crates/octanest-api/src/routes/repo_raw.rs
      provides: "Archive streaming GET"
    - path: apps/web/src/components/repo/clone-box.tsrx
      provides: "Clone/Download dropdown"
  key_links:
    - from: apps/web/src/components/repo/clone-box.tsrx
      to: crates/octanest-api/src/routes/repo_raw.rs
      via: "GET archive URLs"
  assumptions:
    - "ASSUME (GIT-07 empty): archive endpoints return structured 404/409-style empty when no commits; UI disables items"
  prohibitions:
    - statement: "Must not return archive bytes through JSON RPC"
      flagged: true
---

<objective>
Ship source archives (zip + tar.gz) over streaming HTTP and wire the Code clone/download box (GIT-07, D-22, D-29).

Purpose: Downloadable refs without PAT/SSH (those later phases).
Output: Archive routes + clone box; green archive tests.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-UI-SPEC.md
@.planning/phases/07-git-repos-browse/07-RESEARCH.md
@.planning/phases/07-git-repos-browse/07-05-SUMMARY.md
@crates/octanest-api/src/routes/avatar.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → archive GET | Cookie/session ACL must match browse |
| git archive stdout → HTTP body | Stream only; do not buffer unbounded without timeout |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-21 | Information disclosure | archive ACL | high | mitigate | Same acl.rs as browse before spawning git archive |
| T-07-22 | Denial of service | large archive | medium | mitigate | Stream with timeout; only zip/tar.gz formats |
| T-07-SC | Tampering | packages | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (07-08-T1): git archive streaming HTTP</name>
  <files>crates/octanest-git/src/backend.rs, crates/octanest-git/src/cli.rs, crates/octanest-api/src/routes/repo_raw.rs, crates/octanest-api/src/app.rs, crates/octanest-api/tests/repo_archive.rs</files>
  <read_first>crates/octanest-api/src/routes/avatar.rs, crates/octanest-api/src/repo/acl.rs, .planning/phases/07-git-repos-browse/07-RESEARCH.md</read_first>
  <behavior>
    - zip and tar.gz bytes non-empty for seeded ref
    - private non-owner → not found
    - empty repo → structured failure not 500 panic
  </behavior>
  <action>
Add `archive` on GitBackend using `git archive --format=zip|tar.gz --prefix=name/`. Expose `GET /api/repos/{owner}/{repo}/archive/{ref}.zip` and `.tar.gz` (or equivalent stable paths) streaming stdout after ACL. Add `repo_archive` tests (may live as nextest filter `repo_archive` / `git_archive_formats`). Never put archives in RPC JSON.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(repo_archive) | test(git_archive)' ; cargo nextest run -p octanest-git -E 'test(git_archive)'</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'archive|--format' crates/octanest-git/src/cli.rs`
    - `rg -n 'archive' crates/octanest-api/src/routes/repo_raw.rs`
  </acceptance_criteria>
  <done>ZIP and tar.gz downloadable via ACL’d HTTP</done>
</task>

<task type="auto">
  <name>Task 2 (07-08-T2): Clone / Download box on Code home</name>
  <files>apps/web/src/components/repo/clone-box.tsrx, apps/web/src/routes/$owner.$repo.index.tsrx</files>
  <read_first>.planning/phases/07-git-repos-browse/07-UI-SPEC.md, apps/web/src/routes/$owner.$repo.index.tsrx</read_first>
  <action>
Implement Clone/Download dropdown per D-22/D-29: HTTPS URL (instance host + /{owner}/{repo}.git or documented clone path placeholder until Phase 8 auth), Copy HTTPS URL control with Copied / couldn’t-copy errors (E12), SSH muted placeholder “SSH cloning arrives in a later phase.”, Download ZIP / Download tar.gz for current ref disabled when empty. Wire into Code toolbar.
  </action>
  <verify>
    <automated>bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0</fails_when>
    <human-check>On Code tab, confirm HTTPS shown, SSH placeholder, archive menu items present.</human-check>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Copy HTTPS URL|Download ZIP|Download tar.gz|SSH cloning arrives' apps/web/src/components/repo/clone-box.tsrx`
  </acceptance_criteria>
  <done>Clone box matches UI-SPEC</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `git archive` HTTP routes | 07-08 | GIT-07 |
| `clone-box.tsrx` | 07-08 | D-22 |

<verification>
archive tests + build green.
</verification>

<success_criteria>
GIT-07 satisfied; clone UX shows HTTPS + archives.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-08-SUMMARY.md` when done
</output>
