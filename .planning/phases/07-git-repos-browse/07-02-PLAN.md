---
phase: 07-git-repos-browse
plan: "02"
type: execute
wave: 2
depends_on:
  - "00"
  - "01"
files_modified:
  - crates/octanest-git/src/lib.rs
  - crates/octanest-git/src/backend.rs
  - crates/octanest-git/src/cli.rs
  - crates/octanest-git/src/version.rs
  - crates/octanest-git/Cargo.toml
  - crates/octanest-core/src/auth_types.rs
  - crates/octanest-core/src/repo_types.rs
  - crates/octanest-core/src/lib.rs
  - crates/octanest-db/migrations/postgres/0007_repositories.sql
  - crates/octanest-db/migrations/mysql/0007_repositories.sql
  - crates/octanest-db/migrations/sqlite/0007_repositories.sql
  - crates/octanest-db/src/repositories.rs
  - crates/octanest-db/src/lib.rs
  - crates/octanest-api/src/git/mod.rs
  - crates/octanest-api/src/repo/mod.rs
  - crates/octanest-api/src/rpc.rs
  - crates/octanest-api/src/app.rs
  - crates/octanest-api/src/main.rs
  - crates/octanest-api/Dockerfile
  - docker-compose.yml
  - .env.example
  - apps/web/src/routes/new.tsrx
  - apps/web/src/routes/$owner.$repo.index.tsrx
  - apps/web/src/components/signed-in-home.tsrx
  - crates/octanest-api/tests/repo_create.rs
  - crates/octanest-api/tests/repo_fs_layout.rs
requirements:
  - GIT-01
  - GIT-08
  - GIT-09
  - GIT-10
autonomous: false
user_setup: []
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "Verified user can create an empty public repository via repo.create and land on /{owner}/{repo} Quick setup (D-01, D-10, D-14, GIT-01)"
    - "Bare repo exists at {OCTANEST_REPOS_DIR}/{owner}/{name}.git with HEAD symbolic-ref to default branch (D-09, D-30, D-31, GIT-08)"
    - "API boot exits 1 when git missing or version older than 2.5.0 (D-33, GIT-09)"
    - "AppState holds Arc<dyn GitBackend> implemented only by CliGitBackend; module docs mention future GixGitBackend (D-32, GIT-10)"
    - "Dockerfile installs git; Compose binds ./var/repos to /var/repos (D-30, D-38)"
    - "RESERVED_USERNAMES includes new; validate_repo_name allows hyphen underscore period (D-06, D-14)"
    - "Unverified repo.create returns auth.email_unverified (D-11)"
  artifacts:
    - path: crates/octanest-git/src/backend.rs
      provides: "GitBackend trait + GitError"
    - path: crates/octanest-git/src/cli.rs
      provides: "CliGitBackend init_bare via argv"
    - path: crates/octanest-api/src/repo/mod.rs
      provides: "repo.create RPC"
    - path: apps/web/src/routes/new.tsrx
      provides: "/new create form (minimal tracer)"
  key_links:
    - from: crates/octanest-api/src/main.rs
      to: crates/octanest-git/src/version.rs
      via: "assert_git_version → exit(1)"
    - from: crates/octanest-api/src/repo/mod.rs
      to: crates/octanest-git/src/cli.rs
      via: "init_bare after DB insert"
    - from: apps/web/src/routes/new.tsrx
      to: crates/octanest-api/src/rpc.rs
      via: "repo.create"
  assumptions:
    - "ASSUME (GIT-01 empty): tracer creates with name only; description null/empty OK; templates deferred to 07-03"
    - "RESOLVED (07-01): human selected owner_repo_path and fail_boot_git"
  prohibitions:
    - statement: "Must not shell git through sh -c or string-concatenated shell lines"
      flagged: true
    - statement: "Must not implement GixGitBackend body in this phase"
      flagged: true
---

<objective>
Tracer: one production-quality path — verified user creates an empty public repo (DB + bare git on volume path) and opens `/{owner}/{repo}` Quick setup — proving GitBackend CLI, boot gate, and URL scheme end-to-end.

Purpose: Catch architecture dead-ends before browse/template expansion.
Output: Green repo_create + repo_fs_layout + git_version_gate for the happy path; minimal `/new` + Code empty state.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@.planning/phases/07-git-repos-browse/07-RESEARCH.md
@.planning/phases/07-git-repos-browse/07-PATTERNS.md
@.planning/phases/07-git-repos-browse/07-UI-SPEC.md
@.planning/phases/07-git-repos-browse/07-00-SUMMARY.md
@.planning/phases/07-git-repos-browse/07-01-SUMMARY.md
@crates/octanest-api/src/routes/avatar.rs
@crates/octanest-api/src/email/mod.rs
@crates/octanest-api/src/auth/gate.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → repo.create | Untrusted name/visibility; require_verified |
| API → filesystem repos_dir | Path must stay under canonical root |
| API → git argv | User-influenced name/ref must not reach a shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Elevation of privilege | repo.create | high | mitigate | require_verified before create (D-11); bootstrap lock still blocks while needs_setup |
| T-07-06 | Tampering | CliGitBackend | high | mitigate | tokio::process::Command argv arrays only; validate_repo_name before path join |
| T-07-07 | Tampering | repos_dir paths | high | mitigate | Canonicalize under OCTANEST_REPOS_DIR; mirror avatar basename guards |
| T-07-08 | Denial of service | missing git | high | mitigate | Boot assert_git_version((2,5,0)) exit 1 (D-33) |
| T-07-SC | Tampering | apt git in Dockerfile | medium | accept | Distro git package; no new crates.io deps beyond workspace member |
</threat_model>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 0 (07-02-T0): Confirm proceed with D-14/D-33 choices from 07-01</name>
  <decision>
Proceed to implement the one-way doors already selected in 07-01: public URLs `/{owner}/{repo}` (D-14) and fail-boot when system git is missing or older than 2.5 (D-33).
  </decision>
  <context>
Structure requires a same-plan checkpoint before the one-way tracer task. 07-01 recorded the option IDs; this gate confirms the executor may walk through those doors now.
  </context>
  <options>
    <option id="proceed_locked">
      <title>Proceed with owner_repo_path + fail_boot_git (recommended)</title>
      <pros>Honors locked CONTEXT + 07-01 decisions</pros>
      <cons>None if 07-01 already confirmed</cons>
    </option>
    <option id="stop">
      <title>Stop — revisit 07-01</title>
      <pros>Safe if decisions were not actually confirmed</pros>
      <cons>Blocks tracer</cons>
    </option>
  </options>
  <resume-signal>Reply with proceed_locked (recommended) or stop</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1 (07-02-T1): Migration 0007 + core validators + DB repositories API</name>
  <reversibility rating="costly">Bare layout + repositories table uniqueness become storage contracts (D-06, D-30).</reversibility>
  <files>crates/octanest-db/migrations/postgres/0007_repositories.sql, crates/octanest-db/migrations/mysql/0007_repositories.sql, crates/octanest-db/migrations/sqlite/0007_repositories.sql, crates/octanest-db/src/repositories.rs, crates/octanest-db/src/lib.rs, crates/octanest-core/src/repo_types.rs, crates/octanest-core/src/auth_types.rs, crates/octanest-core/src/lib.rs, crates/octanest-db/tests/dialect_repositories.rs</files>
  <read_first>crates/octanest-db/migrations/postgres/0006_bootstrap_flags.sql, crates/octanest-db/src/users.rs, crates/octanest-core/src/auth_types.rs, .planning/phases/07-git-repos-browse/07-PATTERNS.md, docs/database.md</read_first>
  <behavior>
    - migration_parity finds 0007_repositories on all three dialects
    - validate_repo_name accepts my_app and rejects reserved/empty
    - insert_repository + get by owner+name for non-deleted rows
  </behavior>
  <action>
Add tri-dialect `0007_repositories` (owner_id FK, name, visibility, description, default_branch, soft-delete timestamp, updated_at, unique among non-deleted). Add columns for user `default_branch` and instance `default_visibility` (D-08, D-09) via same migration or paired ALTERs matching 0006 style. Implement `repositories.rs` dialect match helpers + Database facade methods. Add `validate_repo_name` (D-06) separate from username; extend `RESERVED_USERNAMES` with `new` and other flat routes from UI-SPEC (D-14). Export DTOs in `repo_types.rs`. Turn dialect_repositories stub toward green for schema presence. Do not wire RPC or git yet.
  </action>
  <verify>
    <automated>cargo test -p octanest-core --lib && cargo test -p octanest-db --lib migration_parity && cargo test -p octanest-db --test dialect_repositories</automated>
    <fails_when>exit code != 0; or migration_parity missing 0007; or validate_repo_name absent from core</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f crates/octanest-db/migrations/postgres/0007_repositories.sql`
    - `rg -n 'validate_repo_name' crates/octanest-core/src/`
    - `rg -n '"new"' crates/octanest-core/src/auth_types.rs`
    - `rg -n 'insert_repository|RepositoryRow' crates/octanest-db/src/repositories.rs`
  </acceptance_criteria>
  <done>DB + validators ready for tracer create</done>
</task>

<task type="tracer">
  <name>Task 2 (07-02-T2): End-to-end empty public create via GitBackend CLI</name>
  <reversibility rating="one-way">Fail-boot on missing/old git (D-33); public /{owner}/{repo} routes (D-14).</reversibility>
  <precondition>07-01 human selected owner_repo_path and fail_boot_git; 07-02-T0 proceed_locked; 07-02-T1 migrations and validate_repo_name exist</precondition>
  <files>crates/octanest-git/src/backend.rs, crates/octanest-git/src/cli.rs, crates/octanest-git/src/version.rs, crates/octanest-git/src/lib.rs, crates/octanest-api/src/git/mod.rs, crates/octanest-api/src/repo/mod.rs, crates/octanest-api/src/rpc.rs, crates/octanest-api/src/app.rs, crates/octanest-api/src/main.rs, crates/octanest-api/Dockerfile, docker-compose.yml, .env.example, crates/octanest-api/tests/repo_create.rs, crates/octanest-api/tests/repo_fs_layout.rs</files>
  <read_first>crates/octanest-api/src/email/mod.rs, crates/octanest-api/src/routes/avatar.rs, crates/octanest-api/src/main.rs, crates/octanest-api/Dockerfile, docker-compose.yml, .planning/phases/07-git-repos-browse/07-RESEARCH.md</read_first>
  <action>
Wire ONE happy path through every layer (D-07 optional description field on create; D-30–D-33, D-05, D-08 default public if unset, D-09 main, GIT-01/08/09/10):

1. Implement GitBackend + CliGitBackend with at least init_bare using git init --bare then git symbolic-ref HEAD refs/heads/{branch} (no reliance on --initial-branch). Argv only. Document future GixGitBackend in module rustdoc (D-32).
2. assert_git_version((2,5,0)) in API boot; missing/old → eprintln + exit(1) (D-33). Install git in crates/octanest-api/Dockerfile. Compose volume ./var/repos:/var/repos; OCTANEST_REPOS_DIR default var/repos on AppState like uploads (D-31, D-38). Document in .env.example.
3. repo.create: require_verified; owner = current user; validate name; optional description; insert row; init_bare under {repos_dir}/{owner_username}/{name}.git; stable duplicate error code for inline UI later; visibility public/private; return DTO with owner/name.
4. Register RPC; hold Arc dyn GitBackend on AppState as CliGitBackend only. Green repo_create verified-create + repo_fs_layout + git_version_gate unit tests for this path. Skip templates, browse tree, archives, branch CRUD (later plans).
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(repo_create) | test(repo_fs)' ; cargo nextest run -p octanest-git -E 'test(git_version_gate)'</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"; or create succeeds without a bare .git directory under repos_dir</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'trait GitBackend' crates/octanest-git/src/backend.rs`
    - `rg -n 'CliGitBackend|init_bare|symbolic-ref' crates/octanest-git/src/cli.rs`
    - `rg -n 'assert_git_version|exit\(1\)' crates/octanest-api/src/main.rs crates/octanest-git/src/version.rs`
    - `rg -n 'repo\.create' crates/octanest-api/src/rpc.rs`
    - `rg -n 'git' crates/octanest-api/Dockerfile`
    - `rg -n 'var/repos|OCTANEST_REPOS_DIR' docker-compose.yml .env.example crates/octanest-api/src/app.rs`
  </acceptance_criteria>
  <done>Empty public create works end-to-end under tests with CLI backend and boot gate</done>
</task>

<task type="auto">
  <name>Task 3 (07-02-T3): Minimal /new + empty Code Quick setup route</name>
  <reversibility rating="costly">Home CTA → /new and /{owner}/{repo} empty IA (D-01, D-13 stub, D-14).</reversibility>
  <files>apps/web/src/routes/new.tsrx, apps/web/src/routes/$owner.$repo.index.tsrx, apps/web/src/components/signed-in-home.tsrx, packages/api-client, apps/web/src/routes/new.integration.test.ts</files>
  <read_first>apps/web/src/components/signed-in-home.tsrx, apps/web/src/routes/signup.tsrx, apps/web/src/lib/session-queries.ts, .planning/phases/07-git-repos-browse/07-UI-SPEC.md, .agents/skills/octane/SKILL.md</read_first>
  <action>
After `make rpc-gen` for `repo.create`: add Octane `/new` with owner read-only, name, optional description, Public/Private, Create repository CTA (templates can be None-only placeholders for tracer). Success navigates to `/{owner}/{repo}`. Unverified: verify wall per UI-SPEC (D-11). Wire SignedInHome **New repository** to `/new` with disabled+hint when unverified (D-01, D-11). Empty Code home shows Quick setup first-push guide (D-10, D-15) — no tree yet. Author `.tsrx` Rivet only. Turn new.integration.test stubs green for wall + CTA navigation as covered by this minimal surface.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/new.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAIL"; or build errors on new.tsrx / $owner.$repo</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/routes/new.tsrx`
    - `rg -n 'Create a new repository|Create repository|Verify your email' apps/web/src/routes/new.tsrx`
    - `rg -n 'Quick setup|/new' apps/web/src/routes/\$owner.\$repo.index.tsrx apps/web/src/components/signed-in-home.tsrx`
  </acceptance_criteria>
  <done>User can click New repository → create empty public repo → see Quick setup</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `GitBackend` / `CliGitBackend` / `assert_git_version` | 07-02 | GIT-09/10, D-32/33 |
| `0007_repositories` + `repositories.rs` | 07-02 | GIT-01/08 |
| `validate_repo_name` + reserved `new` | 07-02 | D-06/D-14 |
| `repo.create` RPC | 07-02 | GIT-01 |
| `OCTANEST_REPOS_DIR` + Compose/Dockerfile git | 07-02 | D-30/31/38 |
| `/new` + `/{owner}/{repo}` empty | 07-02 | D-01/D-10 |

<verification>
nextest create+fs+version green; vitest /new green; build passes.
</verification>

<success_criteria>
Tracer happy path: verified create → bare repo on volume path → Quick setup UI.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-02-SUMMARY.md` when done
</output>
