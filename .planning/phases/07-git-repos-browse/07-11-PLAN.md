---
phase: 07-git-repos-browse
plan: "11"
type: execute
wave: 15
depends_on:
  - "12"
  - "05"
  - "07"
  - "08"
  - "10"
  - "15"
files_modified:
  - docs/ARCHITECTURE.md
  - docs/CONFIGURATION.md
  - .planning/phases/07-git-repos-browse/07-VALIDATION.md
  - packages/api-client
requirements:
  - GIT-09
  - GIT-10
autonomous: true
user_setup: []
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "ARCHITECTURE documents GitBackend seam, CliGitBackend now, future GixGitBackend path (D-32, GIT-10)"
    - "VALIDATION.md task map updated with real plan IDs and nyquist fields ready for validate-phase"
    - "rpc-gen client includes all repo.* procedures used by the phase"
  artifacts:
    - path: docs/ARCHITECTURE.md
      provides: "GitBackend architecture section"
    - path: .planning/phases/07-git-repos-browse/07-VALIDATION.md
      provides: "Updated Nyquist map"
  key_links:
    - from: docs/ARCHITECTURE.md
      to: crates/octanest-git/src/backend.rs
      via: "GitBackend trait documentation"
  assumptions: []
  prohibitions:
    - statement: "Must not document gitoxide as the shipped Phase 7 primary backend"
      flagged: true
---

<objective>
Finalize architecture docs, refresh VALIDATION map, and rpc-gen smoke so GIT-09/GIT-10 documentation matches the CLI-first implementation.

Purpose: Close phase planning/docs loop for swap seam and operator clarity.
Output: ARCHITECTURE GitBackend section; VALIDATION updated; generated client in sync.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@.planning/phases/07-git-repos-browse/07-RESEARCH.md
@.planning/phases/07-git-repos-browse/07-VALIDATION.md
@docs/ARCHITECTURE.md
@docs/CONFIGURATION.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → operators/implementers | Misleading backend docs cause wrong deployments |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Tampering | ARCHITECTURE drift | medium | mitigate | Document CLI-primary + future gix explicitly (D-32) |
| T-07-SC | Tampering | packages | high | mitigate | No new packages; rpc-gen only |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (07-11-T1): ARCHITECTURE GitBackend + CONFIGURATION cross-check</name>
  <files>docs/ARCHITECTURE.md, docs/CONFIGURATION.md</files>
  <read_first>docs/ARCHITECTURE.md, docs/CONFIGURATION.md, crates/octanest-git/src/backend.rs, .planning/phases/07-git-repos-browse/07-CONTEXT.md</read_first>
  <action>
Add/update ARCHITECTURE section describing deep `GitBackend` module in `octanest-git`, shipped `CliGitBackend` (system git ≥2.5), documented future `GixGitBackend` when coverage allows (D-32, GIT-09, GIT-10). Note bare layout `{OCTANEST_REPOS_DIR}/{owner}/{name}.git`, ACL stub, archive HTTP vs RPC. Cross-check CONFIGURATION already has repos/git knobs from 07-10; fill any gaps only. Do not claim gitoxide is primary.
  </action>
  <verify>
    <automated>rg -n 'GitBackend|CliGitBackend|gitoxide|GixGitBackend|OCTANEST_REPOS_DIR' docs/ARCHITECTURE.md docs/CONFIGURATION.md</automated>
    <fails_when>ARCHITECTURE lacks GitBackend/CliGitBackend; or ARCHITECTURE still says Phase 7 is gitoxide-primary</fails_when>
  </verify>
  <acceptance_criteria>
    - ARCHITECTURE names CliGitBackend as current adapter and future gix path
    - CONFIGURATION still documents OCTANEST_REPOS_DIR and git floor
  </acceptance_criteria>
  <done>Architecture docs match D-32/GIT-10</done>
</task>

<task type="auto">
  <name>Task 2 (07-11-T2): VALIDATION map refresh + rpc-gen smoke</name>
  <files>.planning/phases/07-git-repos-browse/07-VALIDATION.md, packages/api-client</files>
  <read_first>.planning/phases/07-git-repos-browse/07-VALIDATION.md, crates/octanest-api/src/rpc.rs</read_first>
  <action>
Update 07-VALIDATION.md Per-Task Verification Map with real plan IDs 07-00…07-11 (replace TBD), mark Wave 0 requirements checked conceptually after execution history, leave nyquist_compliant false until validate-phase. Run `cargo run -p octanest-api --bin rpc-gen` (or `make rpc-gen`) and `bun --cwd apps/web run build` to ensure client includes repo.* procedures. Smoke: `cargo nextest run -p octanest-api -E 'test(repo_)'` and `cargo nextest run -p octanest-git --lib`.
  </action>
  <verify>
    <automated>cargo run -p octanest-api --bin rpc-gen && bun --cwd apps/web run build && cargo nextest run -p octanest-api -E 'test(repo_)' && cargo nextest run -p octanest-git --lib && rg -n '07-0[0-9]|07-1[01]' .planning/phases/07-git-repos-browse/07-VALIDATION.md | head -20</automated>
    <fails_when>exit code != 0; or VALIDATION still lists Plan TBD for all rows; or build/rpc-gen fails</fails_when>
  </verify>
  <acceptance_criteria>
    - VALIDATION map references concrete 07-XX plan IDs
    - Generated client builds with web
  </acceptance_criteria>
  <done>Docs/validation/client sealed for phase execute handoff</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| ARCHITECTURE GitBackend section | 07-11 | GIT-10 |
| Updated 07-VALIDATION.md | 07-11 | Nyquist |
| Regenerated api-client | 07-11 | rpc-codegen |

<verification>
ARCHITECTURE/CONFIGURATION rg; rpc-gen + build + repo_/git tests green.
</verification>

<success_criteria>
GIT-09/10 documentation complete; validation map executable.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-11-SUMMARY.md` when done
</output>
