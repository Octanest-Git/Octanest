---
phase: 07-git-repos-browse
plan: "13"
type: execute
wave: 4
depends_on:
  - "12"
files_modified:
  - apps/web/src/routes/new.tsrx
  - apps/web/src/routes/$owner.$repo.index.tsrx
  - apps/web/src/components/signed-in-home.tsrx
  - apps/web/src/routes/new.integration.test.ts
  - packages/api-client
requirements:
  - GIT-01
autonomous: true
user_setup: []
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Verified user can open /new, create empty public repo, land on /{owner}/{repo} Quick setup (D-01, D-10, D-14)"
    - "Unverified /new shows verify wall; SignedInHome New repository disabled with hint (D-11)"
  artifacts:
    - path: apps/web/src/routes/new.tsrx
      provides: "/new create form (minimal tracer)"
    - path: apps/web/src/routes/$owner.$repo.index.tsrx
      provides: "Empty Code Quick setup"
  key_links:
    - from: apps/web/src/routes/new.tsrx
      to: crates/octanest-api/src/rpc.rs
      via: "repo.create after rpc-gen"
  assumptions:
    - "ASSUME (GIT-01 empty): templates None-only placeholders; full pickers in 07-03"
  prohibitions:
    - statement: "Must not implement React instead of Octane .tsrx for these routes"
      flagged: true
---

<objective>
Minimal Octane `/new` + empty Code Quick setup so the create tracer is user-reachable end-to-end.

Purpose: Close the UI half of the create path after 07-12 API tracer.
Output: Green new.integration.test + web build for /new and empty Code home.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-UI-SPEC.md
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@.planning/phases/07-git-repos-browse/07-12-SUMMARY.md
@apps/web/src/components/signed-in-home.tsrx
@.agents/skills/octane/SKILL.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /new | Unverified sessions must not create |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05b | Elevation of privilege | /new UI | medium | mitigate | Verify wall + disabled CTA; server still require_verified (D-11) |
| T-07-SC | Tampering | packages | high | mitigate | rpc-gen only; no new npm packages |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (07-13-T1): Minimal /new + empty Code Quick setup route</name>
  <reversibility rating="costly">Home CTA → /new and /{owner}/{repo} empty IA (D-01, D-13 stub, D-14).</reversibility>
  <precondition>07-12 repo.create RPC green and rpc-genable</precondition>
  <files>apps/web/src/routes/new.tsrx, apps/web/src/routes/$owner.$repo.index.tsrx, apps/web/src/components/signed-in-home.tsrx, packages/api-client, apps/web/src/routes/new.integration.test.ts</files>
  <read_first>apps/web/src/components/signed-in-home.tsrx, apps/web/src/routes/signup.tsrx, apps/web/src/lib/session-queries.ts, .planning/phases/07-git-repos-browse/07-UI-SPEC.md, .agents/skills/octane/SKILL.md</read_first>
  <action>
After `make rpc-gen` for `repo.create`: add Octane `/new` with owner read-only, name, optional description, Public/Private, Create repository CTA (templates can be None-only placeholders for tracer) (D-01, D-05, D-07). Success navigates to `/{owner}/{repo}` (D-14). Unverified: verify wall per UI-SPEC (D-11). Wire SignedInHome **New repository** to `/new` with disabled+hint when unverified (D-01, D-11). Empty Code home shows Quick setup first-push guide (D-10, D-15) — no tree yet. Author `.tsrx` Rivet only. Turn new.integration.test stubs green for wall + CTA navigation as covered by this minimal surface.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/new.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAIL"; or build errors on new.tsrx / $owner.$repo</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/routes/new.tsrx`
    - `rg -n 'Create a new repository|Create repository|Verify your email' apps/web/src/routes/new.tsrx`
    - `rg -n 'Quick setup|/new' apps/web/src/routes/\$owner.\$repo.index.tsrx apps/web/src/components/signed-in-home.tsrx`
  </acceptance_criteria>
  <done>User can click New repository → create empty public repo → see Quick setup</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `/new` + `/{owner}/{repo}` empty | 07-13 | D-01/D-10 |

<verification>
vitest /new green; build passes.
</verification>

<success_criteria>
UI create path closes the 07-12 tracer for humans.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-13-SUMMARY.md` when done
</output>
