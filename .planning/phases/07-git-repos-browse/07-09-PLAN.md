---
phase: 07-git-repos-browse
plan: "09"
type: execute
wave: 9
depends_on:
  - "04"
  - "05"
  - "08"
files_modified:
  - apps/web/src/routes/$owner.$repo.settings.tsrx
  - crates/octanest-api/src/repo/mod.rs
  - crates/octanest-api/src/rpc.rs
  - crates/octanest-db/src/repositories.rs
  - apps/web/src/components/ui/radio-group.tsx
  - apps/web/src/components/ui/badge.tsx
  - packages/api-client
requirements:
  - GIT-01
  - GIT-08
autonomous: true
user_setup: []
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Owner can toggle public/private in repo settings; non-owner gets 404 (D-26)"
    - "Owner can soft-delete repository (DB deleted_at); disk purge deferred async (D-35)"
    - "Soft-delete confirm requires typing repo name; copy matches UI-SPEC E11"
    - "Visibility save shows Visibility updated toast or equivalent (UI E8)"
  artifacts:
    - path: apps/web/src/routes/$owner.$repo.settings.tsrx
      provides: "Visibility + danger zone"
    - path: crates/octanest-api/src/repo/mod.rs
      provides: "repo.update_visibility / repo.soft_delete"
  key_links:
    - from: apps/web/src/routes/$owner.$repo.settings.tsrx
      to: crates/octanest-api/src/rpc.rs
      via: "visibility + delete RPCs"
  assumptions:
    - "ASSUME (D-35 discretion): soft-delete retention default 14 days until purge job in 07-10"
  prohibitions:
    - statement: "Must not hard-delete bare repo directories synchronously on soft-delete in this plan"
      flagged: true
---

<objective>
Repo settings: visibility toggle and soft-delete danger zone (D-26, D-35).

Purpose: Owner control before org collaborators (Phase 10).
Output: Settings route + RPCs; list/home hide soft-deleted.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-UI-SPEC.md
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@.planning/phases/07-git-repos-browse/07-04-SUMMARY.md
@.planning/phases/07-git-repos-browse/07-05-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner → visibility/delete | Non-owner must 404 |
| Soft-delete → list/browse | Deleted repos disappear from product surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-23 | Elevation of privilege | settings RPCs | high | mitigate | Owner-only; non-owner unified not_found |
| T-07-24 | Tampering | soft-delete confirm | medium | mitigate | Require typed repo name match server-side |
| T-07-SC | Tampering | shadcn radio/badge | medium | mitigate | Official registry only |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (07-09-T1): update_visibility + soft_delete RPC</name>
  <files>crates/octanest-api/src/repo/mod.rs, crates/octanest-db/src/repositories.rs, crates/octanest-api/src/rpc.rs, packages/api-client</files>
  <read_first>crates/octanest-api/src/repo/acl.rs, crates/octanest-db/src/repositories.rs, .planning/phases/07-git-repos-browse/07-CONTEXT.md</read_first>
  <behavior>
    - owner flips private→public and reverse
    - non-owner update → not_found
    - soft_delete sets deleted_at; subsequent get/list omit row; disk still present
  </behavior>
  <action>
Add `repo.update_visibility` and `repo.soft_delete` (confirm name). Owner-only (D-26, D-35). Do not unlink bare dir here — purge job in 07-10. Ensure list_mine and ACL ignore soft-deleted. rpc-gen. Add/adjust api tests under repo_* filters.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(repo_)'</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'update_visibility|soft_delete|deleted_at' crates/octanest-api/src/repo/ crates/octanest-db/src/repositories.rs`
  </acceptance_criteria>
  <done>Visibility and soft-delete RPCs green</done>
</task>

<task type="auto">
  <name>Task 2 (07-09-T2): Settings UI visibility + delete dialog</name>
  <files>apps/web/src/routes/$owner.$repo.settings.tsrx, apps/web/src/components/ui/radio-group.tsx, apps/web/src/components/ui/badge.tsx</files>
  <read_first>.planning/phases/07-git-repos-browse/07-UI-SPEC.md, apps/web/src/routes/admin/auth.tsrx</read_first>
  <action>
Add `/{owner}/{repo}/settings` owner-only (else 404): Public/Private Switch or radios + helper for private=only you; **Save visibility** / toast Visibility updated (D-26, E8). Danger zone AlertDialog soft-delete: type repo name; copy about later disk purge; **Delete repository** (D-35, E11). Add Badge/Radio Group shadcn wrappers if still missing. Title Settings · owner/repo · Octanest.
  </action>
  <verify>
    <automated>bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Save visibility|Delete repository|Only you can see' apps/web/src/routes/\$owner.\$repo.settings.tsrx`
  </acceptance_criteria>
  <done>Owner settings UI complete</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `repo.update_visibility` / `repo.soft_delete` | 07-09 | D-26/D-35 |
| settings route | 07-09 | UI-SPEC |

<verification>
repo_ nextest + build green.
</verification>

<success_criteria>
Owners manage visibility and soft-delete without immediate disk wipe.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-09-SUMMARY.md` when done
</output>
