---
phase: 07-git-repos-browse
plan: "05"
type: execute
wave: 5
depends_on:
  - "02"
  - "04"
files_modified:
  - crates/octanest-git/src/backend.rs
  - crates/octanest-git/src/cli.rs
  - crates/octanest-api/src/git/mod.rs
  - crates/octanest-api/src/repo/mod.rs
  - crates/octanest-api/src/repo/acl.rs
  - crates/octanest-api/src/routes/repo_raw.rs
  - crates/octanest-api/src/app.rs
  - crates/octanest-api/src/rpc.rs
  - apps/web/src/routes/$owner.$repo.index.tsrx
  - apps/web/src/routes/$owner.$repo.tree.$.tsrx
  - apps/web/src/routes/$owner.$repo.blob.$.tsrx
  - apps/web/src/components/repo
  - apps/web/src/lib/markdown.ts
  - apps/web/src/lib/highlight.ts
  - apps/web/src/lib/grammars
  - apps/web/package.json
  - crates/octanest-api/tests/repo_private_404.rs
  - packages/api-client
requirements:
  - GIT-05
  - GIT-08
autonomous: true
user_setup: []
estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Code home shows file tree (dirs first) + sanitized GFM README when present; empty stays Quick setup (D-15, D-18)"
    - "Routes /tree/{ref}/… /blob/{ref}/… /raw/{ref}/… work; refs prefer branch/tag names (D-17)"
    - "Private/non-access returns identical 404 / repo.not_found; anonymous can read public (D-23, D-24, D-25)"
    - "Blob highlighting covers GitHub-class languages plus .tsrx and .ripple; line permalinks #L/#L-L; large-file soft limit; images inline else cannot preview (D-19, D-20)"
    - "Submodules appear as gitlink entries and support recursive browse when resolvable (D-21)"
    - "UI E3/E4 empty/loading/error/populated/partial states honored"
    - statement: "Long paths/descriptions ellipsis or wrap — held-out visual"
      verification: backstop
  artifacts:
    - path: apps/web/src/lib/markdown.ts
      provides: "remark-gfm + rehype-sanitize pipeline"
    - path: apps/web/src/lib/highlight.ts
      provides: "Shiki highlighter + tsrx/ripple grammars"
    - path: crates/octanest-api/src/repo/acl.rs
      provides: "Owner-only private + unified not_found"
  key_links:
    - from: crates/octanest-api/src/repo/mod.rs
      to: crates/octanest-git/src/cli.rs
      via: "ls_tree / cat_blob"
    - from: apps/web/src/routes/$owner.$repo.index.tsrx
      to: apps/web/src/lib/markdown.ts
      via: "README render"
  assumptions:
    - "ASSUME (GIT-05 concurrency): read-only browse; parallel OK"
    - "ASSUME (A2 RESEARCH): minimal TextMate grammars or TS alias with documented gap until grammars land — prefer real grammars in-repo"
  prohibitions:
    - statement: "Must not render unsanitized README HTML"
      flagged: true
    - statement: "Must not show private repository existence via distinct forbidden messaging"
      flagged: true
---

<objective>
Ship Code browse: ACL-safe tree/blob/raw, sanitized Markdown README, Shiki highlighting including `.tsrx`/`.ripple` (GIT-05 core).

Purpose: First readable forge surface after create.
Output: Green private_404 tests; Code/tree/blob UI; markdown+highlight libs.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-git-repos-browse/07-UI-SPEC.md
@.planning/phases/07-git-repos-browse/07-RESEARCH.md
@.planning/phases/07-git-repos-browse/07-PATTERNS.md
@.planning/phases/07-git-repos-browse/07-02-SUMMARY.md
@crates/octanest-api/src/routes/avatar.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous/auth → repo read | ACL stub private=owner-only |
| Repo blob/README → browser HTML | XSS via Markdown/HTML |
| HTTP raw path params → filesystem/git | Path traversal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-13 | Information disclosure | repo ACL | high | mitigate | Unified not_found for missing and unauthorized private (D-25) |
| T-07-14 | Tampering / XSS | markdown.ts | high | mitigate | rehype-sanitize after remark-rehype; no raw HTML passthrough |
| T-07-15 | Tampering | raw route | high | mitigate | Validate ref/path; resolve under repos_dir; avatar-style canonicalize |
| T-07-16 | Denial of service | huge blobs | medium | mitigate | Soft size limit + Download/View raw CTAs (D-20) |
| T-07-SC | Tampering | shiki/unified npm | high | mitigate | Install only RESEARCH OK packages at pinned versions |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (07-05-T1): GitBackend read APIs + ACL + raw HTTP</name>
  <reversibility rating="costly">Private ACL stub until Phase 10 (D-23).</reversibility>
  <files>crates/octanest-git/src/backend.rs, crates/octanest-git/src/cli.rs, crates/octanest-api/src/repo/acl.rs, crates/octanest-api/src/repo/mod.rs, crates/octanest-api/src/routes/repo_raw.rs, crates/octanest-api/src/app.rs, crates/octanest-api/src/rpc.rs, crates/octanest-api/tests/repo_private_404.rs, packages/api-client</files>
  <read_first>crates/octanest-git/src/backend.rs, crates/octanest-api/src/routes/avatar.rs, crates/octanest-api/src/auth/gate.rs, .planning/phases/07-git-repos-browse/07-RESEARCH.md</read_first>
  <behavior>
    - private repo as non-owner → repo.not_found same as missing
    - public anonymous read succeeds
    - ls_tree returns dirs/files/gitlink modes
  </behavior>
  <action>
Extend `GitBackend` with `ls_tree`, `cat_blob`, `list_refs` (as needed). Implement ACL helper: resolve owner+name; if missing OR private and caller≠owner → identical not_found (D-23–D-25). Add RPC procedures for tree/blob/readme metadata. Add `GET` raw blob route under `/api` (Traefik already routes `/api`) with cookie session ACL — not RPC JSON. Validate refs/paths; reject `..`. Green `repo_private_404`. Empty repo read returns structured empty (no 500).
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(repo_private) | test(repo_create)' ; cargo nextest run -p octanest-git --lib</automated>
    <fails_when>exit code != 0; or stdout/stderr contains "FAILED"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'not_found|private' crates/octanest-api/src/repo/acl.rs`
    - `rg -n 'ls_tree|cat_blob' crates/octanest-git/src/`
    - `rg -n 'repo_raw|raw' crates/octanest-api/src/routes/ crates/octanest-api/src/app.rs`
  </acceptance_criteria>
  <done>ACL-safe tree/blob/raw APIs green</done>
</task>

<task type="auto">
  <name>Task 2 (07-05-T2): Markdown sanitize + Shiki highlight libs</name>
  <files>apps/web/package.json, apps/web/src/lib/markdown.ts, apps/web/src/lib/highlight.ts, apps/web/src/lib/grammars</files>
  <read_first>.planning/phases/07-git-repos-browse/07-RESEARCH.md, apps/web/package.json</read_first>
  <action>
Add RESEARCH-pinned packages: `shiki`, `unified`, `remark-parse`, `remark-gfm`, `remark-rehype`, `rehype-sanitize`, `rehype-stringify` (all OK legitimacy). Implement `renderGfm` with sanitize last. Implement Shiki singleton with cool-biased github-light/dark themes; load custom grammars for `tsrx` and `ripple` under `src/lib/grammars/`. Unit-test sanitize strips script tags if easy; otherwise cover via integration in Task 3.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/lib/markdown.ts src/lib/highlight.ts 2>/dev/null || bun --cwd apps/web exec vitest run src/lib/markdown.test.ts src/lib/highlight.test.ts; bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or package.json missing shiki/rehype-sanitize; or build fails</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'rehype-sanitize|remark-gfm' apps/web/src/lib/markdown.ts apps/web/package.json`
    - `rg -n 'tsrx|ripple|createHighlighter' apps/web/src/lib/highlight.ts apps/web/src/lib/grammars`
  </acceptance_criteria>
  <done>Safe Markdown + highlight ready for Code UI</done>
</task>

<task type="auto">
  <name>Task 3 (07-05-T3): Code / tree / blob Octane routes</name>
  <files>apps/web/src/routes/$owner.$repo.index.tsrx, apps/web/src/routes/$owner.$repo.tree.$.tsrx, apps/web/src/routes/$owner.$repo.blob.$.tsrx, apps/web/src/components/repo, packages/api-client</files>
  <read_first>.planning/phases/07-git-repos-browse/07-UI-SPEC.md, apps/web/src/routes/$owner.$repo.index.tsrx, apps/web/src/lib/markdown.ts</read_first>
  <action>
Implement Code home + tree/blob per UI-SPEC (D-15–D-21): link-row nav Code active; ref Select; tree dirs first; README below; breadcrumbs; line numbers + `#L`/`#L-L` highlight wash; large-file notice + Download file / View raw file; binary image inline else cannot-preview copy; submodule icon + recursive navigation when resolvable; titles per Document titles table; skeletons. Private → Not found page same as missing. Omit Issues/PRs nav. Clone box can be stub until 07-07. Prefer official Badge/Breadcrumb if adding shadcn wrappers.
  </action>
  <verify>
    <automated>bun --cwd apps/web run build</automated>
    <fails_when>exit code != 0; or TypeScript/Octane errors on owner/repo routes</fails_when>
    <human-check>Open a seeded repo blob for .ts / .tsrx; confirm highlight + README sanitize (script attempt stripped).</human-check>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Quick setup|Directories|README|Page not found' apps/web/src/routes/ apps/web/src/components/repo/`
    - Routes exist for tree and blob under owner/repo
  </acceptance_criteria>
  <done>Users can browse files on public repos and get 404 on private non-owner</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `ls_tree` / `cat_blob` + ACL | 07-05 | GIT-05/D-25 |
| `repo_raw` HTTP | 07-05 | GIT-05 |
| `markdown.ts` / `highlight.ts` | 07-05 | D-18/D-19 |
| Code/tree/blob routes | 07-05 | D-15–D-21 |

<verification>
private_404 + git lib + web build green.
</verification>

<success_criteria>
GIT-05 file browse core complete with ACL and safe rendering.
</success_criteria>

<output>
Create `.planning/phases/07-git-repos-browse/07-05-SUMMARY.md` when done
</output>
