---
phase: 09-git-ssh
plan: "03"
type: tracer
wave: 2
depends_on:
  - "02"
files_modified:
  - crates/octanest-api/Cargo.toml
  - crates/octanest-api/src/ssh_keys/mod.rs
  - crates/octanest-api/src/ssh/mod.rs
  - crates/octanest-api/src/ssh/server.rs
  - crates/octanest-api/src/ssh/auth.rs
  - crates/octanest-api/src/ssh/pack.rs
  - crates/octanest-api/src/ssh/host_keys.rs
  - crates/octanest-api/src/rpc.rs
  - crates/octanest-api/src/app.rs
  - crates/octanest-api/src/main.rs
  - crates/octanest-api/tests/ssh_key_rpc.rs
  - crates/octanest-api/tests/git_ssh.rs
requirements:
  - GIT-03
  - GIT-04
autonomous: true
user_setup: []
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Verified user can sshKey.add with title + OpenSSH public key; list/revoke work; unverified add returns auth.email_unverified (D-SSH-05, GIT-04)"
    - "When OCTANEST_SSH_ENABLED, russh listens and accepts user git + registered pubkey; public repo git-upload-pack succeeds end-to-end (D-SSH-01, D-SSH-03, GIT-03)"
    - "SSH username other than git is rejected (D-SSH-03)"
  artifacts:
    - path: crates/octanest-api/src/ssh_keys/mod.rs
      provides: "sshKey.add / list / revoke RPC"
    - path: crates/octanest-api/src/ssh/server.rs
      provides: "russh Server + Handler"
    - path: crates/octanest-api/src/ssh/pack.rs
      provides: "git-upload-pack spawn bridge"
  key_links:
    - from: crates/octanest-api/src/ssh/auth.rs
      to: crates/octanest-db/src/ssh_keys.rs
      via: "fingerprint → find_by_fingerprint → user_id"
    - from: crates/octanest-api/src/ssh/pack.rs
      to: crates/octanest-api/src/git/mod.rs
      via: "bare_repo_path for owner/name.git"
  assumptions:
    - "ASSUME (discretion): russh 0.63 + ssh-key =0.7.0-rc.11 in-process; OCTANEST_SSH_ENABLED gates listener"
    - "ASSUME (A1): public fetch over SSH still requires registered key"
    - "PRECONDITION: 09-02 schema + D-SSH-02 confirm complete"
  prohibitions:
    - statement: "Must not use OpenSSH/git-shell AuthorizedKeysCommand"
      flagged: true
    - statement: "Must not apply PAT fine-grained scopes to SSH keys"
      flagged: true
    - statement: "Must not hand-edit packages/api-client (09-06 rpc-gen)"
      flagged: true
---

<objective>
Tracer: one production-quality path — verified user registers an SSH public key via session RPC, then clones/fetches a public repo over SSH with that key as user `git`.

Purpose: Catch architecture dead-ends (russh Handler channel lifetime, fingerprint lookup, pack stdio bridge, env enable gate) before ACL/ops/UI expansion. Keep key mint + SSH upload-pack in one plan so the slice is end-to-end.
Output: Green ssh_key_rpc add/list/revoke + git_ssh public upload-pack + force-user-git reject.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-git-ssh/09-CONTEXT.md
@.planning/phases/09-git-ssh/09-RESEARCH.md
@.planning/phases/09-git-ssh/09-PATTERNS.md
@.planning/phases/09-git-ssh/09-02-SUMMARY.md
@crates/octanest-api/src/pat/mod.rs
@crates/octanest-api/src/auth/gate.rs
@crates/octanest-api/src/git/http_backend.rs
@crates/octanest-api/src/repo/acl.rs
@.agents/skills/rust-async-patterns/SKILL.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → sshKey.add | Session cookie + require_verified; title/public_key untrusted |
| Git client → russh TCP | Pubkey + username untrusted; only user git accepted |
| API → git-upload-pack | Path args must stay under repos_dir via bare_repo_path; no shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Elevation of privilege | russh auth | high | mitigate | Force user git; identity from fingerprint only (D-SSH-03) |
| T-09-06 | Tampering | pack exec path | high | mitigate | Allowlist upload-pack; bare_repo_path; Command argv no sh -c (D-SSH-01) |
| T-09-07 | Elevation of privilege | shell/pty | high | mitigate | channel_failure for shell/pty/subsystem/tcpip_forward |
| T-09-SC | Tampering | russh/ssh-key crates | high | mitigate | Package legitimacy OK in RESEARCH; pin russh 0.63 + ssh-key =0.7.0-rc.11 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (09-03-T1): sshKey.add / list / revoke RPC</name>
  <reversibility rating="costly">Published sshKey.* RPC names + fingerprint storage contract (D-SSH-05).</reversibility>
  <files>crates/octanest-api/src/ssh_keys/mod.rs, crates/octanest-api/src/rpc.rs, crates/octanest-api/tests/ssh_key_rpc.rs</files>
  <read_first>crates/octanest-api/src/pat/mod.rs, crates/octanest-api/src/auth/gate.rs, crates/octanest-api/src/rpc.rs, crates/octanest-core/src/ssh_key_types.rs, crates/octanest-db/src/ssh_keys.rs</read_first>
  <behavior>
    - verified sshKey.add with title + ed25519 OpenSSH line returns SshKeyListItem with SHA256: fingerprint
    - list after add includes item; revoke removes from subsequent list
    - unverified add → auth.email_unverified
    - empty/whitespace title → sshKey.title_required
  </behavior>
  <action>
Implement ssh_keys module per D-SSH-05: sshKey.add requires require_verified; non-empty title (sshKey.title_required); parse OpenSSH line with ssh-key crate matching russh pin; accept ssh-ed25519 and RSA ≥2048 (rsa-sha2); compute OpenSSH-display fingerprint SHA256:…; enforce max 25 keys/user; UNIQUE fingerprint conflicts → clear domain error; persist full public_key; response is list metadata (no one-time secret). sshKey.list session-gated; sshKey.revoke ownership-checked hard delete → sshKey.not_found if missing. Register procedures in rpc.rs. Green matching Wave 0 tests in ssh_key_rpc.rs (max-25 / duplicate may stay RED until 09-04 if needed — prefer green all add/list/revoke core cases here). Do not hand-edit api-client.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(ssh_key)'</automated>
    <fails_when>exit code != 0 for non-ignored core cases; or add succeeds without require_verified</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'sshKey\.add|sshKey\.list|sshKey\.revoke' crates/octanest-api/src/rpc.rs`
    - `rg -n 'require_verified|title_required|fingerprint|25' crates/octanest-api/src/ssh_keys/mod.rs`
  </acceptance_criteria>
  <done>SSH key lifecycle RPC green for tracer path</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2 (09-03-T2): End-to-end russh git-upload-pack for public repo</name>
  <reversibility rating="costly">In-process russh listener + OCTANEST_SSH_* env surface (D-SSH-01).</reversibility>
  <files>crates/octanest-api/Cargo.toml, crates/octanest-api/src/ssh/mod.rs, crates/octanest-api/src/ssh/server.rs, crates/octanest-api/src/ssh/auth.rs, crates/octanest-api/src/ssh/pack.rs, crates/octanest-api/src/ssh/host_keys.rs, crates/octanest-api/src/app.rs, crates/octanest-api/src/main.rs, crates/octanest-api/tests/git_ssh.rs</files>
  <read_first>crates/octanest-api/src/git/http_backend.rs, crates/octanest-api/src/git/mod.rs, crates/octanest-api/src/repo/acl.rs, crates/octanest-api/src/main.rs, .planning/phases/09-git-ssh/09-RESEARCH.md, .agents/skills/rust-async-patterns/SKILL.md</read_first>
  <behavior>
    - OCTANEST_SSH_ENABLED starts listener; disabled leaves no bind
    - auth as user git with registered pubkey → Accept; other username → Reject
    - exec git-upload-pack 'owner/repo.git' on public repo → pack succeeds for key owner
  </behavior>
  <action>
Add russh 0.63 (aws-lc-rs) and ssh-key =0.7.0-rc.11 to octanest-api Cargo.toml per RESEARCH legitimacy OK. Implement crates/octanest-api/src/ssh/: host_keys load/generate Ed25519 under OCTANEST_SSH_HOST_KEY_DIR; server Handler forcing user git (D-SSH-03); auth_publickey fingerprint lookup via DB; retain Channel from channel_open_session; exec_request allowlist git-upload-pack only for tracer (receive-pack may channel_failure until 09-04); parse owner/repo.git safely; bare_repo_path; spawn system git-upload-pack with argv (no shell) and bridge Channel make_reader/make_writer ↔ child stdio. Gate spawn from main/app when OCTANEST_SSH_ENABLED truthy; bind OCTANEST_SSH_PORT (default 2222). Reject shell/pty/subsystem with channel_failure. Green tracer subset of git_ssh tests (force git user, public upload-pack). Private/push/rate-limit cases may remain RED until 09-04.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(git_ssh)'</automated>
    <fails_when>exit code != 0 for non-ignored tracer cases; or username other than git accepted; or OpenSSH/git-shell used</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'russh|0\.63' crates/octanest-api/Cargo.toml`
    - `rg -n 'OCTANEST_SSH_ENABLED|git-upload-pack|auth_publickey' crates/octanest-api/src/ssh/`
    - `rg -n 'user != \"git\"|user == \"git\"|\"git\"' crates/octanest-api/src/ssh/auth.rs crates/octanest-api/src/ssh/server.rs`
  </acceptance_criteria>
  <done>Tracer SSH upload-pack + key registration path production-quality and verified</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `ssh_keys/mod.rs` RPC | 09-03 | GIT-04 tracer |
| `ssh/*` russh module | 09-03 | GIT-03 tracer |
| Green ssh_key + partial git_ssh | 09-03 | Nyquist |

<verification>
Tracer nextest filters green for key lifecycle and public upload-pack + force-user-git.
</verification>

<success_criteria>
Users (via API tests) can register a key and fetch a public repo over SSH as user git.
</success_criteria>

<output>
Create `.planning/phases/09-git-ssh/09-03-SUMMARY.md` when done
</output>
