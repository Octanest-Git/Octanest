---
phase: 09-git-ssh
plan: "02"
type: execute
wave: 1
depends_on:
  - "00"
files_modified:
  - crates/octanest-db/migrations/postgres/0009_ssh_keys.sql
  - crates/octanest-db/migrations/mysql/0009_ssh_keys.sql
  - crates/octanest-db/migrations/sqlite/0009_ssh_keys.sql
  - crates/octanest-db/src/ssh_keys.rs
  - crates/octanest-db/src/lib.rs
  - crates/octanest-db/tests/dialect_ssh_keys.rs
  - crates/octanest-core/src/ssh_key_types.rs
  - crates/octanest-core/src/lib.rs
requirements:
  - GIT-04
autonomous: false
user_setup: []
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Tri-dialect 0009_ssh_keys exists with ssh_public_keys (fingerprint UNIQUE) and title/public_key/key_type/last_used_* (GIT-04, D-SSH-05)"
    - "ssh_key_types DTOs expose list metadata without inventing a one-time secret field (D-SSH-05)"
    - "Database facade can create/list/find_by_fingerprint/revoke/touch_last_used for SSH keys"
  artifacts:
    - path: crates/octanest-db/migrations/postgres/0009_ssh_keys.sql
      provides: "SSH keys migration"
    - path: crates/octanest-db/src/ssh_keys.rs
      provides: "dialect CRUD helpers"
    - path: crates/octanest-core/src/ssh_key_types.rs
      provides: "SshKeyListItem + add DTOs"
  key_links:
    - from: crates/octanest-db/src/ssh_keys.rs
      to: crates/octanest-db/migrations/postgres/0009_ssh_keys.sql
      via: "dialect match helpers for 0009 columns"
  assumptions:
    - "ASSUME (A3 RESEARCH): hard-delete on revoke (no revoked_at column)"
    - "ASSUME (GIT-04 ordering): list_ssh_keys_for_user orders by created_at DESC"
    - "PRECONDITION: 09-00 dialect stub present"
  prohibitions:
    - statement: "Must not place dialect SQL in octanest-api"
      flagged: true
    - statement: "Must not wire sshKey.* RPC or russh in this plan"
      flagged: true
---

<objective>
Confirm the published SSH clone URL door (D-SSH-02), then land SSH key schema + core types + DB CRUD so the tracer (09-03) can register and look up keys by fingerprint.

Purpose: Blocking schema/migration parity gate (tri-dialect 0009) separate from russh and UI.
Output: Green migration_parity + dialect_ssh_keys + ssh_key_types; Database facade methods ready.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-git-ssh/09-CONTEXT.md
@.planning/phases/09-git-ssh/09-RESEARCH.md
@.planning/phases/09-git-ssh/09-PATTERNS.md
@crates/octanest-db/migrations/postgres/0008_pats.sql
@crates/octanest-db/src/pats.rs
@crates/octanest-core/src/pat_types.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → DB | Schema defines fingerprint uniqueness and public key storage |
| Core DTOs → RPC later | List DTOs may include public_key (not secret) but never invent a one-time token field |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Tampering | fingerprint UNIQUE | high | mitigate | UNIQUE fingerprint column + DB enforce (D-SSH-05) |
| T-09-05 | Information disclosure | public_key column | low | accept | Public keys are not secrets; still session-gated at RPC |
| T-09-SC | Tampering | packages | high | mitigate | No new crates.io installs in this plan |
</threat_model>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 0 (09-02-T0): Confirm D-SSH-02 published clone URL contract</name>
  <decision>
Proceed with locked D-SSH-02: primary clone URL always scp-style git@{OCTANEST_SSH_HOST}:{owner}/{repo}.git; Compose/dev listen+advertise OCTANEST_SSH_PORT=2222 (single env); document ~/.ssh/config Port when ≠ 22; do not make ssh:// the primary CloneBox string.
  </decision>
  <context>
CONTEXT locked D-SSH-02 as one-way (published remote URL contract). RESEARCH discretion: one OCTANEST_SSH_PORT for listen and advertise. Schema work enables the tracer that will bake this shape into smoke/UI.
  </context>
  <options>
    <option id="proceed">
      <title>Proceed with scp-style + single OCTANEST_SSH_PORT (recommended)</title>
      <pros>Honors locked CONTEXT + RESEARCH discretion</pros>
      <cons>None if discuss already confirmed</cons>
    </option>
    <option id="split_ports">
      <title>Split listen vs advertise env (revisit discretion only)</title>
      <pros>Supports platforms with different publish mapping</pros>
      <cons>Larger operator surface; still keep scp-style primary string</cons>
    </option>
  </options>
  <resume-signal>Reply with proceed (recommended) or split_ports</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1 (09-02-T1): Migration 0009 + ssh_key_types + DB ssh_keys API</name>
  <reversibility rating="costly">ssh_public_keys schema + fingerprint uniqueness (D-SSH-05) — later columns need migrations.</reversibility>
  <precondition>09-02-T0 human selected proceed or split_ports; D-SSH-02 scp-style primary URL remains locked</precondition>
  <files>crates/octanest-db/migrations/postgres/0009_ssh_keys.sql, crates/octanest-db/migrations/mysql/0009_ssh_keys.sql, crates/octanest-db/migrations/sqlite/0009_ssh_keys.sql, crates/octanest-db/src/ssh_keys.rs, crates/octanest-db/src/lib.rs, crates/octanest-db/tests/dialect_ssh_keys.rs, crates/octanest-core/src/ssh_key_types.rs, crates/octanest-core/src/lib.rs</files>
  <read_first>crates/octanest-db/migrations/postgres/0008_pats.sql, crates/octanest-db/src/pats.rs, crates/octanest-db/tests/dialect_pats.rs, crates/octanest-core/src/pat_types.rs, .planning/phases/09-git-ssh/09-RESEARCH.md</read_first>
  <behavior>
    - tri-dialect 0009 files define ssh_public_keys with UNIQUE fingerprint
    - create → find_by_fingerprint → list_for_user → touch_last_used → revoke (hard delete) round-trip
    - migration_parity includes 0009
  </behavior>
  <action>
Ship identical logical migration 0009_ssh_keys under sqlite/postgres/mysql: table ssh_public_keys with id, user_id FK CASCADE, title, public_key, fingerprint UNIQUE, key_type, last_used_at, last_used_ip nullable, created_at — dialect timestamp style matching 0008_pats (per D-SSH-05). Implement octanest-db ssh_keys.rs with dialect match create/list_for_user/find_by_fingerprint/revoke/touch_last_used; expose on Database facade. Add octanest-core ssh_key_types.rs (SshKeyListItem: id, title, fingerprint, key_type, public_key optional, last_used_*, created_at; add input title+public_key). Green dialect_ssh_keys Wave 0 tests. Do not add russh or RPC.
  </action>
  <verify>
    <automated>cargo test -p octanest-core --lib && cargo test -p octanest-db --lib migration_parity && cargo test -p octanest-db --test dialect_ssh_keys</automated>
    <fails_when>exit != 0; or 0009 missing on any dialect; or fingerprint UNIQUE absent</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f crates/octanest-db/migrations/postgres/0009_ssh_keys.sql && test -f crates/octanest-db/migrations/mysql/0009_ssh_keys.sql && test -f crates/octanest-db/migrations/sqlite/0009_ssh_keys.sql`
    - `rg -n 'ssh_public_keys|fingerprint' crates/octanest-db/migrations/postgres/0009_ssh_keys.sql`
    - `rg -n 'find_by_fingerprint|touch_last_used|revoke' crates/octanest-db/src/ssh_keys.rs`
    - `rg -n 'SshKeyListItem|fingerprint' crates/octanest-core/src/ssh_key_types.rs`
  </acceptance_criteria>
  <done>Schema + types + DB CRUD green; ready for tracer RPC lookup</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Notes |
|----------|------------|-------|
| `0009_ssh_keys` tri-dialect | 09-02 | GIT-04 |
| `ssh_keys.rs` + types | 09-02 | Tracer prerequisite |

<verification>
migration_parity + dialect_ssh_keys green; D-SSH-02 door confirmed.
</verification>

<success_criteria>
SSH key persistence layer ready for fingerprint auth and session CRUD.
</success_criteria>

<output>
Create `.planning/phases/09-git-ssh/09-02-SUMMARY.md` when done
</output>
