---
phase: 10-orgs-permissions
plan: "02"
type: execute
wave: 1
depends_on:
  - "00"
  - "01"
files_modified:
  - crates/octanest-db/migrations/postgres/0010_orgs_acl.sql
  - crates/octanest-db/migrations/mysql/0010_orgs_acl.sql
  - crates/octanest-db/migrations/sqlite/0010_orgs_acl.sql
  - crates/octanest-db/src/organizations.rs
  - crates/octanest-db/src/org_members.rs
  - crates/octanest-db/src/lib.rs
  - crates/octanest-db/tests/dialect_orgs.rs
  - crates/octanest-core/src/org_types.rs
  - crates/octanest-core/src/lib.rs
  - crates/octanest-api/src/org/mod.rs
  - crates/octanest-api/src/rpc.rs
  - crates/octanest-api/tests/org_create_members.rs
  - apps/web/src/routes/orgs.new.tsrx
  - apps/web/src/routes/orgs.new.integration.test.ts
# packages/api-client regenerated by make rpc-gen (not hand-listed; keeps files_modified < 15)
requirements:
  - ORG-01
autonomous: false
user_setup: []
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Verified user can create an organization with a shared-namespace slug and become Owner (ORG-01, D-ORG-01, D-ORG-02a)"
    - "Tri-dialect 0010_orgs_acl lands organizations + members (+ invite/collaborator/owner_type columns for later fill) without dialect SQL in API"
    - "/orgs/new creates via org.create and navigates to /{slug}"
  artifacts:
    - path: crates/octanest-db/migrations/postgres/0010_orgs_acl.sql
      provides: "Org ACL schema (prefer 0010 vs Phase 09 0009_ssh_keys)"
    - path: crates/octanest-api/src/org/mod.rs
      provides: "org.create RPC"
    - path: apps/web/src/routes/orgs.new.tsrx
      provides: "/orgs/new tracer UI"
  key_links:
    - from: apps/web/src/routes/orgs.new.tsrx
      to: crates/octanest-api/src/org/mod.rs
      via: "org.create after make rpc-gen"
    - from: crates/octanest-api/src/org/mod.rs
      to: crates/octanest-db/src/organizations.rs
      via: "Database facade insert + Owner membership row"
  assumptions:
    - "ASSUME (discretion): polymorphic owner uses owner_type + owner_id on repositories (not a separate owners table)"
    - "ASSUME: Migration number is 0010_orgs_acl; if only SSH 0009 exists first, orgs still 0010; never steal 0009 from Phase 09"
    - "ASSUME: Invite/collaborator tables may be created empty in 0010 for architectural completeness; RPCs filled in later plans"
  prohibitions:
    - statement: "Must not implement teams"
      flagged: true
    - statement: "Must not hand-edit api-client as lasting source of truth — run make rpc-gen"
      flagged: true
    - statement: "Must not place dialect SQL in octanest-api"
      flagged: true
---

<objective>
Confirm the one-way shared-slug / polymorphic-owner schema door, then ship the Phase 10 tracer: create organization end-to-end through migration, DB, org.create RPC, generated client, and /orgs/new.

Purpose: Prove architecture on one happy path before expanding members, invites, ACL consumers, and collaborators (tracer-first, D-ORG-01, ORG-01).
Output: Green org.create path + /orgs/new; dialect_orgs migration assertions pass for 0010.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-orgs-permissions/10-CONTEXT.md
@.planning/phases/10-orgs-permissions/10-RESEARCH.md
@.planning/phases/10-orgs-permissions/10-PATTERNS.md
@.planning/phases/10-orgs-permissions/10-UI-SPEC.md
@crates/octanest-core/src/auth_types.rs
@crates/octanest-db/migrations/postgres/0007_repositories.sql
@crates/octanest-db/migrations/postgres/0008_pats.sql
@apps/web/src/routes/new.tsrx
@.agents/skills/octane/SKILL.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → org.create | Untrusted slug/display_name; require_verified |
| Shared slug namespace | User username ↔ org slug collision = takeover risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Tampering | org slug | high | mitigate | validate_username + is_reserved_username + transactional uniqueness vs users.username and organizations.slug (D-ORG-01) |
| T-10-05 | Elevation of privilege | org.create | medium | mitigate | require_verified; creator sole Owner membership row |
| T-10-SC | Tampering | packages | high | mitigate | No new crates.io/npm packages |
</threat_model>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 0 (10-02-T0): Confirm D-ORG-01 schema door (0010 + owner_type)</name>
  <decision>
Confirm proceed with RESEARCH discretion for Phase 10 schema:

1. Migration id **0010_orgs_acl** (tri-dialect) — Phase 09 SSH claims **0009_ssh_keys**; do not reuse 0009 for orgs.
2. Polymorphic repos: **owner_type** (`user`|`org`) + **owner_id** (TEXT, no FK to users); backfill existing rows owner_type=user; drop user-only FK carefully per dialect.
3. Tables in same migration: organizations (slug UNIQUE, member_base_permission default none), organization_members, organization_invites, repository_collaborators — even if invite/collaborator RPCs land later.

Locked product decision D-ORG-01 (shared slug namespace) is not reopened — only the physical schema shape above.
  </decision>
  <context>
One-way door: URL + disk path + ACL identity key off shared slug and owner_type. Phase 09 plans already use 0009_ssh_keys — orgs must not collide.
  </context>
  <options>
    <option id="proceed_0010">
      <title>Proceed with 0010_orgs_acl + owner_type/owner_id (recommended)</title>
      <pros>Matches RESEARCH discretion; avoids Phase 09 migration collision; single migration for org ACL tables</pros>
      <cons>Undo requires data migration after any org-owned repos exist</cons>
    </option>
    <option id="revise">
      <title>Stop — revise schema numbering/shape before tracer</title>
      <pros>Allows alternate owners-table design if preferred</pros>
      <cons>Blocks tracer until replan</cons>
    </option>
  </options>
  <resume-signal>Reply with proceed_0010 (recommended) or revise</resume-signal>
</task>

<task type="tracer">
  <name>End-to-end org.create — one path only</name>
  <reversibility rating="one-way">Shared slug + polymorphic owner_type on repositories is a URL/disk/ACL identity door (D-ORG-01).</reversibility>
  <files>crates/octanest-db/migrations/postgres/0010_orgs_acl.sql, crates/octanest-db/migrations/mysql/0010_orgs_acl.sql, crates/octanest-db/migrations/sqlite/0010_orgs_acl.sql, crates/octanest-db/src/organizations.rs, crates/octanest-db/src/org_members.rs, crates/octanest-db/src/lib.rs, crates/octanest-core/src/org_types.rs, crates/octanest-api/src/org/mod.rs, crates/octanest-api/src/rpc.rs, apps/web/src/routes/orgs.new.tsrx, packages/api-client/src/index.ts, crates/octanest-api/tests/org_create_members.rs, crates/octanest-db/tests/dialect_orgs.rs, apps/web/src/routes/orgs.new.integration.test.ts</files>
  <read_first>.planning/phases/10-orgs-permissions/10-RESEARCH.md, crates/octanest-api/src/repo/mod.rs, crates/octanest-db/src/repositories.rs, crates/octanest-core/src/auth_types.rs, apps/web/src/routes/new.tsrx, .agents/skills/octane/SKILL.md</read_first>
  <action>
After T0 proceed_0010: land tri-dialect 0010_orgs_acl per D-ORG-01 (shared slug) and locked roles D-ORG-02a (Owner/Admin/Member columns). Implement Database helpers for insert organization + insert Owner membership; org_types DTOs; org.create RPC (require_verified, validate_username, reserved check, dual uniqueness vs users+orgs, creator Owner). Wire rpc.rs + make rpc-gen. Ship minimal /orgs/new Octane form per 10-UI-SPEC (slug + optional display name → org.create → navigate /{slug}). Turn green the org.create happy-path tests in org_create_members.rs and dialect_orgs migration assertions. Stub org.get overview route only if needed for navigation target — full overview UI may be a later plan, but create must succeed end-to-end. Do not ship members/invites/collaborator RPCs, ACL rewrite, or Smart HTTP changes in this tracer. Do not implement teams (D-ORG-07).
  </action>
  <verify>
    <automated>cargo test -p octanest-db --lib migration_parity && cargo test -p octanest-db --test dialect_orgs && cargo nextest run -p octanest-api -E 'test(org_create)' && make rpc-gen && make rpc-sync-check && bun --cwd apps/web exec vitest run src/routes/orgs.new.integration.test.ts && bun --cwd apps/web run build</automated>
    <fails_when>vitest exits non-zero or expected assertions fail</fails_when>
  </verify>
  <done>One verified user can create an org via RPC and /orgs/new; creator is Owner; 0010 migrates on all dialects</done>
</task>

</tasks>

<verification>
Tracer happy path: org.create + UI + migration parity green; members/ACL expansion deferred.
</verification>

<success_criteria>
ORG-01 create-organization slice works end-to-end; schema door confirmed; no teams.
</success_criteria>

<output>
Create `.planning/phases/10-orgs-permissions/10-02-SUMMARY.md` when done
</output>
