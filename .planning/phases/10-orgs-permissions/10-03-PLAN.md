---
phase: 10-orgs-permissions
plan: "03"
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - crates/octanest-db/src/repositories.rs
  - crates/octanest-db/src/lib.rs
  - crates/octanest-core/src/repo_types.rs
  - crates/octanest-api/src/repo/acl.rs
  - crates/octanest-api/src/repo/mod.rs
  - crates/octanest-api/src/routes/git_smart_http.rs
  - crates/octanest-api/tests/org_create_members.rs
  - crates/octanest-api/tests/repo_create.rs
  - packages/api-client/src/index.ts
requirements:
  - ORG-01
  - ORG-03
autonomous: true
user_setup: []
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Owner slug resolves to user OR organization; disk path uses that slug (D-ORG-01)"
    - "repo.create accepts optional owner slug; org create requires caller Org Owner or Admin (discretion A5)"
    - "Existing user-owned repos keep owner_type=user and continue to resolve"
  artifacts:
    - path: crates/octanest-api/src/repo/acl.rs
      provides: "OwnerRef resolver (user|org)"
    - path: crates/octanest-core/src/repo_types.rs
      provides: "CreateRepoRequest.owner optional slug"
  key_links:
    - from: crates/octanest-api/src/repo/mod.rs
      to: crates/octanest-api/src/repo/acl.rs
      via: "create + resolve use OwnerRef"
    - from: crates/octanest-api/src/git/mod.rs
      to: bare_repo_path
      via: "org slug segment unchanged helper"
  assumptions:
    - "ASSUME: insert_repository gains owner_type parameter; SELECTs return owner_type"
    - "ASSUME: RepoPublic gains owner_type field (user|org) for UI; owner_username remains the public slug label"
  prohibitions:
    - statement: "Must not rewrite Smart HTTP PAT ACL in this plan — only shared owner resolve if required to compile"
      flagged: true
    - statement: "Must not allow Org Members to create repos under the org"
      flagged: true
---

<objective>
Expand the tracer with polymorphic owner resolution and org-owned repository creation so `/{org}/{repo}` and disk `{slug}/{name}.git` work for org owners (D-ORG-01).

Purpose: Unblock ACL, collaborators, and /new owner picker on real org-owned rows.
Output: repo.create with optional owner; shared OwnerRef used by resolve paths.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-orgs-permissions/10-RESEARCH.md
@.planning/phases/10-orgs-permissions/10-PATTERNS.md
@crates/octanest-api/src/repo/acl.rs
@crates/octanest-api/src/repo/mod.rs
@crates/octanest-api/src/git/mod.rs
@crates/octanest-db/src/repositories.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → repo.create owner slug | Caller could claim another org |
| Owner resolve | Wrong table → cross-tenant path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06 | Elevation of privilege | repo.create owner | high | mitigate | Only self username or org where caller is Owner/Admin; Members denied |
| T-10-07 | Spoofing | OwnerRef | high | mitigate | Resolve user first then org by slug; never trust client-supplied owner_id alone |
| T-10-SC | Tampering | packages | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (10-03-T1): OwnerRef resolve + repository owner_type plumbing</name>
  <files>crates/octanest-db/src/repositories.rs, crates/octanest-db/src/lib.rs, crates/octanest-api/src/repo/acl.rs, crates/octanest-api/src/routes/git_smart_http.rs</files>
  <read_first>crates/octanest-api/src/repo/acl.rs, crates/octanest-api/src/routes/git_smart_http.rs, crates/octanest-db/src/repositories.rs, .planning/phases/10-orgs-permissions/10-PATTERNS.md</read_first>
  <behavior>
    - resolve owner slug → User or Org; missing → not_found path for web
    - find_repository_by_owner_name keys on owner_id regardless of type
    - git Smart HTTP resolve_repo uses same OwnerRef (no user-only find)
  </behavior>
  <action>
Wire Database select/insert for owner_type (from 0010). Replace user-only find_user_by_username in resolve_repo_for_read and Smart HTTP resolve with shared OwnerRef helper per RESEARCH Pattern 3 and D-ORG-01. Keep existing can_read_as_owner behavior temporarily for private checks until plan 04 ACL rewrite (personal owner still works when owner_type=user). Ensure AccessibleRepo.owner_username is the resolved slug (user username or org slug).
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(org_create) | test(repo_create)' && cargo test -p octanest-db --lib</automated>
  </verify>
  <done>User and org owner slugs both resolve to repositories; git resolve shares OwnerRef</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (10-03-T2): repo.create optional owner slug</name>
  <files>crates/octanest-core/src/repo_types.rs, crates/octanest-api/src/repo/mod.rs, packages/api-client/src/index.ts, crates/octanest-api/tests/org_create_members.rs, crates/octanest-api/tests/repo_create.rs</files>
  <read_first>crates/octanest-core/src/repo_types.rs, crates/octanest-api/src/repo/mod.rs, .planning/phases/10-orgs-permissions/10-RESEARCH.md</read_first>
  <behavior>
    - Omit owner → create under session user (compat)
    - owner=org slug + caller Owner/Admin → org-owned bare repo on disk
    - owner=org + caller Member only → denied (stable error)
    - owner=other user slug → denied
  </behavior>
  <action>
Extend CreateRepoRequest with optional owner slug; authorize per discretion A5 (Owner/Admin for org). Pass owner_type+owner_id into insert_repository; bare_repo_path uses resolved slug. make rpc-gen. Green tests: create under org after org.create; personal create unchanged.
  </action>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && cargo nextest run -p octanest-api -E 'test(org_create) | test(repo_create)'</automated>
  </verify>
  <done>Org Owner/Admin can create org-owned repos; Members cannot; personal default preserved</done>
</task>

</tasks>

<verification>
Org-owned repo create + dual slug resolve green.
</verification>

<success_criteria>
Polymorphic owner works for create and resolve; D-ORG-01 disk/URL contract held.
</success_criteria>

<output>
Create `.planning/phases/10-orgs-permissions/10-03-SUMMARY.md` when done
</output>
