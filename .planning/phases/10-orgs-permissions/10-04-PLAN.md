---
phase: 10-orgs-permissions
plan: "04"
type: execute
wave: 3
depends_on:
  - "03"
files_modified:
  - crates/octanest-api/src/repo/acl.rs
  - crates/octanest-api/src/repo/mod.rs
  - crates/octanest-core/src/repo_types.rs
  - crates/octanest-db/src/org_members.rs
  - crates/octanest-db/src/repo_collaborators.rs
  - crates/octanest-db/src/lib.rs
  - crates/octanest-api/tests/repo_private_404.rs
  - crates/octanest-api/tests/repo_collaborators_acl.rs
  - packages/api-client/src/index.ts
requirements:
  - ORG-02
  - ORG-04
autonomous: true
user_setup: []
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "effective_capability uses highest-wins coalesce across personal owner, org Owner/Admin, Member×member_base, collaborator, public (D-ORG-05, D-ORG-02b, D-ORG-02c)"
    - "Web unauthorized private access still returns repo.not_found (D-25); capability helper does not embed HTTP status"
    - "RepoPublic exposes can_admin (and can_write if useful) from ACL for settings UI"
  artifacts:
    - path: crates/octanest-api/src/repo/acl.rs
      provides: "Capability enum + coalesce + resolve_repo_for_read rewrite"
  key_links:
    - from: crates/octanest-api/src/repo/acl.rs
      to: crates/octanest-db/src/org_members.rs
      via: "membership + member_base lookup"
    - from: crates/octanest-api/src/repo/acl.rs
      to: crates/octanest-db/src/repo_collaborators.rs
      via: "collaborator permission lookup"
  assumptions:
    - "ASSUME (A1): highest-wins coalesce — Collaborator raises but cannot lower Owner/Admin"
    - "ASSUME: DB read helpers for membership/collaborator exist even if write RPCs land in later plans"
  prohibitions:
    - statement: "Must not map git 401 inside acl.rs — callers map status"
      flagged: true
    - statement: "Must not implement teams/units"
      flagged: true
---

<objective>
Replace the owner-only `can_read_as_owner` stub with central Capability evaluation and rewire `resolve_repo_for_read` so private data is gated by real forge permissions (ORG-02, ORG-04, D-ORG-05).

Purpose: Single ACL decision module for all later consumers (mutate, Smart HTTP, PAT, UI can_admin).
Output: Green coalesce unit tests + private 404 cases for org Owner vs stranger; RepoPublic.can_admin.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-orgs-permissions/10-CONTEXT.md
@.planning/phases/10-orgs-permissions/10-RESEARCH.md
@.planning/phases/07-git-repos-browse/07-CONTEXT.md
@crates/octanest-api/src/repo/acl.rs
@crates/octanest-api/src/repo/mod.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session user id → ACL | Capability must not trust client-claimed roles |
| ACL → web RPC | Soft not_found anti-enumeration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Information disclosure | resolve_repo_for_read | high | mitigate | Keep identical repo.not_found for missing vs unauthorized private (D-ORG-05 / D-25) |
| T-10-08 | Elevation of privilege | coalesce | high | mitigate | Highest-wins; Member base none denies private; Owner/Admin always admin on org repos (D-ORG-02b) |
| T-10-SC | Tampering | packages | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (10-04-T1): Capability coalesce + DB lookups</name>
  <files>crates/octanest-api/src/repo/acl.rs, crates/octanest-db/src/org_members.rs, crates/octanest-db/src/repo_collaborators.rs, crates/octanest-db/src/lib.rs</files>
  <read_first>crates/octanest-api/src/repo/acl.rs, .planning/phases/10-orgs-permissions/10-RESEARCH.md</read_first>
  <behavior>
    - Personal owner_type=user and owner_id==caller → Admin
    - Org Owner/Admin membership → Admin on org repos
    - Org Member → member_base none|read|write mapped to Capability
    - Collaborator read|write|admin raises via max
    - Public visibility always grants at least Read (including anonymous)
    - Private with no grant → None
  </behavior>
  <action>
Implement Capability {Read,Write,Admin}, meets(), coalesce() per RESEARCH Pattern 1 and D-ORG-05 (highest-wins interpretation of evaluation inputs). Add dialect-safe DB helpers to load org membership role, org member_base_permission, and collaborator permission. Async effective_capability(ctx, caller, repo_row, owner_ref) loads sources and coalesces. Green colocated coalesce unit tests from Wave 0. Delete or thin-wrap can_read_as_owner to meets(Read) only if needed for compile churn, then remove call sites in this plan's resolve path.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(coalesce)'</automated>
    <fails_when>cargo/nextest exits non-zero or expected tests fail</fails_when>
  </verify>
  <done>Coalesce matrix unit tests green; DB lookup helpers available</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (10-04-T2): resolve_repo_for_read + RepoPublic.can_admin</name>
  <files>crates/octanest-api/src/repo/acl.rs, crates/octanest-api/src/repo/mod.rs, crates/octanest-core/src/repo_types.rs, packages/api-client/src/index.ts, crates/octanest-api/tests/repo_private_404.rs, crates/octanest-api/tests/repo_collaborators_acl.rs</files>
  <read_first>crates/octanest-api/src/repo/acl.rs, crates/octanest-api/src/repo/mod.rs, crates/octanest-core/src/repo_types.rs</read_first>
  <behavior>
    - Private org repo: Creator Owner can read; stranger → repo.not_found
    - Public org repo: anonymous read OK even if member_base none
    - repo.get includes can_admin true for Owner
  </behavior>
  <action>
Rewrite resolve_repo_for_read to require meets(Read) else not_found(). Extend RepoPublic with can_admin (bool) derived via meets(Admin); optionally can_write. Confirm raw/archive HTTP handlers still call resolve_repo_for_read (D-ORG-05) — update call sites if they bypass ACL. make rpc-gen. Turn green private 404 org-owner vs stranger cases; leave collaborator CRUD cases ignored until plan 07 if needed.
  </action>
  <acceptance_criteria>
    - `rg -n 'resolve_repo_for_read' crates/octanest-api/src/repo/`
    - raw/archive (or equivalent blob download) paths invoke resolve_repo_for_read — no owner-only bypass (D-ORG-05)
  </acceptance_criteria>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && cargo nextest run -p octanest-api -E 'test(repo_private) | test(org_create) | test(coalesce)'</automated>
    <fails_when>rpc-gen/rpc-sync-check exits non-zero or client drifts; or nextest fails private/ACL cases</fails_when>
  </verify>
  <done>Web private ACL uses central capabilities; can_admin on RepoPublic; D-25 preserved</done>
</task>

</tasks>

<verification>
ACL central module green; private org Owner readable; stranger soft-404.
</verification>

<success_criteria>
ORG-04 web read path and ORG-02 role mapping foundation live in acl.rs.
</success_criteria>

<output>
Create `.planning/phases/10-orgs-permissions/10-04-SUMMARY.md` when done
</output>
