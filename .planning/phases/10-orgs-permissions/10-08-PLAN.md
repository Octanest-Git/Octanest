---
phase: 10-orgs-permissions
plan: "08"
type: execute
wave: 7
depends_on:
  - "04"
  - "07"
files_modified:
  - crates/octanest-api/src/repo/mod.rs
  - crates/octanest-api/src/routes/git_smart_http.rs
  - crates/octanest-api/src/pat/mod.rs
  - crates/octanest-api/tests/git_smart_http.rs
  - crates/octanest-api/tests/pat_rpc.rs
  - crates/octanest-api/tests/repo_branch_soft_protect.rs
requirements:
  - ORG-04
  - ORG-03
autonomous: true
user_setup: []
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Smart HTTP fetch needs Read capability; push needs Write; private unauth → 401 (D-21); insufficient → 403 as today (ORG-04, D-ORG-05)"
    - "Branch mutate gates use Write; soft-delete/collaborator already Admin"
    - "PAT authorize intersects PAT scopes/contents with ACL; collaborator can push with classic repo scope (ORG-04)"
    - "FG All lists personal-owned + org repos where subject is Owner/Admin (A4); Selected allows ACL-accessible repos"
  artifacts:
    - path: crates/octanest-api/src/routes/git_smart_http.rs
      provides: "ACL-based git fetch/push gates"
    - path: crates/octanest-api/src/pat/mod.rs
      provides: "PAT ∩ ACL"
  key_links:
    - from: crates/octanest-api/src/routes/git_smart_http.rs
      to: crates/octanest-api/src/repo/acl.rs
      via: "meets(Read|Write) after OwnerRef resolve"
    - from: crates/octanest-api/src/pat/mod.rs
      to: crates/octanest-api/src/repo/acl.rs
      via: "mint/authorize capability intersect"
  assumptions:
    - "ASSUME (A4): FG All = personal-owned + org Owner/Admin repos; collaborators use Selected"
    - "ASSUME: SSH call site updated only if Phase 09 code present — same acl helpers; do not edit Phase 09 planning files"
  prohibitions:
    - statement: "Must not use web repo.not_found status for Smart HTTP private deny"
      flagged: true
    - statement: "Must not require pat.user_id == repositories.owner_id for push"
      flagged: true
---

<objective>
Wire all non-UI ACL consumers — owner-mutate Write/Admin, Smart HTTP, and PAT ∩ ACL — so unauthorized users cannot read private repos or push without permission (ORG-04, D-ORG-05).

Purpose: Close the Phase 7/8 owner-only git and PAT holes.
Output: Green git_smart collaborator push + private 401; PAT non-owner collaborator path; branch gates by Write.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-orgs-permissions/10-RESEARCH.md
@.planning/phases/08-git-https-pats/08-CONTEXT.md
@.planning/phases/09-git-ssh/09-CONTEXT.md
@crates/octanest-api/src/routes/git_smart_http.rs
@crates/octanest-api/src/pat/mod.rs
@crates/octanest-api/src/repo/mod.rs
@crates/octanest-api/src/repo/acl.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git Basic PAT → ACL | Scope alone must not bypass capability |
| Cookie → Smart HTTP | Cookies ignored (Phase 8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-13 | Elevation of privilege | pat_allows_operation | high | mitigate | Require PAT scope/contents AND meets(needed Capability); remove owner_id equality push check |
| T-10-01 | Information disclosure | Smart HTTP private | high | mitigate | Unauth/private deny → 401 + WWW-Authenticate (D-21), not soft not_found |
| T-10-14 | Elevation of privilege | branch mutate | high | mitigate | meets(Write) instead of owner_id equality |
| T-10-SC | Tampering | packages | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (10-08-T1): Mutate gates + Smart HTTP ACL</name>
  <files>crates/octanest-api/src/repo/mod.rs, crates/octanest-api/src/routes/git_smart_http.rs, crates/octanest-api/tests/git_smart_http.rs, crates/octanest-api/tests/repo_branch_soft_protect.rs</files>
  <read_first>crates/octanest-api/src/routes/git_smart_http.rs, crates/octanest-api/src/repo/mod.rs, crates/octanest-api/src/repo/acl.rs</read_first>
  <behavior>
    - Private anon git → 401
    - Collaborator with Write + PAT can receive-pack
    - Collaborator Read-only cannot push
    - Branch create/rename/delete needs Write capability
  </behavior>
  <action>
Replace resolve_repo_for_owner_mutate owner_id equality with capability: Write for branch ops, Admin for destructive/settings already done in 07. Rewire Smart HTTP private/push checks from can_read_as_owner to effective_capability meets(Read/Write) per D-ORG-05; preserve 401/403 mapping (D-21). If SSH server code exists in-tree from Phase 09, call the same acl helpers (D-SSH-04) without editing .planning/phases/09-*. Green git_smart collaborator cases.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(git_smart) | test(repo_branch) | test(collab)'</automated>
  </verify>
  <done>Git and branch mutate enforce ACL capabilities with correct status mapping</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (10-08-T2): PAT ∩ ACL authorize + FG selection</name>
  <files>crates/octanest-api/src/pat/mod.rs, crates/octanest-api/src/routes/git_smart_http.rs, crates/octanest-api/tests/pat_rpc.rs, crates/octanest-api/tests/git_smart_http.rs</files>
  <read_first>crates/octanest-api/src/pat/mod.rs, crates/octanest-api/src/routes/git_smart_http.rs, .planning/phases/10-orgs-permissions/10-RESEARCH.md</read_first>
  <behavior>
    - Classic PAT with repo scope + collaborator Write → push OK
    - Classic PAT without ACL Write → deny
    - FG Selected may include repos where subject has capability
    - FG All includes personal + org Owner/Admin repos only (A4)
  </behavior>
  <action>
Rewrite pat_allows_operation / FG mint ownership checks to intersect PAT grants with acl meets() per D-ORG-05. Document FG All semantics in code comment + docs later plan. Green PAT collaborator push test; keep password reject and cookie-ignore behavior from Phase 8.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(pat_) | test(git_smart)'</automated>
  </verify>
  <done>PAT push/fetch authorized via ACL∩scope; owner_id-only push check gone</done>
</task>

</tasks>

<verification>
ORG-04 git+PAT enforcement green; mutate gates capability-based.
</verification>

<success_criteria>
Unauthorized cannot private-read via git (401) or push without Write; collaborators can with PAT.
</success_criteria>

<output>
Create `.planning/phases/10-orgs-permissions/10-08-SUMMARY.md` when done
</output>
