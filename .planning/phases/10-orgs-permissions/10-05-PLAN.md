---
phase: 10-orgs-permissions
plan: "05"
type: execute
wave: 4
depends_on:
  - "02"
  - "04"
files_modified:
  - crates/octanest-api/src/org/mod.rs
  - crates/octanest-api/src/org/members.rs
  - crates/octanest-api/src/rpc.rs
  - crates/octanest-db/src/org_members.rs
  - crates/octanest-db/src/organizations.rs
  - crates/octanest-db/src/lib.rs
  - crates/octanest-core/src/org_types.rs
  - crates/octanest-api/tests/org_create_members.rs
  - packages/api-client/src/index.ts
requirements:
  - ORG-01
  - ORG-02
autonomous: true
user_setup: []
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Org Admin+ can add members by username, update roles, remove members; last Owner protected (ORG-01, ORG-02, D-ORG-02a)"
    - "Org Admin+ can set member_base_permission none|read|write; ACL Member path respects it on private repos (D-ORG-02b)"
    - "Username add works regardless of allow_signup (D-ORG-03)"
  artifacts:
    - path: crates/octanest-api/src/org/members.rs
      provides: "members.* + updateSettings RPCs"
  key_links:
    - from: crates/octanest-api/src/org/members.rs
      to: crates/octanest-api/src/repo/acl.rs
      via: "role changes observed by effective_capability"
  assumptions:
    - "ASSUME (A3): Admin manages members/invites/settings; Owner-only for last-owner policy and future delete/transfer"
    - "ASSUME: org.listMine / org.get return public fields for UI"
  prohibitions:
    - statement: "Must not add Collaborator as an org membership role"
      flagged: true
    - statement: "Must not implement email invites in this plan"
      flagged: true
---

<objective>
Ship org membership CRUD, role assignment, org.get/listMine, and member_base_permission settings so Owner/Admin/Member control private repo access (ORG-01, ORG-02, D-ORG-02a, D-ORG-02b).

Purpose: Complete in-instance collaboration without email invites yet.
Output: Green org_ create/members/role/base tests.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-orgs-permissions/10-CONTEXT.md
@.planning/phases/10-orgs-permissions/10-RESEARCH.md
@crates/octanest-api/src/org/mod.rs
@crates/octanest-api/src/auth/gate.rs
@crates/octanest-api/src/repo/acl.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → members.* | Role escalation attempts |
| Admin vs Owner | Destructive last-owner |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-09 | Elevation of privilege | members.updateRole | high | mitigate | Only Admin+; cannot remove/demote last Owner (org.last_owner); cannot grant roles above caller's policy (Admin cannot create Owner unless also Owner — lock: only Owner can grant/transfer Owner) |
| T-10-10 | Denial of service | last Owner | medium | mitigate | Reject demote/remove of final Owner |
| T-10-SC | Tampering | packages | high | mitigate | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (10-05-T1): org.members.* + org.get/listMine</name>
  <files>crates/octanest-api/src/org/members.rs, crates/octanest-api/src/org/mod.rs, crates/octanest-api/src/rpc.rs, crates/octanest-db/src/org_members.rs, crates/octanest-core/src/org_types.rs, packages/api-client/src/index.ts, crates/octanest-api/tests/org_create_members.rs</files>
  <read_first>crates/octanest-api/src/org/mod.rs, crates/octanest-api/src/auth/admin.rs, .planning/phases/10-orgs-permissions/10-RESEARCH.md</read_first>
  <behavior>
    - members.add by username → Member/Admin/Owner per rules; require_verified; works with allow_signup false
    - members.updateRole / remove with last Owner guard
    - Only Owner can grant Owner role
    - members.list returns username, role, ids — no emails required
  </behavior>
  <action>
Implement org.get, org.listMine, org.members.list/add/updateRole/remove per RESEARCH RPC surface and D-ORG-02a (Owner/Admin/Member only — Collaborator is not an org role per D-ORG-02a). Gate mutations: Admin+ for Member/Admin management; Owner for Owner grants and last-owner protection. make rpc-gen. Green org_create_members membership tests.
  </action>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && cargo nextest run -p octanest-api -E 'test(org_)'</automated>
    <fails_when>rpc-gen/rpc-sync-check exits non-zero or client drifts</fails_when>
  </verify>
  <done>Username member add/role/remove works; last Owner protected; listMine/get available</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (10-05-T2): org.updateSettings member_base_permission</name>
  <files>crates/octanest-api/src/org/mod.rs, crates/octanest-db/src/organizations.rs, crates/octanest-core/src/org_types.rs, crates/octanest-api/tests/org_create_members.rs</files>
  <read_first>crates/octanest-api/src/repo/acl.rs, crates/octanest-db/src/organizations.rs</read_first>
  <behavior>
    - Default member_base is none
    - Setting read/write changes Member private-repo capability via ACL
    - Owner/Admin unaffected (always admin)
    - Public repos remain readable regardless of base
  </behavior>
  <action>
Implement org.updateSettings { member_base_permission?, display_name? } for Admin+ per D-ORG-02b. Prove via integration tests that Member with base none cannot read private org repo; base read can; Collaborator path not required here. make rpc-gen if DTOs change.
  </action>
  <verify>
    <automated>cargo nextest run -p octanest-api -E 'test(org_) | test(coalesce) | test(repo_private)'</automated>
    <fails_when>cargo/nextest exits non-zero or expected tests fail</fails_when>
  </verify>
  <done>member_base_permission controls Member private access; Owner/Admin stay admin</done>
</task>

</tasks>

<verification>
ORG-01 username members + ORG-02 roles/base green.
</verification>

<success_criteria>
Org roles assignable; member_base drives ACL for Members; no Collaborator org role.
</success_criteria>

<output>
Create `.planning/phases/10-orgs-permissions/10-05-SUMMARY.md` when done
</output>
