---
phase: 10-orgs-permissions
plan: "10"
type: execute
wave: 8
depends_on:
  - "01"
  - "05"
  - "06"
  - "09"
files_modified:
  - apps/web/src/routes/$owner.tsrx
  - apps/web/src/routes/$owner.settings.tsrx
  - apps/web/src/routes/$owner.settings.members.tsrx
  - apps/web/src/routes/$owner.settings.members.integration.test.ts
  - apps/web/src/components/org/member-lookup.tsrx
  - apps/web/src/components/chrome.tsrx
requirements:
  - ORG-01
  - ORG-02
autonomous: true
user_setup: []
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Org overview at /{org} lists public repos + member count (D-ORG-06)"
    - "Members/invites UI + member_base settings work for Admin+ (ORG-01, ORG-02, D-ORG-02b, D-ORG-03)"
    - "Live username lookup autocomplete wired; no emails shown"
  artifacts:
    - path: apps/web/src/routes/$owner.settings.members.tsrx
      provides: "Members + invites UI"
    - path: apps/web/src/routes/$owner.tsrx
      provides: "Org overview"
  key_links:
    - from: apps/web/src/routes/$owner.settings.members.tsrx
      to: packages/api-client/src/index.ts
      via: "org.members.* / org.invites.* / user.lookup Query"
  assumptions:
    - "ASSUME: Route $owner distinguishes org vs user — org.get success shows overview; unknown user without repos may 404 carefully"
  prohibitions:
    - statement: "Must not mix return ( JSX with Rivet @{ in the same .tsrx module"
      flagged: true
    - statement: "Must not implement teams UI"
      flagged: true
---

<objective>
Ship org overview, settings (member_base), and members/invites UI per 10-UI-SPEC and D-ORG-06 (ORG-01, ORG-02).

Purpose: Humans can manage org membership without API tools.
Output: Green members integration tests; Octane build passes.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-orgs-permissions/10-UI-SPEC.md
@.planning/phases/10-orgs-permissions/10-CONTEXT.md
@apps/web/src/routes/admin/auth.tsrx
@apps/web/src/routes/settings/profile.tsrx
@apps/web/src/components/settings/settings-nav.tsrx
@.agents/skills/octane/SKILL.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → org Admin RPCs | Session cookie only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03 | Information disclosure | member-lookup | medium | mitigate | Render only username/display/avatar from user.lookup |
| T-10-09 | Elevation of privilege | members UI | high | mitigate | UI hides controls without Admin; server still enforces |
| T-10-SC | Tampering | packages | high | mitigate | No new npm packages / combobox libs — use existing Select/Input |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (10-10-T1): Org overview + settings member_base</name>
  <files>apps/web/src/routes/$owner.tsrx, apps/web/src/routes/$owner.settings.tsrx, apps/web/src/components/chrome.tsrx</files>
  <read_first>.planning/phases/10-orgs-permissions/10-UI-SPEC.md, apps/web/src/routes/$owner.$repo.tsrx, apps/web/src/routes/admin/auth.tsrx, .agents/skills/octane/SKILL.md</read_first>
  <action>
Add minimal /{org} overview (public repos + member count) and /{org}/settings for display_name + member_base_permission Select (None/Read/Write) per D-ORG-02b and D-ORG-06. Settings link only when caller is Org Admin+. Avoid colliding with /{owner}/{repo} routes — follow TanStack file-route precedence already used in app. Octane Rivet-only.
  </action>
  <verify>
    <automated>bun --cwd apps/web run build</automated>
  </verify>
  <done>Org overview and settings routes build and call org.get/updateSettings</done>
</task>

<task type="auto">
  <name>Task 2 (10-10-T2): Members + invites UI with live lookup</name>
  <files>apps/web/src/routes/$owner.settings.members.tsrx, apps/web/src/components/org/member-lookup.tsrx, apps/web/src/routes/$owner.settings.members.integration.test.ts</files>
  <read_first>.planning/phases/10-orgs-permissions/10-UI-SPEC.md, apps/web/src/routes/settings/tokens.tsrx, .agents/skills/octane/SKILL.md</read_first>
  <action>
Ship members table (role Select, remove), Add member via user.lookup autocomplete, email invites panel with revoke AlertDialog per D-ORG-03 and 10-UI-SPEC. Turn green members integration Wave 0 tests. Query for server lists; useState for forms; onInput for text.
  </action>
  <verify>
    <automated>bun --cwd apps/web exec vitest run src/routes/\$owner.settings.members.integration.test.ts && bun --cwd apps/web run build</automated>
  </verify>
  <done>Members/invites UI green; lookup wired without emails</done>
</task>

</tasks>

<verification>
ORG-01/02 org management UI complete per UI-SPEC.
</verification>

<success_criteria>
Admins manage members, invites, and member_base in the browser.
</success_criteria>

<output>
Create `.planning/phases/10-orgs-permissions/10-10-SUMMARY.md` when done
</output>
