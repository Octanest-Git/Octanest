---
phase: 15-releases-transfer
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/octanest-api/tests/release_rpc.rs
  - crates/octanest-api/tests/repo_rename_transfer.rs
  - crates/octanest-db/tests/dialect_releases.rs
  - apps/web/src/routes/$owner.$repo.releases.integration.test.ts
  - apps/web/src/routes/$owner.$repo.settings.rename-transfer.integration.test.ts
requirements:
  - GIT-14
  - GIT-15
  - GIT-16
  - GIT-17
autonomous: true
user_setup: []
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Wave 0 discoverable stubs exist for release_*, rename/transfer/redirect, dialect_releases, and web Releases/settings surfaces (GIT-14..17)"
    - "15-VALIDATION.md Wave 0 gap files are present on disk"
  artifacts:
    - path: crates/octanest-api/tests/release_rpc.rs
      provides: "GIT-14/15 release + asset failing stubs"
    - path: crates/octanest-api/tests/repo_rename_transfer.rs
      provides: "GIT-16/17 rename/transfer/redirect stubs"
    - path: crates/octanest-db/tests/dialect_releases.rs
      provides: "releases/redirects migration parity stub"
    - path: apps/web/src/routes/$owner.$repo.releases.integration.test.ts
      provides: "Releases tab discoverability stub"
    - path: apps/web/src/routes/$owner.$repo.settings.rename-transfer.integration.test.ts
      provides: "Danger-zone rename/transfer confirm stub"
  key_links:
    - from: .planning/phases/15-releases-transfer/15-VALIDATION.md
      to: crates/octanest-api/tests/release_rpc.rs
      via: "Wave 0 Requirements checklist"
  assumptions:
    - "ASSUME: Wave 0 stubs may #[ignore] or assert!(false) until later plans turn green"
    - "ASSUME: Migration number is next free 00NN after whatever exists at execute time (not hardcoded)"
  prohibitions:
    - statement: "Must not implement production release RPC, rename/transfer, or migrations in this Wave 0 plan"
      flagged: true
    - statement: "Must not add new crates.io or npm packages"
      flagged: true
---

<objective>
Scaffold Nyquist/Wave 0 failing stubs for Phase 15 releases, assets, rename/transfer redirects, dialect migrations, and web surfaces before implementation plans turn them green.

Purpose: Every later `<automated>` verify names a real test path (15-VALIDATION.md Wave 0). Covers GIT-14..17 stub discoverability.
Output: Discoverable RED/ignored nextest + Vitest filters matching `release`, `rename`/`transfer`/`redirect`, `dialect_releases`.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-releases-transfer/15-CONTEXT.md
@.planning/phases/15-releases-transfer/15-RESEARCH.md
@.planning/phases/15-releases-transfer/15-VALIDATION.md
@crates/octanest-api/tests/repo_create.rs
@crates/octanest-api/tests/repo_private_404.rs
@crates/octanest-db/tests/dialect_orgs.rs
@apps/web/src/routes/$owner.$repo.settings.collaborators.integration.test.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → temp DB / repos_dir / release-assets dir | Parallel nextest must isolate state |
| Stub assertions → production ACL | Soft not_found vs Write+/Admin encoded before impl |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Information disclosure | release/rename stubs | high | mitigate | Stub expects identical soft `repo.not_found` for missing vs forbidden Admin/Write denials (D-REL-07/09/12) |
| T-15-02 | Elevation of privilege | draft/asset stubs | high | mitigate | Stub expects drafts Write+-only; asset download AuthZ via parent repo ACL (D-REL-06) |
| T-15-SC | Tampering | cargo/npm installs | high | mitigate | Wave 0 adds test files only; no new packages |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (15-00-T1): Rust Wave 0 stubs for releases + rename/transfer + dialect</name>
  <files>crates/octanest-api/tests/release_rpc.rs, crates/octanest-api/tests/repo_rename_transfer.rs, crates/octanest-db/tests/dialect_releases.rs</files>
  <read_first>.planning/phases/15-releases-transfer/15-VALIDATION.md, .planning/phases/15-releases-transfer/15-RESEARCH.md, crates/octanest-api/tests/repo_create.rs, crates/octanest-db/tests/dialect_orgs.rs</read_first>
  <action>
Add Wave 0 integration stubs (Phase 10 style: compile, discoverable by nextest, intentionally fail or #[ignore] until later plans). release_rpc.rs must name tests matching filters test(release) covering: release.create for existing tag with notes (GIT-14, D-REL-01); release.tag_missing when tag absent (D-REL-01); draft + prerelease flags (D-REL-02); draft list/get hidden from Read/anon (D-REL-12 discretion); Write+ create/publish (D-REL-12); Admin delete (D-REL-03); asset upload/download ACL and size reject (GIT-15, D-REL-04..06). repo_rename_transfer.rs covering: Admin rename moves disk+DB and inserts redirect (GIT-16, D-REL-07, D-REL-08); non-admin rename soft not_found; Admin transfer to user/org with type-confirm (GIT-17, D-REL-09, D-REL-10); redirect_supersede when new repo occupies old path; cascade assertions that issues/LFS association rows stay on repo_id when those tables exist (D-REL-10). dialect_releases.rs expects tri-dialect next-free 00NN_releases_redirects with releases, release_assets, repository_redirects (mirror dialect_orgs). Do not implement handlers or migrations here.
  </action>
  <verify>
    <automated>test -f crates/octanest-api/tests/release_rpc.rs && test -f crates/octanest-api/tests/repo_rename_transfer.rs && test -f crates/octanest-db/tests/dialect_releases.rs && cargo nextest list -p octanest-api -E 'test(release) | test(rename) | test(transfer) | test(redirect)' | rg -q 'release|rename|transfer|redirect' && cargo nextest list -p octanest-db -E 'test(dialect_releases)' | rg -q 'dialect_releases'</automated>
  </verify>
  <done>Rust Wave 0 stubs exist; filters discover RED/ignored tests for release/rename/transfer/redirect/dialect_releases</done>
</task>

<task type="auto">
  <name>Task 2 (15-00-T2): Web Wave 0 stubs for Releases tab + settings danger zone</name>
  <files>apps/web/src/routes/$owner.$repo.releases.integration.test.ts, apps/web/src/routes/$owner.$repo.settings.rename-transfer.integration.test.ts</files>
  <read_first>apps/web/src/routes/$owner.$repo.settings.collaborators.integration.test.ts, apps/web/src/components/repo/repo-chrome.tsrx, .planning/phases/15-releases-transfer/15-CONTEXT.md</read_first>
  <action>
Add Vitest integration stub files (may fail/skip until UI lands) asserting: Releases tab present in repo chrome and routes under /{owner}/{repo}/releases discoverable (D-REL-13, GIT-14/15); settings Danger zone exposes rename + transfer with type-confirm transfer UX (GIT-16/17, D-REL-07, D-REL-09, D-REL-10). Mirror collaborators.integration.test.ts patterns. No production UI changes in this plan.
  </action>
  <verify>
    <automated>test -f apps/web/src/routes/\$owner.\$repo.releases.integration.test.ts && test -f apps/web/src/routes/\$owner.\$repo.settings.rename-transfer.integration.test.ts</automated>
  </verify>
  <done>Web Wave 0 stub files exist for Releases and rename/transfer settings</done>
</task>

</tasks>

<verification>
Wave 0 nextest/Vitest paths from 15-VALIDATION.md exist and nextest list finds release/rename/transfer/redirect/dialect_releases.
</verification>

<success_criteria>
All Wave 0 stub files exist and are listed by nextest where applicable; no production release/rename/transfer behavior shipped in this plan.
</success_criteria>

<output>
Create `.planning/phases/15-releases-transfer/15-00-SUMMARY.md` when done
</output>
