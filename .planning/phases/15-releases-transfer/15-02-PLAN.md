---
phase: 15-releases-transfer
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - crates/octanest-api/src/routes/release_assets.rs
  - crates/octanest-api/src/app.rs
  - crates/octanest-api/src/release/mod.rs
  - crates/octanest-api/src/rpc.rs
  - crates/octanest-db/src/releases.rs
  - crates/octanest-api/tests/release_rpc.rs
  - apps/web/src/routes/$owner.$repo.releases.$tag.tsrx
  - apps/web/src/routes/$owner.$repo.releases.new.tsrx
  - apps/web/vite.config.ts
  - docker-compose.yml
  - docs/CONFIGURATION.md
  - .env.example
requirements:
  - GIT-14
  - GIT-15
autonomous: true
user_setup: []
estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Write+ can upload release assets to OCTANEST_RELEASE_ASSETS_DIR keyed by asset_id, replace on edit, and enforce configurable max size (GIT-14, D-REL-04, D-REL-05)"
    - "Read (or anon on public published) can download assets from the web UI via stable /api/releases/assets/{asset_id} (GIT-15, D-REL-06)"
    - "Draft release assets require Write+; private repos never anonymous (D-REL-06)"
  artifacts:
    - path: crates/octanest-api/src/routes/release_assets.rs
      provides: "multipart upload + download handlers"
    - path: docker-compose.yml
      provides: "release-assets volume bind"
  key_links:
    - from: apps/web/src/routes/$owner.$repo.releases.$tag.tsrx
      to: GET /api/releases/assets/{asset_id}
      via: "download link with Content-Disposition filename"
    - from: crates/octanest-api/src/routes/release_assets.rs
      to: OCTANEST_RELEASE_ASSETS_DIR/{asset_id}
      via: "opaque file path distinct from LFS OID store (D-REL-04)"
  assumptions:
    - "ASSUME: Default max asset size 512 MiB via OCTANEST_RELEASE_ASSET_MAX_BYTES"
    - "ASSUME: Asset URLs are id-based so rename/transfer never rewrites paths"
  prohibitions:
    - statement: "Must not place release binaries in OCTANEST_LFS_DIR or bare-repo releases/ tree"
      flagged: true
    - statement: "Must not add new crates.io or npm packages"
      flagged: true
---

<objective>
Add downloadable release assets on a separate volume with multipart upload, size limits, replace-on-edit, ACL’d download, and Octane UI upload/download controls (GIT-14 assets + GIT-15).

Purpose: Complete the shipping half of releases after notes tracer; honor D-REL-04/05/06 storage and ACL locks.
Output: Green release_asset_* nextest; Vite proxy + Compose bind; detail UI upload/download.
</objective>

<execution_context>
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/workflows/execute-plan.md
@/home/jesse/wsl-projects/personal/typescript/octanest/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-releases-transfer/15-CONTEXT.md
@.planning/phases/15-releases-transfer/15-RESEARCH.md
@.planning/phases/14-git-lfs/14-CONTEXT.md
@crates/octanest-api/src/routes/avatar.rs
@crates/octanest-api/src/app.rs
@apps/web/vite.config.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Multipart upload body | Untrusted size/filename/content-type |
| Download by asset_id | Cross-tenant IDOR risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-06 | Denial of service | upload body limit | high | mitigate | DefaultBodyLimit::max(OCTANEST_RELEASE_ASSET_MAX_BYTES); reject oversize with structured error (D-REL-05) |
| T-15-07 | Tampering | asset filename | medium | mitigate | Store by opaque asset_id; sanitize Content-Disposition; ignore client paths |
| T-15-08 | Elevation of privilege | GET asset | high | mitigate | Always AuthZ via release→repo ACL; drafts Write+; public published anon only when repo public (D-REL-06) |
| T-15-SC | Tampering | packages | high | mitigate | No new packages; reuse Axum Multipart |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (15-02-T1): Asset upload/download HTTP + deleteAsset RPC</name>
  <files>crates/octanest-api/src/routes/release_assets.rs, crates/octanest-api/src/app.rs, crates/octanest-api/src/release/mod.rs, crates/octanest-api/src/rpc.rs, crates/octanest-db/src/releases.rs, crates/octanest-api/tests/release_rpc.rs</files>
  <read_first>crates/octanest-api/src/routes/avatar.rs, crates/octanest-api/src/app.rs, .planning/phases/15-releases-transfer/15-RESEARCH.md</read_first>
  <behavior>
    - POST multipart to /api/repos/{owner}/{repo}/releases/{id}/assets requires Write; stores under OCTANEST_RELEASE_ASSETS_DIR/{asset_id} (D-REL-04, D-REL-12)
    - Oversized body rejected (D-REL-05)
    - GET /api/releases/assets/{asset_id} allows Read; anon only for public+published; drafts Write+ (D-REL-06, GIT-15)
    - Replace-on-edit updates bytes + metadata for same or new asset row (D-REL-05)
    - release.deleteAsset Write+; release.delete removes DB rows and files
  </behavior>
  <reversibility rating="costly">Separate release-assets volume split from LFS (D-REL-04)</reversibility>
  <action>
Implement release_assets routes mirroring avatar multipart + DefaultBodyLimit pattern. Persist metadata in release_assets; files only under OCTANEST_RELEASE_ASSETS_DIR by asset_id per D-REL-04 (distinct from LFS OID store). Wire AppState release_assets_dir + max bytes (default 512 MiB). Add release.deleteAsset RPC; on release.delete remove asset files. Green Wave 0 release_asset_* tests. make rpc-gen for any new RPC.
  </action>
  <verify>
    <automated>make rpc-gen && make rpc-sync-check && cargo nextest run -p octanest-api -E 'test(release_asset)'</automated>
  </verify>
  <done>Upload/download/replace/size ACL green; assets never touch LFS dir</done>
</task>

<task type="auto">
  <name>Task 2 (15-02-T2): UI upload/download + Vite proxy + Compose volume + CONFIG docs</name>
  <files>apps/web/src/routes/$owner.$repo.releases.$tag.tsrx, apps/web/src/routes/$owner.$repo.releases.new.tsrx, apps/web/vite.config.ts, docker-compose.yml, docs/CONFIGURATION.md, .env.example</files>
  <read_first>apps/web/vite.config.ts, docker-compose.yml, docs/CONFIGURATION.md, apps/web/src/routes/$owner.$repo.releases.$tag.tsrx</read_first>
  <action>
Add Octane asset upload controls on create/detail and download links pointing at stable /api/releases/assets/{asset_id} (GIT-15, D-REL-06, D-REL-13). Proxy /api/releases in vite.config.ts like /api/repos. Bind ./var/release-assets in docker-compose for api service; document OCTANEST_RELEASE_ASSETS_DIR and OCTANEST_RELEASE_ASSET_MAX_BYTES in docs/CONFIGURATION.md and .env.example (no secrets).
  </action>
  <verify>
    <automated>rg -n 'release-assets|OCTANEST_RELEASE_ASSETS|/api/releases' docker-compose.yml docs/CONFIGURATION.md apps/web/vite.config.ts && bun --cwd apps/web run build</automated>
  </verify>
  <done>UI can upload/download assets; Compose+Vite+docs cover the volume and routes</done>
</task>

</tasks>

<verification>
release_asset nextest green; rpc-sync-check; web build; CONFIG/compose/vite mention release assets.
</verification>

<success_criteria>
Users can attach and download release assets from the web UI with correct ACL and size limits on a dedicated volume (GIT-14/15).
</success_criteria>

<output>
Create `.planning/phases/15-releases-transfer/15-02-SUMMARY.md` when done
</output>
